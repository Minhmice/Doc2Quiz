---
phase: 03-question-review
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified:
  - src/components/review/QuestionCard.tsx
  - src/components/review/QuestionEditor.tsx
  - src/components/review/ReviewList.tsx
autonomous: true
requirements:
  - REVIEW-01
  - REVIEW-02
  - REVIEW-03
user_setup: []

must_haves:
  truths:
    - "At most one card shows the editor at a time"
    - "Cancel restores last committed snapshot for that question"
  artifacts:
    - "Three files under src/components/review/"
  key_links:
    - "Behaviors match 03-UI-SPEC collapsed/expanded and radio correct answer"
---

<objective>
Implement **presentational / stateful** review list pieces: **QuestionCard** (collapsed + expand), **QuestionEditor** (fields + Save/Cancel), **ReviewList** (map + enforce single `editingId`) — per `03-CONTEXT.md` D-03–D-05, D-17 and `03-UI-SPEC.md`.

Purpose: Isolate list/edit UX before wiring persistence and page layout in plan 03.
Output: `src/components/review/QuestionCard.tsx`, `QuestionEditor.tsx`, `ReviewList.tsx`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-question-review/03-CONTEXT.md
@.planning/phases/03-question-review/03-UI-SPEC.md
@src/types/question.ts
@src/components/ai/QuestionPreviewList.tsx
</context>

<interfaces>
```typescript
// ReviewList props (representative — adjust names if cleaner)
type ReviewListProps = {
  questions: Question[];
  editingId: string | null;
  onEditRequest: (id: string) => void; // parent may cancel other editor first
  onCancelEdit: () => void;
  onSaveEdit: (id: string, next: Question) => void;
  onDelete: (id: string) => void;
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: QuestionEditor</name>
  <files>src/components/review/QuestionEditor.tsx</files>
  <read_first>src/types/question.ts</read_first>
  <read_first>src/components/ai/AiParseSection.tsx</read_first>
  <action>
Create **`QuestionEditor`** as a client component. Props: **`question: Question`**, **`onSave: (q: Question) => void`**, **`onCancel: () => void`**. Internal state: copy of `question` fields for controlled inputs. **Save** calls `onSave` with updated `Question` (same `id`). **Cancel** calls `onCancel` without calling `onSave`. Use **radio inputs** named by question id for correct answer; `correctIndex` typed 0|1|2|3. Labels **A–D** for options. Use native `<input>`, `<textarea>` or `<input>` for stem — match Tailwind styling used in `AiParseSection` (neutral borders, rounded-lg).
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - File begins with `"use client"`
    - `src/components/review/QuestionEditor.tsx` exports `QuestionEditor` (named export)
    - File contains substring `correctIndex` and `type="radio"`
  </acceptance_criteria>
  <done>Editor component complete</done>
</task>

<task type="auto">
  <name>Task 2: QuestionCard</name>
  <files>src/components/review/QuestionCard.tsx</files>
  <read_first>src/components/review/QuestionEditor.tsx</read_first>
  <read_first>.planning/phases/03-question-review/03-UI-SPEC.md</read_first>
  <action>
Create **`QuestionCard`**. Props include: **`question`**, **`index`** (1-based for **Q{n}** label), **`isEditing: boolean`**, **`onToggleEdit`**, **`onSave`**, **`onCancel`**, **`onDelete`**. Collapsed: show stem, A–D lines, button **Edit**, and **Remove** (or **Delete**) that calls `onDelete`. If `isEditing`, render **`QuestionEditor`** instead of collapsed body (or below header). Card wrapper: bordered container consistent with UI-SPEC. **Required visible label:** render the literal pattern **`Q` + index** for the collapsed header, e.g. JSX `{`Q${index}`}` so the file contains grep-verifiable substring **`Q${index}`**.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/components/review/QuestionCard.tsx` exports `QuestionCard`
    - File imports `QuestionEditor` from `./QuestionEditor`
    - `grep -F 'Q${index}' src/components/review/QuestionCard.tsx` exits 0 (label pattern present)
  </acceptance_criteria>
  <done>Card component complete</done>
</task>

<task type="auto">
  <name>Task 3: ReviewList</name>
  <files>src/components/review/ReviewList.tsx</files>
  <read_first>src/components/review/QuestionCard.tsx</read_first>
  <action>
Create **`ReviewList`**: map `questions` to **`QuestionCard`**; pass **`isEditing={question.id === editingId}`**; **`onToggleEdit(id)`** calls parent **`onEditRequest(id)`** (parent owns `editingId`). Wire **`onSave`** to parent **`onSaveEdit`**, **`onCancel`** to **`onCancelEdit`**, **`onDelete`** to **`onDelete`**. Do **not** implement `editingId` state inside this file — receive from props.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/components/review/ReviewList.tsx` exports `ReviewList`
    - File maps over `questions` and renders `QuestionCard`
  </acceptance_criteria>
  <done>List component complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM | User edits question text; no HTML injection from stored strings if rendered as text nodes |

## STRIDE

| ID | Cat | Component | Disp | Mitigation |
|----|-----|-----------|------|------------|
| T-03b-1 | X | option text display | mitigate | React text escaping; no `dangerouslySetInnerHTML` |
</threat_model>

<verification>
- [ ] `npx tsc --noEmit`
- [ ] `npm run lint`
</verification>

<success_criteria>
- Single-editor semantics and Save/Cancel behavior match CONTEXT D-03–D-04
</success_criteria>

<output>
After completion, create `.planning/phases/03-question-review/03-02-SUMMARY.md`
</output>
