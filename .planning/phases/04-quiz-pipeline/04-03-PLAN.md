---
phase: 04-quiz-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - src/lib/client/studySetDb.ts
  - src/lib/client/studySetDb.test.ts
  - src/lib/client/activityTracking.ts
  - src/lib/client/activityTracking.test.ts
autonomous: true
requirements:
  - QUIZ-06
  - CORE-PRAC-02
user_setup:
  - service: supabase
    why: "Client CRUD for approved_questions and quiz_sessions requires authenticated Supabase"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase project settings"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase project settings"
must_haves:
  truths:
    - "getApprovedBank returns questions from approved_questions via Supabase RLS (not empty stub)"
    - "putApprovedBankForStudySet upserts edits and deletes removed rows (QUIZ-06)"
    - "recordQuizCompletion inserts quiz_sessions row with correct_count and total_questions (CORE-PRAC-02)"
    - "getLatestQuizSession returns most recent session for done page score display"
  artifacts:
    - path: src/lib/client/studySetDb.ts
      provides: "Supabase-backed approved bank CRUD"
      exports: ["getApprovedBank", "putApprovedBankForStudySet"]
    - path: src/lib/client/activityTracking.ts
      provides: "Quiz session persistence + latest session query"
      exports: ["recordQuizCompletion", "getLatestQuizSession"]
  key_links:
    - from: src/lib/client/studySetDb.ts
      to: approved_questions
      via: "select/upsert/delete with user_id filter"
      pattern: "approved_questions"
    - from: src/lib/client/activityTracking.ts
      to: quiz_sessions
      via: "insert on completion; select order completed_at desc"
      pattern: "quiz_sessions"
---

<objective>
Replace stubbed client data layer with Supabase-backed `approved_questions` CRUD and `quiz_sessions` persistence.

Purpose: Fulfill QUIZ-06 review/edit/delete and CORE-PRAC-02 session score — existing UI (ReviewSection, QuizSession, done page) already calls these modules but receives empty no-ops today (D-11).
Output: Implemented `studySetDb.ts` bank functions and `activityTracking.ts` session helpers with unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-quiz-pipeline/04-CONTEXT.md
@.planning/phases/04-quiz-pipeline/04-RESEARCH.md
@.planning/phases/04-quiz-pipeline/04-PATTERNS.md
@src/lib/client/studySetDb.ts
@src/lib/client/activityTracking.ts
@src/types/question.ts
@supabase/migrations/20260725120000_v21_baseline.sql

<interfaces>
Current stubs (MUST replace):
- getApprovedBank → returns emptyApprovedBank()
- putApprovedBankForStudySet → no-op
- recordQuizCompletion → empty function body

Port pattern from git HEAD src/lib/db/studySetDb.ts (lines ~760–832):
- rowToQuestion / questionToRow mappers
- getApprovedBank: select id,prompt,choices,correct_index,explanation,source,updated_at order by updated_at asc
- putApprovedBankForStudySet: upsert + delete orphans

Port pattern from git HEAD src/lib/sets/activityTracking.ts:
- recordQuizCompletion: insert quiz_sessions + upsert study_wrong_history for wrongQuestionIds
- Export QuizSessionRecord type already in activityTracking.ts

Add getLatestQuizSession(studySetId) per RESEARCH Pattern 7:
- select id,study_set_id,completed_at,total_questions,correct_count order completed_at desc limit 1

Question type (src/types/question.ts):
- id, question, options[4], correctIndex, explanation?, sourceChunkId?

Consumers (no changes in this plan — wire in 04-04):
- ReviewSection, QuizSession, useDashboardHome, quiz/[id]/done/page.tsx
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement getApprovedBank + putApprovedBankForStudySet (D-11, QUIZ-06)</name>
  <files>src/lib/client/studySetDb.ts, src/lib/client/studySetDb.test.ts</files>
  <behavior>
    - getApprovedBank returns null when no rows (not empty stub object with savedAt "")
    - rowToQuestion maps prompt→question, choices→options, correct_index→correctIndex, source.concept_id→sourceChunkId
    - putApprovedBankForStudySet upserts all questions in bank and deletes IDs removed from bank
    - putApprovedBankForStudySet with empty questions array deletes all rows for study set
    - All queries scoped by user_id from auth.getUser()
  </behavior>
  <action>
    Replace stub implementations in src/lib/client/studySetDb.ts (lines 155–164):

    Add private helpers questionToRow(q: Question) and rowToQuestion(row) — port from git HEAD src/lib/db/studySetDb.ts. Include explanation column on read/write (baseline schema has explanation text).

    getApprovedBank(studySetId):
    - createSupabaseBrowserClient + requireUserId()
    - Select from approved_questions with user_id + study_set_id filters
    - Return { version: 1, savedAt: ISO now, questions } or null if zero rows

    putApprovedBankForStudySet(studySetId, bank):
    - List existing IDs for study set
    - Upsert each question with id, user_id, study_set_id, mapped columns, updated_at from bank.savedAt
    - Delete IDs in existing but not in bank (orphan cleanup pattern from git HEAD)

    Do NOT use IndexedDB. Do NOT import from @/lib/db/studySetDb — port logic into client module.

    Tests: mock Supabase client chains; verify mapper round-trip and delete-orphan behavior.
  </action>
  <verify>
    <automated>npx vitest run src/lib/client/studySetDb.test.ts --reporter=dot</automated>
  </verify>
  <done>Approved bank CRUD reads/writes Supabase; stubs removed; QUIZ-06 data layer ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement recordQuizCompletion + getLatestQuizSession (CORE-PRAC-02)</name>
  <files>src/lib/client/activityTracking.ts, src/lib/client/activityTracking.test.ts</files>
  <behavior>
    - recordQuizCompletion inserts quiz_sessions with id (createRandomUuid), user_id, study_set_id, completed_at, total_questions, correct_count
    - recordQuizCompletion upserts study_wrong_history when wrongQuestionIds non-empty (port git HEAD)
    - getLatestQuizSession returns null when no sessions; otherwise maps to QuizSessionRecord
    - getMistakeQuestionIds returns IDs from study_wrong_history (restore from git HEAD — needed by dashboard drill links, not Phase 5 UI)
  </behavior>
  <action>
    Replace stub recordQuizCompletion in activityTracking.ts:

    Import createSupabaseBrowserClient, createRandomUuid. On completion:
    1. requireUserId via supabase.auth.getUser()
    2. INSERT quiz_sessions row
    3. If wrongQuestionIds.length > 0: upsert study_wrong_history merging question IDs (git HEAD pattern)

    Add getLatestQuizSession(studySetId: string): Promise&lt;{ correct: number; total: number; completedAt: string } | null&gt;
    - Query quiz_sessions for study_set_id, order completed_at desc, limit 1
    - Map correct_count/total_questions for done page display

    Restore getMistakeQuestionIds and hasMistakesForStudySet from git HEAD (replace stubs) — minimal port, no new API routes.

    getActivityStats may remain stub returning zeros unless git HEAD port is trivial; CORE-PRAC-02 does not require dashboard stats.

    Tests: mock Supabase insert/select; verify session row shape and latest query ordering.
  </action>
  <verify>
    <automated>npx vitest run src/lib/client/activityTracking.test.ts --reporter=dot</automated>
  </verify>
  <done>Quiz session persistence and latest-session query implemented; CORE-PRAC-02 backend ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→Supabase | Client uses anon key + user session; RLS enforces user_id = auth.uid() |
| approved_questions CRUD | User can only mutate own rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Spoofing | studySetDb CRUD | mitigate | requireUserId on every call; explicit eq user_id; RLS policies |
| T-04-10 | Tampering | putApprovedBank orphan delete | mitigate | Delete only IDs belonging to same user_id + study_set_id |
| T-04-11 | Information disclosure | quiz_sessions | mitigate | RLS select_own policy; no cross-user session reads |
</threat_model>

<verification>
- npx vitest run src/lib/client/studySetDb.test.ts src/lib/client/activityTracking.test.ts
- npm run typecheck
- grep confirms getApprovedBank no longer returns emptyApprovedBank stub unconditionally
</verification>

<success_criteria>
- studySetDb approved bank functions use Supabase (D-11)
- activityTracking persists quiz_sessions (CORE-PRAC-02)
- Review and practice UI can load real data once 04-04 wires surfaces
</success_criteria>

<output>
Create `.planning/phases/04-quiz-pipeline/04-03-SUMMARY.md` when done
</output>
