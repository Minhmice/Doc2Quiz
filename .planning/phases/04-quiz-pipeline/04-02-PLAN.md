---
phase: 04-quiz-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
  - 03-02
files_modified:
  - src/lib/pipeline/dedupeAndCapQuestions.ts
  - src/lib/pipeline/dedupeAndCapQuestions.test.ts
  - src/lib/pipeline/mapQuizOutputToRows.ts
  - src/lib/pipeline/quizGenerate.ts
  - src/lib/pipeline/quizGenerate.test.ts
  - src/app/api/study-sets/[id]/quiz/generate/route.ts
  - src/app/api/study-sets/[id]/quiz/generate/route.test.ts
autonomous: true
requirements:
  - QUIZ-01
  - QUIZ-02
  - QUIZ-03
  - QUIZ-04
  - QUIZ-05
user_setup:
  - service: ai-provider
    why: "Quiz generation requires OpenAI-compatible chat API (D-09)"
    env_vars:
      - name: AI_PROVIDER_URL
        source: ".env — OpenAI-compatible base URL"
      - name: AI_PROVIDER_KEY
        source: ".env — API key for upstream provider"
must_haves:
  truths:
    - "POST /api/study-sets/[id]/quiz/generate replaces 501 stub and returns recommendedCount, generatedCount, questionIds (D-12)"
    - "Generator reads canonical_markdown + canonical_sections + metadata.extracted_questions only — never raw_markdown (D-03)"
    - "Questions bulk-insert to approved_questions BEFORE JSON response (D-08, QUIZ-05)"
    - "Duplicate concepts deduped and count capped when content thin (D-07, QUIZ-04)"
    - "Success sets pipeline_stage=quiz and content_kind=quiz (D-12)"
  artifacts:
    - path: src/lib/pipeline/quizGenerate.ts
      provides: "runQuizGenerate orchestration"
      exports: ["runQuizGenerate", "QuizGenerateError", "QuizGenerateValidationError"]
    - path: src/app/api/study-sets/[id]/quiz/generate/route.ts
      provides: "POST quiz generate endpoint"
      exports: ["POST"]
    - path: src/lib/pipeline/dedupeAndCapQuestions.ts
      provides: "QUIZ-04 post-processing"
      exports: ["dedupeAndCapQuestions"]
  key_links:
    - from: src/lib/pipeline/quizGenerate.ts
      to: src/lib/server/openAiChatCompletion.ts
      via: "postChatCompletionAssistantText responseFormatJsonObject"
      pattern: "postChatCompletionAssistantText"
    - from: src/lib/pipeline/quizGenerate.ts
      to: approved_questions
      via: "DELETE existing rows then bulk INSERT before return"
      pattern: "approved_questions"
    - from: src/app/api/study-sets/[id]/quiz/generate/route.ts
      to: src/lib/pipeline/quizGenerate.ts
      via: "runQuizGenerate({ supabase, userId, studySetId, questionCountOverride })"
      pattern: "runQuizGenerate"
---

<objective>
Implement server-side quiz generation: `runQuizGenerate()` orchestration, QUIZ-04 dedupe/cap post-processing, bulk persist to `approved_questions`, and replace the 501 API stub.

Purpose: Fulfill QUIZ-01–05 and D-03–D-12 — MCQs from canonical knowledge only, immediate Supabase save, structured API response.
Output: `quizGenerate.ts`, `dedupeAndCapQuestions.ts`, `mapQuizOutputToRows.ts`, POST route, unit/integration tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-quiz-pipeline/04-CONTEXT.md
@.planning/phases/04-quiz-pipeline/04-RESEARCH.md
@.planning/phases/04-quiz-pipeline/04-PATTERNS.md
@docs/pipeline.md
@src/lib/pipeline/ingest.ts
@src/app/api/study-sets/[id]/quiz/generate/route.ts
@src/app/api/study-sets/[id]/ingest/route.ts
@src/app/api/study-sets/[id]/ingest/route.test.ts
@supabase/migrations/20260725120000_v21_baseline.sql

# Prior wave output
@.planning/phases/04-quiz-pipeline/04-01-SUMMARY.md

<interfaces>
From 04-01:
- loadQuizPrompt(), buildQuizGeneratorMessages(), QUIZ_PROMPT_VERSION from quizPrompt.ts
- quizGeneratorOutputSchema, quizGenerateBodySchema from quizSchemas.ts
- postChatCompletionAssistantText, getAiProcessingConfig, resolveUserAiTier

From quiz/generate/route.ts stub (keep verifyStudySet):
- requireApiUser() auth pattern
- verifyStudySet(supabase, userId, studySetId) ownership guard

Pipeline stage order (src/types/studySet.ts):
input → raw → canonical → mode_selected → quiz → flashcards

D-12 success response:
{ recommendedCount, generatedCount, questionIds[] }

approved_questions row mapping (D-06, RESEARCH Pattern 3):
| LLM | DB column |
| prompt | prompt |
| choices[4] | choices |
| correct_index | correct_index |
| explanation | explanation |
| concept_id | tags[0] + source.concept_id |
| section_key, source_excerpt | source jsonb |

Re-generate semantics (RESEARCH recommendation): DELETE all approved_questions for study_set_id before bulk INSERT (replace-all).

stripJsonFence + one repair retry on Zod failure (mirror canonicalize.ts / extractCanonicalSourceUnits.ts).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dedupeAndCapQuestions + mapQuizOutputToRows + tests</name>
  <files>src/lib/pipeline/dedupeAndCapQuestions.ts, src/lib/pipeline/dedupeAndCapQuestions.test.ts, src/lib/pipeline/mapQuizOutputToRows.ts, src/lib/pipeline/mapQuizOutputToRows.test.ts</files>
  <behavior>
    - dedupeAndCapQuestions removes duplicate concept_id entries (QUIZ-04)
    - When uniqueConcepts.length &lt; 3, caps recommended count to uniqueConcepts.length
    - questionCountOverride from POST body caps final output when provided (D-10)
    - Returns { questions, recommendedCount, generatedCount, warnings }
    - mapQuizOutputToRows assigns server UUIDs via createRandomUuid, maps to approved_questions insert shape with source.prompt_version
  </behavior>
  <action>
    Implement dedupeAndCapQuestions.ts per RESEARCH Pattern 4 and D-07:
    - Dedupe concepts by concept_id (case-insensitive)
    - Pick one question per concept (first in LLM output)
    - Cap: min(recommended_count, uniqueConcepts.length, questions.length, overrideCount ?? Infinity, 40)
  - Append warning "Limited testable content" when generated &lt; recommended

    Implement mapQuizOutputToRows.ts:
    - mapGeneratedQuestionToRow(q, { id, userId, studySetId, promptVersion })
    - Include tags: concept_id ? [concept_id] : []
    - source jsonb: { concept_id, section_key, source_excerpt, prompt_version, generated_at }

    Unit tests for dedupe edge cases: duplicate concepts, thin content, override count.
    Add mapQuizOutputToRows.test.ts: assert choices[4], correct_index, tags, source jsonb (concept_id, section_key, prompt_version). No LLM/DB in this task.
  </action>
  <verify>
    <automated>npx vitest run src/lib/pipeline/dedupeAndCapQuestions.test.ts src/lib/pipeline/mapQuizOutputToRows.test.ts --reporter=dot</automated>
  </verify>
  <done>QUIZ-04 post-processing and row mapping helpers pass unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: runQuizGenerate service — LLM, validate, persist (D-03, D-08)</name>
  <files>src/lib/pipeline/quizGenerate.ts, src/lib/pipeline/quizGenerate.test.ts</files>
  <behavior>
    - Throws QuizGenerateValidationError when pipeline_stage &lt; canonical or canonical_markdown empty (D-10)
    - Returns error when isAiProcessingConfigured() is false (503 path)
    - Loads canonical_documents + canonical_sections + metadata.extracted_questions — NEVER raw_markdown (D-03)
    - Single LLM call with locked prompt; Zod validate; one repair retry (D-05 discretion: single call)
    - Truncates canonical_markdown at 80_000 chars with warning when over budget
    - DELETE approved_questions for study_set then bulk INSERT before returning (D-08, replace-all)
    - UPDATE study_sets SET pipeline_stage='quiz', content_kind='quiz' on success (D-12)
    - Returns { ok: true, recommendedCount, generatedCount, questionIds } — no question bodies in response
  </behavior>
  <action>
    Implement quizGenerate.ts following ingest.ts / canonicalize.ts error-class pattern:

    Export QuizGenerateValidationError, QuizGenerateError, runQuizGenerate({ supabase, userId, studySetId, questionCountOverride? }).

    Pre-flight:
    1. study_sets.pipeline_stage must be ≥ canonical (isStageAtLeast helper)
    2. canonical_documents.canonical_markdown non-empty trim
    3. Load canonical_sections ordered by ordinal; metadata.extracted_questions from doc.metadata

    Builder flow:
    1. loadQuizPrompt + buildQuizGeneratorMessages with canonical inputs only
    2. getAiProcessingConfig(resolveUserAiTier(...))
    3. postChatCompletionAssistantText temperature 0, responseFormatJsonObject true
    4. stripJsonFence + JSON.parse + quizGeneratorOutputSchema.safeParse + repair retry
    5. dedupeAndCapQuestions(output, questionCountOverride)
    6. mapQuizOutputToRows → server-side supabase.from('approved_questions').delete().eq(...) then .insert(rows)
    7. UPDATE study_sets pipeline_stage + content_kind

    On LLM/Zod failure: throw QuizGenerateError — do not partial-insert questions.

    Tests: mock postChatCompletionAssistantText and Supabase chains (mirror ingest.test.ts). Cover validation, dedupe integration, persist-before-return ordering.
  </action>
  <verify>
    <automated>npx vitest run src/lib/pipeline/quizGenerate.test.ts --reporter=dot</automated>
  </verify>
  <done>runQuizGenerate passes tests; QUIZ-01–05 logic implemented with persist-before-response (D-08).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace 501 POST /quiz/generate route + tests (D-P1-17)</name>
  <files>src/app/api/study-sets/[id]/quiz/generate/route.ts, src/app/api/study-sets/[id]/quiz/generate/route.test.ts</files>
  <behavior>
    - 401 when requireApiUser returns error
    - 404 when study set not found
    - 400 when runQuizGenerate throws QuizGenerateValidationError (stage &lt; canonical)
    - 422 when runQuizGenerate throws QuizGenerateError
    - 503 when AI not configured (isAiProcessingConfigured false or QuizGenerateError with status 503)
    - 400 when questionCount body invalid (quizGenerateBodySchema)
    - 200 with { recommendedCount, generatedCount, questionIds } on success (D-12)
  </behavior>
  <action>
    Replace 501 stub per D-P1-17:

    POST handler:
    - Keep verifyStudySet helper unchanged
    - Parse optional body with quizGenerateBodySchema (D-10)
    - Delegate to runQuizGenerate({ supabase, userId, studySetId, questionCountOverride: body.questionCount })
    - Error mapping mirror ingest/route.ts: ValidationError → 400, QuizGenerateError (AI not configured) → 503, other QuizGenerateError/ZodError → 422, unexpected → 500
    - Success 200: { recommendedCount, generatedCount, questionIds }
    - export const runtime = "nodejs"; export const maxDuration = 120;

    Route tests: vi.mock runQuizGenerate and requireApiUser (mirror ingest/route.test.ts). Include 503 case when AI env missing. No real LLM.
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/study-sets/[id]/quiz/generate/route.test.ts" --reporter=dot</automated>
  </verify>
  <done>POST /quiz/generate operational; 501 stub removed; route tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→POST /quiz/generate | Authenticated user triggers LLM on their canonical knowledge |
| server→LLM upstream | Untrusted model output validated before DB write |
| server→approved_questions | Bulk insert scoped by user_id + study_set_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | canonical_markdown in LLM prompt | mitigate | Locked constraints D-03; output Zod only; no tool execution |
| T-04-05 | Spoofing | Cross-user study set access | mitigate | requireApiUser + verifyStudySet + RLS user_id filter |
| T-04-06 | Denial of service | Oversized canonical input / JSON | mitigate | 80k char truncation; max 40 questions; max_tokens on chat call |
| T-04-07 | Information disclosure | AI_PROVIDER_KEY | mitigate | Server-only getAiProcessingConfig; never in response body |
| T-04-08 | Tampering | questionCount body | mitigate | quizGenerateBodySchema bounds 1–40 at route entry |
</threat_model>

<verification>
- npx vitest run src/lib/pipeline/dedupeAndCapQuestions.test.ts src/lib/pipeline/quizGenerate.test.ts "src/app/api/study-sets/[id]/quiz/generate/route.test.ts"
- npm run typecheck
- grep confirms runQuizGenerate wired in route (no 501 not_implemented)
</verification>

<success_criteria>
- QUIZ-01–05 fulfilled via runQuizGenerate + route
- Questions persist before API response (D-08)
- pipeline_stage advances to quiz on success (D-12)
- Canonical-only input enforced (D-03)
</success_criteria>

<output>
Create `.planning/phases/04-quiz-pipeline/04-02-SUMMARY.md` when done
</output>
