---
phase: 04-quiz-pipeline
plan: 01
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - prompt/quiz_generator_v1.json
  - src/lib/pipeline/quizPrompt.ts
  - src/lib/pipeline/quizSchemas.ts
  - src/lib/pipeline/quizPrompt.test.ts
  - src/lib/pipeline/quizSchemas.test.ts
  - src/lib/server/openAiChatCompletion.ts
  - src/lib/server/ai-processing-config.ts
  - src/lib/server/resolveUserAiTier.ts
autonomous: true
requirements:
  - QUIZ-01
  - QUIZ-02
  - QUIZ-03
user_setup: []
must_haves:
  truths:
    - "Locked quiz_generator_v1.json exists with system, tasks, constraints, output_schema per D-04"
    - "Zod schema validates concepts array, recommended_count, and MCQ shape (4 choices, correct_index 0–3) per D-06"
    - "Prompt text lives only in JSON — quizPrompt.ts loads and assembles messages without hardcoded LLM instructions"
    - "Server AI trio exists for downstream runQuizGenerate (D-09)"
  artifacts:
    - path: prompt/quiz_generator_v1.json
      provides: "Locked quiz generation contract"
      contains: "quiz_generator"
    - path: src/lib/pipeline/quizPrompt.ts
      provides: "Runtime prompt load + message assembly"
      exports: ["loadQuizPrompt", "buildQuizGeneratorMessages", "QUIZ_PROMPT_VERSION"]
    - path: src/lib/pipeline/quizSchemas.ts
      provides: "Zod mirror of output_schema + route body"
      exports: ["quizGeneratorOutputSchema", "quizGenerateBodySchema"]
  key_links:
    - from: src/lib/pipeline/quizPrompt.ts
      to: prompt/quiz_generator_v1.json
      via: "readFile at runtime (mirror canonicalPrompt D-04)"
      pattern: "quiz_generator_v1\\.json"
    - from: src/lib/pipeline/quizSchemas.ts
      to: prompt/quiz_generator_v1.json
      via: "hand-written Zod matching output_schema fields"
      pattern: "recommended_count"
---

<objective>
Establish Phase 4 foundation: locked quiz prompt JSON, runtime loader, Zod validation contracts, and verified server AI infrastructure.

Purpose: Wave 2 `runQuizGenerate` depends on prompt contract (D-04), output validation (D-06), and server AI helpers (D-09) before any LLM calls or DB inserts.
Output: `prompt/quiz_generator_v1.json`, `quizPrompt.ts`, `quizSchemas.ts`, unit tests; AI trio verified or restored from git HEAD.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-quiz-pipeline/04-CONTEXT.md
@.planning/phases/04-quiz-pipeline/04-RESEARCH.md
@.planning/phases/04-quiz-pipeline/04-PATTERNS.md
@prompt/canonical_builder_v1.json
@src/lib/pipeline/ingestSchemas.ts
@.planning/phases/03-canonical-knowledge/03-01-PLAN.md

<interfaces>
From 04-CONTEXT locked decisions:
- D-04: prompt/quiz_generator_v1.json mirrors canonical_builder_v1.json structure
- D-06: per-question output { prompt, choices[4], correct_index 0–3, explanation?, concept_id?, section_key?, source_excerpt? }
- D-10: optional POST body { questionCount?: number } bounded 1–40

From prompt/canonical_builder_v1.json (pattern only):
- Fields: name, version, system, input (template vars), tasks[], output_schema, constraints[]

From 03-01 (upstream — must exist before Wave 2):
- postChatCompletionAssistantText from openAiChatCompletion.ts
- getAiProcessingConfig, isAiProcessingConfigured from ai-processing-config.ts
- resolveUserAiTier from resolveUserAiTier.ts

Proposed output_schema (RESEARCH — implement exactly in JSON + Zod):
- recommended_count: number (1–40)
- concepts[]: { concept_id: concept_NNN, label, section_key?, importance? }
- questions[]: MCQ fields per D-06
- warnings[]: string[]

Input template vars (D-03): study_set_id, title, language, canonical_markdown, sections_json, extracted_questions_json, requested_count
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify or restore server AI infrastructure (D-09)</name>
  <files>src/lib/server/openAiChatCompletion.ts, src/lib/server/ai-processing-config.ts, src/lib/server/resolveUserAiTier.ts</files>
  <action>
    Phase 4 shares server AI with Phase 3 (03-01). If any of the three files are missing on disk, restore from git HEAD — same scope as 03-01 Task 1: postChatCompletionAssistantText with response_format json_object, getAiProcessingConfig reading AI_PROVIDER_URL/KEY/AI_MODEL_FREE/AI_MODEL_PRO, resolveUserAiTier.

    If all three files already exist and export the expected symbols, skip restore — only verify imports resolve.

    Do NOT restore generateFromFile/*, /api/ai/forward, or v1 quizGeneration/* pipelines. Phase 4 uses new src/lib/pipeline/quizGenerate.ts only.
  </action>
  <verify>
    <automated>if (-not (Test-Path src/lib/server/openAiChatCompletion.ts)) { exit 1 }; if (-not (Test-Path src/lib/server/ai-processing-config.ts)) { exit 1 }; if (-not (Test-Path src/lib/server/resolveUserAiTier.ts)) { exit 1 }</automated>
  </verify>
  <done>Server AI trio present on disk with postChatCompletionAssistantText + getAiProcessingConfig exports.</done>
</task>

<task type="auto">
  <name>Task 2: Create locked prompt/quiz_generator_v1.json (D-04)</name>
  <files>prompt/quiz_generator_v1.json</files>
  <action>
    Create prompt/quiz_generator_v1.json per D-04 and RESEARCH proposed schema:

    - name: "quiz_generator", version: "1.0"
    - system: canonical knowledge only — never raw_markdown or external facts
    - input template vars: {{study_set_id}}, {{title}}, {{language}}, {{canonical_markdown}}, {{sections_json}}, {{extracted_questions_json}}, {{requested_count}}
    - tasks[]: detect testable concepts (concept_001…), recommend count (QUIZ-02), generate one MCQ per concept with 4 choices + explanation (QUIZ-03), use extracted_questions as hints, fewer questions when content thin
    - output_schema: recommended_count, concepts[], questions[], warnings[]
    - constraints[]: canonical only, exactly 4 choices, correct_index 0–3, no duplicate concept_id, no invented facts, valid JSON only

    Wording is planner/executor discretion per CONTEXT — structure and fields are locked. Do not duplicate this JSON inside TypeScript.
  </action>
  <verify>
    <automated>grep -q "quiz_generator" prompt/quiz_generator_v1.json &amp;&amp; grep -q "canonical_markdown" prompt/quiz_generator_v1.json &amp;&amp; grep -q "recommended_count" prompt/quiz_generator_v1.json</automated>
  </verify>
  <done>quiz_generator_v1.json exists with full contract matching D-04/D-06 field names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: quizPrompt.ts + quizSchemas.ts + unit tests</name>
  <files>src/lib/pipeline/quizPrompt.ts, src/lib/pipeline/quizSchemas.ts, src/lib/pipeline/quizPrompt.test.ts, src/lib/pipeline/quizSchemas.test.ts</files>
  <behavior>
    - loadQuizPrompt returns version "1.0" from prompt/quiz_generator_v1.json (D-04)
    - buildQuizGeneratorMessages assembles system from spec.system + numbered tasks + bullet constraints + serialized output_schema — never hardcode prompt prose in TS
    - substituteQuizInput replaces {{study_set_id}}, {{canonical_markdown}}, etc. in input block
    - quizGenerateBodySchema accepts { questionCount: 12 } and rejects 0, 41, non-int (D-10)
    - quizGeneratorOutputSchema accepts valid fixture with concepts[] and questions[] (QUIZ-01)
    - quizGeneratorOutputSchema validates recommended_count int 1–40 (QUIZ-02)
    - quizGeneratorOutputSchema rejects choices.length !== 4 and correct_index outside 0–3 (QUIZ-03)
    - concept_id regex accepts concept_001 pattern
  </behavior>
  <action>
    Implement quizPrompt.ts mirroring 03-01 canonicalPrompt pattern:
    - loadQuizPrompt(): read prompt/quiz_generator_v1.json via node:fs/promises + path.join(process.cwd(), ...), module-level cache
    - substituteQuizInput(template, vars): replace /\{\{(\w+)\}\}/g
    - buildQuizGeneratorMessages(spec, vars): return ChatMessage[] or { system, user } where user payload is JSON with substituted canonical inputs only (D-03 — never include raw_markdown)
    - export QUIZ_PROMPT_VERSION from loaded spec.version

    Implement quizSchemas.ts:
    - quizGenerateBodySchema: questionCount optional z.number().int().min(1).max(40)
    - quizGeneratorOutputSchema: mirror locked output_schema exactly (D-06)
    - export types QuizGeneratorOutput, GeneratedQuestion, QuizGenerateBody

    Write tests first (RED), then implement (GREEN). No DB or LLM calls in this task.
  </action>
  <verify>
    <automated>npx vitest run src/lib/pipeline/quizPrompt.test.ts src/lib/pipeline/quizSchemas.test.ts --reporter=dot</automated>
  </verify>
  <done>Prompt loader and Zod modules pass unit tests; QUIZ-01–03 validation gates exist before Wave 2 generation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→filesystem | Prompt JSON read server-side only; must not ship in client bundle |
| env→server | AI_PROVIDER_KEY stays server-only via ai-processing-config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information disclosure | ai-processing-config.ts | mitigate | Keys read from process.env only; never export to client |
| T-04-02 | Tampering | quizSchemas.ts | mitigate | Zod rejects malformed LLM shapes before DB insert (QUIZ-03) |
| T-04-03 | Tampering | npm/pip/cargo installs | accept | No new packages; existing zod only |
</threat_model>

<verification>
- npx vitest run src/lib/pipeline/quizPrompt.test.ts src/lib/pipeline/quizSchemas.test.ts
- npm run typecheck
- prompt/quiz_generator_v1.json contains locked contract fields
</verification>

<success_criteria>
- quiz_generator_v1.json locked contract on disk (D-04)
- Zod schemas validate QUIZ-01–03 output shapes (D-06)
- Server AI infrastructure verified for D-09
- No prompt text duplicated in TypeScript source
</success_criteria>

<output>
Create `.planning/phases/04-quiz-pipeline/04-01-SUMMARY.md` when done
</output>
