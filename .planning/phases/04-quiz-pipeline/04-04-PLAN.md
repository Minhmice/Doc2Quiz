---
phase: 04-quiz-pipeline
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - src/lib/client/quizGenerateStudySet.ts
  - src/lib/client/quizGenerateStudySet.test.ts
  - src/components/canonical/CanonicalModeSelectionFooter.tsx
  - src/components/quiz/QuizGenerateProgressCard.tsx
  - src/app/(app)/sets/[id]/source/page.tsx
  - src/components/review/ReviewSection.tsx
  - src/components/review/QuestionReviewNavigator.tsx
  - src/app/(app)/quiz/[id]/done/page.tsx
  - src/lib/ui/studySetActionLabels.ts
  - src/components/dashboard/DashboardLibraryClient.tsx
autonomous: false
requirements:
  - MODE-01
  - QUIZ-06
  - QUIZ-07
  - CORE-DASH-01
  - CORE-DASH-02
  - CORE-PRAC-01
  - CORE-PRAC-02
user_setup: []
must_haves:
  truths:
    - "User on canonical preview can click Quiz CTA → mode_selected → generation progress → review page (MODE-01, D-01, D-02)"
    - "Flashcards CTA disabled with 'coming soon' copy (D-01)"
    - "Review page loads Supabase questions; edit/delete persist; Start quiz navigates to practice (D-13, QUIZ-06, QUIZ-07)"
    - "QuizSession loads approved bank; keyboard 1–4 works (D-14, CORE-PRAC-01)"
    - "Done page shows score from latest quiz_sessions row (D-15, CORE-PRAC-02)"
    - "Dashboard ready cards show Start quiz when pipeline_stage=quiz and approved &gt; 0 (D-16, CORE-DASH-02)"
  artifacts:
    - path: src/app/(app)/sets/[id]/source/page.tsx
      provides: "Mode selection + inline generation state machine"
    - path: src/lib/client/quizGenerateStudySet.ts
      provides: "POST /quiz/generate client helper"
      exports: ["postQuizGenerate"]
    - path: src/components/canonical/CanonicalModeSelectionFooter.tsx
      provides: "Quiz + Flashcards CTAs per UI-SPEC"
    - path: src/components/quiz/QuizGenerateProgressCard.tsx
      provides: "Generation progress with recommended/generated counts"
  key_links:
    - from: src/app/(app)/sets/[id]/source/page.tsx
      to: /api/study-sets/[id]/quiz/generate
      via: "postQuizGenerate after PATCH mode_selected"
      pattern: "quiz/generate"
    - from: src/components/review/ReviewSection.tsx
      to: src/lib/client/studySetDb.ts
      via: "getApprovedBank on mount; putApprovedBankForStudySet on edit"
      pattern: "getApprovedBank"
    - from: src/app/(app)/quiz/[id]/done/page.tsx
      to: src/lib/client/activityTracking.ts
      via: "getLatestQuizSession on load"
      pattern: "getLatestQuizSession"
---

<objective>
Wire end-to-end quiz UX: mode selection and generation on canonical preview, review→practice flow, done page score, and dashboard CTAs per UI-SPEC.

Purpose: Fulfill MODE-01, QUIZ-06–07, CORE-DASH-01/02, CORE-PRAC-01/02 — connect existing UI shells to real data and generation API from Waves 1–2.
Output: Source page state machine, generation components, review finish CTAs, done page score, dashboard label updates.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-quiz-pipeline/04-UI-SPEC.md
@.planning/phases/04-quiz-pipeline/04-CONTEXT.md
@.planning/phases/04-quiz-pipeline/04-PATTERNS.md
@src/app/(app)/sets/[id]/source/page.tsx
@src/components/review/ReviewSection.tsx
@src/components/quiz/QuizSession.tsx
@src/components/dashboard/DashboardLibraryClient.tsx
@src/lib/client/ingestStudySet.ts
@src/hooks/useDashboardHome.ts

# Prior wave output
@.planning/phases/04-quiz-pipeline/04-02-SUMMARY.md
@.planning/phases/04-quiz-pipeline/04-03-SUMMARY.md

<interfaces>
From POST /quiz/generate (04-02):
Success: { recommendedCount, generatedCount, questionIds[] }
Errors: { error, message } with 400/422/500/503

From 04-03 client layer:
- getApprovedBank, putApprovedBankForStudySet
- recordQuizCompletion (called by QuizSession — verify still wired, no change unless import broken)
- getLatestQuizSession(studySetId) → { correct, total, completedAt } | null

UI-SPEC state machine on /sets/[id]/source:
preview → Quiz click → mode_selecting → generating → success redirect /edit/quiz/[id]
When pipeline_stage === 'quiz' && approvedCount > 0 → resume strip (Edit questions / Start quiz)

Copywriting (04-UI-SPEC): Learning mode, Quiz, Flashcards disabled, generation counts, Start quiz (not Done/Open practice)

dashboardCardPrimaryCtaLabel: return "Start quiz" for ready + contentKind === 'quiz'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client helper + generation/mode components (UI-SPEC)</name>
  <files>src/lib/client/quizGenerateStudySet.ts, src/components/canonical/CanonicalModeSelectionFooter.tsx, src/components/quiz/QuizGenerateProgressCard.tsx</files>
  <action>
    Implement quizGenerateStudySet.ts mirroring ingestStudySet.ts / canonicalizeStudySet.ts:
    - postQuizGenerate(studySetId, body?: { questionCount?: number }): POST /api/study-sets/${id}/quiz/generate
    - Throw Error with API message on failure; map TypeError to "Connection lost. Check your network and try again."
    - Return typed { recommendedCount, generatedCount, questionIds }

    CanonicalModeSelectionFooter per 04-UI-SPEC:
    - Props: onSelectQuiz, onSelectFlashcards?, disabled, busy, pipelineStage, approvedCount?
    - Quiz Button variant default; Flashcards outline disabled aria-disabled title "Flashcards are coming soon."
    - Copy: Learning mode label, helper text, optional quiz sublabel
    - When pipeline_stage quiz + approvedCount > 0: resume strip with Edit questions + Start quiz links (pass hrefs as props)

    QuizGenerateProgressCard per UI-SPEC:
    - Adapt IngestProgressCard visual shell (border-l-[6px], d2q-progress-stripes)
    - States: generating, success (brief), error with Try again
    - Display Recommended: {n} · Generated: {n}; thin-content muted note when generated &lt; recommended
    - aria-live="polite", aria-busy during generate
    - Visual steps: Detecting concepts → Building questions → Saving to library (indeterminate)

    No question-count override UI in Phase 4 (UI-SPEC item 3).
  </action>
  <verify>
    <automated>npx vitest run src/lib/client/quizGenerateStudySet.test.ts --reporter=dot &amp;&amp; test -f src/components/canonical/CanonicalModeSelectionFooter.tsx &amp;&amp; test -f src/components/quiz/QuizGenerateProgressCard.tsx</automated>
  </verify>
  <done>Client POST helper (with vitest smoke test mocking fetch) and UI-SPEC generation/mode components exist.</done>
</task>

<task type="auto">
  <name>Task 2: Source page state machine + review finish CTAs (MODE-01, D-01–02, D-13)</name>
  <files>src/app/(app)/sets/[id]/source/page.tsx, src/components/review/ReviewSection.tsx, src/components/review/QuestionReviewNavigator.tsx</files>
  <action>
    Extend /sets/[id]/source/page.tsx (integrate with Phase 3 canonical preview if present; replace placeholder footer):

    State machine additions per UI-SPEC:
    - preview (canonical ready): show CanonicalModeSelectionFooter
    - On Quiz click (D-01, D-02): putStudySetMeta({ ...meta, contentKind: 'quiz', pipelineStage: 'mode_selected' }); transition to generating
    - generating: hide markdown/TOC; show QuizGenerateProgressCard; POST postQuizGenerate (use generatingRef to prevent double POST)
    - success: router.push(/edit/quiz/${id}) — optional 1.5s success message with counts
    - error: toast + error state + Try again
    - pipeline_stage quiz + questions: resume strip — on mount when pipelineStage === 'quiz', call getApprovedBank(studySetId) and pass bank?.questions.length ?? 0 to CanonicalModeSelectionFooter

    Flashcards: disabled button only — no route (Phase 5 stub per D-01).

    ReviewSection updates per D-13, UI-SPEC:
    - Verify getApprovedBank/putApprovedBankForStudySet work (04-03) — no architectural change if already calling them
    - Empty state: link to /sets/{id}/source with copy from UI-SPEC
    - **handleDone (QUIZ-07):** after successful saveApprovedBank, router.push(`/quiz/${studySetId}`) — NOT /dashboard
    - Remove redundant touchStudySetMeta pipelineStage bump on finish save — generation already sets pipeline_stage=quiz (D-12)
    - QuestionReviewNavigator: rename finish button Done → Start quiz; enabled when allMcqsComplete; onClick delegates to handleDone (which routes to practice)
    - Add secondary link Back to library → /dashboard
    - Toast on delete: "Question removed"

    CORE-PRAC-01: QuizSession keyboard 1–4 already implemented — verify no regression; data now loads from 04-03 bank.
  </action>
  <verify>
    <automated>grep -q "postQuizGenerate" src/app/(app)/sets/[id]/source/page.tsx &amp;&amp; grep -q "getApprovedBank" src/app/(app)/sets/[id]/source/page.tsx &amp;&amp; grep -q "/quiz/\${studySetId}" src/components/review/ReviewSection.tsx &amp;&amp; grep -q "Start quiz" src/components/review/QuestionReviewNavigator.tsx</automated>
  </verify>
  <done>Mode selection and generation flow wired; review finish CTAs updated per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: Done page score + dashboard CTAs + human verify checkpoint</name>
  <files>src/app/(app)/quiz/[id]/done/page.tsx, src/lib/ui/studySetActionLabels.ts, src/components/dashboard/DashboardLibraryClient.tsx</files>
  <action>
    Done page (D-15, CORE-PRAC-02):
    - Import getLatestQuizSession from activityTracking
    - On load after meta: fetch latest session; set latestScore { correct, total } when present
    - Display: "{correct} / {total} correct ({pct}%)" per UI-SPEC; omit line when null
    - Subcopy: "Results saved to your account."
    - CTAs: Start quiz → /quiz/{id}, Review → /edit/quiz/{id}, Library → /dashboard
    - Migrate buttons to shadcn Button oxblood primary when touching file

    studySetActionLabels.ts (D-16, CORE-DASH-02):
    - dashboardCardPrimaryCtaLabel: return "Start quiz" (not "Open practice") when variant ready + contentKind === 'quiz'
    - pipelineStageLabel quiz entry already "Quiz ready" — verify unchanged

    DashboardLibraryClient:
    - cardVariantFor: needs_edit when approved === 0 && stage &lt; quiz; ready when approved &gt; 0 && pipeline_stage === 'quiz' (verify existing logic works with real counts)
    - Sets at canonical/mode_selected without questions: overflow Continue setup → /sets/{id}/source (optional polish per UI-SPEC)

    QuizSession: confirm recordQuizCompletion still fires on session end before redirect to done page — fix import path if needed (@/lib/client/activityTracking).
  </action>
  <verify>
    <automated>grep -q "getLatestQuizSession" src/app/(app)/quiz/[id]/done/page.tsx &amp;&amp; grep -q "Start quiz" src/lib/ui/studySetActionLabels.ts</automated>
  </verify>
  <done>Done page shows session score; dashboard shows Start quiz CTA for ready quiz sets.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>End-to-end quiz pipeline UX: canonical preview → Quiz mode → generation → review edit → practice → done score → dashboard re-entry</what-built>
  <how-to-verify>
    1. Sign in; open a study set with pipeline_stage=canonical (Phase 3 complete).
    2. Visit /sets/{id}/source — confirm Learning mode footer with Quiz + disabled Flashcards.
    3. Click Quiz — confirm generation progress card with recommended/generated counts.
    4. On success, land on /edit/quiz/{id} — questions visible; edit one field — refresh — edit persists.
    5. Click Start quiz — /quiz/{id} loads; press 1–4 to answer; complete session.
    6. Done page shows score line (e.g. "3 / 5 correct (60%)").
    7. Dashboard card shows Start quiz CTA for the set.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API | Same-origin fetch with session cookies for quiz generate |
| markdown render | User's own canonical content on preview (Phase 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Spoofing | postQuizGenerate | mitigate | Server requireApiUser on route; client only triggers for owned sets |
| T-04-13 | Tampering | Review auto-save | mitigate | RLS on approved_questions; user_id on upsert |
</threat_model>

<verification>
- npm run typecheck
- npm run build
- Human checkpoint: full express path canonical → quiz → practice → done
</verification>

<success_criteria>
- MODE-01: user chooses Quiz on canonical preview
- QUIZ-06–07: review edit/delete + start practice
- CORE-DASH-01/02: dashboard lists sets with correct CTAs
- CORE-PRAC-01/02: keyboard practice + done score summary
</success_criteria>

<output>
Create `.planning/phases/04-quiz-pipeline/04-04-SUMMARY.md` when done
</output>
