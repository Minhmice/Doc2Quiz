---
phase: 08-flashcards-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/flashcardSession.ts
  - src/components/flashcards/FlashcardSession.tsx
  - src/app/(app)/sets/[id]/flashcards/page.tsx
  - src/components/layout/StepProgressBar.tsx
  - src/app/(app)/sets/[id]/play/page.tsx
  - src/components/layout/CommandPalette.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Flashcards route loads approved MCQs and derives front/back from Question per 08-CONTEXT D-01"
    - "Space flips card; ArrowLeft/ArrowRight change index with no wrap; session region receives initial focus per D-07"
    - "StepProgressBar highlights step 3 Quiz when path is /flashcards"
  artifacts:
    - "src/app/(app)/sets/[id]/flashcards/page.tsx exists and renders FlashcardSession"
    - "src/components/flashcards/FlashcardSession.tsx exists"
    - "src/types/flashcardSession.ts exists"
  key_links:
    - "Aligns with 08-CONTEXT D-01–D-10 and 08-UI-SPEC (typography Label for hint/loading; h1 size roles; no recordQuizCompletion)"
---

<objective>
Ship **Phase 08 flashcards v1**: dedicated **`/sets/[id]/flashcards`** page, **`FlashcardSession`** (derive faces from **`Question`** via **`getApprovedBank`** + **`isMcqComplete`**), keyboard UX and a11y per **08-CONTEXT** / **08-UI-SPEC**, **`StepProgressBar`** pathname tweak, **Take quiz ↔ Flashcards** cross-links, command palette entry.

Output: listed `files_modified` only; no new IndexedDB stores; no **`recordQuizCompletion`** from flashcards.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-flashcards-mode/08-CONTEXT.md
@.planning/phases/08-flashcards-mode/08-UI-SPEC.md
@src/components/play/PlaySession.tsx
@src/app/(app)/sets/[id]/play/page.tsx
</context>

<interfaces>
```typescript
// src/types/flashcardSession.ts
export type FlashcardSessionState = {
  questions: import("@/types/question").Question[];
  index: number;
  flipped: boolean;
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Flashcard session types</name>
  <files>src/types/flashcardSession.ts</files>
  <read_first>src/types/question.ts</read_first>
  <read_first>.planning/phases/08-flashcards-mode/08-CONTEXT.md</read_first>
  <action>
Create **`src/types/flashcardSession.ts`** exporting a **`FlashcardSessionState`** type (or equivalent name) with:
- **`questions: Question[]`** — ordered list from bank after **`isMcqComplete`** filter
- **`index: number`** — current card, **0 .. length-1**
- **`flipped: boolean`** — false = front (stem), true = back (correct option)

Import **`Question`** from **`@/types/question`**. Add one-line JSDoc: snapshot is **read-only** from approved bank at session start.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - File path is exactly `src/types/flashcardSession.ts`
    - File contains the substring `flipped: boolean`
    - File exports a type whose name includes `FlashcardSession`
  </acceptance_criteria>
  <done>Types added</done>
</task>

<task type="auto">
  <name>Task 2: StepProgressBar treats flashcards as Quiz step</name>
  <files>src/components/layout/StepProgressBar.tsx</files>
  <read_first>src/components/layout/StepProgressBar.tsx</read_first>
  <read_first>.planning/phases/08-flashcards-mode/08-UI-SPEC.md</read_first>
  <action>
In **`currentStepFromPathname`**, when **`pathname.includes("/flashcards")`**, return **`"play"`** (same as **`/play`** / **`/practice`**) so step **3. Quiz** stays highlighted per **08-UI-SPEC** § Step progress bar.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `StepProgressBar.tsx` contains substring `/flashcards`
    - `StepProgressBar.tsx` still contains substring `includes("/play")` OR contains substring `` `/play` `` in a pathname check (preserve existing play detection)
  </acceptance_criteria>
  <done>Stepper updated</done>
</task>

<task type="auto">
  <name>Task 3: FlashcardSession component</name>
  <files>src/components/flashcards/FlashcardSession.tsx</files>
  <read_first>src/components/play/PlaySession.tsx</read_first>
  <read_first>.planning/phases/08-flashcards-mode/08-UI-SPEC.md</read_first>
  <read_first>src/lib/review/validateMcq.ts</read_first>
  <action>
Add **`src/components/flashcards/FlashcardSession.tsx`** with **`export function FlashcardSession({ studySetId }: { studySetId: string })`**:

**Data:** **`useCallback`/`useEffect`** pattern like **`PlaySession.reload`**: **`ensureStudySetDb()`**, **`getApprovedBank(studySetId)`**, filter with **`isMcqComplete`**. State: **`questions`**, **`index`**, **`flipped`**, **`loading`**, **`loadError`**. On index change, set **`flipped`** to **`false`** (D-08).

**UI (08-UI-SPEC):**
- **Session root:** **`tabIndex={0}`**, **`role="region"`**, **`aria-label="Flashcard study"`**, **`aria-busy={loading}`**, **`aria-live="polite"`** (or inner element for flip announcements — avoid duplicate noisy updates).
- **Focus:** After successful load and **`questions.length > 0`**, **`useEffect`** with **`requestAnimationFrame(() => sessionRootRef.current?.focus())`** once per successful load (guard with ref flag so it does not steal focus every render).
- **Keyboard on root:** **`onKeyDown`**: **`preventDefault`** for **Space** when handling flip; **`ArrowLeft`** → **`setIndex(i => Math.max(0, i-1))`** then reset flipped; **`ArrowRight`** → **`Math.min(length-1, i+1)`**. Do not handle keys when **`loading`** or **`loadError`** or **`questions.length===0`**.
- **Card:** shadcn **`Card`** + **`shadow-lg`**, **`CardContent`** with **`min-h-[12rem]`** on **`sm:`** per spec. **Body** text = base size (**`text-base`**, 16px). Optional **`CardFooter`** with **`Button variant="outline"`** labels exactly **Previous card** / **Next card** mirroring arrow clamp.
- **Front:** stem **`question`**, optional image if **`questionImageId`** — duplicate the **`MediaImage`** pattern from **`PlaySession`** (local component or inline hook: **`getMediaBlob`**, object URL, revoke on cleanup) with **max-h-48**, rounded border, **`alt=""`**.
- **Back:** **`options[correctIndex]`** text; optional **`optionImageIds?.[correctIndex]`** image same pattern.
- **Flip:** toggle **`flipped`** on Space; visual **instant swap** or **opacity transition ≤200ms** (executor picks one).
- **Progress:** single **Label** line **`text-xs font-semibold tabular-nums text-muted-foreground`**: **`Card {index+1} of {total}`** (omit when total 0).
- **Empty bank:** match **`PlaySession`** orange **`Alert`** pattern (title/body about approve on Review + **`Link`** to **`/sets/${studySetId}/review`**). Use same copy as UI-SPEC table or play string **No approved questions for a quiz yet.** for title parity.
- **Load error:** **`Alert variant="destructive"`** with title **Error** and body including **Could not load flashcards.**
- **Loading:** **`text-xs font-semibold text-muted-foreground`** (Label per **08-UI-SPEC**), **`role="status"`**, text **Loading…**

**Explicit:** Do **not** import or call **`recordQuizCompletion`**.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `FlashcardSession.tsx` exports `FlashcardSession`
    - File contains `getApprovedBank` and `isMcqComplete`
    - File contains `aria-label="Flashcard study"` or `aria-label='Flashcard study'`
    - File does **not** contain substring `recordQuizCompletion`
  </acceptance_criteria>
  <done>Flashcard session UI</done>
</task>

<task type="auto">
  <name>Task 4: Flashcards route page</name>
  <files>src/app/(app)/sets/[id]/flashcards/page.tsx</files>
  <read_first>src/app/(app)/sets/[id]/play/page.tsx</read_first>
  <read_first>.planning/phases/08-flashcards-mode/08-UI-SPEC.md</read_first>
  <action>
Create **`src/app/(app)/sets/[id]/flashcards/page.tsx`** mirroring **`play/page.tsx`**. Do **not** import or call **`recordQuizCompletion`** (**08-CONTEXT D-09**).
- **Client inner** component: **`useParams`**, load meta via **`getStudySetMeta`** / **`ensureStudySetDb`** like play (same error handling pattern).
- **`h1`:** **Flashcards · {headline}** — typography per **08-UI-SPEC**: below **`sm`** use **Heading** (e.g. **`text-xl font-semibold tracking-tight`**); from **`sm:`** use **Display** (e.g. **`sm:text-3xl`** for ~30px close to 32px contract). Use **`font-[family-name:var(--font-display)]`** on the title. Match play’s **`text-[var(--d2q-text)]`** if play uses it.
- **Subtitle / source:** only if play page shows them for the same meta (parity).
- **Hint line:** exact copy from **08-UI-SPEC** Copywriting table default keyboard hint; classes **`text-xs font-semibold text-muted-foreground`** (Label).
- **Cross-links row:** **`Link`** **Take quiz** → **`/sets/${id}/play`**; **`Link`** **Review questions** → **`/sets/${id}/review`** — use **`text-[var(--d2q-accent-hover)]`** or **`text-primary`** consistent with play links.
- **`Suspense`** wrapper + fallback: **`text-xs font-semibold text-muted-foreground`**, **Loading…**, **`role="status"`** (Label per **08-UI-SPEC**).
- Render **`<FlashcardSession studySetId={id} />`** below header block.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - File exists at `src/app/(app)/sets/[id]/flashcards/page.tsx`
    - File imports `FlashcardSession` from `@/components/flashcards/FlashcardSession`
    - File contains substring `Flashcards ·`
    - File contains substring `Take quiz`
    - File does **not** contain substring `recordQuizCompletion`
  </acceptance_criteria>
  <done>Route page added</done>
</task>

<task type="auto">
  <name>Task 5: Play page link to Flashcards</name>
  <files>src/app/(app)/sets/[id]/play/page.tsx</files>
  <read_first>src/app/(app)/sets/[id]/play/page.tsx</read_first>
  <read_first>.planning/phases/08-flashcards-mode/08-CONTEXT.md</read_first>
  <action>
In **`StudySetPlayPageInner`**, add a **Next.js `Link`** (muted, **Label** size **`text-xs font-semibold`**) **Flashcards** or **Study with flashcards** → **`/sets/${id}/flashcards`**, placed in the header area (e.g. below the existing muted **`p`** hint or on the same row) so **D-04** “clear entry from play” is satisfied. Do **not** use **`text-sm`** for this link (UI-SPEC four-size contract on flashcards page does not apply to play, but prefer **`text-xs`** for consistency with flashcards hint tier).
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `play/page.tsx` contains `/flashcards` in a href or template literal
    - Link visible copy includes `Flashcard` (case-insensitive grep)
  </acceptance_criteria>
  <done>Play entry link</done>
</task>

<task type="auto">
  <name>Task 6: Command palette — Flashcards</name>
  <files>src/components/layout/CommandPalette.tsx</files>
  <read_first>src/components/layout/CommandPalette.tsx</read_first>
  <action>
Inside **`studySetId`** **CommandGroup** (“Current set”), add **`CommandItem`** navigating to **`/sets/${studySetId}/flashcards`** with label **Flashcards** (or **Study flashcards**) and reuse **`BookOpenIcon`** or **`Layers`** / similar lucide icon already imported or add one import.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `CommandPalette.tsx` contains `/flashcards`
  </acceptance_criteria>
  <done>Command palette updated</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Client-only | Same as play: question text/images from local IndexedDB only |

## STRIDE

| ID | Cat | Component | Disp | Mitigation |
|----|-----|-----------|------|------------|
| T-08a-1 | Tampering | N/A local | — | No new persistence writes |
| T-08a-2 | XSS | Stem/option strings in DOM | Low | React text escaping; images from blob URLs same as PlaySession |

</threat_model>

<verification>
- [ ] `npx tsc --noEmit`
- [ ] `npm run build`
- [ ] `npm run lint`
</verification>

<success_criteria>
- `/sets/[id]/flashcards` works for a set with approved MCQs: flip and navigate keys per CONTEXT
- Empty and error states match spec intent
</success_criteria>

<output>
After completion, create `.planning/phases/08-flashcards-mode/08-01-SUMMARY.md`
</output>
