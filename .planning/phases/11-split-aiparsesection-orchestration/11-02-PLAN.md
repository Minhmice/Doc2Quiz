---
phase: 11-split-aiparsesection-orchestration
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - src/components/ai/AiParseParseStrategyPanel.tsx
  - src/components/ai/AiParsePreferenceToggles.tsx
  - src/components/ai/AiParseActions.tsx
  - src/components/ai/AiParseSectionHeader.tsx
  - src/components/ai/AiParseSection.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "User-visible strings unchanged where Phase 07 UI-SPEC/copy applied: heading **Parse with AI**; cancel button **Cancel parsing (stop AI processing)**; parse strategy radio copy blocks for Fast / Hybrid / Accurate (grep anchors in acceptance)."
    - "`forwardRef` + `useImperativeHandle` API on `AiParseSection` unchanged: `runParse` and `cancel` still exposed the same way to parents."
    - "`variant` prop `full` vs `embedded` preserves conditional blocks: embedded path keeps summary + parse time + vision fallback footnote behavior from current file."
  artifacts:
    - path: src/components/ai/AiParseParseStrategyPanel.tsx
      provides: Parse strategy radiogroup JSX + `parseStrategy` / `onChange` props
    - path: src/components/ai/AiParsePreferenceToggles.tsx
      provides: Attach page image + Run OCR checkboxes (or two small presentational subcomponents)
    - path: src/components/ai/AiParseActions.tsx
      provides: Parse + Cancel buttons row for non-embedded
    - path: src/components/ai/AiParseSectionHeader.tsx
      provides: H2 + API key badge / settings link row
    - path: src/components/ai/AiParseSection.tsx
      provides: Orchestration wiring only; composes presenters
  key_links:
    - from: AiParseSection.tsx
      to: AiParseParseStrategyPanel.tsx
      via: "props: parseStrategy, parseStrategyGroupId, setParseStrategyPreference"
---

<objective>
**Wave 2 — presenter split:** Extract **static / low-logic JSX** from `AiParseSection.tsx` into focused presenter components under `src/components/ai/`. `AiParseSection` keeps all `useCallback` / `useEffect` orchestration and parse pipeline; presenters receive props only (no `fetch`, no IDB, no `runVisionSequential`).

**Purpose:** Reduce `AiParseSection.tsx` below ~1700 lines toward a thin shell + hook-ready structure for a follow-up optional `useAiParseOrchestration` (out of scope unless time permits — do **not** block 11-02 on new hooks).

**Output:** New presenter files; `AiParseSection.tsx` imports them and passes existing state/handlers.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| XSS via new `dangerouslySetInnerHTML` | High | Forbidden — use existing React text nodes / components only. |
| Breaking a11y (labels / radiogroup) | Medium | Preserve `htmlFor`, `id`, `role="radiogroup"`, `aria-labelledby` wiring; pass `useId()` values from parent as today. |

**Blockers:** None.
</threat_model>

## Task 1 — `AiParseSectionHeader`

<action>
Add `src/components/ai/AiParseSectionHeader.tsx` exporting a component that renders the **non-embedded** header block currently in `AiParseSection.tsx`: `h2` with `id="ai-parse-heading"` and text **Parse with AI**; the flex row with API key `Badge` or Settings `Link` (`href="/settings"`). Accept props: `hasKey: boolean` (and any other primitive props strictly required to match current markup).

Replace the inline JSX in `AiParseSection.tsx` with `<AiParseSectionHeader ... />`.
</action>

<read_first>
- `src/components/ai/AiParseSection.tsx` (search `ai-parse-heading`, `Parse with AI`, `API key set`)
</read_first>

<acceptance_criteria>
- `AiParseSectionHeader.tsx` contains the literal string `Parse with AI`.
- `AiParseSectionHeader.tsx` contains `id="ai-parse-heading"` on the `h2` (or equivalent single heading element with that id).
- `rg "Parse with AI" src/components/ai/AiParseSection.tsx` shows **at most one** occurrence (either removed from section or only in comment — prefer **zero** in `AiParseSection.tsx` body).
</acceptance_criteria>

## Task 2 — `AiParsePreferenceToggles`

<action>
Add `src/components/ai/AiParsePreferenceToggles.tsx` (or split into two files if clearer) containing the JSX currently built into `attachPageImageControl` and `ocrExtractionControl` variables (~lines 1713–1763 region): checkboxes, `Label`s, helper text — **identical copy** as today. Props: `attachCheckboxId`, `ocrCheckboxId`, `attachPageImage`, `enableOcr`, `onAttachChange`, `onOcrChange` (typed from React event handlers).

Remove the large inline `const attachPageImageControl = (` blocks from `AiParseSection.tsx` and render `<AiParsePreferenceToggles ... />` in the same conditional positions.
</action>

<read_first>
- `src/components/ai/AiParseSection.tsx` (`attachPageImageControl`, `ocrExtractionControl`)
</read_first>

<acceptance_criteria>
- `AiParsePreferenceToggles.tsx` contains literal substring **Attach page image to parsed questions**.
- `AiParsePreferenceToggles.tsx` contains literal substring **Run OCR before vision parse**.
- `npm run lint` exits 0.
</acceptance_criteria>

## Task 3 — `AiParseParseStrategyPanel`

<action>
Add `src/components/ai/AiParseParseStrategyPanel.tsx` with the parse strategy card JSX from variable `parseStrategyControl` (radiogroup, three labels, Fast/Hybrid/Accurate descriptions). Props: `parseStrategy`, `parseStrategyGroupId`, `onSelectStrategy: (s: ParseStrategy) => void` (internally calls existing `setParseStrategyPreference` from parent via callback).

Strings to preserve verbatim include **≥85%** in Hybrid description and the Accurate line **Full-page vision parse (same as before).**
</action>

<read_first>
- `src/components/ai/AiParseSection.tsx` (`parseStrategyControl`, `Parse strategy`)
- `src/lib/ai/parseLocalStorage.ts` (type `ParseStrategy` if defined there)
</read_first>

<acceptance_criteria>
- `AiParseParseStrategyPanel.tsx` contains `role="radiogroup"`.
- File contains `≥85%` as literal text.
- `rg "Parse strategy" src/components/ai/AiParseSection.tsx` returns **no matches** in JSX (moved to panel).
</acceptance_criteria>

## Task 4 — `AiParseActions` + footer cleanup

<action>
Add `src/components/ai/AiParseActions.tsx` for the **non-embedded** bottom action row: **Parse** button, conditional **Cancel parsing (stop AI processing)** destructive button. Props: `isRunning`, `unifiedParseDisabled`, `onParse`, `onCancel`.

Replace duplicate embedded vs full cancel/parse patterns only where it reduces duplication **without** changing embedded-only flows; if embedded has separate buttons, leave those in `AiParseSection` until a later phase if splitting risks regression.

Minimum: the `border-t` footer with Parse + Cancel from ~lines 2007–2021 must use `AiParseActions`.
</action>

<read_first>
- `src/components/ai/AiParseSection.tsx` (`Cancel parsing`, `handleUnifiedParse`, `handleCancel`, `unifiedParseDisabled`)
</read_first>

<acceptance_criteria>
- `AiParseActions.tsx` contains the exact string `Cancel parsing (stop AI processing)`.
- `AiParseActions.tsx` contains a button or `Button` with child text **Parse** (same variant/disabled wiring as current).
</acceptance_criteria>

## Verification (phase)

- `npm run lint` and `npm run build` exit 0.
- `wc -l` or line count: `AiParseSection.tsx` line count **decreases by ≥200** vs pre-11-02 baseline (executor records before/after in SUMMARY).
