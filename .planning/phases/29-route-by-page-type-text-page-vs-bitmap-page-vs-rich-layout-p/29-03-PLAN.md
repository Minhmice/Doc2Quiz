---
phase: 29-route-by-page-type-text-page-vs-bitmap-page-vs-rich-layout-p
plan: 03
type: execute
wave: 3
depends_on:
  - "29-02"
files_modified:
  - src/lib/pdf/classifyPdfPages.ts
  - src/lib/pdf/pageRoutePlan.ts
  - src/components/ai/AiParseSection.tsx
autonomous: true
requirements:
  - ROUTE-29-03
  - ROUTE-29-04
must_haves:
  truths:
    - "Routing never increases vision workload beyond existing caps (≤20) even on mixed PDFs with many bitmap pages."
    - "Preview-first budget remains 3–5 pages and is applied consistently to both text-first and bitmap/vision subsets."
    - "Routing emits a single concise routing-summary log line with stable reason codes so debugging does not require UI changes."
  artifacts:
    - path: "src/lib/pdf/pageRoutePlan.ts"
      provides: "Explicit limit application + stable reason-code constants"
    - path: "src/lib/pdf/classifyPdfPages.ts"
      provides: "Conservative defaults + cap enforcement"
    - path: "src/components/ai/AiParseSection.tsx"
      provides: "One routing summary log line and merge/dedupe behavior"
  key_links:
    - from: "src/components/ai/AiParseSection.tsx"
      to: "pipelineLog"
      via: "routing summary"
      pattern: "routing"
---

<objective>
Harden the per-page routing integration with explicit cap enforcement, conservative fallbacks, and a single debuggable routing summary, ensuring we preserve Phase 27/28 budgets and don’t regress into over-rasterization.

Purpose: Make routing behavior predictable and safe under edge cases (encrypted PDFs, weird text layer, large mixed docs) while keeping UI unchanged.
Output: Guardrails + tightened reason codes and verification for cap/budget invariants.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-route-by-page-type-text-page-vs-bitmap-page-vs-rich-layout-p/29-CONTEXT.md
@.planning/phases/27-preview-first-parsing-while-full-upload-continues/27-CONTEXT.md

@src/components/ai/AiParseSection.tsx
@src/lib/pdf/renderPagesToImages.ts
@src/lib/pdf/classifyPdfPages.ts
@src/lib/pdf/pageRoutePlan.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make cap/budget enforcement explicit and grep-stable</name>
  <files>src/lib/pdf/pageRoutePlan.ts, src/lib/pdf/classifyPdfPages.ts</files>
  <read_first>
    - Read `29-01` outputs (`pageRoutePlan.ts` and `classifyPdfPages.ts`) and confirm where caps are applied.
    - Read `src/lib/pdf/renderPagesToImages.ts` (post-29-02) for the effective vision cap and preview semantics.
  </read_first>
  <action>
    Tighten the routing-plan contract so invariants are always true:
    - In `pageRoutePlan.ts`, add a small helper that:
      - Normalizes and de-dupes page indices
      - Truncates bitmap pages to `visionMaxPages`
      - Computes a `droppedBitmapPagesCount` field when truncation happens
      - Exposes a single exported function like `finalizePageRoutePlan(...)` used by `classifyPdfPages`
    - In `classifyPdfPages.ts`, ensure:
      - On any error/abort, the returned plan is conservative and respects caps:
        - `pages.length === pageCount` (no silent skips)
        - `bitmapPageIndicesForVision.length <= visionMaxPages` and any excluded bitmap pages are recorded via `droppedBitmapPagesCount`
      - Threshold decisions are deterministic and include one of the exported reason-code constants for each classified page
    Keep behavior aligned with D-07 (caps) and D-08 (reason codes). Do not introduce any new taxonomy (no rich-layout detector per D-06).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - `PageRoutePlan.bitmapPageIndicesForVision.length <= visionMaxPages` is guaranteed by construction (while `bitmapPageIndicesAll` may exceed it).
    - The plan exposes whether any pages were dropped due to caps (for logging in Task 2).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add a single routing summary log and ensure merge/dedupe preserves one unified bank</name>
  <files>src/components/ai/AiParseSection.tsx</files>
  <read_first>
    - Read the Phase 29 wiring in `AiParseSection` (from 29-02) and identify where routing happens and where results merge.
    - Read the existing logging pattern: `pipelineLog("VISION", "pre-route", ...)` (doc-level policy logs).
  </read_first>
  <action>
    Add one concise routing summary (no new UI controls) that is always emitted for quiz parses when page routing is used:
    - Use `pipelineLog` with a dedicated step key (e.g. domain `VISION`, step `page-route`) and include:
      - counts: `pageCount`, `pages.length`, `textPageIndices.length`, `bitmapPageIndicesAll.length`, `bitmapPageIndicesForVision.length`, `droppedBitmapPagesCount`
      - applied caps: `previewFirstPageBudget`, `visionMaxPages`
      - stable reason code list (plan-level) in a consistent field name
    Confirm merge/dedupe semantics:
    - Ensure the final `Question[]` is the merge of text-first + vision-derived questions, deduped by stem (existing `dedupeQuestionsByStem`) before persistence.
    - Ensure persistence is still a single unified ApprovedBank update (D-04).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
    - Logs include exactly one routing summary per quiz parse run, with stable fields for debugging.
    - The persisted bank remains one unified output; no split runs, no new study set IDs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF content → routing plan | Untrusted input affects downstream API workload |
| routing plan → vision pages | Bitmap page subset becomes images sent to the model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------:|----------|-----------|-------------|-----------------|
| T-29-07 | D | large PDFs / many bitmap pages | mitigate | Guarantee truncation to `visionMaxPages` and record dropped count; never exceed caps. |
| T-29-08 | R | debugging routing regressions | mitigate | Emit a single structured routing summary log with stable fields/reason codes. |
| T-29-09 | I | logs leak content | mitigate | Ensure logs contain counts/indices only; never include extracted strings. |
</threat_model>

<verification>
- `npm run lint`
- `npm run build`
</verification>

<success_criteria>
- Routing is robust under failures and cannot exceed the vision cap or preview budgets.
- Debugging mixed-PDF routing is possible via a single structured log line with stable reason codes.
</success_criteria>

<output>
After completion, create `.planning/phases/29-route-by-page-type-text-page-vs-bitmap-page-vs-rich-layout-p/29-03-SUMMARY.md`
</output>

