---
phase: 29-route-by-page-type-text-page-vs-bitmap-page-vs-rich-layout-p
plan: 02
type: execute
wave: 2
depends_on:
  - "29-01"
files_modified:
  - src/components/ai/AiParseSection.tsx
  - src/lib/pdf/renderPagesToImages.ts
  - src/lib/ai/parseRoutePolicy.ts
autonomous: true
requirements:
  - ROUTE-29-03
  - ROUTE-29-04
must_haves:
  truths:
    - "Quiz parsing uses per-page routing: text pages do not get rasterized; bitmap pages are rasterized (within existing caps) and sent to vision."
    - "Mixed PDFs still produce a single unified ApprovedBank; routing does not create separate set runs (per D-04)."
    - "Routing decisions are visible in logs via stable reason codes (no new UI controls) (per D-08)."
    - "Preview-first and vision max pages budgets remain unchanged (3–5 preview, ≤20 vision pages) (per D-07)."
  artifacts:
    - path: "src/components/ai/AiParseSection.tsx"
      provides: "Per-page route planning + execution wiring for quiz lane"
    - path: "src/lib/pdf/renderPagesToImages.ts"
      provides: "Rasterize only selected bitmap page indices"
    - path: "src/lib/ai/parseRoutePolicy.ts"
      provides: "Reason-code conventions remain compatible"
  key_links:
    - from: "src/components/ai/AiParseSection.tsx"
      to: "src/lib/pdf/classifyPdfPages.ts"
      via: "build PageRoutePlan before rasterization"
      pattern: "classifyPdfPages\\("
    - from: "src/components/ai/AiParseSection.tsx"
      to: "src/lib/pdf/renderPagesToImages.ts"
      via: "render only bitmap indices"
      pattern: "renderPdfPagesToImages\\("
---

<objective>
Wire the Phase 29 per-page routing plan into the quiz lane so we classify pages first and only rasterize bitmap pages, while preserving the existing preview-first and vision caps and keeping `AiParseSection` as the sole routing authority (D-01..D-08).

Purpose: Avoid unnecessary rasterization and vision workload for text-heavy pages in mixed PDFs, without changing the overall UX surface or adding new controls.
Output: `AiParseSection` executes page-aware routing before rasterization; `renderPdfPagesToImages` supports rendering selected page indices.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-route-by-page-type-text-page-vs-bitmap-page-vs-rich-layout-p/29-CONTEXT.md

@src/components/ai/AiParseSection.tsx
@src/lib/pdf/renderPagesToImages.ts
@src/lib/pdf/extractPdfText.ts
@src/lib/pdf/sampleTextLayerSignal.ts
@src/lib/ai/parseRoutePolicy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add selected-page rasterization support (bitmap pages only)</name>
  <files>src/lib/pdf/renderPagesToImages.ts</files>
  <read_first>
    - Read `src/lib/pdf/renderPagesToImages.ts` and identify where it currently loops `i = 1..limit`.
    - Confirm how `previewPageBudget` and `onPreviewPagesAvailable` behave today and preserve semantics.
  </read_first>
  <action>
    Extend `renderPdfPagesToImages(file, options)` to optionally accept a `pageIndices?: number[]` (1-based indices).
    - When `pageIndices` is present:
      - Render only those pages, in ascending order.
      - Enforce the existing `maxPages` cap by truncating `pageIndices` to `maxPages` (do not increase work beyond caps; D-07).
      - Preserve existing `onPageRendered` calls and `PageImageResult.pageIndex` values (original page numbers, not 1..N).
      - Preserve `previewPageBudget` semantics by firing `onPreviewPagesAvailable` after rasterizing the first `min(previewPageBudget, renderedCount)` selected pages.
    - When `pageIndices` is absent: behavior is unchanged.
    - Ensure AbortSignal checks remain correct and "aborted" still throws AbortError as today.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - Existing call sites with no `pageIndices` behave identically.
    - A caller can rasterize only specific bitmap pages and still get preview callbacks as before.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire per-page routing into quiz lane before rasterization (preserve caps + observability)</name>
  <files>src/components/ai/AiParseSection.tsx, src/lib/ai/parseRoutePolicy.ts</files>
  <read_first>
    - Read Phase 29 decisions D-01..D-08 in `29-CONTEXT.md` and implement exactly.
    - Read the `runUnifiedParseInternal` pre-route section (today: doc-level `sampleTextLayerSignal` + `decideParseRoute`).
    - Read `runRenderPagesAndOptionalOcr` (today it always rasterizes 1..maxPages).
  </read_first>
  <action>
    Implement page-aware routing for **quiz** only (flashcards remain vision-batch-only):
    - Keep `AiParseSection` as the sole routing authority (D-01): the orchestration must live here.
    - Integrate routing **before** calling `renderPdfPagesToImages` (D-02).
    - Use `classifyPdfPages(activePdfFile, { ... })` from Plan 29-01 to produce a `PageRoutePlan` that **accounts for all pages** (`pages.length === pageCount`), while enforcing `VISION_MAX_PAGES_DEFAULT` **only** on the bitmap→vision execution subset (`bitmapPageIndicesForVision`).
    - Preserve existing budgets (D-07):
      - preview-first budget remains 3–5 pages (use existing `previewFirstPageBudget` clamped exactly as current product behavior does)
      - vision max pages default remains `VISION_MAX_PAGES_DEFAULT`
      - ensure the bitmap pages sent to vision are `<= VISION_MAX_PAGES_DEFAULT` via `plan.bitmapPageIndicesForVision`
    - Mixed PDFs (D-04):
      - Execute text-first parsing for `plan.textPageIndices` (may exceed 20) by extracting text only for those pages and parsing them via the existing text chunk lane (reuse `extractPdfTextForPageRange` in contiguous runs; do not extract the entire document text just to subset pages).
      - Execute vision raster+parse only for `plan.bitmapPageIndicesForVision` via `renderPdfPagesToImages({ pageIndices })` and existing vision batch lane (quiz mode).
      - Merge and dedupe results into one unified `Question[]` and persist once (do not create multiple banks; do not create separate study sets).
    - Rich layout semantics (D-06):
      - Do not add new detection logic. Continue to rely on downstream `includePageImage` / attach behavior only.
    - OCR policy (D-05):
      - Do not require OCR to classify or route in product create flows. The route plan must be computed from pdf.js text layer only.
    - Observability (D-08):
      - Add stable reason-code logs (via `pipelineLog`) that summarize:
        - `pageCount` and `pages.length` (to prove no silent skips)
        - counts of text vs bitmap pages (using `textPageIndices` and `bitmapPageIndicesAll`)
        - counts of bitmap pages sent to vision (`bitmapPageIndicesForVision.length`)
        - whether any bitmap pages were dropped due to caps (`droppedBitmapPagesCount`)
      - Do not add any new UI controls or strategy toggles.

    Update `src/lib/ai/parseRoutePolicy.ts` only if needed to keep reason-code naming consistent and grep-stable (do not introduce a second policy authority).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
    - For quiz parse, bitmap pages are the only pages rasterized and sent to vision.
    - Text pages are parsed via text-first lane without rasterization.
    - Total vision pages processed never exceeds `VISION_MAX_PAGES_DEFAULT` and preview-first remains 3–5.
    - `pipelineLog` includes stable reason codes explaining the routing outcome, including explicit dropped-bitmap counts when caps apply (no silent skipped pages).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user PDF bytes → pdf.js | Untrusted file content parsed client-side |
| routing plan → vision API | Plan determines which pages become images sent to model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------:|----------|-----------|-------------|-----------------|
| T-29-04 | D | `renderPdfPagesToImages(pageIndices)` | mitigate | Enforce `maxPages` truncation and keep AbortSignal fast paths. |
| T-29-05 | I | per-page text extraction | mitigate | Only extract text for routed text pages; never log raw extracted text; keep numeric-only reason codes. |
| T-29-06 | T | "two routers" risk | mitigate | Keep routing logic only in `AiParseSection`; `parseRoutePolicy` remains doc-level intent only (D-01). |
</threat_model>

<verification>
- `npm run lint`
- `npm run build`
</verification>

<success_criteria>
- Page-aware routing is live for quiz lane, and bitmap-only rasterization reduces unnecessary work for mixed PDFs.
- Existing caps/budgets (preview 3–5; vision ≤20) are preserved and enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/29-route-by-page-type-text-page-vs-bitmap-page-vs-rich-layout-p/29-02-SUMMARY.md`
</output>

