---
phase: 14-friend-presence-and-chat-layout-repair
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/components/friends/FriendsHub.tsx
  - src/components/friends/FriendsHub.test.tsx
  - src/components/layout/FriendsMenu.tsx
  - src/components/friends/DirectMessageDialog.tsx
  - src/components/friends/DirectMessageDialog.test.tsx
  - src/components/friends/FriendActionMenu.tsx
  - src/components/friends/FriendActionMenu.test.tsx
  - src/components/friends/ConversationView.tsx
  - src/components/friends/ConversationView.test.tsx
autonomous: true
requirements: [SOCIAL-09, SOCIAL-10]
must_haves:
  truths:
    - "A friend shown in Online appears only when presence is online; all other presence states appear only in Offline."
    - "A currently open Friends tab refetches its first server page after focus/visible recovery, each 60-second activity cadence, and five-minute presence boundary, so either direction of a transition cannot leave bucket membership stale."
    - "Offline and recently active friend rows expose no online dot, ping, or online-status text; online rows retain current live treatment."
    - "Long ordinary text and long unbroken tokens stay inside incoming and outgoing message bubbles on desktop and mobile."
    - "Friend paging, URL destination behavior, durable message history, and responsive desktop/mobile chat remain in place."
  artifacts:
    - path: "src/components/friends/FriendsHub.tsx"
      provides: "URL-authoritative Online/Offline sub-tabs using server-bucketed page requests"
    - path: "src/components/friends/DirectMessageDialog.tsx"
      provides: "Desktop direct-message header derived from friend presence enum"
    - path: "src/components/friends/DirectMessageDialog.test.tsx"
      provides: "Direct-message enum contract and online-header derivation proof"
    - path: "src/components/friends/FriendActionMenu.tsx"
      provides: "Online-only presence affordance rendering"
    - path: "src/components/friends/ConversationView.tsx"
      provides: "Shared flex-safe message bubble wrapping for desktop and mobile"
    - path: "src/components/friends/FriendActionMenu.test.tsx"
      provides: "Presence visual derivation testable without new DOM package"
    - path: "src/components/friends/ConversationView.test.tsx"
      provides: "Normal whitespace and unbroken-token bubble class contract tests"
  key_links:
    - from: "src/components/friends/FriendsHub.tsx"
      to: "src/lib/client/friends.ts"
      via: "active presence tab passes bucket and only follows matching nextCursor"
      pattern: "listAcceptedFriendPage"
    - from: "src/components/friends/FriendsHub.tsx"
      to: "FriendsHub.load"
      via: "focus/visible lifecycle, 60-second bounded revalidation, and earliest online lastActiveAt plus five minutes reset current bucket to first page"
      pattern: "createFriendsPresenceRefreshController|resetCurrentBucket"
    - from: "src/components/friends/PlayfulReactionOverlay.tsx"
      to: "/api/friends/activity"
      via: "current app-level focus/visibility/60-second activity touch; it has no hub invalidation consumer"
      pattern: "touchSocialActivity"
    - from: "src/components/layout/FriendsMenu.tsx"
      to: "src/components/friends/DirectMessageDialog.tsx"
      via: "AcceptedFriendSummary passes server presence enum to desktop dialog friend contract"
      pattern: "friend=\{messageFriend\}"
    - from: "src/components/friends/DirectMessageDialog.tsx"
      to: "friend.presence"
      via: "strict enum comparison derives desktop header online state"
      pattern: "presence === \"online\""
    - from: "src/components/layout/FriendsMenu.tsx"
      to: "src/components/friends/FriendActionMenu.tsx"
      via: "strict enum comparison derives row online state"
      pattern: "presence === \"online\""
    - from: "src/components/friends/ConversationView.tsx"
      to: "desktop dialog and mobile page shells"
      via: "both render same shared view"
      pattern: "wrap-anywhere"
---

<objective>
Wire bucketed presence into friend UI and repair shared message-bubble wrapping without touching durable chat/realtime controller.

Purpose: D-01/D-02 require correct rows and offline visuals; D-03 requires every shared chat presentation to contain long text.
Output: accessible Online/Offline sub-tabs, enum-based launcher/action rows, focused row visual tests, and flex-safe shared bubble class tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-friend-presence-and-chat-layout-repair/14-CONTEXT.md
@.planning/phases/14-friend-presence-and-chat-layout-repair/14-RESEARCH.md
@.planning/phases/14-friend-presence-and-chat-layout-repair/14-01-SUMMARY.md
@.planning/phases/12-study-together/12-08-SUMMARY.md
@.planning/phases/12-study-together/12-09-SUMMARY.md
@src/components/friends/FriendsHub.tsx
@src/components/layout/FriendsMenu.tsx
@src/components/friends/DirectMessageDialog.tsx
@src/components/friends/FriendActionMenu.tsx
@src/components/friends/ConversationView.tsx
@src/components/friends/ConversationView.test.tsx

<interfaces>
From Plan 14-01: `PresenceBucket = "online" | "offline"`; accepted friend `presence` is exactly `"online" | "recently_active" | "offline"`; `listAcceptedFriendPage(bucket, cursor?)` returns `{ items, nextCursor, hasMore }` from one server-filtered keyset set.

Hub keeps existing `destination` URL behavior. Add presence URL state only when `destination === "friends"`, using a single named query parameter such as `presence=online|offline`; normalize missing/invalid to chosen default. On tab switch clear accumulated friend page/cursor and request first page for selected bucket. Never filter generic fetched rows in memory.

Tracer evidence before implementation: `PlayfulReactionOverlay` is current `touchSocialActivity` owner; it POSTs `/api/friends/activity` on focus, visibility, and 60-second interval, which updates only caller `last_active_at`. `createSocialCountsController` receives only notification/request/count channels and does not notify `FriendsHub`; `FriendsHub` has no subscription or refresh lifecycle. Therefore no reusable presence invalidation source exists. Per D-01/D-02 and deferred no-new-protocol decision, add minimum local refresh at `FriendsHub` owner: refetch first page of active bucket on window focus, visible-document transition, a 60-second bounded interval matching current activity cadence, and timer for earliest displayed online `lastActiveAt + 5 minutes`; never add a realtime channel or move rows locally. Export a deterministic `createFriendsPresenceRefreshController` or equivalent narrow helper from `FriendsHub.tsx` for Node-Vitest fake-timer/lifecycle proof.

`FriendsMenu` passes `AcceptedFriendSummary` directly to desktop `DirectMessageDialog`. Per Plan 14-01, remove `isOnline` from that DTO; update dialog `Friend` contract to require exact `presence: "online" | "recently_active" | "offline"`, derive desktop header online state only with `friend.presence === "online"`, and retain its current desktop-only header/mobile route split. Keep non-online header fallback text based on existing `lastActiveAt`; do not introduce another boolean or a recently-active UI label.

`ConversationView` owns both desktop `DirectMessageDialog` and mobile `ConversationPageClient`. Preserve `createConversationController`, `browserTransport`, `before` cursor, event invalidation, reconcile, read, send, and layout shell classes; only bubble presentation may change.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Trace and refresh active server-bucketed presence tab</name>
  <files>src/components/friends/FriendsHub.tsx, src/components/friends/FriendsHub.test.tsx, src/components/layout/FriendsMenu.tsx, src/components/friends/DirectMessageDialog.tsx, src/components/friends/DirectMessageDialog.test.tsx, src/components/friends/FriendActionMenu.tsx, src/components/friends/FriendActionMenu.test.tsx</files>
  <behavior>
    - Friends destination presents keyboard-accessible Online and Offline tabs, URL-authoritative via presence query; non-friends destinations retain existing behavior and no presence sub-tab request.
    - Switching bucket discards old items/cursor, starts matching bucket first page, and Load more appends/de-duplicates only that bucket's returned page.
    - A current Friends tab resets and refetches its active bucket first page on window focus, visible-document transition, each 60-second bounded interval, and timer expiry at earliest displayed `online.lastActiveAt + 5 minutes`; stale or stopped lifecycle callbacks cannot write state.
    - `online` is strict `friend.presence === "online"`; `recently_active` and `offline` are Offline. Compact launcher counts/groups and desktop `DirectMessageDialog` header derive state from same enum, not `isOnline`.
    - Online row and desktop dialog header retain current online treatment. Offline/recently_active row renders neither status dot/ping nor status text, while dialog header retains its existing non-online `lastActiveAt` fallback and avatar/name/actions remain accessible.
    - `FriendsMenu` can pass `AcceptedFriendSummary` directly to `DirectMessageDialog` after Plan 14-01 removes `isOnline`; TypeScript rejects a stale boolean dialog contract.
  </behavior>
  <action>Tracer-first: record in test names/assertions that `PlayfulReactionOverlay` only calls `touchSocialActivity` and `createSocialCountsController` has no FriendsHub callback, so neither can refresh hub membership. Start RED in `FriendsHub.test.tsx`; because Vitest is Node-only and project has no DOM renderer, export small deterministic pure helpers/constants only where needed to test URL bucket normalization, bucket-to-row membership, page-reset intent, lifecycle scheduling, and bubble-free row presentation model. Add `createFriendsPresenceRefreshController({ onRefresh, getNextTransitionAt, windowTarget, documentTarget, setTimeout, clearTimeout })` or equivalent narrow exported controller in `FriendsHub.tsx`. It must call `onRefresh` after focus and visible-document events, schedule one 60-second bounded revalidation interval plus one timer at `min(earliest currently displayed online lastActiveAt + 5 minutes)`, reschedule after every current-bucket page replacement/append, and cancel interval/timer/listeners on bucket/destination change or unmount. Its `onRefresh` must invoke hub owner `load(undefined)` with a fresh request identity so first page replaces items/cursor; a completion from prior bucket/request must be ignored. Do not add Supabase channel, broadcast topic, global store, or client-side membership mutation: D-01 requires server-filtered replacement and deferred decisions prohibit realtime protocol changes.

Then add Online/Offline tabs only under Friends destination using existing Link/URL search pattern; preserve five destination navigation. Each selected tab must call Plan 14-01 `listAcceptedFriendPage(bucket, cursor)` and retain only matching server page state. Reset items, `nextCursor`, loading/error state, stale request identity, and refresh controller when selected bucket changes; ignore stale completion. Keep page limit 20, server cursor only, existing total behavior, and responsive `min-w-0`/horizontal tab scrolling. In `FriendsMenu`, replace all `isOnline` reads with strict enum comparison and pass real presence into `FriendActionMenu`. Update `DirectMessageDialog` `Friend` type from `isOnline: boolean` to exact Plan 14-01 `presence` enum, then derive its existing desktop header online branch only with `friend.presence === "online"`; retain current online copy and non-online `relativeLastActive(friend.lastActiveAt)` fallback, portal, desktop-only class, and mobile route handoff. Start RED in new Node-Vitest `DirectMessageDialog.test.tsx`: test a typed API-shaped online friend and each non-online enum value; assert no `isOnline` field is accepted by dialog-facing friend fixture, strict enum derivation selects online only for `"online"`, and existing non-online fallback path remains selected for `"recently_active"`/`"offline"`. Export a narrow pure header-status helper only if needed; do not add DOM renderer or duplicate chat controller tests. In `FriendActionMenu`, derive one `isOnline`, preserve existing online dot/status markup only in that branch, and render no status node/text for non-online values. Do not add recently-active label, client filtering after pagination, new presence subscription, new actions, or locale redesign. Preserve existing count controller; it remains independent of membership refresh.</action>
  <verify><automated>npx vitest run src/components/friends/FriendsHub.test.tsx src/components/friends/DirectMessageDialog.test.tsx src/components/friends/FriendActionMenu.test.tsx src/lib/client/friends.test.ts && npm run typecheck && npm run lint</automated></verify>
  <done>Online and Offline are independently paged server result sets; hub, compact launcher, row, and desktop dialog use enum truth; non-online rows have no online presence affordance; active Friends view refetches current bucket after focus/visibility recovery, each 60-second cadence, and five-minute transition boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Contain normal and unbroken chat text in shared bubbles</name>
  <files>src/components/friends/ConversationView.tsx, src/components/friends/ConversationView.test.tsx</files>
  <behavior>
    - Incoming and outgoing bubble class contract includes `min-w-0` and Tailwind `wrap-anywhere` while retaining current max width, colors, rounded direction, and text size.
    - A long whitespace-separated sentence and 2,000-character unbroken token both select the same safe bubble presentation for sent and received messages.
    - Desktop dialog and mobile full-screen route continue consuming `ConversationView`; controller tests still prove before-cursor history, dedupe, reconnect, read, and send behavior.
  </behavior>
  <action>Write RED tests in `ConversationView.test.tsx` against a narrow exported bubble-class/presentation helper if required by Node Vitest. Cover four variants: incoming normal long whitespace text, outgoing normal long whitespace text, incoming 2,000-character no-space token, outgoing 2,000-character no-space token. Assert both variants include `min-w-0` and `wrap-anywhere`, retain `max-w-[75%]`, and preserve sent/received visual classes. Then apply smallest class-only change directly to message `<p>` in shared `ConversationView`: add flex-safe `min-w-0 wrap-anywhere`; keep plain React interpolation, existing body/maxLength validation, newline behavior, controller, history `before` paging, merge order, realtime invalidation, send/read reconciliation, and desktop/mobile parent shells unchanged. Retain existing controller and mobile-shell assertions in same focused test file. Do not add JS token splitting, `break-all`, HTML rendering, package, or a second chat component.</action>
  <verify><automated>npx vitest run src/components/friends/ConversationView.test.tsx src/lib/client/messages.test.ts && npm run typecheck && npm run lint</automated></verify>
  <done>Both message text variants stay within incoming/outgoing shared bubbles at desktop and mobile widths without changing durable chat behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Server page to friends UI | Presence enum and bucketed pages are trusted only after authenticated route/RPC handling. |
| Realtime event to chat UI | Events remain invalidation hints; durable authenticated history is display authority. |
| Message body to React bubble | User text is untrusted but rendered as escaped plain text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-14-04 | Tampering / Repudiation | friends hub page state | mitigate | Consume only Plan 14-01 bucketed pages; reset/refetch first page on tab, focus, visibility, and threshold lifecycle triggers; reject stale result writes. |
| T-14-05 | Information disclosure | presence row | mitigate | Render normalized enum only; use existing `lastActiveAt` only to schedule local refresh, never display raw activity timestamp/status data or accept client membership input. |
| T-14-06 | Tampering | chat message body | mitigate | Preserve escaped React text interpolation; CSS-only wrap, no HTML/token transformation. |
| T-14-07 | Tampering / Repudiation | chat realtime | mitigate | Do not alter event-as-invalidation or authenticated durable history controller. |
| T-14-SC | Tampering | npm installs | accept | No package installation. |
</threat_model>

<verification>
Run focused hub, desktop-dialog, action-row, conversation, client paging, and message-history tests; then typecheck and lint. Automated hub test must fake focus, visibility, and timer transition for an open current bucket, prove `load(undefined)` replaces page/cursor, and prove cleanup/stale request guards. Human check after automated gates: two accepted accounts with more than 20 rows per bucket; confirm tab paging and a state transition refetch; send ordinary long and 2,000-character no-space messages at 375px and desktop, verify no horizontal viewport overflow and preserved message history after reload.
</verification>

<success_criteria>
SOCIAL-09 and SOCIAL-10 preserve responsive Friends/chat behavior while enforcing exact two-bucket presence visuals and safe message wrapping. No online row leaks into Offline, no non-online row shows online status, current bucket revalidates after recovery/threshold transitions, and no long text escapes shared bubbles.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 14 | Correct tabs, suppressed offline visuals, readable bubbles | 14-02 | COVERED | UI consumes Plan 14-01 and shared bubble change
REQ | SOCIAL-09 | Scalable responsive friends hub | 14-02 | COVERED | URL tabs retain server paging and responsive hub
REQ | SOCIAL-10 | Durable responsive chat | 14-02 | COVERED | Shared CSS-only bubble repair preserves controller
RESEARCH | Pattern 1 | Enum-only presence rendering | 14-02 | COVERED | Strict comparison replaces stale boolean in hub, launcher, row, and desktop dialog
RESEARCH | Pitfall 1 | Dialog caller contract cannot retain stale isOnline boolean | 14-02 | COVERED | FriendsMenu-to-DirectMessageDialog enum contract and focused test
RESEARCH | Pattern 3 | Shared bubble CSS | 14-02 | COVERED | `min-w-0 wrap-anywhere` on ConversationView bubble
RESEARCH | Pitfall 5 | Presence refresh preserves architecture | 14-02 | COVERED | Tracer found no reusable hub invalidation; owner adds focus/visible/threshold first-page current-bucket refetch, no protocol
CONTEXT | D-01 | Pre-pagination filtering and two-bucket mapping | 14-02 | COVERED | Plan 14-01 contract consumed; tab reset/fetch behavior
CONTEXT | D-02 | No offline/recent activity online visual | 14-02 | COVERED | Row rendering gate/test
CONTEXT | D-03 | Normal/unbroken text inside both bubble directions | 14-02 | COVERED | Four text/direction tests on shared view
CONTEXT | Deferred | New category/realtime protocol | NONE | EXCLUDED | No third state, transport, or subscription
</source_audit>

<output>Create `.planning/phases/14-friend-presence-and-chat-layout-repair/14-02-SUMMARY.md` when done.</output>
