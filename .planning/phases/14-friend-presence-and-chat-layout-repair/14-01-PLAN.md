---
phase: 14-friend-presence-and-chat-layout-repair
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260801233000_phase14_friend_presence_bucket.sql
  - supabase/schemas/70_functions.sql
  - supabase/tests/phase12_bounded_social_lists.sql
  - src/lib/server/friends/socialLists.ts
  - src/lib/server/friends/socialLists.test.ts
  - src/lib/server/friends/socialListQuery.ts
  - src/app/api/friends/route.ts
  - src/app/api/friends/friends.route.test.ts
  - src/lib/client/friends.ts
  - src/lib/client/friends.test.ts
autonomous: true
requirements: [SOCIAL-09]
must_haves:
  truths:
    - "Online requests return only friends whose server-derived presence is online."
    - "Offline requests return only friends whose server-derived presence is recently_active or offline."
    - "Each presence tab can page its complete filtered result without duplicate, skip, or cursor replay across tabs."
    - "Malformed presence values and cross-bucket cursors fail before or at the authenticated server boundary without revealing social data."
  artifacts:
    - path: "supabase/migrations/20260801233000_phase14_friend_presence_bucket.sql"
      provides: "Additive list_social_friends presence-bucket predicate applied before keyset paging"
      contains: "p_presence"
    - path: "src/lib/server/friends/socialLists.ts"
      provides: "Versioned opaque friends cursors bound to destination and requested presence bucket"
      exports: ["PresenceBucket", "listSocialFriends"]
    - path: "src/lib/server/friends/socialListQuery.ts"
      provides: "Validated presence query parsing for friends list endpoint"
    - path: "src/app/api/friends/friends.route.test.ts"
      provides: "Authenticated friends-route presence validation and adapter forwarding proof"
  key_links:
    - from: "src/app/api/friends/route.ts"
      to: "src/lib/server/friends/socialListQuery.ts"
      via: "query parser validates presence before list adapter"
      pattern: "parseSocialListQuery"
    - from: "src/lib/server/friends/socialLists.ts"
      to: "public.list_social_friends"
      via: "typed RPC includes p_presence and bucket-bound cursor"
      pattern: "list_social_friends"
    - from: "supabase/migrations/20260801233000_phase14_friend_presence_bucket.sql"
      to: "public.list_social_friends"
      via: "rows CTE bucket predicate precedes cursor, order, and limit plus one"
      pattern: "p_presence"
---

<objective>
Create production tracer for server-authoritative presence tabs: bucket filtering, cursor binding, validation, and typed browser contract.

Purpose: D-01 requires filtering before keyset pagination; browser filtering after an unbucketed page loses matching friends.
Output: additive SQL/schema mirror, bucket-aware RPC adapter/route/client contract, and focused server-side pagination tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-friend-presence-and-chat-layout-repair/14-CONTEXT.md
@.planning/phases/14-friend-presence-and-chat-layout-repair/14-RESEARCH.md
@.planning/phases/12-study-together/12-08-SUMMARY.md
@.planning/phases/12-study-together/12-09-SUMMARY.md
@supabase/migrations/20260731102000_phase12_bounded_social_lists.sql
@supabase/schemas/70_functions.sql
@src/lib/server/friends/socialLists.ts
@src/lib/server/friends/socialListQuery.ts
@src/app/api/friends/route.ts
@src/lib/client/friends.ts

<interfaces>
Existing list authority accepts `p_limit`, `p_cursor`, returns `items` and optional `totalCount`; `socialLists.ts` fetches `limit + 1`, trims to limit, and creates an opaque destination/version cursor from stable tuple `[presenceRank, username, userId]`.

Define `PresenceBucket = "online" | "offline"`. `online` maps only `presence_rank = 0`; `offline` maps `presence_rank in (1, 2)`, covering `recently_active` and `offline` per D-01. The SQL RPC, route query, client query, and friends cursor must all receive this value. Extend only friends cursor payload with a bucket field or bump its safe format so a cursor from online cannot be used for offline. Keep other social destination cursor semantics unchanged.

Existing browser DTO currently has stale `isOnline`; Phase 14 sole presence truth is `presence: "online" | "recently_active" | "offline"`. Derive a boolean locally only with `presence === "online"`; do not serialize a second boolean.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Prove and implement bucketed social pagination tracer</name>
  <files>supabase/migrations/20260801233000_phase14_friend_presence_bucket.sql, supabase/schemas/70_functions.sql, supabase/tests/phase12_bounded_social_lists.sql, src/lib/server/friends/socialLists.ts, src/lib/server/friends/socialLists.test.ts</files>
  <behavior>
    - Online adapter request sends `p_presence: "online"`, requests `limit + 1`, returns no more than requested limit, and creates a continuation only from final returned stable tuple.
    - Offline adapter request sends `p_presence: "offline"`; its result includes server-normalized `recently_active` and `offline` rows only.
    - Reusing online cursor under offline throws `social_unavailable`; malformed/legacy-invalid cursor also throws generic error.
    - SQL filters selected bucket in `rows` before keyset predicate, order, and `limit least(greatest(...), 51)`, preserves accepted-friend/block scope, `auth.uid()`, SECURITY DEFINER search_path, and authenticated-only grants.
  </behavior>
  <action>Start RED in `socialLists.test.ts`: assert typed online/offline RPC arguments, limit-plus-one trimming, continuation tuple, malformed cursor rejection, and bidirectional bucket mismatch rejection. Then add `PresenceBucket` and change only friends cursor encoding/decoding to bind the requested bucket while retaining all non-friends destination cursor behavior. Add additive migration `20260801233000_phase14_friend_presence_bucket.sql`; `create or replace` only `public.list_social_friends` with optional validated `p_presence text default 'offline'` or equivalent backward-compatible default matching prior all-friends behavior only if existing non-Phase-14 callers require it. Route Phase-14 client requests explicitly so UI never relies on default. Reject any value outside online/offline inside SQL; retain `auth.uid()` accepted friend and block predicates. Apply `(presence_rank = 0)` for online and `(presence_rank in (1,2))` for offline inside rows before cursor predicate and `limit + 1`. Mirror final function signature/body and grants in `supabase/schemas/70_functions.sql`; never edit Phase 12 migration. Extend static SQL proof with function signature/execute grant and ordering/predicate tokens, preserving approved-disposable-target policy. No package, offset paging, client filtering, new presence category, or realtime transport change.</action>
  <verify><automated>npx vitest run src/lib/server/friends/socialLists.test.ts && npm run typecheck</automated></verify>
  <done>Server has an additive, authenticated, bucket-before-pagination contract; cursors cannot cross online/offline result sets and existing stable paging semantics remain intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate and expose explicit friends presence query</name>
  <files>src/lib/server/friends/socialListQuery.ts, src/app/api/friends/route.ts, src/app/api/friends/friends.route.test.ts, src/lib/client/friends.ts, src/lib/client/friends.test.ts</files>
  <behavior>
    - `GET /api/friends` defaults explicitly to the chosen documented bucket and accepts only `presence=online|offline`, limit 1..50, and opaque cursor.
    - Invalid presence yields HTTP 400 before `listSocialFriends`/RPC executes; unauthenticated requests still stop at `requireApiUser` before parsing untrusted query data.
    - Browser `listAcceptedFriendPage(presence, cursor?)` creates URLSearchParams with `limit=20`, selected presence, and only server-returned cursor; returned rows expose enum `presence`, never phantom `isOnline`.
    - Route tests preserve generic unavailable mapping and no list operation mutates durable social/message state.
  </behavior>
  <action>Write failing route/client tests using module mocks following existing `friends.route.test.ts` and `friends.test.ts` patterns. Extend `parseSocialListQuery` with presence schema only for friends GET; authenticate first in route, parse after auth, and pass typed bucket to `listSocialFriends`. Keep invalid parser and cross-bucket cursor errors as existing generic `invalid` 400 response; do not add client-supplied user IDs or service-role reads. Replace `AcceptedFriendSummary.isOnline` with exact SQL enum spelling `online | recently_active | offline`; update page helper signature to require bucket and include `presence` in URLSearchParams. Update legacy convenience `listAcceptedFriends` only if required by compact menu, explicitly requesting both bounded buckets and combining only at that presentation boundary; it must not become hub data source. Tests must assert default/valid values, invalid pre-adapter rejection, adapter args, client URL for each bucket, enum DTO, and error mapping. Per D-01 and D-02, preserve cursor paging, durable history, and existing count/realtime invalidation mechanisms.</action>
  <verify><automated>npx vitest run src/lib/server/friends/socialLists.test.ts src/app/api/friends/friends.route.test.ts src/lib/client/friends.test.ts && npm run typecheck</automated></verify>
  <done>Friends API and client expose one validated enum/bucket contract usable by both tabs without stale boolean drift.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser query to `/api/friends` | `presence`, limit, and cursor are attacker-controlled. |
| Route to RPC | User-selected bucket must not select friend identities or bypass authenticated scope. |
| Cursor continuation | Cursor belongs only to one ordered, filtered result set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-14-01 | Tampering / Information disclosure | friends cursor | mitigate | Bind presence bucket to typed opaque friends cursor; reject mismatch as `social_unavailable`. |
| T-14-02 | Elevation / Information disclosure | friends RPC | mitigate | Preserve `auth.uid()`, accepted-friend/block predicates, restricted search path, and authenticated-only execute grant. |
| T-14-03 | Denial of service | friends list query | mitigate | Preserve Zod and SQL limits 1..50 plus limit+1 page cap. |
| T-14-SC | Tampering | npm installs | accept | No package installation. |
</threat_model>

<verification>
Run focused adapter, route, and browser-client tests; then typecheck. Inspect migration and schema mirror for matching `list_social_friends` signature, bucket predicate placement before keyset paging, and unchanged grants. Do not run SQL against a non-approved database.
</verification>

<success_criteria>
SOCIAL-09 supports Online and Offline as independent, complete, server-filtered keyset pages. Online contains only online; Offline contains recently_active/offline; cursors and route validation cannot mix result sets.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 14 | Correct online/offline tabs without losing paging | 14-01 | COVERED | Bucket predicate precedes cursor paging
REQ | SOCIAL-09 | Scalable `/friends` destination | 14-01 | COVERED | Bounded authenticated RPC/route/client contract
RESEARCH | Pattern 1 | Enum is sole presence truth | 14-01 | COVERED | Removes consumer reliance on `isOnline`
RESEARCH | Pattern 2 | Filter before keyset paging | 14-01 | COVERED | SQL rows CTE bucket predicate
CONTEXT | D-01 | online only online; offline/recently_active only offline; pre-pagination | 14-01 | COVERED | Exact bucket mapping and cursor binding
CONTEXT | Deferred | New categories/realtime protocol | NONE | EXCLUDED | No new category or transport
</source_audit>

<output>Create `.planning/phases/14-friend-presence-and-chat-layout-repair/14-01-SUMMARY.md` when done.</output>
