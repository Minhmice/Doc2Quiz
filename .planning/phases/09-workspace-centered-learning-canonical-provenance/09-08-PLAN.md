---
phase: 09-workspace-centered-learning-canonical-provenance
plan: 08
type: execute
wave: 8
depends_on: [09-07]
files_modified:
  - src/app/api/study-sets/[id]/flashcards/generate/route.ts
  - src/app/api/study-sets/[id]/flashcards/generate/route.test.ts
autonomous: true
requirements: [WORK-06, WORK-07, WORK-08, WORK-09]
user_setup: []
must_haves:
  truths:
    - "Legacy flashcard set IDs resolve deterministically through output bridges without parent-history fallback."
    - "Legacy flashcard generation preserves its current response, quota, session, and mistake behavior while workspace-native output remains authoritative."
  artifacts:
    - {path: src/app/api/study-sets/[id]/flashcards/generate/route.ts, provides: retained flashcard bridge adapter, exports: [POST]}
  key_links:
    - {from: src/app/api/study-sets/[id]/flashcards/generate/route.ts, to: src/lib/workspaces/legacyBridge.ts, via: explicit flashcards route kind, pattern: "resolveLegacyStudySetBridge"}
---
<objective>
Complete retained flashcard set-ID adapter contract against legacy bridge resolver.

Purpose: Isolate flashcard compatibility behavior from non-flashcard legacy migration work.
Output: Flashcard adapter contract coverage.
</objective>
<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-05-SUMMARY.md
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-07-SUMMARY.md
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-VALIDATION.md
@src/lib/workspaces/legacyBridge.ts
@src/app/api/study-sets/[id]/flashcards/generate/route.ts
<interfaces>
- `resolveLegacyStudySetBridge({ studySetId, routeKind: "flashcards", userId })` provides bridge-first/no-parent-fallback resolution from 09-07.
- Flashcard workspace generation and response mapping remain owned by 09-05.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Harden legacy flashcard adapter contract</name>
<files>src/app/api/study-sets/[id]/flashcards/generate/route.ts, src/app/api/study-sets/[id]/flashcards/generate/route.test.ts</files>
<behavior>
- Parent resolution selects only flashcard child; bridge resolves own output and bridge-keyed new history first.
- Unknown/inaccessible parent or bridge retains route-compatible 404.
- Existing success DTO and quota/session/mistake associations remain unchanged; source snapshots remain studyable after soft delete.
</behavior>
<action>Use `resolveLegacyStudySetBridge` with explicit `routeKind: "flashcards"`, then delegate to 09-05 workspace-native flashcard service without changing established public response shape. Add focused route tests for 401, inaccessible 404 for parent and bridge, authorized bridge resolution, parent kind matching, unchanged parent-keyed historic quota/session/mistake fixtures, bridge no-fallback, bridge-keyed new-history behavior, explicit source-selection policy, and snapshot study after source soft delete. Do not add duplicate adapter logic, mutate historic records, invoke replacement semantics, or add Phase 10 controls.</action>
<verify><automated>npm run test -- src/lib/workspaces/legacyBridge.test.ts src/app/api/study-sets/[id]/flashcards/generate/route.test.ts --run</automated></verify>
<done>Legacy flashcard generation is deterministic, bridge-safe, and separately testable.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| legacy flashcard route→workspace bridge | Set ID and generation payload map to membership-scoped output |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-19 | Tampering | flashcard legacy adapter | mitigate | Explicit flashcards route kind, bridge-first resolver, focused contract tests |
| T-09-20 | Elevation | flashcard bridge lookup | mitigate | Resolver membership scope and parent/bridge 404 tests |
| T-09-SC | Tampering | npm installs | accept | No package installs |
</threat_model>
<verification>
- `npm run test -- src/lib/workspaces/legacyBridge.test.ts src/app/api/study-sets/[id]/flashcards/generate/route.test.ts --run`
</verification>
<success_criteria>WORK-06/07/08 flashcard compatibility preserves frozen provenance and bridge-only new history.</success_criteria>
<output>Create `.planning/phases/09-workspace-centered-learning-canonical-provenance/09-08-SUMMARY.md` when done.</output>
