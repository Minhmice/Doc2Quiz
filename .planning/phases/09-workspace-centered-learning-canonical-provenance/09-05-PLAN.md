---
phase: 09-workspace-centered-learning-canonical-provenance
plan: 05
type: execute
wave: 5
depends_on: [09-04]
files_modified:
  - src/lib/pipeline/flashcardMultiSourceGenerate.ts
  - src/lib/pipeline/flashcardMultiSourceGenerate.test.ts
  - src/lib/pipeline/mapFlashcardOutputToRows.ts
  - src/app/api/workspaces/[workspaceId]/outputs/flashcards/route.ts
  - src/app/api/workspaces/[workspaceId]/outputs/flashcards/route.test.ts
  - src/app/api/study-sets/[id]/flashcards/generate/route.ts
  - src/app/api/study-sets/[id]/flashcards/generate/route.test.ts
  - src/lib/client/flashcardGenerateStudySet.ts
  - src/components/flashcards/FlashcardActions.tsx
autonomous: true
requirements: [WORK-06, WORK-07, WORK-08, WORK-09]
user_setup: []
must_haves:
  truths:
    - "Flashcard generation has same selected-source and frozen-snapshot guarantees as quiz generation."
    - "Soft-deleted sources disappear from active flashcard choices while historic output remains usable through snapshots."
  artifacts:
    - {path: src/lib/pipeline/flashcardMultiSourceGenerate.ts, provides: multi-source flashcard generator, exports: [runMultiSourceFlashcardGenerate]}
    - {path: src/app/api/workspaces/[workspaceId]/outputs/flashcards/route.ts, provides: workspace flashcard generation endpoint, exports: [POST]}
  key_links:
    - {from: src/lib/pipeline/flashcardMultiSourceGenerate.ts, to: create_learning_output, via: frozen output and bridge RPC, pattern: "create_learning_output"}
    - {from: src/components/flashcards/FlashcardActions.tsx, to: /api/workspaces/:workspaceId/outputs/flashcards, via: selected canonical-version IDs only, pattern: "canonicalVersionIds"}
---
<objective>
Bring flashcards to multi-source provenance parity without changing workspace dashboard reads.

Purpose: Preserve selected-source evidence for every generated flashcard output.
Output: Workspace-native flashcard generation, narrow set-ID adapter, explicit selection UI, and focused tests.
</objective>
<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-04-SUMMARY.md
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-RESEARCH.md
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-VALIDATION.md
@src/lib/pipeline/flashcardGenerate.ts
@src/app/api/study-sets/[id]/flashcards/generate/route.ts
<interfaces>
- Reuse `buildOutputSourceSnapshots`, source eligibility validation, and `create_learning_output` from 09-04.
- `create_learning_output` returns one output-specific bridge study-set ID; quota, sessions, and mistakes use that ID.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Add multi-source flashcard generation and routes</name>
<files>src/lib/pipeline/flashcardMultiSourceGenerate.ts, src/lib/pipeline/flashcardMultiSourceGenerate.test.ts, src/lib/pipeline/mapFlashcardOutputToRows.ts, src/app/api/workspaces/[workspaceId]/outputs/flashcards/route.ts, src/app/api/workspaces/[workspaceId]/outputs/flashcards/route.test.ts, src/app/api/study-sets/[id]/flashcards/generate/route.ts, src/app/api/study-sets/[id]/flashcards/generate/route.test.ts</files>
<behavior>
- Flashcard selection requires one or more unique completed canonical versions in caller's editable workspace.
- Generation preserves goal, coverage, amount, detected format, citations, and frozen source snapshots.
- New output never deletes old quiz or flashcard outputs; legacy route stays a narrow bridge adapter.
</behavior>
<action>Implement flashcard parallel of 09-04 using existing prompt/schema/dedupe logic and shared source validation/snapshot builder. Preserve goal, coverage, amount; coverage maps only selected version sections and rejects mismatched keys. Per D-01, atomic output RPC allocates exactly one output-specific compatible bridge `study_sets` row and writes its ID plus `output_id` to flashcard items; reject mixed kinds and preserve quota/session/mistake references through that bridge. Add workspace API with authenticated Zod/error/quota contract and focused route tests. Keep set-ID route a narrow adapter retaining public response and quota/session/mistake behavior. Do not perform Phase 5 cross-mode deletion or mutable latest-version fallback.</action>
<verify><automated>npm run test -- src/lib/pipeline/flashcardMultiSourceGenerate.test.ts src/app/api/workspaces/[workspaceId]/outputs/flashcards/route.test.ts src/app/api/study-sets/[id]/flashcards/generate/route.test.ts --run</automated></verify>
<done>Flashcards satisfy multi-source snapshot guarantees without destructive cross-mode cleanup.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Wire explicit flashcard source selection</name>
<files>src/lib/client/flashcardGenerateStudySet.ts, src/components/flashcards/FlashcardActions.tsx</files>
<behavior>
- Client submits selected completed canonical-version IDs only.
- Versions display number, provenance, and creation date.
- Prior user selection persists; latest completed is visible editable default only when no selection exists.
</behavior>
<action>Update owned client/actions to require explicit selected completed versions and post IDs only. Show visible version/provenance/date labels, retain prior selection, and per D-03 preselect latest completed only when no prior selection exists. Preserve existing goal/coverage/amount UX, progress, quota 402 handling, locale, and navigation through returned bridge set ID. Do not silently select latest after user choice or add Phase 10 controls.</action>
<verify><automated>npm run typecheck && npm run test -- src/lib/pipeline/flashcardMultiSourceGenerate.test.ts src/app/api/workspaces/[workspaceId]/outputs/flashcards/route.test.ts --run</automated></verify>
<done>Flashcard UI submits explicit multi-source selection and cannot mutate provenance.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| client→flashcard output route | Untrusted source/version IDs and generation options |
| server→output RPC | Output, snapshots, bridge, and items must persist atomically |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-13 | Tampering | flashcard source/citations | mitigate | Shared source eligibility and snapshot transaction before success |
| T-09-14 | Disclosure | flashcard source selection | mitigate | Membership/editor scope; client sends IDs only |
| T-09-SC | Tampering | npm installs | accept | No package installs |
</threat_model>
<verification>
- `npm run test -- src/lib/pipeline/flashcardMultiSourceGenerate.test.ts src/app/api/workspaces/[workspaceId]/outputs/flashcards/route.test.ts --run`
- `npm run typecheck`
</verification>
<success_criteria>WORK-06/07/08 flashcard source selection and frozen evidence work without deleting existing output or Phase 10 features.</success_criteria>
<output>Create `.planning/phases/09-workspace-centered-learning-canonical-provenance/09-05-SUMMARY.md` when done.</output>
