---
phase: 09-workspace-centered-learning-canonical-provenance
plan: 04
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - src/lib/provenance/outputSnapshot.ts
  - src/lib/provenance/outputSnapshot.test.ts
  - src/lib/pipeline/multiSourceGenerate.ts
  - src/lib/pipeline/multiSourceGenerate.test.ts
  - src/lib/pipeline/mapQuizOutputToRows.ts
  - src/app/api/workspaces/[workspaceId]/outputs/quiz/route.ts
  - src/app/api/workspaces/[workspaceId]/outputs/quiz/route.test.ts
  - src/app/api/study-sets/[id]/quiz/generate/route.ts
  - src/app/api/study-sets/[id]/quiz/generate/route.test.ts
  - src/lib/client/quizGenerateStudySet.ts
  - src/components/quiz/QuizGenerateProgressCard.tsx
autonomous: true
requirements: [WORK-06, WORK-07, WORK-08, WORK-09]
user_setup: []
must_haves:
  truths:
    - "Editor selects one or more completed canonical versions in one readable workspace for quiz generation."
    - "Quiz output persists frozen content/evidence snapshots before success response."
    - "Legacy set ID quiz route remains reachable through narrow bridge adapter."
  artifacts:
    - {path: src/lib/pipeline/multiSourceGenerate.ts, provides: authorized multi-source quiz generator, exports: [runMultiSourceQuizGenerate]}
    - {path: src/lib/provenance/outputSnapshot.ts, provides: frozen canonical snapshot builder, exports: [buildOutputSourceSnapshots]}
    - {path: src/app/api/workspaces/[workspaceId]/outputs/quiz/route.ts, provides: workspace quiz generation endpoint, exports: [POST]}
  key_links:
    - {from: src/lib/pipeline/multiSourceGenerate.ts, to: create_learning_output, via: atomic output/snapshot/item RPC, pattern: "create_learning_output"}
    - {from: src/app/api/study-sets/[id]/quiz/generate/route.ts, to: learning_outputs.legacy_study_set_id, via: bridge adapter, pattern: "legacy_study_set_id"}
---
<objective>Create multi-source quiz generation with frozen provenance, then retain legacy route compatibility through output bridge.</objective>
<execution_context>@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-03-SUMMARY.md
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-RESEARCH.md
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-VALIDATION.md
@src/lib/pipeline/quizGenerate.ts
@src/lib/pipeline/mapQuizOutputToRows.ts
@src/app/api/study-sets/[id]/quiz/generate/route.ts
@src/lib/server/quota/assertGenerationQuota.ts
<interfaces>
- Workspace generator accepts `canonicalVersionIds: string[]`, not canonical markdown.
- Per D-01, `create_learning_output` atomically allocates one output-specific compatible `study_sets` bridge, saves output/snapshots, and populates quiz items with both `study_set_id` and `output_id`.
- Quota, sessions, and mistakes remain keyed to returned bridge set ID; adapters preserve current setId response contracts.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Build and test frozen multi-source snapshot contract</name>
<files>src/lib/provenance/outputSnapshot.ts, src/lib/provenance/outputSnapshot.test.ts, src/lib/pipeline/multiSourceGenerate.ts, src/lib/pipeline/multiSourceGenerate.test.ts, src/lib/pipeline/mapQuizOutputToRows.ts</files>
<behavior>
- Empty, duplicate, incomplete, deleted, cross-workspace, or unreadable version selections reject before AI call.
- Selected source IDs dedupe without silently changing declared provenance order.
- Prompt assembly uses selected completed ordered sections only.
- Output snapshots retain canonical version ID, both checksums, markdown, ordered sections, model/prompt/parser/settings, and never API keys.
- One new quiz output receives exactly one compatible bridge study set; its item rows use that bridge ID and output ID, preserving quota/session/mistake behavior.
</behavior>
<action>Create `buildOutputSourceSnapshots` from canonical version rows and ordered section rows; preserve source selection order after first-ID dedupe. Implement workspace-native quiz generator by reusing prompt, output validation, dedupe/cap, and row mapping from `quizGenerate.ts`; never call destructive replace or delete prior quiz/flashcard outputs. Require caller editor authorization and one common workspace, fetch active completed versions only, assemble explicit source boundaries, run quota for returned output-specific bridge set ID, then call `create_learning_output` once after valid AI output. Per D-01, require RPC result to include one newly allocated compatible `study_sets.id`, write that ID and `output_id` to every quiz item, and return bridge ID for existing review/practice/session/mistake callers. Tests must prove validation before AI, exact snapshots, atomic bridge/output/item persistence, and quota/session/mistake compatibility mapping before success.</action>
<verify><automated>npm run test -- src/lib/provenance/outputSnapshot.test.ts src/lib/pipeline/multiSourceGenerate.test.ts --run</automated></verify>
<done>Quiz generator validates exact selected source versions and atomically freezes evidence.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Expose workspace quiz route and legacy adapter</name>
<files>src/app/api/workspaces/[workspaceId]/outputs/quiz/route.ts, src/app/api/workspaces/[workspaceId]/outputs/quiz/route.test.ts, src/app/api/study-sets/[id]/quiz/generate/route.ts, src/app/api/study-sets/[id]/quiz/generate/route.test.ts</files>
<behavior>
- Workspace route returns 400 for invalid source selection, 422 for ineligible sources/model output, 402 for quota, and 200 only after snapshot persistence.
- Client cannot inject source markdown, checksums, storage paths, or role.
- Existing `/api/study-sets/[id]/quiz/generate` remains available via bridge, without reintroducing destructive behavior.
- Legacy adapter returns bridge set ID and preserves existing quota/session/mistake association for one quiz output.
</behavior>
<action>Add `POST /api/workspaces/[workspaceId]/outputs/quiz` with `requireApiUser`, Zod body containing canonical-version IDs and existing count options, quota handling, maxDuration, and stable errors. Per D-03, require explicit selection; client may submit visible latest-completed default only when no prior user selection exists. Test auth, JSON/Zod, source eligibility, quota, bridge-study-set allocation, typed failure, and exact success. Rewrite legacy study-set route only as narrow adapter resolving bridge output/workspace source and delegating to workspace service; preserve response shape plus quota/session/mistake compatibility. Do not delete route or permit mutable latest fallback after selection.</action>
<verify><automated>npm run test -- src/app/api/workspaces/[workspaceId]/outputs/quiz/route.test.ts src/app/api/study-sets/[id]/quiz/generate/route.test.ts --run</automated></verify>
<done>Workspace-native quiz API is authoritative while existing set-ID callers remain safe.</done>
</task>
<task type="auto">
<name>Task 3: Wire quiz source selection client</name>
<files>src/lib/client/quizGenerateStudySet.ts, src/components/quiz/QuizGenerateProgressCard.tsx</files>
<action>Change owned quiz generation UI/client contract from one implicit current canonical document to explicit completed canonical-version selection grouped by document. Require at least one selection; post IDs only; label each source version with version number, provenance, and date. Per D-03, preserve prior user selection; only if none exists, visibly preselect latest completed version as editable default. Preserve progress, quota 402, locale, and review navigation through returned bridge ID/output. Do not auto-merge, silently select latest after user choice, or add sharing controls.</action>
<verify><automated>npm run typecheck && npm run test -- src/lib/pipeline/multiSourceGenerate.test.ts src/app/api/workspaces/[workspaceId]/outputs/quiz/route.test.ts --run</automated></verify>
<done>Quiz UI submits explicit multi-source selection and cannot mutate provenance.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| client→output route | Untrusted source IDs and generation options |
| server→AI provider | Selected canonical text becomes model input |
| server→output RPC | Output/items/snapshots must be all-or-nothing |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-10 | Tampering/Disclosure | source selection | mitigate | Server validates common workspace, completion, active state, editor role |
| T-09-11 | Repudiation | output evidence | mitigate | Exact immutable snapshots in same RPC transaction |
| T-09-12 | Disclosure | AI provenance | mitigate | Redact secrets; client posts IDs only |
| T-09-SC | Tampering | npm installs | accept | No package installs |
</threat_model>
<verification>
- `npm run test -- src/lib/provenance/outputSnapshot.test.ts src/lib/pipeline/multiSourceGenerate.test.ts --run`
- `npm run typecheck`
</verification>
<success_criteria>WORK-06/07 quiz source selection and frozen evidence work without destroying existing output or routes.</success_criteria>
<output>Create `.planning/phases/09-workspace-centered-learning-canonical-provenance/09-04-SUMMARY.md` when done.</output>
