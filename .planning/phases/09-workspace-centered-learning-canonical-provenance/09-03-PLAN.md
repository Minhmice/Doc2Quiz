---
phase: 09-workspace-centered-learning-canonical-provenance
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - src/lib/pipeline/canonicalVersion.ts
  - src/lib/pipeline/canonicalVersion.test.ts
  - src/lib/workspaces/canonicalReader.ts
  - src/lib/workspaces/canonicalReader.test.ts
  - src/lib/client/canonicalReader.ts
  - src/app/api/workspaces/[workspaceId]/documents/[documentId]/versions/[documentVersionId]/canonicalize/route.ts
  - src/app/api/workspaces/[workspaceId]/canonical-versions/[versionId]/route.ts
  - src/app/api/workspaces/[workspaceId]/canonical-versions/[versionId]/sections/route.ts
  - src/app/api/workspaces/[workspaceId]/canonical-versions/**/*.test.ts
  - src/components/canonical/CanonicalMarkdownViewer.tsx
  - src/components/canonical/CanonicalSourceReview.tsx
autonomous: true
requirements: [WORK-04, WORK-05, WORK-08, WORK-09]
user_setup: []
must_haves:
  truths:
    - "Every canonical run appends a completed version with reproducibility metadata and checksums."
    - "Reader loads canonical section bodies in bounded ordered pages, never full document payload."
    - "Deleted source versions remain excluded from active selection while old output snapshots stay historical truth."
  artifacts:
    - {path: src/lib/pipeline/canonicalVersion.ts, provides: append-only canonicalization service, exports: [runCanonicalVersion]}
    - {path: src/app/api/workspaces/[workspaceId]/canonical-versions/[versionId]/sections/route.ts, provides: paginated section endpoint, exports: [GET]}
    - {path: src/components/canonical/CanonicalMarkdownViewer.tsx, provides: progressive section renderer}
  key_links:
    - {from: src/lib/pipeline/canonicalVersion.ts, to: persist_canonical_version, via: validated output and checksum payload, pattern: "persist_canonical_version"}
    - {from: src/components/canonical/CanonicalMarkdownViewer.tsx, to: /api/workspaces/:workspaceId/canonical-versions/:versionId/sections, via: paginated client fetch and IntersectionObserver, pattern: "IntersectionObserver"}
---
<objective>Append immutable canonical versions and replace full-document reader payloads with progressive, paginated sections.</objective>
<execution_context>@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-02-SUMMARY.md
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-RESEARCH.md
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-VALIDATION.md
@src/lib/pipeline/canonicalize.ts
@src/app/api/study-sets/[id]/canonical/route.ts
@src/components/canonical/CanonicalMarkdownViewer.tsx
@src/components/canonical/CanonicalSourceReview.tsx
<interfaces>
- `checksumCanonicalMarkdown(markdown)` and `checksumSections(sections)` from 09-01.
- Workspace document versions are immutable and carry raw markdown; canonical versions are selected by explicit UUID.
- `persist_canonical_version` accepts canonical metadata/provenance plus ordered section records and returns canonical version identity/count.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Implement append-only canonical-version service</name>
<files>src/lib/pipeline/canonicalVersion.ts, src/lib/pipeline/canonicalVersion.test.ts</files>
<behavior>
- Valid canonicalization appends canonical version N+1 for a document version and never calls mutable replacement RPC.
- Persisted provenance contains model, prompt version, parser version, generator settings, canonical checksum, section checksum, and no API secret.
- Rerun targets explicit document version and always creates another immutable canonical version; invalid/failed output creates no partial version.
- Version metadata exposes version number, provenance/model-prompt-parser identity, and creation date for UI labels.
</behavior>
<action>Reuse current prompt/AI/heuristic output validation from `canonicalize.ts`, but create dedicated workspace-native `runCanonicalVersion` service. Load one readable editable active document version; process raw markdown; compute both D-02 checksums; build redacted provenance including conversion/MarkItDown, mode/fallback reason, model identity, prompt/parser versions, generator settings, and timestamps. Call `persist_canonical_version` only after output validates. Per D-03, every rerun appends a new immutable canonical version, never updates/replaces prior canonical content. Do not update legacy canonical_documents or invoke `replace_canonical_content`; retain it exclusively for legacy adapter path. Test append request, checksum payload, metadata mapping, failure atomicity, and rerun behavior.</action>
<verify><automated>npm run test -- src/lib/pipeline/canonicalVersion.test.ts --run</automated></verify>
<done>Canonical provenance is complete, append-only, reproducible, and secret-free.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Create canonical version and bounded reader APIs</name>
<files>src/lib/workspaces/canonicalReader.ts, src/lib/workspaces/canonicalReader.test.ts, src/app/api/workspaces/[workspaceId]/documents/[documentId]/versions/[documentVersionId]/canonicalize/route.ts, src/app/api/workspaces/[workspaceId]/canonical-versions/[versionId]/route.ts, src/app/api/workspaces/[workspaceId]/canonical-versions/[versionId]/sections/route.ts, src/app/api/workspaces/[workspaceId]/canonical-versions/**/*.test.ts</files>
<behavior>
- Canonicalization route requires editor role and returns new canonical version ID/count.
- Metadata endpoint returns version number, provenance/model-prompt-parser identity, creation date, and section headings/ordinals only.
- Section endpoint accepts cursor ordinal and clamps limit to 1–50; ordered page response includes next cursor.
- Neither reader endpoint returns raw markdown, full canonical markdown, or all section bodies.
</behavior>
<action>Define reader request/response types and services. Add authenticated workspace routes with explicit workspace/version match and RLS access. Metadata route selects only version number, provenance-safe model/prompt/parser identity, creation date, status, checksum identifiers, and section index; UI must label this data. Slice route validates `afterOrdinal` and `limit`, filters ascending ordinals, returns one markdown-body page, and computes `nextAfterOrdinal`. Map Zod bad input to 400, inaccessible/deleted active resources to 404, expected canonical failures to 422/503. Test field selection, pagination/clamp/cursor, no-full-content payload, and author roles.</action>
<verify><automated>npm run test -- src/lib/workspaces/canonicalReader.test.ts src/app/api/workspaces/[workspaceId]/canonical-versions --run</automated></verify>
<done>Reader API prevents full-document mount and exposes only bounded canonical section pages.</done>
</task>
<task type="auto">
<name>Task 3: Render progressive canonical sections</name>
<files>src/lib/client/canonicalReader.ts, src/components/canonical/CanonicalMarkdownViewer.tsx, src/components/canonical/CanonicalSourceReview.tsx</files>
<action>Replace full canonical preview client model with metadata plus paginated `CanonicalSectionPage` fetcher. Reuse installed `react-markdown`/`remark-gfm` section rendering. Load first page, then fetch next through native `IntersectionObserver` sentinel with accessible explicit Load more fallback; dedupe in-flight cursor fetches, clamp retained page cache, and unmount far-away loaded section components rather than mounting entire document. Preserve headings, per-section anchors, errors, retry, and existing review actions. Do not add virtualization dependency or request full markdown.</action>
<verify><automated>npm run typecheck && npm run test -- src/lib/workspaces/canonicalReader.test.ts src/app/api/workspaces/[workspaceId]/canonical-versions --run</automated></verify>
<done>Canonical review renders progressively with bounded client memory and accessible fallback.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| client→canonical routes | IDs, page cursor, and canonicalization request |
| server→AI provider | Untrusted model output must not become provenance blindly |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-07 | Tampering | canonical append | mitigate | Validate model output before one append RPC; checksums and immutable versions |
| T-09-08 | Disclosure | canonical reader | mitigate | RLS membership, metadata/body field separation, 1–50 clamp |
| T-09-09 | Disclosure | provenance | mitigate | Explicit redaction; never persist provider API keys or secret URLs |
| T-09-SC | Tampering | npm installs | accept | No package installs |
</threat_model>
<verification>
- `npm run test -- src/lib/pipeline/canonicalVersion.test.ts src/lib/workspaces/canonicalReader.test.ts --run`
- `npm run typecheck`
</verification>
<success_criteria>WORK-04/05 append-only canonical versions and progressive reader work with member-only access.</success_criteria>
<output>Create `.planning/phases/09-workspace-centered-learning-canonical-provenance/09-03-SUMMARY.md` when done.</output>
