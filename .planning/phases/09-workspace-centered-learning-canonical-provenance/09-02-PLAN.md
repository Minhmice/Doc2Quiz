---
phase: 09-workspace-centered-learning-canonical-provenance
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/lib/workspaces/schemas.ts
  - src/lib/workspaces/createWorkspaceIngest.ts
  - src/lib/workspaces/documentVersions.ts
  - src/lib/workspaces/createWorkspaceIngest.test.ts
  - src/lib/workspaces/documentVersions.test.ts
  - src/lib/client/ingestWorkspace.ts
  - src/lib/client/workspaceApi.ts
  - src/app/api/workspaces/route.ts
  - src/app/api/workspaces/ingest/route.ts
  - src/app/api/workspaces/[workspaceId]/route.ts
  - src/app/api/workspaces/[workspaceId]/documents/[documentId]/route.ts
  - src/app/api/workspaces/[workspaceId]/documents/[documentId]/versions/route.ts
  - src/app/api/workspaces/**/*.test.ts
  - src/components/edit/new/import/UnifiedInputZone.tsx
autonomous: true
requirements: [WORK-01, WORK-03, WORK-08, WORK-09]
user_setup: []
must_haves:
  truths:
    - "First valid upload creates workspace, owner membership, document, and immutable version without title prompt."
    - "Workspace/document metadata changes never alter source version."
    - "Replacement appends a version; soft deletion hides source without deleting output evidence."
  artifacts:
    - {path: src/lib/workspaces/createWorkspaceIngest.ts, provides: validated server-owned first ingest, exports: [runWorkspaceIngest]}
    - {path: src/lib/workspaces/documentVersions.ts, provides: replacement and soft-delete services}
    - {path: src/app/api/workspaces/ingest/route.ts, provides: POST workspace ingest, exports: [POST]}
  key_links:
    - {from: src/components/edit/new/import/UnifiedInputZone.tsx, to: /api/workspaces/ingest, via: ingestWorkspace client, pattern: "ingestWorkspace"}
    - {from: src/lib/workspaces/createWorkspaceIngest.ts, to: create_workspace_document_version, via: authenticated RPC after validation, pattern: "create_workspace_document_version"}
---
<objective>
Move first upload and source lifecycle onto workspace-native, server-owned APIs while retaining legacy study-set routes as adapters.

Purpose: Deliver auto-created durable workspace and immutable document-version behavior.
Output: validated services, APIs, clients, import transition, route/service tests.
</objective>
<execution_context>@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-01-SUMMARY.md
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-RESEARCH.md
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-VALIDATION.md
@src/lib/pipeline/ingest.ts
@src/lib/client/ingestStudySet.ts
@src/app/api/study-sets/[id]/ingest/route.ts
@src/app/api/study-sets/[id]/route.ts
@src/components/edit/new/import/UnifiedInputZone.tsx
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Create workspace source contracts and services</name>
<files>src/lib/workspaces/schemas.ts, src/lib/workspaces/createWorkspaceIngest.ts, src/lib/workspaces/documentVersions.ts, src/lib/workspaces/createWorkspaceIngest.test.ts, src/lib/workspaces/documentVersions.test.ts</files>
<behavior>
- Invalid upload/paste/URL rejects before workspace RPC/storage work.
- First valid source derives workspace title and returns workspace/document/version IDs.
- Replacement creates version N+1; metadata edit touches only document/workspace metadata.
- Document/version soft delete never deletes learning output snapshots.
</behavior>
<action>Extract Zod contracts for ingest, rename, document metadata, replacement, and soft delete. Build workspace-native services from `ingest.ts` validation/conversion/storage behavior, with immutable path `{workspaceId}/{documentId}/{versionId}/...`; do not overload legacy `runIngest`. Call first-upload RPC only after boundary validation, derive title from filename/source, and return IDs plus conversion status. Keep failed conversion as explicit failed version, not empty workspace orphan. Add narrow legacy adapter documentation/exports that resolve `legacy_study_set_id`; do not delete study_sets or sessions/quota FK behavior. Unit-test validation order, title derivation, append semantics, metadata separation, and delete query contract.</action>
<verify><automated>npm run test -- src/lib/workspaces/createWorkspaceIngest.test.ts src/lib/workspaces/documentVersions.test.ts --run</automated></verify>
<done>Workspace source lifecycle is server-owned, immutable, and tested.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Expose authenticated workspace write APIs and tests</name>
<files>src/app/api/workspaces/route.ts, src/app/api/workspaces/ingest/route.ts, src/app/api/workspaces/[workspaceId]/route.ts, src/app/api/workspaces/[workspaceId]/documents/[documentId]/route.ts, src/app/api/workspaces/[workspaceId]/documents/[documentId]/versions/route.ts, src/app/api/workspaces/**/*.test.ts</files>
<behavior>
- Routes return 401 without `requireApiUser`, 400 for invalid Zod input, 404 for inaccessible active resources.
- POST ingest returns new workspace/document/version identity.
- PATCH rename/document metadata cannot submit source data; replacement only posts versions.
- DELETE performs soft delete and viewer writes fail.
</behavior>
<action>Create API handlers using `requireApiUser`, route schemas, stable `{ error, message? }` response contract, and typed service errors. Keep `runtime = "nodejs"` for conversion/storage. Implement `POST /api/workspaces/ingest`, workspace PATCH, document PATCH/DELETE, and document-version POST/DELETE. Ensure all authorization is membership/RLS-backed; client role claims never authorize. Add route tests with mocked services covering 401, malformed JSON/Zod 400, 404, viewer 403/404 according to policy abstraction, and exact success payloads. No invitation/share/public endpoints.</action>
<verify><automated>npm run test -- src/app/api/workspaces --run</automated></verify>
<done>Workspace lifecycle API has validated, authorized, stable contracts.</done>
</task>
<task type="auto">
<name>Task 3: Switch import client and input zone to workspace ingest</name>
<files>src/lib/client/ingestWorkspace.ts, src/lib/client/workspaceApi.ts, src/components/edit/new/import/UnifiedInputZone.tsx</files>
<action>Create client helpers mirroring current network-error normalization. Change `UnifiedInputZone` to call `POST /api/workspaces/ingest`, preserve existing local validation/progress/toasts, avoid pre-upload title UI, and redirect using returned workspace/document/version identity. Remove browser-side early study-set creation from owned flow. Preserve legacy clients/routes for existing routes through documented compatibility adapter; do not mass-refactor unrelated creation UI yet.</action>
<verify><automated>npm run typecheck && npm run test -- src/lib/workspaces src/app/api/workspaces --run</automated></verify>
<done>New upload creates workspace only after validation and leaves legacy route compatibility intact.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser→workspace ingest | Untrusted source payload and metadata |
| API→storage/RPC | Server selects immutable path and authorized transition |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-04 | Tampering | source replacement | mitigate | Separate replacement route/service and append-only RPC |
| T-09-05 | Elevation | workspace writes | mitigate | requireApiUser, Zod, RLS role authorization, route tests |
| T-09-06 | DoS | ingest | mitigate | Existing file/paste/URL validators run before storage/conversion |
| T-09-SC | Tampering | npm installs | accept | No package installs |
</threat_model>
<verification>
- `npm run test -- src/lib/workspaces src/app/api/workspaces --run`
- `npm run typecheck`
</verification>
<success_criteria>WORK-01/03/08 lifecycle works without destructive legacy migration or Phase 10 access features.</success_criteria>
<output>Create `.planning/phases/09-workspace-centered-learning-canonical-provenance/09-02-SUMMARY.md` when done.</output>
