---
phase: 09-workspace-centered-learning-canonical-provenance
plan: 09
type: execute
wave: 9
depends_on: [09-08]
files_modified:
  - supabase/tests/workspace_rls.sql
  - scripts/verify-phase9-workspace-bridge.mjs
  - package.json
autonomous: false
requirements: [WORK-01, WORK-02, WORK-03, WORK-04, WORK-05, WORK-06, WORK-07, WORK-08, WORK-09]
user_setup: []
must_haves:
  truths:
    - "Backfill and authorization matrix prove parent history remains immutable and bridge records never fall back to parent."
    - "Every WORK requirement has automated proof; no Phase 10 sharing surface is exposed."
  artifacts:
    - {path: scripts/verify-phase9-workspace-bridge.mjs, provides: static compatibility/deprecation audit}
    - {path: supabase/tests/workspace_rls.sql, provides: final migration, RLS, and bridge assertions}
  key_links:
    - {from: scripts/verify-phase9-workspace-bridge.mjs, to: src/app/api/study-sets, via: adapter-only audit, pattern: "resolveLegacyStudySetBridge"}
---
<objective>
Run final compatibility, migration, RLS, and user-facing Phase 9 gates.

Purpose: Prove workspace migration keeps provenance and legacy routes safe before any later legacy deletion.
Output: Static audit, reproducible automated gate, human verification checkpoint.
</objective>
<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-08-SUMMARY.md
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-RESEARCH.md
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-VALIDATION.md
@supabase/tests/workspace_rls.sql
@src/lib/workspaces/legacyBridge.ts
</context>
<tasks>
<task type="auto">
<name>Task 1: Add static compatibility audit and final SQL assertions</name>
<files>supabase/tests/workspace_rls.sql, scripts/verify-phase9-workspace-bridge.mjs, package.json</files>
<action>Extend local SQL tests with final migration backfill assertions: every legacy set has exactly one workspace/owner/document/version; canonical section count parity; each migrated generated output has frozen snapshot or explicit provenance exception; owner/editor/viewer/nonmember authorization matrix; soft-deleted source invisible but output snapshot readable to authorized member. Seed dual-mode parent quota/session/mistake fixtures and assert IDs, parent FKs, counts, and payloads stay unchanged after backfill; assert exactly two derived outputs/bridges retain same immutable `legacy_parent_study_set_id`; parent+quiz resolves only quiz child, parent+flashcards resolves only flashcard child; parent-resolution exposes parent-keyed historic records only; bridge resolutions return no parent records; bridge-keyed new history never falls back to parent; nonmember resolves neither ID. Add Node audit checking every retained legacy API adapter passes explicit kind into `resolveLegacyStudySetBridge` and rejects direct mutable `replace_canonical_content`, quiz replacement, or flashcard replacement semantics. Add `verify:phase9-workspace` script chaining static audit and focused bridge behavior tests only; no new dependency. Run local Supabase reset/test, Vitest, typecheck, lint, and build.</action>
<verify><automated>supabase db reset && supabase test db && npm run verify:phase9-workspace && npm run typecheck && npm run lint && npm run test -- --run && npm run build</automated></verify>
<done>Compatibility proves immutable parent history, kind-aware resolution, bridge-only new history, RLS denial, and reproducible full gates.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
<name>Task 2: Verify workspace provenance transition</name>
<files>Workspace routes and UI completed by Plans 09-01 through 09-09</files>
<action>Perform final user-facing verification after all automated gates pass; report failures without changing Phase 10 scope.</action>
<what-built>Workspace upload/replacement/canonical reader/multi-source output/dashboard transition with legacy set-ID bridge.</what-built>
<how-to-verify>
1. Start `npm run dev`, sign in, and upload a source from `/create`; verify no title prompt and derived workspace title.
2. Rename workspace and edit document metadata; replace source; verify title/metadata persist and new version appears.
3. Canonicalize twice, open each canonical version, scroll/load more; verify only progressive sections render.
4. Select two completed versions for quiz and flashcards; generate, then soft-delete a selected source; verify existing output still opens/studies.
5. Verify dashboard counts documents, canonical versions, quizzes, flashcards; verify legacy `/quiz/[setId]` and `/flashcard/[setId]` still open existing output.
6. Verify no invite/public-link/friend/anonymous controls appear.
</how-to-verify>
<resume-signal>Type "approved" or describe failures.</resume-signal>
<verify><automated>npm run verify:phase9-workspace</automated></verify>
<done>Manual workspace lifecycle and compatibility behavior is approved.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| migration/RLS test→database | Assertions guard historical data and role permissions |
| static audit→retained adapters | Source inspection detects compatibility bypasses |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-21 | Tampering | migration/backfill | mitigate | Cardinality, immutable-history, and bridge fallback SQL assertions |
| T-09-22 | Elevation | retained adapter authorization | mitigate | Static resolver audit plus four-principal RLS matrix |
| T-09-23 | Repudiation | historic output evidence | mitigate | Snapshot-or-exception assertions and soft-delete human study flow |
| T-09-SC | Tampering | npm installs | accept | No package installs |
</threat_model>
<verification>
- `supabase db reset && supabase test db`
- `npm run verify:phase9-workspace && npm run typecheck && npm run lint && npm run test -- --run && npm run build`
</verification>
<success_criteria>All WORK-01–09 requirements are proven with compatibility bridge retained; Phase 10 features remain absent.</success_criteria>
<output>Create `.planning/phases/09-workspace-centered-learning-canonical-provenance/09-09-SUMMARY.md` when done.</output>
