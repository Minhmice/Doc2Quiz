---
phase: 09-workspace-centered-learning-canonical-provenance
plan: 07
type: execute
wave: 7
depends_on: [09-06]
files_modified:
  - src/lib/workspaces/legacyBridge.ts
  - src/lib/workspaces/legacyBridge.test.ts
  - src/app/api/study-sets/[id]/route.ts
  - src/app/api/study-sets/[id]/canonical/route.ts
  - src/app/api/study-sets/[id]/ingest/route.ts
  - src/app/api/study-sets/[id]/canonicalize/route.ts
  - src/app/api/study-sets/[id]/quiz/generate/route.ts
  - src/app/api/study-sets/[id]/route.test.ts
  - src/app/api/study-sets/[id]/canonical/route.test.ts
  - src/app/api/study-sets/[id]/ingest/route.test.ts
  - src/app/api/study-sets/[id]/canonicalize/route.test.ts
  - src/app/api/study-sets/[id]/quiz/generate/route.test.ts
autonomous: true
requirements: [WORK-01, WORK-03, WORK-04, WORK-06, WORK-07, WORK-08, WORK-09]
user_setup: []
must_haves:
  truths:
    - "Legacy quiz set IDs resolve through deterministic kind-aware workspace/output bridges without duplicating historic quota, session, or mistake rows."
    - "Retained metadata, canonical, ingest, canonicalize, and quiz routes preserve their response contracts while using workspace-native services."
  artifacts:
    - {path: src/lib/workspaces/legacyBridge.ts, provides: kind-aware parent-or-bridge resolution adapter, exports: [resolveLegacyStudySetBridge]}
  key_links:
    - {from: src/app/api/study-sets/[id]/quiz/generate/route.ts, to: src/lib/workspaces/legacyBridge.ts, via: explicit quiz route kind, pattern: "resolveLegacyStudySetBridge"}
    - {from: src/app/api/study-sets/[id]/canonical/route.ts, to: src/lib/workspaces/legacyBridge.ts, via: explicit canonical route kind, pattern: "resolveLegacyStudySetBridge"}
---
<objective>
Harden retained non-flashcard legacy set-ID adapters around one deterministic bridge resolver.

Purpose: Preserve old quiz and source lifecycle URLs while workspace-native data becomes authoritative.
Output: Typed resolver, retained non-flashcard adapters, and route contract tests.
</objective>
<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-06-SUMMARY.md
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-RESEARCH.md
@.planning/phases/09-workspace-centered-learning-canonical-provenance/09-VALIDATION.md
@src/app/api/study-sets/[id]/route.ts
@src/app/api/study-sets/[id]/canonical/route.ts
@src/app/api/study-sets/[id]/ingest/route.ts
@src/app/api/study-sets/[id]/canonicalize/route.ts
@src/app/api/study-sets/[id]/quiz/generate/route.ts
<interfaces>
- Resolver signature: `resolveLegacyStudySetBridge({ studySetId, routeKind, userId })`.
- Parent IDs require explicit kind; bridge IDs return own output first and never inherit parent history.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Define and test legacy bridge resolver</name>
<files>src/lib/workspaces/legacyBridge.ts, src/lib/workspaces/legacyBridge.test.ts</files>
<behavior>
- Output-specific bridge ID resolves its own output and bridge-keyed quota/session/mistake records first; it never falls back to legacy parent history.
- Historic parent ID requires explicit route kind and resolves only matching derived child; historic records remain parent-keyed by parent-resolution lookup only.
- Unknown or inaccessible parent/bridge returns no cross-workspace or cross-kind result.
</behavior>
<action>Create narrowly typed `resolveLegacyStudySetBridge({ studySetId, routeKind, userId })`, scoped by authenticated membership/RLS. Its contract is deterministic: bridge ID returns only its bridge/output and bridge-keyed history with no parent fallback; historic parent requires `routeKind`, selects matching child, retains immutable `legacy_parent_study_set_id`, and exposes historic quota/session/mistake records through parent lookup only. Reject unknown, inaccessible, cross-kind, or cross-workspace IDs. Do not duplicate/rekey historic records, repoint FKs, add redirects, or create Phase 10 access paths.</action>
<verify><automated>npm run test -- src/lib/workspaces/legacyBridge.test.ts --run</automated></verify>
<done>Shared resolver preserves immutable parent history and deterministic kind-aware bridge behavior.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Migrate retained non-flashcard adapters</name>
<files>src/app/api/study-sets/[id]/route.ts, src/app/api/study-sets/[id]/canonical/route.ts, src/app/api/study-sets/[id]/ingest/route.ts, src/app/api/study-sets/[id]/canonicalize/route.ts, src/app/api/study-sets/[id]/quiz/generate/route.ts, src/app/api/study-sets/[id]/route.test.ts, src/app/api/study-sets/[id]/canonical/route.test.ts, src/app/api/study-sets/[id]/ingest/route.test.ts, src/app/api/study-sets/[id]/canonicalize/route.test.ts, src/app/api/study-sets/[id]/quiz/generate/route.test.ts</files>
<behavior>
- Each retained adapter preserves current response DTO and authorization behavior.
- Quiz parent resolution selects quiz output only; no adapter writes mutable canonical content or destructive replacement output.
- Authorized snapshot study remains possible after source soft delete.
</behavior>
<action>Refactor retained metadata, canonical read, ingest, canonicalize, and quiz-generation handlers to pass explicit route kind and delegate to resolver/workspace-native services. Retain exact response shapes for existing setId practice/session/quota flows. Remove direct mutable canonical-document and destructive replacement calls from these adapters. Add one route behavior test per retained adapter covering 401, inaccessible 404, legacy DTO, bridge resolution, parent kind selection, unchanged historic quota/session/mistake fixtures, no duplicate parent records, bridge no-fallback, source-selection policy, and snapshot study after source soft delete. Do not touch flashcard adapter; 09-08 owns its focused retained-adapter contract.</action>
<verify><automated>npm run test -- src/lib/workspaces/legacyBridge.test.ts src/app/api/study-sets/[id]/route.test.ts src/app/api/study-sets/[id]/canonical/route.test.ts src/app/api/study-sets/[id]/ingest/route.test.ts src/app/api/study-sets/[id]/canonicalize/route.test.ts src/app/api/study-sets/[id]/quiz/generate/route.test.ts --run</automated></verify>
<done>Non-flashcard set-ID compatibility is explicit, safe, and backed by focused adapter tests.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| legacy route→workspace bridge | Preserved external set ID resolves workspace-native records |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-17 | Tampering | legacy adapters | mitigate | Central bridge resolver and tests forbidding mutable/destructive transitions |
| T-09-18 | Elevation | bridge lookup | mitigate | Authenticated membership/RLS scope and inaccessible-ID tests |
| T-09-SC | Tampering | npm installs | accept | No package installs |
</threat_model>
<verification>
- `npm run test -- src/lib/workspaces/legacyBridge.test.ts src/app/api/study-sets/[id]/route.test.ts src/app/api/study-sets/[id]/canonical/route.test.ts src/app/api/study-sets/[id]/ingest/route.test.ts src/app/api/study-sets/[id]/canonicalize/route.test.ts src/app/api/study-sets/[id]/quiz/generate/route.test.ts --run`
</verification>
<success_criteria>Retained non-flashcard set-ID adapters preserve stable contracts without parent-history mutation or Phase 10 scope.</success_criteria>
<output>Create `.planning/phases/09-workspace-centered-learning-canonical-provenance/09-07-SUMMARY.md` when done.</output>
