---
phase: 33-adopt-a-vector-store-matched-to-your-scale
plan: 03
type: execute
wave: 2
depends_on: ["33-02"]
files_modified:
  - src/components/ai/AiParseSection.tsx
  - src/lib/ai/runSequentialParse.ts
  - src/lib/ai/parseChunk.ts
  - src/components/ai/RagChunkSearchPanel.tsx
autonomous: true
requirements:
  - "RAG-33-05"
  - "RAG-33-06"
  - "RAG-33-07"
  - "RAG-33-08"

must_haves:
  truths:
    - "Visible search UI lists ranked chunks; user selects inject."
    - "Injection adds explicit labeled context block to text parse — not silent."
    - "Chunk display uses plain text / existing safe patterns."
---

<objective>
Ship **RAG-33-05..08**: **semantic search panel** (Mint-aligned, compact), **injection** into **`runSequentialParse`** via optional **`ragContextPrefix`** (or equivalent) on **`parseChunkOnce`** params, **safe rendering**, and **no change** to review approval rules.

**Injection contract (pick one, document):**

- **`ragContextPrefix: string`** prepended once per chunk in `parseChunkOnce` as:

  `[RAG context]\n{prefix}\n\n---\n\n{chunkText}`

  User toggles “Use RAG context” and prefix comes from selected search hits (concat top-k with separators).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/references/ui-brand.md
</execution_context>

<context>
@.planning/phases/33-adopt-a-vector-store-matched-to-your-scale/33-CONTEXT.md
@src/components/ai/AiParseSection.tsx
@src/lib/ai/runSequentialParse.ts
@src/lib/ai/parseChunk.ts
</context>

<threat_model>
- **XSS:** search results render as **text nodes** or escaped; no HTML injection from embeddings DB.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: ParseChunkOnceParams + runSequentialParse</name>
  <files>src/lib/ai/parseChunk.ts, src/lib/ai/runSequentialParse.ts</files>
  <action>
Add optional **`ragContextPrefix?: string`** to **`ParseChunkOnceParams`**.

Inside **`parseChunkOnce`**, when calling the draft LLM, use **effectiveChunkText** = prefix + delimiter + `chunkText` when prefix non-empty (document delimiter constant).

**Thread** through **`runSequentialParse`** options: `ragContextPrefix?: string`.

**Cache note:** `contentFingerprint` for **draft** parse must include **hash of ragContextPrefix** when present (extend fingerprint input) — **RAG-33-03** spirit: no false parse-cache hits when RAG prefix changes.
  </action>
  <verify><automated>npm run lint</automated></verify>
</task>

<task type="auto">
  <name>Task 2: RagChunkSearchPanel UI</name>
  <files>src/components/ai/RagChunkSearchPanel.tsx, src/components/ai/AiParseSection.tsx</files>
  <action>
New **client component**: query input, **Search** button, results list (score, snippet **truncated**, `sourceLabel`), **multi-select** or “Add to context” accumulating a **preview** of injected block.

**RAG-33-07:** use `<pre className=...>` or tailwind `whitespace-pre-wrap` **without** HTML from chunks.

Mount panel in **`AiParseSection`** (collapsible section) when `parseOutputMode === "quiz"` and index exists or “Build index” CTA.

**RAG-33-08:** copy: “Suggestions only — review questions before saving.”
  </action>
  <verify><automated>npm run lint</automated></verify>
</task>

<task type="auto">
  <name>Task 3: Wire injection + verify</name>
  <files>src/components/ai/AiParseSection.tsx</files>
  <action>
Pass **`ragContextPrefix`** from panel state into **`runSequentialParse`** calls (all sites updated in Phase 32).

**npm run lint** + **npm run build**.
  </action>
  <verify>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
  </verify>
</task>

</tasks>
