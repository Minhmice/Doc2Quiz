---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - src/lib/pipeline/validation.ts
  - src/lib/pipeline/validation.test.ts
autonomous: true
requirements:
  - INPUT-VAL-01
must_haves:
  truths:
    - "Supported MIME types for all pipeline input formats are exported as constants"
    - "Per-format max byte limits are exported for Phase 2 enforcement"
    - "Paste and YouTube input discriminated types exist alongside file input type"
  artifacts:
    - path: src/lib/pipeline/validation.ts
      provides: "INPUT-VAL-01 contract"
      exports: ["SUPPORTED_MIME_TYPES", "MAX_UPLOAD_BYTES_BY_MIME"]
    - path: src/lib/pipeline/validation.test.ts
      provides: "automated contract tests"
      contains: "SUPPORTED_MIME_TYPES"
  key_links:
    - from: src/lib/pipeline/validation.ts
      to: docs/pipeline.md
      via: "MIME list matches Accept section"
      pattern: "application/pdf"
---

<objective>
Ship the shared input validation contract (types + constants) for the MVP pipeline so Phase 2 can enforce type/size checks without redefining allowlists (D-19, INPUT-VAL-01).

Purpose: Validation enforcement is Phase 2; Phase 1 exports the authoritative contract aligned to `docs/pipeline.md`.
Output: `src/lib/pipeline/validation.ts` + unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/pipeline.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md

<interfaces>
MIME allowlist from docs/pipeline.md Accept section:
PDF, DOCX, PPTX, XLSX/XLS, JPG/JPEG/PNG, WAV/MP3, HTML, CSV, JSON, XML, plain text, pasted text, YouTube URL.

Export from validation.ts (no enforcement functions in Phase 1):
- `SUPPORTED_MIME_TYPES` readonly array + `SupportedMimeType` union
- `MAX_UPLOAD_BYTES_BY_MIME` Record with conservative defaults [ASSUMED]: PDF 50MB, Office 25MB, images 15MB, audio 100MB, text-like 10MB — document assumptions in file comment
- `PasteInput` { kind: "paste"; text: string }
- `YoutubeInput` { kind: "youtube"; url: string }
- `FileInput` { kind: "file"; mimeType: SupportedMimeType; sizeBytes: number }
- `PipelineInput` discriminated union of the three

Pattern reference: git `src/lib/pdf/validatePdfFile.ts` for ok/false result style — do NOT implement validate functions yet unless trivial type guards for tests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validation contract tests (RED then GREEN)</name>
  <read_first>
    - docs/pipeline.md (Accept list)
    - .planning/phases/01-foundation/01-RESEARCH.md (validation sketch)
    - package.json (check test runner — use npx vitest if not configured)
  </read_first>
  <behavior>
    - SUPPORTED_MIME_TYPES includes application/pdf and at least 10 distinct MIME strings
    - MAX_UPLOAD_BYTES_BY_MIME has an entry for every SUPPORTED_MIME_TYPES member with value &gt; 0
    - PipelineInput union accepts paste, youtube, and file discriminants
    - MIME list includes image/jpeg, audio/mpeg, text/plain
  </behavior>
  <files>src/lib/pipeline/validation.test.ts</files>
  <action>
    If no test runner exists, add minimal Vitest devDependency and `npm test` script (vitest only — no e2e). Write failing tests for behaviors above. Tests import from `@/lib/pipeline/validation`.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/pipeline/validation.test.ts 2>&1; if ($LASTEXITCODE -eq 0) { Write-Error "RED phase: tests must fail before validation.ts exists"; exit 1 }</automated>
  </verify>
  <done>Tests exist; they fail until Task 2 implements exports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement validation.ts contract</name>
  <read_first>
    - src/lib/pipeline/validation.test.ts
    - docs/pipeline.md
  </read_first>
  <files>src/lib/pipeline/validation.ts</files>
  <action>
    Implement D-19 contract in `src/lib/pipeline/validation.ts`:

    1. `SUPPORTED_MIME_TYPES` covering every file format in pipeline.md (map XLS → application/vnd.ms-excel, DOCX/PPTX/XLSX to correct Office MIME types).
    2. `MAX_UPLOAD_BYTES_BY_MIME` with conservative per-format limits; comment `[ASSUMED]` on each default.
    3. Discriminated unions `PasteInput`, `YoutubeInput`, `FileInput`, `PipelineInput`.
    4. Optional: `isSupportedMimeType(mime: string): mime is SupportedMimeType` for tests — no upload validation logic.

    Do NOT add route handlers or MarkItDown calls. Enforcement deferred to Phase 2 per CONTEXT.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/pipeline/validation.test.ts</automated>
  </verify>
  <done>All validation tests pass; module exports contract without side effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API (Phase 2) | Constants only in Phase 1; no runtime enforcement yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | Oversized uploads | transfer | Phase 2 enforces MAX_UPLOAD_BYTES_BY_MIME at ingest route |
| T-01-SC | Tampering | vitest install | mitigate | Only add vitest if missing; verify package on npm before install |
</threat_model>

<verification>
- `npm test` passes for validation.test.ts
- `npm run typecheck` passes
- Every MIME in pipeline.md Accept file formats appears in SUPPORTED_MIME_TYPES
</verification>

<success_criteria>
- INPUT-VAL-01 contract exported (enforcement explicitly Phase 2)
- D-19 complete
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
