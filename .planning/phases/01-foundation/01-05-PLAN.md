---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/types/studySet.ts
  - src/lib/client/studySetDb.ts
  - src/lib/ui/studySetActionLabels.ts
  - src/app/(app)/layout.tsx
  - src/app/(auth)/logout/route.ts
  - src/components/layout/AppTopBar.tsx
autonomous: false
requirements:
  - CORE-AUTH-01
  - CORE-AUTH-02
  - CANON-09
must_haves:
  truths:
    - "Unauthenticated users hitting (app) routes redirect to /login?next= with safe path"
    - "User can log in with email/password and reach dashboard"
    - "Log out clears server session and user cannot access protected routes"
    - "Dashboard study set list reads real Supabase data (not empty stub)"
    - "StudySet types use pipeline_stage not draft/ready status"
  artifacts:
    - path: src/app/(app)/layout.tsx
      provides: "protected layout guard"
      contains: "requireUser"
    - path: src/app/(auth)/logout/route.ts
      provides: "POST signOut + redirect"
      contains: "signOut"
    - path: src/lib/client/studySetDb.ts
      provides: "real listStudySetMetas against study_sets"
      contains: "from(\"study_sets\")"
  key_links:
    - from: src/app/(app)/layout.tsx
      to: src/lib/supabase/auth-guard.ts
      via: "await requireUser()"
      pattern: "requireUser"
    - from: src/components/layout/AppTopBar.tsx
      to: /logout
      via: "POST form submit or fetch"
      pattern: "/logout"
    - from: src/lib/client/studySetDb.ts
      to: study_sets
      via: "browser Supabase client"
      pattern: "pipeline_stage"
---

<objective>
Wire auth UI and client data layer: protected app shell, real logout, and Supabase-backed study set metadata queries aligned to v2.1 schema (CORE-AUTH-01, CORE-AUTH-02, CANON-09 client path).

Purpose: Completes Foundation user-visible auth loop per UI-SPEC; replaces stub studySetDb for dashboard.
Output: requireUser layout, logout route, AppTopBar fix, pipeline_stage types, real studySetDb.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-PATTERNS.md
@src/app/(auth)/login/LoginClient.tsx
@src/lib/client/studySetDb.ts
@src/types/studySet.ts

<interfaces>
UI-SPEC requirements (preserve markup):
- LoginClient/SignupClient: no visual changes; real client from 01-02
- AppTopBar logout: POST to /logout (not router.replace("/login") alone)
- (app)/layout: async + requireUser before AppProviders (D-14)
- Logout: POST handler calls signOut, redirect /login with ?next= sanitization (git HEAD pattern)

studySetDb (adapt git HEAD src/lib/db/studySetDb.ts):
- listStudySetMetas, getStudySetMeta, createStudySetEarlyMeta, putStudySetMeta, deleteStudySet
- Select pipeline_stage, content_kind — NOT status, NOT OCR/PDF columns
- requireUserId via browser client getUser()
- Keep approved bank stubs returning empty until Phase 4/5 OR minimal no-op if dashboard needs them

Types (src/types/studySet.ts):
- Replace StudySetStatus with PipelineStage union
- StudySetMeta: pipelineStage, drop sourceFileName from required fields (optional in metadata later)
- Replace StudySetDocumentRecord with CanonicalDocumentSummary stub or deprecate with comment — canonical detail in Phase 3
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: v2.1 study set types + studySetDb</name>
  <read_first>
    - git show HEAD:src/lib/db/studySetDb.ts
    - src/types/studySet.ts
    - src/lib/ui/studySetActionLabels.ts
    - .planning/phases/01-foundation/01-PATTERNS.md (studySetDb + types sections)
  </read_first>
  <files>
    src/types/studySet.ts
    src/lib/client/studySetDb.ts
    src/lib/ui/studySetActionLabels.ts
  </files>
  <action>
  1. Update `src/types/studySet.ts` per D-09: `PipelineStage` type; `StudySetMeta` with `pipelineStage` and `contentKind`; remove `StudySetStatus` draft/ready. Update or narrow `StudySetDocumentRecord` for forward compatibility (canonical fields deferred).

  2. Replace stub `src/lib/client/studySetDb.ts` with real Supabase queries via `createSupabaseBrowserClient` from `@/lib/client/supabase`:
     - Implement listStudySetMetas, getStudySetMeta, putStudySetMeta, deleteStudySet, createStudySetEarlyMeta (insert study_sets with pipeline_stage `input`)
     - Map DB snake_case to camelCase meta fields
     - Keep getApprovedBank / flashcard bank functions as empty-bank stubs if still imported by dashboard (no DB tables populated in Phase 1)

  3. Update `studySetActionLabels.ts` to label `pipeline_stage` values instead of draft/ready (discretion: human-readable labels for input/raw/canonical/etc.).

  Fix any TypeScript errors in components importing old `status` field.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Types use pipelineStage; studySetDb queries study_sets table; no references to StudySetStatus draft/ready in type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Protected layout, logout route, AppTopBar wiring</name>
  <read_first>
    - git show HEAD:src/app/(auth)/logout/route.ts
    - git show HEAD:src/app/(app)/layout.tsx
    - .planning/phases/01-foundation/01-UI-SPEC.md (logout + protected shell)
    - src/components/layout/AppTopBar.tsx
  </read_first>
  <files>
    src/app/(app)/layout.tsx
    src/app/(auth)/logout/route.ts
    src/components/layout/AppTopBar.tsx
  </files>
  <action>
  1. `(app)/layout.tsx`: make async default export; `await requireUser()` from `@/lib/supabase/auth-guard` before rendering `AppProviders` (D-14). No intermediate "please log in" page.

  2. Restore `src/app/(auth)/logout/route.ts` POST handler: createSupabaseServerClient → signOut → redirect 303 to sanitized `/login` (open-redirect guard on ?next=). Per UI-SPEC use POST (not GET).

  3. `AppTopBar.tsx`: change Log out menu item to trigger server logout — hidden form POST to `/logout` or fetch POST with redirect follow (CORE-AUTH-02). Preserve menu copy "Log out". No confirmation dialog.

  4. LoginClient/SignupClient: verify imports resolve to real client (no mock errors); do not change UI markup (D-13 email/password only).

  No dev backdoor (D-15).
  </action>
  <verify>
    <automated>npm run typecheck && powershell -Command "Select-String -Path src/app/(app)/layout.tsx -Pattern 'requireUser' -Quiet; Select-String -Path src/app/(auth)/logout/route.ts -Pattern 'signOut' -Quiet"</automated>
  </verify>
  <done>Layout guards auth; logout route signs out server-side; AppTopBar hits /logout not /login alone.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Auth smoke test + build gate</name>
  <read_first>
    - .env.example (Supabase URL + anon key required)
    - supabase/EMAIL_AUTH_SETUP.md if signup fails
  </read_first>
  <files>package.json</files>
  <action>
    Run automated build gate first: `npm run build` must pass before human smoke test.

    Human verification (CORE-AUTH-01, CORE-AUTH-02): requires `.env.local` with Supabase credentials. If testing against hosted DB, user must manually apply `20260725120000_v21_baseline.sql` per D-02 (no remote push in Phase 1).

    Steps for human: unauthenticated `/dashboard` redirects to login; email/password sign-in reaches dashboard; navigation preserves session; Log out clears session and blocks `/dashboard`. Resume when approved or report failures.
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>
      1. `.env.local` configured with NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY.
      2. Schema applied manually if using hosted Supabase.
      3. `npm run dev` — `/dashboard` unauthenticated → `/login?next=...`.
      4. Sign in → dashboard loads; navigate within (app) → still authenticated.
      5. Log out → `/login`; `/dashboard` redirects to login again.
    </human-check>
  </verify>
  <done>Build green; human confirms full auth loop per steps above.</done>
  <what-built>
    Full auth loop: login → protected dashboard → logout → blocked re-entry.
  </what-built>
  <how-to-verify>
    See human-check steps in verify block.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (app) routes | Server requireUser before any shell HTML |
| Logout | Server signOut clears httpOnly cookies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Spoofing | Client-only logout | mitigate | AppTopBar POST /logout with server signOut (CORE-AUTH-02) |
| T-01-12 | Spoofing | Open redirect | mitigate | logout + requireUser sanitize ?next= paths |
| T-01-13 | Spoofing | Session fixation | mitigate | proxy refresh + server signOut on logout |
</threat_model>

<verification>
- `npm run typecheck` and `npm run build` pass
- Manual auth smoke (Task 3) approved
- studySetDb returns data when sets exist in DB
</verification>

<success_criteria>
- CORE-AUTH-01 and CORE-AUTH-02 satisfied
- D-12, D-13, D-14, D-15 implemented in UI wiring
- CANON-09 client path reads study_sets; canonical_documents queried in Phase 3
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-05-SUMMARY.md` when done
</output>
