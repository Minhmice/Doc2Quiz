---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/lib/api/requireApiUser.ts
  - src/app/api/study-sets/route.ts
  - src/app/api/study-sets/[id]/route.ts
  - src/app/api/study-sets/[id]/ingest/route.ts
  - src/app/api/study-sets/[id]/canonicalize/route.ts
  - src/app/api/study-sets/[id]/quiz/generate/route.ts
  - src/app/api/study-sets/[id]/flashcards/generate/route.ts
  - src/app/api/study-sets/[id]/generate-from-file/route.ts
  - src/app/api/study-sets/[id]/generation-debug/route.ts
autonomous: true
requirements:
  - CANON-09
  - CORE-AUTH-01
must_haves:
  truths:
    - "Authenticated user can list and create study sets via API"
    - "Authenticated user can GET/PATCH/DELETE a study set by id"
    - "Pipeline step endpoints return structured 501 stubs until Phases 2–5"
    - "Unauthenticated API calls receive 401"
    - "Legacy v1 study-set API routes are removed or unreachable"
  artifacts:
    - path: src/lib/api/requireApiUser.ts
      provides: "shared API auth guard"
      exports: ["requireApiUser"]
    - path: src/app/api/study-sets/route.ts
      provides: "GET list, POST create"
      exports: ["GET", "POST"]
    - path: src/app/api/study-sets/[id]/ingest/route.ts
      provides: "ingest stub"
      contains: "not_implemented"
  key_links:
    - from: src/app/api/study-sets/route.ts
      to: src/lib/api/requireApiUser.ts
      via: "requireApiUser() before queries"
      pattern: "requireApiUser"
    - from: src/app/api/study-sets/route.ts
      to: study_sets table
      via: "supabase.from('study_sets')"
      pattern: "from\\(\"study_sets\"\\)"
---

<objective>
Scaffold step-based pipeline API routes with authenticated CRUD for study sets and structured 501 stubs for ingest/canonicalize/generate steps (D-16, D-17, D-18).

Purpose: Mirrors `docs/pipeline.md` flow; later phases replace stubs without renaming routes.
Output: `src/app/api/study-sets/**` route map + `requireApiUser` helper; legacy v1 routes removed.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/pipeline.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-PATTERNS.md
@src/lib/supabase/server.ts

<interfaces>
requireApiUser (create in this plan):
- Returns `{ supabase, user }` or `{ error: NextResponse 401 }`
- Uses createSupabaseServerClient + getUser()

Route map D-17:
- GET/POST `/api/study-sets` — list user's sets ordered by updated_at; POST inserts row with pipeline_stage `input`, title from JSON body (default "Untitled study set"), user_id from auth
- GET/PATCH/DELETE `/api/study-sets/[id]` — scoped by RLS; PATCH allows title, subtitle, content_kind, pipeline_stage (no transition validation yet)
- POST stubs (501 JSON): ingest, canonicalize, quiz/generate, flashcards/generate

Stub body shape (D-16):
{ error: "not_implemented", step: "<step>", studySetId: id, message: "<phase hint>" }

Next.js 16 params: `ctx: { params: Promise<{ id: string }> }` — await ctx.params.

Remove legacy: `generate-from-file/route.ts`, `generation-debug/route.ts` (deferred ideas / not v2.1 pipeline).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: requireApiUser + study-sets CRUD routes</name>
  <read_first>
    - git show HEAD:src/app/api/study-sets/[id]/generate-from-file/route.ts (inline auth pattern)
    - .planning/phases/01-foundation/01-PATTERNS.md (requireApiUser + CRUD routes)
    - src/lib/supabase/server.ts
  </read_first>
  <files>
    src/lib/api/requireApiUser.ts
    src/app/api/study-sets/route.ts
    src/app/api/study-sets/[id]/route.ts
  </files>
  <action>
  1. Create `src/lib/api/requireApiUser.ts` per RESEARCH Pattern 2 (D-18).

  2. `src/app/api/study-sets/route.ts`:
     - GET: requireApiUser → select id, title, subtitle, pipeline_stage, content_kind, created_at, updated_at from study_sets for user.id
     - POST: parse JSON `{ title?: string, subtitle?: string }`; insert study_sets with pipeline_stage `input`; return created row

  3. `src/app/api/study-sets/[id]/route.ts`:
     - GET: single row or 404
     - PATCH: allowed fields only; return updated row
     - DELETE: delete row; return 204 or { ok: true }

  All handlers: 401 from requireApiUser; 500 on Supabase errors with message; 400 on invalid JSON.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>CRUD route files compile; every handler starts with requireApiUser.</done>
</task>

<task type="auto">
  <name>Task 2: Pipeline step stubs + legacy route cleanup</name>
  <read_first>
    - docs/pipeline.md
    - .planning/phases/01-foundation/01-CONTEXT.md (D-16, D-17, deferred legacy routes)
  </read_first>
  <files>
    src/app/api/study-sets/[id]/ingest/route.ts
    src/app/api/study-sets/[id]/canonicalize/route.ts
    src/app/api/study-sets/[id]/quiz/generate/route.ts
    src/app/api/study-sets/[id]/flashcards/generate/route.ts
    src/app/api/study-sets/[id]/generate-from-file/route.ts
    src/app/api/study-sets/[id]/generation-debug/route.ts
  </files>
  <action>
  1. Create POST handlers for ingest, canonicalize, quiz/generate, flashcards/generate:
     - requireApiUser first (D-18)
     - Optional: verify study set exists for id (404 if missing)
     - Return 501 JSON stub per D-16 with distinct `step` and phase message

  2. Delete `generate-from-file/route.ts` and `generation-debug/route.ts` (v1 pipeline; not in D-17 route map).

  3. Grep `src/app/api` for other v1 pipeline routes referencing dropped tables — delete or leave only if unrelated to study-set pipeline; document removals in SUMMARY.
  </action>
  <verify>
    <automated>npm run typecheck && powershell -Command "@('ingest','canonicalize','quiz/generate','flashcards/generate') | ForEach-Object { if (-not (Test-Path \"src/app/api/study-sets/[id]/$_/route.ts\")) { exit 1 } }; if (Test-Path 'src/app/api/study-sets/[id]/generate-from-file/route.ts') { exit 1 }"</automated>
  </verify>
  <done>Four stub routes exist with 501 responses; legacy generate-from-file and generation-debug removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/study-sets/** | Untrusted JSON; auth required on every handler |
| API → Postgres | RLS second layer after requireApiUser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Spoofing | API routes | mitigate | requireApiUser on every handler; RLS on study_sets |
| T-01-09 | Tampering | PATCH study set | mitigate | Allowlist fields; RLS prevents cross-user updates |
| T-01-10 | Information disclosure | IDOR | mitigate | RLS user_id = auth.uid(); 404 when row not visible |
</threat_model>

<verification>
- `npm run typecheck` passes
- Stub routes return 501 with `not_implemented` key
- Legacy generate-from-file route absent
</verification>

<success_criteria>
- D-16, D-17, D-18 implemented
- CANON-09 API layer can create/list study sets (canonical rows created in ingest Phase 2)
- CORE-AUTH-01 API auth gate in place
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done
</output>
