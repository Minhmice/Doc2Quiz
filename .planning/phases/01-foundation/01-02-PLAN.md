---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/lib/supabase/env.ts
  - src/lib/supabase/browser.ts
  - src/lib/supabase/server.ts
  - src/lib/supabase/middlewareClient.ts
  - src/lib/supabase/auth-guard.ts
  - src/lib/client/supabase.ts
  - src/proxy.ts
autonomous: true
requirements:
  - CORE-AUTH-01
must_haves:
  truths:
    - "Supabase SSR client trio exists and uses project env var names"
    - "Every matched navigation refreshes auth cookies via proxy.ts"
    - "x-next-pathname header is injected for deep-link login redirects"
  artifacts:
    - path: src/lib/supabase/server.ts
      provides: "createSupabaseServerClient for RSC and route handlers"
      exports: ["createSupabaseServerClient"]
    - path: src/lib/supabase/middlewareClient.ts
      provides: "updateSession for proxy"
      exports: ["updateSession"]
    - path: src/proxy.ts
      provides: "Next.js 16 session refresh entry"
      contains: "updateSession"
  key_links:
    - from: src/proxy.ts
      to: src/lib/supabase/middlewareClient.ts
      via: "updateSession(request)"
      pattern: "updateSession"
    - from: src/lib/client/supabase.ts
      to: src/lib/supabase/browser.ts
      via: "re-export createSupabaseBrowserClient"
      pattern: "createSupabaseBrowserClient"
---

<objective>
Restore real Supabase SSR clients and wire Next.js 16 `proxy.ts` for session cookie refresh so authenticated sessions persist across navigation (CORE-AUTH-01 infrastructure).

Purpose: v2.0 strip left mocks and passthrough proxy; auth cannot work without this layer (D-12, D-15).
Output: `src/lib/supabase/*`, real `src/lib/client/supabase.ts` re-export, wired `src/proxy.ts`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@src/lib/client/supabase.ts
@src/proxy.ts
@.env.example

<interfaces>
Recover from git HEAD (`git show HEAD:src/lib/supabase/*.ts`) and adapt:

env.ts — `getSupabaseUrl()`, `getSupabaseAnonKey()` throw if `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` missing (.env.example names).

browser.ts — `createBrowserClient` from `@supabase/ssr`.

server.ts — `createServerClient` with `cookies()` from `next/headers`; swallow `setAll` errors in RSC (proxy handles refresh).

middlewareClient.ts — `updateSession(request)`:
- Inject `x-next-pathname` header on request (preserve v1 deep-link behavior).
- Use `await supabase.auth.getClaims()` instead of v1 `getUser()` per RESEARCH.
- Sync cookies via `setAll` on `NextResponse.next`.

auth-guard.ts — `requireUser()` uses `getUser()`, reads `x-next-pathname`, redirects to `/login?next=` with open-redirect guard (`startsWith("/")` and not `//`).

proxy.ts — export async function `proxy` (not `middleware`); call `updateSession`; keep existing matcher config.

client/supabase.ts — replace mock with: `export { createSupabaseBrowserClient } from "@/lib/supabase/browser"`.

No dev backdoor env flags (D-15). No `src/middleware.ts` — proxy only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore Supabase client modules</name>
  <read_first>
    - git show HEAD:src/lib/supabase/env.ts
    - git show HEAD:src/lib/supabase/browser.ts
    - git show HEAD:src/lib/supabase/server.ts
    - git show HEAD:src/lib/supabase/middlewareClient.ts
    - git show HEAD:src/lib/supabase/auth-guard.ts
    - .planning/phases/01-foundation/01-PATTERNS.md (supabase/* sections)
  </read_first>
  <files>
    src/lib/supabase/env.ts
    src/lib/supabase/browser.ts
    src/lib/supabase/server.ts
    src/lib/supabase/middlewareClient.ts
    src/lib/supabase/auth-guard.ts
  </files>
  <action>
    Create `src/lib/supabase/` with five modules per D-12, restoring v1 patterns from git HEAD where available.

    Required exports:
    - `env.ts`: `getSupabaseUrl`, `getSupabaseAnonKey`
    - `browser.ts`: `createSupabaseBrowserClient`
    - `server.ts`: `createSupabaseServerClient` (async)
    - `middlewareClient.ts`: `updateSession` — adapt to `getClaims()`; preserve `x-next-pathname` injection
    - `auth-guard.ts`: `requireUser` — async; open-redirect safe `?next=` per PATTERNS

    Do not add service-role key usage. Email/password only (D-13) — no OAuth helpers here.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>All five modules exist, compile, and export documented functions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire proxy and replace client mock</name>
  <read_first>
    - src/proxy.ts
    - src/lib/client/supabase.ts
    - .planning/phases/01-foundation/01-UI-SPEC.md (session refresh — no UI change)
  </read_first>
  <files>
    src/proxy.ts
    src/lib/client/supabase.ts
  </files>
  <action>
    1. Replace `src/proxy.ts` passthrough with `updateSession` import from `@/lib/supabase/middlewareClient` per D-15. Keep existing `config.matcher`. Export name must be `proxy` (Next.js 16).

    2. Replace mock in `src/lib/client/supabase.ts` with thin re-export of `createSupabaseBrowserClient` from `@/lib/supabase/browser` so `LoginClient` / `SignupClient` import paths stay stable (D-12, UI-SPEC).

    3. If `src/middleware.ts` exists on disk, delete it — Next.js 16 uses `proxy.ts` only.

    4. Confirm `.env.example` documents `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` (update only if missing).
  </action>
  <verify>
    <automated>npm run typecheck && powershell -Command "Select-String -Path src/proxy.ts -Pattern 'updateSession' -Quiet; Select-String -Path src/lib/client/supabase.ts -Pattern 'MockSupabaseClient' -NotMatch -Quiet"</automated>
  </verify>
  <done>Proxy calls updateSession; client mock removed; no middleware.ts conflict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ Supabase Auth | Anon key only; session in httpOnly cookies via @supabase/ssr |
| proxy.ts ↔ all routes | Runs before RSC/API; refreshes JWT cookies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Spoofing | Session cookies | mitigate | getClaims() refresh in proxy; getUser() in requireUser |
| T-01-05 | Spoofing | Open redirect ?next= | mitigate | auth-guard validates path starts with / not // |
| T-01-06 | Elevation | Dev auth bypass | mitigate | No backdoor flags or skip-login UI (D-15) |
| T-01-SC | Tampering | @supabase/ssr | accept | Pre-installed official package; no new installs |
</threat_model>

<verification>
- `npm run typecheck` passes
- `src/proxy.ts` contains `updateSession`, not bare `NextResponse.next()`
- `src/lib/client/supabase.ts` has no `MockSupabaseClient`
</verification>

<success_criteria>
- D-12, D-15 satisfied for client infrastructure
- CORE-AUTH-01 session persistence plumbing ready for layout + login wiring in 01-05
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
