---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260418_000001_doc2quiz_cloud_first.sql
  - supabase/migrations/20260418_000002_add_study_set_parse_progress.sql
  - supabase/migrations/20260418_000003_create_storage_bucket_doc2quiz.sql
  - supabase/migrations/20260430120000_canonical_document_extractions.sql
  - supabase/migrations/20260430150000_generation_output_cache.sql
  - supabase/migrations/20260725000000_v2_clean_slate.sql
  - supabase/migrations/20260725120000_v21_baseline.sql
autonomous: true
requirements:
  - CANON-09
must_haves:
  truths:
    - "Exactly one migration file exists in supabase/migrations/ for v2.1"
    - "Baseline schema supports study_sets, canonical_documents (1:1), canonical_sections, practice tables, and doc2quiz storage bucket"
    - "No v1 tables (media_assets, ocr_results, study_set_documents, etc.) appear in the baseline"
  artifacts:
    - path: supabase/migrations/20260725120000_v21_baseline.sql
      provides: "v2.1 single-file Postgres baseline"
      contains: "canonical_documents"
    - path: supabase/migrations/20260725120000_v21_baseline.sql
      provides: "canonical_sections table"
      contains: "canonical_sections"
    - path: supabase/migrations/20260725120000_v21_baseline.sql
      provides: "doc2quiz storage bucket + RLS"
      contains: "doc2quiz"
  key_links:
    - from: canonical_documents
      to: study_sets
      via: "composite FK (study_set_id, user_id)"
      pattern: "references public.study_sets"
    - from: canonical_sections
      to: canonical_documents
      via: "composite FK (canonical_document_id, user_id)"
      pattern: "references public.canonical_documents"
---

<objective>
Replace all legacy Supabase migrations with one v2.1 baseline SQL file that models the full MVP pipeline storage contract (canonical knowledge + practice tables + storage).

Purpose: Phase 2–5 need a clean schema anchor; git history retains deleted migrations (D-03).
Output: `supabase/migrations/20260725120000_v21_baseline.sql` only; six old files removed.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@docs/pipeline.md
@supabase/migrations/20260418_000001_doc2quiz_cloud_first.sql

<interfaces>
Carry forward from v1 baseline (practice table shapes, RLS `user_id = auth.uid()`, composite FK pattern):
- `study_sets`: add `UNIQUE (id, user_id)`; replace `status` with `pipeline_stage` CHECK (`input`,`raw`,`canonical`,`mode_selected`,`quiz`,`flashcards`); keep `content_kind` nullable `quiz`|`flashcards`; keep `subtitle` (discretion).
- `approved_questions`, `approved_flashcards`, `quiz_sessions`, `study_wrong_history`: v1 column shapes per D-08.
- Storage: private bucket `doc2quiz` + INSERT/SELECT/UPDATE/DELETE policies on `storage.objects` with `owner = auth.uid()` (merge `20260418_000003` + v1 policy block).

New v2.1 tables (D-04–D-06, D-10):
- `canonical_documents`: 1:1 `study_set_id` UNIQUE; `original_storage_path`, `original_filename`, `original_mime_type`; `raw_markdown`, `canonical_markdown` text; `metadata` jsonb; `UNIQUE (id, user_id)`; composite FK to `study_sets`.
- `canonical_sections`: `canonical_document_id`, `ordinal`, `heading`, `body_markdown`, `section_type` nullable; composite FK to `canonical_documents`.

Do NOT create (D-07): `media_assets`, `ocr_results`, `canonical_document_extractions`, `generation_output_cache`, `study_set_documents`, v1 PDF/OCR columns on `study_sets`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge legacy migration files</name>
  <read_first>
    - supabase/migrations/ (list all files)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-01, D-03)
  </read_first>
  <files>
    supabase/migrations/20260418_000001_doc2quiz_cloud_first.sql
    supabase/migrations/20260418_000002_add_study_set_parse_progress.sql
    supabase/migrations/20260418_000003_create_storage_bucket_doc2quiz.sql
    supabase/migrations/20260430120000_canonical_document_extractions.sql
    supabase/migrations/20260430150000_generation_output_cache.sql
    supabase/migrations/20260725000000_v2_clean_slate.sql
  </files>
  <action>
    Delete all six existing files in `supabase/migrations/` per D-01 and D-03. Do not copy them to `.planning/` or any archive folder. Confirm `supabase/migrations/` is empty before adding the new baseline in Task 2.
  </action>
  <verify>
    <automated>powershell -Command "(Get-ChildItem -Path supabase/migrations -File).Count -eq 0"</automated>
  </verify>
  <done>Zero migration files remain; no archive copies created.</done>
</task>

<task type="auto">
  <name>Task 2: Write v2.1 baseline migration SQL</name>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md (migration section)
    - supabase/migrations/20260418_000001_doc2quiz_cloud_first.sql (practice tables + RLS patterns — read from git if deleted: `git show HEAD:supabase/migrations/20260418_000001_doc2quiz_cloud_first.sql`)
    - docs/pipeline.md (Accept list for metadata keys)
  </read_first>
  <files>supabase/migrations/20260725120000_v21_baseline.sql</files>
  <action>
    Create `supabase/migrations/20260725120000_v21_baseline.sql` as a single `begin;` … `commit;` file implementing D-04 through D-11 and D-08/D-09:

    1. Extensions + `set_updated_at()` trigger function (PATTERNS pattern).
    2. `study_sets` with `pipeline_stage` text + CHECK constraint (not `status` draft/ready); drop v1 OCR/PDF/parse columns; `UNIQUE (id, user_id)`.
    3. `canonical_documents` 1:1 with `study_sets` (`UNIQUE (study_set_id)`); columns per D-05 and D-10; `UNIQUE (id, user_id)`.
    4. `canonical_sections` per D-06; composite FK to `canonical_documents`.
    5. Practice tables `approved_questions`, `approved_flashcards`, `quiz_sessions`, `study_wrong_history` — carry v1 shapes with composite FKs to `study_sets` (D-08).
    6. RLS enabled on every public table; four policies per table (`select/insert/update/delete`) scoped `user_id = auth.uid()` (discretion on policy names).
    7. Storage bucket `doc2quiz` (private) + four `storage.objects` policies for authenticated users with `bucket_id = 'doc2quiz' and owner = auth.uid()` (D-10, D-11 documents paste/YouTube in metadata only — no storage row required).

    Use `create table if not exists` and `do $$ … if not exists` for policies where practical. Filename timestamp is discretion; keep `20260725120000_v21_baseline.sql`.
  </action>
  <verify>
    <automated>powershell -Command "if ((Get-ChildItem supabase/migrations -File).Count -ne 1) { exit 1 }; Select-String -Path supabase/migrations/*.sql -Pattern 'canonical_documents|canonical_sections|pipeline_stage|doc2quiz' -Quiet"</automated>
  </verify>
  <done>Single baseline file exists with all required tables, no v1 dropped tables, bucket + RLS policies present.</done>
</task>

<task type="auto" gate="blocking">
  <name>[BLOCKING] Task 3: Schema files-only verification (D-02)</name>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-02)
    - supabase/migrations/20260725120000_v21_baseline.sql
  </read_first>
  <files>README.md</files>
  <action>
    Per D-02: do NOT run `supabase db push`, `supabase db reset` against remote, or any automation that wipes the user's hosted project.

    1. SQL review: confirm baseline is self-contained (no references to dropped v1 tables); `canonical_documents` UNIQUE on `study_set_id`; all child tables use composite FK pattern.
    2. Optional local only: if Supabase CLI is configured for a **local** project, `supabase db reset` may validate apply — never against remote without explicit user action.
    3. Add a short "Schema apply (manual)" note to README.md (or existing setup section): user applies `20260725120000_v21_baseline.sql` via local `supabase db reset` or Dashboard SQL when ready; Phase 1 ships files only.
    4. Run `npm run typecheck` and `npm run build` — must pass (code may not reference old `status` column yet; this wave only adds SQL).

    Document in SUMMARY: schema is files-only; remote apply deferred to user.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>README documents manual schema apply; typecheck and build green; no remote Supabase commands executed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration file → remote DB | DDL not auto-applied; user controls when schema lands on hosted project (D-02) |
| storage.objects RLS | Upload auth enforced at Storage layer in Phase 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Postgres RLS policies | mitigate | User-scoped policies on all tables; composite FKs prevent cross-user orphans |
| T-01-02 | Information disclosure | storage.objects | mitigate | Private bucket; SELECT policy `owner = auth.uid()` |
| T-01-03 | Elevation | Remote DB wipe | mitigate | No remote reset/push in automation (D-02) |
| T-01-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
- `supabase/migrations/` contains exactly one file: `20260725120000_v21_baseline.sql`
- Grep baseline excludes: `study_set_documents`, `media_assets`, `ocr_results`
- `npm run typecheck` and `npm run build` pass after Task 3
</verification>

<success_criteria>
- CANON-09 schema artifacts exist in SQL (documents, sections, storage refs, metadata jsonb column)
- All D-01, D-02, D-03, D-04–D-11, D-07–D-09 implemented in migration file
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
