---
phase: 16-learning-parse-domain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ARCHITECTURE-domain-boundaries.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "A single canonical architecture note exists under `docs/` describing parse vs learning domains, allowed dependency direction, and forbidden import patterns for learning-facing UI."
    - "The note explicitly states that Phase 15 (scale mode / server parse) is not a hard gate for this boundary work (incremental alignment)."
    - "Current `rg` inventory of `@/lib/ai` usage from learning-oriented surfaces (`src/components/review/**`, `src/components/play/**`, `src/components/flashcards/**`, `src/app/(app)/sets/[id]/{play,review,practice,flashcards,done}/`) is recorded so regressions are visible in later phases."
  artifacts:
    - path: docs/ARCHITECTURE-domain-boundaries.md
      provides: "Domain boundary map, dependency rules, optional ESLint strategy (deferred if tooling not ready)"
      min_lines: 80
  key_links:
    - from: docs/ARCHITECTURE-domain-boundaries.md
      to: "src/lib/ai/*, src/lib/db/studySetDb.ts, src/types/question.ts"
      via: "Documented consumer/producer relationships"
      pattern: "N/A (documentation)"
---

<objective>
**Wave 1 — boundary definition (non-breaking):** Add `docs/ARCHITECTURE-domain-boundaries.md` as the **single** canonical location for parse-domain vs learning-domain boundaries (not `CLAUDE.md`) so the contract can grow (import matrices, diagrams, phased refactors) without bloating the contributor quick-start file.

**Purpose:** Phase 16 roadmap goal — long-term decoupling so review/play/practice/flashcards/session flows do not stay tightly coupled to OCR/vision runners (`parseChunk`, `runVisionSequential`, etc.).

**Output:** One new markdown doc + explicit note that work may proceed **without blocking on Phase 15** code completion.

**Note (orchestrator):** Do not treat Phase 15 as a merge blocker for documentation or incremental import-path refactors in `16-02`.
</objective>

<context>
@.planning/ROADMAP.md (Phase 16 goal)
@CLAUDE.md (high-level engines — align terminology but do not duplicate full file)
@eslint.config.mjs (FlatCompat + `next/core-web-vitals` only — restricted-import rules are **documented as future**, not enabled here)
@src/lib/ai/mappingQuality.ts (example of parse-provenance logic consumed by review UI today)
@src/components/ai/AiParseSection.tsx (parse orchestration anchor — **do not rewrite** this phase)
</context>

<threat_model>
| Threat | Severity | Mitigation | Blockers |
|--------|----------|------------|----------|
| Doc prescribes unsafe data flow (e.g., learning UI calling vision endpoints directly) | Medium | State explicitly: learning surfaces consume persisted `Question[]` + study-set meta from `studySetDb`; network/vision only in parse/settings domains. | None |
| Misleading "completed" boundary if inventory section is stale | Low | Executor must run listed `rg` commands at authoring time and paste outputs into the doc (or a dated appendix). | None |
| ESLint rule added carelessly breaks `next build` / CI | Medium | **Do not** add `no-restricted-imports` in this plan; document a future gated task with suggested globs once `eslint.config.mjs` has explicit overrides per folder. | None |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author `docs/ARCHITECTURE-domain-boundaries.md`</name>
  <files>docs/ARCHITECTURE-domain-boundaries.md</files>
  <read_first>
  - `.planning/ROADMAP.md` (Phase 16 block, lines ~377–389)
  - `CLAUDE.md` (Architecture section — reuse names: Document Ingestion, Question Bank, Practice)
  - `eslint.config.mjs` (confirm minimal flat config — cite why ESLint enforcement is deferred)
  - `src/types/question.ts` (fields like `mappingMethod`, `mappingConfidence` — learning displays these without re-running parse)
  </read_first>
  <action>
  Create **new** `docs/ARCHITECTURE-domain-boundaries.md` (English) with the following **mandatory sections** (headings may be adjusted slightly but content must exist):

  1. **Purpose & non-goals** — Clarify this phase documents boundaries; it does **not** require rewriting `AiParseSection.tsx`.

  2. **Two domains**
     - **Document ingestion / parse:** `src/lib/pdf/**`, `src/lib/ai/**` (including OCR, vision runners, `parseChunk`, layout chunks, `parseRoutePolicy`, staging, `ocrDb`), `src/components/ai/**`, parse-related API routes under `src/app/api/ai/**`, `src/app/api/parse-jobs/**`. Produces **draft artifacts** (questions, OCR blobs, vision staging metadata) persisted via `src/lib/db/studySetDb.ts` and related types.
     - **Learning / session:** Review, play, practice, flashcards, wrong-answer loop, session/score UX — primarily `src/app/(app)/sets/[id]/{review,play,practice,flashcards,done}/`, `src/components/play/**`, `src/components/review/**`, `src/components/flashcards/**` (adjust list if glob misses a route; must mention `studySetDb` as shared persistence boundary).

  3. **Dependency direction (allowed)** — Parse → writes study set + `Question[]` (+ provenance fields). Learning → reads `Question[]`, meta, session/local state; **must not** import vision/OCR **runners** (e.g. `runVisionSequential`, `runOcrSequential`, `parseChunk` execution paths) from learning routes/components.

  4. **Forbidden imports (normative for new code)** — Learning-facing UI **must not** import modules that orchestrate upstream multimodal/OCR calls. Give concrete examples of **forbidden** path prefixes/patterns: `@/lib/ai/parseChunk`, `@/lib/ai/runVisionSequential`, `@/lib/ai/runOcrSequential`, `@/lib/ai/parseVisionPage`, `@/lib/ai/ocrAdapter`, etc. Clarify that **read-only helpers** operating on in-memory `Question` (e.g. mapping tier badges) may live under `src/lib/learning/**` as facades even if implementation temporarily stays in `src/lib/ai/` (see Phase `16-02`).

  5. **Target folder map** — Table or bullet list: `src/lib/ai` = parse; proposed `src/lib/learning` = learning-facing stable imports (barrel); `src/lib/db` = shared persistence; `src/types` = shared contracts.

  6. **Relationship to Phase 15** — Short paragraph: scale mode / server parse (Phase 15) can evolve in parallel; this boundary doc is the contract learning features rely on.

  7. **Tooling: ESLint `no-restricted-imports` (future)** — Explain current `eslint.config.mjs` uses `FlatCompat` + `next/core-web-vitals` only. **Do not** edit `eslint.config.mjs` in this plan. Describe a **future** task: add overrides for `src/components/{play,review,flashcards}/**` and learning routes forbidding deep `@/lib/ai/*` except an allowlist (e.g. none — use `@/lib/learning` only), after a baseline inventory shows zero violations.

  8. **Optional:** One-line pointer in text: "Contributor quick-start remains `CLAUDE.md`; deep domain rules live here."

  Use fenced `text` or `bash` blocks only where listing example commands — no secrets.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
  - File `docs/ARCHITECTURE-domain-boundaries.md` exists.
  - `rg -n "Forbidden imports|Document ingestion|Learning / session|Phase 15|no-restricted-imports" docs/ARCHITECTURE-domain-boundaries.md` returns at least one match line for each substring (five distinct concepts present).
  - `rg "eslint.config" docs/ARCHITECTURE-domain-boundaries.md` returns at least one match (documents ESLint deferral).
  - Word count ≥ 250 words (approximate: `rg --count '' docs/ARCHITECTURE-domain-boundaries.md` not available on Windows — use manual check or PowerShell `(Get-Content ...).Length` ≥ ~40 lines of substance).
  </acceptance_criteria>
  <done>Architecture doc committed with all mandatory sections; `npm run lint` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Import inventory appendix (learning-oriented surfaces)</name>
  <files>docs/ARCHITECTURE-domain-boundaries.md</files>
  <read_first>
  - Task 1 output file (same path)
  - PowerShell or Git Bash available for `rg` / `grep`
  </read_first>
  <action>
  Append a section **`## Appendix: Current @/lib/ai imports (Phase 16 baseline)`** to `docs/ARCHITECTURE-domain-boundaries.md`.

  Run these searches from repo root and paste **full command + stdout** (truncate only if &gt; 120 lines — then state "truncated" and keep all hits under `src/components/review`, `src/components/play`, `src/components/flashcards`, and `src/app/(app)/sets/[id]/` subfolders `play`, `review`, `practice`, `flashcards`, `done`):

  - `rg "@/lib/ai" src/components/review --glob "*.{ts,tsx}"`
  - `rg "@/lib/ai" src/components/play --glob "*.{ts,tsx}"`
  - `rg "@/lib/ai" src/components/flashcards --glob "*.{ts,tsx}"`
  - `rg "@/lib/ai" "src/app/(app)/sets/[id]" --glob "*.{ts,tsx}"`

  Add one sentence interpreting results for Doc2Quiz today (e.g. "Review components import mapping quality helpers; play pages use `studySetDb` only," — adjust to actual output).

  **Do not** modify any `.ts`/`.tsx` source in this task — documentation only.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
  - `rg -n "Appendix: Current" docs/ARCHITECTURE-domain-boundaries.md` returns ≥ 1 match.
  - Appendix contains the literal substring `rg "@/lib/ai"` at least once (proving commands were embedded).
  - `rg "@/lib/ai" src/components/review --glob "*.{ts,tsx}"` re-run by verifier shows documented output is still consistent OR doc dated with **Last inventoried: YYYY-MM-DD** in appendix header.
  </acceptance_criteria>
  <done>Baseline inventory captured in doc; lint still green.</done>
</task>

</tasks>

<verification>
- `npm run lint` passes from repo root after both tasks.
- No changes to `eslint.config.mjs`, `AiParseSection.tsx`, or production TypeScript beyond the new doc (Task 2 only touches the same doc).
</verification>

<success_criteria>
- `docs/ARCHITECTURE-domain-boundaries.md` exists and is internally consistent with actual folder layout.
- Parse → learning dependency direction and forbidden runner imports are explicit and grep-verifiable in the doc body.
- Phase 15 non-blocker called out.
</success_criteria>

<output>
After completion, create `.planning/phases/16-t-ch-r-domain-h-c-t-p-ra-kh-i-domain-parse-t-i-li-u-hi-n-cod/16-01-SUMMARY.md` using the standard summary template.
</output>
