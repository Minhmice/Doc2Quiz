---
phase: 16-ide-themes
plan: 03
type: execute
wave: 3
depends_on: [16-01, 16-02]
files_modified:
  - src/app/layout.tsx
  - src/components/providers/app-root-providers.tsx
  - src/components/providers/ThemePreferenceProvider.tsx
  - src/lib/profile/themePreferenceCoordinator.ts
  - src/lib/profile/themePreferenceCoordinator.test.ts
  - src/app/api/profile/route.test.ts
  - src/components/settings/AppearanceSettings.tsx
  - src/components/settings/AppearanceSettings.test.tsx
  - scripts/verify-ide-themes.mjs
  - playwright.ide-themes.config.mjs
  - src/app/globals.css
  - src/components/flashcards/FlashcardActions.tsx
  - src/components/flashcards/FlashcardActions.test.ts
  - package.json
autonomous: false
requirements: [THEME-03, THEME-04, THEME-05]
gap_closure: true
user_setup:
  - service: ide-themes-test-account
    why: "Standalone Playwright verification must authenticate through the real login and profile API; no repository credential or autonomous account creation is allowed."
    env_vars:
      - name: TEST_EMAIL
        source: "Export the email of an existing non-production Doc2Quiz account in the execution shell."
      - name: TEST_PASSWORD
        source: "Export that account's password in the execution shell; do not place it in .env, source, planning artifacts, logs, or command arguments."
must_haves:
  truths:
    - "A provider preserved across a signed-out to signed-in account boundary visibly selects that account's server preference, applies it to the document, and persists the next choice through authenticated PATCH /api/profile."
    - "Rapid selections converge provider state, document theme, optional local storage, and persisted profile value on the latest selection regardless of older PATCH completion or failure order."
    - "Denied or quota-limited local storage never blocks DOM theme application or authenticated profile persistence."
    - "All five appearance radios support ArrowLeft, ArrowRight, ArrowUp, and ArrowDown focus/selection movement with first/last wraparound."
    - "Flashcard Done and Next labels meet WCAG AA contrast in every named palette, including normal and hover backgrounds in High Contrast."
    - "Authenticated GET and PATCH /api/profile theme behavior, invalid input, fallback, field mapping, and unauthenticated rejection have real route coverage."
  artifacts:
    - path: "src/lib/profile/themePreferenceCoordinator.ts"
      provides: "Node-testable account authority, applied-theme, guarded-storage, and latest-mutation decisions"
      exports: ["THEME_PREFERENCE_STORAGE_KEY", "resolveThemeAuthority", "resolveAppliedTheme", "safeReadThemePreference", "safeWriteThemePreference", "createThemePreferenceCoordinator"]
    - path: "src/lib/profile/themePreferenceCoordinator.test.ts"
      provides: "Node Vitest coverage for account boundaries, deferred completion order, latest failure, denied storage, and System mapping"
    - path: "src/components/providers/ThemePreferenceProvider.tsx"
      provides: "React wiring from pure decisions to next-themes, document DOM, browser storage, and profile PATCH effects"
      exports: ["ThemePreferenceProvider", "useThemePreference"]
    - path: "scripts/verify-ide-themes.mjs"
      provides: "Standalone Playwright plus node:assert browser verification without @playwright/test"
    - path: "playwright.ide-themes.config.mjs"
      provides: "Importable base URL, web-server, and timeout constants for standalone browser verification"
      exports: ["IDE_THEMES_BASE_URL", "IDE_THEMES_WEB_SERVER", "IDE_THEMES_TIMEOUT_MS"]
    - path: "src/app/api/profile/route.test.ts"
      provides: "Real GET/PATCH theme contract tests at authenticated profile route boundary"
    - path: "src/components/flashcards/FlashcardActions.test.ts"
      provides: "Automated WCAG contrast checks for semantic action foreground/background token pairs"
    - path: "src/app/globals.css"
      provides: "Contrast-safe semantic flashcard action foreground tokens for base and named palettes"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/providers/ThemePreferenceProvider.tsx"
      via: "stable authenticated account ID plus allowlisted server preference"
      pattern: "accountId|initialThemePreference"
    - from: "src/components/providers/ThemePreferenceProvider.tsx"
      to: "src/lib/profile/themePreferenceCoordinator.ts"
      via: "pure authority, storage, applied-theme, and latest-mutation decisions"
      pattern: "createThemePreferenceCoordinator|resolveThemeAuthority|resolveAppliedTheme|safeReadThemePreference|safeWriteThemePreference"
    - from: "src/components/providers/ThemePreferenceProvider.tsx"
      to: "/api/profile"
      via: "latest-selection reconciliation PATCH using only ThemePreference allowlist values"
      pattern: "themePreference"
    - from: "scripts/verify-ide-themes.mjs"
      to: "playwright.ide-themes.config.mjs"
      via: "named ESM imports for server lifecycle and browser timeouts"
      pattern: "IDE_THEMES_BASE_URL|IDE_THEMES_WEB_SERVER|IDE_THEMES_TIMEOUT_MS"
    - from: "src/components/settings/AppearanceSettings.tsx"
      to: "src/components/providers/ThemePreferenceProvider.tsx"
      via: "radio selection and latest-only save feedback"
      pattern: "setThemePreference"
    - from: "src/components/flashcards/FlashcardActions.tsx"
      to: "src/app/globals.css"
      via: "semantic Done and Next foreground tokens paired with variable action backgrounds"
      pattern: "--qp-.*action.*foreground"
---

<objective>
Close IDE Themes gaps with a credential-gated production tracer, Node-testable pure coordination decisions, standalone real-browser provider coverage, route contract tests, keyboard repair, and contrast-safe actions.

Purpose: Satisfy THEME-03, THEME-04, and THEME-05 while closing WR-01, WR-02, WR-03, WR-04, and IN-01 under the existing ASVS L1 boundary.
Output: Minimum coordinator module and Node tests, corrected provider/radio/action behavior, real route tests, and one standalone Playwright verification script using installed `playwright`.
</objective>

<execution_context>
@/Users/minhmice/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.config/opencode/gsd-core/templates/summary.md
@/Users/minhmice/.config/opencode/gsd-core/references/checkpoints.md
@/Users/minhmice/.config/opencode/gsd-core/references/planner-preconditions.md

Gap-closure mode: research skipped. UI contract gate explicitly overridden by `--skip-ui`; do not create UI-SPEC.md or add a design checkpoint. Task 1 is the production tracer and its verification must pass before Tasks 2-4 expand coverage. `autonomous: false` is intentional: execution must stop at Task 1's precondition when either credential is absent; credentials are never auto-approved or synthesized.
</execution_context>

<context>
@.planning/phases/16-ide-themes/16-CONTEXT.md
@.planning/phases/16-ide-themes/16-VERIFICATION.md
@.planning/phases/16-ide-themes/16-REVIEW.md
@.planning/phases/16-ide-themes/16-01-PLAN.md
@.planning/phases/16-ide-themes/16-01-SUMMARY.md
@.planning/phases/16-ide-themes/16-02-PLAN.md
@.planning/phases/16-ide-themes/16-02-SUMMARY.md
@src/app/layout.tsx
@src/components/providers/app-root-providers.tsx
@src/components/providers/ThemePreferenceProvider.tsx
@src/app/api/profile/route.ts
@src/app/api/profile/route.test.ts
@src/lib/profile/themePreference.ts
@src/lib/profile/themePreference.test.ts
@src/components/settings/AppearanceSettings.tsx
@src/components/settings/AppearanceSettings.test.tsx
@src/app/(auth)/login/LoginClient.tsx
@src/app/(app)/settings/page.tsx
@src/app/(app)/settings/SettingsPageClient.tsx
@src/components/layout/AccountMenu.tsx
@src/app/globals.css
@src/components/flashcards/FlashcardActions.tsx
@package.json
@vitest.config.ts
@eslint.config.mjs
@scripts/dev.mjs
@scripts/capture-dashboard.mjs

<interfaces>
Theme values remain exactly `system`, `vscode-dark`, `vscode-light`, `monokai`, and `high-contrast`. `src/lib/profile/themePreference.ts` remains sole validation/default authority. Existing internal authenticated `GET/PATCH /api/profile` remains sole server persistence contract; add no route, external API, SDK coverage artifact, dependency, schema change, migration, or schema push.

Current `vitest.config.ts` uses `environment: "node"`; package.json has React server rendering, Vitest, and `playwright`, but no DOM test environment, DOM renderer, or direct `@playwright/test` dependency. Do not create a provider DOM test under Vitest and do not import `@playwright/test`. Extract only decisions that do not require React or browser globals into `src/lib/profile/themePreferenceCoordinator.ts`; keep actual React effects in `ThemePreferenceProvider.tsx` and exercise them in a standalone browser script.

The pure module must export exactly:
- `THEME_PREFERENCE_STORAGE_KEY = "doc2quiz-theme-preference"`.
- `resolveThemeAuthority(input)`, where input contains `previousAccountId`, `accountId`, `serverPreference`, `storedPreference`, and `currentPreference`, and output states whether the account changed plus the preference to adopt. Same-account rerenders retain optimistic current state; account changes adopt defined authenticated server preference, or validated signed-out storage/default.
- `resolveAppliedTheme(preference, systemTheme)`, mapping `system` plus `light` to `vscode-light` and `system` plus `dark` to `vscode-dark`, while named values pass through.
- `safeReadThemePreference(storage)` and `safeWriteThemePreference(storage, preference)`, using a minimal structural `getItem`/`setItem` interface, validating reads, catching denial/quota errors, and returning `undefined`/`false` rather than throwing.
- `createThemePreferenceCoordinator(initialConfirmedPreference)`, returning `begin(preference)`, `settle(mutationId, succeeded)`, and `reset(confirmedPreference)`. `begin` returns a monotonic mutation ID. `settle` returns explicit `currentPreference`, `confirmedPreference`, `rollbackPreference`, `reconcilePreference`, and `isLatest` decisions; stale settlement never rolls back UI, while a settlement that could leave server state stale requests reconciliation to current desired value. `reset` invalidates prior mutation authority at an account boundary.

Root server rendering passes `user.id` separately as optional `accountId`; no account ID enters local storage, request bodies, logs, browser assertions, or feedback. Local storage remains optional fallback/cross-tab transport, never authority over a defined account preference. Storage events pass `isThemePreference` before use.

`playwright.ide-themes.config.mjs` is a plain ESM data module exporting `IDE_THEMES_BASE_URL`, `IDE_THEMES_WEB_SERVER`, and `IDE_THEMES_TIMEOUT_MS`. `IDE_THEMES_WEB_SERVER` contains exact `command`, `url`, `timeout`, and `reuseExistingServer` values. `scripts/verify-ide-themes.mjs` imports `assert` from `node:assert/strict`, `spawn` from `node:child_process`, `chromium` from `playwright`, and those three named config exports from `../playwright.ide-themes.config.mjs`. It polls the configured URL, spawns the configured command only when unavailable, and terminates only the child it owns in `finally`. It supports `--case account-tracer` and `--case full`; default is `full`.

The browser script checks `TEST_EMAIL` and `TEST_PASSWORD` before server/browser startup. If either is absent, it exits nonzero naming only missing variable names. It never prints values, writes credentials, creates screenshots/traces/videos, or supplies defaults. All profile mutation cleanup runs in `finally`: capture original preference through authenticated `GET /api/profile`, restore it through authenticated `PATCH /api/profile`, and reauthenticate if a preceding account-boundary check signed out.

Latest-selection safety remains explicit: stale PATCH success/failure cannot alter current provider state, DOM, storage, or current feedback; after reconciliation settles, server-observed value equals latest desired value. Do not disable the selector to hide the race.

Real keyboard coverage dispatches browser key events against rendered radios. Source searches and pure index tests do not satisfy focus, `aria-checked`, or wrap acceptance.

Central traceability is registered under Phase 16. Do not edit `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/phases/12-study-together`, completed plans/summaries, review, or verification during plan execution. Never use or claim SOCIAL-01..10.
</interfaces>
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Prove account-to-provider-to-PATCH tracer</name>
  <objective>Observable tracer: after a preserved signed-out provider holding `monokai` signs into an account whose server preference is `vscode-light`, Settings selects `vscode-light`, `<html data-theme>` becomes `vscode-light`, and selecting `vscode-dark` produces authenticated `PATCH /api/profile` plus a confirming GET value of `vscode-dark`.</objective>
  <precondition>Both TEST_EMAIL and TEST_PASSWORD are non-empty in the execution environment; if either is absent, stop before edits and return the blocking missing-prerequisite checkpoint without displaying values.</precondition>
  <files>src/app/layout.tsx, src/components/providers/app-root-providers.tsx, src/components/providers/ThemePreferenceProvider.tsx, src/lib/profile/themePreferenceCoordinator.ts, src/lib/profile/themePreferenceCoordinator.test.ts, scripts/verify-ide-themes.mjs, playwright.ide-themes.config.mjs, package.json</files>
  <read_first>
    - `.planning/phases/16-ide-themes/16-VERIFICATION.md` lines 30-37 and 96-111 for exact account, ordering, and storage failures.
    - `.planning/phases/16-ide-themes/16-REVIEW.md` WR-02, WR-03, and IN-01.
    - `package.json` scripts and installed dependencies; confirm `playwright` exists and `@playwright/test` is not directly available.
    - `vitest.config.ts` Node environment.
    - `src/app/layout.tsx`, `src/components/providers/app-root-providers.tsx`, and `src/components/providers/ThemePreferenceProvider.tsx` complete account/preference/effect path.
    - `src/lib/profile/themePreference.ts` exact allowlist/default contract.
    - `src/app/(auth)/login/LoginClient.tsx` login selectors and redirect behavior.
    - `src/components/layout/AccountMenu.tsx` real sign-out path.
    - `scripts/dev.mjs` server lifecycle behavior and `scripts/capture-dashboard.mjs` Playwright import/login shape; do not copy its credential defaults, screenshot behavior, or logging.
  </read_first>
  <action>
Create `src/lib/profile/themePreferenceCoordinator.ts` with only the six exact exports and contracts stated in `<interfaces>`. Import its decisions into `ThemePreferenceProvider.tsx`; do not duplicate decision logic in provider effects. Pass authenticated `user.id` from `getInitialThemePreference` through `AppRootProviders` as optional `accountId`. On account identity change, call coordinator `reset`, adopt defined server preference for authenticated accounts, and invalidate pending mutations; same-account rerenders must not erase optimistic state.

Provider owns browser effects: map next-themes via `resolveAppliedTheme`, update `document.documentElement.dataset.theme` and compatible `dark` class, call safe storage helpers, validate storage events, and issue authenticated profile PATCHes. Use coordinator settlement output so stale outcomes cannot roll back provider/DOM/storage. If settlement returns `reconcilePreference`, PATCH that value and settle reconciliation before declaring the latest caller saved. A latest failure with no newer selection rolls back to last confirmed value and rejects; account reset prevents prior-account completion from affecting the new account.

Create Node Vitest tests in `themePreferenceCoordinator.test.ts`, using deferred promises only around a tiny fake persistence harness that invokes `begin`/`settle`. Cover signed-out to account A, account A to B, same-account rerender, older success after newer success, older failure after newer success, latest failure rollback, stale completion after latest rollback, denied `getItem`, denied/quota `setItem`, invalid stored value, and both System mappings. Assert mutation IDs, settlement decisions, caller result, PATCH body sequence, and fake final server value. Do not import React, render provider JSX, access real `document`/`window`, change Vitest environment, or add a DOM dependency.

Create plain ESM config/script using exact exports/imports from `<interfaces>` and add only `"test:ide-themes": "node scripts/verify-ide-themes.mjs"` to package scripts. Implement the narrow `--case account-tracer`: authenticate, capture original preference, set server to `vscode-light`, sign out through actual UI, establish signed-out local fallback `monokai`, sign in without a full browser-context remount, assert selected radio and `<html data-theme="vscode-light">`, select `vscode-dark`, observe the PATCH body, and confirm GET returns `vscode-dark`. Restore original preference in `finally`. Use `node:assert/strict`; no test runner, screenshots, artifacts, hardcoded credentials, or dependency changes.
  </action>
  <verify>
    <automated>npm run test -- src/lib/profile/themePreferenceCoordinator.test.ts --run &amp;&amp; npm run test:ide-themes -- --case account-tracer &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Node suite executes every pure symbol and proves account/reset, ordering, failure, storage-denial, and System decisions without DOM globals.
    - Browser tracer proves real account boundary to selected radio and document theme, then real selector to authenticated PATCH and confirming GET.
    - Script exits before server/browser startup with a nonzero status naming missing `TEST_EMAIL` and/or `TEST_PASSWORD`, never values.
    - Provider contains no direct unguarded localStorage read/write and sends no account ID in profile PATCH.
    - No expansion task starts until all tracer verification commands pass.
  </acceptance_criteria>
  <done>Tracer visibly converges account preference, provider consumer, document theme, and authenticated profile persistence; Node and browser checks pass and original account preference is restored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair profile route theme contract coverage</name>
  <objective>Observable route contract: exported GET/PATCH handlers reject unauthenticated calls, normalize malformed stored themes to `system`, persist each valid client value as `theme_preference` for authenticated `user.id`, reject invalid input before upsert, and return current `error.message` on persistence failure.</objective>
  <files>src/app/api/profile/route.test.ts</files>
  <read_first>
    - `src/app/api/profile/route.ts` complete GET/PATCH handlers, including auth, selected columns, normalization, validation, upsert mapping, and lines 226-229 persistence-error response.
    - `src/app/api/profile/route.test.ts` existing avatar mocks/helpers and auth style.
    - `src/lib/profile/themePreference.ts` supported values and fallback.
    - `src/lib/api/requireApiUser.ts` response contract.
  </read_first>
  <action>
Extend only the existing route test file. Reuse `requireApiUserMock`; add focused Supabase chain helpers capturing selected columns, authenticated row filter, upsert payload, selected return row, and study-set query without weakening avatar coverage.

Call exported handlers for: unauthenticated GET before profile query; authenticated GET for each valid stored preference; malformed, null, and missing stored values falling back to `system`; unauthenticated PATCH before persistence; malformed JSON returning 400; unsupported, null, and non-string supplied `themePreference` values returning 400 without upsert; every valid value mapping client `themePreference` to database `theme_preference` with only authenticated `id`; and persistence failure returning status 500 with the current contract `{ error: error.message }`. Assert GET selects `theme_preference`, PATCH return select includes it, and client-supplied `id` cannot select another row. Do not expect a generic error, change `route.ts`, test SDK internals, or create external API/SDK artifacts.
  </action>
  <verify>
    <automated>npm run test -- src/app/api/profile/route.test.ts src/lib/profile/themePreference.test.ts --run &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - GET tests prove auth rejection, all valid values, and malformed/null/missing fallback to `system` through exported GET.
    - PATCH tests prove auth rejection, malformed JSON and invalid-value rejection, all valid field mappings, own authenticated ID, and exact current persistence-error message through exported PATCH.
    - Existing avatar tests remain passing; production route remains unchanged.
  </acceptance_criteria>
  <done>Focused route suite passes with real handler calls and truthfully locks current GET/PATCH theme behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify radio traversal and provider browser effects</name>
  <objective>Observable browser behavior: each arrow key moves focus and selection to the correct radio with wraparound, rapid deferred saves finish on the latest radio/DOM/storage/server value, and denied theme storage still permits DOM application and PATCH persistence.</objective>
  <precondition>Both TEST_EMAIL and TEST_PASSWORD remain non-empty for the standalone full browser check; if missing, stop and return the blocking missing-prerequisite checkpoint without displaying values.</precondition>
  <files>src/components/settings/AppearanceSettings.tsx, src/components/settings/AppearanceSettings.test.tsx, scripts/verify-ide-themes.mjs</files>
  <read_first>
    - `.planning/phases/16-ide-themes/16-REVIEW.md` WR-01 and WR-03.
    - `src/components/settings/AppearanceSettings.tsx` localized cloned options, map callback, keyboard handler, roving tabindex, focus IDs, and feedback.
    - `src/components/settings/AppearanceSettings.test.tsx` server-render semantic checks and current source-string assertions.
    - `src/components/providers/ThemePreferenceProvider.tsx` Task 1 promise semantics.
    - `src/app/(app)/settings/page.tsx` actual routed Settings composition.
    - `scripts/verify-ide-themes.mjs` tracer helpers, cleanup, and server ownership.
  </read_first>
  <action>
Fix traversal using `options.map((option, index) =&gt; ...)`: Right/Down use `+1`, Left/Up use `-1`, modulo `options.length`; focus and select the same destination. Keep exactly one selected tab stop, radio semantics, visible focus, checkmark/text selected state, and polite feedback. Add a monotonic feedback request ID so only the latest selection promise writes Saved/Failed text.

Remove `readFileSync`, `resolve`, and arrow-name source assertions from `AppearanceSettings.test.tsx`. Keep Node-compatible `renderToStaticMarkup` checks only for actual semantic output: one radiogroup, five radios, one checked radio, one `tabindex="0"`, four `tabindex="-1"`, named labels, and polite live region. Do not claim DOM interaction from this file.

Expand standalone script `--case full` using Playwright page APIs plus `node:assert/strict`. Against rendered `/settings`, dispatch ArrowRight, ArrowDown, ArrowLeft, and ArrowUp; after each assert `document.activeElement`, destination `aria-checked="true"`, previous `aria-checked="false"`, and exactly one `tabindex="0"`. Prove High Contrast to System forward wrap and System to High Contrast backward wrap.

For actual provider effects, intercept profile PATCHes and defer completion deterministically: run older-success-after-newer-success and older-failure-after-newer-success sequences, allow coordinator reconciliation, then assert latest selected radio, document `data-theme`, compatible `dark` class, stored preference when storage is available, latest-only live feedback, observed PATCH sequence, and GET server value. In a fresh authenticated page/context, override only reads/writes for `THEME_PREFERENCE_STORAGE_KEY` to throw `SecurityError`/`QuotaExceededError`; select a named theme and assert radio plus DOM update and successful PATCH/GET. Always restore original profile value in outer `finally` even after assertion failure.
  </action>
  <verify>
    <automated>npm run test -- src/components/settings/AppearanceSettings.test.tsx --run &amp;&amp; npm run test:ide-themes -- --case full &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - No `OPTIONS.indexOf(option)`, source-file arrow assertion, or `@playwright/test` import remains.
    - Real browser dispatches all four arrow keys and proves focus, `aria-checked`, one roving tab stop, and both wrap directions.
    - Deferred browser checks prove stale success/failure cannot displace latest radio, DOM, feedback, optional storage, or final server preference.
    - Denied storage browser check proves provider DOM effects and authenticated PATCH continue.
    - Test account preference is restored after success or failure and no credential/artifact is written.
  </acceptance_criteria>
  <done>Node semantic render check and full standalone browser check pass for WR-01, WR-03, IN-01, and THEME-03/05 behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Make flashcard action foregrounds contrast-safe</name>
  <objective>Observable accessibility result: Done and Next label foregrounds each measure at least 4.5:1 against their normal and hover backgrounds in base light, base dark, VS Code Dark, VS Code Light, Monokai, and High Contrast.</objective>
  <files>src/app/globals.css, src/components/flashcards/FlashcardActions.tsx, src/components/flashcards/FlashcardActions.test.ts</files>
  <read_first>
    - `.planning/phases/16-ide-themes/16-VERIFICATION.md` High Contrast evidence and measured failures.
    - `.planning/phases/16-ide-themes/16-REVIEW.md` WR-04.
    - `src/app/globals.css` base light/dark Stitch token blocks and all four named palette blocks, including nested selectors.
    - `src/components/flashcards/FlashcardActions.tsx` Done/Next normal and hover backgrounds plus hardcoded `text-white`.
    - `src/components/ui/button.tsx` variant classes affecting foreground/background.
  </read_first>
  <action>
Add distinct semantic Done and Next foreground variables to base light, base dark, VS Code Dark, VS Code Light, Monokai, and High Contrast token contracts. Keep action backgrounds unless a minimal token correction is required. Replace both `text-white` literals in `FlashcardActions` with those foreground variables. High Contrast Done must use black on yellow and remain readable on cyan hover; High Contrast Next must use a foreground/background pair measuring at least 4.5:1 in normal and hover states. Preserve behavior, labels, disabled behavior, dimensions, focus, and layout.

Create a dependency-free Node Vitest using `node:fs`, standard sRGB relative luminance, and contrast ratio functions. Read actual `globals.css` declarations and `FlashcardActions.tsx` class contract. Resolve Done foreground against `--qp-secondary` and hover `--qp-tertiary`; resolve Next foreground against `--qp-on-primary-container` and hover `--qp-primary`; cover base light, base dark, and all four named palettes. Fail on missing/unresolvable colors, ratio below 4.5, absent semantic consumers, or remaining `text-white`; do not hardcode ratios disconnected from CSS.
  </action>
  <verify>
    <automated>npm run test -- src/components/flashcards/FlashcardActions.test.ts --run &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Done and Next consume distinct semantic foreground variables; neither contains `text-white`.
    - Automated checks calculate ratios from actual CSS for normal/hover pairs across six theme contexts.
    - Every pair is at least 4.5:1; High Contrast no longer yields white/yellow or white/white.
    - No palette layout, schema, migration, dependency, or unrelated legacy literal changes.
  </acceptance_criteria>
  <done>WR-04 and THEME-04 pass automated WCAG AA checks sourced from production token declarations.</done>
</task>
</tasks>

<threat_model>
## ASVS Level and Execution Gate

Target: OWASP ASVS L1. Block execution before implementation if any unmitigated High or Critical threat is discovered. Current review identifies no unmitigated High/Critical threat; listed threats require planned mitigations and regression tests.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser theme state to `/api/profile` | Theme value and request timing are attacker-controlled; authentication and allowlist remain server authority. |
| Auth/session transition to preserved provider | Account identity and server preference may change while client state survives. |
| localStorage/storage event to DOM | Storage may throw or contain attacker-written malformed values. |
| Execution shell to authenticated browser script | Credentials grant account access and remain environment-only. |
| CSS token to visible action label | Incorrect semantic pairing can make core controls unreadable. |

## STRIDE / ASVS L1 Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation / blocking condition |
|---|---|---|---|---|---|
| T-16-01 | High | Spoofing / Elevation | `GET/PATCH /api/profile` | mitigate | Preserve `requireApiUser`, authenticated user ID, own-row Supabase/RLS write; auth-negative tests pass. Any client account-ID path blocks execution. |
| T-16-02 | Medium | Tampering / Integrity | overlapping theme PATCHes | mitigate | Monotonic coordinator plus final latest-value reconciliation and deferred Node/browser tests. |
| T-16-03 | Medium | Information disclosure | account boundary | mitigate | Account ID exists only in React props; never storage, body, logs, or feedback. |
| T-16-04 | Low | Tampering | localStorage/storage event | mitigate | Validate values with `isThemePreference`; ignore malformed input; authenticated server preference remains authority. |
| T-16-05 | Medium | Denial of service | denied/quota storage | mitigate | Catch reads/writes; DOM and authenticated PATCH continue; Node and browser checks inject failures. |
| T-16-06 | High | Credential disclosure | Playwright auth | mitigate | Require environment-only `TEST_EMAIL`/`TEST_PASSWORD`, no defaults/logging/artifacts; missing values block execution and any committed credential blocks completion. |
| T-16-07 | Medium | Repudiation / test data integrity | browser preference test | mitigate | Capture original preference and restore in `finally` through authenticated internal API. |
| T-16-08 | Medium | Accessibility | flashcard actions/radio focus | mitigate | WCAG 4.5:1 token checks plus actual keyboard focus/selection browser assertions. |
| T-16-SC | Low | Supply chain | dependencies | accept | Use installed Vitest and `playwright` only; no package install or dependency mutation. |
</threat_model>

<verification>
1. Confirm `TEST_EMAIL` and `TEST_PASSWORD` are present without printing values; absent values block plan execution at Task 1.
2. Run `npm run test -- src/lib/profile/themePreference.test.ts src/lib/profile/themePreferenceCoordinator.test.ts src/app/api/profile/route.test.ts src/components/settings/AppearanceSettings.test.tsx src/components/flashcards/FlashcardActions.test.ts --run`.
3. Run `npm run test:ide-themes -- --case full`; verify outer `finally` restores original account preference.
4. Run `npm run typecheck`.
5. Run exact focused lint: `npm run lint -- src/app/layout.tsx src/components/providers/app-root-providers.tsx src/components/providers/ThemePreferenceProvider.tsx src/lib/profile/themePreferenceCoordinator.ts src/lib/profile/themePreferenceCoordinator.test.ts src/app/api/profile/route.test.ts src/components/settings/AppearanceSettings.tsx src/components/settings/AppearanceSettings.test.tsx scripts/verify-ide-themes.mjs playwright.ide-themes.config.mjs src/components/flashcards/FlashcardActions.tsx src/components/flashcards/FlashcardActions.test.ts`.
6. Run `npm run build`; preserve unrelated generated `next-env.d.ts` changes and do not stage or revert them.
7. Inspect `git diff -- .planning/phases/16-ide-themes/16-03-PLAN.md src/app/layout.tsx src/components/providers src/lib/profile/themePreferenceCoordinator.ts src/lib/profile/themePreferenceCoordinator.test.ts src/app/api/profile/route.test.ts src/components/settings scripts/verify-ide-themes.mjs playwright.ide-themes.config.mjs src/components/flashcards src/app/globals.css package.json` and confirm no prohibited central, Study Together, schema, migration, dependency, external API, or unrelated changes.
</verification>

<success_criteria>
- THEME-03 / WR-02 / WR-03 / IN-01: account authority, optional storage, and latest-selection convergence pass pure Node decisions plus actual provider browser effects.
- THEME-04 / WR-04: Done/Next semantic foreground pairs pass WCAG AA 4.5:1 for normal and hover states in base and all named palettes.
- THEME-05 / WR-01: actual radios pass all arrow keys, wrap directions, roving tabindex, latest-only feedback, and account-backed persistence.
- Real `/api/profile` GET/PATCH tests cover auth, every valid theme, invalid input, malformed/null/missing fallback, own-user mapping, and current `error.message` persistence-failure response.
- No DOM Vitest environment, `@playwright/test`, dependency/schema change, schema push, external API artifact, central metadata edit, SOCIAL requirement, Study Together edit, completed artifact edit, or commit occurs.
- Central THEME traceability remains mapped to Phase 16; Study Together remains Phase 12.
</success_criteria>

<output>Create `.planning/phases/16-ide-themes/16-03-SUMMARY.md` after execution. Do not update central planning metadata during plan execution.</output>
