---
phase: 16-ide-themes
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/app/globals.css
  - src/components/settings/AppearanceSettings.tsx
  - src/app/(app)/settings/SettingsPageClient.tsx
  - src/components/layout/ThemeToggle.tsx
  - src/components/layout/AccountMenu.tsx
requirements: [THEME-04, THEME-05]
autonomous: false
must_haves:
  truths:
    - Four named IDE-inspired palettes visibly change core application surfaces.
    - Settings selector is keyboard usable and persists selected value.
    - System option chooses VS Code Dark/Light based on OS.
---
<objective>
Create palette token overrides and Settings appearance selector.

Purpose: make account preference usable and visibly coherent across core app UI.
Output: four CSS token palettes and accessible Appearance UI.
</objective>
<context>
@.planning/phases/16-ide-themes/16-CONTEXT.md
@.planning/phases/16-ide-themes/16-01-PLAN.md
@src/app/globals.css
@src/app/(app)/settings/SettingsPageClient.tsx
@src/components/layout/ThemeToggle.tsx
@src/components/layout/AccountMenu.tsx
</context>
<tasks>
<task type="auto">
<name>Task 1: Define semantic VS Code-inspired token palettes</name>
<files>src/app/globals.css</files>
<action>Keep base `:root` and `.dark` intact. Add `[data-theme="vscode-dark"]`, `[data-theme="vscode-light"]`, `[data-theme="monokai"]`, and `[data-theme="high-contrast"]` overrides for semantic shadcn variables, sidebar/chart variables, `--d2q-*`, and `--qp-*`. Use contrast-safe palette choices inspired by named IDE themes, not copied trademark assets. Ensure High Contrast meets strong text/border/focus contrast, respects existing `prefers-reduced-motion`, and does not depend on color alone for state. Do not refactor archived code. Minimize changes to hardcoded non-token colors; record remaining exceptions in summary.</action>
<verify><automated>npm run lint -- src/app/globals.css</automated></verify>
<done>Core shell, semantic components, quiz, and flashcard surfaces use selected palette tokens.</done>
</task>
<task type="auto">
<name>Task 2: Add accessible account-backed appearance selector</name>
<files>src/components/settings/AppearanceSettings.tsx, src/app/(app)/settings/SettingsPageClient.tsx, src/components/layout/ThemeToggle.tsx, src/components/layout/AccountMenu.tsx</files>
<action>Create Appearance card ahead of Language in Settings. Use radio-group-like buttons/cards with visible name, compact color swatches, selected checkmark/text, keyboard navigation, and `aria-live` persistence feedback. Options: Theo hệ thống, VS Code Dark, VS Code Light, Monokai, High Contrast. Use ThemePreferenceProvider public hook; no duplicate fetch/state. Keep quick toggle only if it maps system or exact VS Code light/dark semantics; otherwise make it link to Settings Appearance. Update EN/VI literals through current locale pattern or add safe literal fallback consistent with provider coverage.</action>
<verify><automated>npm run test -- src/components/settings --run && npm run typecheck</automated></verify>
<done>User can choose, understand, and persist an IDE-inspired theme from Settings.</done>
</task>
<task type="checkpoint:human-verify">
<name>Task 3: Verify themed core routes</name>
<action>On desktop and mobile-width viewport, sign in and choose each preset and System under light/dark OS. Reload, open a second tab, and sign in another browser/device. Confirm selected palette persists, no initial wrong-theme flash/hydration warning, keyboard selector works, focus remains visible, Settings/sidebar/topbar/quiz/flashcard surfaces retain readable contrast, and no core interaction loses meaning by color alone.</action>
<verify><automated>npm run build</automated></verify>
<done>Human confirms complete theme selection and visible palette behavior.</done>
</task>
</tasks>
