---
phase: 16-ide-themes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260731210000_profile_theme_preference.sql
  - src/app/api/profile/route.ts
  - src/lib/profile/themePreference.ts
  - src/lib/profile/themePreference.test.ts
  - src/app/layout.tsx
  - src/components/providers/ThemePreferenceProvider.tsx
requirements: [THEME-01, THEME-02, THEME-03]
autonomous: true
must_haves:
  truths:
    - Signed-in user preference survives device/browser changes.
    - First paint uses known server theme value without hydration mismatch.
    - Invalid API/database theme values safely fall back to system.
---
<objective>
Create validated theme preference persistence and SSR-safe application controller.

Purpose: account theme setting must be authoritative before selector UI and CSS presets.
Output: migration, profile API contract, typed preference utility, provider wired to root layout.
</objective>
<context>
@.planning/phases/16-ide-themes/16-CONTEXT.md
@src/app/layout.tsx
@src/app/api/profile/route.ts
@src/components/layout/ThemeToggle.tsx
@src/components/locale/LocaleProvider.tsx
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Add account theme preference contract</name>
<files>supabase/migrations/20260731210000_profile_theme_preference.sql, src/lib/profile/themePreference.ts, src/lib/profile/themePreference.test.ts, src/app/api/profile/route.ts</files>
<action>Add `profiles.theme_preference text not null default 'system'` with check constraint limited to `system`, `vscode-dark`, `vscode-light`, `monokai`, `high-contrast`; make migration idempotent for populated profiles. Create pure validation/default utility. Extend profile GET DTO and PATCH validation/update to expose only this allowlisted value. Existing own-profile authorization remains unchanged; unauthenticated callers cannot read/change preference. Tests cover every valid value and malformed/null fallback/rejection.</action>
<verify><automated>npm run test -- src/lib/profile/themePreference.test.ts --run</automated></verify>
<done>Theme preference is a validated account setting available through existing profile API.</done>
</task>
<task type="auto">
<name>Task 2: Apply server preference without hydration flash</name>
<files>src/app/layout.tsx, src/components/providers/ThemePreferenceProvider.tsx</files>
<action>Read authenticated user preference server-side using existing profile/server client pattern, pass it as initial value to a tiny client controller beneath existing `ThemeProvider`. Preserve `@teispace/next-themes` system behavior; convert system mode into selected VS Code dark/light `data-theme` based on resolved scheme. Named presets set `document.documentElement.dataset.theme`; compatible dark class stays true for VS Code Dark, Monokai, and High Contrast, false for VS Code Light. On client selection, update DOM immediately, persist through PATCH `/api/profile`, reconcile cross-tab `storage` events, and roll back/report failure safely. Do not introduce global store/dependency or a second source of truth.</action>
<verify><automated>npm run typecheck</automated></verify>
<done>Selected account theme is present before interactive Settings render and applies reliably across system mode changes.</done>
</task>
</tasks>
