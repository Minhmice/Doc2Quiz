---
phase: 03-canonical-knowledge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/server/openAiChatCompletion.ts
  - src/lib/server/ai-processing-config.ts
  - src/lib/server/resolveUserAiTier.ts
  - supabase/migrations/20260725130000_canonical_section_key.sql
  - src/lib/pipeline/canonicalPrompt.ts
  - src/lib/pipeline/canonicalSchemas.ts
  - src/lib/pipeline/canonicalPrompt.test.ts
  - src/lib/pipeline/canonicalSchemas.test.ts
autonomous: true
requirements:
  - CANON-08
must_haves:
  truths:
    - "Minimal server AI helpers exist and export postChatCompletionAssistantText + getAiProcessingConfig"
    - "canonical_sections has section_key column with unique index per document"
    - "Locked prompt loads at runtime from prompt/canonical_builder_v1.json without duplicating system/tasks/constraints in code"
    - "Zod schema rejects malformed LLM output shapes before any DB write path exists"
  artifacts:
    - path: src/lib/server/openAiChatCompletion.ts
      provides: "Server-direct OpenAI-compatible chat completion"
      exports: ["postChatCompletionAssistantText"]
    - path: src/lib/server/ai-processing-config.ts
      provides: "AI provider URL/key/model resolution"
      exports: ["getAiProcessingConfig", "isAiProcessingConfigured"]
    - path: supabase/migrations/20260725130000_canonical_section_key.sql
      provides: "section_key column for stable sec_001 IDs"
      contains: "section_key"
    - path: src/lib/pipeline/canonicalPrompt.ts
      provides: "Runtime prompt load + template substitution"
      exports: ["loadCanonicalPrompt", "buildCanonicalMessages", "CANONICAL_PROMPT_VERSION"]
    - path: src/lib/pipeline/canonicalSchemas.ts
      provides: "Zod mirror of locked output_schema"
      exports: ["canonicalBuilderOutputSchema"]
  key_links:
    - from: src/lib/pipeline/canonicalPrompt.ts
      to: prompt/canonical_builder_v1.json
      via: "readFile at runtime (D-13)"
      pattern: "canonical_builder_v1\\.json"
    - from: src/lib/pipeline/canonicalSchemas.ts
      to: prompt/canonical_builder_v1.json
      via: "hand-written Zod matching output_schema fields"
      pattern: "document_type.*theory.*exam.*mixed"
---

<objective>
Establish Phase 3 foundation: restore deleted server AI helpers, add `section_key` migration, and implement runtime prompt loading with Zod validation contracts.

Purpose: Wave 2 canonicalize service depends on AI infrastructure, stable section IDs (D-17), and locked prompt contract (D-13–D-15) before any LLM calls or DB writes.
Output: Restored `src/lib/server/*` AI trio, migration SQL, `canonicalPrompt.ts`, `canonicalSchemas.ts`, and unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-canonical-knowledge/03-CONTEXT.md
@.planning/phases/03-canonical-knowledge/03-RESEARCH.md
@.planning/phases/03-canonical-knowledge/03-PATTERNS.md
@prompt/canonical_builder_v1.json
@supabase/migrations/20260725120000_v21_baseline.sql
@.env.example

<interfaces>
From prompt/canonical_builder_v1.json (LOCKED — do not rewrite):
- version: "1.0"
- system, tasks[], constraints[], output_schema (title, filename, language, document_type, topics, canonical_markdown, sections[], extracted_questions[], warnings[])
- input template vars: source_id, source_type, original_filename, raw_markdown

From supabase/migrations/20260725120000_v21_baseline.sql canonical_sections:
- id uuid, user_id, canonical_document_id, ordinal, heading, body_markdown, section_type
- NO section_key yet — add via new migration (D-17)

From git HEAD src/lib/server/openAiChatCompletion.ts (restore):
- postChatCompletionAssistantText({ configUrl, apiKey, model, messages, responseFormatJsonObject?, temperature?, signal? })
- Returns { ok: true, text } | { ok: false, status, body }

From git HEAD src/lib/server/ai-processing-config.ts (restore):
- getAiProcessingConfig(tier: "free" | "pro"): { url, key, model }
- isAiProcessingConfigured(): boolean
- getChatCompletionsUrl(configUrl)

From git HEAD src/lib/server/resolveUserAiTier.ts (restore):
- resolveUserAiTier(user): "free" | "pro"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore minimal server AI infrastructure</name>
  <files>src/lib/server/openAiChatCompletion.ts, src/lib/server/ai-processing-config.ts, src/lib/server/resolveUserAiTier.ts</files>
  <action>
    Restore these three files from git HEAD. They are deleted in the working tree but required for Phase 3 LLM calls (D-01, RESEARCH Pitfall 1).

    Restore ONLY:
    - openAiChatCompletion.ts — postChatCompletionAssistantText with response_format json_object support
    - ai-processing-config.ts — reads AI_PROVIDER_URL, AI_PROVIDER_KEY, AI_MODEL_FREE, AI_MODEL_PRO from process.env
    - resolveUserAiTier.ts — tier resolution from env/user metadata

    Do NOT restore generateFromFile/*, canonical_document_extractions, or full v1 pipeline (D-12). Do NOT restore /api/ai/forward in this task — canonicalize uses server-direct postChatCompletionAssistantText per RESEARCH Pattern 4.

    Verify imports resolve: openAiChatCompletion imports getChatCompletionsUrl from ai-processing-config.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | Select-String -NotMatch "openAiChatCompletion|ai-processing-config|resolveUserAiTier" | Select-String "error" ; if (-not (Test-Path src/lib/server/openAiChatCompletion.ts)) { exit 1 }</automated>
  </verify>
  <done>Three server AI files exist on disk; typecheck passes for restored modules.</done>
</task>

<task type="auto">
  <name>Task 2: Add section_key migration (D-17)</name>
  <files>supabase/migrations/20260725130000_canonical_section_key.sql</files>
  <action>
    Create new migration (do not edit baseline file) per D-17 locked decision — use dedicated column, not jsonb metadata:

    1. ALTER TABLE public.canonical_sections ADD COLUMN IF NOT EXISTS section_key text NULL;
    2. CREATE UNIQUE INDEX IF NOT EXISTS canonical_sections_document_section_key_unique ON public.canonical_sections (canonical_document_id, section_key) WHERE section_key IS NOT NULL;

    Wrap in begin/commit. No RLS changes needed — existing policies cover the table.

    Document in migration comment: section_key stores LLM stable IDs like sec_001 for Phase 5 flashcard coverage picker.
  </action>
  <verify>
    <automated>grep -q "section_key" supabase/migrations/20260725130000_canonical_section_key.sql</automated>
  </verify>
  <done>Migration file adds section_key column and partial unique index.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prompt loader + Zod schemas + unit tests</name>
  <files>src/lib/pipeline/canonicalPrompt.ts, src/lib/pipeline/canonicalSchemas.ts, src/lib/pipeline/canonicalPrompt.test.ts, src/lib/pipeline/canonicalSchemas.test.ts</files>
  <behavior>
    - loadCanonicalPrompt returns version "1.0" from prompt/canonical_builder_v1.json (D-13)
    - substituteTemplate replaces {{source_id}}, {{source_type}}, {{original_filename}}, {{raw_markdown}} in input values (D-14)
    - buildCanonicalMessages assembles system from spec.system + numbered tasks + bullet constraints + serialized output_schema — never hardcode prompt text in TS (D-13)
    - canonicalBuilderOutputSchema accepts valid fixture matching output_schema shape
    - canonicalBuilderOutputSchema rejects empty canonical_markdown, invalid document_type, section id not matching sec_NNN pattern (CANON-08)
    - section content_type enum validates theory|question|answer_key|example|reference
    - extracted_questions.answer accepts null
  </behavior>
  <action>
    Implement canonicalPrompt.ts:
    - loadCanonicalPrompt(): read prompt/canonical_builder_v1.json via node:fs/promises + path.join(process.cwd(), ...), module-level cache
    - substituteTemplate(template, vars): replace /\{\{(\w+)\}\}/g
    - buildCanonicalMessages(spec, vars): return { system, user } where user is JSON.stringify of substituted input block
    - export CANONICAL_PROMPT_VERSION from loaded spec.version

    Implement canonicalSchemas.ts mirroring locked output_schema exactly (D-15):
    - document_type: z.enum(["theory", "exam", "mixed"])
    - section id: z.string().regex(/^sec_\d{3}$/)
    - filename: z.string().min(1) (endsWith .md optional — prompt shows filename.md as example not strict requirement)
    - sections: z.array(...).min(1)
    - export type CanonicalBuilderOutput = z.infer&lt;typeof canonicalBuilderOutputSchema&gt;

    Write tests first (RED), then implement (GREEN). No DB or LLM calls in this task.
  </action>
  <verify>
    <automated>npx vitest run src/lib/pipeline/canonicalPrompt.test.ts src/lib/pipeline/canonicalSchemas.test.ts --reporter=dot</automated>
  </verify>
  <done>Prompt loader and Zod schema modules pass unit tests; prompt text lives only in JSON file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→filesystem | Prompt JSON read from disk; must not be exposed to client bundle |
| env→server | AI_PROVIDER_KEY stays server-only via ai-processing-config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information disclosure | ai-processing-config.ts | mitigate | Never export API keys; keys read from process.env only |
| T-03-02 | Tampering | canonicalSchemas.ts | mitigate | Zod rejects malformed LLM shapes before persistence (CANON-08) |
| T-03-03 | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan; existing zod only |
| T-03-SC | Tampering | dependency installs | accept | No installs this wave |
</threat_model>

<verification>
- npx vitest run src/lib/pipeline/canonicalPrompt.test.ts src/lib/pipeline/canonicalSchemas.test.ts
- npm run typecheck
- Migration file contains section_key column
</verification>

<success_criteria>
- Server AI trio restored from git HEAD
- section_key migration ready for Supabase apply
- Prompt loads at runtime; Zod schema validates locked output_schema shape
- All CANON-08 validation gates exist before Wave 2 builder
</success_criteria>

<output>
Create `.planning/phases/03-canonical-knowledge/03-01-SUMMARY.md` when done
</output>
