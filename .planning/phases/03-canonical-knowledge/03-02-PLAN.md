---
phase: 03-canonical-knowledge
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/lib/pipeline/canonicalize.ts
  - src/lib/pipeline/canonicalize.test.ts
  - src/app/api/study-sets/[id]/canonicalize/route.ts
  - src/app/api/study-sets/[id]/canonical/route.ts
  - src/app/api/study-sets/[id]/canonicalize/route.test.ts
autonomous: true
requirements:
  - CANON-01
  - CANON-02
  - CANON-03
  - CANON-04
  - CANON-05
  - CANON-06
  - CANON-07
  - CANON-08
user_setup:
  - service: ai-provider
    why: "Canonical Knowledge Builder requires OpenAI-compatible chat API"
    env_vars:
      - name: AI_PROVIDER_URL
        source: ".env — OpenAI-compatible base URL"
      - name: AI_PROVIDER_KEY
        source: ".env — API key for upstream provider"
must_haves:
  truths:
    - "POST /api/study-sets/[id]/canonicalize transforms raw_markdown into canonical knowledge via locked prompt + LLM"
    - "Successful run sets pipeline_stage=canonical, persists canonical_markdown, sections with section_key, and metadata"
    - "Failed run returns 422, sets canonicalization_status=failed, does not overwrite existing canonical data"
    - "GET /api/study-sets/[id]/canonical returns read-only canonical preview payload"
    - "Builder cleans noise, preserves structure, detects language/type, extracts Q&A, splits sections, generates title/filename per CANON-01–07"
  artifacts:
    - path: src/lib/pipeline/canonicalize.ts
      provides: "runCanonicalize orchestration"
      exports: ["runCanonicalize", "CanonicalizeError", "CanonicalizeValidationError"]
    - path: src/app/api/study-sets/[id]/canonicalize/route.ts
      provides: "POST canonicalize endpoint"
      exports: ["POST"]
    - path: src/app/api/study-sets/[id]/canonical/route.ts
      provides: "GET canonical preview data"
      exports: ["GET"]
  key_links:
    - from: src/lib/pipeline/canonicalize.ts
      to: src/lib/server/openAiChatCompletion.ts
      via: "postChatCompletionAssistantText with responseFormatJsonObject"
      pattern: "postChatCompletionAssistantText"
    - from: src/lib/pipeline/canonicalize.ts
      to: canonical_documents + canonical_sections
      via: "validate first, then delete sections + update doc + update study_sets"
      pattern: "pipeline_stage.*canonical"
    - from: src/app/api/study-sets/[id]/canonicalize/route.ts
      to: src/lib/pipeline/canonicalize.ts
      via: "runCanonicalize({ supabase, userId, studySetId })"
      pattern: "runCanonicalize"
---

<objective>
Implement the Canonical Knowledge Builder service and API routes: LLM orchestration with locked prompt, Zod validation, Supabase persistence, and read endpoint for preview.

Purpose: Fulfill CANON-01–08 and D-01–D-08 — transform raw Markdown into cleaned canonical knowledge stored in Supabase.
Output: `canonicalize.ts` service, POST canonicalize + GET canonical routes, unit/integration tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-canonical-knowledge/03-CONTEXT.md
@.planning/phases/03-canonical-knowledge/03-RESEARCH.md
@.planning/phases/03-canonical-knowledge/03-PATTERNS.md
@prompt/canonical_builder_v1.json
@src/lib/pipeline/ingest.ts
@src/app/api/study-sets/[id]/canonicalize/route.ts
@src/app/api/study-sets/[id]/ingest/route.ts
@src/app/api/study-sets/[id]/ingest/route.test.ts

# Prior wave output
@.planning/phases/03-canonical-knowledge/03-01-SUMMARY.md

<interfaces>
From 03-01 (Wave 1):
- loadCanonicalPrompt(), buildCanonicalMessages(), CANONICAL_PROMPT_VERSION from canonicalPrompt.ts
- canonicalBuilderOutputSchema, CanonicalBuilderOutput from canonicalSchemas.ts
- postChatCompletionAssistantText from openAiChatCompletion.ts
- getAiProcessingConfig, isAiProcessingConfigured from ai-processing-config.ts
- resolveUserAiTier from resolveUserAiTier.ts

From canonicalize/route.ts stub (keep verifyStudySet):
- requireApiUser() auth pattern
- verifyStudySet(supabase, userId, studySetId) ownership guard

D-16 schema → DB mapping:
| LLM field | DB target |
| title | metadata.title + study_sets.title |
| filename | metadata.clean_filename |
| language | metadata.language |
| document_type | metadata.content_type |
| topics | metadata.topics |
| canonical_markdown | canonical_documents.canonical_markdown |
| sections[i].id | canonical_sections.section_key |
| sections[i] order | canonical_sections.ordinal (1-based) |
| sections[i].title | canonical_sections.heading |
| sections[i].content | canonical_sections.body_markdown |
| sections[i].content_type | canonical_sections.section_type |
| extracted_questions | metadata.extracted_questions |
| warnings | metadata.warnings |
| prompt version | metadata.prompt_version |

stripJsonFence pattern (from git HEAD extractCanonicalSourceUnits.ts):
- Trim markdown code fences before JSON.parse
- One repair retry on Zod failure with schema error in follow-up message
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: runCanonicalize service — LLM, validate, persist</name>
  <files>src/lib/pipeline/canonicalize.ts, src/lib/pipeline/canonicalize.test.ts</files>
  <behavior>
    - Throws CanonicalizeValidationError when raw_markdown empty or pipeline_stage before raw (D-07)
    - Returns 503-equivalent error when isAiProcessingConfigured() is false
    - Calls LLM with locked prompt; parses JSON; validates with canonicalBuilderOutputSchema (CANON-08)
    - On Zod/LLM failure: sets metadata.canonicalization_status=failed without deleting existing sections (D-06)
    - On success: updates canonical_markdown, replaces sections with section_key, merges metadata, sets pipeline_stage=canonical (D-05)
    - Maps all D-16 fields including extracted_questions and warnings (CANON-05)
    - Truncates raw_markdown at 120_000 chars with warning when over budget (RESEARCH Pattern 6)
    - LLM + validate completes BEFORE any section delete (Pitfall 5)
  </behavior>
  <action>
    Implement src/lib/pipeline/canonicalize.ts following ingest.ts error-class pattern:

    Export CanonicalizeValidationError, CanonicalizeError, runCanonicalize({ supabase, userId, studySetId }).

    Pre-flight (D-07):
    1. Load study_sets.pipeline_stage — must be at least "raw" (reject "input" with CanonicalizeValidationError)
    2. Load canonical_documents row (raw_markdown, original_filename, metadata)
    3. Reject if raw_markdown trim empty

    Builder flow (D-01, D-13–D-15):
    1. loadCanonicalPrompt + buildCanonicalMessages with vars: source_id=studySetId, source_type=metadata.input_type ?? "file", original_filename=doc.original_filename ?? study set title, raw_markdown=(possibly truncated)
    2. getAiProcessingConfig(resolveUserAiTier(user)) — pass user from caller context; if not configured throw CanonicalizeError
    3. postChatCompletionAssistantText with responseFormatJsonObject: true, temperature: 0
    4. stripJsonFence + JSON.parse + canonicalBuilderOutputSchema.safeParse
    5. On parse fail: one repair retry with assistant message listing Zod errors; if still fail throw CanonicalizeError

    Persistence (D-05, D-06, D-16, D-17):
    - mapCanonicalOutputToDb helper (inline or separate export): build section rows with ordinal 1-based, section_key from sections[i].id
    - Success: UPDATE canonical_documents (canonical_markdown + merged metadata including prompt_version, canonicalization_status="ok"); DELETE canonical_sections for doc id; INSERT new sections; UPDATE study_sets SET pipeline_stage='canonical', title=output.title
    - Failure handler persistCanonicalizationFailure: merge metadata.canonicalization_status="failed" + canonicalization_error message ONLY — do not clear canonical_markdown or delete sections

    Do NOT import forwardAiPost or call /api/ai/forward from server (RESEARCH Pattern 4). Do NOT use generateFromFile/canonical* v1 pipeline (D-12).

    Tests: mock postChatCompletionAssistantText and Supabase client chains (mirror ingest.test.ts). Cover validation errors, successful mapping, failure without section delete.
  </action>
  <verify>
    <automated>npx vitest run src/lib/pipeline/canonicalize.test.ts --reporter=dot</automated>
  </verify>
  <done>runCanonicalize passes tests; implements full CANON-01–08 builder logic and D-05/D-06 persistence rules.</done>
</task>

<task type="auto">
  <name>Task 2: POST canonicalize + GET canonical API routes</name>
  <files>src/app/api/study-sets/[id]/canonicalize/route.ts, src/app/api/study-sets/[id]/canonical/route.ts</files>
  <action>
  Replace 501 stub in canonicalize/route.ts (D-P1-17):

  POST handler:
  - Keep existing verifyStudySet helper
  - Delegate to runCanonicalize({ supabase: auth.supabase, userId: auth.user.id, studySetId: id })
  - Error mapping (mirror ingest/route.ts): CanonicalizeValidationError → 400 { error: "validation_error", message }; CanonicalizeError / ZodError → 422 { error: "canonicalize_error", message }; unexpected → 500
  - Success 200: { studySetId, pipelineStage: "canonical", sectionCount, title }
  - export const runtime = "nodejs"; export const maxDuration = 120;

  Create GET src/app/api/study-sets/[id]/canonical/route.ts (D-08):
  - requireApiUser + ownership via study_sets select
  - Join canonical_documents (canonical_markdown, metadata, original_filename) + canonical_sections ordered by ordinal asc
  - Select section fields: id, ordinal, heading, body_markdown, section_type, section_key
  - Return { data: { studySet: { id, title, pipelineStage }, document: { canonicalMarkdown, metadata, originalFilename }, sections: [...] } }
  - 404 when study set or document missing

  Both routes: user_id scoping on all queries (RLS + explicit eq).
  </action>
  <verify>
    <automated>grep -q "runCanonicalize" src/app/api/study-sets/[id]/canonicalize/route.ts &amp;&amp; grep -q "export async function GET" src/app/api/study-sets/[id]/canonical/route.ts</automated>
  </verify>
  <done>POST canonicalize replaces 501; GET canonical returns preview payload.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route integration tests</name>
  <files>src/app/api/study-sets/[id]/canonicalize/route.test.ts</files>
  <behavior>
    - 401 when requireApiUser returns error
    - 404 when study set not found
    - 400 when runCanonicalize throws CanonicalizeValidationError (empty raw)
    - 422 when runCanonicalize throws CanonicalizeError
    - 200 with pipelineStage canonical on success
  </behavior>
  <action>
    Mirror ingest/route.test.ts structure: vi.mock runCanonicalize and requireApiUser.

    Cases: unauthenticated 401, not found 404, validation 400, canonicalize failure 422, success 200 with mocked result.

    Do not call real LLM in tests.
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/study-sets/[id]/canonicalize/route.test.ts" --reporter=dot</automated>
  </verify>
  <done>Route tests pass for auth, validation, error, and success paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→POST /canonicalize | Authenticated user triggers LLM on their raw_markdown |
| server→LLM upstream | Untrusted model output validated before DB write |
| client→GET /canonical | Read-only; RLS + user_id filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | raw_markdown in LLM prompt | mitigate | Locked constraints D-18; Zod output validation; no tool execution |
| T-03-05 | Tampering | LLM hallucinated content | mitigate | CANON-08 prompt constraints; reject invalid schema before persist |
| T-03-06 | Elevation | Cross-user canonical access | mitigate | requireApiUser + eq user_id on all queries; RLS |
| T-03-07 | Information disclosure | AI_PROVIDER_KEY | mitigate | Server-only getAiProcessingConfig; never return key in responses |
| T-03-08 | Denial of service | Oversized raw_markdown | mitigate | 120k char truncation + max_tokens cap in chat completion |
</threat_model>

<verification>
- npx vitest run src/lib/pipeline/canonicalize.test.ts src/app/api/study-sets/[id]/canonicalize/route.test.ts
- npm run typecheck
- Manual: POST /canonicalize on study set with pipeline_stage=raw returns 200 when AI env configured
</verification>

<success_criteria>
- CANON-01–08 fulfilled via locked prompt + validated persistence
- POST canonicalize and GET canonical routes operational
- Failure paths preserve existing canonical data (D-06)
- Idempotent re-run allowed (D-07)
</success_criteria>

<output>
Create `.planning/phases/03-canonical-knowledge/03-02-SUMMARY.md` when done
</output>
