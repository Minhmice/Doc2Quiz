---
phase: 03-canonical-knowledge
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - package.json
  - src/lib/client/canonicalizeStudySet.ts
  - src/components/canonical/CanonicalPreviewHeader.tsx
  - src/components/canonical/CanonicalMetadataChips.tsx
  - src/components/canonical/CanonicalMarkdownViewer.tsx
  - src/components/canonical/CanonicalSectionToc.tsx
  - src/components/canonical/CanonicalizeProgressCard.tsx
  - src/components/canonical/CanonicalNextStepPlaceholder.tsx
  - src/app/(app)/sets/[id]/source/page.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - CANON-01
  - CANON-02
  - CANON-03
  - CANON-04
  - CANON-05
  - CANON-06
  - CANON-07
user_setup: []
must_haves:
  truths:
    - "User landing on /sets/[id]/source after ingest sees canonical preview (not legacy redirect)"
    - "When pipeline_stage=raw with non-empty raw_markdown, page auto-POSTs canonicalize and shows progress"
    - "When pipeline_stage>=canonical, page shows metadata chips, markdown viewer, optional TOC, Phase 4 placeholder CTA"
    - "User can view cleaned canonical output before choosing learning mode (ROADMAP success criterion 4)"
  artifacts:
    - path: src/app/(app)/sets/[id]/source/page.tsx
      provides: "Canonical preview page with state machine"
    - path: src/lib/client/canonicalizeStudySet.ts
      provides: "POST canonicalize + GET canonical client helpers"
      exports: ["postCanonicalize", "fetchCanonicalPreview"]
    - path: src/components/canonical/CanonicalMarkdownViewer.tsx
      provides: "Read-only prose markdown render"
  key_links:
    - from: src/app/(app)/sets/[id]/source/page.tsx
      to: /api/study-sets/[id]/canonicalize
      via: "postCanonicalize on raw stage mount"
      pattern: "canonicalize"
    - from: src/app/(app)/sets/[id]/source/page.tsx
      to: /api/study-sets/[id]/canonical
      via: "fetchCanonicalPreview after canonicalize success"
      pattern: "fetchCanonicalPreview"
---

<objective>
Build the canonical knowledge preview UI at `/sets/[id]/source` with auto-canonicalize on raw stage, read-only markdown viewer, and Phase 4 placeholder CTA.

Purpose: Fulfill D-09–D-11 and ROADMAP success criterion — user reviews canonical output before mode selection.
Output: Client API helpers, six canonical components, replaced source page per 03-UI-SPEC.md.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-canonical-knowledge/03-UI-SPEC.md
@.planning/phases/03-canonical-knowledge/03-CONTEXT.md
@.planning/phases/03-canonical-knowledge/03-PATTERNS.md
@src/app/(app)/sets/[id]/source/page.tsx
@src/app/(app)/sets/[id]/layout.tsx
@src/components/edit/new/import/IngestProgressCard.tsx
@src/lib/client/ingestStudySet.ts

# Prior wave output
@.planning/phases/03-canonical-knowledge/03-02-SUMMARY.md

<interfaces>
From GET /api/study-sets/[id]/canonical (03-02):
{ data: { studySet: { id, title, pipelineStage }, document: { canonicalMarkdown, metadata, originalFilename }, sections: [{ id, ordinal, heading, bodyMarkdown, sectionType, sectionKey }] } }

From POST /api/study-sets/[id]/canonicalize (03-02):
Success: { studySetId, pipelineStage: "canonical", sectionCount, title }
Errors: { error, message } with 400/422/500

metadata fields for chips (D-16):
- language → metadata.language
- content_type → metadata.content_type (theory|exam|mixed)
- sections.length for count

UI-SPEC state machine:
loadMeta → raw+content → auto canonicalize → preview | emptySource | error
Use canonicalizeStartedRef to prevent double POST on Strict Mode (D-07 guard)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client API helpers + markdown dependencies</name>
  <files>package.json, src/lib/client/canonicalizeStudySet.ts</files>
  <action>
    Add npm dependencies per UI-SPEC: react-markdown and remark-gfm. Do NOT add @tailwindcss/typography — use scoped d2q-prose utilities instead.

    Implement canonicalizeStudySet.ts mirroring ingestStudySet.ts fetch patterns:

    postCanonicalize(studySetId: string): POST /api/study-sets/${id}/canonicalize, throw Error with API message on failure, map TypeError to "Connection lost. Check your network and try again."

    fetchCanonicalPreview(studySetId: string): GET /api/study-sets/${id}/canonical, return typed preview payload, throw on non-OK with friendly message.

    Export types for preview payload aligned with GET route response shape.
  </action>
  <verify>
    <automated>grep -q "postCanonicalize" src/lib/client/canonicalizeStudySet.ts &amp;&amp; grep -q "remark-gfm" package.json</automated>
  </verify>
  <done>Client helpers exist; markdown render dependencies installed.</done>
</task>

<task type="auto">
  <name>Task 2: Canonical preview components (UI-SPEC)</name>
  <files>src/components/canonical/CanonicalPreviewHeader.tsx, src/components/canonical/CanonicalMetadataChips.tsx, src/components/canonical/CanonicalMarkdownViewer.tsx, src/components/canonical/CanonicalSectionToc.tsx, src/components/canonical/CanonicalizeProgressCard.tsx, src/components/canonical/CanonicalNextStepPlaceholder.tsx, src/app/globals.css</files>
  <action>
    Build six components per 03-UI-SPEC.md — identity preservation, no redesign:

    CanonicalPreviewHeader: eyebrow "Study set" + optional filename, H1 "Canonical knowledge · {title}", subcopy from UI-SPEC.

    CanonicalMetadataChips: three Badge variant="outline" chips — Language (uppercase), Content (Theory|Exam|Mixed map), Sections count. Show "—" for missing values. Skeleton loading state.

    CanonicalizeProgressCard: adapt IngestProgressCard — border-l-[6px] border-d2q-accent, eyebrow "Canonical knowledge", headline "Building canonical knowledge…", optional subcopy, d2q-progress-stripes, aria-live="polite". Error variant eyebrow "Canonicalization failed".

    CanonicalMarkdownViewer: react-markdown + remark-gfm, no rehype-raw. Links: rel="noopener noreferrer" target="_blank". Scoped d2q-prose class in globals.css — max-w-[72ch], typography per UI-SPEC table. Math: plain text fallback acceptable for Phase 3 if MathJax wiring is heavy.

    CanonicalSectionToc: nav aria-label="Document sections". Show only when sections.length >= 2. Desktop lg+ sidebar w-60; mobile collapsible toggle "Sections ({n})" with List icon. scrollIntoView with 80px offset; active item text-chart-2 aria-current. prefers-reduced-motion: behavior auto.

    CanonicalNextStepPlaceholder: footer strip with disabled "Choose learning mode" button (opacity-50, aria-disabled), helper "Choose learning mode next — quiz or flashcards." No onClick in Phase 3 (D-10).

    Use existing Badge, Button, Alert, ScrollArea, Skeleton from @/components/ui. Normalize font weights to 400 body / 800 labels per UI-SPEC.
  </action>
  <verify>
    <automated>test -f src/components/canonical/CanonicalMarkdownViewer.tsx &amp;&amp; test -f src/components/canonical/CanonicalizeProgressCard.tsx &amp;&amp; grep -q "d2q-prose" src/app/globals.css</automated>
  </verify>
  <done>All six canonical components render per UI-SPEC copy and layout contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Replace source page — state machine + auto-canonicalize</name>
  <files>src/app/(app)/sets/[id]/source/page.tsx</files>
  <action>
    Replace entire source/page.tsx — remove legacy router.replace to editQuiz/editFlashcards (D-09).

    Implement UI-SPEC state machine:
    - loading: show "Loading…" role="status"
    - missing: redirect to /dashboard
    - emptySource: pipeline_stage=raw but empty raw_markdown — "No source content" copy
    - canonicalizing: show CanonicalizeProgressCard; aria-busy on main
    - canonicalizeError: progress card error variant + Alert destructive + enabled "Try again" button
    - preview: header + chips + TOC (if >=2 sections) + CanonicalMarkdownViewer + CanonicalNextStepPlaceholder

    Auto-canonicalize (D-10, UI-SPEC):
    1. On mount after meta load: if pipelineStage === "raw" and raw_markdown non-empty → postCanonicalize
    2. Use useRef canonicalizeStartedRef to prevent double POST on React Strict Mode remount
    3. On success: fetchCanonicalPreview → transition to preview
    4. On failure: toast (sonner) + canonicalizeError state; Try again re-POSTs

    Preview data: prefer fetchCanonicalPreview API over direct Supabase reads for canonical stage (D-08). Initial meta may still use Supabase for pipeline_stage check.

    Layout: Back link to /dashboard (text-chart-2 font-label). Do not nest competing max-w wrapper — parent sets/[id]/layout.tsx provides container. space-y-6 rhythm. article aria-label="Canonical knowledge preview" around viewer.

    Remove quiz/flashcard generation buttons (D-10). Keep read-only — no edit affordances.

    Optional: wrap preview reveal in d2q-route-transition class.
  </action>
  <verify>
    <automated>grep -q "postCanonicalize" src/app/(app)/sets/[id]/source/page.tsx &amp;&amp; grep -q "Canonical knowledge" src/app/(app)/sets/[id]/source/page.tsx &amp;&amp; ! grep -q "router.replace" src/app/(app)/sets/[id]/source/page.tsx</automated>
  </verify>
  <done>Source page implements full preview flow; auto-canonicalize on raw; no legacy redirects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API | Authenticated fetch to canonicalize/canonical routes |
| markdown render | Untrusted canonical_markdown from user's own document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | CanonicalMarkdownViewer | mitigate | No rehype-raw; remark-gfm only; links get noopener noreferrer |
| T-03-10 | Spoofing | API calls | mitigate | Same-origin fetch with session cookies; server auth on routes |
| T-03-11 | Tampering | npm installs | mitigate | react-markdown + remark-gfm are established packages; slopcheck before install |
| T-03-SC | Tampering | react-markdown install | mitigate | Verify npmjs.com package pages before npm install |
</threat_model>

<verification>
- npm run typecheck
- npm run build
- Visit /sets/{id}/source after ingest: progress card → preview with chips and markdown
- Verify no Quiz/Flashcards generation buttons present
</verification>

<success_criteria>
- Canonical preview page replaces legacy redirect (D-09)
- Auto-canonicalize on raw stage with progress UI (D-10)
- Read-only viewer with metadata chips and Phase 4 placeholder (D-11)
- User can view canonical output before choosing learning mode
</success_criteria>

<output>
Create `.planning/phases/03-canonical-knowledge/03-03-SUMMARY.md` when done
</output>
