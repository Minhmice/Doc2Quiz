---
phase: 24-vision-parse-fewer-round-trips-single-request-or-max-window-
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/components/ai/AiParseSection.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "`handleVisionParse` uses the same batch count/progress semantics as `runVisionBatchSequential` (no duplicate `buildVisionBatches(pages,10,2)` when runner already computes batches — either pass counts from runner callback or share helper)."
    - "Overlay log line reflects **actual** batch count (e.g. `1 request` when single batch)."
    - "`npm run build` passes."
  artifacts:
    - path: src/components/ai/AiParseSection.tsx
      provides: "accurate progress + copy for min-requests vision"
  key_links:
    - from: AiParseSection.tsx
      to: runVisionBatchSequential.ts
      via: "optional `onBatchPlan` or precomputed `batches.length`"
      pattern: "single source of truth for batch list"
---

<objective>
**Wave 2 — UI wiring:** Align `AiParseSection` with wave-1 batching defaults; fix duplicate batch computation; improve user-visible messages for “one API call” vs fallback.
</objective>

<context>
@.planning/phases/24-vision-parse-fewer-round-trips-single-request-or-max-window-/24-01-PLAN.md
@src/components/ai/AiParseSection.tsx
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Progress bar off-by-one vs runner | Medium | Export `countBatches(pages, opts)` from lib or return `batches` from a thin `planVisionBatches` export |
| User thinks attach mode is batched | Low | Log line clarifies attach vs batch path unchanged |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Single source of truth for batch list</name>
  <files>src/components/ai/AiParseSection.tsx,src/lib/ai/visionBatching.ts or src/lib/ai/runVisionBatchSequential.ts</files>
  <read_first>src/components/ai/AiParseSection.tsx</read_first>
  <action>
Remove redundant `buildVisionBatches(pages, 10, 2)` in `handleVisionParse` when only used for **logging/progress**, by importing the same policy helper used in `runVisionBatchSequential` (e.g. export `planVisionBatches(pages, options)` from `visionBatching.ts` or re-export batch array from runner before loop — choose smallest diff).

Update overlay string: describe **≤N requests** using real `batches.length` after policy.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Grep shows at most one place constructing default batch list for a given parse run (runner + shared planner).
  </acceptance_criteria>
  <done>UI matches runner batching.</done>
</task>

<task type="auto">
  <name>Task 2: Build verification</name>
  <files>(none — command only)</files>
  <read_first></read_first>
  <action>
Run `npm run build` from repo root; fix any TS errors from wave 1–2.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Build exits 0.
  </acceptance_criteria>
  <done>Phase 24 waves ready for SUMMARY + execute sign-off.</done>
</task>

</tasks>
