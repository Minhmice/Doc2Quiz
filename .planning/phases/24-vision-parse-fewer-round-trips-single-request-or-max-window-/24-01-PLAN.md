---
phase: 24-vision-parse-fewer-round-trips-single-request-or-max-window-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai/visionBatching.ts
  - src/lib/ai/runVisionBatchSequential.ts
  - src/lib/ai/visionPrompts.ts
  - src/lib/ai/parseVisionQuizResponse.ts
  - src/lib/ai/parseVisionFlashcardResponse.ts
  - src/lib/ai/validateVisionQuizItems.ts
  - src/lib/ai/validateVisionFlashcardItems.ts
  - src/lib/ai/visionParseCache.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "`runVisionBatchSequential` accepts batching parameters (or a named preset) and uses `buildVisionBatches(pages, batchSize, overlap)` with a path that yields **one batch** when `pages.length` fits the default cap and provider accepts it."
    - "When `overlap === 0`, vision user prompt instructs the model to tag each item with **source page number(s)**; validators reject items missing page provenance in that mode (or attach `sourcePages` in post-parse)."
    - "On hard failure of a monolithic batch, runner retries once then **splits** into smaller batches (documented algorithm — e.g. fall back to 10/2) without dropping already-successful items from earlier sub-runs."
  artifacts:
    - path: src/lib/ai/visionBatching.ts
      provides: "`buildVisionBatches` unchanged semantics; new helper e.g. `buildVisionBatchesPreferredPolicy(pages, opts)` optional"
    - path: src/lib/ai/runVisionBatchSequential.ts
      provides: "configurable batching + split-on-failure"
  key_links:
    - from: src/lib/ai/visionPrompts.ts
      to: runVisionBatchSequential.ts
      via: "user prompt includes overlap=0 rules"
      pattern: "keep prompts in one module"
---

<objective>
**Wave 1 — Library only:** Make vision-batch parsing use **minimal round-trips** (ideally **one** `postVisionBatchCompletion` for full capped PDFs), with **safe fallback** to multi-batch windows. Tighten **prompt + validation** so items remain page-attributable without batch overlap.
</objective>

<context>
@.planning/phases/24-vision-parse-fewer-round-trips-single-request-or-max-window-/24-CONTEXT.md
@src/lib/ai/runVisionBatchSequential.ts
@src/lib/ai/visionBatching.ts
@src/lib/ai/visionPrompts.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Provider 413 / max images | High | Catch + split batches; user-visible `failedBatches` |
| JSON too large / truncated | High | Smaller batches on `finish_reason` / parse errors |
| Cache serves stale shape after prompt change | Medium | Optional `VISION_PROMPT_VERSION` in hash input |
| Overlap=0 loses dedupe across boundary | Low | Model must emit page tags; tests on multi-page fixture |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Batching policy API</name>
  <files>src/lib/ai/visionBatching.ts,src/lib/ai/runVisionBatchSequential.ts</files>
  <read_first>src/lib/pdf/renderPagesToImages.ts</read_first>
  <action>
Add exported helpers or options on `RunVisionBatchSequentialOptions`, e.g. `batchSize?: number`, `overlap?: number`, or `batchingPreset: "min_requests" | "legacy_10_2"`.

Default **`min_requests`**: `batchSize = min(pages.length, VISION_MAX_PAGES_DEFAULT)` (import constant or duplicate safe max), `overlap = 0` when `pages.length > 1`.

Keep **`legacy_10_2`** behavior available for forced fallback.

Implement **split-on-failure** inside `runVisionBatchSequential`: if all attempts fail for a batch and batch was single full-document window, re-enter loop with `legacy_10_2` batches for the same `pages` (clear partial state for that wave only; append successful items). Document in code comment.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Unit-level: `buildVisionBatches` with (20 pages, 20, 0) returns 1 batch covering all indexes.
    - `runVisionBatchSequential` uses new defaults without `AiParseSection` changes (wave 2 wires UI).
  </acceptance_criteria>
  <done>Runner owns batch policy + fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Prompts + validation for overlap=0</name>
  <files>src/lib/ai/visionPrompts.ts,src/lib/ai/parseVisionQuizResponse.ts,src/lib/ai/parseVisionFlashcardResponse.ts,src/lib/ai/validateVisionQuizItems.ts,src/lib/ai/validateVisionFlashcardItems.ts</files>
  <read_first>src/types/visionParse.ts</read_first>
  <action>
Extend `buildVisionUserPrompt` (or batch-specific variant) so when the batch has **no overlap with neighbors** (entire doc or single batch), the model must output **per-item `sourcePages: number[]`** (or existing field name in schema).

Update parsers/validators to **require** `sourcePages` when prompt mode requests it (pass flag from runner based on `overlap === 0` for that batch). Reuse existing types where possible; avoid breaking flashcard vs quiz split from Phase 21.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Valid sample JSON with `sourcePages` passes; missing pages fails validation in strict mode.
  </acceptance_criteria>
  <done>Schema + prompt aligned for single-batch provenance.</done>
</task>

<task type="auto">
  <name>Task 3: Cache key stability (optional)</name>
  <files>src/lib/ai/visionParseCache.ts</files>
  <read_first>src/lib/ai/visionParseCache.ts</read_first>
  <action>
If prompt body version changes, add a **short constant** (e.g. `VISION_BATCH_PROMPT_V = "24-1"`) concatenated into `hashVisionBatch` input so cached batches are not confused across prompt generations.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Hash differs when version constant changes (comment or trivial test).
  </acceptance_criteria>
  <done>Cache safety for prompt churn.</done>
</task>

</tasks>
