---
phase: 15-social-presence-scaling
plan: 05
type: execute
wave: 5
depends_on: [15-01, 15-02, 15-03, 15-04]
files_modified:
  - scripts/verify-phase15-social-presence.mjs
  - scripts/verify-phase15-social-presence.test.mjs
  - .planning/phases/15-social-presence-scaling/15-LOAD-EVIDENCE.md
autonomous: false
requirements: [SCALE-01, SCALE-02, SCALE-03, SCALE-04, SCALE-05, SCALE-06, SCALE-07, SCALE-08, SCALE-09, SCALE-10]
user_setup:
  - service: phase15-external-evidence
    why: Real Redis/SQL/worker/browser/load proof cannot be produced by repository automation.
    env_vars:
      - name: PHASE15_TEST_REDIS_URL
        source: Approved disposable Redis target.
      - name: PHASE15_TEST_DATABASE_URL
        source: Approved disposable SQL target.
      - name: PHASE15_REDIS_TEST_CONFIRM
        source: Exact value YES.
      - name: PHASE15_TEST_CONFIRM
        source: Exact value YES.
      - name: PHASE15_ALLOW_DISPOSABLE_TEST_HOST
        source: Explicit nonlocal disposable host allowlist when required.
    dashboard_config:
      - task: Deploy external worker with service-role credential, supervisor, health/restart, and approved Redis/SQL network access.
        location: Deployment platform configuration.
      - task: Provide two accepted accounts plus blocked/nonparticipant account and desktop/375px browser sessions.
        location: Disposable app/auth environment.
must_haves:
  truths:
    - Automated evidence schema/config validator rejects malformed, secret-bearing, incomplete, or contradictory Phase 15 evidence.
    - All ten SCALE requirements map to owned automated checks and separately labeled external evidence.
    - Real Redis TTL, bounded discovery, rate limits, outage/recovery, SQL privacy/idempotency, worker lifecycle, browser behavior, and 100/1,000 load thresholds are recorded without simulated pass.
    - Spike 001 partial results are each mapped to an implementation or evidence row.
    - Phase 14 baseline failures remain labeled baseline and do not weaken Phase 15 focused gates.
  artifacts:
    - path: scripts/verify-phase15-social-presence.mjs
      provides: Executable evidence schema/config validator and focused gate.
      contains: --schema-test
    - path: scripts/verify-phase15-social-presence.test.mjs
      provides: Validator tests for required fields, redaction, thresholds, and prerequisite states.
      contains: BLOCKED_PREREQUISITE
    - path: .planning/phases/15-social-presence-scaling/15-LOAD-EVIDENCE.md
      provides: Real-run evidence record with automated/external separation.
      contains: runId
  key_links:
    - from: scripts/verify-phase15-social-presence.mjs
      to: .planning/phases/15-social-presence-scaling/15-LOAD-EVIDENCE.md
      via: schema validation and redaction gate
      pattern: LOAD-EVIDENCE
    - from: scripts/verify-phase15-social-presence.mjs
      to: .planning/phases/15-social-presence-scaling/15-VALIDATION.md
      via: requirement/threshold/config assertions
      pattern: SCALE-01
    - from: 15-LOAD-EVIDENCE.md
      to: 15-01..15-04 artifacts
      via: owner and evidence-row references
      pattern: 15-0
---

<objective>
Close Phase 15 with executable evidence validation and explicit external proof gates.

Purpose: Distinguish what repository automation proves from what requires approved Redis/SQL/worker/browser/load environments, preventing simulated validation.
Output: Validator script/tests, updated validation contract, and evidence record/template covering all SCALE requirements, Spike 001 partial mapping, and Phase 14 baseline.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-social-presence-scaling/15-RESEARCH.md
@.planning/phases/15-social-presence-scaling/15-VALIDATION.md
@.planning/phases/15-social-presence-scaling/15-01-PLAN.md
@.planning/phases/15-social-presence-scaling/15-02-PLAN.md
@.planning/phases/15-social-presence-scaling/15-03-PLAN.md
@.planning/phases/15-social-presence-scaling/15-04-PLAN.md
@.planning/spikes/001-redis-social-presence-scaling/README.md

<interfaces>
Evidence JSON requires `runId`, UTC timestamps, target class/version, machine/Node/Redis/provider versions, scenario, ramp, duration, concurrency, cadence/jitter, page/session/batch limits, request/status/error counts, p50/p95/p99, Redis command/key/latency/reconnect counts, Postgres calls/writes per heartbeat, queue age/batches/retries/dead letters, TTL/expiry checks, privacy outcomes, outage/recovery timestamps, threshold results, redaction scan, and caveats. Rejection rules: missing required fields, non-UTC timestamps, credentials in URLs, message bodies, raw Redis values, tokens, session IDs, IPs, negative counts, p95 > p99, unknown scenario, contradictory PASS with missing prerequisite, or Phase 12 variable use.

External status values: `PASS`, `FAIL`, `BLOCKED_PREREQUISITE`, `NOT_RUN`. Missing external target/deployment/browser evidence must be `BLOCKED_PREREQUISITE`; validator must never convert absence to pass.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement executable evidence schema/config validator</name>
  <files>scripts/verify-phase15-social-presence.mjs, scripts/verify-phase15-social-presence.test.mjs</files>
  <behavior>
    - Test 1: `--schema-test` accepts complete safe evidence and rejects missing required fields, wrong UTC timestamps, negative counts, p95 > p99, unknown scenario, and malformed status.
    - Test 2: Redaction gate rejects credentials in URLs, message bodies, raw Redis values, tokens, session IDs, IPs, and secret env values.
    - Test 3: Config gate rejects absent/unsafe disposable confirmations, production/shared URLs, Phase 12 variable use, invalid worker bounds, and `.env.example` claims.
    - Test 4: Validator maps missing Redis/SQL/worker/browser/load prerequisites to `BLOCKED_PREREQUISITE`, never simulated PASS; verifies all SCALE-01..10 and Spike 001 rows exist.
  </behavior>
  <action>Write Node-stdlib-only validator with `--check-config`, `--schema-test`, and evidence-file validation. Encode the exact evidence schema and threshold names from `15-VALIDATION.md`, redaction patterns, requirement ownership rows, Spike 001 partial mapping, Phase 14 baseline label, safe disposable target checks, and external status semantics. Parse only committed evidence/template data; fail closed on contradictions. Do not edit `.env.example`, source code, roadmap, or dependencies.</action>
  <verify><automated>npx vitest run scripts/verify-phase15-social-presence.test.mjs && node scripts/verify-phase15-social-presence.mjs --schema-test && node scripts/verify-phase15-social-presence.mjs --check-config</automated></verify>
  <done>Validator tests pass and executable checks reject malformed/secret/unsafe evidence while distinguishing automated proof from `BLOCKED_PREREQUISITE` external proof.</done>
</task>

<task type="auto">
  <name>Task 2: Record evidence template and align validation ownership</name>
  <files>.planning/phases/15-social-presence-scaling/15-LOAD-EVIDENCE.md</files>
  <action>Keep validation ownership aligned with the plan/task tables: 15-01 owns heartbeat route/test and package, 15-02 owns activity handoff and canonical typing, 15-03 owns queue/worker/SQL, 15-04 owns client/realtime/load harness, and 15-05 owns validator plus evidence record. Create `15-LOAD-EVIDENCE.md` with one row for every SCALE-01..10 requirement, every Spike 001 partial item, Phase 14 baseline statement, automated command/result, external prerequisite/status, target redaction, threshold result, and caveat. Mark unrun external checks `BLOCKED_PREREQUISITE` or `NOT_RUN`, never pass. Do not edit `.env.example`.</action>
  <verify><automated>node scripts/verify-phase15-social-presence.mjs --check-config && node scripts/verify-phase15-social-presence.mjs --schema-test</automated></verify>
  <done>Validation and task ownership tables are identical, evidence file has complete rows and safe status semantics, and no source/roadmap/branch edits occur.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run approved external Phase 15 evidence</name>
  <files>.planning/phases/15-social-presence-scaling/15-LOAD-EVIDENCE.md</files>
  <action>After Tasks 1–2 pass, use only approved disposable Redis/SQL targets and deployed external worker/browser environments to execute the exact Redis, SQL, worker, browser, healthy 100/1,000, and outage/recovery checks listed in `15-VALIDATION.md`. Record redacted results and prerequisite statuses in `15-LOAD-EVIDENCE.md`; never mark absent evidence as pass.</action>
  <verify>
    <automated>node scripts/verify-phase15-social-presence.mjs --check-config && node scripts/verify-phase15-social-presence.mjs --schema-test</automated>
    <human-check>External rows contain target class/version, timestamps, thresholds, redaction result, and PASS/FAIL/BLOCKED_PREREQUISITE/NOT_RUN status; browser, Redis, SQL, worker, and load evidence match recorded results.</human-check>
  </verify>
  <done>All SCALE-01–10 rows and Spike 001 partial rows have owned automated evidence plus explicit external status; validator passes, and no missing external prerequisite is represented as PASS.</done>
  <what-built>Automated validator, safe load harness, Redis/SQL/worker integration contracts, browser checks, and evidence schema ready for real external proof.</what-built>
  <how-to-verify>
    1. Supply only approved disposable `PHASE15_TEST_REDIS_URL` and `PHASE15_TEST_DATABASE_URL`; set both confirmation variables to exact `YES` and configure explicit nonlocal allowlist if needed.
    2. Deploy `scripts/social-presence-worker.mjs` with unique consumer, service-role credential, supervisor restart/health/SIGTERM, and network access.
    3. Run Redis TTL/index/MGET/rate-limit/reconnect, SQL privacy/idempotency/RPC, worker retry/claim/dead-letter, healthy 100/1,000, and outage/recovery scenarios using exact thresholds in `15-VALIDATION.md`.
    4. Run browser checks with two accepted accounts plus blocked/nonparticipant account at desktop and 375px mobile; verify presence, typing, message history/send/read, outage unknown state, and invalidation-only realtime.
    5. Record redacted results in `15-LOAD-EVIDENCE.md`; run validator. Expected: every requirement has automated ownership, external status is PASS only with evidence, and missing prerequisites remain BLOCKED_PREREQUISITE.
  </how-to-verify>
  <resume-signal>Type "approved" when external evidence is recorded and validator passes, or describe failed/blocked rows.</resume-signal>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Evidence files to validator | Potentially untrusted JSON/Markdown enters gate logic. |
| Validator to external target config | CLI/env target values determine where load/proof runs. |
| Worker/browser/SQL evidence to repository record | External results enter a durable project artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-15-21 | Tampering | evidence status | mitigate | Fail closed; PASS requires complete thresholds and prerequisites; missing proof is BLOCKED_PREREQUISITE. |
| T-15-22 | Information disclosure | evidence JSON/Markdown | mitigate | Reject credentials, bodies, Redis values, tokens, session IDs, IPs; require redaction scan. |
| T-15-23 | Tampering | external target selection | mitigate | Require explicit confirmations and disposable allowlist; reject production/shared URLs and Phase 12 variable. |
| T-15-24 | Denial of service | validator/load | mitigate | Node stdlib bounded input and scenario limits; no uncontrolled load by default. |
| T-15-SC | Tampering | script execution | mitigate | Node stdlib only, no new dependency, explicit human external gate. |
</table>
</threat_model>

<verification>
Automated validator/schema/config checks must pass. Human approval required for external Redis, SQL, worker, browser, and load evidence; no external result is fabricated by repository checks.
</verification>

<success_criteria>
All SCALE-01..10 requirements, Spike 001 partial mappings, environment contract, ownership rows, failure tables, load thresholds, and Phase 14 baseline are machine-checkable or explicitly blocked on human prerequisites. Phase status can be `VALIDATED` only after the approved external evidence gate passes.
</success_criteria>

<output>
Create `.planning/phases/15-social-presence-scaling/15-05-SUMMARY.md` when done.
</output>

