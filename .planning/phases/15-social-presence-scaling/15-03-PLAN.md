---
phase: 15-social-presence-scaling
plan: 03
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - src/lib/server/social/activityQueue.ts
  - src/lib/server/social/activityQueue.test.ts
  - scripts/social-presence-worker.mjs
  - scripts/social-presence-worker.test.mjs
  - supabase/migrations/20260802010000_social_activity_scaling.sql
  - supabase/tests/social_activity_scaling.sql
  - supabase/schemas/70_functions.sql
  - src/app/api/friends/messages/[conversationId]/route.ts
  - src/app/api/friends/messages/[conversationId]/route.test.ts
  - src/app/api/friends/messages/[conversationId]/read/route.ts
  - src/app/api/friends/messages/[conversationId]/read/route.test.ts
  - src/lib/client/activityTracking.test.ts
autonomous: true
requirements: [SCALE-06, SCALE-08]
user_setup:
  - service: external-worker
    why: Worker process management, service-role credential, real Redis Streams, SQL RPC, restart, and network proof cannot run inside repository tests.
    env_vars:
      - name: REDIS_URL
        source: Server/worker deployment secret.
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: Worker deployment secret.
      - name: SOCIAL_WORKER_GROUP
        source: Deployment process configuration.
      - name: SOCIAL_WORKER_CONSUMER
        source: Unique deployment process configuration.
    dashboard_config:
      - task: Run worker under external supervisor with restart, health, SIGTERM, and network access to approved Redis/Supabase targets.
        location: Deployment platform configuration.
must_haves:
  truths:
    - Successful meaningful events are bounded in Redis Stream and durable in private.social_activity.
    - Worker batches, coalesces, retries, claims stale work, dead-letters poison events, and acknowledges only after durable commit.
    - Duplicate/reordered events cannot create duplicate durable activity or move last_active_at backward.
    - Message send/read routes enqueue exact events only after their existing durable operation succeeds.
    - Worker config has bounded batch, block, lease, and retry settings and no production browser dependency.
    - Study completion remains excluded from social activity.
  artifacts:
    - path: src/lib/server/social/activityQueue.ts
      provides: Closed event schema, stream enqueue, dedupe/coalesce helpers.
      contains: d2q:activity
    - path: scripts/social-presence-worker.mjs
      provides: External worker `--once`, `--serve`, and `--check-config` entrypoint.
      contains: XREADGROUP
    - path: supabase/migrations/20260802010000_social_activity_scaling.sql
      provides: Idempotent durable activity RPC/schema/RLS.
      contains: apply_social_activity_batch
  key_links:
    - from: src/app/api/friends/messages/[conversationId]/route.ts
      to: src/lib/server/social/activityQueue.ts
      via: enqueue only after send_direct_message success
      pattern: message_sent
    - from: src/app/api/friends/messages/[conversationId]/read/route.ts
      to: src/lib/server/social/activityQueue.ts
      via: enqueue only after mark_direct_conversation_read success
      pattern: conversation_read
    - from: scripts/social-presence-worker.mjs
      to: supabase.apply_social_activity_batch
      via: ack-after-commit batch processing
      pattern: apply_social_activity_batch
---

<objective>
Build bounded meaningful-activity queue, durable SQL contract, and externally runnable worker.

Purpose: Complete SCALE-06 with idempotency, retry safety, and exact operation ownership without coupling request handlers to worker state.
Output: Queue schema/helpers, message/read producer wiring, SQL migration/tests, and worker entrypoint with config validation.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-social-presence-scaling/15-RESEARCH.md
@.planning/phases/15-social-presence-scaling/15-VALIDATION.md
@.planning/phases/15-social-presence-scaling/15-02-PLAN.md
@src/app/api/friends/messages/[conversationId]/route.ts
@src/app/api/friends/messages/[conversationId]/read/route.ts
@src/lib/server/friends/realtimeBroadcast.ts

<interfaces>
Closed event schema: `eventId` UUID, authenticated `userId`, UTC `occurredAt`, `activityKind` in `presence_transition | message_sent | conversation_read`, closed `source`, deterministic `dedupeKey`. Stream key `d2q:activity`; approximate `MAXLEN ~10000`, age policy 24h. Batch size 50–200, block 10–30s, lease 30s, poison threshold exactly 5.

SQL contract: `private.social_activity(user_id)` remains durable conflict target. `apply_social_activity_batch` dedupes by event/dedupe key, coalesces newest event per user/window/kind, and never moves `last_active_at` backward. Worker `XACK`s only after committed RPC; retryable entries remain pending and stale entries are `XAUTOCLAIM`ed.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define bounded queue and idempotent SQL batch contract</name>
  <files>src/lib/server/social/activityQueue.ts, src/lib/server/social/activityQueue.test.ts, supabase/migrations/20260802010000_social_activity_scaling.sql, supabase/schemas/70_functions.sql, supabase/tests/social_activity_scaling.sql</files>
  <behavior>
    - Test 1: Queue rejects unknown activity kinds, missing UUID/user/source/time, raw payloads, and oversized fields.
    - Test 2: Enqueue uses `d2q:activity`, `MAXLEN ~10000`, 24-hour age policy, deterministic dedupeKey, and no unbounded stream operation.
    - Test 3: SQL function dedupes duplicate event/dedupe keys, coalesces newest event per user/window/kind, and does not move last_active_at backward.
    - Test 4: Study-action input is rejected; no `study_action` can be serialized.
    - Test 5: SQL assertions cover RLS/service-role boundary and no message body/token/IP persistence.
  </behavior>
  <action>Implement closed event validation and Redis Stream enqueue against the 15-02 producer interface. Use only allowlisted kinds/sources and metadata listed in `15-VALIDATION.md`; exclude message bodies and study-action events. Add migration preserving `private.social_activity`, conflict target `user_id`, `apply_social_activity_batch`, dedupe event/dedupe keys, newest timestamp policy, bounded retention metadata, and service-role-only worker RPC. Update `supabase/schemas/70_functions.sql` mirror with the same RPC signature and idempotency contract. Add static SQL mirror assertions; do not claim runtime SQL proof.</action>
  <verify><automated>npx vitest run src/lib/server/social/activityQueue.test.ts && node -e "const fs=require('fs'); for(const f of ['supabase/migrations/20260802010000_social_activity_scaling.sql','supabase/tests/social_activity_scaling.sql']){const s=fs.readFileSync(f,'utf8'); if(!/apply_social_activity_batch|social_activity|dedupe/i.test(s)) process.exit(1)}"</automated></verify>
  <done>Closed queue/schema and idempotent SQL contract exist; queue bounds, privacy fields, study-action exclusion, and static SQL assertions pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire message and read success events</name>
  <files>src/app/api/friends/messages/[conversationId]/route.ts, src/app/api/friends/messages/[conversationId]/route.test.ts, src/app/api/friends/messages/[conversationId]/read/route.ts, src/app/api/friends/messages/[conversationId]/read/route.test.ts, src/lib/client/activityTracking.test.ts</files>
  <behavior>
    - Test 1: `send_direct_message` success enqueues exactly one `message_sent`; RPC failure enqueues none.
    - Test 2: `mark_direct_conversation_read` success enqueues exactly one `conversation_read`; RPC failure enqueues none.
    - Test 3: Existing auth, participant/block, response, and realtime invalidation contracts remain intact.
    - Test 4: `recordQuizCompletion` and `completeStudySession` tests assert no social queue call or `study_action` event.
  </behavior>
  <action>After existing durable send/read RPC success and authorization, call the closed activity queue interface from Task 1. Preserve current response/error/realtime behavior; event payload remains invalidation metadata only and never becomes client display state. Keep activity queue failure observable but do not roll back a committed message/read operation. Update only message/read route tests and the client activity-tracking regression test; no client integration work belongs here.</action>
  <verify><automated>npx vitest run "src/app/api/friends/messages/[conversationId]/route.test.ts" "src/app/api/friends/messages/[conversationId]/read/route.test.ts" src/lib/client/activityTracking.test.ts</automated></verify>
  <done>Successful message/read operations enqueue exact events after commit; failures do not; existing route tests and explicit study-action exclusion pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement external worker lifecycle and retry contract</name>
  <files>scripts/social-presence-worker.mjs, scripts/social-presence-worker.test.mjs</files>
  <behavior>
    - Test 1: `--check-config` rejects missing/unsafe env, batch outside 50–200, retry outside 1–5, or unbounded block/lease.
    - Test 2: `--once` reads bounded batches with `XREADGROUP` and calls durable batch RPC before `XACK`.
    - Test 3: Retryable failure leaves entry pending; `XAUTOCLAIM` reclaims entries idle >= lease; fifth failure moves event to `d2q:activity:dead` then acknowledges.
    - Test 4: SIGTERM stops new reads, closes Redis/SQL clients, and writes redacted health state; `--serve` does not create request-handler timers.
  </behavior>
  <action>Create `scripts/social-presence-worker.mjs` as an external entrypoint with `--once`, `--serve`, and `--check-config`. Validate server-only Redis/SQL config and explicit bounded worker settings. Implement consumer group creation, bounded `XREADGROUP`, lease `XAUTOCLAIM`, 50–200 batch processing, newest-per-user coalescing, ack-after-commit, retry/dead-letter at exactly 5 attempts, redacted health, and SIGTERM drain. Deployment process management is a human prerequisite; do not add in-process worker timers to Next.js routes.</action>
  <verify><automated>npx vitest run scripts/social-presence-worker.test.mjs && node scripts/social-presence-worker.mjs --check-config</automated></verify>
  <done>Worker tests prove bounded reads, ack-after-commit, lease recovery, retry/dead-letter, config validation, and clean shutdown; real process deployment remains external evidence.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Route to Redis Stream | Server-authenticated event crosses into queue storage. |
| Worker to Redis/SQL | External process with service credential reads queue and writes durable activity. |
| SQL RPC to durable table | Batch procedure updates user activity under RLS/service-role rules. |
| Worker logs/health | Operational state crosses into supervisor/operator systems. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-15-11 | Tampering | activity event schema | mitigate | Validate closed kinds, UUIDs, timestamps, sources, dedupe keys; reject raw payloads. |
| T-15-12 | Repudiation | queue/worker | mitigate | Event IDs, dedupe keys, attempt counts, redacted structured health, ack-after-commit. |
| T-15-13 | Information disclosure | queue/SQL/logs | mitigate | Persist no bodies/tokens/IPs; service-role only worker RPC; redact health/log output. |
| T-15-14 | Denial of service | stream retention/worker | mitigate | MAXLEN ~10000, 24h age policy, bounded batch/block/lease/retry, dead letter. |
| T-15-15 | Elevation of privilege | worker SQL | mitigate | Require server-only service-role credential and SQL function privilege boundary. |
| T-15-SC | Tampering | package install | mitigate | Reuse 15-01 dependency decision; no unreviewed package addition. |
</table>
</threat_model>

<verification>
Run only 15-03 owned unit, route, SQL-static, worker-config, and worker lifecycle tests. Real worker process and SQL/Redis proof is external and must be recorded by 15-05 without simulated pass.
</verification>

<success_criteria>
SCALE-06 and SCALE-08 durable activity behavior is complete: closed event queue, exact message/read producers, idempotent SQL batch, external worker lifecycle, bounded retry/dead-letter, and explicit deployment prerequisite.
</success_criteria>

<output>
Create `.planning/phases/15-social-presence-scaling/15-03-SUMMARY.md` when done.
</output>
