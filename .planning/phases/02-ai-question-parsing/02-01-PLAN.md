---
phase: 02-ai-question-parsing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/question.ts
  - src/lib/ai/storage.ts
autonomous: true
requirements:
  - AI-01
user_setup: []

must_haves:
  truths:
    - "localStorage keys doc2quiz:ai:provider, doc2quiz:ai:openaiKey, doc2quiz:ai:anthropicKey read/write correctly"
    - "Question type supports exactly 4 options and correctIndex 0-3"
  artifacts:
    - "src/types/question.ts exists"
    - "src/lib/ai/storage.ts exports provider + key helpers"
  key_links:
    - "Storage key strings match 02-CONTEXT D-04 exactly"
---

<objective>
Add the **Question** domain type and **localStorage** helpers for AI provider + API keys per `02-CONTEXT.md` (D-02–D-04, D-20).

Purpose: Ground Phase 2 UI and services on stable types and persistence contract.
Output: `src/types/question.ts`, `src/lib/ai/storage.ts`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ai-question-parsing/02-CONTEXT.md
@.planning/REQUIREMENTS.md
@src/types/pdf.ts
</context>

<interfaces>
```typescript
// src/types/question.ts
export type AiProvider = "openai" | "anthropic";

export type Question = {
  id: string;
  question: string;
  options: [string, string, string, string];
  correctIndex: 0 | 1 | 2 | 3;
};

// localStorage keys — character-for-character
export const LS_PROVIDER = "doc2quiz:ai:provider";
export const LS_OPENAI_KEY = "doc2quiz:ai:openaiKey";
export const LS_ANTHROPIC_KEY = "doc2quiz:ai:anthropicKey";
export const LS_DRAFT_QUESTIONS = "doc2quiz:ai:draftQuestions";
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Question + provider types</name>
  <files>src/types/question.ts</files>
  <read_first>.planning/phases/02-ai-question-parsing/02-CONTEXT.md</read_first>
  <action>
Create `src/types/question.ts` exporting `AiProvider`, `Question` (4-tuple options, `correctIndex` 0–3), and **string constants** for every localStorage key listed in `interfaces` including `LS_DRAFT_QUESTIONS` for Phase 2 draft persistence (D-21).
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/types/question.ts` contains `export type Question`
    - File exports `LS_PROVIDER` with value exactly `doc2quiz:ai:provider`
    - File exports `LS_OPENAI_KEY` and `LS_ANTHROPIC_KEY` with exact names from CONTEXT
  </acceptance_criteria>
  <done>Types compile</done>
</task>

<task type="auto">
  <name>Task 2: AI storage helpers</name>
  <files>src/lib/ai/storage.ts</files>
  <read_first>src/types/question.ts</read_first>
  <action>
Create `src/lib/ai/storage.ts` with functions: `getProvider()`, `setProvider(p)`, `getKeyForProvider(p)`, `setKeyForProvider(p, value)`, `clearKeyForProvider(p)` — all use `typeof window !== "undefined"` guards for SSR safety. Default provider when missing: **`openai`**. Keys must use constants from `src/types/question.ts`.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/lib/ai/storage.ts` imports from `@/types/question` or `../types/question`
    - File exports `getProvider` and `setKeyForProvider`
  </acceptance_criteria>
  <done>Storage module complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage | API keys at rest on device; XSS could exfiltrate |

## STRIDE

| ID | Cat | Component | Disp | Mitigation |
|----|-----|-----------|------|------------|
| T-02a-1 | I | localStorage keys | mitigate | No `dangerouslySetInnerHTML`; React escaping; document risk in README |
</threat_model>

<verification>
- [ ] `npx tsc --noEmit`
- [ ] `npm run build`
</verification>

<success_criteria>
- Types + storage match CONTEXT D-04
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-question-parsing/02-01-SUMMARY.md`
</output>
