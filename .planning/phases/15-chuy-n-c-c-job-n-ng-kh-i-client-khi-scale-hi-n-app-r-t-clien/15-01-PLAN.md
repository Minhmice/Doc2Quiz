---
phase: 15-server-parse-scale
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/SCALE-MODE-parse-queue.md
  - src/types/parseJob.ts
  - src/lib/serverParse/env.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Default remains **client-local**: no outbound parse-queue traffic unless `D2Q_SERVER_PARSE_ENABLED` is explicitly set (see `src/lib/serverParse/env.ts`)."
    - "Job/progress types are shared so App Router routes and future client hooks import one module (`src/types/parseJob.ts`)."
    - "`docs/SCALE-MODE-parse-queue.md` names env vars, privacy boundaries, and that Wave 2 only adds HTTP stubs — no worker binary in this plan."
  artifacts:
    - path: docs/SCALE-MODE-parse-queue.md
      provides: Operator-facing architecture for server/queue mode vs current client-heavy path
    - path: src/types/parseJob.ts
      provides: `ParseJobStatus`, `ParseJobSummary`, optional `ParseJobErrorCode` union
    - path: src/lib/serverParse/env.ts
      provides: `isServerParseQueueEnabled()` for route handlers (server-only)
  key_links:
    - from: env.ts
      to: src/app/api/parse-jobs/** (wave 2)
      via: "import `isServerParseQueueEnabled`"
---

<objective>
**Wave 1 — contracts:** Document scale-mode parse queue + add **shared types** and a **single server-side env gate** so Wave 2 API stubs compile and behave consistently with offline-first defaults.

**Output:** `docs/SCALE-MODE-parse-queue.md`, `src/types/parseJob.ts`, `src/lib/serverParse/env.ts`.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Accidentally enabling server mode in prod without review | Medium | Env gate requires explicit `1` or `true`; doc states default off. |
| Leaking PDF bytes in docs | Low | Doc describes **metadata + job ids** only for queue payloads; no sample base64. |

**Blockers:** None.
</threat_model>

## Task 1 — Architecture note `SCALE-MODE-parse-queue.md`

<action>
Create `docs/SCALE-MODE-parse-queue.md` with Markdown sections (exact `##` headings for grep):

- `## Goals` — one paragraph: move heavy PDF render + parse orchestration off the main thread / weak devices via optional server or worker queue; **local client path remains default**.
- `## Job lifecycle` — bullet list: create job → queued → running (render / OCR / vision steps) → succeeded | failed | cancelled; mention correlation id for Phase 13 alignment.
- `## Privacy and retention` — bullets: PDF bytes on server only when mode on; TTL / delete policy TBD in execute; no API keys in job payloads.
- `## Environment` — bullet that lists the literal env var name **`D2Q_SERVER_PARSE_ENABLED`** (and optional `D2Q_SERVER_PARSE_MAX_MB` placeholder name for future caps — if you add the second var, document it as “reserved, unused in code until 15-02+”).
- `## Wave 2 scope` — one sentence: Next.js `app/api/parse-jobs` stubs return **501 Not Implemented** when the flag is on (contract only), **404** when off.

Minimum ~40 lines prose total across sections.
</action>

<read_first>
- `.planning/ROADMAP.md` (Phase 15 block)
- `CLAUDE.md` (offline-first)
</read_first>

<acceptance_criteria>
- File `docs/SCALE-MODE-parse-queue.md` exists.
- `rg "## Goals" docs/SCALE-MODE-parse-queue.md` returns at least one match.
- `rg "D2Q_SERVER_PARSE_ENABLED" docs/SCALE-MODE-parse-queue.md` returns at least one match.
- `npm run lint` exits 0.
</acceptance_criteria>

## Task 2 — Shared types `parseJob.ts`

<action>
Add `src/types/parseJob.ts` exporting:

- `export type ParseJobStatus = "queued" | "running" | "succeeded" | "failed" | "cancelled"`
- `export type ParseJobSummary = { id: string; status: ParseJobStatus; updatedAt: string; progress?: { done: number; total: number } }`
- `export type ParseJobCreateResponse = { job: ParseJobSummary }` (or equivalent single export used by routes in 15-02)

No React imports. JSDoc one line referencing Phase 15 roadmap.
</action>

<read_first>
- `src/types/question.ts` (style: export types, no runtime)
</read_first>

<acceptance_criteria>
- `src/types/parseJob.ts` exists.
- `rg "ParseJobStatus" src/types/parseJob.ts` returns at least one match.
- `rg "ParseJobSummary" src/types/parseJob.ts` returns at least one match.
- `npm run lint` exits 0.
</acceptance_criteria>

## Task 3 — Env gate `src/lib/serverParse/env.ts`

<action>
Create `src/lib/serverParse/env.ts` with:

```ts
export function isServerParseQueueEnabled(): boolean
```

Implementation: return true iff `process.env.D2Q_SERVER_PARSE_ENABLED === "1"` **or** case-insensitive `=== "true"` (trim whitespace). Otherwise false.

Add file-top comment: **server / route handlers only** — do not import from client components (no `NEXT_PUBLIC_` for this flag in 15-01).

Export no other functions in this file.
</action>

<read_first>
- `src/lib/serverParse/env.ts` (does not exist yet — read `src/lib/ai/visionStagingStore.ts` first line style for server-only comment patterns if useful)
</read_first>

<acceptance_criteria>
- `src/lib/serverParse/env.ts` exists.
- `rg "isServerParseQueueEnabled" src/lib/serverParse/env.ts` returns at least one match.
- `rg "D2Q_SERVER_PARSE_ENABLED" src/lib/serverParse/env.ts` returns at least one match.
- `npm run lint` exits 0.
</acceptance_criteria>

## Verification (plan)

- `npm run lint` and `npm run build` exit 0.
