---
phase: 20-ai-first-content-creation-output-type-selector-quiz-vs-flash
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/components/ai/AiParseSection.tsx
  - src/app/(app)/sets/[id]/source/page.tsx
  - src/app/(app)/dev/ocr/page.tsx
  - src/app/(app)/dev/ocr/[id]/page.tsx
  - docs/ARCHITECTURE-domain-boundaries.md
  - src/app/(app)/sets/[id]/flashcards/review/page.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Main-path source UI does not surface OcrInspector / mapping debug / OCR toggle copy for sets created with `contentKind` quiz or flashcards (restore via explicit debug query if implemented)."
    - "A dev-only route under `src/app/(app)/dev/ocr/` exists and is not linked from dashboard primary CTAs."
    - "Flashcard creators have a post-upload path: either dedicated flashcard review route or documented interim CTA — plan implements at least one concrete navigation improvement beyond quiz-only copy."
  artifacts:
    - path: src/app/(app)/dev/ocr/page.tsx
      provides: "Dev OCR entry (upload or id paste — see tasks)"
    - path: src/components/ai/AiParseSection.tsx
      provides: "`surface` (or equivalent) prop to collapse/hide technical controls"
  key_links:
    - from: src/app/(app)/sets/[id]/source/page.tsx
      to: src/components/ai/AiParseSection.tsx
      via: new prop
      pattern: "product vs full parse chrome"
---

<objective>
**Wave 2 — Parse UX split + dev surface + flashcards handoff:** (1) Add a **`surface`** (name bikeshed OK: `variant`, `chrome`) prop to **`AiParseSection`** + wire **`source/page.tsx`** so quiz/flashcards sets hide OCR inspector, mapping debug, and technical parse toggles from default view (PRD §4.4, §3.4). (2) Add **`/dev/ocr`** routes composing existing parse-domain components — **no second OCR engine**. (3) Add minimal **flashcard review** route **or** strong CTA from source when `contentKind === "flashcards"` toward `/sets/[id]/flashcards` + backlog note for true `Flashcard[]` storage.

**Depends on:** `20-01` (`contentKind` persisted, typed upload routes exist).
</objective>

<context>
@.planning/phases/20-ai-first-content-creation-output-type-selector-quiz-vs-flash/20-CONTEXT.md
@.planning/phases/20-ai-first-content-creation-output-type-selector-quiz-vs-flash/20-UI-SPEC.md
@docs/ARCHITECTURE-domain-boundaries.md
@docs/BACKLOG-flashcards.md
</context>

<threat_model>
| Threat | Severity | Mitigation | Blockers |
|--------|----------|------------|----------|
| `/dev/ocr` exposes destructive actions to random users | Medium | Do not link from production dashboard; optional `process.env.NODE_ENV === "production"` guard returning 404 or static “not found” unless `NEXT_PUBLIC_DEV_TOOLS=true` — pick one and document in plan SUMMARY | Product decision |
| Hiding OCR controls prevents power users from recovering | Low | Document `?debug=1` (or chosen flag) in `20-02-SUMMARY.md` + `CLAUDE.md` one line | None |
| `AiParseSection` prop explosion | Medium | Single enum `surface: "product" | "developer"` default `"product"` when `contentKind` set — **default existing callers** to `"developer"` for backward compat | None |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: `AiParseSection` — `surface` prop hides technical chrome</name>
  <files>src/components/ai/AiParseSection.tsx</files>
  <read_first>src/components/ai/AiParseSection.tsx,src/components/ai/AiParsePreferenceToggles.tsx,src/components/ai/AiParseParseStrategyPanel.tsx,.planning/phases/20-ai-first-content-creation-output-type-selector-quiz-vs-flash/20-UI-SPEC.md</read_first>
  <action>
Add optional prop **`surface?: "product" | "developer"`** (default **`"developer"`** when omitted so existing call sites behave today).

When **`surface === "product"`**:
- Do **not** render **`AiParsePreferenceToggles`** (contains OCR enable).
- Hide **`AiParseParseStrategyPanel`** behind a single **`<details>` / Collapsible** labeled **Advanced** (default closed) **or** omit entirely if collapse is too invasive — choose **collapsible** so engine still reachable.
- Keep **`AiParseActions`** + parse progress + preview list user-visible; reword visible headings if any contain substring **`OCR`** in user-visible text (grep `AiParseSection.tsx` for `"OCR"` in JSX string literals).

Export type from `AiParseSection.tsx` or small `types` colocated file if needed.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "surface\\?:" src/components/ai/AiParseSection.tsx` OR `rg -n "surface:" src/components/ai/AiParseSection.tsx` finds the new prop on the component props type.
    - When `surface === "product"`, file does not render `<AiParsePreferenceToggles` as direct child (grep confirms conditional or absence).
  </acceptance_criteria>
  <done>AiParseSection supports product chrome.</done>
</task>

<task type="auto">
  <name>Task 2: Wire `source/page.tsx` product mode + hide inspectors</name>
  <files>src/app/(app)/sets/[id]/source/page.tsx</files>
  <read_first>src/app/(app)/sets/[id]/source/page.tsx,src/components/ai/AiParseSection.tsx,src/components/ai/OcrInspector.tsx</read_first>
  <action>
After `meta` loads: compute **`const surface = meta?.contentKind === "quiz" || meta?.contentKind === "flashcards" ? "product" : "developer"`** (treat **`contentKind` undefined** as **`developer`** chrome to avoid breaking legacy sets).

Pass **`surface`** into **`AiParseSection`**.

When **`surface === "product"`**, **do not mount** `OcrInspector` or `QuestionMappingDebug` **unless** URL has `debug=1` query (implement `useSearchParams` check).

Ensure **`ParseProgressOverlay` / `ParseResultOverlay`** still function.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/sets/[id]/source/page.tsx` passes prop containing literal `surface` to `AiParseSection`.
    - Same file wraps `OcrInspector` render in condition involving `debug` **or** `surface === "developer"`.
  </acceptance_criteria>
  <done>Source page matches PRD visibility.</done>
</task>

<task type="auto">
  <name>Task 3: Dev OCR routes under `/dev/ocr`</name>
  <files>src/app/(app)/dev/ocr/page.tsx,src/app/(app)/dev/ocr/[id]/page.tsx</files>
  <read_first>src/components/ai/OcrInspector.tsx,src/app/(app)/sets/[id]/source/page.tsx,.planning/phases/20-ai-first-content-creation-output-type-selector-quiz-vs-flash/20-UI-SPEC.md</read_first>
  <action>
Add **`src/app/(app)/dev/ocr/page.tsx`**: minimal client UI — short explanation “Internal OCR lab”, **`UploadBox`** or button file input reusing validation from `sets/new/quiz` pattern **without** `contentKind` product concerns — on success `router.push(\`/dev/ocr/${id}\`)` after creating a study set via **`createStudySet`** with title prefix **`[Dev OCR]`** and `contentKind: "quiz"` (or omit) **only if** no better temp story — acceptable dev pollution.

Add **`src/app/(app)/dev/ocr/[id]/page.tsx`**: render **`OcrInspector`** for `id` + link back to `/dev/ocr` + link to `/sets/[id]/source` (“Open full parse page”) for parity.

**Guard (choose one, document in SUMMARY):** (a) `process.env.NODE_ENV !== "production"` return notFound; or (b) env flag `NEXT_PUBLIC_ENABLE_DEV_OCR_LAB`.

No links from `DashboardLibraryClient` / `AppTopBar` to `/dev/ocr`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - PowerShell: `Test-Path "src/app/(app)/dev/ocr/page.tsx"` and `Test-Path "src/app/(app)/dev/ocr/[id]/page.tsx"` both output `True`.
    - `rg -n "OcrInspector" src/app/(app)/dev/ocr` returns ≥1 match.
    - `rg "dev/ocr" src/components/dashboard` returns exit code 1 (no matches).
  </acceptance_criteria>
  <done>Dev OCR lab route exists.</done>
</task>

<task type="auto">
  <name>Task 4: Flashcards handoff — review route stub + doc touch</name>
  <files>src/app/(app)/sets/[id]/flashcards/review/page.tsx,docs/ARCHITECTURE-domain-boundaries.md</files>
  <read_first>src/app/(app)/sets/[id]/review/page.tsx,src/app/(app)/sets/[id]/flashcards/page.tsx,docs/BACKLOG-flashcards.md,docs/ARCHITECTURE-domain-boundaries.md</read_first>
  <action>
Add **`src/app/(app)/sets/[id]/flashcards/review/page.tsx`** as a **client** page that:
- Loads **`getDraftQuestions`** for the set.
- Shows a simple list: for each `Question`, show **front** = question stem, **back** = correct option text (per backlog option 1) with **Edit** / **Delete** using existing question editor patterns **or** minimal textarea per row — keep scope small; persistence via **`putDraftQuestions`**.

When **`contentKind === "flashcards"`** on **`source/page.tsx`**, after successful parse overlay, adjust primary CTA copy to **Review flashcards** linking **`/sets/[id]/flashcards/review`** (if parse result flow uses a button today, extend).

Append **short subsection** to **`docs/ARCHITECTURE-domain-boundaries.md`**: “Product create flow vs dev OCR lab (`/dev/ocr`)" — 3–5 bullets, no diagram required.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/(app)/sets/[id]/flashcards/review/page.tsx` exists.
    - `docs/ARCHITECTURE-domain-boundaries.md` contains substring `/dev/ocr`.
    - `rg -n "/flashcards/review|Review flashcards" src/app/(app)/sets/[id]/source/page.tsx` exits 0 with ≥1 match.
  </acceptance_criteria>
  <done>Flashcards review v1 + doc link.</done>
</task>

</tasks>

## Verification (wave)

- `npm run lint` && `npm run build`
- Manual: create flashcards set → source product chrome → open `/dev/ocr` only when guard allows → flashcards review lists drafts
