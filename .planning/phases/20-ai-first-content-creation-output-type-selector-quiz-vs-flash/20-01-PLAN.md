---
phase: 20-ai-first-content-creation-output-type-selector-quiz-vs-flash
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/studySet.ts
  - src/lib/db/studySetDb.ts
  - src/app/(app)/sets/new/page.tsx
  - src/app/(app)/sets/new/quiz/page.tsx
  - src/app/(app)/sets/new/flashcards/page.tsx
  - src/components/dashboard/DashboardLibraryClient.tsx
  - src/components/layout/AppTopBar.tsx
  - src/components/layout/CommandPalette.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "`/sets/new` renders the output-type selector (Quiz vs Flashcards) and no longer performs PDF import inline."
    - "`StudySetMeta` persists a `contentKind` (or equivalent) of `quiz` | `flashcards` for sets created from the new upload routes."
    - "All primary dashboard/top-bar/palette entry CTAs route learners to `/sets/new` (selector), not directly to a single upload-only path."
  artifacts:
    - path: src/app/(app)/sets/new/page.tsx
      provides: "Selector UI + navigation to typed upload routes"
    - path: src/app/(app)/sets/new/quiz/page.tsx
      provides: "Quiz-branded upload + createStudySet with contentKind quiz"
    - path: src/app/(app)/sets/new/flashcards/page.tsx
      provides: "Flashcards-branded upload + createStudySet with contentKind flashcards"
  key_links:
    - from: src/components/dashboard/DashboardLibraryClient.tsx
      to: /sets/new
      via: href updates
      pattern: "single entry funnel"
---

<objective>
**Wave 1 — Create funnel + persistence hook:** Implement PRD §3.1–3.2 entry và §7 Option B: `/sets/new` = **chọn loại**; `/sets/new/quiz` và `/sets/new/flashcards` = **upload + tạo set** (reuse logic hiện tại của `NewStudySetPage`), lưu **`contentKind`** trên `StudySetMeta`, redirect `/sets/[id]/source`. Cập nhật mọi link tạo set từ dashboard/top bar/command palette trỏ về `/sets/new`.

**Out of scope for 20-01:** Ẩn OCR trên source, dev `/dev/ocr`, flashcard AI prompt — those are `20-02`.
</objective>

<context>
@.planning/phases/20-ai-first-content-creation-output-type-selector-quiz-vs-flash/20-CONTEXT.md
@.planning/phases/20-ai-first-content-creation-output-type-selector-quiz-vs-flash/20-UI-SPEC.md
@docs/ARCHITECTURE-domain-boundaries.md
</context>

<threat_model>
| Threat | Severity | Mitigation | Blockers |
|--------|----------|------------|----------|
| `contentKind` không persist → downstream không biết quiz vs flashcards | Medium | Unit of work: extend `createStudySet` + `StudySetMeta` type; assert meta round-trip in acceptance grep | None |
| Broken deep links to old `/sets/new` upload-only bookmark | Low | `/sets/new` still valid entry (selector); document one-click extra step in SUMMARY | None |
| XSS via malicious PDF name in UI | Low | Reuse existing display escaping from `UploadBox` / titles; no new `dangerouslySetInnerHTML` | None |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend `StudySetMeta` + `createStudySet` for `contentKind`</name>
  <files>src/types/studySet.ts,src/lib/db/studySetDb.ts</files>
  <read_first>src/types/studySet.ts,src/lib/db/studySetDb.ts,.planning/phases/20-ai-first-content-creation-output-type-selector-quiz-vs-flash/20-CONTEXT.md</read_first>
  <action>
Add exported type alias e.g. `export type StudyContentKind = "quiz" | "flashcards";` and optional field **`contentKind?: StudyContentKind`** on `StudySetMeta` (default `undefined` treated as **`quiz`** for backward compatibility in readers).

Extend `createStudySet` input object with optional **`contentKind?: StudyContentKind`**. When building `meta`, set `meta.contentKind = input.contentKind ?? "quiz"` **or** leave undefined for legacy — planner instruction: **persist explicit** `"quiz"` | `"flashcards"` for all new creates from typed routes so grep/tests are simple.

Ensure `putStudySetMeta` / any meta clone paths preserve the new field.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "StudyContentKind|contentKind" src/types/studySet.ts src/lib/db/studySetDb.ts` returns ≥1 match per file.
    - `createStudySet` signature in `studySetDb.ts` includes `contentKind` in its input type (optional or required).
  </acceptance_criteria>
  <done>Meta + API support content kind.</done>
</task>

<task type="auto">
  <name>Task 2: Add `/sets/new/quiz` upload page (move logic off root)</name>
  <files>src/app/(app)/sets/new/quiz/page.tsx</files>
  <read_first>src/app/(app)/sets/new/page.tsx,src/lib/db/studySetDb.ts,.planning/phases/20-ai-first-content-creation-output-type-selector-quiz-vs-flash/20-UI-SPEC.md</read_first>
  <action>
Create **`src/app/(app)/sets/new/quiz/page.tsx`** by **moving** (copy then later delete from root in Task 4) the current client implementation from `src/app/(app)/sets/new/page.tsx`: `UploadBox`, `ensureStudySetDb`, `getPdfPageCount`, `extractPdfText`, `generateStudySetTitle`, `createStudySet`, `router.push(\`/sets/${id}/source\`)`, error states.

Call **`createStudySet({ ..., contentKind: "quiz" })`**.

UI strings per **20-UI-SPEC §2** (`Create quiz from file`, one-line AI subcopy).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/(app)/sets/new/quiz/page.tsx` exists and exports `default` component.
    - File contains literal `Create quiz from file`.
    - File contains `contentKind: "quiz"` inside `createStudySet` call.
  </acceptance_criteria>
  <done>Quiz upload route works.</done>
</task>

<task type="auto">
  <name>Task 3: Add `/sets/new/flashcards` upload page</name>
  <files>src/app/(app)/sets/new/flashcards/page.tsx</files>
  <read_first>src/app/(app)/sets/new/quiz/page.tsx,.planning/phases/20-ai-first-content-creation-output-type-selector-quiz-vs-flash/20-UI-SPEC.md</read_first>
  <action>
Duplicate quiz upload page structure with **flashcards** copy (**20-UI-SPEC §3**) and **`contentKind: "flashcards"`** passed to `createStudySet`.

Reuse same PDF pipeline (flashcards still ingest PDF same as quiz for this phase).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/sets/new/flashcards/page.tsx` contains `Create flashcards from file`.
    - File contains `contentKind: "flashcards"` in `createStudySet` call.
  </acceptance_criteria>
  <done>Flashcards upload route works.</done>
</task>

<task type="auto">
  <name>Task 4: Replace `/sets/new` root with selector page</name>
  <files>src/app/(app)/sets/new/page.tsx</files>
  <read_first>src/app/(app)/sets/new/page.tsx,.planning/phases/20-ai-first-content-creation-output-type-selector-quiz-vs-flash/20-UI-SPEC.md,src/components/ui/card.tsx,src/components/ui/button.tsx</read_first>
  <action>
Replace default export: **remove** all `UploadBox` / PDF import logic (now lives under `quiz`/`flashcards`).

Implement **20-UI-SPEC §1**: heading `What do you want to create?`, two links to **`/sets/new/quiz`** and **`/sets/new/flashcards`**, plus `Link` to `/dashboard`.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/sets/new/page.tsx` contains string `What do you want to create?` exactly once.
    - Same file contains `href="/sets/new/quiz"` and `href="/sets/new/flashcards"` (or `Link` + same paths).
    - Same file contains both primary choice titles **`Create Quiz`** and **`Create Flashcards`** as visible text (per `20-UI-SPEC.md` §1 table).
    - Same file has `aria-label` on each primary choice control **or** documents in `20-01-SUMMARY.md` why native `<Link>` text is sufficient for SR.
    - Same file does **not** import `UploadBox`.
  </acceptance_criteria>
  <done>Selector page live at /sets/new.</done>
</task>

<task type="auto">
  <name>Task 5: Point dashboard, top bar, command palette to `/sets/new`</name>
  <files>src/components/dashboard/DashboardLibraryClient.tsx,src/components/layout/AppTopBar.tsx,src/components/layout/CommandPalette.tsx</files>
  <read_first>src/components/dashboard/DashboardLibraryClient.tsx,src/components/layout/AppTopBar.tsx,src/components/layout/CommandPalette.tsx</read_first>
  <action>
Verify every **`/sets/new`** href that was intended as “start import” still targets **`/sets/new`** (now selector). If any path pointed to a removed sub-route, fix to `/sets/new`.

Search repo: `rg "sets/new"` — ensure no stale assumptions in comments.

Command palette label may stay or become “New study set” — must still `go("/sets/new")`.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "href=\\\"/sets/new\\\"|go\\(\"/sets/new\"\\)|router\\.push\\(\"/sets/new\"\\)" src/components/dashboard/DashboardLibraryClient.tsx src/components/layout/AppTopBar.tsx src/components/layout/CommandPalette.tsx` shows at least the dashboard + top bar entries still use `/sets/new` exactly.
    - No `href="/sets/new/quiz"` from global chrome (only selector page should deep-link to quiz/flashcards).
  </acceptance_criteria>
  <done>Global entry funnel updated.</done>
</task>

</tasks>

## Verification (wave)

- `npm run lint` and `npm run build` succeed.
- Manual smoke: dashboard → Create → selector → quiz upload → lands on `/sets/[id]/source` with new set; repeat flashcards.
