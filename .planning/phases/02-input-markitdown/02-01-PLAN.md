---
phase: 02-input-markitdown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - README.md
  - src/lib/pipeline/validation.ts
  - src/lib/pipeline/validation.test.ts
  - src/lib/pipeline/markitdown.ts
  - src/lib/pipeline/markitdown.test.ts
  - src/app/api/study-sets/[id]/ingest/route.ts
  - supabase/migrations/20260725120000_v21_baseline.sql
autonomous: true
requirements:
  - INPUT-VAL-01
  - CONV-01
must_haves:
  truths:
    - "Phase 1 ingest stub, validation contract, and baseline migration exist on disk (or are restored)"
    - "Unsupported MIME, oversize files, empty paste, and non-YouTube URLs are rejected with clear error strings before any conversion"
    - "MarkItDown subprocess runner converts a temp file to markdown and cleans up temp paths"
    - "Python dependency is pinned in requirements.txt at markitdown[all]==0.1.6"
  artifacts:
    - path: requirements.txt
      provides: "Pinned MarkItDown Python dependency"
      contains: "markitdown[all]==0.1.6"
    - path: src/lib/pipeline/markitdown.ts
      provides: "Server-side MarkItDown subprocess runner"
      exports: ["convertWithMarkItDown", "convertPasteWithMarkItDown", "convertUrlWithMarkItDown"]
    - path: src/lib/pipeline/validation.ts
      provides: "INPUT-VAL-01 enforcement helpers"
      exports: ["validateFileUpload", "validatePasteInput", "validateYoutubeUrl"]
    - path: src/lib/pipeline/markitdown.test.ts
      provides: "Mocked spawn tests for CLI invocation"
  key_links:
    - from: src/lib/pipeline/markitdown.ts
      to: "python -m markitdown"
      via: "child_process.spawn with MARKITDOWN_PYTHON env override"
      pattern: "spawn\\(.*markitdown"
    - from: src/lib/pipeline/validation.ts
      to: SUPPORTED_MIME_TYPES
      via: "isSupportedMimeType + MAX_UPLOAD_BYTES_BY_MIME"
      pattern: "validateFileUpload"
---

<objective>
Establish the MarkItDown conversion engine and INPUT-VAL-01 enforcement layer that the ingest API and UI will call.

Purpose: Phase 2 conversion and validation must be testable in isolation before wiring the full ingest route (D-01, D-03, D-05).
Output: `requirements.txt`, `markitdown.ts` subprocess module with tests, extended `validation.ts` enforce functions with tests, verified Phase 1 prerequisites.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-input-markitdown/02-CONTEXT.md
@.planning/phases/02-input-markitdown/02-RESEARCH.md
@docs/pipeline.md
@src/lib/pipeline/validation.ts
@src/lib/pipeline/validation.test.ts
@src/app/api/study-sets/[id]/ingest/route.ts
@supabase/migrations/20260725120000_v21_baseline.sql

<interfaces>
From src/lib/pipeline/validation.ts (extend, do not replace constants):
- SUPPORTED_MIME_TYPES — 15 MIME strings covering INPUT-01–10
- MAX_UPLOAD_BYTES_BY_MIME — per-format byte limits
- isSupportedMimeType(mime: string): mime is SupportedMimeType
- PasteInput, YoutubeInput, FileInput, PipelineInput union types

From src/app/api/study-sets/[id]/ingest/route.ts (stub — keep auth pattern):
- requireApiUser() from @/lib/api/requireApiUser
- verifyStudySet(supabase, userId, studySetId) — ownership check before ingest

MarkItDown CLI contract (D-03):
- python -m markitdown <input-path-or-url> -o <output.md>
- Env override: MARKITDOWN_PYTHON (default "python")
- Pin: markitdown[all]==0.1.6 in requirements.txt
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Phase 1 prerequisites</name>
  <read_first>
    src/app/api/study-sets/[id]/ingest/route.ts
    src/lib/pipeline/validation.ts
    supabase/migrations/20260725120000_v21_baseline.sql
    src/lib/api/requireApiUser.ts
  </read_first>
  <files>src/app/api/study-sets/[id]/ingest/route.ts, src/lib/pipeline/validation.ts, supabase/migrations/20260725120000_v21_baseline.sql</files>
  <action>
    Confirm Phase 1 artifacts exist on disk per D-P1-17 and D-P1-19. Required files:
    1. Ingest stub at src/app/api/study-sets/[id]/ingest/route.ts returning 501 with requireApiUser + verifyStudySet — restore from Phase 1 pattern if missing.
    2. validation.ts with SUPPORTED_MIME_TYPES and MAX_UPLOAD_BYTES_BY_MIME — restore if missing.
    3. Baseline migration with canonical_documents (raw_markdown, original_storage_path, metadata) and study_sets.pipeline_stage enum including 'raw' — restore if missing.

    Do NOT implement ingest logic in this task — only verify/fix prerequisites. If ingest route was partially edited, preserve auth + verifyStudySet and keep 501 stub until Plan 02-02.
  </action>
  <verify>
    <automated>test -f src/app/api/study-sets/[id]/ingest/route.ts &amp;&amp; test -f src/lib/pipeline/validation.ts &amp;&amp; test -f supabase/migrations/20260725120000_v21_baseline.sql &amp;&amp; grep -q "canonical_documents" supabase/migrations/20260725120000_v21_baseline.sql &amp;&amp; grep -q "requireApiUser" src/app/api/study-sets/[id]/ingest/route.ts</automated>
  </verify>
  <acceptance_criteria>
    All three prerequisite files exist; ingest route has auth gate; migration defines canonical_documents.raw_markdown and pipeline_stage 'raw'.
  </acceptance_criteria>
  <done>Phase 1 foundation artifacts verified or restored; ingest route remains 501 stub.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: INPUT-VAL-01 enforcement functions + tests</name>
  <read_first>
    src/lib/pipeline/validation.ts
    src/lib/pipeline/validation.test.ts
    docs/pipeline.md
  </read_first>
  <files>src/lib/pipeline/validation.ts, src/lib/pipeline/validation.test.ts</files>
  <behavior>
    - validateFileUpload("application/pdf", 1024) returns null (valid)
    - validateFileUpload("application/zip", 1024) returns non-null error mentioning unsupported type
    - validateFileUpload("application/pdf", MAX+1) returns non-null error mentioning size/limit
    - validatePasteInput("hello world") returns null for text >= 20 chars (or documented min)
    - validatePasteInput("") and validatePasteInput("   ") return non-null error
    - validateYoutubeUrl("https://www.youtube.com/watch?v=abc") returns null
    - validateYoutubeUrl("https://youtu.be/abc") returns null
    - validateYoutubeUrl("http://169.254.169.254/") returns non-null error
    - validateYoutubeUrl("https://example.com/video") returns non-null error
    - All SUPPORTED_MIME_TYPES pass validateFileUpload at 1 byte
  </behavior>
  <action>
  Extend validation.ts per D-05 and INPUT-VAL-01 (not deferred to route-only checks):

  1. Add validateFileUpload(mimeType: string, sizeBytes: number): string | null — returns human-readable error or null. Use isSupportedMimeType and MAX_UPLOAD_BYTES_BY_MIME. Export human-readable limit helper formatBytesForError(limit) if needed by UI later.

  2. Add validatePasteInput(text: string): string | null — reject empty/whitespace-only; require minimum length (use 20 chars per UI-SPEC "few lines" intent; document constant MIN_PASTE_CHARS).

  3. Add validateYoutubeUrl(url: string): string | null — strict host allowlist: youtube.com, www.youtube.com, m.youtube.com, youtu.be; require https: protocol; reject IPs and non-YouTube hosts (SSRF mitigation per RESEARCH Pitfall 6).

  4. Export YOUTUBE_HOST_ALLOWLIST and MIN_PASTE_CHARS for client reuse.

  5. Extend validation.test.ts with cases above BEFORE implementing functions (TDD). Remove header comment "Enforcement at ingest routes is deferred to Phase 2" once functions exist.

  Do NOT add route handlers or MarkItDown calls in this task.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/pipeline/validation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    All INPUT-VAL-01 unit tests pass; enforcement functions exported; no conversion side effects in validation module.
  </acceptance_criteria>
  <done>validateFileUpload, validatePasteInput, validateYoutubeUrl implemented and fully tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MarkItDown subprocess module + requirements.txt</name>
  <read_first>
    .planning/phases/02-input-markitdown/02-RESEARCH.md
    docs/pipeline.md
  </read_first>
  <files>requirements.txt, README.md, src/lib/pipeline/markitdown.ts, src/lib/pipeline/markitdown.test.ts</files>
  <behavior>
    - convertWithMarkItDown(tempPath) spawns python -m markitdown with -o outPath; returns markdown string; deletes outPath in finally
    - convertPasteWithMarkItDown(text) writes temp .txt, calls convertWithMarkItDown, deletes input temp
    - convertUrlWithMarkItDown(url) passes URL as CLI positional arg (no app-side download)
    - On non-zero exit code, throws Error containing stderr snippet
    - markitdown.test.ts mocks child_process.spawn — asserts argv includes "-m", "markitdown", input, "-o"
  </behavior>
  <action>
  Per D-01, D-02, D-03:

  1. Create requirements.txt at repo root: markitdown[all]==0.1.6 (single line or with comment Python >=3.10).

  2. Add README section "Python / MarkItDown setup" with venv create, pip install -r requirements.txt, python -m markitdown --version, and MARKITDOWN_PYTHON env var. Note Vercel pure-serverless limitation (Python not available) per RESEARCH A2.

  3. Create src/lib/pipeline/markitdown.ts:
     - export const MARKITDOWN_VERSION = "0.1.6"
     - convertWithMarkItDown(inputPath: string): Promise&lt;string&gt; — spawn MARKITDOWN_PYTHON ?? "python" with ["-m","markitdown", inputPath, "-o", outPath]; read outPath; finally unlink outPath and any input temps owned by caller
     - convertPasteWithMarkItDown(text: string): Promise&lt;string&gt; — write temp paste-{uuid}.txt under os.tmpdir(), convert, cleanup
     - convertUrlWithMarkItDown(url: string): Promise&lt;string&gt; — pass url directly to CLI (caller must validate URL first)
     - Use node:child_process spawn, node:fs/promises, node:os, node:path, node:crypto

  4. Create markitdown.test.ts with vi.mock("node:child_process") — test happy path (exit 0 + file read) and failure path (non-zero exit). Do NOT require real Python in unit tests.

  No npm packages added. No ingest route changes in this task.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/pipeline/markitdown.test.ts &amp;&amp; grep -q "markitdown\[all\]==0.1.6" requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    markitdown.test.ts passes with mocked spawn; requirements.txt pins 0.1.6; README documents Python setup; three convert exports exist.
  </acceptance_criteria>
  <done>MarkItDown server module ready for ingest orchestration; Python dependency documented and pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node → Python subprocess | Untrusted file paths and URLs passed to MarkItDown after validation |
| validation.ts consumers | Client and server both import enforce helpers — errors must not leak internals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | validateYoutubeUrl | mitigate | HTTPS-only + host allowlist (youtube.com, youtu.be variants) before URL reaches MarkItDown |
| T-02-02 | Tampering | validateFileUpload | mitigate | Reject unsupported MIME and oversize bytes using MAX_UPLOAD_BYTES_BY_MIME before any storage/subprocess |
| T-02-03 | Denial of Service | validateFileUpload | mitigate | Per-MIME byte caps; paste min/max length guard |
| T-02-04 | Elevation | MarkItDown subprocess | accept | Runs with server process privileges; gated by upstream auth in ingest route (Plan 02-02) |
| T-02-05 | Tampering | pip install markitdown | mitigate | Pin markitdown[all]==0.1.6 in requirements.txt; official Microsoft PyPI package per Package Legitimacy Audit |
| T-02-SC | Tampering | npm/pip installs | mitigate | No new npm deps; Python package from official PyPI only |
</threat_model>

<verification>
- npm test -- --run src/lib/pipeline/ passes
- requirements.txt contains markitdown[all]==0.1.6
- Optional local smoke: python -m markitdown --version (human dev env only)
</verification>

<success_criteria>
1. INPUT-VAL-01 enforcement functions reject bad inputs with clear strings (unit tested)
2. MarkItDown subprocess module exists with mocked unit tests (CONV-01 foundation)
3. Phase 1 prerequisites verified on disk
4. Python setup documented in README + requirements.txt
</success_criteria>

<output>
Create `.planning/phases/02-input-markitdown/02-01-SUMMARY.md` when done
</output>
