---
phase: 02-input-markitdown
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - src/components/upload/UploadBox.tsx
  - src/components/edit/new/import/UnifiedInputZone.tsx
  - src/components/edit/new/import/IngestProgressCard.tsx
  - src/components/edit/new/import/StudySetNewImportStepContext.tsx
  - src/lib/client/ingestStudySet.ts
  - src/app/(app)/edit/new/quiz/page.tsx
  - src/app/(app)/edit/new/flashcards/page.tsx
  - src/app/(app)/sets/[id]/source/page.tsx
  - src/components/edit/new/format-selection/FormatSelectionCard.tsx
  - src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx
  - src/components/edit/new/NewStudySetPdfImportFlowDynamic.tsx
  - src/components/edit/new/NewStudySetTextImportFlow.tsx
autonomous: true
requirements:
  - INPUT-01
  - INPUT-02
  - INPUT-03
  - INPUT-04
  - INPUT-05
  - INPUT-06
  - INPUT-07
  - INPUT-08
  - INPUT-09
  - INPUT-10
  - INPUT-11
  - INPUT-12
  - INPUT-VAL-01
  - CONV-02
user_setup: []
must_haves:
  truths:
    - "User sees unified File | Paste | YouTube tabs on /edit/new/quiz and /edit/new/flashcards (D-10)"
    - "Client validates MIME/size/paste/URL before calling ingest using shared validation.ts helpers (INPUT-VAL-01)"
    - "Large files upload directly to doc2quiz Storage then POST file_ref JSON ingest (RESEARCH Pattern 3)"
    - "Progress shows real steps: validating → uploading → converting — no fake percentage bar (D-12)"
    - "After successful ingest, user lands on /sets/{id}/source placeholder — not quiz generation (D-13)"
    - "Legacy PDF-only import paths removed from product routes (D-16)"
  artifacts:
    - path: src/components/edit/new/import/UnifiedInputZone.tsx
      provides: "Unified input zone with tabs and CTA"
    - path: src/components/edit/new/import/IngestProgressCard.tsx
      provides: "Real ingest progress step labels"
    - path: src/lib/client/ingestStudySet.ts
      provides: "Client ingest orchestration (storage upload + API)"
    - path: src/components/upload/UploadBox.tsx
      provides: "Generalized multi-format drop zone"
  key_links:
    - from: UnifiedInputZone
      to: src/lib/client/ingestStudySet.ts
      via: "Convert document CTA"
      pattern: "ingestStudySet"
    - from: ingestStudySet.ts
      to: "doc2quiz bucket"
      via: "supabase.storage.upload for files >10MB or all production files"
      pattern: "storage\\.from\\(['\"]doc2quiz"
    - from: ingestStudySet.ts
      to: "/api/study-sets/[id]/ingest"
      via: "fetch POST JSON or multipart"
      pattern: "api/study-sets/.*/ingest"
    - from: quiz/page.tsx
      to: UnifiedInputZone
      via: "replaces NewStudySetTextImportFlow"
      pattern: "UnifiedInputZone"
---

<objective>
Wire the unified input zone UI to the ingest API, with client-side validation, Storage upload, real progress states, and legacy import cleanup.

Purpose: Delivers the user-facing INPUT zone and completes the Phase 2 end-to-end flow (D-10–D-13, D-16).
Output: UnifiedInputZone, IngestProgressCard, ingestStudySet client module, updated quiz/flashcards import pages, source placeholder route.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-input-markitdown/02-UI-SPEC.md
@.planning/phases/02-input-markitdown/02-CONTEXT.md
@.planning/phases/02-input-markitdown/02-02-SUMMARY.md
@src/components/edit/new/import/StudySetNewImportStepContext.tsx
@src/app/(app)/edit/new/quiz/page.tsx
@src/app/(app)/edit/new/flashcards/page.tsx
@src/lib/pipeline/validation.ts
@src/lib/client/studySetDb.ts
@DESIGN.md

<interfaces>
From 02-UI-SPEC.md (copy + layout contract):
- H1: "Add your source material" on both quiz and flashcards routes
- Tabs: File | Paste | YouTube
- CTA: "Convert document" / busy "Converting…"
- Progress steps: validating → uploading (file only) → converting
- Toast error strings per UI-SPEC error table
- ImportStepTabStrip PHASE_LABELS: upload="Add source", read="Convert", generate="Review"

From src/lib/client/studySetDb.ts:
- createStudySetEarlyMeta({ title, contentKind }) → studySetId (pipeline_stage "input")
- createSupabaseBrowserClient via @/lib/client/supabase

From Plan 02-02 ingest API:
- POST /api/study-sets/{id}/ingest
- JSON: { kind: "paste"|"youtube"|"file_ref", ... }
- multipart: form field "file" (small files)
- Success: { studySetId, pipelineStage: "raw" }

Storage upload (D-14): path `${userId}/${studySetId}/${sanitizedFilename}` to bucket doc2quiz

Post-ingest href (D-13): `/sets/${id}/source`

Note: UploadBox.tsx is deleted on disk (git status D) — recreate per UI-SPEC, generalized for all MIME types.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UploadBox + UnifiedInputZone + IngestProgressCard</name>
  <read_first>
    .planning/phases/02-input-markitdown/02-UI-SPEC.md
    src/components/edit/new/NewStudySetTextImportFlow.tsx
    src/components/edit/new/import/StudySetNewImportStepContext.tsx
    src/lib/pipeline/validation.ts
    DESIGN.md
  </read_first>
  <files>src/components/upload/UploadBox.tsx, src/components/edit/new/import/UnifiedInputZone.tsx, src/components/edit/new/import/IngestProgressCard.tsx, src/components/edit/new/import/StudySetNewImportStepContext.tsx</files>
  <action>
  Build UI components per 02-UI-SPEC and D-10, D-11, D-12:

  1. Recreate UploadBox.tsx (deleted — do not import PDF-only validators):
     - Props: onFileSelected(file: File), disabled?, tall mode min-h-[280px]
     - accept attribute built from SUPPORTED_MIME_TYPES extensions
     - Drag-drop + hidden input + "Choose file" link span
     - CloudUpload icon, framer breathe animation with useReducedMotion guard
     - Dashed border, muted bg, coral focus ring per UI-SPEC

  2. UnifiedInputZone.tsx:
     - Props: contentKind, onIngestStart, onIngestComplete(studySetId), onIngestError
     - shadcn Tabs variant="line": File | Paste | YouTube (default File)
     - File panel: UploadBox + filename/size meta
     - Paste panel: Textarea min-h-[280px] font-mono, label "Source text"
     - YouTube panel: Input type="url", label "YouTube URL"
     - Client validation via validateFileUpload, validatePasteInput, validateYoutubeUrl — toast errors per UI-SPEC copy table
     - Primary CTA bottom-right: "Convert document" / busy "Converting…"
     - Card shell: rounded-xl border bg-card/60 p-5 ring-1 ring-foreground/10 (no shadow-sm stack)
     - H1 + subcopy from UI-SPEC (not PDF-centric)

  3. IngestProgressCard.tsx:
     - Props: step: "validating" | "uploading" | "converting" | "error", errorMessage?, technicalDetails?
     - Layout: border-l-[6px] border-d2q-accent, step checklist (no fake % bar)
     - aria-live="polite" on headline
     - Reuse ImportFlowTechnicalDetails collapsed section
     - Copy per UI-SPEC progress table

  4. Update StudySetNewImportStepContext.tsx PHASE_LABELS: "Add source" | "Convert" | "Review" (D-11, UI-SPEC)

  Do NOT wire API calls yet — components render and client-validate only.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    UnifiedInputZone renders three tabs; UploadBox accepts pipeline MIME types; IngestProgressCard shows step labels; PHASE_LABELS updated; build passes.
  </acceptance_criteria>
  <done>UI shell components exist matching UI-SPEC layout, copy, and accessibility requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Client ingest module + page wiring</name>
  <read_first>
    src/lib/client/studySetDb.ts
    src/lib/client/supabase.ts
    .planning/phases/02-input-markitdown/02-RESEARCH.md
    src/app/(app)/edit/new/quiz/page.tsx
    src/app/(app)/edit/new/flashcards/page.tsx
  </read_first>
  <files>src/lib/client/ingestStudySet.ts, src/components/edit/new/import/UnifiedInputZone.tsx, src/app/(app)/edit/new/quiz/page.tsx, src/app/(app)/edit/new/flashcards/page.tsx</files>
  <action>
  Wire end-to-end client ingest per D-04, D-07–D-09, RESEARCH Pattern 3:

  1. Create src/lib/client/ingestStudySet.ts:
     - export type IngestUiStep = "idle" | "validating" | "uploading" | "converting" | "done" | "error"
     - export async function ingestStudySetSource(params): onStep callback for UI progress
     - Flow:
       a. createStudySetEarlyMeta({ title: provisional from filename or "New study set", contentKind })
       b. For File: if file.size > 10*1024*1024 OR always in production path — upload to doc2quiz at `${userId}/${studySetId}/${sanitizedFilename}` via browser supabase client; POST JSON { kind:"file_ref", storagePath, mimeType, filename, sizeBytes }
       c. For File small/dev: optional multipart POST with form field "file"
       d. For Paste: POST JSON { kind:"paste", text }
       e. For YouTube: POST JSON { kind:"youtube", url }
     - Parse API errors; surface message for toast
     - Return studySetId on success

  2. Integrate into UnifiedInputZone:
     - On CTA click: set ingestStep validating → call ingestStudySetSource
     - Map steps to StudySetNewImportStep: idle=upload, validating/uploading/converting=read, done=generate then redirect
     - Show IngestProgressCard during non-idle non-error steps; hide input zone while ingesting
     - Paste/YouTube: skip uploading step visually (D-12)

  3. Update quiz/page.tsx and flashcards/page.tsx:
     - Replace NewStudySetTextImportFlow with UnifiedInputZone inside existing workbench shell
     - Update page copy to UI-SPEC H1/subcopy
     - getPostIngestHref: (id) => `/sets/${id}/source`
     - Keep Back link + ImportStepTabStrip layout unchanged (D-11)

  4. Stop using createStudySet({ extractedText }) — ingest API is sole path (D-15).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    Both import pages use UnifiedInputZone; ingestStudySet calls Storage + /ingest API; progress steps update; redirect targets /sets/{id}/source.
  </acceptance_criteria>
  <done>User can submit file, paste, or YouTube URL from quiz and flashcards import pages through the ingest API.</done>
</task>

<task type="auto">
  <name>Task 3: Source placeholder + legacy cleanup</name>
  <read_first>
    src/app/(app)/sets/[id]/source/page.tsx
    src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx
    .planning/phases/02-input-markitdown/02-CONTEXT.md
  </read_first>
  <files>src/app/(app)/sets/[id]/source/page.tsx, src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx, src/components/edit/new/NewStudySetPdfImportFlowDynamic.tsx, src/components/edit/new/NewStudySetTextImportFlow.tsx, src/components/edit/new/format-selection/FormatSelectionCard.tsx</files>
  <action>
  Complete D-13 and D-16 legacy cleanup:

  1. Replace src/app/(app)/sets/[id]/source/page.tsx redirect with Phase 2 placeholder:
     - Fetch study set meta + canonical_documents.raw_markdown preview (first 500 chars or full if short)
     - Show banner: "Source converted — canonical builder coming next." (RESEARCH resolved post-ingest copy)
     - Link to dashboard and note pipeline_stage
     - If pipeline_stage still "input", redirect back to appropriate /edit/new route
     - Do NOT redirect immediately to quiz editor

  2. Remove legacy import from product paths (D-16):
     - Ensure no route imports NewStudySetPdfImportFlow or NewStudySetPdfImportFlowDynamic
     - Delete or gut NewStudySetPdfImportFlow.tsx — remove generate-from-file, pdf.js, OCR hooks; if file kept for reference, export nothing used by app routes
     - Mark NewStudySetTextImportFlow.tsx deprecated — no remaining imports from app routes (UnifiedInputZone replaces it)

  3. Update FormatSelectionCard copy (minor): mention documents + paste + YouTube, not ".txt only" per UI-SPEC implementation note 7.

  4. Grep repo for generate-from-file, runPdfUploadSession, validatePdfFile — remove dead imports from Phase 2 surfaces. Do not resurrect deleted API routes.

  Verify no product route links to PDF-only import flow.
  </action>
  <verify>
    <automated>rg "NewStudySetPdfImportFlow|generate-from-file|NewStudySetTextImportFlow" src/app --glob "*.tsx" -c | rg "^0$" || (rg "NewStudySetPdfImportFlow|generate-from-file" src/app --glob "*.tsx" ; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    /sets/[id]/source shows raw placeholder not editor redirect; no app route imports legacy PDF/text-only flows; format chooser copy updated.
  </acceptance_criteria>
  <done>Legacy v1 import paths removed; post-ingest navigation lands on source placeholder per D-13.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Supabase Storage | Authenticated user uploads to own prefix only |
| Browser → /ingest API | Session cookie auth; study set must belong to user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Tampering | Client file MIME | mitigate | validateFileUpload on client AND server; never trust client-only |
| T-02-13 | Elevation | Storage upload path | mitigate | Build path from auth.uid() + server-created studySetId; never accept path from user input alone |
| T-02-14 | Information Disclosure | source placeholder page | accept | User sees own raw_markdown preview only; RLS enforced via client fetch |
| T-02-15 | Denial of Service | large file upload | mitigate | Client-side size check before upload; Storage RLS limits scope to owner prefix |
</threat_model>

<verification>
- npm run build — passes
- npm test -- --run src/lib/pipeline/ — still green (no regressions)
- Manual: quiz import page shows File/Paste/YouTube tabs; paste ingest reaches /sets/{id}/source
</verification>

<success_criteria>
1. Unified input zone on both /edit/new routes accepts all INPUT-01–12 types
2. INPUT-VAL-01 client validation with server error surfacing
3. Real progress labels during ingest (D-12)
4. Post-ingest navigation to /sets/{id}/source placeholder (D-13)
5. Legacy PDF-only import removed from product (D-16)
</success_criteria>

<output>
Create `.planning/phases/02-input-markitdown/02-03-SUMMARY.md` when done
</output>
