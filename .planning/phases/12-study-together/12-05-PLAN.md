---
phase: 12-study-together
plan: 05
type: execute
wave: 5
depends_on: [12-02, 12-03, 12-04]
files_modified:
  - src/app/api/friends/[userId]/route.ts
  - src/app/api/friends/friends.route.test.ts
  - src/components/friends/FriendActionMenu.tsx
  - src/lib/client/friends.ts
  - src/lib/locale/messages.ts
  - src/lib/locale/types.ts
autonomous: true
requirements: [SOCIAL-08, SOCIAL-09]
must_haves:
  truths:
    - "Friend removal is explicit, separate from block, and safe to retry."
    - "Friends API returns safe cached/proxied avatar data and accurate social status vocabulary."
  artifacts:
    - path: "src/app/api/friends/[userId]/route.ts"
      provides: "Validated dedicated remove-friend endpoint"
    - path: "src/components/friends/FriendActionMenu.tsx"
      provides: "Separate study, message, profile, remove, block, report actions"
  key_links:
    - from: "FriendActionMenu"
      to: "/api/friends/[userId]"
      via: "explicit DELETE remove action"
      pattern: "removeFriend"
---
<objective>
Finish discrete friend repair contracts before hub wiring.
Purpose: Remove, block, avatar handling, and action UI need isolated executable ownership.
Output: Dedicated remove endpoint, safe overview data, action-menu proof.
</objective>
<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/12-study-together/12-VALIDATION.md
@.planning/phases/12-study-together/12-01-PLAN.md
@src/app/api/friends/route.ts
@src/components/friends/FriendActionMenu.tsx
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Ship distinct remove-friend and safe overview contracts</name>
  <files>src/app/api/friends/[userId]/route.ts, src/app/api/friends/friends.route.test.ts, src/lib/client/friends.ts</files>
  <behavior>
    - DELETE removes only authenticated caller accepted relationship; it does not create a block and is deterministic for retry/unavailable state.
    - Overview contains stable server-safe avatar URL, presence vocabulary, and separately named counts from 12-03.
  </behavior>
  <action>Create authenticated Zod UUID `DELETE /api/friends/[userId]` calling 12-01 typed `remove_friend` RPC and its common error mapper; preserve block/report endpoints unchanged. Finish overview client type handling for safe avatar proxy/cache URL only, never storage object path, and `online`, `recently active`, `offline`. Add tests proving delete invokes remove RPC not block RPC, rejects unauthenticated/invalid/cross-user input, and overview never exposes raw avatar path.</action>
  <verify><automated>npx vitest run src/app/api/friends/friends.route.test.ts</automated></verify>
  <done>Remove is an independently testable API action; overview client handles safe social data.</done>
</task>
<task type="auto">
  <name>Task 2: Wire distinct action menu copy and confirmation</name>
  <files>src/components/friends/FriendActionMenu.tsx, src/lib/client/friends.ts, src/lib/locale/messages.ts, src/lib/locale/types.ts</files>
  <action>Wire `Study together` as main per-friend action and preserve separate view profile, message, remove friend, block, report actions. Use localized explicit destructive confirmation; remove calls dedicated delete endpoint and refreshes 12-03 count/friends state, while block uses existing block endpoint. Use safe avatar URL and defined status vocabulary; do not add friend-shared challenge source flows.</action>
  <verify><automated>npm run typecheck && npm run lint</automated></verify>
  <done>UI exposes clear separate social actions and cannot route removal through block.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| friend action→API | User-controlled ID/action must not mutate unrelated relationship |
| storage metadata→overview | Internal avatar paths must remain server-only |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-12-16 | T | remove endpoint | mitigate | Auth, UUID validation, locked RPC, no block reuse. |
| T-12-17 | I | avatar response | mitigate | Stable safe URL only and route tests. |
| T-12-SC | Tampering | npm installs | accept | No package installation. |
</threat_model>
<verification>Run focused route tests, typecheck, lint.</verification>
<success_criteria>Distinct remove/block behavior and safe overview/action presentation satisfy SOCIAL-08/09.</success_criteria>
<output>Create `.planning/phases/12-study-together/12-05-SUMMARY.md` when done.</output>
