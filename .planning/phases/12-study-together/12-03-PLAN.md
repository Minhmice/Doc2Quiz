---
phase: 12-study-together
plan: 03
type: execute
wave: 3
depends_on: [12-02, 12-07]
files_modified:
  - src/app/api/friends/route.ts
  - src/app/api/friends/requests/route.ts
  - src/app/api/friends/messages/[conversationId]/route.ts
  - src/app/api/friends/activity/route.ts
  - src/app/api/friends/notifications/unread-count/route.ts
  - src/lib/client/friends.ts
  - src/lib/client/messages.ts
  - src/lib/client/socialCounts.ts
  - src/lib/client/socialCounts.test.ts
  - src/components/layout/FriendsMenu.tsx
  - src/app/api/friends/friends.route.test.ts
  - src/app/api/friends/notifications/notifications.route.test.ts
autonomous: true
requirements: [SOCIAL-07, SOCIAL-08, SOCIAL-09, SOCIAL-10]
must_haves:
  truths:
    - "Topbar separately displays server-derived notification, incoming-request, and unread-message counts."
    - "Each count reconciles after private event, subscribe/rejoin, window focus, and visible-document transition."
  artifacts:
    - path: "src/lib/client/socialCounts.ts"
      provides: "Single three-count fetch and private invalidation reconciliation controller"
    - path: "src/components/layout/FriendsMenu.tsx"
      provides: "Compact topbar launcher using reconciled social counts"
  key_links:
    - from: "FriendsMenu"
      to: "/api/friends, /api/friends/notifications/unread-count"
      via: "one reconciliation function invoked by events and lifecycle recovery"
      pattern: "refreshSocialCounts"
---

<objective>
Make social badge counts durable, separate, and reconcilable before adding new social UI.

Purpose: Notifications, incoming requests, and unread messages must not depend on event delivery.
Output: Count endpoints/events, shared reconciliation client, focused tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-study-together/12-VALIDATION.md
@.planning/phases/12-study-together/12-02-PLAN.md
@src/components/layout/FriendsMenu.tsx
@src/lib/client/friends.ts
@src/lib/client/messages.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Expose separate durable social counts and invalidation writes</name>
  <files>src/app/api/friends/route.ts, src/app/api/friends/requests/route.ts, src/app/api/friends/messages/[conversationId]/route.ts, src/app/api/friends/activity/route.ts, src/app/api/friends/notifications/unread-count/route.ts, src/app/api/friends/friends.route.test.ts, src/app/api/friends/notifications/notifications.route.test.ts</files>
  <behavior>
    - Overview returns separately named incoming-request and unread-message counts; notification endpoint returns notification count.
    - Friend-request, message-read/write, challenge notification, and reaction writes emit a private invalidation event or durable notification after committed state, without treating broadcast failure as mutation failure.
  </behavior>
  <action>Implement bounded authenticated count contracts required by `12-VALIDATION.md`: `incomingRequestCount`, `unreadMessageCount`, and `notificationUnreadCount`; aggregate only at display layer. Make request writes emit recipient `social-requests:{userId}` invalidation after durable request state, message writes/read changes emit participant `social-messages:{conversationId}` invalidation plus recipient `social-counts:{userId}` invalidation, and challenge/notification writes retain durable notification-trigger invalidation. Consume private-topic authorization from additive migration `20260731101000_phase12_social_realtime_topics.sql` created by 12-07; do not edit any prior migration. Tests must prove count isolation, event emission target, write success despite event failure, and no opening/list endpoint marks anything read.</action>
  <verify><automated>npx vitest run src/app/api/friends/friends.route.test.ts src/app/api/friends/notifications/notifications.route.test.ts</automated></verify>
  <done>Three counts are server-derived and distinct; all relevant writes leave durable state then issue safe private invalidation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile all topbar social counts on events and lifecycle recovery</name>
  <files>src/lib/client/friends.ts, src/lib/client/messages.ts, src/lib/client/socialCounts.ts, src/lib/client/socialCounts.test.ts, src/components/layout/FriendsMenu.tsx</files>
  <behavior>
    - One reconciliation operation fetches all three count sources and newest notification page.
    - It runs on each private event, SUBSCRIBED/rejoin, window focus, and visible-document transition; events never supply displayed count truth.
  </behavior>
  <action>Create tested `socialCounts` client/controller. It opens private notifications, requests, and count/message topics for authenticated user, schedules one bounded reconciliation for event bursts, and cleans all channels/listeners on unmount. `FriendsMenu` uses controller and shows aggregate badge while retaining individual counts for Requests, Study invites, and Unread messages. Add tests with mocked fetch/channel lifecycle proving all three endpoints are refetched on notification/request/message event, subscribe/rejoin, focus, and visibility recovery; prove stale event payload cannot replace fetched counts.</action>
  <verify><automated>npx vitest run src/lib/client/socialCounts.test.ts && npm run typecheck</automated></verify>
  <done>Topbar reliably converges notification/request/message badges after missed events or lifecycle recovery.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Realtime event→topbar | Event is untrusted invalidation hint, not count authority |
| request/message mutation→recipient | Private topic must not expose another user count |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-12-11 | I | count topics | mitigate | Recipient/participant RLS plus private channels. |
| T-12-12 | T/R | stale events | mitigate | Fetch all server-derived counts on every event/recovery. |
| T-12-13 | D | event bursts | mitigate | Coalesce bounded reconciliation calls and cleanup listeners. |
| T-12-SC | Tampering | npm installs | accept | No package installation. |
</threat_model>

<verification>Run focused count tests, then `npm run typecheck`.</verification>
<success_criteria>Topbar has three durable count paths and reconciliation evidence required by `12-VALIDATION.md`.</success_criteria>
<output>Create `.planning/phases/12-study-together/12-03-SUMMARY.md` when done.</output>
