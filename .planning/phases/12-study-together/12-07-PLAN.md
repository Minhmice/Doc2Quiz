---
phase: 12-study-together
plan: 07
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - supabase/migrations/20260731101000_phase12_social_realtime_topics.sql
  - scripts/verify-phase12-study-together-sql.mjs
autonomous: true
requirements: [SOCIAL-07, SOCIAL-09, SOCIAL-10]
must_haves:
  truths:
    - "Only intended recipients or conversation participants can subscribe to Phase 12 private invalidation topics."
    - "Realtime topic authorization is added by a new migration and never by editing an applied migration."
  artifacts:
    - path: "supabase/migrations/20260731101000_phase12_social_realtime_topics.sql"
      provides: "Additive private-topic RLS and invalidation support"
      contains: "realtime.messages"
  key_links:
    - from: "private realtime topic policies"
      to: "auth.uid() and conversation participants"
      via: "recipient/participant-bound RLS predicates"
      pattern: "realtime.topic"
---
<objective>
Add private request, message, and social-count realtime authorization as an additive migration.
Purpose: Later count and chat clients need secure private invalidation without retroactively changing foundation SQL.
Output: One later migration plus SQL/RLS proof.
</objective>
<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/12-study-together/CONTEXT.md
@.planning/phases/12-study-together/12-RESEARCH.md
@.planning/phases/12-study-together/12-VALIDATION.md
@.planning/phases/12-study-together/12-01-PLAN.md
@supabase/migrations/20260730170000_friends_messages_presence.sql
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add additive private-topic realtime migration</name>
  <files>supabase/migrations/20260731101000_phase12_social_realtime_topics.sql</files>
  <behavior>
    - `social-requests:{userId}` and `social-counts:{userId}` permit only matching authenticated user.
    - `social-messages:{conversationId}` permits only authenticated conversation participants.
    - A different authenticated user cannot receive or send on any protected topic.
  </behavior>
  <action>Create a new timestamped migration after `20260731100000_phase12_study_together_foundation.sql`; never edit prior migrations for these policies. Add narrowly scoped `realtime.messages` SELECT/INSERT authorization for private broadcast topics used by Plans 12-03 and 12-09. Bind user topics to `auth.uid()` and conversation topics to existing participant authority. Revoke broad access and avoid `using (true)`. Keep notification-topic policy owned by 12-01; do not duplicate it.</action>
  <verify><automated>node scripts/verify-phase12-study-together-sql.mjs --migration supabase/migrations/20260731101000_phase12_social_realtime_topics.sql</automated></verify>
  <done>New migration applies after foundation and authorizes only intended user/conversation private topics.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Prove topic isolation and migration ordering</name>
  <files>scripts/verify-phase12-study-together-sql.mjs</files>
  <behavior>
    - Runner proves owner/participant access and cross-user denial for every new topic.
    - Runner rejects missing or incorrectly ordered additive migration.
  </behavior>
  <action>Extend safe disposable-target SQL runner with migration-order assertion and authenticated fixtures for request, count, and conversation topics. Prove intended subscribe/send access and unrelated-user denial. Preserve target allowlist and fixture-only cleanup.</action>
  <verify><automated>node scripts/verify-phase12-study-together-sql.mjs --migration supabase/migrations/20260731101000_phase12_social_realtime_topics.sql</automated></verify>
  <done>RLS proof covers all new private topics and confirms foundation migration remained unchanged.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| authenticated client→realtime.messages | Predictable topic names cross private authorization boundary |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-12-21 | I/E | private topics | mitigate | User-bound and participant-bound RLS predicates; cross-user SQL proof. |
| T-12-22 | T | migration history | mitigate | Additive later migration; ordering proof; no prior migration edits. |
| T-12-SC | Tampering | npm installs | accept | No package installation. |
</threat_model>
<verification>Run additive migration SQL proof against approved disposable target.</verification>
<success_criteria>Secure private invalidation contracts exist in their own later migration.</success_criteria>
<output>Create `.planning/phases/12-study-together/12-07-SUMMARY.md` when done.</output>
