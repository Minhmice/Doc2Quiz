---
phase: 12-study-together
plan: 04
type: execute
wave: 4
depends_on: [12-02, 12-03]
files_modified:
  - src/lib/client/studyTogether.ts
  - src/lib/client/studyTogether.test.ts
  - src/components/friends/NotificationsMenu.tsx
  - src/components/friends/NotificationsMenu.test.tsx
  - src/components/friends/StudyChallengeDialog.tsx
  - src/components/friends/StudyChallengePractice.tsx
  - src/app/(app)/friends/study/[sessionId]/play/page.tsx
  - src/app/(app)/friends/study/[sessionId]/play/StudyChallengePlayClient.tsx
  - src/lib/locale/types.ts
  - src/lib/locale/messages.ts
  - src/lib/locale/messages.test.ts
  - src/lib/locale/coverage.test.ts
autonomous: true
requirements: [SOCIAL-01, SOCIAL-02, SOCIAL-03, SOCIAL-04, SOCIAL-05, SOCIAL-06, SOCIAL-07]
must_haves:
  truths:
    - "Creator sends a ready owned-quiz challenge, then starts or reopens exactly one creator attempt from a reachable entry action."
    - "Recipient safely accepts, resumes, and completes the same immutable challenge flow."
    - "Notification target-open reads only that record; explicit mark-all and accept/decline archive refresh authoritative counts."
    - "Practice and result UI only use safe server DTOs."
  artifacts:
    - path: "src/lib/client/studyTogether.ts"
      provides: "Typed challenge and notification HTTP clients"
    - path: "src/components/friends/StudyChallengePractice.tsx"
      provides: "Server-scored snapshot practice"
    - path: "src/components/friends/NotificationsMenu.test.tsx"
      provides: "Creator entry, targeted read, explicit mark-all, invite archive, and count refresh proof"
  key_links:
    - from: "StudyChallengePractice"
      to: "/api/friends/study-challenges/[sessionId]/attempt"
      via: "safe DTO fetch, progress save, completion"
      pattern: "study-challenges"
---
<objective>
Build typed client contracts and snapshot challenge create/play UI.
Purpose: Convert durable backend lifecycle into usable asynchronous study loop.
Output: Typed clients, accessible dialog/play route, locale proof.
</objective>
<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/12-study-together/12-VALIDATION.md
@.planning/phases/12-study-together/12-02-PLAN.md
@.planning/phases/12-study-together/12-03-PLAN.md
@src/components/quiz/QuizSession.tsx
@src/lib/client/friends.ts
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add typed challenge clients and localized safe states</name>
  <files>src/lib/client/studyTogether.ts, src/lib/client/studyTogether.test.ts, src/lib/locale/types.ts, src/lib/locale/messages.ts, src/lib/locale/messages.test.ts, src/lib/locale/coverage.test.ts</files>
  <behavior>
    - Client creates/lists/accepts/declines/starts-or-reopens creator attempts/resumes/loads/saves/completes challenges through HTTP only.
    - Creator start adapter returns typed attempt ID plus play href and preserves one-attempt identity across retry/reopen.
    - Notification client supports target read, explicit mark-all, invite archive after accept/decline, and authoritative list/count refresh.
    - Completion payload cannot contain claimed score/correctness; errors do not expose authorization/source details.
  </behavior>
  <action>Create readonly typed DTO/client methods matching 12-02 routes and shared social HTTP mapping, including `startOrResumeCreatorAttempt(sessionId)`, targeted notification read, explicit mark-all-read, and archive-invite methods. Add tests for ready-owned eligibility error surface, idempotent recipient accept, creator start double-submit/reopen preserving the same attempt ID and play href, notification mutation request shapes, safe practice/result DTOs, and completion payload exclusion. Add full EN/VI locale parity for challenge, notification, status, confirmation, and accessibility copy.</action>
  <verify><automated>npx vitest run src/lib/client/studyTogether.test.ts src/lib/locale/messages.test.ts src/lib/locale/coverage.test.ts</automated></verify>
  <done>Typed browser client cannot invent source authority or score data; social copy passes locale coverage.</done>
</task>
<task type="auto">
  <name>Task 2: Build ready-owned challenge dialog and snapshot play route</name>
  <files>src/components/friends/NotificationsMenu.tsx, src/components/friends/NotificationsMenu.test.tsx, src/components/friends/StudyChallengeDialog.tsx, src/components/friends/StudyChallengePractice.tsx, src/app/(app)/friends/study/[sessionId]/play/page.tsx, src/app/(app)/friends/study/[sessionId]/play/StudyChallengePlayClient.tsx</files>
  <action>Create recipient-preselected dialog with source selector limited by server-provided current-user quizzes eligible only when `status='ready'`, non-deleted, and non-empty; do not present a publish switch or friend-shared source. Implement locked defaults and deadline choices. After send, expose creator `Start challenge`/`Continue challenge` action from created-session success state and creator-visible invite/session rows; call `startOrResumeCreatorAttempt`, navigate only from returned `playHref`, disable duplicate in-flight clicks without relying on UI for idempotency, and keep action available after refresh/navigation failure. Create authenticated snapshot-only play route supporting creator and recipient safe attempts: keyboard 1–4/Enter interaction, progress save, retryable loading, selected-indices-only completion, safe result/reveal/unavailable UI. `NotificationsMenu` displays durable records, marks only clicked target read before navigation/action, exposes explicit `Mark all as read`, archives invite notification after successful accept or decline, and then invokes 12-03 authoritative reconciliation so newest page plus notification/request/message counts refresh. Opening menu performs no read mutation. Add `NotificationsMenu.test.tsx` component tests proving creator entry navigation/start/reopen behavior, target-only read, explicit mark-all, accept/decline archive only after success, count/page refresh after each mutation, and no read-on-open.</action>
  <verify><automated>npx vitest run src/components/friends/NotificationsMenu.test.tsx src/lib/client/studyTogether.test.ts && npm run typecheck && npm run lint</automated></verify>
  <done>Creator can send eligible owned-quiz challenge; recipient completes server-scored snapshot attempt without early answers.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| UI→HTTP | Source selection and answers originate in browser |
| safe DTO→practice UI | UI must not receive or derive answer keys |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-12-14 | T | challenge client | mitigate | Server eligibility and selected-indices-only requests. |
| T-12-15 | I | practice UI | mitigate | Safe DTO/result policy only; no client scoring. |
| T-12-SC | Tampering | npm installs | accept | No package installation. |
</threat_model>
<verification>Run focused client/locale tests, typecheck, lint; manual gate inspects pre-completion network payload.</verification>
<success_criteria>Ready owned-source creation and server-owned snapshot play are reachable and safe.</success_criteria>
<output>Create `.planning/phases/12-study-together/12-04-SUMMARY.md` when done.</output>
