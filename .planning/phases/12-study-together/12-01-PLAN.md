---
phase: 12-study-together
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260731100000_phase12_study_together_foundation.sql
  - scripts/verify-phase12-study-together-sql.mjs
  - src/lib/server/friends/studyTogether.ts
  - src/lib/server/friends/studyTogether.test.ts
autonomous: true
requirements: [SOCIAL-01, SOCIAL-02, SOCIAL-03, SOCIAL-04, SOCIAL-05, SOCIAL-06, SOCIAL-07, SOCIAL-08]
must_haves:
  truths:
    - "Only accepted friends can create creator-owned eligible-quiz challenges."
    - "A source edit, deletion, or ownership change cannot alter a created challenge."
    - "Accept/start and completion are idempotent, server-scored, and never leak answer keys early."
    - "Notifications persist before private realtime broadcast."
  artifacts:
    - path: "supabase/migrations/20260731100000_phase12_study_together_foundation.sql"
      provides: "Challenge, participant, attempt, durable notification/reaction schema; secure RPC state machine; private broadcast policies"
      contains: "create_study_challenge"
    - path: "src/lib/server/friends/studyTogether.ts"
      provides: "Typed RPC result validation and consolidated social error mapping"
      exports: ["createStudyChallenge", "acceptStudyChallenge", "completeStudyAttempt"]
    - path: "scripts/verify-phase12-study-together-sql.mjs"
      provides: "Repeatable two-user SQL/RPC proof runner"
  key_links:
    - from: "create_study_challenge"
      to: "public.learning_outputs and public.approved_questions"
      via: "locked creator-owned ready-output snapshot transaction"
      pattern: "learning_outputs.*created_by"
    - from: "social_notifications insert trigger"
      to: "realtime.send"
      via: "AFTER INSERT recipient topic broadcast"
      pattern: "social-notifications:"
---

<objective>
Create Phase 12 durable authority: immutable challenge snapshots, lifecycle RPCs, notification ledger, reaction durability, and typed RPC boundary.

Purpose: Security-sensitive state must exist before HTTP or UI work.
Output: One migration, repeatable SQL proof runner, typed social RPC adapter tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-study-together/CONTEXT.md
@.planning/phases/12-study-together/12-RESEARCH.md
@.planning/phases/12-study-together/12-VALIDATION.md
@supabase/migrations/20260730150000_workspace_foundation.sql
@supabase/migrations/20260730170000_friends_messages_presence.sql
@supabase/migrations/20260731001500_friend_profile_shared_quizzes.sql
@src/lib/server/friends/friends.ts

<interfaces>
From `src/lib/server/friends/friends.ts`:
- Existing RPC adapters accept `rpc(functionName, args)` and map generic unavailable states at HTTP boundary.
- Extend shared mapper; do not create parallel ad-hoc social error parsing in routes.

From `supabase/migrations/20260730170000_friends_messages_presence.sql`:
- `private.social_are_accepted_friends(uuid, uuid)` is existing friendship authority.
- Social mutating functions use `SECURITY DEFINER`, `set search_path = public`, revoked public/anon grants, authenticated-only execute grants.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create SQL proof harness and typed social RPC contracts</name>
  <files>scripts/verify-phase12-study-together-sql.mjs, src/lib/server/friends/studyTogether.ts, src/lib/server/friends/studyTogether.test.ts</files>
  <behavior>
    - Adapter maps typed RPC payloads and generic unavailable/error tokens without exposing ownership, friendship, or answer-key state.
    - Existing and new social mutations share one error mapping contract.
    - SQL runner can inspect a migration before applying fixtures and later proves concurrent accept and duplicate complete create one attempt and one semantic notification each.
  </behavior>
  <action>Write adapter types for challenge summaries, safe practice DTO, attempt/result DTO, notification DTO, status vocabulary (`online`, `recently_active`, `offline`), and named error union. Include `startStudyChallengeAttempt` for creator create-or-reopen behavior. Reuse and extend `mapSocialRpcError`/`mapSocialRouteError` conventions rather than duplicating string checks. Validate unknown RPC shapes as generic unavailable. Write Vitest tests for each typed decoder/error mapping. Create Node SQL proof runner exactly per `12-VALIDATION.md`: accept `--migration` for static migration contract checks before a database exists; require an explicit `PHASE12_TEST_DATABASE_URL` or documented local Supabase test connection for runtime SQL/RLS cases; reject any host outside approved local/disposable-test allowlist before opening a connection; generate per-run fixture UUIDs/usernames, delete only fixture-reachable rows in `finally`, and never run against production/shared developer databases. If approved runtime setup is unavailable, print `SQL_PROOF_BLOCKED: <reason>` and exit code 2 so validation records a real blocker rather than a pass.</action>
  <verify><automated>npx vitest run src/lib/server/friends/studyTogether.test.ts && node scripts/verify-phase12-study-together-sql.mjs --migration supabase/migrations/20260731100000_phase12_study_together_foundation.sql</automated></verify>
  <done>Typed adapter has exhaustive status/error tests, including creator start/reopen; SQL runner exists before migration verification and supports static migration checks plus safe runtime SQL proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create secure immutable challenge and notification SQL authority</name>
  <files>supabase/migrations/20260731100000_phase12_study_together_foundation.sql</files>
  <behavior>
    - Challenge create rejects self, nonfriends, nonowners, non-quiz, non-ready, deleted, and zero-question `learning_outputs`.
    - Create stores canonical ordered snapshot JSON, digest, creator/recipient participants, and received notification atomically; only `owned_quiz` is creatable per D-01/D-04.
    - Accept/start returns same unique recipient attempt under repeat calls; decline, resume, expired/cancelled unavailable states use deterministic tokens per D-05.
    - Attempt progress stores selected indices only; complete locks once, scores private snapshot server-side, persists metrics, applies immediate/after-both/after-deadline reveal policy, and inserts deduped notifications per D-04.
  </behavior>
  <action>Create migration per locked CONTEXT policies. Source eligibility is caller-owned `learning_outputs` with `kind = 'quiz'`, `status = 'ready'`, `deleted_at IS NULL`, and at least one `approved_questions.output_id` row; no publish gate. Add challenge/session/participant/attempt, durable notification, and durable reaction authority with checks, indexes, RLS, revoked direct grants, restricted-search-path RPCs, safe DTOs, row locks, one-attempt uniqueness, server scoring, and reveal gating. Challenge send inserts session, two participants, and recipient notification atomically but creates no creator attempt; creator attempt appears only through idempotent `start_study_challenge_attempt` when creator opens/starts. `remove_friend` cancels pending invites, blocks new social actions, and preserves active/completed snapshot sessions through normal terminal/reveal lifecycle. `block_user` cancels pending and active sessions for both participants, preserves completed result readability, and blocks future contact/challenges. Add notification trigger and notification-topic private RLS only; later Plan 12-07 owns all additional request/message/count private-topic RLS and invalidation support in a separate additive migration. Add callable idempotent reminder sweep with full dedupe uniqueness and leave it unscheduled; add no `cron.schedule` or scheduler checkpoint.</action>
  <verify><automated>node scripts/verify-phase12-study-together-sql.mjs --migration supabase/migrations/20260731100000_phase12_study_together_foundation.sql</automated></verify>
  <done>Migration exposes authenticated-only typed RPCs; no direct snapshot/attempt reads are granted; SQL proof demonstrates source immutability, foreign-source rejection, one accept attempt, server-only scoring/reveal, notification dedupe, recipient-only broadcast topic, distinct remove/block behavior, and repeat-safe reminder sweep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser→challenge API/RPC | Untrusted IDs, answer choices, deadline, message, and claimed client state |
| RPC→learning_outputs | Cross-user ownership and mutable source data |
| RPC→realtime.messages | Predictable private notification topics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-12-01 | E/T | create_study_challenge | mitigate | Lock and validate authenticated owner, accepted friend, ready/non-deleted quiz, and nonempty approved questions inside security-definer RPC. |
| T-12-02 | T/R | accept/complete RPCs | mitigate | `FOR UPDATE`, unique attempt/dedupe constraints, deterministic idempotent return DTOs, server-side scoring. |
| T-12-03 | I | snapshot/practice DTO | mitigate | Revoke raw table access; participant-safe projection strips answer keys/explanations until reveal policy allows. |
| T-12-04 | I | notification broadcast | mitigate | Private channel and recipient-bound `realtime.messages` RLS; payload contains notification ID only. |
| T-12-05 | D | malformed mutation bodies | mitigate | Route Zod bounds in downstream API plan plus SQL checks/page bounds. |
| T-12-06 | T | remove/block race | mitigate | Lock relationship during mutation; remove changes accepted friendship, block remains separate. |
| T-12-SC | Tampering | npm installs | accept | No package installation; existing dependencies only. |
</threat_model>

<verification>
Run typed adapter tests and SQL proof runner. Confirm migration contains no Phase 12 `cron.schedule`, no friend-shared creation path, and no participant DTO exposing `correctIndex` before reveal.
</verification>

<success_criteria>
Immutable creator-owned challenge authority exists; state transitions and notifications survive retries; privacy and idempotency have repeatable proof.
</success_criteria>

<output>
Create `.planning/phases/12-study-together/12-01-SUMMARY.md` when done.
</output>
