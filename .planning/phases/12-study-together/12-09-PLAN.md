---
phase: 12-study-together
plan: 09
type: execute
wave: 6
depends_on: [12-03, 12-05, 12-07]
files_modified:
  - src/app/(app)/friends/messages/[conversationId]/page.tsx
  - src/app/(app)/friends/messages/[conversationId]/ConversationPageClient.tsx
  - src/components/friends/ConversationView.tsx
  - src/components/friends/ConversationView.test.tsx
  - src/components/friends/DirectMessageDialog.tsx
  - src/lib/client/messages.test.ts
autonomous: true
requirements: [SOCIAL-10]
must_haves:
  truths:
    - "Mobile users can open a full-screen conversation route/drawer and desktop users retain current dialog presentation."
    - "Both presentations share one cursor-history and private realtime/reconnect controller."
    - "Older history, new messages, read state, and reconnect converge without duplicate messages."
  artifacts:
    - path: "src/components/friends/ConversationView.tsx"
      provides: "Shared conversation controller/view used by mobile and desktop shells"
    - path: "src/app/(app)/friends/messages/[conversationId]/page.tsx"
      provides: "Mobile full-screen conversation route"
    - path: "src/components/friends/ConversationView.test.tsx"
      provides: "History, reconnect, responsive presentation tests"
  key_links:
    - from: "ConversationView"
      to: "src/lib/client/messages.ts"
      via: "existing before cursor and realtime transport"
      pattern: "before"
---
<objective>
Make direct chat fully responsive without duplicating transport or regressing desktop UI.
Purpose: Existing desktop-only dialog is invisible below `md`; SOCIAL-10 requires durable mobile and desktop chat.
Output: Shared conversation view/controller, mobile full-screen route, preserved desktop shell, history/reconnect tests.
</objective>
<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/12-study-together/CONTEXT.md
@.planning/phases/12-study-together/12-RESEARCH.md
@.planning/phases/12-study-together/12-VALIDATION.md
@.planning/phases/12-study-together/12-07-PLAN.md
@src/components/friends/DirectMessageDialog.tsx
@src/lib/client/messages.ts
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extract shared cursor and realtime conversation view</name>
  <files>src/components/friends/ConversationView.tsx, src/components/friends/ConversationView.test.tsx, src/components/friends/DirectMessageDialog.tsx, src/lib/client/messages.test.ts</files>
  <behavior>
    - Initial history and older pages use existing `before` cursor with bounded limit and stable dedupe/order.
    - Incoming private realtime events invalidate/refetch or merge safely; SUBSCRIBED/rejoin, focus, and visible transition reconcile history/read state.
    - Send/read success follows durable API state even when broadcast misses; reconnect creates no duplicates.
    - Desktop dialog retains current placement, sizing, close behavior, and friend context.
  </behavior>
  <action>Extract conversation data/controller and accessible message body from `DirectMessageDialog` into shared `ConversationView`. Reuse existing `src/lib/client/messages.ts` cursor and realtime transport exactly; do not create a second pagination scheme, endpoint, or channel protocol. Treat events as invalidation hints and reconcile on subscribe/rejoin/focus/visibility. Preserve scroll anchor when prepending older pages, dedupe by message ID, maintain read watermark, loading/error/retry/send states, and focus behavior. Refactor desktop dialog into presentation shell around shared view without changing desktop UX. Add deterministic tests for multiple history pages, scroll-anchor contract, send/read, missed event, reconnect, focus, visibility, cleanup, and duplicate suppression.</action>
  <verify><automated>npx vitest run src/lib/client/messages.test.ts src/components/friends/ConversationView.test.tsx</automated></verify>
  <done>One tested conversation controller owns durable cursor history and private realtime reconciliation for every presentation.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add mobile full-screen conversation route</name>
  <files>src/app/(app)/friends/messages/[conversationId]/page.tsx, src/app/(app)/friends/messages/[conversationId]/ConversationPageClient.tsx, src/components/friends/ConversationView.test.tsx</files>
  <behavior>
    - Small-screen message action opens full-screen conversation with back/close, safe-area layout, fixed composer, and scrollable history.
    - Desktop direct route remains usable while normal desktop entry keeps dialog presentation.
    - Invalid/unauthorized conversation returns generic unavailable state without participant disclosure.
  </behavior>
  <action>Create authenticated conversation route and responsive page shell reusing `ConversationView`. Use full viewport/safe-area layout on mobile with accessible heading, back navigation to Messages hub, visible composer above virtual keyboard, and no `hidden md:flex` root. Preserve desktop dialog as default desktop action; route remains a valid deep link. Validate conversation ID through existing API and show generic loading/error/unavailable states. Extend component tests for mobile full-screen classes/landmarks, desktop shell preservation, keyboard focus, history loading, reconnect, and unauthorized state.</action>
  <verify><automated>npx vitest run src/components/friends/ConversationView.test.tsx && npm run typecheck && npm run lint</automated></verify>
  <done>Chat is reachable and usable at 375px and desktop while sharing identical durable transport/history behavior.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| route ID→conversation API | User-controlled conversation ID may target another pair |
| realtime event→message state | Event may be missed, duplicated, or stale |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-12-25 | I/E | conversation route/topic | mitigate | Existing participant-only API plus 12-07 private topic RLS; generic unavailable response. |
| T-12-26 | T/R | realtime/history merge | mitigate | Durable refetch, ID dedupe, cursor ordering, reconnect tests. |
| T-12-SC | Tampering | npm installs | accept | No package installation. |
</threat_model>
<verification>Run message/conversation tests, typecheck, lint; final plan verifies mobile and desktop manually.</verification>
<success_criteria>Responsive chat preserves desktop presentation and reuses tested durable cursor/realtime transport.</success_criteria>
<output>Create `.planning/phases/12-study-together/12-09-SUMMARY.md` when done.</output>
