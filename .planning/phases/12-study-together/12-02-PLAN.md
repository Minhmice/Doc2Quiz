---
phase: 12-study-together
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/app/api/friends/study-challenges/route.ts
  - src/app/api/friends/study-challenges/[sessionId]/accept/route.ts
  - src/app/api/friends/study-challenges/[sessionId]/start/route.ts
  - src/app/api/friends/study-challenges/[sessionId]/attempt/route.ts
  - src/app/api/friends/study-challenges/[sessionId]/route.ts
  - src/app/api/friends/notifications/route.ts
  - src/app/api/friends/notifications/unread-count/route.ts
  - src/app/api/friends/reactions/route.ts
  - src/app/api/friends/study-challenges/study-challenges.route.test.ts
  - src/app/api/friends/notifications/notifications.route.test.ts
autonomous: true
requirements: [SOCIAL-01, SOCIAL-02, SOCIAL-03, SOCIAL-04, SOCIAL-05, SOCIAL-06, SOCIAL-07, SOCIAL-08]
must_haves:
  truths:
    - "Authenticated users can create, receive, accept, decline, resume, and complete authorized asynchronous challenges through validated HTTP APIs."
    - "A client can retrieve safe snapshot practice and reveal-gated result DTOs only."
    - "Notification pages/counts are durable authority; reaction broadcast failure cannot report a committed mutation as failed."
  artifacts:
    - path: "src/app/api/friends/study-challenges/route.ts"
      provides: "Validated list/create challenge API"
      exports: ["GET", "POST"]
    - path: "src/app/api/friends/study-challenges/[sessionId]/attempt/route.ts"
      provides: "Safe snapshot, progress, and server-scored completion API"
      exports: ["GET", "PATCH", "POST"]
    - path: "src/app/api/friends/notifications/route.ts"
      provides: "Durable notification list/read/read-all API"
      exports: ["GET", "PATCH", "POST"]
  key_links:
    - from: "study-challenges route handlers"
      to: "src/lib/server/friends/studyTogether.ts"
      via: "typed adapter calls"
      pattern: "StudyChallenge"
    - from: "notifications unread endpoint"
      to: "notification unread-count RPC"
      via: "server-derived count"
      pattern: "unread"
---

<objective>
Expose secure challenge and notification authority as validated typed HTTP APIs, while correcting social action delivery semantics.

Purpose: Browser clients need stable safe contracts, not direct Supabase writes.
Output: Route handlers and contract tests for challenge lifecycle, durable notifications, and reactions.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-study-together/12-01-PLAN.md
@.planning/phases/12-study-together/12-VALIDATION.md
@src/lib/api/requireApiUser.ts
@src/lib/server/friends/studyTogether.ts
@src/app/api/friends/messages/[conversationId]/route.ts
@src/app/api/friends/reactions/route.ts
@src/app/api/friends/route.ts
@src/app/api/friends/friends.route.test.ts

<interfaces>
From `src/app/api/friends/messages/[conversationId]/route.ts`:
- Auth gate: `const auth = await requireApiUser(); if ("error" in auth) return auth.error`.
- Route validates JSON and UUIDs with Zod and returns `{ data }` on success, `{ error }` on failure.

From `src/lib/server/friends/studyTogether.ts` created in 12-01:
- Use typed adapter exports for every RPC call; routes must not parse raw RPC `jsonb` inline.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build validated challenge lifecycle and safe practice routes</name>
  <files>src/app/api/friends/study-challenges/route.ts, src/app/api/friends/study-challenges/[sessionId]/route.ts, src/app/api/friends/study-challenges/[sessionId]/accept/route.ts, src/app/api/friends/study-challenges/[sessionId]/start/route.ts, src/app/api/friends/study-challenges/[sessionId]/attempt/route.ts, src/app/api/friends/study-challenges/study-challenges.route.test.ts</files>
  <behavior>
    - Create accepts recipient UUID, eligible owned output UUID, practice/score mode, validated deadline/custom deadline, optional bounded message, and defaults score/no deadline/one attempt/after-both-complete.
    - Recipient accept returns same attempt and play route identity for duplicate POST; decline/resume/expired/cancelled return safe actionable unavailable data.
    - Creator start/reopen POST returns the same creator attempt and play route identity across double-click, retry, refresh, and existing in-progress state; challenge creation alone creates no creator attempt.
    - Practice GET omits answers/explanations before allowed reveal; PATCH saves bounded progress/selected indices; POST completion never accepts client score and returns server-scored result.
  </behavior>
  <action>Create collection route GET for caller-visible invites/sessions and POST create. Enforce locked source eligibility only through 12-01 RPC: owner=current user + `status='ready'` + `deleted_at IS NULL` + at least one approved question; never add a publish field or publish check.  create session detail GET for status/result-safe DTO, recipient accept POST route, creator-only `POST /api/friends/study-challenges/[sessionId]/start`, and attempt GET/PATCH/POST routes. Creator start route invokes typed `startStudyChallengeAttempt`, returns `{ attemptId, playHref }`, creates-or-reopens only caller's creator attempt, and preserves generic unavailable responses for recipient/cross-user/terminal states. Authenticate before parsing/adapter calls. Use Zod UUID/enums/max lengths/page limits/deadline bounds and call 12-01 typed adapter only. Preserve generic unavailable response for cross-user authorization failure; map invalid bodies to 400 and typed conflict/lifecycle errors to documented statuses without exposing source/friend existence. Return snapshot practice route data after acceptance only and never original output IDs as client authority. Add route unit tests mocking adapter/auth for authorization ordering, validation, default fields, duplicate recipient accept, creator start double-submit returning one attempt ID/play href, creator reopen after refresh/navigation failure returning that same attempt, recipient/cross-user start denial, resume/unavailable, withheld answer keys, server-only score, and reveal-gated response.</action>
  <verify><automated>npx vitest run src/app/api/friends/study-challenges/study-challenges.route.test.ts</automated></verify>
  <done>All challenge actions have authenticated Zod-validated routes; browser receives only safe DTOs and duplicate/retry behavior is deterministic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build durable notification API and repair social delivery endpoints</name>
  <files>src/app/api/friends/notifications/route.ts, src/app/api/friends/notifications/unread-count/route.ts, src/app/api/friends/reactions/route.ts, src/app/api/friends/notifications/notifications.route.test.ts</files>
  <behavior>
    - Notification list is paginated/bounded and supports target read, mark-all-read, archive-on-invite action, plus unread count without marking records read on list/menu open.
    - Notification list/count is durable authority and reaction HTTP success follows durable mutation even if post-commit realtime acceleration fails.
    - Request/message count endpoints, avatar data, and remove-friend route remain owned by later dedicated plans.
  </behavior>
  <action>Create notification GET/PATCH/POST contract around 12-01 RPCs: list newest page, mark a targeted notification read, mark all read, and archive invitation actions; add unread count GET. Modify reactions route so durable RPC success returns success regardless of acceleration delivery outcome; remove post-commit 503 behavior. Keep request/message counts, avatar proxy/cache behavior, overview shaping, and remove-friend endpoint out of this plan: 12-03 and 12-05 own them. Test notification and reaction contracts only.</action>
  <verify><automated>npx vitest run src/app/api/friends/notifications/notifications.route.test.ts</automated></verify>
  <done>Notifications are queryable/readable/reconcileable from database authority; broadcast failures do not reverse committed reactions.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| client→route | UUIDs, deadlines, answer selections, messages, pagination are attacker-controlled |
| route→RPC adapter | Routes must not reinterpret raw security-sensitive SQL JSON |
| route→storage URL | Avatar path must never leave server response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-12-07 | T/D | challenge/notification routes | mitigate | Require auth first; Zod UUID/enums/bounds; bounded notification page sizes. |
| T-12-08 | I | snapshot/result HTTP DTO | mitigate | Typed server adapter returns participant-safe projections only; tests reject answer keys pre-reveal. |
| T-12-09 | I | avatar handling | mitigate | Convert validated storage path to stable cached/proxied URL server-side; never return path. |
| T-12-10 | R/T | reaction endpoint | mitigate | Mutation response reflects durable RPC, not best-effort broadcast; route test covers failed acceleration. |
| T-12-SC | Tampering | npm installs | accept | No package installation. |
</threat_model>

<verification>
Run focused route tests, then `npm run typecheck`. Inspect API fixtures to confirm no test payload/result exposes `correctIndex` before reveal and no route calls raw table queries from browser data.
</verification>

<success_criteria>
All Phase 12 browser mutations cross authenticated validated APIs, durable notification API supports reconciliation, and old broadcast-failure inconsistency is removed.
</success_criteria>

<output>
Create `.planning/phases/12-study-together/12-02-SUMMARY.md` when done.
</output>
