---
phase: 12-study-together
plan: 06
type: execute
wave: 7
depends_on: [12-01, 12-02, 12-03, 12-04, 12-05, 12-07, 12-08, 12-09]
files_modified:
  - src/lib/server/friends/studyTogether.test.ts
  - src/app/api/friends/study-challenges/study-challenges.route.test.ts
  - src/app/api/friends/notifications/notifications.route.test.ts
  - src/lib/client/studyTogether.test.ts
  - src/lib/client/socialCounts.test.ts
  - src/lib/client/messages.test.ts
  - src/components/friends/FriendsHub.test.tsx
  - src/components/friends/ConversationView.test.tsx
  - .planning/phases/12-study-together/12-VALIDATION.md
autonomous: false
requirements: [SOCIAL-01, SOCIAL-02, SOCIAL-03, SOCIAL-04, SOCIAL-05, SOCIAL-06, SOCIAL-07, SOCIAL-08, SOCIAL-09, SOCIAL-10]
must_haves:
  truths:
    - "Every SOCIAL-01–10 requirement has validation evidence or an explicit deployment-owned database check."
    - "Two accounts prove desktop/mobile/reconnect behavior and scheduler decision."
  artifacts:
    - path: ".planning/phases/12-study-together/12-VALIDATION.md"
      provides: "Pre-existing contract updated with executed evidence and deployment-owned database checks"
      contains: "SOCIAL-01"
---
<objective>
Prove Phase 12 against pre-existing validation contract and make scheduler decision.
Purpose: Durable cross-user state needs race, privacy, realtime, and responsive proof.
Output: Regression evidence and recorded two-account/scheduler results.
</objective>
<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/12-study-together/12-VALIDATION.md
@.planning/phases/12-study-together/12-RESEARCH.md
@.planning/phases/12-study-together/12-01-PLAN.md
@.planning/phases/12-study-together/12-05-PLAN.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Complete validation-contract regression proof</name>
  <files>src/lib/server/friends/studyTogether.test.ts, src/app/api/friends/study-challenges/study-challenges.route.test.ts, src/app/api/friends/notifications/notifications.route.test.ts, src/lib/client/studyTogether.test.ts, src/lib/client/socialCounts.test.ts, src/lib/client/messages.test.ts, src/components/friends/FriendsHub.test.tsx, src/components/friends/ConversationView.test.tsx, .planning/phases/12-study-together/12-VALIDATION.md</files>
  <behavior>
    - Self-hosted Supabase owns migration and SQL/RLS validation; repository verification does not require a runtime database target.
    - Focused tests prove ready/non-deleted/non-empty source eligibility, count reconciliation triggers, no answer/avatar leakage, paginated responsive hub behavior, and mobile/desktop chat history/reconnect behavior.
  </behavior>
  <action>Run focused suites. Add only missing regression proofs required by `12-VALIDATION.md`; do not change product behavior without failing evidence. Record exact commands and pass/fail evidence by updating existing `12-VALIDATION.md`; never recreate it.</action>
  <verify><automated>npm test && npm run typecheck && npm run lint && npm run build</automated></verify>
  <done>Validation artifact records evidence for every requirement, user-approved responsive verification, and deployment-owned SQL/RLS validation.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify two-account responsive study loop</name>
  <files>.planning/phases/12-study-together/12-VALIDATION.md</files>
  <action>After Task 1, execute manual matrix in `12-VALIDATION.md` and append outcomes. Confirm reminder sweep is callable, idempotent, and unscheduled; no scheduler capability decision or `cron.schedule` belongs to Phase 12.</action>
  <verify><automated>npm test && npm run typecheck && npm run lint && npm run build</automated></verify>
  <done>Two-account desktop/mobile/reconnect outcomes and callable-unscheduled reminder proof are recorded in existing validation artifact.</done>
  <what-built>Complete challenge loop, separate durable count reconciliation, responsive friends/chat, and callable deduped reminder sweep.</what-built>
  <how-to-verify>
    1. Create from creator-owned ready, non-deleted, non-empty quiz; verify foreign, friend-shared, unready, deleted, empty sources unavailable.
    2. Accept twice; resume after refresh/navigation failure; inspect network for no pre-reveal answer data.
    3. Complete both attempts; verify reveal policy and durable result-ready notification.
    4. Trigger/miss/rejoin notification, request, and message private events; then focus and return visible. Confirm topbar refetches and converges all three counts.
    5. At 375px and desktop, verify hub destinations, cursor message history, and separate remove/block actions.
    6. Invoke reminder sweep twice and confirm one deduped expiring notification; confirm no schedule was installed.
  </how-to-verify>
  <resume-signal>Type "approved" or describe failures.</resume-signal>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| tests→database | Repository tests must not require a database target |
| scheduler→notifications | Reminder must remain deduped and opt-in |
| manual browser→Realtime | Private delivery must reconcile missed events |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-12-19 | T/R | scheduler | mitigate | Deduped callable sweep; human capability confirmation before scheduling. |
| T-12-20 | I | regression proof | mitigate | Two-account RLS/safe DTO/count reconciliation tests. |
| T-12-SC | Tampering | npm installs | accept | No package installation. |
</threat_model>
<verification>Automated full gate plus manual matrix from `12-VALIDATION.md`.</verification>
<success_criteria>SOCIAL-01–10 evidence meets validation contract without unsafe database test execution.</success_criteria>
<output>Create `.planning/phases/12-study-together/12-06-SUMMARY.md` when done; update existing `12-VALIDATION.md`.</output>
