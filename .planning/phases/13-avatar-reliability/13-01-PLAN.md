---
phase: 13-avatar-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260731223000_avatar_storage_contract.sql
  - supabase/tests/avatar_storage_rls.sql
  - src/lib/profile/profileValidation.ts
  - src/lib/profile/profileValidation.test.ts
  - src/app/api/profile/route.ts
requirements: [AVATAR-01, AVATAR-02, AVATAR-03]
autonomous: false
must_haves:
  truths:
    - Authenticated owner can insert, overwrite, read, and remove only own canonical avatar object.
    - One user cannot read/write another user avatar by direct Storage API.
    - Profile API accepts only canonical own-avatar paths and returns signed URL after write/reload.
---
<objective>
Replace fragmented avatar Storage policy changes with one explicit, tested private contract.

Purpose: deployed RLS must exactly match browser upload path and `upsert` behavior.
Output: idempotent migration, Storage RLS SQL tests, shared validation/API contract.
</objective>
<context>
@.planning/phases/13-avatar-reliability/13-CONTEXT.md
@supabase/migrations/20260725120000_v21_baseline.sql
@supabase/migrations/20260731010000_friend_profile_avatar_access.sql
@supabase/migrations/20260731180000_fix_profile_avatar_storage_rls.sql
@src/components/profile/ProfilePageClient.tsx
@src/app/api/profile/route.ts
</context>
<tasks>
<task type="checkpoint:decision">
<name>Task 1: Confirm target Supabase migration history and Storage policy state</name>
<action>Before modifying remote policy state, inspect target project migration history and query `pg_policies` for `storage.objects` / bucket `doc2quiz`. Record whether baseline owner policies, all avatar migrations, and any manually created policies already exist. If target is unlinked, link CLI to the explicit project or require user-provided Dashboard SQL result. Do not run destructive deletes blindly. This is a one-way DB authorization change.</action>
<verify><automated>supabase migration list && policy query against target project</automated></verify>
<done>Target state is known; migration can safely reconcile every policy name found.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Create a single idempotent owner-avatar Storage contract</name>
<files>supabase/migrations/20260731223000_avatar_storage_contract.sql, supabase/tests/avatar_storage_rls.sql</files>
<action>Create one additive, idempotent migration that defines canonical path helper and reconciles all known baseline/previous avatar policy names. Keep bucket private and RLS enabled. Remove only conflicting `doc2quiz` avatar/owner policies after confirming their exact predicates and replace them with narrow `SELECT`, `INSERT`, `UPDATE`, `DELETE` policies for authenticated users where bucket is `doc2quiz` and object path exactly equals their own `<uuid>/profile/avatar.(png|jpg|jpeg|webp|gif)`. Ensure `upsert: true` is authorized for both insert and update. No policy may authorize arbitrary folder/object access. SQL tests use two JWT identities and prove owner first upload, overwrite, select, delete; cross-user read/write/delete denial; noncanonical extension/path denial; bucket privacy; friend-related policy cannot grant direct Storage access.</action>
<verify><automated>supabase db reset && supabase test db</automated></verify>
<done>Storage RLS precisely authorizes current avatar client lifecycle and denies all other objects/users.</done>
</task>
<task type="auto" tdd="true">
<name>Task 3: Consolidate validation and profile signed URL behavior</name>
<files>src/lib/profile/profileValidation.ts, src/lib/profile/profileValidation.test.ts, src/app/api/profile/route.ts</files>
<action>Keep one exported MIME allowlist, canonical extension map, 2 MiB cap, canonical path builder/parser. Use it in route validation rather than duplicated regex. API GET must return null only when no avatar is stored; when stored signed URL generation fails, return structured `avatar_unavailable` status without exposing bucket path. PATCH must validate only own canonical path then preserve normal own-profile RLS. Tests cover GIF, JPEG normalization, bad MIME/size/path, stored avatar signed success, signed failure, and no raw `avatar_path` in browser-safe response where not needed.</action>
<verify><automated>npm run test -- src/lib/profile/profileValidation.test.ts src/app/api/profile --run</automated></verify>
<done>Server and client share one exact avatar input/path contract with observable signed URL failures.</done>
</task>
</tasks>
