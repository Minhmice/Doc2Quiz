---
phase: 13-avatar-reliability
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - src/components/profile/ProfilePageClient.tsx
  - src/components/profile/ProfilePageClient.test.tsx
  - src/components/layout/AccountMenu.tsx
  - src/components/profile/FriendProfilePageClient.tsx
requirements: [AVATAR-04, AVATAR-05]
autonomous: false
must_haves:
  truths:
    - Owner sees uploaded static or animated avatar immediately and after reload.
    - Failure tells user whether upload, profile save, or private preview failed.
    - Sidebar/account and accepted-friend profile render signed avatar safely with fallback.
---
<objective>
Make avatar UI reliable, observable, and consistent across owner/sidebar/friend views.

Purpose: prove real user outcome rather than only API/migration correctness.
Output: hardened upload client, tests, visual E2E checklist.
</objective>
<context>
@.planning/phases/13-avatar-reliability/13-CONTEXT.md
@.planning/phases/13-avatar-reliability/13-01-PLAN.md
@src/components/profile/ProfilePageClient.tsx
@src/components/layout/AccountMenu.tsx
@src/components/profile/FriendProfilePageClient.tsx
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Harden avatar upload UI and image rendering</name>
<files>src/components/profile/ProfilePageClient.tsx, src/components/profile/ProfilePageClient.test.tsx, src/components/layout/AccountMenu.tsx, src/components/profile/FriendProfilePageClient.tsx</files>
<action>Use shared validation/path utility. Make upload sequence explicit: authenticated user → Storage upload → profile PATCH → signed preview refresh. Report stage-specific safe errors and retain previous avatar on failure. Render static and GIF signed URLs via plain `<img>` with `onError` fallback, non-empty meaningful alt where avatar communicates identity, default avatar otherwise. Sidebar/account must consume server-signed avatar URL from existing profile DTO rather than stale local fallback. Friend avatar comes only from friend-authorized API signed URL. Avoid raw path leaks, blob URL persistence, image optimization that freezes GIF, and client-only optimistic success before server confirmation. Tests mock every stage and assert no signed URL null silently replaces avatar, GIF accept behavior, retry after failure, and fallback rendering.</action>
<verify><automated>npm run test -- src/components/profile/ProfilePageClient.test.tsx --run</automated></verify>
<done>Avatar interaction presents accurate state and all visible identity locations use same authorized signed URL result.</done>
</task>
<task type="checkpoint:human-verify">
<name>Task 2: Verify target Storage runtime with two accounts</name>
<action>Apply migration to target Supabase through tracked migration deployment. With account A, upload ≤2 MiB PNG, JPEG, WebP, GIF; replace one image; hard reload; verify profile and sidebar display. With account B, attempt direct Storage read/write/delete of A canonical path and confirm denial. Make A/B accepted friends; confirm friend profile displays signed avatar; remove friendship/block and confirm avatar URL is unavailable. Inspect Network: Storage upload and profile PATCH succeed, GET returns signed URL, no raw bucket path. Confirm failures show stage-specific UI copy. Record target project migration ID and SQL test result.</action>
<verify><automated>npm run typecheck && npm run build</automated></verify>
<done>Cloud runtime—not only local tests—proves private owner avatar lifecycle and friend authorization.</done>
</task>
</tasks>
