---
phase: 06-bilingual-en-vi-language-selector-and-reusable-contextual-sl
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/components/locale/LocaleProvider.tsx
  - src/components/locale/LanguageSelector.tsx
  - src/components/locale/LocaleProvider.test.tsx
  - src/components/layout/AppProviders.tsx
  - src/components/layout/AppTopBar.tsx
  - src/components/layout/CommandPalette.tsx
  - src/app/(app)/settings/page.tsx
autonomous: true
requirements:
  - LOCALE-01
  - LOCALE-02
  - LOCALE-03
  - LOCALE-04
  - LOCALE-05
  - SLANG-04
user_setup: []
must_haves:
  truths:
    - "User can switch explicitly between English and Vietnamese from both account menu and Settings without reloading."
    - "Chosen locale persists across reloads and synchronizes across tabs."
    - "Server and first client render remain English; stored locale applies after hydration without mismatch and updates document language."
    - "App-shell navigation, search, settings, and accessibility labels use literal localized copy with zero slang in account/auth/accessibility text."
  artifacts:
    - path: src/components/locale/LocaleProvider.tsx
      provides: "Hydration-safe locale context, typed messages, slang event helper, persistence, and document.lang sync"
      exports: ["LocaleProvider", "useLocale"]
    - path: src/components/locale/LanguageSelector.tsx
      provides: "Reusable explicit EN/VI selector preserving existing control geometry"
      exports: ["LanguageSelector"]
    - path: src/components/locale/LocaleProvider.test.tsx
      provides: "Provider hydration, persistence, cross-tab, and stable slang-event tests"
    - path: src/components/layout/AppProviders.tsx
      provides: "App-level locale provider wiring without deferred palette regression"
  key_links:
    - from: src/components/locale/LocaleProvider.tsx
      to: src/lib/locale/localeStorage.ts
      via: "post-mount validated read/write and storage event"
      pattern: "readLocale|writeLocale"
    - from: src/components/locale/LocaleProvider.tsx
      to: document.documentElement.lang
      via: "effect after locale hydration/change"
      pattern: "documentElement.lang"
    - from: src/components/layout/AppProviders.tsx
      to: src/components/locale/LocaleProvider.tsx
      via: "provider wraps AppShell and DeferredCommandPalette while preserving mount order"
      pattern: "LocaleProvider"
    - from: src/components/locale/LanguageSelector.tsx
      to: src/components/locale/LocaleProvider.tsx
      via: "useLocale setLocale contract"
      pattern: "setLocale"
---

<objective>
Add hydration-safe app locale state and visible EN/VI controls in account/settings surfaces, then localize shared shell navigation.

Purpose: Make bilingual preference user-controlled and persistent without route changes, SSR mismatch, account-copy slang, or shell layout regression.
Output: Locale provider/hook, reusable selector, provider tests, account/settings integration, and localized top bar/command palette.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-bilingual-en-vi-language-selector-and-reusable-contextual-sl/06-RESEARCH.md
@TASTE.md
@src/components/layout/AppProviders.tsx
@src/components/profile/DisplayNameProvider.tsx
@src/components/layout/AppTopBar.tsx
@src/components/layout/CommandPalette.tsx
@src/app/(app)/settings/page.tsx
@src/app/layout.tsx
@.planning/phases/06-bilingual-en-vi-language-selector-and-reusable-contextual-sl/06-01-SUMMARY.md

<interfaces>
From Wave 1:
- Locale = "en" | "vi"; DEFAULT_LOCALE = "en".
- messages[locale] returns typed literal catalog.
- readLocale/writeLocale and LOCALE_STORAGE_KEY validate browser persistence.
- createSlangRotator or equivalent exposes event-driven getRandomSlang(context, locale) with previous history scoped by locale/context.

Existing provider precedent:
- DisplayNameProvider initializes stable empty state, reads localStorage in useEffect, and listens for storage events.
- AppProviders must keep AppShell before DeferredCommandPalette because Base UI useId order is sensitive.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement hydration-safe locale provider contract</name>
  <files>src/components/locale/LocaleProvider.tsx, src/components/locale/LocaleProvider.test.tsx, src/components/layout/AppProviders.tsx</files>
  <behavior>
    - Server/initial render returns English catalog and no random slang selection.
    - Mount reads valid stored vi in an effect, then updates visible context and html lang.
    - Invalid stored value remains en; storage event updates locale across tabs.
    - setLocale writes validated preference and updates html lang without reload.
    - getRandomSlang remains stable across unrelated rerenders and advances only when called for a semantic event.
  </behavior>
  <action>
    Add component tests first. Because Vitest defaults to Node and no DOM package may be installed, test provider behavior using React server rendering plus dependency-injected storage/document boundaries or pure exported provider state helpers; do not add dependencies. Ensure representative initial markup from provider stays English and matches server output, then verify post-hydration transition logic through testable effects/boundaries. Never use localStorage in a `useState` initializer, never call Math.random during render, and do not add `suppressHydrationWarning` to localized content.

    Implement LocaleProvider with initial `en`, `hydrated` state, typed `messages`, `locale`, `setLocale`, and event-driven `getRandomSlang(context)` API. Read validated storage in mount effect; update `document.documentElement.lang` after preference application and every locale change; listen to only LOCALE_STORAGE_KEY storage events; reset or isolate slang history by locale. Memoize context values and keep random phrase selection out of render.

    Wrap existing AppProviders content with LocaleProvider while preserving the current DisplayNameProvider, AppShell, RoutePrefetch, and deferred `ssr:false` CommandPalette ordering/useId safeguard. Do not change root routes or cookies; static `<html lang="en">` remains server fallback and provider upgrades it after hydration.
  </action>
  <verify>
    <automated>npm test -- src/components/locale/LocaleProvider.test.tsx --run &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Provider starts deterministically in English, applies validated persisted locale after hydration, syncs document.lang/tabs, and exposes event-driven slang without random render behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add selectors and localize shared app shell</name>
  <files>src/components/locale/LanguageSelector.tsx, src/components/layout/AppTopBar.tsx, src/components/layout/CommandPalette.tsx, src/app/(app)/settings/page.tsx</files>
  <action>
    Build reusable `LanguageSelector` using existing Base UI/shadcn controls and LocaleProvider. Show explicit English/EN and Tiếng Việt/VI choices, visible selected state, keyboard operation, literal localized label, and no slang. Preserve current minimum touch targets and width; compact mode fits existing account dropdown, full labeled mode fits Settings card.

    Add compact selector inside AppTopBar account dropdown without changing avatar trigger, top-bar height, search geometry, settings navigation, logout behavior, or study metadata loading. Localize top-bar search placeholders/ARIA labels, new-set labels, account menu, settings, logout, loading/unavailable shell copy, and static source prefix through typed messages. Keep study-set title/subtitle/source filename untouched. Account, auth, logout, error, and accessibility strings remain literal with zero slang.

    Add full selector in settings Application section while preserving export/dev panels and card layout. Convert page to a client component or isolate selector/client copy in a child as needed; no server-dependent preference read. Localize settings chrome touched by this plan.

    Localize CommandPalette headings, input, empty state, navigation items, current-set actions, and shortcut labels from provider. Preserve deferred loading, keyboard shortcut, routing, content-kind lookup, and Base UI mount behavior. Use no slang for navigation action labels; optional slang support belongs to later secondary-label integrations, never required wayfinding.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test -- src/components/locale --run</automated>
  </verify>
  <done>Account dropdown and Settings both switch EN/VI immediately; top bar and command palette follow locale while routes, keyboard behavior, layout, user content, and critical copy remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server markup→hydrated client | English SSR markup transitions to browser preference |
| localStorage/storage event→provider | Browser-controlled locale value updates global UI state |
| provider→account/navigation UI | Locale and optional personality API reach critical wayfinding surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | LocaleProvider storage events | mitigate | Reuse closed locale validator; ignore unrelated keys and invalid values |
| T-06-06 | Denial of service | Provider rerender/random rotation | mitigate | Memoized context and semantic-event selection; no random selection in render/effect loops |
| T-06-07 | Spoofing | Navigation/account copy | mitigate | Literal typed catalog only; user/generated content never interpolated as UI labels and slang API unused here |
| T-06-08 | Elevation of privilege | Locale selector | accept | Presentation-only state; no auth/session/access-control mutation |
| T-06-SC | Tampering | package supply chain | accept | No package installs in Phase 6 |
</threat_model>

<verification>
- `npm test -- src/components/locale/LocaleProvider.test.tsx --run`
- `npm run typecheck`
- `npm run build`
- Switch account menu and Settings selectors without reload; reload and second tab retain preference; console has no hydration warning.
</verification>

<success_criteria>
- User-visible selector exists in both account menu and Settings.
- Valid locale persists; invalid locale falls back safely; document language follows selected locale.
- Initial SSR/client text is deterministic English and random slang never executes during render.
- Shared shell changes language without altering routes, functionality, keyboard support, or layout.
</success_criteria>

<output>
Create `.planning/phases/06-bilingual-en-vi-language-selector-and-reusable-contextual-sl/06-02-SUMMARY.md` when done.
</output>
