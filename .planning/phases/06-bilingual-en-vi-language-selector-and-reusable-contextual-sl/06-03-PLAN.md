---
phase: 06-bilingual-en-vi-language-selector-and-reusable-contextual-sl
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - src/components/locale/LocalizedCopy.tsx
  - src/components/locale/LocalizedCopy.test.tsx
  - src/components/processing/conversion-progress.tsx
  - src/components/edit/new/import/IngestProgressCard.tsx
  - src/components/canonical/CanonicalizeProgressCard.tsx
  - src/components/quiz/QuizGenerateProgressCard.tsx
  - src/components/flashcards/FlashcardGenerateProgressCard.tsx
autonomous: true
requirements:
  - LOCALE-01
  - LOCALE-04
  - SLANG-01
  - SLANG-02
  - SLANG-03
  - SLANG-04
user_setup: []
must_haves:
  truths:
    - "Shared progress shells show localized useful status first and optional contextual slang second."
    - "Upload, conversion, canonicalization, quiz generation, and flashcard generation select slang only on semantic state changes, never render repaint."
    - "Errors and ARIA status remain literal and stable; slang is hidden for serious/error states."
    - "Existing progress geometry, steps, retry behavior, and reduced-motion behavior remain unchanged."
  artifacts:
    - path: src/components/locale/LocalizedCopy.tsx
      provides: "Reusable literal-primary plus optional event-selected slang composition"
      exports: ["LocalizedSlangLine", "useEventSlang"]
    - path: src/components/processing/conversion-progress.tsx
      provides: "Shared optional slang slot separate from ARIA-live literal status"
    - path: src/components/locale/LocalizedCopy.test.tsx
      provides: "Representative hydration-safe, stable-rerender, locale/context UI tests"
  key_links:
    - from: src/components/locale/LocalizedCopy.tsx
      to: src/components/locale/LocaleProvider.tsx
      via: "typed messages and event-driven getRandomSlang"
      pattern: "useLocale"
    - from: src/components/processing/conversion-progress.tsx
      to: src/components/locale/LocalizedCopy.tsx
      via: "optional visual slang node excluded from aria-live literal title"
      pattern: "slang"
    - from: src/components/quiz/QuizGenerateProgressCard.tsx
      to: src/lib/locale/slang.ts
      via: "quizGeneration context selected on state transition"
      pattern: "quizGeneration"
---

<objective>
Create reusable clear-copy-plus-slang UI composition and integrate it through shared upload/conversion/generation progress surfaces.

Purpose: Deliver high-leverage bilingual progress coverage with natural rotation while protecting comprehension, accessibility, hydration, and existing workflows.
Output: Reusable localized slang component/hook, integration tests, localized shared shell, and four pipeline progress cards.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-bilingual-en-vi-language-selector-and-reusable-contextual-sl/06-RESEARCH.md
@TASTE.md
@src/components/processing/conversion-progress.tsx
@src/components/edit/new/import/IngestProgressCard.tsx
@src/components/canonical/CanonicalizeProgressCard.tsx
@src/components/quiz/QuizGenerateProgressCard.tsx
@src/components/flashcards/FlashcardGenerateProgressCard.tsx
@.planning/phases/06-bilingual-en-vi-language-selector-and-reusable-contextual-sl/06-01-SUMMARY.md

<interfaces>
Wave 1 provides typed messages, SlangContext, Locale, and event-driven no-repeat slang rotation. Components must request phrases only after hydration and only when semantic event keys change.

Existing ConversionProgressShellProps: title, subtitle?, meta?, steps?, error?, showBar?, footer?, className?. Extend compatibly with optional visual slang support; literal title keeps aria-live and progress ARIA labels remain localized literal copy.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reusable stable contextual-copy composition</name>
  <files>src/components/locale/LocalizedCopy.tsx, src/components/locale/LocalizedCopy.test.tsx, src/components/processing/conversion-progress.tsx</files>
  <behavior>
    - Initial/server output contains literal English status and no randomly selected phrase.
    - Slang appears only after hydration for allowed context and stays unchanged across unrelated rerenders.
    - Changing event key selects next phrase and prevents immediate repeat; changing locale uses matching locale bank.
    - disabled/error/forbidden use renders no slang while literal copy and accessible status remain present.
  </behavior>
  <action>
    Write representative component tests first using existing test capabilities and dependency-injected provider boundaries; add no DOM dependency. Implement `useEventSlang(context, eventKey, enabled)` or equivalent that selects in an effect after hydration and only when locale/context/event key changes. Keep selected line in state; no Math.random call in render, module scope, or SSR. Add `LocalizedSlangLine` as plain text with `aria-hidden="true"` unless product semantics require otherwise; stable literal status remains assistive source of truth.

    Extend ConversionProgressShell props with optional `slang`/supporting personality slot. Render it visually below useful subtitle/title without changing card geometry, progress bar, step order, footer, aria-busy, or reduced-motion classes. Keep title as sole `aria-live` content; localize progressbar and step-list ARIA labels through typed literal messages. Suppress slang automatically when `error` is true.
  </action>
  <verify>
    <automated>npm test -- src/components/locale/LocalizedCopy.test.tsx --run &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Reusable helper composes literal status plus optional post-hydration slang, stays stable across rerenders, rotates on events, and keeps assistive/error copy literal.</done>
</task>

<task type="auto">
  <name>Task 2: Localize and contextualize pipeline progress cards</name>
  <files>src/components/edit/new/import/IngestProgressCard.tsx, src/components/canonical/CanonicalizeProgressCard.tsx, src/components/quiz/QuizGenerateProgressCard.tsx, src/components/flashcards/FlashcardGenerateProgressCard.tsx</files>
  <action>
    Replace hard-coded progress UI chrome with typed EN/VI messages while preserving state unions, props, count calculations, retry callbacks, technical detail values, and workflow behavior. Use context mapping: ingest validation/upload uses upload; converting uses conversion; canonicalization uses conversion; quiz generation uses quizGeneration; flashcard generation uses flashcardGeneration; successful generation may use success. Build interpolation through catalog functions for step/count/format labels.

    Select one slang line per semantic step/status transition via reusable helper and pass it into ConversionProgressShell. For long-running progress, rotate only when actual step/status/event key changes; do not add timer unless existing component receives a genuine phase transition. Keep literal useful title/subtitle first. Error state, errorMessage, warning about thin content, retry action text, technical details, aria-live, and progress labels remain literal and receive zero slang. Never translate dynamic server error detail or detected format fallback values beyond known format-label map.

    Preserve component signatures where possible and avoid layout/style changes. Vietnamese strings must be concise enough for current cards; wrapping only where existing text already wraps.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test -- src/components/locale src/lib/locale --run</automated>
  </verify>
  <done>All shared ingest/canonical/quiz/flashcard progress states switch EN/VI, show safe event-stable supporting slang only in allowed states, and preserve current state machines and layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| locale/slang provider→shared progress shell | Optional personality copy enters repeated high-visibility status UI |
| server error detail→localized progress card | Dynamic technical message crosses into translated UI chrome |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Spoofing | progress status | mitigate | Literal typed status remains primary/aria-live; slang is aria-hidden secondary copy |
| T-06-10 | Denial of service | event slang effects | mitigate | Effect keyed to semantic state; no timer/render loop or retry-loop RNG |
| T-06-11 | Tampering | server error detail | mitigate | Preserve detail verbatim as data; localize stable fallback/recovery copy only and disable slang on errors |
| T-06-SC | Tampering | package supply chain | accept | No package installs in Phase 6 |
</threat_model>

<verification>
- `npm test -- src/components/locale src/lib/locale --run`
- `npm run typecheck`
- Render every progress state in EN then VI; literal status remains first, slang stable per state, errors show none.
</verification>

<success_criteria>
- Reusable helper prevents SSR randomness and rerender churn.
- Upload, conversion, canonicalization, and both generation flows have typed bilingual progress copy.
- Slang supports comprehension and never enters error/recovery/accessibility copy.
- Existing progress props, transitions, controls, geometry, and behavior remain intact.
</success_criteria>

<output>
Create `.planning/phases/06-bilingual-en-vi-language-selector-and-reusable-contextual-sl/06-03-SUMMARY.md` when done.
</output>
