---
phase: 06-bilingual-en-vi-language-selector-and-reusable-contextual-sl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/locale/types.ts
  - src/lib/locale/messages.ts
  - src/lib/locale/slang.ts
  - src/lib/locale/selectSlang.ts
  - src/lib/locale/localeStorage.ts
  - src/lib/locale/messages.test.ts
  - src/lib/locale/slang.test.ts
  - src/lib/locale/selectSlang.test.ts
  - src/lib/locale/localeStorage.test.ts
autonomous: true
requirements:
  - LOCALE-01
  - LOCALE-02
  - SLANG-01
  - SLANG-02
  - SLANG-03
  - SLANG-04
user_setup: []
must_haves:
  truths:
    - "English and Vietnamese product catalogs expose identical typed keys for listed product contexts."
    - "Every required slang context has curated English and Vietnamese entries while forbidden copy classes remain unavailable to slang consumers."
    - "Repeated requests for one locale/context do not return the immediately previous phrase when at least two candidates exist."
    - "Selection tests control randomness and stored locale accepts only en or vi with English fallback."
  artifacts:
    - path: src/lib/locale/types.ts
      provides: "Closed Locale, message-domain, SlangContext, SlangEntry, and catalog contracts"
      exports: ["Locale", "SlangContext", "SlangEntry"]
    - path: src/lib/locale/messages.ts
      provides: "Parity-checked EN/VI literal product catalogs"
      exports: ["messages", "DEFAULT_LOCALE"]
    - path: src/lib/locale/slang.ts
      provides: "Curated contextual EN/VI slang allowlists"
      exports: ["slangCatalog"]
    - path: src/lib/locale/selectSlang.ts
      provides: "Pure deterministic selector and reusable getRandomSlang-style rotation boundary"
      exports: ["selectSlang", "createSlangRotator"]
    - path: src/lib/locale/localeStorage.ts
      provides: "Validated localStorage preference boundary"
      exports: ["LOCALE_STORAGE_KEY", "readLocale", "writeLocale"]
  key_links:
    - from: src/lib/locale/messages.ts
      to: src/lib/locale/types.ts
      via: "Vietnamese catalog satisfies canonical message shape"
      pattern: "satisfies"
    - from: src/lib/locale/slang.ts
      to: src/lib/locale/types.ts
      via: "Record<Locale, Record<SlangContext, ...>> contract"
      pattern: "SlangContext"
    - from: src/lib/locale/selectSlang.ts
      to: src/lib/locale/slang.ts
      via: "rotator selects locale/context bank and stores previous by locale plus context"
      pattern: "locale.*context"
---

<objective>
Create typed bilingual copy and contextual slang foundation with deterministic no-repeat rotation and validated persistence.

Purpose: Give every later UI plan one safe, testable contract; keep normal product copy primary and prevent unsafe slang placement, hydration randomness, and catalog drift.
Output: Locale types, EN/VI message and slang catalogs, selection/storage utilities, and focused unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-bilingual-en-vi-language-selector-and-reusable-contextual-sl/06-RESEARCH.md
@TASTE.md
@dictionary/Brainrot_Slang.md
@dictionary/Slang_Dictionary.md
@src/lib/profile/displayNameStorage.ts
@vitest.config.ts

<interfaces>
Create and preserve these contracts for downstream plans:
- Locale = "en" | "vi"; DEFAULT_LOCALE = "en".
- SlangContext must cover loading, upload, conversion, quizGeneration, flashcardGeneration, correct, wrong, retry, success, empty, warning, streak, score, navigation, toast, progress, result, badge, secondaryLabel.
- SlangEntry contains plain text plus tone classification; no HTML payload.
- Public rotation API supports getRandomSlang(context, locale)-style use while injected RNG remains available for deterministic tests.
- Locale storage key follows project naming and validates unknown localStorage values before returning Locale.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define typed literal and slang catalogs</name>
  <files>src/lib/locale/types.ts, src/lib/locale/messages.ts, src/lib/locale/slang.ts, src/lib/locale/messages.test.ts, src/lib/locale/slang.test.ts</files>
  <behavior>
    - Both en and vi satisfy identical literal catalog structure and required domains.
    - Every required SlangContext exists and contains usable entries for both locales.
    - Duplicate text inside one locale/context is rejected.
    - Forbidden coarse, hostile, shaming, identity-targeting, auth, privacy, destructive, serious-error, or accessibility copy cannot enter approved slang banks.
  </behavior>
  <action>
    Write failing parity/policy tests first, then define closed TypeScript contracts and catalogs. Group literal messages by practical domains covering loading, upload, conversion, quiz/flashcard generation, correct/wrong feedback, retry, success, empty state, warning, streak, score, navigation, toast, progress, results, badges, secondary labels, settings, and shared accessibility labels. Use English as canonical shape and require Vietnamese via `satisfies`; use typed interpolation functions for counts and dynamic labels rather than ad-hoc replacement. Translate UI chrome only: never translate study-set titles, filenames, canonical content, generated questions/options, flashcard faces/backs, or raw server error detail.

    Curate slang from both dictionary files into explicit allowlists keyed by locale and context. Keep clear product copy in messages.ts; slang.ts contains supporting personality only. Apply TASTE.md density and safety policy: wrong-answer phrases gentle and shown only after explanation; warning entries only noncritical secondary support; exclude `Thô`, profanity, direct ability blame such as "skill issue", hostile/identity/sexual/racist/ableist language, and fast-expiring brainrot except rare entries explicitly classified as optional easter eggs. Include no destructive, privacy, auth, account recovery, accessibility, or serious error contexts in SlangContext. Renderable values remain plain strings, never HTML.
  </action>
  <verify>
    <automated>npm test -- src/lib/locale/messages.test.ts src/lib/locale/slang.test.ts --run &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Typed EN/VI literal catalogs have compile/runtime parity; every required slang context is populated safely in both locales; policy tests reject missing contexts, duplicates, and forbidden content classes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build deterministic no-repeat rotation and locale storage</name>
  <files>src/lib/locale/selectSlang.ts, src/lib/locale/localeStorage.ts, src/lib/locale/selectSlang.test.ts, src/lib/locale/localeStorage.test.ts</files>
  <behavior>
    - Empty banks return null; one-entry banks return their entry without looping.
    - Banks with two or more entries exclude immediate previous text before indexing.
    - RNG values at 0, interior boundaries, and 1-like upper boundary choose deterministic valid candidates.
    - Rotation history is isolated by locale plus context and can reset on locale changes.
    - SSR/no-window and invalid, missing, or cleared storage return en; valid vi persists and reads back.
  </behavior>
  <action>
    Write tests first. Implement pure `selectSlang(entries, previous, random)` with injected RNG and bounded indexing; no retry loops and no global Math.random mocks. Add a small reusable rotator exposing a getRandomSlang(context, locale)-style method backed by slangCatalog, tracking only last phrase per locale/context in session memory. Do not select at module load or React render time; downstream consumers trigger selection on semantic events/context changes.

    Implement `localeStorage.ts` around `doc2quiz.locale` (or matching project key convention) with dependency-injectable Storage for Node tests. Validate every value against closed Locale union, default to English without throwing when window/storage is unavailable, write only valid locales, and expose storage key for provider cross-tab sync. Locale persistence contains no slang history and no user/source content.
  </action>
  <verify>
    <automated>npm test -- src/lib/locale/selectSlang.test.ts src/lib/locale/localeStorage.test.ts --run &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Selection is deterministic, edge-safe, no-repeat per locale/context, and storage validates persisted locale with hydration-safe English fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage→locale runtime | Browser-controlled string enters typed presentation state |
| dictionary source→runtime catalog | Broad editorial source contains unsafe/coarse entries requiring curated allowlisting |
| slang catalog→React text | Static personality copy reaches user-visible UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | localeStorage.ts | mitigate | Runtime allowlist accepts only en/vi and falls back to en without executing stored data |
| T-06-02 | Information disclosure | locale persistence | accept | Stores non-sensitive locale code only; no auth/session/user content |
| T-06-03 | Tampering | slang rendering | mitigate | Curated static plain strings; no runtime Markdown parsing or dangerouslySetInnerHTML |
| T-06-04 | Content safety | slang.ts | mitigate | Policy tests and explicit allowlists ban coarse, hostile, shaming, identity-targeting, auth/privacy/destructive/accessibility copy |
| T-06-SC | Tampering | package supply chain | accept | No package installs in Phase 6 |
</threat_model>

<verification>
- `npm test -- src/lib/locale --run`
- `npm run typecheck`
- Confirm required context list appears once in closed type and both locale catalogs satisfy it.
</verification>

<success_criteria>
- Catalog completeness and locale/context typing fail fast in tests/typecheck.
- EN/VI slang is curated by requested contexts and cannot replace critical product copy.
- Immediate repeat prevention works for all bank sizes without nondeterministic tests.
- Invalid localStorage values safely resolve to English.
</success_criteria>

<output>
Create `.planning/phases/06-bilingual-en-vi-language-selector-and-reusable-contextual-sl/06-01-SUMMARY.md` when done.
</output>
