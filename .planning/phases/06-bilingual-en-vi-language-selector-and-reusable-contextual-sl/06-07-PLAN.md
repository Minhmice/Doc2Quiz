---
phase: 06-bilingual-en-vi-language-selector-and-reusable-contextual-sl
plan: 07
type: execute
wave: 5
depends_on:
  - 06-04
  - 06-05
  - 06-06
files_modified:
  - src/lib/locale/coverage.test.ts
  - scripts/verify-locale-coverage.mjs
autonomous: false
requirements:
  - LOCALE-01
  - LOCALE-02
  - LOCALE-03
  - LOCALE-04
  - LOCALE-05
  - SLANG-01
  - SLANG-02
  - SLANG-03
  - SLANG-04
user_setup: []
must_haves:
  truths:
    - "Automated gates prove catalog/context completeness, deterministic no-repeat behavior, storage validation, hydration-safe provider behavior, and representative UI integration."
    - "Hard-coded-copy audit identifies remaining listed-context English chrome while excluding user/source/generated content and banned critical surfaces from slang."
    - "Production build, lint, typecheck, and full tests pass after all integrations."
    - "Human verifies EN/VI persistence and major routes at mobile/desktop widths with no hydration warnings, mixed-language listed contexts, layout shifts, or behavior regressions."
  artifacts:
    - path: src/lib/locale/coverage.test.ts
      provides: "Cross-catalog policy and representative integration coverage gate"
    - path: scripts/verify-locale-coverage.mjs
      provides: "Repeatable hard-coded listed-context audit with explicit exclusions"
  key_links:
    - from: src/lib/locale/coverage.test.ts
      to: src/lib/locale/messages.ts
      via: "required domain/key parity assertions"
      pattern: "messages"
    - from: scripts/verify-locale-coverage.mjs
      to: src/components
      via: "scan listed user-visible integration surfaces for unapproved hard-coded copy"
      pattern: "locale"
---

<objective>
Close Phase 6 with repeatable coverage/security gates and a focused human route/layout verification matrix.

Purpose: Prove broad bilingual/slang integration is complete, deterministic, hydration-safe, accessible, and regression-free rather than relying on spot checks.
Output: Coverage test, copy-audit script, all automated gates green, and human verification checkpoint.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-bilingual-en-vi-language-selector-and-reusable-contextual-sl/06-RESEARCH.md
@TASTE.md
@.planning/phases/06-bilingual-en-vi-language-selector-and-reusable-contextual-sl/06-04-SUMMARY.md
@.planning/phases/06-bilingual-en-vi-language-selector-and-reusable-contextual-sl/06-05-SUMMARY.md
@.planning/phases/06-bilingual-en-vi-language-selector-and-reusable-contextual-sl/06-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add phase-wide locale/slang coverage and hard-coded-copy audit</name>
  <files>src/lib/locale/coverage.test.ts, scripts/verify-locale-coverage.mjs</files>
  <behavior>
    - Required literal domains and slang contexts exist for both locales and contain no empty values.
    - Forbidden critical categories have no slang context/API mapping and curated banks contain no banned content/tone.
    - Representative provider/shared progress/quiz/dashboard integration imports typed locale APIs rather than local random copy.
    - Audit flags remaining hard-coded listed-context UI strings but excludes tests, catalog files, logs, route constants, user/source/generated content, and approved technical details.
  </behavior>
  <action>
    Add a phase-wide unit gate aggregating earlier contracts: EN/VI key parity, all requested contexts, no duplicates/empty entries, no-repeat and storage suites reachable, forbidden category policy, and representative integration markers for provider, selector, progress shell, quiz feedback, and dashboard empty state. Avoid brittle snapshots of full copy; assert contracts, contexts, and safety properties.

    Add repeatable Node audit script scanning scoped user-visible source files for remaining hard-coded English in Phase 6 listed contexts. Use an explicit allowlist/exclusion configuration for locale catalogs, tests, logs, route/path constants, API technical errors/details, user/source/generated content, proper nouns, and unchanged out-of-scope server strings. Script must fail with file/line findings, not silently count comments. It must also scan for forbidden direct `Math.random()`/`getRandomSlang()` calls in JSX/render paths and `dangerouslySetInnerHTML` use involving slang. Keep script read-only.

    Resolve every true finding by assigning it back to owning prior plan files before completing this task; do not broaden into route localization, generated-content translation, auth flow redesign, DB persistence, or layout redesign.
  </action>
  <verify>
    <automated>node scripts/verify-locale-coverage.mjs &amp;&amp; npm test -- src/lib/locale src/components/locale --run &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test -- --run &amp;&amp; npm run build</automated>
  </verify>
  <done>Coverage/audit gates detect missing listed-context copy, unsafe slang placement, render-time randomness, and catalog drift; all focused/full tests, lint, typecheck, and production build pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify bilingual routes, persistence, safety, and layout</name>
  <files></files>
  <action>
    Verify completed automated work after all gates pass. Do not modify data beyond normal test study sets. Confirm current functionality and layout remain intact in both locales.
  </action>
  <verify>
    <automated>node scripts/verify-locale-coverage.mjs &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test -- --run &amp;&amp; npm run build</automated>
    <human-check>
      1. Open app with cleared `doc2quiz.locale`; confirm first render English and browser console has no hydration warning.
      2. Switch to VI in account menu. Confirm top bar, command palette, and Settings update without reload; `<html lang>` becomes `vi`.
      3. Reload and open second tab; confirm VI persists/synchronizes. Set invalid storage value manually, reload, and confirm safe EN fallback.
      4. Check account dropdown and Settings at 375px and desktop widths: no clipped choices, changed top-bar height, wrapped primary action row, lost keyboard focus, or touch target below 44px.
      5. Run file/paste/YouTube import through upload/conversion/canonicalization and both quiz/flashcard generation. Literal progress appears first; slang appears only as secondary support, stays stable during unrelated repaint, and does not repeat immediately across semantic transitions.
      6. Trigger validation failure, generation failure/retry, and any serious error available. Confirm factual recovery copy remains literal and no slang appears in errors.
      7. Complete quiz with one correct and one wrong answer. Confirm explanation/literal feedback precedes optional reaction; wrong reaction is gentle; score, progress, badges, retry, mistakes, and done result localize; keyboard controls still work.
      8. Complete flashcard session. Confirm front/back content unchanged, Space/arrows/focus work, progress/actions/done page localize, and ARIA announcements stay literal.
      9. Check dashboard loading, empty/filter-empty, populated cards, stats, streak, badges, mobile nav, rename, and delete dialog. Confirm delete/account/accessibility copy has zero slang and all existing actions/routes work.
      10. Switch EN/VI while viewing existing study content. Confirm study-set titles, filenames, canonical content, MCQs/options/explanations, and flashcard faces/backs never translate.
      11. At mobile and desktop widths, compare existing screens: no card/grid geometry change, overflow, hidden controls, reordered content, or reduced-motion regression.
    </human-check>
  </verify>
  <done>User approves locale switching/persistence, contextual slang safety/rotation, major route coverage, accessibility, mobile/desktop layout, and existing workflow regression matrix.</done>
  <what-built>Typed EN/VI locale system, persistent selectors, contextual no-repeat slang, and integrations across workflow, practice, results, dashboard, navigation, toasts, badges, and secondary labels.</what-built>
  <how-to-verify>Follow numbered human-check matrix above after automated command passes.</how-to-verify>
  <resume-signal>Type "approved" or report route, locale, copy, layout, console, or behavior failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source scan→release gate | Audit classifies code strings as allowed or missing localization |
| automated verification→human browser | Static/test evidence is validated against hydration, layout, keyboard, and semantic behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-24 | Tampering | audit exclusions | mitigate | Explicit narrow allowlist with file/line findings; true findings returned to owning integration plan |
| T-06-25 | Repudiation | phase completion | mitigate | Full command gate plus numbered human route matrix and explicit approval signal |
| T-06-26 | Information disclosure | audit script | accept | Local read-only source scan; no source text sent externally |
| T-06-27 | Content safety | integrated slang | mitigate | Automated forbidden-context/content gate plus human errors/destructive/wrong-answer review |
| T-06-SC | Tampering | package supply chain | accept | No package installs in Phase 6 |
</threat_model>

<verification>
- `node scripts/verify-locale-coverage.mjs`
- `npm run lint`
- `npm run typecheck`
- `npm test -- --run`
- `npm run build`
- Human EN/VI route, hydration, safety, accessibility, and responsive-layout matrix.
</verification>

<success_criteria>
- Catalog completeness, locale/context typing, no-repeat, storage validation, hydration safety, and representative integration have automated coverage.
- Audit finds no unapproved hard-coded listed-context copy or render-time slang randomness.
- Lint, typecheck, full tests, and production build pass.
- Human confirms selectors, persistence, major workflows, safety bans, keyboard/accessibility behavior, and unchanged mobile/desktop layout.
</success_criteria>

<output>
Create `.planning/phases/06-bilingual-en-vi-language-selector-and-reusable-contextual-sl/06-07-SUMMARY.md` when done.
</output>
