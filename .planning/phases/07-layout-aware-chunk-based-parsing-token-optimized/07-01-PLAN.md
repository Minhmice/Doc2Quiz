---
phase: 07-layout-aware-chunk-based-parsing-token-optimized
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai/layoutChunksFromOcr.ts
  - src/lib/ai/prompts/mcq-extraction.prompts.json
  - src/lib/ai/prompts/mcqExtractionPrompts.ts
  - src/lib/ai/parseChunk.ts
autonomous: true
requirements:
  - AI-02
  - AI-03
  - AI-04
user_setup: []

must_haves:
  truths:
    - "Assembled chunk text is trimmed and excessive blank lines collapsed before model calls (ROADMAP token hygiene), verifiable in `layoutChunksFromOcr.ts` per Task 1 acceptance."
    - "Layout chunks are built from OCR blocks in reading order (vertical center then horizontal center in relative space), with question-boundary heuristics and 2–3 block fallback per D-05–D-08, D-20."
    - "Single-chunk MCQ extraction uses a dedicated system prompt (not the multi-question vision prompt) and returns one validated Question or null per D-09–D-10, D-18–D-19."
    - "parseChunkSingleMcqOnce does not add performance.now() or other wall-clock attribution for chunk AI time; exactly one timing layer will wrap await parse() in runLayoutChunkParse (07-02) per D-27 and 07-RESEARCH.md (no double-wrap)."
  artifacts:
    - path: src/lib/ai/layoutChunksFromOcr.ts
      provides: buildLayoutChunksFromRun + chunk user content helpers aligned to CONTEXT
    - path: src/lib/ai/prompts/mcq-extraction.prompts.json
      provides: mcqSingleChunk.system JSON for one-object MCQ output
    - path: src/lib/ai/parseChunk.ts
      provides: parseChunkSingleMcqOnce (OpenAI / Custom / Anthropic text paths)
  key_links:
    - from: layoutChunksFromOcr.ts
      to: parseChunk.ts
      via: "AiParseSection / runLayoutChunkParse inject parse(() => parseChunkSingleMcqOnce(...)) in 07-02"
      pattern: "parseChunkSingleMcqOnce"
---

<objective>
Deliver and **lock** the **pure library** slice for Phase 7: OCR → `LayoutChunk[]` → **text-only** single-MCQ `parseChunkSingleMcqOnce`, with prompts and reading-order rules matching **07-CONTEXT** (D-05–D-12, D-18–D-21, D-24 intent for expand helper only here).

**Purpose:** Wave 2 (`07-02-PLAN.md`) depends on stable chunk text, spatial hint rules, and a **single** AI-parse entrypoint with **no** inner wall-clock layer (per **D-27**, **07-RESEARCH.md** “double-counting” mitigation).

**Output:** Audited/refined `layoutChunksFromOcr.ts`, `mcq-extraction.prompts.json` + `mcqExtractionPrompts.ts`, `parseChunk.ts` (`parseChunkSingleMcqOnce`).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-layout-aware-chunk-based-parsing-token-optimized/07-CONTEXT.md
@.planning/phases/07-layout-aware-chunk-based-parsing-token-optimized/07-RESEARCH.md
@src/types/ocr.ts
@src/lib/ai/layoutChunksFromOcr.ts
@src/lib/ai/parseChunk.ts
@src/lib/ai/prompts/mcq-extraction.prompts.json
@src/lib/ai/prompts/mcqExtractionPrompts.ts
@src/lib/ai/validateQuestions.ts
</context>

## Decision coverage (D-01–D-29 for this plan)

| ID    | Plan | Task | Full / Partial | Notes |
|-------|------|------|------------------|-------|
| D-05–D-08, D-20–D-21 | 01 | 1 | Full | Chunk engine + spatial hint input for prompts |
| D-09–D-10, D-18–D-19, D-24 (expand text helper) | 01 | 2–3 | Full | Prompt + single-MCQ parse; retry loop stays 07-02 |
| D-27 | 01 | 3 | Partial | **Document + enforce** no timing inside `parseChunkSingleMcqOnce`; wall-clock measurement only in **07-02** Task 1 |
| D-28–D-29, D-26, pipelineLog | 02 | — | — | Out of scope for 07-01 |

<tasks>

<task type="auto">
  <name>Task 1: Layout chunk engine audit (reading order + boundaries)</name>
  <files>src/lib/ai/layoutChunksFromOcr.ts</files>
  <read_first>src/types/ocr.ts, .planning/phases/07-layout-aware-chunk-based-parsing-token-optimized/07-CONTEXT.md (D-05–D-08, D-20)</read_first>
  <action>
Confirm or implement in `buildLayoutChunksFromRun` (and helpers such as `expandChunkText`, `buildChunkUserContent`):
- Sort blocks per page by bbox **center** `(cx, cy)` in `relative` space: sort key **cy ascending, then cx** (D-20). Blocks without usable bbox: append at end of page in **original array order** (D-20).
- Question-start patterns (D-06): keep one extensible array; minimum regexes: `^\s*Câu\s*\d+`, `^\s*\d+\s*[.)]`, `^\s*Question\s*\d+` (case-insensitive for “Question”).
- Chunks ≈ one question (D-07); if no boundary, group **2–3** adjacent sorted blocks (or single if only one).
- Export stable `LayoutChunk.id` pattern `p{pageIndex}-c{seq}` (0- or 1-based pageIndex must match existing repo convention — **do not change** if already consistent with `runLayoutChunkParse`).
- `buildChunkUserContent`: when ≥1 block has bbox, include **one-line** bbox summary in user content (D-21); omit only when no bbox (document in code comment).

Do **not** add `performance.now()` here.

**Token hygiene (ROADMAP):** When assembling each chunk’s `text`, apply `trim()` and replace sequences of 2+ newlines with a single `\n` (or call a small shared `normalizeChunkTextForModel(s: string): string` in the same file). Keep bbox / spatial hint line unchanged.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "buildLayoutChunksFromRun" src/lib/ai/layoutChunksFromOcr.ts` shows exported builder.
    - `rg -n "cy|cx|center" src/lib/ai/layoutChunksFromOcr.ts` shows reading-order logic (not raw array order only).
    - No `performance\\.now` in `src/lib/ai/layoutChunksFromOcr.ts`.
    - `rg -n "normalizeChunkTextForModel|\\.trim\\(" src/lib/ai/layoutChunksFromOcr.ts` shows at least one call site applying hygiene to chunk text before it is returned or passed to `buildChunkUserContent`.
  </acceptance_criteria>
  <done>Chunk builder matches D-05–D-08, D-20–D-21; compiles and lints clean.</done>
</task>

<task type="auto">
  <name>Task 2: Single-chunk MCQ prompt contract</name>
  <files>src/lib/ai/prompts/mcq-extraction.prompts.json,src/lib/ai/prompts/mcqExtractionPrompts.ts</files>
  <read_first>.planning/phases/07-layout-aware-chunk-based-parsing-token-optimized/07-CONTEXT.md (D-09, D-18)</read_first>
  <action>
Ensure `mcq-extraction.prompts.json` contains `mcqSingleChunk.system` instructing: exactly **one** MCQ from the given chunk text; output **JSON only** with keys `question`, `options` (length 4), `correctIndex` (0–3). Do **not** instruct multi-question arrays for the default path.

In `mcqExtractionPrompts.ts`, export `MCQ_SINGLE_CHUNK_SYSTEM_PROMPT` using the same pattern as `MCQ_EXTRACTION_SYSTEM_PROMPT` (JSON import + trim).

Confirm this prompt is **not** reused for full-page multi-MCQ vision extraction elsewhere (D-18: distinct system prompt for small chunks).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "mcqSingleChunk" src/lib/ai/prompts/mcq-extraction.prompts.json` matches.
    - `rg -n "MCQ_SINGLE_CHUNK_SYSTEM_PROMPT" src/lib/ai/prompts/mcqExtractionPrompts.ts` exports constant.
  </acceptance_criteria>
  <done>Prompt asset + TS export aligned to D-09, D-18.</done>
</task>

<task type="auto">
  <name>Task 3: parseChunkSingleMcqOnce — providers, validation, no inner wall timer</name>
  <files>src/lib/ai/parseChunk.ts</files>
  <read_first>src/lib/ai/parseChunk.ts,src/lib/ai/validateQuestions.ts,.planning/phases/07-layout-aware-chunk-based-parsing-token-optimized/07-RESEARCH.md (Instrumentation table)</read_first>
  <action>
Audit `parseChunkSingleMcqOnce`:
- Branches for **openai**, **custom**, **anthropic** all use `MCQ_SINGLE_CHUNK_SYSTEM_PROMPT` (D-18).
- Reuse existing HTTP / `FatalParseError` behavior from `parseChunkOnce` patterns (401/429 etc.).
- Map model JSON to **one** `Question` via existing validation helpers (`validateQuestionsFromJson` or equivalent); return `null` when model yields no usable MCQ (D-09–D-10, AI-04).

**Critical (D-27 / RESEARCH):** Add a **short file-level or function-level comment** next to `parseChunkSingleMcqOnce` stating: *Per-chunk AI wall time is measured only in `runLayoutChunkParse` around each `await parse(...)`; do not wrap this function with `performance.now()` to avoid double-counting.*

Verify the function body contains **no** `performance.now(` calls.

Optional (only if needed for 07-02 raw debug): add an **optional** debug-only parameter (e.g. `onRawAssistantText?: (text: string) => void`) that never runs unless passed — must not log full chunk text in production by default. If not needed yet, skip and note in `07-02-PLAN.md` follow-up; prefer minimal diff.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "export async function parseChunkSingleMcqOnce" src/lib/ai/parseChunk.ts` exists.
    - `rg -n "performance\\.now" src/lib/ai/parseChunk.ts` returns **no** matches inside `parseChunkSingleMcqOnce` (entire file may use none).
    - `rg -n "MCQ_SINGLE_CHUNK_SYSTEM_PROMPT" src/lib/ai/parseChunk.ts` shows all three provider branches.
  </acceptance_criteria>
  <done>Single-chunk parse is provider-complete, validated, and timing-neutral for outer wrapper (07-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → vendor APIs | Chunk text and API keys cross to OpenAI/Anthropic/custom endpoints |

## STRIDE (register)

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-1 | I | `parseChunkSingleMcqOnce` | mitigate | Reuse existing error handling; never `console.log` full user chunk in production |
| T-07-01-2 | E | Prompt injection via OCR text | accept | Local exam content; same trust as existing `parseChunkOnce` |
</threat_model>

<verification>
- `npm run lint`
- `npm run build`
</verification>

<success_criteria>
- D-05–D-10, D-18–D-21 satisfied for lib layer; D-27 timing responsibility documented and enforced (no inner `performance.now` in `parseChunkSingleMcqOnce`).
- REQUIREMENTS **AI-02**, **AI-03**, **AI-04** addressed by chunk builder + single-MCQ extraction + validation path.
</success_criteria>

<output>
After completion, create `.planning/phases/07-layout-aware-chunk-based-parsing-token-optimized/07-01-SUMMARY.md` per execute-plan workflow.
</output>
