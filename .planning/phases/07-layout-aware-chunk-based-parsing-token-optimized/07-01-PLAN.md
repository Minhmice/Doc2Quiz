---
phase: 07-layout-aware-chunk-based-parsing-token-optimized
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/ocr.ts
  - src/lib/ai/prompts/mcq-extraction.prompts.json
  - src/lib/ai/prompts/mcqExtractionPrompts.ts
  - src/lib/ai/parseChunk.ts
  - src/lib/ai/layoutChunksFromOcr.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "OCR blocks are ordered by reading order (cy then cx) before chunking"
    - "Single-chunk MCQ prompt returns one JSON object with question + 4 options + correctIndex"
    - "parseChunk path supports openai, custom, anthropic for text-only single-MCQ calls"
  artifacts:
    - "src/lib/ai/layoutChunksFromOcr.ts exports chunk builder from OcrRunResult"
    - "mcq-extraction.prompts.json includes mcqSingleChunk.system"
    - "parseChunk.ts (or sibling) exports parseChunkSingleMcqOnce"
  key_links:
    - "Aligns with 07-CONTEXT D-05–D-12, D-18–D-21, D-24"
---

<objective>
Implement **pure layout chunking from OCR** and **single-MCQ text parse** (one API call per chunk) without yet wiring `AiParseSection`. This wave delivers types, prompts, and `parseChunkSingleMcqOnce` so wave 2 can orchestrate retries and vision fallback.

Output: `layoutChunksFromOcr.ts`, prompt JSON + TS export, `parseChunkSingleMcqOnce`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-layout-aware-chunk-based-parsing-token-optimized/07-CONTEXT.md
@src/types/ocr.ts
@src/lib/ai/parseChunk.ts
@src/lib/ai/prompts/mcq-extraction.prompts.json
@src/lib/ai/prompts/mcqExtractionPrompts.ts
@src/lib/ai/validateQuestions.ts
</context>

<interfaces>
```typescript
// Layout chunk — stable id for debug + Question.layoutChunkId
export type LayoutChunk = {
  id: string;
  pageIndex: number;
  text: string;
  blockIndices: number[]; // indices into OcrPageResult.blocks
};

// buildLayoutChunksFromRun(run: OcrRunResult): LayoutChunk[]
// parseChunkSingleMcqOnce(params: ParseChunkOnceParams & { chunk: LayoutChunk }): Promise<Question | null>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Layout chunk builder</name>
  <files>src/lib/ai/layoutChunksFromOcr.ts</files>
  <read_first>src/types/ocr.ts, 07-CONTEXT.md</read_first>
  <action>
Add `src/lib/ai/layoutChunksFromOcr.ts`:
- For each `OcrPageResult`, sort `blocks` by bbox center `(cy, cx)` in `relative` space (D-20); blocks without usable bbox append in original order at end of page.
- Detect question starts with regex list (D-06): at minimum `^\s*Câu\s*\d+`, `^\s*\d+\s*[.)]`, `^\s*Question\s*\d+` (case-insensitive); make list extensible in one constant array.
- Emit `LayoutChunk[]`: merge blocks until next boundary; if no boundary in page, group **2–3** adjacent blocks (D-07) or single block if only one.
- `chunk.id`: stable string e.g. `p{pageIndex}-c{seq}`.
- Export `buildLayoutChunksFromRun(run: OcrRunResult): LayoutChunk[]`.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - File exports `buildLayoutChunksFromRun`
    - Sort uses vertical then horizontal center of relative bbox
  </acceptance_criteria>
  <done>Chunk engine compiles</done>
</task>

<task type="auto">
  <name>Task 2: Single-MCQ system prompt</name>
  <files>src/lib/ai/prompts/mcq-extraction.prompts.json, src/lib/ai/prompts/mcqExtractionPrompts.ts</files>
  <read_first>07-CONTEXT.md</read_first>
  <action>
Add `mcqSingleChunk` object in JSON with `system` string: extract **exactly one** MCQ from OCR text chunk; output **JSON only** with keys `question`, `options` (4 strings), `correctIndex` (0–3); if impossible return a sentinel the code can detect (prefer `{ "question": "", "options": ["","","",""], "correctIndex": 0 }` with documented rule **or** `{ "questions": [] }` wrapped — planner picks one and document in code comment). Must not ask for full-page context.

Export `MCQ_SINGLE_CHUNK_SYSTEM_PROMPT` from `mcqExtractionPrompts.ts` (mirror `MCQ_EXTRACTION_SYSTEM_PROMPT` pattern).
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - JSON valid; TS exports new constant
  </acceptance_criteria>
  <done>Prompt wired</done>
</task>

<task type="auto">
  <name>Task 3: parseChunkSingleMcqOnce</name>
  <files>src/lib/ai/parseChunk.ts</files>
  <read_first>src/lib/ai/parseChunk.ts, validateQuestions.ts</read_first>
  <action>
Add `parseChunkSingleMcqOnce` (or equivalent name) that:
- Accepts same provider/url/model/apiKey/signal as `parseChunkOnce` plus **user content** = chunk `text` + optional one-line spatial hint from chunk bboxes (D-21).
- Uses `MCQ_SINGLE_CHUNK_SYSTEM_PROMPT` instead of `MCQ_EXTRACTION_SYSTEM_PROMPT`.
- Parses model JSON into **one** `Question` via existing validation helpers (`validateQuestionsFromJson` with wrapper shape, or small adapter).
- Returns `null` or empty when model says no question (per sentinel rule).
- Preserves `FatalParseError` behavior for 401/429 like `parseChunkOnce`.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Exported function callable from future orchestrator
    - OpenAI + Anthropic + custom paths covered (reuse `parseChunkOnce` branching)
  </acceptance_criteria>
  <done>Single-chunk AI parse ready</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Same-origin forward | API keys never logged; chunk text may contain exam content — no `console.log` of full chunks in production |

## STRIDE

| ID | Cat | Component | Mitigation |
|----|-----|-----------|------------|
| T-07a-1 | I | User content in prompts | Do not reflect untrusted HTML; text-only |
</threat_model>

<verification>
- [ ] `npm run build`
- [ ] `npm run lint`
</verification>

<success_criteria>
- Chunk builder + single-MCQ parse compile and match 07-CONTEXT D-05–D-12, D-18–D-21
</success_criteria>
