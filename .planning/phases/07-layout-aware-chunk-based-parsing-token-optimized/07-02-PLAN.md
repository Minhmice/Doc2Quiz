---
phase: 07-layout-aware-chunk-based-parsing-token-optimized
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01-PLAN.md
files_modified:
  - src/lib/ai/runLayoutChunkParse.ts
  - src/types/question.ts
  - src/lib/ai/mapQuestionsToPages.ts
  - src/components/ai/AiParseSection.tsx
  - src/components/ai/OcrInspector.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Orchestrator runs OCR if missing when Fast/Hybrid chooses chunk (D-16)"
    - "Per-chunk retry expands context once (D-24); bulk fallback triggers vision per D-23"
    - "Hybrid auto uses successPages/totalPages >= 0.85 and failedPages === 0 (D-22)"
  artifacts:
    - "runLayoutChunkParse (or equivalent) exported from src/lib/ai/"
    - "Parse mode persisted (localStorage) and read in AiParseSection"
  key_links:
    - "Accurate mode calls existing vision path only (D-17)"
    - "Question gains optional layoutChunkId + parseConfidence (+ parseStructureValid) (D-25)"
---

<objective>
Wire **layout-chunk orchestration**, **fallback to existing vision**, **parse mode UX (Fast / Accurate / Hybrid)**, and **minimal debug visibility**. Depends on **07-01** (chunk builder + `parseChunkSingleMcqOnce`).

Output: `runLayoutChunkParse.ts`, `AiParseSection` branches, optional `Question` fields, chunk debug in inspector or sibling panel.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-layout-aware-chunk-based-parsing-token-optimized/07-CONTEXT.md
@.planning/phases/07-layout-aware-chunk-based-parsing-token-optimized/07-01-PLAN.md
@src/components/ai/AiParseSection.tsx
@src/lib/ai/runVisionSequential.ts
@src/lib/ai/runOcrSequential.ts
@src/lib/ai/ocrDb.ts
@src/lib/ai/mapQuestionsToPages.ts
@src/lib/ai/dedupeQuestions.ts
</context>

<interfaces>
```typescript
// runLayoutChunkParse(opts): Promise<{ questions: Question[]; chunkResults: ChunkParseResult[]; usedVisionFallback: boolean }>
// ChunkParseResult: { chunkId, ok, rawSnippet?, error?, expandedOnce: boolean }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Question optional parse fields</name>
  <files>src/types/question.ts, src/lib/ai/validateQuestions.ts</files>
  <read_first>07-CONTEXT D-25</read_first>
  <action>
Extend `Question` with optional `layoutChunkId?: string`, `parseConfidence?: number` (0..1), `parseStructureValid?: boolean`. Ensure `validateQuestionsFromJson` / draft hydrate **preserve** unknown optional fields or set defaults when importing JSON without them (no breaking change).
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Types compile; existing flows unchanged when fields absent
  </acceptance_criteria>
  <done>Types extended</done>
</task>

<task type="auto">
  <name>Task 2: runLayoutChunkParse orchestrator</name>
  <files>src/lib/ai/runLayoutChunkParse.ts</files>
  <read_first>src/lib/ai/layoutChunksFromOcr.ts, src/lib/ai/parseChunk.ts, 07-CONTEXT.md</read_first>
  <action>
Implement `runLayoutChunkParse`:
- Input: `OcrRunResult`, provider settings, `AbortSignal`, `onProgress({ current, total })`, optional callback for debug `onChunkResult`.
- Build chunks via `buildLayoutChunksFromRun`.
- For each chunk: call `parseChunkSingleMcqOnce`; on failure **once** retry with **expanded text** (append next block’s text from same page if available) (D-24).
- Collect `Question[]`; set `layoutChunkId`, `parseConfidence` heuristic (e.g. structure valid + option lengths), `parseStructureValid`.
- **Failure accounting:** track `failedChunks / totalChunks`. If **> 0.5** OR **zero valid questions** after pass → set flag `needsVisionFallback: true` (actual vision invocation in Task 3 to avoid circular imports if needed).

Pure module preferred; vision call may be injected as optional `runVisionFallback: () => Promise<Question[]>` from UI layer.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Orchestrator compiles; progress callbacks fire per chunk
  </acceptance_criteria>
  <done>Chunk loop + retry + metrics</done>
</task>

<task type="auto">
  <name>Task 3: AiParseSection — modes + OCR prefetch + merge</name>
  <files>src/components/ai/AiParseSection.tsx</files>
  <read_first>handleVisionParse flow, 07-CONTEXT D-16 D-17 D-22 D-23</read_first>
  <action>
- Add **parse mode** `fast | accurate | hybrid` stored in **localStorage** (key namespaced `doc2quiz:`), UI: segmented control or select near parse controls.
- **Accurate:** keep current `handleVisionParse` behavior (render pages → vision only); skip layout chunk path (D-17).
- **Fast:** after `renderPdfPagesToImages`, ensure OCR: if no `getOcrResult` rows, run `runOcrSequential` + `putOcrResult` (D-16). Then `runLayoutChunkParse`. If `needsVisionFallback` or orchestrator requests → invoke **existing** vision sequential path once, then **dedupe** with `dedupeQuestionsByStem` favoring chunk-derived where duplicate stems (document rule in code comment) (D-23).
- **Hybrid:** if OCR stats meet D-22 → Fast path; else Accurate path.
- After questions: call `applyQuestionPageMapping` with appropriate `VisionParseMode` — extend type if needed (`layout_chunk` mapping method) per CONTEXT code_context note; or reuse `ocr_text_overlap` + provenance fields.

Use `pipelineLog` for mode chosen, fallback triggered, chunk/vision counts.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - All three modes reachable without runtime throw when keys set
    - Default mode is **Fast** (or **Hybrid** if product prefers — pick one and document in UI label)
  </acceptance_criteria>
  <done>Integration complete</done>
</task>

<task type="auto">
  <name>Task 4: Chunk debug visibility</name>
  <files>src/components/ai/OcrInspector.tsx or src/components/ai/ChunkParseDebug.tsx</files>
  <read_first>07-CONTEXT D-26</read_first>
  <action>
Expose **last run** chunk → outcome (ok / error / expanded retry) in collapsible section. Options: (a) extend `OcrInspector` with props `lastChunkDebug?: ChunkParseResult[]` set from parent after parse, or (b) small standalone component under parse section. Do **not** persist raw model text to IndexedDB (D-25); in-memory or session-only is fine.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Developer can see chunk id list + status after a parse without opening network tab
  </acceptance_criteria>
  <done>Debug hooks shipped</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vision fallback | Doubles API cost in worst case — user-visible summary should mention fallback |

## STRIDE

| ID | Cat | Mitigation |
|----|-----|------------|
| T-07b-1 | DoS | Cap chunks same as pages; reuse abort signal |
</threat_model>

<verification>
- [ ] `npm run build`
- [ ] `npm run lint`
- [ ] Manual: Fast mode on sample PDF with OCR on → questions appear; Accurate → vision-only still works
</verification>

<success_criteria>
- Phase 7 CONTEXT D-16–D-26 reflected in behavior
- Existing vision-only users unaffected via Accurate mode
</success_criteria>
