diff --git a/src/components/dashboard/DashboardMobileBottomNav.tsx b/src/components/dashboard/DashboardMobileBottomNav.tsx
index c5bd2bd..09aff43 100644
--- a/src/components/dashboard/DashboardMobileBottomNav.tsx
+++ b/src/components/dashboard/DashboardMobileBottomNav.tsx
@@ -1,70 +1,15 @@
+"use client";
+
 import Link from "next/link";
 import { usePathname } from "next/navigation";
-import { BarChart3, LayoutDashboard, Library, Plus, Settings } from "lucide-react";
-import { newRoot } from "@/lib/routes/studySetPaths";
+import { CircleUserRound, LayoutDashboard, Library, Plus } from "lucide-react";
+import { useLocale } from "@/components/locale/LocaleProvider";
 import { cn } from "@/lib/utils";
 
-export type DashboardMobileBottomNavProps = Readonly<{
-  className?: string;
-}>;
-
-export function DashboardMobileBottomNav({
-  className,
-}: DashboardMobileBottomNavProps) {
+export function DashboardMobileBottomNav({ className }: Readonly<{ className?: string }>) {
   const pathname = usePathname() ?? "";
-
-  const dashActive = pathname === "/dashboard";
-  const libActive = pathname === "/dashboard"; /* hash not in pathname — subtle */
-
-  return (
-    <nav
-      className={cn(
-        "fixed bottom-0 left-0 right-0 z-50 flex h-16 items-center justify-around border-t border-border/30 bg-card/95 shadow-[0_-4px_20px_rgba(0,0,0,0.04)] backdrop-blur-md md:hidden dark:bg-card/95",
-        className,
-      )}
-      aria-label="Mobile primary"
-    >
-      <Link
-        href="/dashboard"
-        className={cn(
-          "flex cursor-pointer flex-col items-center gap-0.5 font-label text-[10px] font-bold uppercase transition-colors",
-          dashActive ? "text-[color:var(--d2q-accent)]" : "text-[color:var(--d2q-blue)]",
-        )}
-      >
-        <LayoutDashboard className="size-6" aria-hidden />
-        Dash
-      </Link>
-      <Link
-        href="/dashboard#library"
-        className={cn(
-          "flex cursor-pointer flex-col items-center gap-0.5 font-label text-[10px] uppercase transition-colors",
-          libActive ? "text-[color:var(--d2q-blue)]" : "text-muted-foreground",
-        )}
-      >
-        <Library className="size-6" aria-hidden />
-        Library
-      </Link>
-      <Link
-        href={newRoot()}
-        className="-mt-10 flex size-12 cursor-pointer items-center justify-center rounded-full bg-[color:var(--d2q-accent)] text-primary-foreground shadow-lg transition-transform active:scale-95 dark:text-primary-foreground"
-        aria-label="Create new study set"
-      >
-        <Plus className="size-6" aria-hidden />
-      </Link>
-      <Link
-        href="/dashboard#stats"
-        className="flex cursor-pointer flex-col items-center gap-0.5 font-label text-[10px] uppercase text-[color:var(--d2q-blue)] transition-colors"
-      >
-        <BarChart3 className="size-6" aria-hidden />
-        Stats
-      </Link>
-      <Link
-        href="/settings"
-        className="flex cursor-pointer flex-col items-center gap-0.5 font-label text-[10px] uppercase text-[color:var(--d2q-blue)] transition-colors"
-      >
-        <Settings className="size-6" aria-hidden />
-        Set
-      </Link>
-    </nav>
-  );
+  const { messages } = useLocale();
+  const n = messages.navigation;
+  const items = [{ href: "/dashboard", label: n.dashboard, icon: LayoutDashboard }, { href: "/dashboard?type=all", label: n.allSets, icon: Library }, { href: "/create", label: n.create, icon: Plus }, { href: "/settings", label: n.settings, icon: CircleUserRound }];
+  return <nav className={cn("fixed inset-x-0 bottom-0 z-50 grid h-16 grid-cols-4 border-t border-border/60 bg-card md:hidden", className)} aria-label={n.primaryNavigation}>{items.map(({ href, label, icon: Icon }) => <Link key={href} href={href} className={cn("flex min-h-11 flex-col items-center justify-center gap-0.5 text-xs font-semibold text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring", pathname === href.split("?")[0] && "text-[color:var(--d2q-accent)]")}>{<Icon className="size-5" aria-hidden />}{label}</Link>)}</nav>;
 }
diff --git a/src/components/layout/AppShell.tsx b/src/components/layout/AppShell.tsx
index 020b0c1..f160f7d 100644
--- a/src/components/layout/AppShell.tsx
+++ b/src/components/layout/AppShell.tsx
@@ -1,49 +1,22 @@
 "use client";
 
-import { useEffect } from "react";
+import { useEffect, useState } from "react";
 import { usePathname } from "next/navigation";
+import { AppSidebar } from "@/components/layout/AppSidebar";
 import { AppTopBar } from "@/components/layout/AppTopBar";
-import {
-  FOCUS_LIBRARY_SEARCH_EVENT,
-  LibrarySearchProvider,
-  useLibrarySearch,
-} from "@/components/layout/LibrarySearchContext";
+import { DashboardMobileBottomNav } from "@/components/dashboard/DashboardMobileBottomNav";
+import { FOCUS_LIBRARY_SEARCH_EVENT, LibrarySearchProvider, useLibrarySearch } from "@/components/layout/LibrarySearchContext";
+
 function FocusSearchListener() {
   const { focusLibrarySearch } = useLibrarySearch();
-
-  useEffect(() => {
-    const fn = () => {
-      focusLibrarySearch();
-    };
-    window.addEventListener(FOCUS_LIBRARY_SEARCH_EVENT, fn);
-    return () => window.removeEventListener(FOCUS_LIBRARY_SEARCH_EVENT, fn);
-  }, [focusLibrarySearch]);
-
+  useEffect(() => { const fn = () => focusLibrarySearch(); window.addEventListener(FOCUS_LIBRARY_SEARCH_EVENT, fn); return () => window.removeEventListener(FOCUS_LIBRARY_SEARCH_EVENT, fn); }, [focusLibrarySearch]);
   return null;
 }
 
 export function AppShell({ children }: { children: React.ReactNode }) {
-  const pathname = usePathname();
-  const isStudyMode =
-    !!pathname && /^\/(flashcards|quiz)\/[^/]+(?:\/done)?$/.test(pathname);
-
-  return (
-    <LibrarySearchProvider>
-      <FocusSearchListener />
-      <div className="flex h-dvh min-h-0 flex-col overflow-hidden bg-background">
-        <div className="flex min-h-0 min-w-0 flex-1 flex-col overflow-hidden">
-          <AppTopBar />
-          <main
-            className={
-              isStudyMode
-                ? "relative flex min-h-0 flex-1 flex-col overflow-hidden bg-background d2q-technical-grid px-3 py-5 sm:px-8 sm:py-8"
-                : "relative flex min-h-0 flex-1 flex-col overflow-y-auto bg-background d2q-technical-grid px-3 py-5 sm:px-8 sm:py-8"
-            }
-          >
-            {children}
-          </main>
-        </div>
-      </div>
-    </LibrarySearchProvider>
-  );
+  const pathname = usePathname() ?? "";
+  const [collapsed, setCollapsed] = useState(false);
+  const focusMode = /^\/(quiz|flashcard)\/[^/]+\/(play|drill-mistake)$/.test(pathname);
+  const topLevel = ["/dashboard", "/create", "/settings", "/help"].includes(pathname);
+  return <LibrarySearchProvider><FocusSearchListener /><div className="flex h-dvh min-h-0 overflow-hidden bg-background"><AppSidebar collapsed={collapsed} onToggle={() => setCollapsed((value) => !value)} hidden={focusMode} /><div className="flex min-w-0 flex-1 flex-col overflow-hidden"><AppTopBar hidden={focusMode} onToggleSidebar={() => setCollapsed((value) => !value)} /><main className={focusMode ? "relative flex min-h-0 flex-1 flex-col overflow-hidden bg-background d2q-technical-grid px-3 py-5 sm:px-8 sm:py-8" : "relative flex min-h-0 flex-1 flex-col overflow-y-auto bg-background d2q-technical-grid px-3 py-5 pb-24 sm:px-8 sm:py-8"}>{children}</main>{topLevel && !focusMode && <DashboardMobileBottomNav />}</div></div></LibrarySearchProvider>;
 }
diff --git a/src/components/layout/AppTopBar.tsx b/src/components/layout/AppTopBar.tsx
index d23cdb6..f1bac92 100644
--- a/src/components/layout/AppTopBar.tsx
+++ b/src/components/layout/AppTopBar.tsx
@@ -1,260 +1,17 @@
 "use client";
 
-import Link from "next/link";
-import { usePathname, useRouter } from "next/navigation";
-import { useCallback, useEffect, useMemo, useRef, useState } from "react";
-import { SearchIcon } from "lucide-react";
-import { ApiStatusIndicator } from "@/components/layout/ApiStatusIndicator";
-import { ThemeToggle } from "@/components/layout/ThemeToggle";
-import { LanguageSelector } from "@/components/locale/LanguageSelector";
+import { usePathname } from "next/navigation";
+import { Menu } from "lucide-react";
+import { Button } from "@/components/buttons/button";
 import { useLocale } from "@/components/locale/LocaleProvider";
 import { useLibrarySearch } from "@/components/layout/LibrarySearchContext";
-import { useDisplayName } from "@/components/profile/DisplayNameProvider";
-import { Avatar, AvatarFallback } from "@/components/ui/avatar";
-import { Button, buttonVariants } from "@/components/buttons/button";
-import {
-  Dialog,
-  DialogContent,
-  DialogHeader,
-  DialogTitle,
-} from "@/components/ui/dialog";
-import {
-  DropdownMenu,
-  DropdownMenuContent,
-  DropdownMenuItem,
-  DropdownMenuSeparator,
-  DropdownMenuTrigger,
-} from "@/components/ui/dropdown-menu";
-import { Input } from "@/components/ui/input";
-import { topBarCreateSetLinkClassName } from "@/lib/dashboard/createSetCtaLinks";
-import { newRoot } from "@/lib/routes/studySetPaths";
-import { cn } from "@/lib/utils";
-import { ensureStudySetDb, getStudySetMeta } from "@/lib/client/studySetDb";
-import type { StudySetMeta } from "@/types/studySet";
 
-function avatarInitial(displayName: string): string {
-  const t = displayName.trim();
-  if (!t) {
-    return "?";
-  }
-  const first = Array.from(t.normalize("NFC"))[0];
-  return first ? first.toLocaleUpperCase() : "?";
-}
-
-export function AppTopBar() {
-  const router = useRouter();
-  const pathname = usePathname();
-  const {
-    search,
-    setSearch,
-    mobileSearchOpen,
-    setMobileSearchOpen,
-    desktopSearchRef,
-  } = useLibrarySearch();
-  const { displayName } = useDisplayName();
+export function AppTopBar({ hidden = false, onToggleSidebar }: { hidden?: boolean; onToggleSidebar?: () => void }) {
+  const pathname = usePathname() ?? "";
   const { messages } = useLocale();
-  const mobileInputRef = useRef<HTMLInputElement>(null);
-  const [studyMeta, setStudyMeta] = useState<StudySetMeta | null>(null);
-  const [studyMetaError, setStudyMetaError] = useState<string | null>(null);
-
-  const studyRoute = useMemo(() => {
-    if (!pathname) {
-      return null;
-    }
-    const match = pathname.match(/^\/(flashcards|quiz)\/([^/]+)(?:\/done)?$/);
-    if (!match) {
-      return null;
-    }
-    const kind = match[1] as "flashcards" | "quiz";
-    const id = match[2] ?? "";
-    if (!id) {
-      return null;
-    }
-    return { kind, id };
-  }, [pathname]);
-
-  const isStudyMode = studyRoute !== null;
-
-  const loadStudyMeta = useCallback(async () => {
-    if (!studyRoute) {
-      setStudyMeta(null);
-      setStudyMetaError(null);
-      return;
-    }
-    setStudyMetaError(null);
-    try {
-      await ensureStudySetDb();
-      const meta = await getStudySetMeta(studyRoute.id);
-      setStudyMeta(meta ?? null);
-      if (!meta) {
-        setStudyMetaError("Study set not found.");
-      }
-    } catch (e) {
-      setStudyMeta(null);
-      setStudyMetaError(e instanceof Error ? e.message : "Failed to load study set.");
-    }
-  }, [studyRoute]);
-
-  useEffect(() => {
-    if (mobileSearchOpen) {
-      queueMicrotask(() => mobileInputRef.current?.focus());
-    }
-  }, [mobileSearchOpen]);
-
-  useEffect(() => {
-    void loadStudyMeta();
-  }, [loadStudyMeta]);
-
-  return (
-    <header
-      className="sticky top-0 z-40 shrink-0 border-b border-border/60 bg-card/85 backdrop-blur-xl dark:bg-card/80"
-      role="banner"
-    >
-      <div className="mx-auto flex h-16 w-full max-w-[1440px] items-center gap-3 px-3 sm:gap-6 sm:px-8">
-        <div className="flex min-w-0 flex-1 items-center gap-4 lg:gap-8">
-          <Link
-            href="/dashboard"
-            className="flex shrink-0 cursor-pointer items-center gap-2 rounded-sm pr-1 transition-opacity hover:opacity-90 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"
-          >
-            <span
-              className="flex h-9 w-9 shrink-0 items-center justify-center rounded-sm bg-primary text-sm font-bold text-primary-foreground shadow-sm"
-              aria-hidden
-            >
-              D2
-            </span>
-            <span className="font-heading text-lg font-bold tracking-tight text-foreground">
-              Doc2Quiz
-            </span>
-          </Link>
-
-          {isStudyMode ? (
-            <div className="hidden min-w-0 flex-1 justify-center sm:flex md:px-2">
-              <div className="min-w-0 text-center">
-                <div className="line-clamp-1 font-heading text-base font-extrabold tracking-tight text-foreground">
-                  {studyMeta?.title ?? (studyMetaError ? messages.navigation.studySetUnavailable : messages.loading.title)}
-                </div>
-                {studyMeta?.subtitle ? (
-                  <div className="mt-0.5 line-clamp-1 text-xs text-muted-foreground">
-                    {studyMeta.subtitle}
-                  </div>
-                ) : studyMeta?.sourceFileName ? (
-                  <div className="mt-0.5 line-clamp-1 text-xs text-muted-foreground">
-                    {messages.navigation.sourcePrefix}: {studyMeta.sourceFileName}
-                  </div>
-                ) : null}
-              </div>
-            </div>
-          ) : (
-            <div className="hidden min-w-0 flex-1 justify-center sm:flex md:px-2">
-              <div className="relative w-full max-w-md lg:max-w-xl xl:max-w-2xl">
-                <SearchIcon
-                  className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground"
-                  aria-hidden
-                />
-                <Input
-                  ref={desktopSearchRef}
-                  type="search"
-                  value={search}
-                  onChange={(e) => setSearch(e.target.value)}
-                  placeholder={messages.navigation.searchStudySetsPlaceholder}
-                  className="h-9 w-full pl-9"
-                  aria-label={messages.navigation.searchStudySets}
-                />
-              </div>
-            </div>
-          )}
-        </div>
-
-        <div className="flex shrink-0 items-center gap-2 sm:gap-4">
-          {!isStudyMode ? (
-            <Button
-              type="button"
-              variant="outline"
-              size="icon"
-              className="shrink-0 sm:hidden"
-              aria-label={messages.navigation.searchStudySets}
-              onClick={() => setMobileSearchOpen(true)}
-            >
-              <SearchIcon className="size-4" />
-            </Button>
-          ) : null}
-
-          {!isStudyMode ? (
-            <Link href={newRoot()} className={topBarCreateSetLinkClassName}>
-              <span className="sm:hidden">{messages.navigation.newSet}</span>
-              <span className="hidden sm:inline">{messages.navigation.createNewSet}</span>
-            </Link>
-          ) : null}
-
-          <div className="hidden lg:block">
-            <ApiStatusIndicator />
-          </div>
-
-          <ThemeToggle />
-
-          <DropdownMenu>
-            <DropdownMenuTrigger
-              className={cn(
-                buttonVariants({ variant: "ghost", size: "icon" }),
-                "size-10 shrink-0 cursor-pointer rounded-full",
-              )}
-              aria-label={messages.navigation.accountMenu}
-            >
-              <Avatar className="size-10 border-2 border-border">
-                <AvatarFallback className="bg-linear-to-br from-(--d2q-blue) to-chart-4 text-sm font-bold text-white">
-                  {avatarInitial(displayName)}
-                </AvatarFallback>
-              </Avatar>
-            </DropdownMenuTrigger>
-            <DropdownMenuContent align="end" className="w-[min(18rem,calc(100vw-24px))] min-w-48">
-              <div className="border-b border-border px-2 py-2 lg:hidden">
-                <ApiStatusIndicator />
-              </div>
-              <LanguageSelector mode="compact" />
-              <DropdownMenuItem
-                className="min-h-11 cursor-pointer"
-                onClick={() => router.push("/settings")}
-              >
-                {messages.navigation.settings}
-              </DropdownMenuItem>
-              <DropdownMenuSeparator />
-              <DropdownMenuItem
-                className="min-h-11 cursor-pointer"
-                onClick={() => {
-                  window.location.assign("/logout");
-                }}
-              >
-                {messages.navigation.logout}
-              </DropdownMenuItem>
-            </DropdownMenuContent>
-          </DropdownMenu>
-        </div>
-      </div>
-
-      {!isStudyMode ? (
-        <Dialog open={mobileSearchOpen} onOpenChange={setMobileSearchOpen}>
-          <DialogContent className="sm:max-w-md">
-            <DialogHeader>
-              <DialogTitle>{messages.navigation.searchStudySets}</DialogTitle>
-            </DialogHeader>
-            <div className="relative mt-2">
-              <SearchIcon
-                className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground"
-                aria-hidden
-              />
-              <Input
-                ref={mobileInputRef}
-                type="search"
-                value={search}
-                onChange={(e) => setSearch(e.target.value)}
-                placeholder={messages.navigation.searchStudySetsPlaceholder}
-                className="h-10 pl-9"
-                aria-label={messages.navigation.searchStudySets}
-              />
-            </div>
-          </DialogContent>
-        </Dialog>
-      ) : null}
-    </header>
-  );
+  const { search, setSearch, desktopSearchRef } = useLibrarySearch();
+  if (hidden) return null;
+  const title = pathname === "/dashboard" ? messages.navigation.dashboard : pathname === "/help" ? messages.navigation.help : pathname === "/settings" ? messages.navigation.settings : pathname.startsWith("/create") ? messages.navigation.create : messages.navigation.currentSet;
+  const searchable = pathname === "/dashboard";
+  return <header className="sticky top-0 z-40 flex h-14 shrink-0 items-center gap-3 border-b border-border/60 bg-card/85 px-3 backdrop-blur-xl sm:px-6" role="banner"><Button type="button" variant="ghost" size="icon" className="size-11 lg:hidden" onClick={onToggleSidebar} aria-label={messages.navigation.openNavigation}><Menu className="size-5" /></Button><div className="min-w-0 flex-1"><h1 className="truncate font-heading text-base font-bold">{title}</h1></div>{searchable && <input ref={desktopSearchRef} type="search" value={search} onChange={(event) => setSearch(event.target.value)} placeholder={messages.navigation.searchStudySetsPlaceholder} aria-label={messages.navigation.searchStudySets} className="hidden h-9 w-full max-w-md rounded-md border border-border bg-background px-3 text-sm outline-none focus-visible:ring-2 focus-visible:ring-ring sm:block" />}</header>;
 }
diff --git a/src/lib/locale/messages.ts b/src/lib/locale/messages.ts
index 16ef89c..3037d4d 100644
--- a/src/lib/locale/messages.ts
+++ b/src/lib/locale/messages.ts
@@ -4,7 +4,7 @@ export const DEFAULT_LOCALE: Locale = "en";
 export const messageDomains = [
   "loading", "upload", "conversion", "quizGeneration", "flashcardGeneration", "correct",
   "wrong", "retry", "success", "empty", "warning", "streak", "score", "navigation",
-  "toast", "progress", "result", "badge", "secondaryLabel", "settings", "accessibility", "workflows", "pipeline",
+  "toast", "help", "progress", "result", "badge", "secondaryLabel", "settings", "accessibility", "dashboard", "workflows", "pipeline",
 ] as const satisfies readonly (keyof MessageCatalog)[];
 
 const en = {
@@ -29,9 +29,10 @@ const en = {
     studySetUnavailable: "Study set unavailable", sourcePrefix: "Source",
     searchCommands: "Search commands…", noResults: "No results.", navigate: "Navigate",
     newStudySet: "New study set", currentSet: "Current set", review: "Review",
-    practice: "Practice", shortcuts: "Shortcuts", openCommandPalette: "Open command palette",
+    practice: "Practice", shortcuts: "Shortcuts", openCommandPalette: "Open command palette", create: "Create", allSets: "All sets", quizzes: "Quizzes", flashcards: "Flashcards", continueStudying: "Continue studying", mistakeDrills: "Mistake drills", help: "Help", primaryNavigation: "Primary navigation", collapseSidebar: "Collapse sidebar", expandSidebar: "Expand sidebar", openNavigation: "Open navigation", planUsage: "Free plan · usage available",
   },
   toast: { saved: "Changes saved.", failed: "We couldn’t complete this action. Try again." },
+  help: { intro: "Find your way around the library, create workflow, and study sessions.", workflow: "Workflow", workflowBody: "Create a set, review generated items, then start studying from its overview.", shortcuts: "Shortcuts", shortcutsBody: "During quiz practice, use number keys to choose an answer and Enter to continue.", faq: "FAQ", faqBody: "Your source text and generated study items stay associated with the study set you created." },
   progress: { label: "Progress", processing: "Processing", steps: "Processing steps", percent: (value: number) => `${value}% complete` },
   result: { title: "Results", review: "Review answers" },
   badge: { new: "New", complete: "Complete" },
@@ -45,6 +46,22 @@ const en = {
     vietnameseTooltip: "tày là người việt còn gì",
   },
   accessibility: { openMenu: "Open menu", closeDialog: "Close dialog", nextItem: "Next item", previousItem: "Previous item" },
+  dashboard: {
+    library: "Your library", totalSets: (count) => `${count} total`, filterAria: "Filter study sets",
+    filters: { all: "All", ready: "Ready", needs_edit: "Needs edit", in_review: "In review" },
+    sortAria: "Sort study sets", sortRecent: "Sort by: Recent", sortTitle: "Sort by: Title",
+    loadingSets: "Loading your study sets", loadingDashboard: "Loading dashboard",
+    emptyTitle: "No study sets yet.", emptyCta: "Create study set", emptyDescription: "Add study text to create your first set.", emptyAria: "Create a study set. Add study text to build your first set.",
+    addTitle: "Add another set", addDescription: "Add study text to create another set.", addAria: "Add another study set. Paste or upload text to add more content.",
+    noFilterMatch: "No sets match this filter.", noSearchMatch: (query) => `No sets match “${query}” for this filter.`,
+    loadRecovery: "You can still create a set below. If the message mentions a missing database column, apply pending Supabase migrations.",
+    deleteTitle: "Delete study set?", deleteDescription: (title, approved) => `This will remove “${title}” and all of its data${approved ? `, including ${approved} approved items` : ""}. This cannot be undone.`, cancel: "Cancel", deleteAction: "Delete",
+    kinds: { flashcards: "Flip study", quiz: "Multiple choice", studySet: "Study set" }, statuses: { needs_edit: "Needs edit", ready: "Ready", in_progress: "In progress" },
+    units: { cards: (count) => `${count} cards`, questions: (count) => `${count} questions` }, readyPractice: "Ready to practice", percentDone: (percent) => `${percent}% done`, percentComplete: (percent) => `${percent}% complete`,
+    actions: { openEditor: "Open editor", practice: "Practice", resumePractice: "Resume practice", startFlashcards: "Start flashcards", startQuiz: "Start quiz", continueSetup: "Continue setup", review: "Review", drillMistakes: "Drill mistakes", more: "More", moreFor: (title) => `More actions for ${title}`, rename: "Rename", delete: "Delete" },
+    mobile: { aria: "Mobile primary", dashboard: "Dash", library: "Library", create: "Create new study set", settings: "Set" },
+    hero: { welcome: (name) => `Welcome back, ${name}!`, learner: "learner", summary: (editing, ready) => `You have ${editing} ready to edit and ${ready} ready to practice.`, createFirst: "Create your first set", createNew: "Create new set", practice: "Practice", reviewLatest: "Review latest" },
+  },
   workflows: {
     import: { eyebrow: "Build a quiz from your material", file: "File", paste: "Paste", sourceText: "Source text", pastePlaceholder: "Paste your notes, article, or study material here…", youtubeHelp: "We'll fetch and convert the transcript when available.", convert: "Convert document", converting: "Converting…", missingInput: "Add a file, paste text, or enter a YouTube URL before continuing.", unsupportedFile: "That file type isn't supported. Use PDF, Office, images, audio, HTML, CSV, JSON, XML, or plain text.", fallbackError: "Conversion failed. Try a different file or paste the text directly." },
     uploadBox: { drop: "Drag and drop a file here, or", choose: "Choose file", chooseAnother: "Choose another file", formats: "PDF, Word, PowerPoint, Excel, images, audio, HTML, CSV, JSON, XML, or plain text." },
@@ -124,9 +141,10 @@ const vi = {
     studySetUnavailable: "Bộ học không khả dụng", sourcePrefix: "Nguồn",
     searchCommands: "Tìm lệnh…", noResults: "Không có kết quả.", navigate: "Điều hướng",
     newStudySet: "Bộ học mới", currentSet: "Bộ hiện tại", review: "Xem lại",
-    practice: "Luyện tập", shortcuts: "Phím tắt", openCommandPalette: "Mở bảng lệnh",
+    practice: "Luyện tập", shortcuts: "Phím tắt", openCommandPalette: "Mở bảng lệnh", create: "Tạo", allSets: "Tất cả bộ học", quizzes: "Quiz", flashcards: "Flashcard", continueStudying: "Tiếp tục học", mistakeDrills: "Ôn câu sai", help: "Trợ giúp", primaryNavigation: "Điều hướng chính", collapseSidebar: "Thu gọn thanh bên", expandSidebar: "Mở rộng thanh bên", openNavigation: "Mở điều hướng", planUsage: "Gói miễn phí · mức sử dụng",
   },
   toast: { saved: "Đã lưu thay đổi.", failed: "Không thể hoàn tất thao tác này. Hãy thử lại." },
+  help: { intro: "Tìm hiểu thư viện, quy trình tạo bộ và các lượt học.", workflow: "Quy trình", workflowBody: "Tạo bộ học, xem lại nội dung rồi bắt đầu học từ trang tổng quan.", shortcuts: "Phím tắt", shortcutsBody: "Trong quiz, dùng các phím số để chọn đáp án và Enter để tiếp tục.", faq: "Câu hỏi thường gặp", faqBody: "Văn bản nguồn và nội dung tạo ra luôn thuộc về bộ học bạn đã tạo." },
   progress: { label: "Tiến độ", processing: "Đang xử lý", steps: "Các bước xử lý", percent: (value: number) => `Hoàn thành ${value}%` },
   result: { title: "Kết quả", review: "Xem lại đáp án" },
   badge: { new: "Mới", complete: "Hoàn tất" },
@@ -140,6 +158,22 @@ const vi = {
     vietnameseTooltip: "tày là người việt còn gì",
   },
   accessibility: { openMenu: "Mở menu", closeDialog: "Đóng hộp thoại", nextItem: "Mục tiếp theo", previousItem: "Mục trước" },
+  dashboard: {
+    library: "Thư viện", totalSets: (count) => `${count} bộ`, filterAria: "Lọc bộ học",
+    filters: { all: "Tất cả", ready: "Sẵn sàng", needs_edit: "Cần sửa", in_review: "Đang duyệt" },
+    sortAria: "Sắp xếp bộ học", sortRecent: "Xếp: Gần đây", sortTitle: "Xếp: Tiêu đề",
+    loadingSets: "Đang tải các bộ học", loadingDashboard: "Đang tải trang chủ",
+    emptyTitle: "Chưa có bộ học nào.", emptyCta: "Tạo bộ học", emptyDescription: "Thêm tài liệu học để tạo bộ đầu tiên.", emptyAria: "Tạo bộ học. Thêm tài liệu để tạo bộ đầu tiên.",
+    addTitle: "Thêm bộ khác", addDescription: "Thêm tài liệu để tạo bộ học khác.", addAria: "Thêm bộ học khác bằng cách dán hoặc tải tài liệu lên.",
+    noFilterMatch: "Không có bộ nào khớp bộ lọc.", noSearchMatch: (query) => `Không có bộ nào khớp “${query}” và bộ lọc này.`,
+    loadRecovery: "Bạn vẫn có thể tạo bộ bên dưới. Nếu thông báo nhắc tới cột cơ sở dữ liệu bị thiếu, hãy áp dụng các migration Supabase đang chờ.",
+    deleteTitle: "Xóa bộ học?", deleteDescription: (title, approved) => `Thao tác này sẽ xóa “${title}” và toàn bộ dữ liệu${approved ? `, gồm ${approved} mục đã duyệt` : ""}. Không thể hoàn tác.`, cancel: "Hủy", deleteAction: "Xóa",
+    kinds: { flashcards: "Học lật thẻ", quiz: "Trắc nghiệm", studySet: "Bộ học" }, statuses: { needs_edit: "Cần sửa", ready: "Sẵn sàng", in_progress: "Đang học" },
+    units: { cards: (count) => `${count} thẻ`, questions: (count) => `${count} câu hỏi` }, readyPractice: "Sẵn sàng luyện", percentDone: (percent) => `Đã làm ${percent}%`, percentComplete: (percent) => `Hoàn thành ${percent}%`,
+    actions: { openEditor: "Mở trình sửa", practice: "Luyện tập", resumePractice: "Tiếp tục luyện", startFlashcards: "Bắt đầu flashcard", startQuiz: "Bắt đầu quiz", continueSetup: "Tiếp tục thiết lập", review: "Xem lại", drillMistakes: "Luyện câu sai", more: "Thêm", moreFor: (title) => `Thêm thao tác cho ${title}`, rename: "Đổi tên", delete: "Xóa" },
+    mobile: { aria: "Điều hướng chính di động", dashboard: "Trang chủ", library: "Thư viện", create: "Tạo bộ học mới", settings: "Cài đặt" },
+    hero: { welcome: (name) => `Chào mừng trở lại, ${name}!`, learner: "bạn", summary: (editing, ready) => `Bạn có ${editing} sẵn sàng chỉnh sửa và ${ready} sẵn sàng luyện tập.`, createFirst: "Tạo bộ đầu tiên", createNew: "Tạo bộ mới", practice: "Luyện tập", reviewLatest: "Xem bộ gần nhất" },
+  },
   workflows: {
     import: { eyebrow: "Tạo quiz từ tài liệu", file: "Tệp", paste: "Dán", sourceText: "Văn bản nguồn", pastePlaceholder: "Dán ghi chú, bài viết hoặc tài liệu học vào đây…", youtubeHelp: "Ứng dụng sẽ lấy và chuyển đổi bản chép lời nếu có.", convert: "Chuyển đổi tài liệu", converting: "Đang chuyển đổi…", missingInput: "Hãy thêm tệp, dán văn bản hoặc nhập URL YouTube trước khi tiếp tục.", unsupportedFile: "Loại tệp này không được hỗ trợ. Hãy dùng PDF, Office, hình ảnh, âm thanh, HTML, CSV, JSON, XML hoặc văn bản thuần.", fallbackError: "Chuyển đổi thất bại. Hãy thử tệp khác hoặc dán văn bản trực tiếp." },
     uploadBox: { drop: "Kéo thả tệp vào đây, hoặc", choose: "Chọn tệp", chooseAnother: "Chọn tệp khác", formats: "PDF, Word, PowerPoint, Excel, hình ảnh, âm thanh, HTML, CSV, JSON, XML hoặc văn bản thuần." },
diff --git a/src/lib/locale/types.ts b/src/lib/locale/types.ts
index 88aa48c..9ef95fd 100644
--- a/src/lib/locale/types.ts
+++ b/src/lib/locale/types.ts
@@ -48,8 +48,10 @@ export type MessageCatalog = {
     practice: string;
     shortcuts: string;
     openCommandPalette: string;
+    create: string; allSets: string; quizzes: string; flashcards: string; continueStudying: string; mistakeDrills: string; help: string; primaryNavigation: string; collapseSidebar: string; expandSidebar: string; openNavigation: string; planUsage: string;
   };
   toast: { saved: string; failed: string };
+  help: { intro: string; workflow: string; workflowBody: string; shortcuts: string; shortcutsBody: string; faq: string; faqBody: string };
   progress: { label: string; processing: string; steps: string; percent: (value: number) => string };
   result: { title: string; review: string };
   badge: { new: string; complete: string };
@@ -63,6 +65,23 @@ export type MessageCatalog = {
     vietnameseTooltip: string;
   };
   accessibility: { openMenu: string; closeDialog: string; nextItem: string; previousItem: string };
+  dashboard: {
+    library: string; totalSets: (count: string) => string; filterAria: string;
+    filters: Record<"all" | "ready" | "needs_edit" | "in_review", string>;
+    sortAria: string; sortRecent: string; sortTitle: string; loadingSets: string; loadingDashboard: string;
+    emptyTitle: string; emptyCta: string; emptyDescription: string; emptyAria: string;
+    addTitle: string; addDescription: string; addAria: string;
+    noFilterMatch: string; noSearchMatch: (query: string) => string; loadRecovery: string;
+    deleteTitle: string; deleteDescription: (title: string, approved: string | null) => string;
+    cancel: string; deleteAction: string;
+    kinds: Record<"flashcards" | "quiz" | "studySet", string>;
+    statuses: Record<"needs_edit" | "ready" | "in_progress", string>;
+    units: { cards: (count: string) => string; questions: (count: string) => string };
+    readyPractice: string; percentDone: (percent: string) => string; percentComplete: (percent: string) => string;
+    actions: { openEditor: string; practice: string; resumePractice: string; startFlashcards: string; startQuiz: string; continueSetup: string; review: string; drillMistakes: string; more: string; moreFor: (title: string) => string; rename: string; delete: string };
+    mobile: { aria: string; dashboard: string; library: string; create: string; settings: string };
+    hero: { welcome: (name: string) => string; learner: string; summary: (editing: string, ready: string) => string; createFirst: string; createNew: string; practice: string; reviewLatest: string };
+  };
   workflows: {
     import: Record<"eyebrow" | "file" | "paste" | "sourceText" | "pastePlaceholder" | "youtubeHelp" | "convert" | "converting" | "missingInput" | "unsupportedFile" | "fallbackError", string>;
     uploadBox: Record<"drop" | "choose" | "chooseAnother" | "formats", string>;
