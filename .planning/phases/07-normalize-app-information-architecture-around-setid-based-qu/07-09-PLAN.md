---
phase: 07-normalize-app-information-architecture-around-setid-based-qu
plan: 09
type: execute
wave: 6
depends_on: [07-08]
files_modified: [src/app/(app)/edit, src/app/(app)/sets, src/app/(app)/flashcards, src/app/(app)/quiz/[id], scripts/verify-phase7-route-smoke.mjs, package.json, .planning/phases/07-normalize-app-information-architecture-around-setid-based-qu/07-VALIDATION.md]
autonomous: false
requirements: [IA-01, IA-02, IA-03, IA-04, IA-05, IA-06, IA-07, IA-08, IA-09, IA-10]
must_haves:
  truths: ["Legacy route folders are deleted only after recorded zero references", "Canonical protected routes are present in build inventory and return authenticated non-3xx responses", "Representative old paths return 404 with no Location"]
  artifacts:
    - {path: scripts/verify-phase7-route-smoke.mjs, provides: deterministic manifest plus production HTTP hard-cutover smoke}
    - {path: .planning/phases/07-normalize-app-information-architecture-around-setid-based-qu/07-VALIDATION.md, provides: final evidence matrix}
  key_links:
    - {from: scripts/verify-phase7-route-smoke.mjs, to: .next/server/app-paths-manifest.json and production server, via: inventory and authenticated HTTP assertions, pattern: "app-paths-manifest|Location|404"}
---
<objective>Delete legacy route files after the zero-reference gate, then run deterministic route smoke, full quality gates, and human acceptance.</objective>
<execution_context>@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/07-normalize-app-information-architecture-around-setid-based-qu/07-DIRTY-PREFLIGHT.txt
@.planning/phases/07-normalize-app-information-architecture-around-setid-based-qu/07-08-SUMMARY.md
@.planning/phases/07-normalize-app-information-architecture-around-setid-based-qu/07-VALIDATION.md</context>
<tasks>
<task type="auto"><name>Task 1: Pass callers gate, delete exact roots, then pass full audit</name><files>src/app/(app)/edit, src/app/(app)/sets, src/app/(app)/flashcards, src/app/(app)/quiz/[id]</files><action>First run `npm run verify:phase7-callers` and require literal zero plus matching recorded Plan 08 evidence; abort deletion otherwise. Capture scoped diffs and compare to 07-DIRTY-PREFLIGHT. Delete exactly and completely these four roots: `src/app/(app)/edit`, `src/app/(app)/sets`, `src/app/(app)/flashcards`, and `src/app/(app)/quiz/[id]`. The canonical replacement is `src/app/(app)/quiz/[setId]`, so do not delete it. Then run `npm run verify:phase7-references`; this full post-deletion audit has no legacy-root exclusion, includes filesystem route inventory and product `[id]` segment detection, and must report literal zero before route smoke or any quality gate may start. Preserve APIs, domain plural identifiers, user content, and unrelated dirty files; never reset/checkout/clean/blanket replace or commit.</action><verify><automated>npm run verify:phase7-callers && npm run verify:phase7-references && git diff --check</automated></verify><done>Both audits report literal zero in order, all four exact legacy roots are absent, canonical `[setId]` source remains, and preservation comparison passes.</done></task>
<task type="auto" tdd="true"><name>Task 2: Add deterministic real-auth route and 404 smoke automation</name><files>scripts/verify-phase7-route-smoke.mjs, package.json</files><behavior>- Build inventory asserts every canonical D-02 route page key exists and every representative legacy key is absent.
- No backdoor is added: `src/lib/supabase/auth-guard.ts`, `src/lib/supabase/middlewareClient.ts`, and `src/proxy.ts` remain byte-for-byte unmodified. The script reuses their real Supabase cookie boundary.
- Script creates a random temporary Supabase Auth user and owned quiz/flashcard fixture sets through the existing admin/database boundary, signs in through Supabase password auth, forwards the returned auth cookies to the spawned production server, and deletes all temporary rows/user in `finally`.
- Fail-closed negatives prove `D2Q_ROUTE_SMOKE_AUTH=1` alone does not authenticate, a random `x-d2q-route-smoke-secret` header alone does not authenticate, both together still do not authenticate, and an ordinary production startup has no smoke path. Repository scan proves no `D2Q_ROUTE_SMOKE_SECRET` value/default is committed.
- Canonical protected URLs with fixture setIds return 200 and never 3xx; `/dashboard` normalization is asserted separately.
- Old paths `/edit/new`, `/edit/new/quiz`, `/edit/quiz/{id}`, `/sets/{id}/source`, `/flashcards/{id}`, `/quiz/{id}/done`, and `/quiz/{id}?review=mistakes` return exactly 404, are not 3xx, and have no `Location`.</behavior><action>After Task 1 full audit passes, create the Node script with manifest and HTTP phases. Require runtime-provided Supabase URL, anon key, and service-role key; generate random user credentials and fixture IDs in memory with no defaults or committed secrets. Spawn `npm start -- -p 4317` without any `D2Q_ROUTE_SMOKE_AUTH` or secret environment variables, poll readiness, test unauthenticated and real-cookie requests with `redirect: manual`, and place child termination plus fixture/user cleanup in nested `finally` blocks so cleanup runs on assertion, timeout, signal, or success. Add `verify:phase7-routes` as `npm run build && node scripts/verify-phase7-route-smoke.mjs`; no conditional/manual fallback and no auth/proxy source modifications.</action><verify><automated>npm run verify:phase7-routes</automated></verify><done>One command deterministically proves canonical inventory/HTTP behavior and old-path exact 404/no-Location behavior under authenticated fixtures.</done></task>
<task type="checkpoint:human-verify" gate="blocking"><name>Task 3: Run final gates and approve matrix</name><files>.planning/phases/07-normalize-app-information-architecture-around-setid-based-qu/07-VALIDATION.md</files><action>Run all commands first, record results, then pause for the human matrix. Correct failures and rerun before approval.</action><what-built>Hard-cutover IA with route smoke, durable resume/mistakes, responsive shell, and preserved content.</what-built><how-to-verify>Follow 07-VALIDATION.md at 1440px/375px EN/VI for create→review→edit→play→results, exact reload/close resume, mistakes, URL filters, sidebar/mobile focus modes, accessibility, and content/dirty-hunk preservation.</how-to-verify><verify><automated>npm run verify:phase7-callers && npm run verify:phase7-references && npm run verify:phase7-routes && npm run typecheck && npm run lint && npm test -- --run && npm run build</automated></verify><done>All automated gates pass and user approves every manual matrix cell.</done><resume-signal>Type "approved" or list failing cells.</resume-signal></task>
</tasks>
<threat_model>Real Supabase authentication boundary. T-07-14 Elevation mitigated by adding no backdoor, exercising real cookies, proving smoke flag/header combinations cannot authenticate, requiring runtime-only admin credentials, and cleaning temporary identities/data. T-07-15 shadow redirects mitigated by redirect-manual exact assertions. T-07-WS preservation. T-07-SC no installs.</threat_model>
<verification>Exact final command passes and manual matrix is approved.</verification>
<success_criteria>Legacy URLs deterministically 404 without Location and canonical authenticated routes are build/runtime proven.</success_criteria>
<output>Create `07-09-SUMMARY.md` with smoke output and final matrix.</output>
