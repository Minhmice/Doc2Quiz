---
phase: 07-normalize-app-information-architecture-around-setid-based-qu
plan: 08
type: execute
wave: 5
depends_on: [07-03, 07-04, 07-05, 07-06, 07-07]
files_modified: [next.config.ts, src/components/layout/RoutePrefetch.tsx, src/components/layout/CommandPalette.tsx, src/hooks/useStudySetProductSurfaceRedirect.ts, src/lib/routing/studySetContentKindRedirects.ts, scripts/audit-phase7-route-callers.mjs, scripts/audit-phase7-route-references.mjs, package.json]
autonomous: true
requirements: [IA-01, IA-02, IA-05, IA-06, IA-07, IA-10]
must_haves:
  truths: ["All live callers/config/tests use canonical routes before deletion begins", "A deterministic executable audit records zero forbidden references"]
  artifacts:
    - {path: scripts/audit-phase7-route-references.mjs, provides: zero-reference precondition gate with allowlisted API/domain/archive exclusions}
  key_links:
    - {from: package.json, to: scripts/audit-phase7-route-references.mjs, via: verify:phase7-references command, pattern: "verify:phase7-references"}
---
<objective>Migrate residual callers and establish the recorded zero-forbidden-reference precondition for legacy deletion.</objective>
<execution_context>@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/07-normalize-app-information-architecture-around-setid-based-qu/07-DIRTY-PREFLIGHT.txt
@.planning/phases/07-normalize-app-information-architecture-around-setid-based-qu/07-CONTEXT.md
@next.config.ts
@package.json</context>
<tasks>
<task type="auto"><name>Task 1: Migrate residual callers, config, and tests without deleting routes</name><files>next.config.ts, src/components/layout/RoutePrefetch.tsx, src/components/layout/CommandPalette.tsx, src/hooks/useStudySetProductSurfaceRedirect.ts, src/lib/routing/studySetContentKindRedirects.ts, package.json</files><action>Capture scoped pre-task diffs and compare to 07-DIRTY-PREFLIGHT. Per D-01/D-03/D-05, update all live links, prefetch, command items, middleware/guards/loaders/tests and config to canonical helpers; remove `next.config.ts` redirects and obsolete redirect verification command. Do not delete legacy route folders in this task. Preserve unrelated hunks and do not commit overlapping dirty work.</action><verify><automated>npm test -- src/lib/routes/studySetPaths.test.ts src/lib/dashboard/studySetDashboardLinks.test.ts --run && npm run typecheck && git diff --check</automated></verify><done>Callers/config/tests are canonical while legacy route files remain available for the subsequent gated deletion.</done></task>
<task type="auto" tdd="true"><name>Task 2: Create satisfiable pre-deletion callers gate and full-audit script</name><files>scripts/audit-phase7-route-callers.mjs, scripts/audit-phase7-route-references.mjs, package.json</files><behavior>- `verify:phase7-callers` scans all live caller/config/helper/test/import/redirect/middleware/loader files for legacy literals/imports and fails on findings.
- The callers scan excludes only these exact pending-deletion roots: `src/app/(app)/edit/**`, `src/app/(app)/sets/**`, `src/app/(app)/flashcards/**`, `src/app/(app)/quiz/[id]/**`; no other source exclusion is allowed.
- `verify:phase7-references` is the post-deletion full audit: it uses no legacy-root exclusion, scans live references plus filesystem route inventory, and fails on legacy product `[id]` segments.
- Both scripts exclude generated/dependency/planning archives and allow only API `/api/study-sets/[id]/flashcards/generate` plus database/domain plural identifiers by exact enumerated matcher.</behavior><action>Implement both deterministic sorted Node audits without dependencies. Add package commands `verify:phase7-callers` and `verify:phase7-references`. In Plan 08 run only the satisfiable callers audit and record literal zero; do not claim the full audit can pass while route roots still exist. Compare post-task scoped diff to baseline.</action><verify><automated>npm run verify:phase7-callers</automated></verify><done>Pre-deletion callers audit exits 0 with literal `0 forbidden caller references`; self-test fixtures prove references outside the four roots fail, while files inside exactly those roots are deferred.</done></task>
</tasks>
<threat_model>Repository boundary. T-07-13 shadow legacy caller mitigated deterministic gate. T-07-WS preservation baseline. T-07-SC no installs.</threat_model>
<verification>Caller migration tests/typecheck pass and `npm run verify:phase7-callers` records literal zero before deletion.</verification>
<success_criteria>Deletion cannot start until a machine-verifiable zero-reference precondition exists.</success_criteria>
<output>Create `07-08-SUMMARY.md` containing the exact zero-caller-reference output and the four deferred route roots.</output>
