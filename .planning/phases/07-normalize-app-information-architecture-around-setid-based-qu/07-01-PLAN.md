---
phase: 07-normalize-app-information-architecture-around-setid-based-qu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/07-normalize-app-information-architecture-around-setid-based-qu/07-DIRTY-PREFLIGHT.txt, src/lib/routes/studySetPaths.ts, src/lib/routes/studySetPaths.test.ts, src/lib/dashboard/studySetDashboardLinks.ts, src/lib/dashboard/studySetDashboardLinks.test.ts]
autonomous: false
requirements: [IA-01, IA-03, IA-06, IA-07, IA-09]
must_haves:
  truths: ["Every executor starts from a recorded dirty-workspace preservation baseline", "Canonical helpers emit only singular setId routes"]
  artifacts:
    - {path: .planning/phases/07-normalize-app-information-architecture-around-setid-based-qu/07-DIRTY-PREFLIGHT.txt, provides: immutable scoped dirty-file inventory and user safety authorization}
    - {path: src/lib/routes/studySetPaths.test.ts, provides: exhaustive canonical route contracts}
  key_links:
    - {from: all Phase 7 plans, to: 07-DIRTY-PREFLIGHT.txt, via: required pre/post scoped-diff protocol, pattern: "07-DIRTY-PREFLIGHT"}
---
<objective>Establish the mandatory dirty-workspace safety gate and canonical route vocabulary consumed by every Phase 7 plan.</objective>
<execution_context>@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/STATE.md
@.planning/phases/06-bilingual-en-vi-language-selector-and-reusable-contextual-sl/06-06-SUMMARY.md
@.planning/phases/07-normalize-app-information-architecture-around-setid-based-qu/07-CONTEXT.md
@src/lib/routes/studySetPaths.ts
@src/lib/dashboard/studySetDashboardLinks.ts</context>
<tasks>
<task type="auto"><name>Task 1: Record scoped dirty-workspace preservation baseline</name><files>.planning/phases/07-normalize-app-information-architecture-around-setid-based-qu/07-DIRTY-PREFLIGHT.txt</files><action>Before source edits, record `git status --short`, `git diff --name-status`, and per-file `git diff -- <all files owned by Plans 01–09>`, explicitly including `src/lib/supabase/auth-guard.ts`, `src/lib/supabase/middlewareClient.ts`, `src/proxy.ts`, `scripts/audit-phase7-route-callers.mjs`, `scripts/audit-phase7-route-references.mjs`, `scripts/verify-phase7-route-smoke.mjs`, and `package.json` even though the frozen smoke design leaves the three auth/proxy source files unmodified plus explicit identification of Phase 6 Plan 06-06 dashboard/locale hunks. Record hashes of the captured diff text. This artifact is evidence, not documentation scope. Prohibit `git reset`, `git checkout`, `git clean`, blanket file replacement, staging, and commits. Every downstream plan must compare its pre-task scoped diff to this baseline and its post-task diff to its own pre-task snapshot. If an owned file already has unrelated hunks or an atomic commit cannot isolate Phase 7 work, execution must enter no-commit safety mode and obtain the blocking authorization below rather than capturing unrelated work.</action><verify><automated>git status --short && git diff --check</automated></verify><done>Baseline inventories every overlapping dirty file/hunk and states non-destructive/no-commit rules for all plans.</done></task>
<task type="checkpoint:human-verify" gate="blocking"><name>Task 2: Authorize dirty-file no-commit safety mode</name><files>.planning/phases/07-normalize-app-information-architecture-around-setid-based-qu/07-DIRTY-PREFLIGHT.txt</files><action>Pause before any source edit. Present the overlap inventory and request explicit authorization to edit surgically without commits wherever Phase 7 ownership overlaps existing dirty work.</action><what-built>Scoped preservation inventory and prohibited-operation contract.</what-built><how-to-verify>Confirm listed pre-existing hunks, especially Phase 6 06-06 dashboard/locale work, must remain byte-for-byte unless a Phase 7 task explicitly changes the same lines.</how-to-verify><verify><automated>git diff --check</automated></verify><done>User explicitly authorizes no-commit surgical execution for identified overlaps.</done><resume-signal>Type "authorized" or identify files that must not be touched.</resume-signal></task>
<task type="auto" tdd="true"><name>Task 3: Lock canonical route and dashboard-link contracts</name><files>src/lib/routes/studySetPaths.ts, src/lib/routes/studySetPaths.test.ts, src/lib/dashboard/studySetDashboardLinks.ts, src/lib/dashboard/studySetDashboardLinks.test.ts</files><behavior>- Every D-02 route preserves setId and uses `/results`, `/drill-mistake`, `/create`, `/quiz/create`, `/flashcard/create`.
- No compatibility helper emits `/edit`, `/sets`, plural `/flashcards`, `/done`, or `review=mistakes` per D-01–D-05.
- Dashboard overview/review/edit/play/results/drill destinations are type-correct per D-09.</behavior><action>Capture a scoped pre-task diff, write failing tests, replace helpers, then compare post-task diff against both pre-task snapshot and 07-DIRTY-PREFLIGHT; preserve all unrelated hunks. Use explicit canonical exports and remove deprecated aliases.</action><verify><automated>npm test -- src/lib/routes/studySetPaths.test.ts src/lib/dashboard/studySetDashboardLinks.test.ts --run && git diff --check</automated></verify><done>Tests pass, outputs are canonical, and scoped-diff comparison shows no unrelated hunk loss.</done></task>
</tasks>
<threat_model>Trust boundary: route parameter to owner data. T-07-01 Tampering mitigate with central builders. T-07-WS Tampering mitigate with recorded diffs and prohibited destructive git operations. T-07-SC no installs.</threat_model>
<verification>Authorization is recorded, route tests pass, and preservation diff check passes.</verification>
<success_criteria>All downstream plans have an authorized safety baseline and tested route contracts.</success_criteria>
<output>Create `07-01-SUMMARY.md`; retain `07-DIRTY-PREFLIGHT.txt` for Plans 02–09.</output>
