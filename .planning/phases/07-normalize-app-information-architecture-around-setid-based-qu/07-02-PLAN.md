---
phase: 07-normalize-app-information-architecture-around-setid-based-qu
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [supabase/migrations/20260726150000_resumable_study_sessions.sql, src/types/studySession.ts, src/lib/client/activityTracking.ts, src/lib/client/activityTracking.test.ts]
autonomous: true
requirements: [IA-08, IA-09]
must_haves:
  truths: ["Quiz and flashcard resume restores the exact actionable item and interaction state", "Unresolved mistakes are durable independent records for both modes"]
  artifacts:
    - {path: supabase/migrations/20260726150000_resumable_study_sessions.sql, provides: resumable sessions plus unresolved mistake ledger with RLS/indexes}
    - {path: src/lib/client/activityTracking.test.ts, provides: state-machine, concurrency, reconciliation, and mistake-ledger tests}
  key_links:
    - {from: src/lib/client/activityTracking.ts, to: public.study_sessions and public.study_mistakes, via: owner-scoped Supabase operations, pattern: "study_(sessions|mistakes)"}
---
<objective>Define and test durable resume and unresolved-mistake state machines for both study modes.</objective>
<execution_context>@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/07-normalize-app-information-architecture-around-setid-based-qu/07-DIRTY-PREFLIGHT.txt
@supabase/migrations/20260725120000_v21_baseline.sql
@src/lib/client/activityTracking.ts
@src/lib/client/activityTracking.test.ts</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Specify additive session and mistake schemas</name><files>supabase/migrations/20260726150000_resumable_study_sessions.sql, src/types/studySession.ts</files><behavior>- Session stores owner/set/mode/practice, ordered itemIds, currentItemId, nextItemId, revision, answers or known-state payload, started/updated/completed timestamps.
- `currentItemId` is the item displayed after restore; `nextItemId` is the next unanswered/unrated item after the last committed semantic action.
- Mistake row stores owner, set, item, mode, unresolved, mistakeCount, firstMistakeAt, lastMistakeAt, lastPracticedAt, resolvedAt; unique owner+set+mode+item.</behavior><action>First capture and compare scoped diffs against 07-DIRTY-PREFLIGHT. Add tables/constraints/RLS/indexes additively without altering records/IDs. Session uniqueness permits multiple unfinished sessions but each row has monotonic integer revision and updated_at. Mistake upsert increments count and clears resolution; resolution sets unresolved false/resolved_at; indexes support owner unresolved mode and sort by count then lastPracticedAt. Validate mode/practice enums and owner/set FKs.</action><verify><automated>npm run typecheck && git diff --check</automated></verify><done>Schema exactly represents both state machines, preserves existing data, and supports unified indexed queries.</done></task>
<task type="auto" tdd="true"><name>Task 2: Implement and test concurrency-safe state machines</name><files>src/lib/client/activityTracking.ts, src/lib/client/activityTracking.test.ts</files><behavior>- Save occurs after answer commit/card rating/navigation and on pagehide/visibility hidden via final best-effort flush; reload/browser close restores committed state.
- Restore validates owner/set/mode/practice; reconciles missing items by dropping them, preserves persisted order for surviving IDs, appends newly added IDs deterministically, and selects currentItemId else next surviving item.
- Optimistic update requires expected revision/updated_at; stale writes are rejected/refetched, never overwrite newer progress.
- Completion occurs only after final item semantic action is persisted, atomically sets completedAt, and excludes row from unfinished queries.
- Quiz answers/correctness and flashcard known/rating state restore exactly.
- Mistake upsert increments, repeated miss increments, correct resolution marks resolved, cross-mode rows remain isolated, unified query returns unresolved sets ordered total count desc then last practiced desc, and empty result is stable.</behavior><action>Write mocked Supabase tests first, then implement typed start/save/restore/complete/list/select functions and durable mistake increment/resolve/query functions. Resume payload stores IDs and interaction state, never generated text. Smart resume returns exact session, picker list, recent overview, or empty. Preserve existing completion APIs while adapting them to update the durable ledger.</action><verify><automated>npm test -- src/lib/client/activityTracking.test.ts --run && git diff --check</automated></verify><done>All state-transition, stale-write, reconciliation, boundary, cross-mode, ordering, empty, and destination-input tests pass; post-task diff preserves baseline hunks.</done></task>
</tasks>
<threat_model>Browser to Supabase. T-07-02 IDOR mitigated by owner RLS. T-07-03 stale-write tampering mitigated by revision compare-and-swap. T-07-04 DoS mitigated bounded item/payload contracts. T-07-WS preservation gate. T-07-SC no installs.</threat_model>
<verification>Focused tests cover every listed transition and scoped diff is preservation-safe.</verification>
<success_criteria>Durable resume and mistake behavior is deterministic across reload, close, edits, concurrency, both modes, and completion.</success_criteria>
<output>Create `07-02-SUMMARY.md` with state-machine invariants and preservation check.</output>
