---
phase: 05-flashcards-e2e
plan: 01
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - prompt/flashcard_generator_v1.json
  - src/lib/pipeline/flashcardPrompt.ts
  - src/lib/pipeline/flashcardSchemas.ts
  - src/lib/pipeline/flashcardPrompt.test.ts
  - src/lib/pipeline/flashcardSchemas.test.ts
autonomous: true
requirements:
  - FLASH-01
  - FLASH-02
  - FLASH-03
  - FLASH-04
user_setup: []
must_haves:
  truths:
    - "Locked flashcard_generator_v1.json exists with system, tasks, constraints, output_schema per D-07"
    - "Zod body schema accepts learningGoal, coverage union, amount union (5–60 custom) per D-12"
    - "Zod output schema validates detected_format, cards with front/back, optional concept_id per D-09"
    - "Prompt text lives only in JSON — flashcardPrompt.ts loads and assembles messages without hardcoded LLM instructions"
  artifacts:
    - path: prompt/flashcard_generator_v1.json
      provides: "Locked flashcard generation contract"
      contains: "flashcard_generator"
    - path: src/lib/pipeline/flashcardPrompt.ts
      provides: "Runtime prompt load + message assembly"
      exports: ["loadFlashcardPrompt", "buildFlashcardGeneratorMessages", "FLASHCARD_PROMPT_VERSION"]
    - path: src/lib/pipeline/flashcardSchemas.ts
      provides: "Zod mirror of output_schema + route body"
      exports: ["flashcardGeneratorOutputSchema", "flashcardGenerateBodySchema"]
  key_links:
    - from: src/lib/pipeline/flashcardPrompt.ts
      to: prompt/flashcard_generator_v1.json
      via: "readFile at runtime (mirror quizPrompt D-07)"
      pattern: "flashcard_generator_v1\\.json"
    - from: src/lib/pipeline/flashcardSchemas.ts
      to: prompt/flashcard_generator_v1.json
      via: "hand-written Zod matching output_schema fields"
      pattern: "detected_format"
---

<objective>
Establish Phase 5 foundation: locked flashcard prompt JSON, runtime loader, Zod validation contracts for wizard body (FLASH-01–03) and LLM output (FLASH-04).

Purpose: Wave 2 `runFlashcardGenerate` depends on prompt contract (D-07), output validation (D-09), and wizard request body schema (D-12) before any LLM calls or DB inserts.
Output: `prompt/flashcard_generator_v1.json`, `flashcardPrompt.ts`, `flashcardSchemas.ts`, unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-flashcards-e2e/05-CONTEXT.md
@.planning/phases/05-flashcards-e2e/05-RESEARCH.md
@.planning/phases/05-flashcards-e2e/05-PATTERNS.md
@prompt/quiz_generator_v1.json
@src/lib/pipeline/quizPrompt.ts
@src/lib/pipeline/quizSchemas.ts
@.planning/phases/04-quiz-pipeline/04-01-SUMMARY.md

<interfaces>
From 05-CONTEXT locked decisions:
- D-03: wizard vocabulary memorize | understand | exam_preparation; entire_document | selected_sections; recommended | custom 5–60
- D-05: do NOT use legacy FlashcardGenerationConfig / quick_recall / focusMode
- D-07: prompt/flashcard_generator_v1.json mirrors quiz_generator_v1.json structure
- D-09: per-card { front, back, format?, concept_id?, section_key?, source_excerpt? }
- D-12: POST body { learningGoal, coverage, amount }

From quiz_generator_v1.json (pattern only):
- Fields: name, version, system, input (template vars), tasks[], output_schema, constraints[]

Proposed output_schema (RESEARCH — implement exactly in JSON + Zod):
- detected_format: term_definition | question_answer | cloze | mixed (FLASH-04)
- recommended_count: number (1–60)
- concepts[]: { concept_id: concept_NNN, label, section_key?, importance? }
- cards[]: front, back, format?, concept_id?, section_key?, source_excerpt?
- warnings[]: string[]

Input template vars (D-06, D-07): study_set_id, title, language, learning_goal, canonical_markdown, sections_json, extracted_questions_json, requested_count, coverage_mode
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create locked prompt/flashcard_generator_v1.json (D-07)</name>
  <files>prompt/flashcard_generator_v1.json</files>
  <action>
    Create prompt/flashcard_generator_v1.json per D-07 and RESEARCH Pattern 3 proposed schema:

    - name: "flashcard_generator", version: "1.0"
    - system: canonical knowledge only — never raw_markdown or external facts (D-06)
    - input template vars: {{study_set_id}}, {{title}}, {{language}}, {{learning_goal}}, {{canonical_markdown}}, {{sections_json}}, {{extracted_questions_json}}, {{requested_count}}, {{coverage_mode}}
    - tasks[]: detect best card format (FLASH-04), assign concept_001… concept_ids, recommend count based on learning_goal, generate one card per concept with front/back grounded in canonical text, adapt style per learning_goal (memorize=term→def, understand=why/how, exam_preparation=exam Q→A), fewer cards when content thin
    - output_schema: detected_format, recommended_count, concepts[], cards[], warnings[]
    - constraints[]: canonical only, non-empty front/back, no duplicate concept_id, no invented facts, valid JSON only

    Do NOT reference quick_recall, focusMode, or vision flashcard vocabulary (D-05). Wording is executor discretion — structure and field names are locked. Do not duplicate this JSON inside TypeScript.
  </action>
  <verify>
    <automated>grep -q "flashcard_generator" prompt/flashcard_generator_v1.json &amp;&amp; grep -q "canonical_markdown" prompt/flashcard_generator_v1.json &amp;&amp; grep -q "detected_format" prompt/flashcard_generator_v1.json</automated>
  </verify>
  <done>flashcard_generator_v1.json exists with full contract matching D-07/D-09 field names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: flashcardPrompt.ts + flashcardSchemas.ts + unit tests</name>
  <files>src/lib/pipeline/flashcardPrompt.ts, src/lib/pipeline/flashcardSchemas.ts, src/lib/pipeline/flashcardPrompt.test.ts, src/lib/pipeline/flashcardSchemas.test.ts</files>
  <behavior>
    - loadFlashcardPrompt returns version "1.0" from prompt/flashcard_generator_v1.json (D-07)
    - buildFlashcardGeneratorMessages assembles system from spec.system + numbered tasks + bullet constraints + serialized output_schema — never hardcode prompt prose in TS
    - substituteFlashcardInput replaces {{study_set_id}}, {{learning_goal}}, {{canonical_markdown}}, etc.
    - flashcardGenerateBodySchema accepts valid wizard payloads per D-12 (FLASH-01–03)
    - flashcardGenerateBodySchema rejects invalid learningGoal, empty sectionKeys, count 4 and 61
    - flashcardGeneratorOutputSchema accepts valid fixture with detected_format and cards[]
    - flashcardGeneratorOutputSchema rejects empty front/back and invalid detected_format enum
    - concept_id regex accepts concept_001 pattern; section_key accepts sec_001 pattern
  </behavior>
  <action>
    Implement flashcardPrompt.ts mirroring quizPrompt.ts:
    - loadFlashcardPrompt(): read prompt/flashcard_generator_v1.json via node:fs/promises + path.join(process.cwd(), ...), module-level cache
    - substituteFlashcardInput(template, vars): replace /\{\{(\w+)\}\}/g
    - buildFlashcardGeneratorMessages(spec, vars): return { system, user } where user payload is JSON with substituted canonical inputs only (D-06 — never include raw_markdown)
    - export FLASHCARD_PROMPT_VERSION from loaded spec.version

    Implement flashcardSchemas.ts per D-12 and D-09:
    - flashcardLearningGoalSchema: z.enum(["memorize", "understand", "exam_preparation"]) (FLASH-01)
    - flashcardCoverageSchema: z.union([z.literal("entire_document"), z.object({ sectionKeys: z.array(sectionKeySchema).min(1) })]) (FLASH-02)
    - flashcardAmountSchema: z.union([z.literal("recommended"), z.object({ count: z.number().int().min(5).max(60) })]) (FLASH-03)
    - flashcardGenerateBodySchema: { learningGoal, coverage, amount }
    - flashcardFormatSchema: term_definition | question_answer | cloze | mixed
    - generatedFlashcardSchema: front/back min(1), optional format, concept_id, section_key, source_excerpt
    - flashcardGeneratorOutputSchema: detected_format, recommended_count 1–60, cards min(1), warnings default []
    - export types FlashcardGeneratorOutput, GeneratedFlashcard, FlashcardGenerateBody

    Write tests first (RED), then implement (GREEN). No DB or LLM calls in this task.
  </action>
  <verify>
    <automated>npx vitest run src/lib/pipeline/flashcardPrompt.test.ts src/lib/pipeline/flashcardSchemas.test.ts --reporter=dot</automated>
  </verify>
  <done>Prompt loader and Zod modules pass unit tests; FLASH-01–04 validation gates exist before Wave 2 generation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→filesystem | Prompt JSON read server-side only; must not ship in client bundle |
| env→server | AI keys stay server-only (D-P4-09) — consumed in Wave 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information disclosure | flashcardPrompt.ts | mitigate | Prompt JSON loaded server-side only; no client import |
| T-05-02 | Tampering | flashcardSchemas.ts | mitigate | Zod rejects malformed LLM shapes before DB insert (D-09) |
| T-05-03 | Tampering | npm/pip/cargo installs | accept | No new packages; existing zod only |
</threat_model>

<verification>
- npx vitest run src/lib/pipeline/flashcardPrompt.test.ts src/lib/pipeline/flashcardSchemas.test.ts
- npm run typecheck
- prompt/flashcard_generator_v1.json contains locked contract fields
</verification>

<success_criteria>
- flashcard_generator_v1.json locked contract on disk (D-07)
- Zod schemas validate FLASH-01–04 shapes (D-09, D-12)
- No prompt text duplicated in TypeScript source
- No legacy vision flashcard vocabulary in schemas (D-05)
</success_criteria>

<output>
Create `.planning/phases/05-flashcards-e2e/05-01-SUMMARY.md` when done
</output>
