---
phase: 05-flashcards-e2e
plan: 03
type: execute
wave: 2
depends_on:
  - 04-03
  - 05-01
files_modified:
  - src/lib/client/studySetDb.ts
  - src/lib/client/studySetDb.test.ts
  - src/lib/client/flashcardGenerateStudySet.ts
  - src/lib/client/flashcardGenerateStudySet.test.ts
autonomous: true
requirements:
  - FLASH-06
  - FLASH-07
user_setup:
  - service: supabase
    why: "Client CRUD for approved_flashcards requires authenticated Supabase"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase project settings"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase project settings"
must_haves:
  truths:
    - "getApprovedFlashcardBank returns cards from approved_flashcards via Supabase RLS (not empty stub) per D-14"
    - "putApprovedFlashcardBankForStudySet upserts edits and deletes removed rows"
    - "postFlashcardGenerate POSTs wizard body to /flashcards/generate and returns typed response per D-12"
    - "FlashcardSession and useDashboardHome can load real card counts after generation"
  artifacts:
    - path: src/lib/client/studySetDb.ts
      provides: "Supabase-backed flashcard bank CRUD"
      exports: ["getApprovedFlashcardBank", "putApprovedFlashcardBankForStudySet"]
    - path: src/lib/client/flashcardGenerateStudySet.ts
      provides: "POST /flashcards/generate client helper"
      exports: ["postFlashcardGenerate"]
  key_links:
    - from: src/lib/client/studySetDb.ts
      to: approved_flashcards
      via: "select/upsert/delete with user_id filter"
      pattern: "approved_flashcards"
    - from: src/lib/client/flashcardGenerateStudySet.ts
      to: /api/study-sets/[id]/flashcards/generate
      via: "fetch POST with learningGoal, coverage, amount"
      pattern: "flashcards/generate"
---

<objective>
Replace stubbed flashcard client data layer with Supabase-backed `approved_flashcards` CRUD and add the client POST helper for flashcard generation.

Purpose: Fulfill D-14 and FLASH-07 — existing UI (FlashcardSession, useDashboardHome, source page) already calls these modules but receives empty stubs today.
Output: Implemented flashcard bank functions in `studySetDb.ts` and `flashcardGenerateStudySet.ts` with unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-flashcards-e2e/05-CONTEXT.md
@.planning/phases/05-flashcards-e2e/05-RESEARCH.md
@.planning/phases/05-flashcards-e2e/05-PATTERNS.md
@src/lib/client/studySetDb.ts
@src/lib/client/quizGenerateStudySet.ts
@src/lib/db/studySetDb.ts
@supabase/migrations/20260725120000_v21_baseline.sql
@.planning/phases/04-quiz-pipeline/04-03-SUMMARY.md

<interfaces>
Current stubs (MUST replace):
- getApprovedFlashcardBank → returns emptyFlashcardBank()
- putApprovedFlashcardBankForStudySet → no-op

Port pattern from git HEAD src/lib/db/studySetDb.ts (flashcard section ~639–748):
- getApprovedFlashcardBank: select id,front,back,tags,source,updated_at order by updated_at asc
- putApprovedFlashcardBankForStudySet: upsert + delete orphans (mirror putApprovedBankForStudySet)

Mirror getApprovedBank in current src/lib/client/studySetDb.ts (lines 200–280) for Supabase client pattern.

ApprovedFlashcardBank type: { version: 1, savedAt: ISO string, items: FlashcardVisionItem[] }
FlashcardVisionItem: { id, front, back }

postFlashcardGenerate response (D-11):
{ recommendedCount, generatedCount, detectedFormat, cardIds[] }

FlashcardGenerateBody (from flashcardSchemas.ts — created in 05-01):
{ learningGoal, coverage, amount }

Consumers (wire in 05-04):
- FlashcardSession, useDashboardHome, source/page.tsx loadApprovedCount
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement getApprovedFlashcardBank + putApprovedFlashcardBankForStudySet (D-14)</name>
  <files>src/lib/client/studySetDb.ts, src/lib/client/studySetDb.test.ts</files>
  <behavior>
    - getApprovedFlashcardBank returns null when no rows; otherwise maps rows to ApprovedFlashcardBank with items[]
    - Maps DB columns: front, back, id; ignores vision-only fields (kind, confidence) per D-05
    - putApprovedFlashcardBankForStudySet upserts all items and deletes orphan rows not in bank
    - Both functions filter by user_id + study_set_id (RLS-aligned)
    - assertNoError on Supabase errors (no silent empty return)
  </behavior>
  <action>
    Replace stub implementations in studySetDb.ts per D-14 and RESEARCH Pattern 4:

    getApprovedFlashcardBank(studySetId):
    - createSupabaseBrowserClient + requireUserId
    - SELECT id,front,back,tags,source,updated_at FROM approved_flashcards WHERE user_id AND study_set_id ORDER BY updated_at ASC
    - Return null if no rows; else { version: 1, savedAt: new Date().toISOString(), items: [{ id, front, back }] }

    putApprovedFlashcardBankForStudySet(studySetId, bank):
    - Upsert each item with createRandomUuid for new ids
    - Preserve tags/source from existing rows when editing; new items get empty tags unless provided
    - Delete rows for study_set_id whose id not in bank.items
  - Mirror putApprovedBankForStudySet orphan-delete pattern from quiz bank (04-03)

    Extend studySetDb.test.ts with flashcard bank tests (mock Supabase chains — mirror quiz bank tests lines 47–80). Do NOT import legacy FlashcardGenerationConfig types.
  </action>
  <verify>
    <automated>npx vitest run src/lib/client/studySetDb.test.ts --reporter=dot</automated>
  </verify>
  <done>Flashcard bank CRUD returns real Supabase data; stubs removed (D-14).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: postFlashcardGenerate client helper (D-12)</name>
  <files>src/lib/client/flashcardGenerateStudySet.ts, src/lib/client/flashcardGenerateStudySet.test.ts</files>
  <behavior>
    - POST /api/study-sets/${studySetId}/flashcards/generate with JSON body { learningGoal, coverage, amount }
    - Returns { recommendedCount, generatedCount, detectedFormat, cardIds } on 200
    - Throws Error with API message on 4xx/5xx
    - Maps TypeError/network failure to "Connection lost. Check your network and try again."
  </behavior>
  <action>
    Implement flashcardGenerateStudySet.ts mirroring quizGenerateStudySet.ts:
    - Import FlashcardGenerateBody type from flashcardSchemas.ts (05-01)
    - postFlashcardGenerate(studySetId, body: FlashcardGenerateBody): POST fetch
    - parseApiError helper for { error, message } JSON bodies
    - mapNetworkError for TypeError → connection lost copy (05-UI-SPEC)
    - Export typed response interface matching D-11

    Write flashcardGenerateStudySet.test.ts: mock fetch for 200 success, 422 error message propagation, network TypeError mapping. No real API calls.
  </action>
  <verify>
    <automated>npx vitest run src/lib/client/flashcardGenerateStudySet.test.ts --reporter=dot</automated>
  </verify>
  <done>Client POST helper passes tests; ready for source page wizard wiring in 05-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→Supabase | RLS enforces user_id on approved_flashcards reads/writes |
| browser→POST /flashcards/generate | Auth cookie required; no API keys in client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Spoofing | getApprovedFlashcardBank | mitigate | requireUserId + eq user_id filter on all queries |
| T-05-10 | Tampering | putApprovedFlashcardBankForStudySet | mitigate | Upsert scoped to authenticated user_id; orphan delete limited to study_set_id |
| T-05-11 | Information disclosure | flashcardGenerateStudySet.ts | mitigate | No AI keys in client; server route handles LLM |
</threat_model>

<verification>
- npx vitest run src/lib/client/studySetDb.test.ts src/lib/client/flashcardGenerateStudySet.test.ts
- npm run typecheck
- grep confirms getApprovedFlashcardBank queries approved_flashcards (not emptyFlashcardBank)
</verification>

<success_criteria>
- D-14 fulfilled: flashcard bank stubs replaced with Supabase CRUD
- FLASH-07 data path ready: FlashcardSession can load real cards
- postFlashcardGenerate client helper matches D-11/D-12 contracts
</success_criteria>

<output>
Create `.planning/phases/05-flashcards-e2e/05-03-SUMMARY.md` when done
</output>
