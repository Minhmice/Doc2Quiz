---
phase: 05-flashcards-e2e
plan: 04
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
files_modified:
  - src/components/flashcards/FlashcardSetupWizard.tsx
  - src/components/flashcards/FlashcardGenerateProgressCard.tsx
  - src/components/canonical/CanonicalModeSelectionFooter.tsx
  - src/app/(app)/sets/[id]/source/page.tsx
  - src/components/flashcards/FlashcardSession.tsx
  - src/app/(app)/flashcards/[id]/done/page.tsx
  - src/app/(app)/quiz/[id]/done/page.tsx
  - src/lib/ui/studySetActionLabels.ts
  - src/components/dashboard/DashboardStudySetCard.tsx
  - src/lib/client/activityTracking.ts
autonomous: false
requirements:
  - MODE-01
  - FLASH-01
  - FLASH-02
  - FLASH-03
  - FLASH-04
  - FLASH-05
  - FLASH-06
  - FLASH-07
  - CORE-MIST-01
user_setup: []
must_haves:
  truths:
    - "User on canonical preview can click Flashcards CTA → wizard 3 steps → generation → /flashcards/[id] (MODE-01, D-01, FLASH-01–07)"
    - "Flashcards resume strip when pipeline_stage=flashcards and cards exist (D-02)"
    - "FlashcardSession loads real bank; Space flip; Done → /flashcards/[id]/done (D-15)"
    - "Dashboard ready flashcard sets show Start flashcards CTA (D-17)"
    - "Mistakes drill: dashboard Drill mistakes link + quiz done CTA + ?review=mistakes filter works (CORE-MIST-01, D-18–19)"
    - "npm run build and npm test run pass (D-20)"
  artifacts:
    - path: src/components/flashcards/FlashcardSetupWizard.tsx
      provides: "3-step inline wizard (FLASH-01–03)"
    - path: src/app/(app)/sets/[id]/source/page.tsx
      provides: "Flashcard mode selection + wizard + generation state machine"
    - path: src/components/canonical/CanonicalModeSelectionFooter.tsx
      provides: "Enabled Flashcards CTA + flashcards resume strip"
    - path: src/components/flashcards/FlashcardSession.tsx
      provides: "Practice session wired to Supabase bank"
  key_links:
    - from: src/app/(app)/sets/[id]/source/page.tsx
      to: /api/study-sets/[id]/flashcards/generate
      via: "postFlashcardGenerate after wizard submit"
      pattern: "flashcards/generate"
    - from: src/components/flashcards/FlashcardSession.tsx
      to: src/lib/client/studySetDb.ts
      via: "getApprovedFlashcardBank on mount"
      pattern: "getApprovedFlashcardBank"
    - from: src/components/dashboard/DashboardStudySetCard.tsx
      to: /quiz/{id}?review=mistakes
      via: "reviewMistakesHref when hasMistakes"
      pattern: "review=mistakes"
---

<objective>
Wire end-to-end flashcard UX: enable Flashcards mode, 3-step wizard, generation progress, practice session, dashboard CTAs; verify mistakes-only quiz drill; milestone E2E human checkpoint.

Purpose: Fulfill MODE-01 Flashcards path, FLASH-01–07, CORE-MIST-01, D-01–21 — connect existing UI shells to generation API and client data from Waves 1–2.
Output: Wizard components, source page state machine, session wiring, done pages, dashboard labels, mistakes drill verification.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-flashcards-e2e/05-UI-SPEC.md
@.planning/phases/05-flashcards-e2e/05-CONTEXT.md
@.planning/phases/05-flashcards-e2e/05-PATTERNS.md
@src/app/(app)/sets/[id]/source/page.tsx
@src/components/canonical/CanonicalModeSelectionFooter.tsx
@src/components/flashcards/FlashcardSession.tsx
@src/components/quiz/QuizSession.tsx
@src/components/dashboard/DashboardStudySetCard.tsx
@src/hooks/useDashboardHome.ts
@src/lib/client/activityTracking.ts

# Prior wave output
@.planning/phases/05-flashcards-e2e/05-02-SUMMARY.md
@.planning/phases/05-flashcards-e2e/05-03-SUMMARY.md

<interfaces>
From POST /flashcards/generate (05-02):
Success: { recommendedCount, generatedCount, detectedFormat, cardIds[] }
Errors: { error, message } with 400/422/500/503

From 05-03 client layer:
- getApprovedFlashcardBank, putApprovedFlashcardBankForStudySet
- postFlashcardGenerate(studySetId, { learningGoal, coverage, amount })

From activityTracking (existing — verify CORE-MIST-01):
- recordQuizCompletion, getMistakeQuestionIds, hasMistakesForStudySet
- reviewMistakesHref from studySetDashboardLinks.ts

UI-SPEC state machine on /sets/[id]/source:
preview → Flashcards click → wizard 1→2→3 → generating → success redirect /flashcards/[id]
When pipeline_stage === 'flashcards' && cardCount > 0 → flashcards resume strip

Copywriting (05-UI-SPEC): Learning mode, Flashcards enabled, wizard steps, Generate flashcards, Start flashcards, Drill mistakes

dashboardCardPrimaryCtaLabel: return "Start flashcards" for ready + contentKind === 'flashcards'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flashcard wizard + progress card + enable footer (FLASH-01–03, D-01)</name>
  <files>src/components/flashcards/FlashcardSetupWizard.tsx, src/components/flashcards/FlashcardGenerateProgressCard.tsx, src/components/canonical/CanonicalModeSelectionFooter.tsx</files>
  <action>
    Create FlashcardSetupWizard.tsx per 05-UI-SPEC (inline panel, NOT modal):

    Props: sections (from canonical preview { sectionKey, heading }[]), onSubmit(body: FlashcardGenerateBody), onCancel, initialStep?
    State machine: step 1→2→3 with Back/Continue/Generate flashcards
    Step 1 (FLASH-01, D-03): RadioGroup learningGoal — memorize | understand | exam_preparation with labels/sublabels from UI-SPEC
    Step 2 (FLASH-02): Radio entire_document | selected_sections; when selected_sections show Checkbox list from sections; validate min 1 section
    Step 3 (FLASH-03): Radio recommended | custom; custom number input min 5 max 60 with inline validation
    Map wizard state to API body per D-12 on submit
    Use shadcn RadioGroup, Checkbox, Input, Label, Button — min-h-11 touch targets
    Copy: exact strings from 05-UI-SPEC Copywriting Contract

    Create FlashcardGenerateProgressCard.tsx mirroring QuizGenerateProgressCard:
    - Props: status generating|success|error, recommendedCount?, generatedCount?, detectedFormat?, errorMessage?, onRetry?
    - Display format labels: term_definition → "Term → definition", etc. per UI-SPEC
    - States: generating headline "Generating flashcards…", success counts line, error with Try again
    - aria-live="polite", aria-busy during generate
    - Visual steps: Detecting format → Building cards → Saving to library

    Update CanonicalModeSelectionFooter per D-01, D-02, 05-UI-SPEC:
    - Wire onSelectFlashcards prop (remove disabled state and "coming soon" copy)
    - Flashcards Button variant="outline" enabled with onClick={onSelectFlashcards}
    - Add flashcards resume strip when pipelineStage === 'flashcards' && cardCount > 0: "Flashcards ready", "Start flashcards" → startFlashcardsHref
    - Keep quiz resume strip when pipeline_stage === 'quiz' — mode-specific strips (D-02)
    - Props: onSelectFlashcards, startFlashcardsHref, approvedFlashcardCount (or cardCount)

    Do NOT import FlashcardGenerationConfig, FlashcardsGenerationControls, or quick_recall/focusMode (D-05).
  </action>
  <verify>
    <automated>test -f src/components/flashcards/FlashcardSetupWizard.tsx &amp;&amp; test -f src/components/flashcards/FlashcardGenerateProgressCard.tsx &amp;&amp; grep -q "onSelectFlashcards" src/components/canonical/CanonicalModeSelectionFooter.tsx &amp;&amp; grep -q "Recall cards" src/components/canonical/CanonicalModeSelectionFooter.tsx</automated>
  </verify>
  <done>Wizard and progress components exist; Flashcards CTA enabled in footer per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Source page flashcard flow + FlashcardSession + done page (FLASH-07, D-15–16)</name>
  <files>src/app/(app)/sets/[id]/source/page.tsx, src/components/flashcards/FlashcardSession.tsx, src/app/(app)/flashcards/[id]/done/page.tsx</files>
  <action>
    Extend /sets/[id]/source/page.tsx with flashcard state machine (mirror quiz flow, avoid cross-mode collision via contentKindIntent):

    State additions per 05-UI-SPEC:
    - preview: show CanonicalModeSelectionFooter with both Quiz and Flashcards enabled
    - On Flashcards click (D-01): putStudySetMeta({ contentKind: 'flashcards', pipelineStage: 'mode_selected' }); transition to wizard step 1; hide markdown/TOC
    - wizard: render FlashcardSetupWizard with sections from canonical preview data
    - On wizard submit: transition to flashcard generating; POST postFlashcardGenerate with wizard payload (generatingRef guard)
    - generating: show FlashcardGenerateProgressCard; on success router.push(/flashcards/${id}) — skip edit page (FLASH-07)
    - error: toast + Try again re-POSTs last wizard payload
    - On mount when pipelineStage === 'flashcards': loadApprovedFlashcardCount via getApprovedFlashcardBank

    Quiz path unchanged — when flashcard wizard/generating active, quiz UI hidden (contentKindIntent).

    FlashcardSession.tsx (D-15):
    - Verify getApprovedFlashcardBank loads real cards (05-03) — filter items with non-empty front/back trim
    - Update empty state copy per UI-SPEC: "No flashcards yet." + link to /sets/{id}/source
    - Fix onDone: router.push(flashcardsDone(studySetId)) instead of /dashboard (D-16)
    - Keyboard Space flip and arrow navigation unchanged

    Flashcards done page per 05-UI-SPEC:
    - H1: "Session complete · {title}"
    - Optional subcopy: "{cardCount} cards in this set" from getApprovedFlashcardBank count
    - Body: "Results saved to your account."
    - CTAs: Start flashcards → /flashcards/{id}, Library → /dashboard, Canonical preview → /sets/{id}/source
    - Migrate buttons to shadcn Button oxblood primary (remove legacy --d2q-accent inline styles)
  </action>
  <verify>
    <automated>grep -q "postFlashcardGenerate" src/app/(app)/sets/[id]/source/page.tsx &amp;&amp; grep -q "FlashcardSetupWizard" src/app/(app)/sets/[id]/source/page.tsx &amp;&amp; grep -q "getApprovedFlashcardBank" src/components/flashcards/FlashcardSession.tsx</automated>
  </verify>
  <done>Full flashcard path wired: source wizard → generate → practice → done (FLASH-07).</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard CTAs + mistakes drill verify (CORE-MIST-01, D-17–19)</name>
  <files>src/lib/ui/studySetActionLabels.ts, src/components/dashboard/DashboardStudySetCard.tsx, src/app/(app)/quiz/[id]/done/page.tsx, src/lib/client/activityTracking.ts</files>
  <action>
    studySetActionLabels.ts (D-17):
    - dashboardCardPrimaryCtaLabel: return "Start flashcards" (not "Open flip study") when contentKind === 'flashcards' && ready variant
    - pipelineStageLabel flashcards entry: "Flashcards ready" — verify present

    DashboardStudySetCard.tsx (D-19):
    - Rename secondary mistakes link from "Review mistakes" to "Drill mistakes" per 05-UI-SPEC
    - Visibility: hasMistakes && approvedCount > 0 && contentKind === 'quiz' (flashcard sets never show mistakes)
    - href via reviewMistakesHref → /quiz/{id}?review=mistakes

    Quiz done page (CORE-MIST-01):
    - On load: call hasMistakesForStudySet(id)
    - When mistakes exist: show "Drill mistakes" as first CTA (primary oxblood) → /quiz/{id}?review=mistakes
    - When no mistakes: omit or disable Drill mistakes with title="No missed questions yet."
    - Preserve existing Start quiz, Review, Library CTAs

    Mistakes drill verification fixes per RESEARCH Pattern 6 gaps:
    - activityTracking.ts: add console.error in dev when recordQuizCompletion Supabase insert/upsert fails (D-18 — no silent swallow)
    - Verify QuizSession review=mistakes filter still calls getMistakeQuestionIds — no regression
    - If PlaySession shows 0 questions when reviewMistakesOnly && list empty: ensure existing empty-state copy visible ("No missed items to review")

    Run automated gates (D-20):
    - npm test run
    - npm run typecheck
    - npm run build
  </action>
  <verify>
    <automated>npm test run &amp;&amp; npm run typecheck &amp;&amp; npm run build &amp;&amp; grep -q "Start flashcards" src/lib/ui/studySetActionLabels.ts &amp;&amp; grep -q "Drill mistakes" src/components/dashboard/DashboardStudySetCard.tsx</automated>
  </verify>
  <done>Dashboard and quiz done show correct CTAs; mistakes drill paths verified; build + tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human E2E checkpoint — flashcards + quiz regression + mistakes drill (D-20, D-21)</name>
  <action>
    Pause for human verification after automated gates pass (npm test run, npm run build). User executes the how-to-verify checklist covering flashcard express path (FLASH-01–07), quiz regression, mistakes drill (CORE-MIST-01), and cross-mode isolation (D-21). Resume on "approved" or fix reported issues.
  </action>
  <verify>
    <human-check>User confirms all steps in how-to-verify below; resume signal "approved"</human-check>
  </verify>
  <done>Human approves full express paths for both quiz and flashcards modes plus mistakes drill (D-21).</done>
  <what-built>End-to-end flashcard pipeline + mistakes drill + milestone E2E verification (D-20, D-21)</what-built>
  <how-to-verify>
    **Automated gates (confirm green before manual steps):**
    1. npm test run — all vitest suites pass
    2. npm run build — Next.js production build succeeds

    **Express path — Flashcards (FLASH-01–07):**
    3. Sign in; open study set with pipeline_stage ≥ canonical.
    4. Visit /sets/{id}/source — confirm Flashcards CTA enabled (not disabled / coming soon).
    5. Click Flashcards — wizard step 1: select learning goal → Continue.
    6. Step 2: select entire document OR specific sections → Continue.
    7. Step 3: recommended OR custom count (5–60) → Generate flashcards.
    8. Generation progress shows recommendedCount, generatedCount, detectedFormat.
    9. Land on /flashcards/{id} — cards load; Space flips; arrows navigate; Done → /flashcards/{id}/done.
    10. Dashboard shows "Start flashcards" for content_kind=flashcards ready set.
    11. Return to source — flashcards resume strip shows "Start flashcards" when cards exist.

    **Express path — Quiz regression (Phase 4):**
    12. On a quiz set: canonical → Quiz → edit → practice → done still works.

    **Mistakes drill (CORE-MIST-01):**
    13. Complete quiz with ≥1 wrong answer.
    14. Dashboard shows "Drill mistakes" link on quiz set.
    15. Quiz done page shows "Drill mistakes" as first CTA when mistakes exist.
    16. Link opens /quiz/{id}?review=mistakes with only wrong questions.
    17. Perfect score — mistakes link hidden; study_wrong_history cleared.

    **Cross-mode (D-21):**
    18. Same account: flashcard set and quiz set coexist without wrong counts or CTAs.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API | Same-origin fetch with session cookies for flashcard generate |
| browser→Supabase | RLS on approved_flashcards reads for practice session |
| quiz mistakes data | study_wrong_history scoped per user + study_set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Spoofing | postFlashcardGenerate | mitigate | Server requireApiUser on route; client only triggers for owned sets |
| T-05-13 | Tampering | Wizard body | mitigate | Zod validation at route; client sends pipeline enums only (D-05) |
| T-05-14 | Information disclosure | Mistakes question IDs | accept | User's own wrong answers only; RLS on study_wrong_history |
</threat_model>

<verification>
- npm run typecheck
- npm run build
- npm test run
- Human checkpoint: full express paths canonical → flashcards + quiz regression + mistakes drill (D-21)
</verification>

<success_criteria>
- MODE-01: user chooses Flashcards on canonical preview (D-01)
- FLASH-01–07: wizard, generation, save, practice, dashboard re-entry
- CORE-MIST-01: mistakes-only drill from dashboard and quiz done
- D-20: build + vitest green
- D-21: human approves both express paths
</success_criteria>

<output>
Create `.planning/phases/05-flashcards-e2e/05-04-SUMMARY.md` when done
</output>
