---
phase: 05-flashcards-e2e
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
  - 04-02
files_modified:
  - src/lib/pipeline/dedupeAndCapFlashcards.ts
  - src/lib/pipeline/dedupeAndCapFlashcards.test.ts
  - src/lib/pipeline/mapFlashcardOutputToRows.ts
  - src/lib/pipeline/mapFlashcardOutputToRows.test.ts
  - src/lib/pipeline/flashcardGenerate.ts
  - src/lib/pipeline/flashcardGenerate.test.ts
  - src/app/api/study-sets/[id]/flashcards/generate/route.ts
  - src/app/api/study-sets/[id]/flashcards/generate/route.test.ts
autonomous: true
requirements:
  - FLASH-04
  - FLASH-05
  - FLASH-06
user_setup:
  - service: ai-provider
    why: "Flashcard generation requires OpenAI-compatible chat API (D-P4-09)"
    env_vars:
      - name: AI_PROVIDER_URL
        source: ".env — OpenAI-compatible base URL"
      - name: AI_PROVIDER_KEY
        source: ".env — API key for upstream provider"
must_haves:
  truths:
    - "POST /api/study-sets/[id]/flashcards/generate replaces 501 stub and returns recommendedCount, generatedCount, detectedFormat, cardIds (D-11)"
    - "Generator reads canonical_markdown + filtered canonical_sections + metadata only — never raw_markdown (D-06)"
    - "Cards bulk-insert to approved_flashcards BEFORE JSON response (D-10, FLASH-06)"
    - "Duplicate concepts deduped; count capped; dominant detected_format aggregated (FLASH-04)"
    - "Success sets pipeline_stage=flashcards and content_kind=flashcards (D-11)"
    - "Re-generate deletes approved_questions to prevent cross-mode dashboard confusion (RESEARCH recommendation)"
  artifacts:
    - path: src/lib/pipeline/flashcardGenerate.ts
      provides: "runFlashcardGenerate orchestration"
      exports: ["runFlashcardGenerate", "FlashcardGenerateError", "FlashcardGenerateValidationError"]
    - path: src/app/api/study-sets/[id]/flashcards/generate/route.ts
      provides: "POST flashcard generate endpoint"
      exports: ["POST"]
    - path: src/lib/pipeline/dedupeAndCapFlashcards.ts
      provides: "FLASH-04 post-processing"
      exports: ["dedupeAndCapFlashcards", "resolveDominantFormat"]
  key_links:
    - from: src/lib/pipeline/flashcardGenerate.ts
      to: src/lib/server/openAiChatCompletion.ts
      via: "postChatCompletionAssistantText responseFormatJsonObject"
      pattern: "postChatCompletionAssistantText"
    - from: src/lib/pipeline/flashcardGenerate.ts
      to: approved_flashcards
      via: "DELETE existing rows then bulk INSERT before return"
      pattern: "approved_flashcards"
    - from: src/app/api/study-sets/[id]/flashcards/generate/route.ts
      to: src/lib/pipeline/flashcardGenerate.ts
      via: "runFlashcardGenerate({ supabase, userId, studySetId, learningGoal, coverage, amount })"
      pattern: "runFlashcardGenerate"
---

<objective>
Implement server-side flashcard generation: `runFlashcardGenerate()` orchestration, FLASH-04 dedupe/cap + format detection, bulk persist to `approved_flashcards`, and replace the 501 API stub.

Purpose: Fulfill FLASH-04–06 and D-06–D-13 — cards from canonical knowledge only, immediate Supabase save, structured API response.
Output: `flashcardGenerate.ts`, `dedupeAndCapFlashcards.ts`, `mapFlashcardOutputToRows.ts`, POST route, unit/integration tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-flashcards-e2e/05-CONTEXT.md
@.planning/phases/05-flashcards-e2e/05-RESEARCH.md
@.planning/phases/05-flashcards-e2e/05-PATTERNS.md
@docs/pipeline.md
@src/lib/pipeline/quizGenerate.ts
@src/app/api/study-sets/[id]/flashcards/generate/route.ts
@src/app/api/study-sets/[id]/quiz/generate/route.ts
@supabase/migrations/20260725120000_v21_baseline.sql

# Prior wave output
@.planning/phases/05-flashcards-e2e/05-01-SUMMARY.md

<interfaces>
From 05-01:
- loadFlashcardPrompt(), buildFlashcardGeneratorMessages(), FLASHCARD_PROMPT_VERSION from flashcardPrompt.ts
- flashcardGeneratorOutputSchema, flashcardGenerateBodySchema from flashcardSchemas.ts
- postChatCompletionAssistantText, getAiProcessingConfig, resolveUserAiTier (from 04-01)

From flashcards/generate/route.ts stub (keep verifyStudySet):
- requireApiUser() auth pattern
- verifyStudySet(supabase, userId, studySetId) ownership guard

Pipeline stage order (src/types/studySet.ts):
input → raw → canonical → mode_selected → quiz → flashcards

D-11 success response:
{ recommendedCount, generatedCount, detectedFormat, cardIds[] }

approved_flashcards row mapping (D-09, RESEARCH Pattern 4):
| LLM | DB column |
| front | front |
| back | back |
| concept_id | tags[0] + source.concept_id |
| format, section_key, source_excerpt, learning_goal | source jsonb |

Re-generate semantics (D-10): DELETE all approved_flashcards for study_set_id before bulk INSERT (replace-all). Also DELETE approved_questions on flashcard generate (RESEARCH cross-mode cleanup).

stripJsonFence + one repair retry on Zod failure (mirror quizGenerate.ts).

Section filter: when coverage is { sectionKeys }, filter canonical_sections before sections_json template var (D-03).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dedupeAndCapFlashcards + mapFlashcardOutputToRows + tests</name>
  <files>src/lib/pipeline/dedupeAndCapFlashcards.ts, src/lib/pipeline/dedupeAndCapFlashcards.test.ts, src/lib/pipeline/mapFlashcardOutputToRows.ts, src/lib/pipeline/mapFlashcardOutputToRows.test.ts</files>
  <behavior>
    - dedupeAndCapFlashcards removes duplicate concept_id entries (FLASH-04)
    - Caps final count: min(recommended_count, uniqueConcepts.length, cards.length, customCount ?? Infinity, 60)
    - resolveDominantFormat picks plurality vote from per-card format; tie → "mixed" (D-08)
    - Returns { cards, recommendedCount, generatedCount, detectedFormat, warnings }
    - mapFlashcardOutputToRows assigns server UUIDs via createRandomUuid, maps to approved_flashcards insert shape with source.detected_format and learning_goal
  </behavior>
  <action>
    Implement dedupeAndCapFlashcards.ts per RESEARCH Pattern 1 and D-08:
    - Dedupe cards by concept_id (case-insensitive); keep first occurrence
    - Cap count based on amount: "recommended" uses LLM recommended_count; { count: N } uses N bounded 5–60
    - resolveDominantFormat(cards): count per-card format values; plurality wins; tie → "mixed"
    - Append warning "Limited content" when generated &lt; recommended

    Implement mapFlashcardOutputToRows.ts:
    - mapGeneratedCardToRow(card, { id, userId, studySetId, promptVersion, detectedFormat, learningGoal })
    - tags: concept_id ? [concept_id] : []
    - source jsonb: { concept_id, section_key, source_excerpt, format, detected_format, learning_goal, prompt_version, generated_at }

    Unit tests for dedupe edge cases: duplicate concepts, thin content, custom amount bounds, format plurality/tie.
    mapFlashcardOutputToRows.test.ts: assert front/back, tags, source jsonb fields. No LLM/DB in this task.
  </action>
  <verify>
    <automated>npx vitest run src/lib/pipeline/dedupeAndCapFlashcards.test.ts src/lib/pipeline/mapFlashcardOutputToRows.test.ts --reporter=dot</automated>
  </verify>
  <done>FLASH-04 post-processing and row mapping helpers pass unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: runFlashcardGenerate service — LLM, validate, persist (D-06, D-10)</name>
  <files>src/lib/pipeline/flashcardGenerate.ts, src/lib/pipeline/flashcardGenerate.test.ts</files>
  <behavior>
    - Throws FlashcardGenerateValidationError when pipeline_stage &lt; canonical or canonical_markdown empty (D-13)
    - Returns error when isAiProcessingConfigured() is false (503 path)
    - Loads canonical_documents + canonical_sections filtered by coverage + metadata — NEVER raw_markdown (D-06)
    - Single LLM call with locked prompt; Zod validate; one repair retry
    - Truncates canonical_markdown at 80_000 chars with warning when over budget
    - DELETE approved_flashcards + approved_questions for study_set then bulk INSERT cards before returning (D-10, replace-all)
    - UPDATE study_sets SET pipeline_stage='flashcards', content_kind='flashcards' on success (D-11)
    - Returns { ok: true, recommendedCount, generatedCount, detectedFormat, cardIds } — no card bodies in response
  </behavior>
  <action>
    Implement flashcardGenerate.ts following quizGenerate.ts error-class pattern:

    Export FlashcardGenerateValidationError, FlashcardGenerateError, runFlashcardGenerate({ supabase, userId, studySetId, user, learningGoal, coverage, amount }).

    Pre-flight:
    1. study_sets.pipeline_stage must be ≥ canonical (isAtLeastPipelineStage helper — reuse PIPELINE_STAGES from quiz)
    2. canonical_documents.canonical_markdown non-empty trim
    3. Load canonical_sections ordered by ordinal; filter by coverage.sectionKeys when not entire_document
    4. metadata.extracted_questions from doc.metadata for prompt hints

    Builder flow:
    1. Resolve requested_count: "recommended" → pass "recommended" string; { count: N } → N string
    2. loadFlashcardPrompt + buildFlashcardGeneratorMessages with canonical inputs + learning_goal + coverage_mode
    3. getAiProcessingConfig(resolveUserAiTier(...))
    4. postChatCompletionAssistantText temperature 0, responseFormatJsonObject true
    5. stripJsonFence + JSON.parse + flashcardGeneratorOutputSchema.safeParse + repair retry
    6. dedupeAndCapFlashcards(output, amount)
    7. mapFlashcardOutputToRows → supabase.from('approved_flashcards').delete().eq(...) then .insert(rows)
    8. supabase.from('approved_questions').delete().eq(...) for cross-mode cleanup
    9. UPDATE study_sets pipeline_stage + content_kind

    On LLM/Zod failure: throw FlashcardGenerateError — do not partial-insert cards.

    Tests: mock postChatCompletionAssistantText and Supabase chains (mirror quizGenerate.test.ts). Cover validation, section filter, persist-before-return ordering, cross-mode question delete.
  </action>
  <verify>
    <automated>npx vitest run src/lib/pipeline/flashcardGenerate.test.ts --reporter=dot</automated>
  </verify>
  <done>runFlashcardGenerate passes tests; FLASH-05–06 logic implemented with persist-before-response (D-10).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace 501 POST /flashcards/generate route + tests (D-P1-17)</name>
  <files>src/app/api/study-sets/[id]/flashcards/generate/route.ts, src/app/api/study-sets/[id]/flashcards/generate/route.test.ts</files>
  <behavior>
    - 401 when requireApiUser returns error
    - 404 when study set not found
    - 400 when runFlashcardGenerate throws FlashcardGenerateValidationError (stage &lt; canonical)
    - 422 when runFlashcardGenerate throws FlashcardGenerateError
    - 503 when AI not configured
    - 400 when body invalid (flashcardGenerateBodySchema — missing learningGoal, bad sectionKeys, count out of range)
    - 200 with { recommendedCount, generatedCount, detectedFormat, cardIds } on success (D-11)
  </behavior>
  <action>
    Replace 501 stub per D-P1-17:

    POST handler:
    - Keep verifyStudySet helper unchanged
    - Parse body with flashcardGenerateBodySchema (D-12)
    - Delegate to runFlashcardGenerate({ supabase, userId, studySetId, user, learningGoal, coverage, amount })
    - Error mapping mirror quiz/generate/route.ts: ValidationError → 400, FlashcardGenerateError (AI not configured) → 503, other FlashcardGenerateError/ZodError → 422, unexpected → 500
    - Success 200: { recommendedCount, generatedCount, detectedFormat, cardIds }
    - export const runtime = "nodejs"; export const maxDuration = 120;

    Route tests: vi.mock runFlashcardGenerate and requireApiUser (mirror quiz/generate/route.test.ts). Include 503 case when AI env missing. No real LLM.
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/study-sets/[id]/flashcards/generate/route.test.ts" --reporter=dot</automated>
  </verify>
  <done>POST /flashcards/generate operational; 501 stub removed; route tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→POST /flashcards/generate | Authenticated user triggers LLM on their canonical knowledge |
| server→LLM upstream | Untrusted model output validated before DB write |
| server→approved_flashcards | Bulk insert scoped by user_id + study_set_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | canonical_markdown in LLM prompt | mitigate | Locked constraints D-06; output Zod only; no tool execution |
| T-05-05 | Spoofing | Cross-user study set access | mitigate | requireApiUser + verifyStudySet + RLS user_id filter |
| T-05-06 | Denial of service | Oversized canonical input / JSON | mitigate | 80k char truncation; max 60 cards; max_tokens on chat call |
| T-05-07 | Information disclosure | AI_PROVIDER_KEY | mitigate | Server-only getAiProcessingConfig; never in response body |
| T-05-08 | Tampering | Wizard body fields | mitigate | flashcardGenerateBodySchema at route entry (D-12) |
</threat_model>

<verification>
- npx vitest run src/lib/pipeline/dedupeAndCapFlashcards.test.ts src/lib/pipeline/flashcardGenerate.test.ts "src/app/api/study-sets/[id]/flashcards/generate/route.test.ts"
- npm run typecheck
- grep confirms runFlashcardGenerate wired in route (no 501 not_implemented)
</verification>

<success_criteria>
- FLASH-04–06 fulfilled via runFlashcardGenerate + route
- Cards persist before API response (D-10)
- pipeline_stage advances to flashcards on success (D-11)
- Canonical-only input enforced (D-06)
- Section coverage filter applied before LLM call (D-03)
</success_criteria>

<output>
Create `.planning/phases/05-flashcards-e2e/05-02-SUMMARY.md` when done
</output>
