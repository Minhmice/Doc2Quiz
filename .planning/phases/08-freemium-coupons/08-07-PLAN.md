---
phase: 08-freemium-coupons
plan: 07
type: execute
wave: 7
depends_on:
  - 08-06
files_modified:
  - src/app/api/usage/route.ts
  - src/lib/server/quota/quotaClient.ts
autonomous: true
gap_closure: true
requirements:
  - PLAN-05
  - PLAN-06
user_setup: []
must_haves:
  truths:
    - "npm run build completes without TypeScript errors."
    - "GET /api/usage returns the same usage DTO at runtime as before the type fix."
    - "Focused Phase 8 quota and usage Vitest suite still passes."
  artifacts:
    - path: src/app/api/usage/route.ts
      provides: "Authenticated usage endpoint without TS2589 build failure"
      exports: ["GET"]
    - path: src/lib/server/quota/quotaClient.ts
      provides: "Shared quota Supabase boundary type and cast helper (only if explicit call-site fix is insufficient)"
      exports: ["QuotaClient", "toQuotaClient"]
  key_links:
    - from: src/app/api/usage/route.ts
      to: src/lib/server/quota/getUserUsage.ts
      via: "explicit supabase/user args instead of auth object spread"
      pattern: "getUserUsage\\(\\{ supabase"
---

<objective>
Close the remaining Phase 8 verification blocker: production build fails on GET /api/usage.

Purpose: Success criterion 5 requires Vitest and build to pass. Runtime behavior and tests are already correct; only TypeScript compatibility between `requireApiUser` and `getUserUsage` blocks ship.
Output: Build-green usage route with zero runtime behavior change.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-freemium-coupons/08-VERIFICATION.md
@.planning/phases/08-freemium-coupons/08-05-SUMMARY.md
@.planning/phases/08-freemium-coupons/08-06-SUMMARY.md
@src/app/api/usage/route.ts
@src/lib/api/requireApiUser.ts
@src/lib/server/quota/getUserUsage.ts
@src/app/(app)/layout.tsx

<interfaces>
From `src/lib/api/requireApiUser.ts`:
```typescript
export async function requireApiUser() {
  const supabase = await createSupabaseServerClient();
  // ...
  return { supabase, user } as const;
}
```

From `src/lib/server/quota/getUserUsage.ts`:
```typescript
type UsageArgs = {
  supabase: QuotaSupabase; // minimal rpc + from chain
  user: User;
  studySetId?: string;
};
export async function getUserUsage({ supabase, user, studySetId }: UsageArgs): Promise<UserUsage>
```

Working call-site pattern (builds cleanly today) in `src/app/(app)/layout.tsx`:
```typescript
getUserUsage({ supabase, user })
```

Known failing pattern in `src/app/api/usage/route.ts`:
```typescript
getUserUsage({ ...auth, studySetId }) // TS2589 at build
```

Precedent workaround elsewhere: `src/app/api/workspaces/[workspaceId]/outputs/quiz/route.ts` uses `auth.supabase as never` — avoid duplicating `as never`; prefer explicit named fields first, then a shared boundary helper only if needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix usage-route getUserUsage call-site typing</name>
  <files>src/app/api/usage/route.ts, src/lib/server/quota/quotaClient.ts</files>
  <action>Replace the object spread in GET /api/usage with explicit named fields after the auth guard. Do not change route behavior, response shape, or error handling.

Primary fix (try first): after `if ("error" in auth) return auth.error`, destructure and call:
`getUserUsage({ supabase: auth.supabase, user: auth.user, studySetId })`
Match the working layout/settings pattern — do not spread the narrowed `auth` object. Spreading `{ ...auth, studySetId }` forces TypeScript to structurally compare the full Supabase SSR client against the minimal `QuotaSupabase` interface and triggers TS2589 "Type instantiation is excessively deep."

Fallback (only if `npx tsc --noEmit` still reports TS2589 on the usage route after explicit fields): add `src/lib/server/quota/quotaClient.ts` exporting a single shared `QuotaClient` type (union of rpc + from needs from `getUserUsage`) and `toQuotaClient(client: unknown): QuotaClient` that performs one intentional boundary cast. Use `toQuotaClient(auth.supabase)` at the usage route call site. Optionally refactor `getUserUsage.ts` and `generationQuotaReservation.ts` to import `QuotaClient` instead of duplicating local `QuotaSupabase` / `QuotaRpcSupabase` aliases — keep that refactor limited to type imports only, no logic changes. Do not touch human UAT flows, migrations, or unrelated routes unless they share the same TS2589 error after Task 1.

Do not use `as never` at the usage route. Preserve existing `route.test.ts` expectations (`getUserUsage` called with `studySetId` when query param present).</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | Select-String -Pattern "usage/route" ; if ($LASTEXITCODE -ne 0 -and (npx tsc --noEmit 2>&1 | Select-String -Pattern "usage/route")) { exit 1 }</automated>
  </verify>
  <done>Usage route compiles without TS2589; call passes explicit supabase/user/studySetId; runtime contract unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm production build and Phase 8 test regression gate</name>
  <files>src/app/api/usage/route.test.ts</files>
  <action>Run the focused Phase 8 verification test command from 08-VERIFICATION.md and full production build. No new tests required unless Task 1 changes the public call signature (it should not). If build passes but usage route test fails due to mock expectation drift, update only the mock assertion to match explicit argument object — do not weaken coverage.</action>
  <verify>
    <automated>npm run test -- --run src/lib/server/quota src/app/api/usage/route.test.ts src/app/api/study-sets/[id]/quiz/generate/route.test.ts src/app/api/study-sets/[id]/flashcards/generate/route.test.ts src/app/api/coupons/redeem/route.test.ts src/lib/client/quizGenerateStudySet.test.ts; npm run build</automated>
  </verify>
  <done>70-test Phase 8 suite passes and `npm run build` exits 0 with no TypeScript errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Type boundary only | No new trust boundary; cast helper is compile-time only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | Tampering | quotaClient boundary cast | accept | Cast does not alter runtime auth or RLS; same Supabase client instance passed through |
| T-08-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
Re-run phase verifier success criterion 5: `npm run build` must pass alongside existing Vitest green state. No human UAT, SQL concurrency probe, or browser checks in this plan.
</verification>

<success_criteria>
- `npx tsc --noEmit` reports zero errors in `src/app/api/usage/route.ts`
- `npm run build` completes successfully
- Phase 8 focused Vitest command (10 files, 70 tests) still passes
- GET /api/usage response JSON shape unchanged
</success_criteria>

<output>
Create `.planning/phases/08-freemium-coupons/08-07-SUMMARY.md` when done
</output>

## Multi-Source Coverage Audit

| Source | Item | Plan Coverage |
|--------|------|---------------|
| GOAL / SC5 | Vitest + build pass | 08-07 Tasks 1–2 |
| REQ PLAN-05 | Server enforcement before generation | Unchanged; build unblock preserves deployability |
| REQ PLAN-06 | Sidebar/Settings usage via GET /api/usage | Task 1 keeps endpoint buildable and behavior-identical |
| RESEARCH | QuotaSupabase vs full client typing conflict | Task 1 explicit fields; Task 1 fallback `quotaClient.ts` |
| CONTEXT | No new locked decisions | Honored — type fix only, no scope expansion |
| VERIFICATION gap | TS2589 at usage route line 13 | Primary target of 08-07 |
| Excluded | Human UAT, SQL concurrency probe | Out of scope per orchestrator |
