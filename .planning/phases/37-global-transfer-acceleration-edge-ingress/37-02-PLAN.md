---
phase: 37-global-transfer-acceleration-edge-ingress
plan: 02
type: execute
wave: 2
depends_on:
  - "37-01"
files_modified:
  - next.config.ts
  - src/middleware.ts
autonomous: true
requirements:
  - "EDGE-37-02"
  - "EDGE-37-04"

must_haves:
  truths:
    - "Static chunk caching headers only where safe; dynamic routes unchanged."
    - "Local-only code paths do not import upload-only modules unconditionally."
---

<objective>
Wave 2 — **EDGE-37-02** apply conservative **headers** for static assets via Next config; **EDGE-37-04** verify local-first routes have no regression (conditional dynamic import for upload client if needed).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@docs/DEPLOY-edge-and-upload.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: next.config headers</name>
  <files>next.config.ts</files>
  <action>Add headers for `_next/static` long-cache where appropriate per Next 15 docs.</action>
  <verify><automated>npm run lint && npm run build</automated></verify>
</task>

</tasks>

<verification>npm run build</verification>
