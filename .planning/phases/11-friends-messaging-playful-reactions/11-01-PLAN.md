---
phase: 11-friends-messaging-playful-reactions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260730170000_friends_messages_presence.sql
  - supabase/tests/friends_messages_rls.sql
  - src/lib/client/friends.ts
  - src/lib/client/messages.ts
requirements: [FRIEND-01, FRIEND-02, MSG-01, SAFE-01]
autonomous: true
must_haves:
  truths:
    - Accepted friends can list each other without exposing non-friends.
    - Only accepted, non-blocked friends can create or read a 1:1 conversation.
    - Preset reaction sends fail for opted-out or blocked recipients.
  artifacts:
    - {path: supabase/migrations/20260730170000_friends_messages_presence.sql, provides: accepted-friends, messages, reaction-preference, RPC authority}
    - {path: supabase/tests/friends_messages_rls.sql, provides: authorization SQL coverage}
---
<objective>
Add secure social authority for accepted friends, 1:1 messages, presence eligibility, and preset reactions.

Purpose: establish server authority before rendering social UI or opening Realtime channels.
Output: migration, SQL authorization tests, typed client API contracts.
</objective>
<context>
@.planning/phases/11-friends-messaging-playful-reactions/11-CONTEXT.md
@supabase/migrations/20260730140400_phase10_social_safety.sql
@supabase/migrations/20260730140500_phase10_social_list_respond_rpcs.sql
@src/lib/client/friends.ts
</context>
<tasks>
<task type="auto">
<name>Task 1: Extend social database authority</name>
<files>supabase/migrations/20260730170000_friends_messages_presence.sql, supabase/tests/friends_messages_rls.sql</files>
<action>Create a migration which builds only on accepted `friend_requests` rows. Add security-definer, fixed-search-path RPCs to list accepted friends with safe profile DTO and server-calculated recent activity (last active within five minutes), and to fetch incoming pending request count/list. Do not reveal users who are not accepted friends.

Create canonical pair-keyed 1:1 conversations and immutable messages. Expose RPCs to create-or-open a conversation, list paginated messages, and send bounded plain-text messages. Derive sender from `auth.uid()`; verify accepted friendship and no block on every read/write. Add indexes for conversation timeline and last activity. Direct table grants stay revoked; RLS must also deny non-members as defense in depth.

Add per-user reaction preferences: global enabled default true plus per-sender block list. Add send-reaction RPC accepting only a database allowlist of 6–8 fixed reaction IDs, validates friendship/blocks/preferences, then returns a safe event payload. It must not persist reaction content/history. Add secure current-user activity touch RPC with rate limit/interval guard; presence uses this ephemeral recency signal, not heartbeat rows. Enable only required Realtime publication and private-channel authorization configuration; no public broadcast that lets arbitrary clients target users.

SQL tests must cover: non-friend enumeration denial, accepted list symmetry, block override, self denial, direct-table denial, conversation membership, message sender forgery denial, message read/write rejection after unfriend/block, reaction preset validation, global/per-sender opt-out, and activity privacy.</action>
<verify><automated>supabase db reset && supabase test db</automated></verify>
<done>Server is sole authority for friendship, messages, reaction opt-outs, and presence data.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Add typed browser API adapters</name>
<files>src/lib/client/friends.ts, src/lib/client/messages.ts</files>
<action>Extend current friends client module with safe DTOs/fetchers for accepted friends and pending count. Add minimal `messages.ts` for create/open, list, send, preference update, activity touch, and fixed reaction IDs. Normalize API failure handling into generic user-safe error states; never branch UX on username existence. Keep DOM/UI out of these modules.</action>
<verify><automated>npm run test -- src/lib/client --run</automated></verify>
<done>UI can use typed social contracts without direct Supabase social-table access.</done>
</task>
</tasks>
<threat_model>
- Client IDs, message bodies, and reaction IDs are untrusted.
- Friendship status and blocks are server-authoritative at every RPC.
- Username lookup remains request-only to prevent account enumeration.
- Realtime messages/reactions require private authorized channels and unsubscribe cleanup.
</threat_model>
