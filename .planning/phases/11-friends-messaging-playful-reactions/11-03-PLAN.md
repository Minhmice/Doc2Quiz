---
phase: 11-friends-messaging-playful-reactions
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - public/friends-add-icon.png
  - src/components/layout/FriendsMenu.tsx
  - src/components/layout/AppTopBar.tsx
  - src/components/friends/AddFriendDialog.tsx
  - src/components/friends/FriendActionMenu.tsx
  - src/components/friends/DirectMessageDialog.tsx
  - src/components/friends/PlayfulReactionOverlay.tsx
  - src/components/settings/SocialSafetySettings.tsx
  - src/app/globals.css
requirements: [FRIEND-04, FRIEND-05, MSG-03, REACT-01, REACT-02]
autonomous: false
must_haves:
  truths:
    - Navbar icon appears immediately left of Ping AI and is keyboard accessible.
    - Dropdown presents add friend, requests, online then offline accepted friends.
    - Receiver sees only allowed preset reaction animation for about three seconds while active.
    - Receiver can disable all reactions and mute individual senders.
---
<objective>
Build accessible navbar friend UI, private chat UI, and recipient-only playful reaction animation.

Purpose: make server-authorized social features usable without expanding initial scope into a Friends page.
Output: topbar dropdown, dialogs/menus, animation overlay, settings controls, human UI checkpoint.
</objective>
<context>
@.planning/phases/11-friends-messaging-playful-reactions/11-CONTEXT.md
@.planning/phases/11-friends-messaging-playful-reactions/11-02-PLAN.md
@src/components/layout/AppTopBar.tsx
@src/components/layout/ApiStatusButton.tsx
@src/components/settings/SocialSafetySettings.tsx
@src/components/ui/sonner.tsx
</context>
<tasks>
<task type="auto">
<name>Task 1: Add friend dropdown and action dialogs</name>
<files>public/friends-add-icon.png, src/components/layout/FriendsMenu.tsx, src/components/layout/AppTopBar.tsx, src/components/friends/AddFriendDialog.tsx, src/components/friends/FriendActionMenu.tsx, src/components/friends/DirectMessageDialog.tsx</files>
<action>Copy supplied person-plus image into `public/friends-add-icon.png`. Add `FriendsMenu` in AppTopBar action slot immediately before `ApiStatusButton`. Use existing project dropdown/dialog primitives. Trigger needs localized accessible label and pending-request count badge. Dropdown order: Add friend, incoming requests with accept/decline actions, Online friends, Offline friends with last-active label, empty states. Add friend dialog takes username and calls current request API. Clicking accepted friend opens keyboard-safe action menu: view protected profile, open 1:1 message dialog, choose playful reaction. Chat dialog fetches paginated history, supports safe plain-text send, live-region delivery/error feedback, scroll anchoring, and realtime subscription cleanup. Do not add a full Friends page or global directory.</action>
<verify><automated>npm run lint && npm run test -- src/components --run</automated></verify>
<done>Users can manage friend requests, distinguish online/offline friends, view profiles, and exchange private messages from navbar.</done>
</task>
<task type="auto">
<name>Task 2: Add receiver-only preset reaction effect and controls</name>
<files>src/components/friends/PlayfulReactionOverlay.tsx, src/components/settings/SocialSafetySettings.tsx, src/app/globals.css</files>
<action>Mount one client-side reaction listener at authenticated app-shell scope. Subscribe only to private authorized recipient channel. Validate reaction ID against local fixed preset map; discard unknown, stale, unauthorized, or disabled events. On receipt while active, render text from friend icon origin and animate it on a bounded, pointer-events-none viewport path for about three seconds; honor `prefers-reduced-motion` with static accessible toast/live text instead. Never render server-supplied HTML. Sender interaction only offers fixed friendly Vietnamese presets such as “Ê, tập trung học nè!”, “Đừng để điểm số chạy mất!”, “Làm một câu nữa đi!”, “Nghỉ chút rồi chiến tiếp!”, “Quiz đang đợi kìa!”, “Bạn làm được mà!”, subject to final copy review for non-targeted tone.

Extend SocialSafetySettings with global reaction switch and per-friend mute/unmute controls. Default is enabled. Explain that reaction appears only during active use and can be disabled. Realtime setup must touch activity on bounded visibility/focus intervals, not every render; unsubscribe all channels on cleanup.</action>
<verify><automated>npm run lint && npm run test -- src/components/friends --run</automated></verify>
<done>Preset reactions are fun, short-lived, opt-out capable, motion-safe, and impossible to turn into arbitrary abusive content.</done>
</task>
<task type="checkpoint:human-verify">
<name>Task 3: Verify two-account social flow</name>
<action>With two authenticated accounts: send/accept request; confirm topbar badge clears; verify online/offline ordering after five minutes/activity update; send/read 1:1 message; send each preset reaction while recipient is active; verify receiver animation ends near three seconds; turn off global reactions and mute sender, then confirm reactions do not display; verify block stops messages/reactions/presence exposure. Check desktop/mobile keyboard navigation and reduced motion.</action>
<verify><automated>npm run build</automated></verify>
<done>Human confirms expected friend, privacy, chat, and animation behavior across both accounts.</done>
</task>
</tasks>
