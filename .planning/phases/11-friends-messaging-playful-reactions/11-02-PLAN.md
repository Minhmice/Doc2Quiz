---
phase: 11-friends-messaging-playful-reactions
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/app/api/friends/route.ts
  - src/app/api/friends/requests/route.ts
  - src/app/api/friends/messages/route.ts
  - src/app/api/friends/messages/[conversationId]/route.ts
  - src/app/api/friends/reactions/route.ts
  - src/app/api/friends/preferences/route.ts
  - src/app/api/friends/activity/route.ts
  - src/app/api/friends/friends.route.test.ts
requirements: [FRIEND-03, MSG-02, SAFE-02]
autonomous: true
must_haves:
  truths:
    - Every browser social operation reaches an authenticated route backed by RPC authority.
    - API payloads never expose data for non-friends or arbitrary reaction text.
---
<objective>
Expose authenticated HTTP contracts for social UI.

Purpose: routes preserve existing Next API patterns and keep browser code independent of privileged schema.
Output: friend, message, reaction, preference, and activity API routes with route tests.
</objective>
<context>
@.planning/phases/11-friends-messaging-playful-reactions/11-CONTEXT.md
@.planning/phases/11-friends-messaging-playful-reactions/11-01-PLAN.md
@src/lib/api/requireApiUser.ts
@src/app/api/friends/requests/route.ts
@src/app/api/friends/requests/[id]/route.ts
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Add safe social API routes</name>
<files>src/app/api/friends/route.ts, src/app/api/friends/messages/route.ts, src/app/api/friends/messages/[conversationId]/route.ts, src/app/api/friends/reactions/route.ts, src/app/api/friends/preferences/route.ts, src/app/api/friends/activity/route.ts, src/app/api/friends/friends.route.test.ts</files>
<action>Use `requireApiUser` for every handler. Add GET `/api/friends` for accepted online/offline DTOs plus incoming pending count/list, relying on Plan 01 RPC. Keep existing request routes intact; only extend response DTO if needed. Add conversation open/list/send routes with strict JSON validation, length caps, opaque conversation IDs, cursor pagination, and generic forbidden/not-found responses. Add reaction POST which accepts only known reaction ID enum, never text. Add preference PATCH for global enable and sender-specific mute, validating target IDs through RPC. Add activity POST that is rate-tolerant/no-content. Return structured app errors without revealing friendship, profile, or preference state to unauthorized callers.

Tests mock auth/RPC results and prove 401, malformed JSON, invalid preset, oversized message, generic authorization responses, and normal DTO shape.</action>
<verify><automated>npm run test -- src/app/api/friends/friends.route.test.ts --run</automated></verify>
<done>Topbar UI has small authenticated HTTP contracts, no direct social-table access.</done>
</task>
</tasks>
