---
phase: 22-implement-example-mint-ui-ux-into-main-app-dashboard-shell-t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/DashboardLibraryClient.tsx
  - src/components/dashboard/DashboardStatsWidget.tsx
  - src/app/(app)/dashboard/page.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Dashboard library header uses Mint typography (`font-heading` + optional `font-label` eyebrow) per `22-UI-SPEC.md` §1."
    - "Stats widget cards read as tonal Mint modules (labels uppercase micro, chart bars use forest-forward accent)."
    - "Empty / no-match states use dashed tonal panels and a single CTA to `/sets/new`."
  artifacts:
    - path: src/components/dashboard/DashboardLibraryClient.tsx
      provides: "Library grid + empty state + card chrome aligned to example dashboard mock"
    - path: src/components/dashboard/DashboardStatsWidget.tsx
      provides: "Stats row visual parity with mock stat tiles + weekly chart"
  key_links:
    - from: src/app/(app)/dashboard/page.tsx
      to: DashboardStatsWidget + DashboardLibraryClient
      via: unchanged composition
      pattern: "layout wrapper only if needed"
---

<objective>
**Wave 1 — Dashboard Mint parity:** Restyle `/dashboard` to match `example/doc2quiz_action_focused_dashboard/code.html` **visually** (typography, surfaces, card deck strip, stats row) while **preserving** all existing data hooks, routes, and behaviors (`deleteStudySet`, search filter, mistakes link, `DECK_ACCENTS`).
</objective>

<context>
@.planning/phases/22-implement-example-mint-ui-ux-into-main-app-dashboard-shell-t/22-CONTEXT.md
@.planning/phases/22-implement-example-mint-ui-ux-into-main-app-dashboard-shell-t/22-UI-SPEC.md
@example/doc2quiz_action_focused_dashboard/code.html
</context>

<threat_model>
| Threat | Severity | Mitigation | Blockers |
|--------|----------|------------|----------|
| Regression in delete/rename/navigate flows | Medium | Visual-only edits; do not change handler signatures or `href` targets except copy/aria | None |
| WCAG contrast loss on new background combos | Low | Use semantic tokens only; verify primary text on `bg-card` / `muted` | None |
| Misleading “approved” counts | Low | Do not change counting logic in this plan | None |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Library header + empty / no-match states</name>
  <files>src/components/dashboard/DashboardLibraryClient.tsx</files>
  <read_first>src/components/dashboard/DashboardLibraryClient.tsx,.planning/phases/22-implement-example-mint-ui-ux-into-main-app-dashboard-shell-t/22-UI-SPEC.md</read_first>
  <action>
- Above the `h1` “Library”, add a single line **eyebrow** using `font-label text-[10px] font-bold tracking-[0.2em] text-muted-foreground` with literal text **`Study hub`** (ASCII).
- Replace wrapper `rounded-2xl` on empty + no-match panels with **`rounded-xl border border-dashed border-border/80 bg-muted/25`** (keep inner copy meaning; keep `Link href="/sets/new"`; button text may read **`Create study set`** if it currently says “Import PDF” — must still route to **`/sets/new`**).
- On populated deck `Card`, add **`border-0 shadow-sm`** (or `shadow-md` if needed for lift) and ensure `CardTitle` uses **`text-foreground`**; optional `rounded-t-lg` on gradient strip container if strip bleeds — do not change gradient color tuples in `DECK_ACCENTS` in this task.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Study hub" src/components/dashboard/DashboardLibraryClient.tsx` exits 0.
    - `rg -n "rounded-xl border border-dashed" src/components/dashboard/DashboardLibraryClient.tsx` exits 0.
    - `rg -n 'href="/sets/new"' src/components/dashboard/DashboardLibraryClient.tsx` exits 0.
  </acceptance_criteria>
  <done>Library chrome matches UI-SPEC §1 baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Stats widget — Mint stat tiles + weekly chart</name>
  <files>src/components/dashboard/DashboardStatsWidget.tsx</files>
  <read_first>src/components/dashboard/DashboardStatsWidget.tsx,.planning/phases/22-implement-example-mint-ui-ux-into-main-app-dashboard-shell-t/22-UI-SPEC.md</read_first>
  <action>
- Wrap grid in **`mb-8`** unchanged; for each stats `Card`, set **`border-0 bg-card shadow-sm`**.
- Change `CardDescription` for first two tiles to use **`font-label text-[10px] font-bold uppercase tracking-widest text-muted-foreground`** and human-readable titles: literals **`Study sets`** and **`Current streak`** remain or shorten to same meaning.
- For bar chart inner bars, replace `bg-primary/80` with **`bg-secondary`** (forest `#376757` token) or **`bg-[#376757]/85`** if token secondary is too light on card — pick one and apply consistently to all bars.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "font-label" src/components/dashboard/DashboardStatsWidget.tsx` exits 0.
    - `rg -n "border-0 bg-card" src/components/dashboard/DashboardStatsWidget.tsx` exits 0.
    - `rg -n "bg-\\[#376757\\]|bg-secondary" src/components/dashboard/DashboardStatsWidget.tsx` exits 0.
  </acceptance_criteria>
  <done>Stats row visually aligned with Mint mock.</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard page wrapper spacing</name>
  <files>src/app/(app)/dashboard/page.tsx</files>
  <read_first>src/app/(app)/dashboard/page.tsx,src/components/dashboard/DashboardLibraryClient.tsx</read_first>
  <action>
Add vertical rhythm: outer `div` wraps both widgets with **`className="space-y-8"`** (or `space-y-10`) if not already equivalent — goal: clear separation between stats block and library header per mock section spacing.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "space-y-(8|10)" src/app/(app)/dashboard/page.tsx` exits 0.
  </acceptance_criteria>
  <done>Dashboard vertical spacing tightened.</done>
</task>

</tasks>
