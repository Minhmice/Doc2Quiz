---
phase: 22-implement-example-mint-ui-ux-into-main-app-dashboard-shell-t
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/app/(app)/settings/page.tsx
  - src/components/settings/AiProviderForm.tsx
  - src/app/(app)/sets/[id]/source/page.tsx
  - src/app/(app)/sets/[id]/review/page.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Settings shell matches `22-UI-SPEC.md` §2 (Mint panel, label typography)."
    - "`AiProviderForm` text inputs expose bottom-border focus styling without breaking react-hook-form `Field` wiring."
    - "Source + Review page headers share consistent vertical rhythm (`space-y-*`, subtitle rules) per §3."
  artifacts:
    - path: src/components/settings/AiProviderForm.tsx
      provides: "Mint form field chrome"
    - path: src/app/(app)/settings/page.tsx
      provides: "Settings page container + title block"
  key_links:
    - from: src/app/(app)/settings/page.tsx
      to: AiProviderForm
      via: existing import
      pattern: "presentational only"
---

<objective>
**Wave 2 — Settings + study headers:** Align `/settings` and the **headers** of `/sets/[id]/source` + `/sets/[id]/review` with Mint mocks (`doc2quiz_ai_connection_settings`, workspace/review) — **presentation only**; no API, IDB, or parse logic changes.
</objective>

<context>
@.planning/phases/22-implement-example-mint-ui-ux-into-main-app-dashboard-shell-t/22-UI-SPEC.md
@example/doc2quiz_ai_connection_settings/code.html
@example/doc2quiz_professional_review_workspace/code.html
</context>

<threat_model>
| Threat | Severity | Mitigation | Blockers |
|--------|----------|------------|----------|
| Breaking RHF validation / submit | High | Only `className` / layout wrappers on `Field`/`Input`; do not rename form fields | None |
| Leaking API key in new UI | Low | No new plain-text echo; keep existing show/hide behavior | None |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Settings page shell</name>
  <files>src/app/(app)/settings/page.tsx</files>
  <read_first>src/app/(app)/settings/page.tsx,.planning/phases/22-implement-example-mint-ui-ux-into-main-app-dashboard-shell-t/22-UI-SPEC.md</read_first>
  <action>
- Outer wrapper: change `max-w-xl` container to **`max-w-2xl`** and add **`space-y-2`** between title block and card.
- Title `h1`: ensure **`font-heading`**; add **`font-label`** eyebrow line above title with literal **`Connection`** (ASCII) in `text-[10px] font-bold uppercase tracking-widest text-muted-foreground`.
- Card wrapper: use **`rounded-xl border border-border bg-card p-6 shadow-sm`** (adjust padding if already `p-6` — avoid duplicate `rounded-2xl` + heavy shadow unless kept minimally as `shadow-sm`).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Connection" src/app/(app)/settings/page.tsx` exits 0.
    - `rg -n "max-w-2xl" src/app/(app)/settings/page.tsx` exits 0.
  </acceptance_criteria>
  <done>Settings shell Mint-aligned.</done>
</task>

<task type="auto">
  <name>Task 2: AiProviderForm field chrome</name>
  <files>src/components/settings/AiProviderForm.tsx</files>
  <read_first>src/components/settings/AiProviderForm.tsx,src/components/ui/input.tsx,src/components/ui/field.tsx</read_first>
  <action>
For each visible `Input` tied to `Field` (base URL, model id, API key): append Tailwind classes so the control uses **`rounded-none border-0 border-b-2 border-border bg-transparent px-0 shadow-none focus-visible:border-primary focus-visible:ring-0`** (merge with existing `className` via `cn(...)` where used).

Ensure `FieldLabel` uses **`font-label text-[10px] font-bold uppercase tracking-widest text-muted-foreground`** (apply via `className` on `FieldLabel` instances — grep all `FieldLabel` in this file).

Do **not** change Zod schema, `aiSettingsSchema`, or submit handler names.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "border-b-2 border-border" src/components/settings/AiProviderForm.tsx` exits 0.
    - `rg -n "font-label" src/components/settings/AiProviderForm.tsx` exits 0.
  </acceptance_criteria>
  <done>Form fields match underline Mint style.</done>
</task>

<task type="auto">
  <name>Task 3: Source + Review headers rhythm</name>
  <files>src/app/(app)/sets/[id]/source/page.tsx,src/app/(app)/sets/[id]/review/page.tsx</files>
  <read_first>src/app/(app)/sets/[id]/source/page.tsx,src/app/(app)/sets/[id]/review/page.tsx,.planning/phases/22-implement-example-mint-ui-ux-into-main-app-dashboard-shell-t/22-UI-SPEC.md</read_first>
  <action>
- On **source** page root content wrapper (the outer `div` with `space-y-6`): add **`max-w-3xl`** on the header block only OR on entire column if layout allows without breaking `PdfInfoCard` width — prefer tightening **`header` + first section** with `max-w-3xl mx-auto` **only if** it does not shrink parse tools; safest: add **`prose`-free** `mb-2 font-label text-[10px] font-bold uppercase tracking-widest text-muted-foreground` line with literal **`Study set`** above the `h1` title.
- On **review** page: add the same **`Study set`** eyebrow above `h1` “Review”.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Study set" src/app/(app)/sets/[id]/source/page.tsx` exits 0.
    - `rg -n "Study set" src/app/(app)/sets/[id]/review/page.tsx` exits 0.
  </acceptance_criteria>
  <done>Headers carry Mint label cadence.</done>
</task>

</tasks>
