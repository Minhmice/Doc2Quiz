---
phase: quick-260802-2hj-increase-avatar-upload-limit-from-2-mb-t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/profile/profileValidation.ts
  - src/lib/profile/profileValidation.test.ts
  - src/app/api/profile/route.test.ts
autonomous: true
requirements:
  - QUICK-260802-2HJ
must_haves:
  truths:
    - "User can select and upload a supported avatar whose size is any whole-byte value strictly below 10 MiB."
    - "Client and server reject an avatar at or above 10 MiB with copy stating that the image must be smaller than 10 MB."
    - "Existing MIME allowlist, signature checks, authenticated upload flow, canonical storage path, and private signed preview behavior remain unchanged."
  artifacts:
    - path: "src/lib/profile/profileValidation.ts"
      provides: "Shared client/server avatar size validation and consistent rejection copy"
      exports: ["PROFILE_IMAGE_MAX_BYTES", "validateProfileImage"]
    - path: "src/lib/profile/profileValidation.test.ts"
      provides: "Exact lower and upper byte-boundary coverage for shared validation"
    - path: "src/app/api/profile/route.test.ts"
      provides: "Route-level proof that the exclusive 10 MiB boundary is rejected before storage upload"
  key_links:
    - from: "src/components/profile/ProfilePageClient.tsx"
      to: "src/lib/profile/profileValidation.ts"
      via: "validateProfileImage(file) before POST"
      pattern: "validateProfileImage\(file\)"
    - from: "src/app/api/profile/route.ts"
      to: "src/lib/profile/profileValidation.ts"
      via: "validateProfileImage(file) before reading bytes or creating admin storage client"
      pattern: "validateProfileImage\(file\)"
---

<objective>
Raise avatar upload ceiling from 2 MiB to an exclusive 10 MiB boundary while retaining one shared client/server validator.

Purpose: Accept larger avatar files without allowing a file whose size reaches 10 MiB, and keep validation behavior and user-facing copy aligned at both trust boundaries.
Output: Updated shared size constant/check/copy plus focused unit and API boundary tests.
</objective>

<execution_context>
@C:/Users/minhmice/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/minhmice/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/lib/profile/profileValidation.ts
@src/lib/profile/profileValidation.test.ts
@src/components/profile/ProfilePageClient.tsx
@src/app/api/profile/route.ts
@src/app/api/profile/route.test.ts

<interfaces>
Existing shared contract from `src/lib/profile/profileValidation.ts`:
- `PROFILE_IMAGE_MAX_BYTES` is imported by focused tests and defines the avatar byte boundary.
- `validateProfileImage(file: Pick<File, "type" | "size">): string | null` runs unchanged from both `ProfilePageClient.uploadAvatar` and `POST /api/profile`.
- Accepted MIME types remain PNG, JPEG, WebP, and GIF.

Existing upload contract:
- Client validates before constructing `FormData` and posting only `file` to `/api/profile`.
- API authenticates first, validates the same `File`, verifies content signature, derives canonical user path, uploads through admin Storage, persists path, then returns signed preview URL.
</interfaces>

<constraints>
- Preserve existing dirty user changes in all touched files; inspect scoped diffs before editing and change only avatar-size assertions/logic/copy.
- Treat existing product byte convention as binary MiB because current limit uses `2 * 1024 * 1024`; define requested boundary as `10 * 1024 * 1024` bytes.
- “Below 10 MB” is exclusive: `10 * 1024 * 1024 - 1` bytes is accepted and exactly `10 * 1024 * 1024` bytes is rejected.
- Reuse `PROFILE_IMAGE_MAX_BYTES` and `validateProfileImage`; add no dependency, helper abstraction, duplicate client constant, route-specific limit, or unrelated avatar-flow change.
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock exclusive 10 MiB boundary in focused tests</name>
  <files>src/lib/profile/profileValidation.test.ts, src/app/api/profile/route.test.ts</files>
  <behavior>
    - Shared validator accepts a supported image at `PROFILE_IMAGE_MAX_BYTES - 1` bytes.
    - Shared validator rejects a supported image at exactly `PROFILE_IMAGE_MAX_BYTES` bytes with copy matching “smaller than 10 MB”.
    - Shared validator still rejects unsupported MIME types independently of size.
    - `POST /api/profile` rejects an exactly 10 MiB supported-type file with status 400 before creating the admin storage client.
  </behavior>
  <action>Update existing focused assertions before production logic. In `profileValidation.test.ts`, assert the threshold equals `10 * 1024 * 1024`, exercise both adjacent whole-byte boundaries, and replace the stale `/2 MB/` expectation with the exclusive-limit copy. In the existing API test for unsupported, oversized, and spoofed files, construct the oversized case at exactly `PROFILE_IMAGE_MAX_BYTES` and import the shared constant rather than duplicating the byte value. Keep the route test focused on pre-upload rejection; do not allocate or submit a near-10-MiB accepted upload because unit boundary coverage plus existing successful route upload already proves each side without needless test memory. Run tests once and confirm new boundary expectations fail against the 2 MiB implementation before Task 2.</action>
  <verify>
    <automated>npm test -- --run src/lib/profile/profileValidation.test.ts src/app/api/profile/route.test.ts</automated>
  </verify>
  <done>Focused tests encode acceptance one byte below 10 MiB, rejection at exactly 10 MiB, consistent copy, and route rejection before storage; tests fail only because production validator still has old limit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Raise shared avatar validator to exclusive 10 MiB</name>
  <files>src/lib/profile/profileValidation.ts</files>
  <behavior>
    - Every supported avatar smaller than 10 MiB passes size validation.
    - Every avatar at or above 10 MiB fails size validation.
    - Rejection message says `Image must be smaller than 10 MB.` for identical client and API copy.
  </behavior>
  <action>Set `PROFILE_IMAGE_MAX_BYTES` to `10 * 1024 * 1024` as the exclusive threshold. Change the size comparison from inclusive-maximum semantics to reject `file.size >= PROFILE_IMAGE_MAX_BYTES`, and update returned copy to `Image must be smaller than 10 MB.` Keep export names, function signature, MIME check order, and all path/signature/text validation unchanged. Do not edit `ProfilePageClient.tsx` or `route.ts`: both already import and call the shared validator, so changing those consumers would duplicate behavior.</action>
  <verify>
    <automated>npm test -- --run src/lib/profile/profileValidation.test.ts src/app/api/profile/route.test.ts src/components/profile/ProfilePageClient.test.tsx &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Shared validator accepts at most 10,485,759 bytes, rejects 10,485,760 bytes and above with consistent copy, both current consumers retain shared validation, and focused tests plus typecheck pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to shared validator | User-selected file metadata is untrusted; client validation provides fast feedback but cannot be authoritative. |
| multipart request to profile API | Client-controlled MIME type, size, and bytes cross into authenticated server upload flow. |
| profile API to private Supabase Storage | Validated bytes are uploaded with elevated admin credentials only after auth, size, MIME, and signature checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-QUICK-2HJ-01 | Denial of Service | `POST /api/profile` multipart avatar | mitigate | Keep authoritative server `validateProfileImage` call before `arrayBuffer()` and admin Storage creation; reject exactly 10 MiB and above. |
| T-QUICK-2HJ-02 | Tampering | client-declared image MIME | mitigate | Preserve existing post-validation `hasProfileImageSignature` byte check before upload. |
| T-QUICK-2HJ-03 | Elevation of Privilege | admin Storage upload | mitigate | Preserve authentication-first ordering and server-derived canonical avatar path; tests continue proving rejected files never reach admin upload. |
| T-QUICK-2HJ-SC | Tampering | package installs | accept | Plan adds no package or dependency change. |
</threat_model>

<verification>
1. `PROFILE_IMAGE_MAX_BYTES` equals 10,485,760 and is treated as an exclusive threshold.
2. Shared unit tests prove 10,485,759 accepted and 10,485,760 rejected for supported MIME type.
3. API test proves exact-threshold rejection occurs before admin Storage upload.
4. Existing accepted MIME, content-signature, canonical path, persistence, and signed-preview tests remain green.
5. Client remains wired to same validator, so displayed validation error matches API error without duplicate copy.
6. Focused Vitest suite and TypeScript typecheck pass; scoped diff contains no dependency or unrelated profile changes.
</verification>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | QUICK-260802-2HJ | Increase avatar upload limit from 2 MB to below 10 MB | 01 | COVERED | Tasks set and verify exclusive 10 MiB threshold.
REQ | QUICK-260802-2HJ | Client/server validation and copy stay consistent | 01 | COVERED | Existing shared validator remains sole source used by both consumers.
RESEARCH | — | No research phase | 01 | COVERED | User prohibited research; repository convention supplies binary-byte interpretation.
CONTEXT | C-01 | Strictly less than 10 MiB unless product convention differs | 01 | COVERED | Existing `1024 * 1024` convention retained; adjacent-byte tests lock exclusivity.
CONTEXT | C-02 | Reuse existing validation; no dependency or abstraction | 01 | COVERED | Only existing constant, validator, and focused tests change.
CONTEXT | C-03 | Atomic single plan with 1-3 tasks | 01 | COVERED | One plan contains two RED/GREEN-focused tasks.
CONTEXT | C-04 | Do not commit during planning | 01 | COVERED | Planning writes this file only and performs no commit.
</source_audit>

<success_criteria>
- Supported avatar files from zero through 10,485,759 bytes pass shared size validation; files of 10,485,760 bytes or more fail.
- Browser and API expose identical `Image must be smaller than 10 MB.` validation copy through existing shared function.
- Focused validation, API upload, and client wiring tests pass with typecheck.
- No new dependency, abstraction, duplicate limit, or unrelated profile behavior enters diff.
</success_criteria>

<output>
Create `.planning/quick/260802-2hj-increase-avatar-upload-limit-from-2-mb-t/260802-2hj-SUMMARY.md` when done. Do not commit unless execution instructions explicitly request it.
</output>
