---
phase: 13-monitoring-error-reporting-observability
plan: 02
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - src/lib/observability/reportPipelineError.ts
  - src/lib/ai/runOcrSequential.ts
  - src/lib/ai/runVisionSequential.ts
  - src/lib/ai/runLayoutChunkParse.ts
  - .planning/phases/13-monitoring-error-reporting-observability/13-02-SUMMARY.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "`PipelineDomain` values from `pipelineLogger.ts` appear as Sentry tags **or** equivalent stable string keys (`PDF`/`OCR`/`VISION`/`IDB`/`STUDY_SET`) on captured events from wired sites."
    - "Call sites do **not** attach `File`, `Blob`, full `dataUrl`, API key strings, or full question text to Sentry `extra` / contexts (metadata only: e.g. truncated ids, page counts)."
    - "Local `pipelineLog(..., 'error', ...)` behavior remains for wired failures (Sentry is additive, not replacement), unless a specific duplicate-noise decision is documented in SUMMARY."
  artifacts:
    - path: src/lib/observability/reportPipelineError.ts
      provides: `reportPipelineError(domain, stage, err, meta?)` — Sentry optional + `pipelineLog` error
    - path: src/lib/ai/runOcrSequential.ts
      provides: At least one top-level failure path reports via helper
    - path: src/lib/ai/runVisionSequential.ts
      provides: At least one top-level failure path reports via helper
    - path: src/lib/ai/runLayoutChunkParse.ts
      provides: At least one catch / failure path reports via helper
  key_links:
    - from: reportPipelineError.ts
      to: pipelineLogger.ts
      via: "`pipelineLog(domain, stage, 'error', ...)` + `normalizeUnknownError`"
---

<objective>
**Wave 2 — Pipeline-tagged remote errors:** Add a small **`reportPipelineError`** helper that combines existing **`pipelineLog` + `normalizeUnknownError`** with **optional `Sentry.captureException`** (when 13-01 SDK is enabled). Wire **three** AI pipeline modules so failures are attributable to **OCR vs vision vs chunk parse**, not a generic toast.

**Purpose:** Operators can triage “render vs OCR vs vision vs mapping vs persistence” starting from Sentry issue tags; local console behavior preserved.

**Output:** New `src/lib/observability/reportPipelineError.ts` + targeted edits in `runOcrSequential.ts`, `runVisionSequential.ts`, `runLayoutChunkParse.ts`. **Out of scope:** `AiParseSection.tsx` giant refactor, `studySetDb` (defer unless a single obvious catch exists with zero risk).
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Logging secrets / PII to Sentry | High | `meta` whitelist in helper TypeScript type; strip unknown keys; document in code comment. |
| Double-reporting same error (toast + Sentry + pipelineLog) | Low | One capture per catch block; no inner+outer duplicate for same exception without `cause` chaining. |

**Blockers:** If `runVisionSequential` / `runOcrSequential` are browser-only and Sentry server SDK differs, use dynamic `import("@sentry/nextjs")` only when `typeof window !== "undefined"` for client paths — adjust helper to split client/server or use `@sentry/nextjs` unified API per 13-01 install.
</threat_model>

## Task 1 — `reportPipelineError` helper

<action>
Create `src/lib/observability/reportPipelineError.ts` exporting:

```ts
import type { PipelineDomain } from "@/lib/logging/pipelineLogger";
export function reportPipelineError(
  domain: PipelineDomain,
  stage: string,
  err: unknown,
  meta?: Record<string, string | number | boolean | null | undefined>,
): void;
```

Implementation requirements:
1. Call `pipelineLog(domain, stage, "error", message, { ...normalizeUnknownError(err), ...safeMeta })` where `safeMeta` only passes **primitive** values from an allowlisted subset of `meta` keys (executor defines 3–5 allowed keys in the type or runtime filter).
2. If Sentry is available **and** enabled (import `@sentry/nextjs` / use `getCurrentScope` pattern per 13-01 — avoid hard crash when DSN unset: use try/catch or check `process.env` + dynamic import), call `Sentry.captureException` with `tags`: `{ d2q_pipeline_domain: domain, d2q_pipeline_stage: stage }` (exact key strings) and **no** raw file contents.

Keep file **≤80 lines** if possible; no new dependency beyond 13-01.
</action>

<read_first>
- `src/lib/logging/pipelineLogger.ts`
- `sentry.client.config.ts` and `sentry.server.config.ts` (from 13-01) — confirm init export pattern
</read_first>

<acceptance_criteria>
- File `src/lib/observability/reportPipelineError.ts` exists.
- File contains the literal substring `d2q_pipeline_domain`.
- File imports type `PipelineDomain` from `@/lib/logging/pipelineLogger` (or re-import path consistent with repo aliases).
</acceptance_criteria>

## Task 2 — Wire `runOcrSequential.ts`

<action>
Locate the **outermost** `catch` or failure return that represents OCR batch failure (user-visible path). Call `reportPipelineError("OCR", "<stage-name>", err, { ... })` with **one** stable `stage` string (e.g. `batch` or `page` — pick the coarsest meaningful stage present in file).

Do not change OCR success paths or retry semantics.
</action>

<read_first>
- `src/lib/ai/runOcrSequential.ts` (full file)
</read_first>

<acceptance_criteria>
- `runOcrSequential.ts` contains `reportPipelineError`.
- `runOcrSequential.ts` first argument to `reportPipelineError` is the string `OCR` (or variable typed as `PipelineDomain` equal to `"OCR"`).
</acceptance_criteria>

## Task 3 — Wire `runVisionSequential.ts`

<action>
Same as Task 2 for domain **`VISION`** — one stable `stage` for the primary failure path (e.g. `page` or `request`).
</action>

<read_first>
- `src/lib/ai/runVisionSequential.ts` (full file)
</read_first>

<acceptance_criteria>
- `runVisionSequential.ts` contains `reportPipelineError`.
- First domain argument uses `"VISION"`.
</acceptance_criteria>

## Task 4 — Wire `runLayoutChunkParse.ts`

<action>
Invoke `reportPipelineError("VISION", "chunk-parse", err, ...)` or domain **`VISION`** with stage **`chunk`** / **`chunk-parse`** (choose one, document in SUMMARY) for the injected-parse failure surface that is not already covered by a duplicate upstream catch.

Avoid reporting the same exception twice if `runLayoutChunkParse` callers already wrap — prefer the **deepest** useful catch that has `Error` object.
</action>

<read_first>
- `src/lib/ai/runLayoutChunkParse.ts` (full file)
</read_first>

<acceptance_criteria>
- `runLayoutChunkParse.ts` contains `reportPipelineError`.
- `grep -n "reportPipelineError" src/lib/ai/runLayoutChunkParse.ts` returns at least one line (use ripgrep in executor environment).
</acceptance_criteria>

## Verification (phase)

<action>
Run `npm run lint` and `npm run build`. Write `.planning/phases/13-monitoring-error-reporting-observability/13-02-SUMMARY.md` with bullets: which `stage` strings were wired per file, confirm **`pipelineLogger.ts` was not modified** (helper imports only), and list deferred surfaces (e.g. `studySetDb`, `AiParseSection` catch-all).
</action>

<acceptance_criteria>
- `.planning/phases/13-monitoring-error-reporting-observability/13-02-SUMMARY.md` exists.
- That file contains the substring `reportPipelineError`.
- `npm run lint` and `npm run build` exit 0.
</acceptance_criteria>
