---
phase: 27-preview-first-parsing-while-full-upload-continues
plan: 03
type: execute
wave: 3
depends_on:
  - "27-02"
files_modified:
  - src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx
  - src/components/upload/UploadBox.tsx
autonomous: true
requirements:
  - PREVIEW-07
  - PREVIEW-12
  - PREVIEW-13
  - PREVIEW-14
must_haves:
  truths:
    - "User cannot navigate into study/play until background upload completes when upload is enabled."
    - "If upload fails with finalize/non-retryable error, the flow cancels everything and returns to start."
    - "If parse fails while upload is OK, user can retry parse / adjust settings using existing parse UX."
    - "Refreshing/closing mid-flow does not resume; on reload the user is back at the upload step."
  artifacts:
    - path: "src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx"
      provides: "Navigation gating + upload-vs-parse failure policy + no-resume behavior"
    - path: "src/components/upload/UploadBox.tsx"
      provides: "Upload step baseline; ensures reload starts here and no resume UI appears"
  key_links:
    - from: "NewStudySetPdfImportFlow"
      to: "Navigation action"
      via: "disabled/blocked until uploadComplete when upload enabled (D-07)"
      pattern: "disabled|aria-disabled|canNavigate|uploadComplete"
---

<objective>
Enforce Phase 27 gating and failure policies: block entering study/play until background upload completes (when enabled), differentiate upload-failure vs parse-failure handling, and explicitly keep "no resume across refresh" behavior.

Purpose: Maintain data integrity and user expectations while improving time-to-first-preview.
Output: Updated create-flow behavior for gating + error policies consistent with D-07, D-14, D-15, D-11.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-preview-first-parsing-while-full-upload-continues/27-CONTEXT.md
@.planning/phases/26-direct-multipart-resumable-upload-to-object-storage/26-CONTEXT.md

@src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx
@src/components/upload/UploadBox.tsx
@src/components/ai/AiParseSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate navigation into study/play until background upload completes (per D-07)</name>
  <files>
    - src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx
  </files>
  <read_first>
    - src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx
    - src/components/ai/AiParseSection.tsx
    - .planning/phases/27-preview-first-parsing-while-full-upload-continues/27-CONTEXT.md
    - .planning/phases/26-direct-multipart-resumable-upload-to-object-storage/26-CONTEXT.md
  </read_first>
  <action>
    In the create/import flow UI, ensure that any action that navigates into study/play (or otherwise "starts using the set")
    is blocked until the background upload has completed when direct-upload is enabled (D-07).

    Implementation requirements:
    - In upload-enabled mode, disable the continue/start button(s) or intercept the navigation handler so it cannot proceed until `uploadComplete === true`.
    - In local-only mode (upload hidden), preserve the current behavior (no additional gating).
    - Keep messaging minimal; do not add new cloud messaging in local-only mode (D-13).
  </action>
  <acceptance_criteria>
    - Grep in `NewStudySetPdfImportFlow.tsx` shows a boolean gate that prevents navigation until upload completion when upload is enabled.
    - In local-only mode, the navigation path remains unchanged (no upload completion requirement).
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
    Navigation into study/play is impossible before upload completes in upload-enabled mode, and unchanged in local-only mode.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enforce failure policy split (upload failure cancels all; parse failure offers retry) + keep no-resume-on-refresh (per D-11, D-14, D-15)</name>
  <files>
    - src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx
    - src/components/upload/UploadBox.tsx
  </files>
  <read_first>
    - src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx
    - src/components/upload/UploadBox.tsx
    - src/lib/db/studySetDb.ts
    - .planning/phases/27-preview-first-parsing-while-full-upload-continues/27-CONTEXT.md
    - .planning/phases/26-direct-multipart-resumable-upload-to-object-storage/26-CONTEXT.md
  </read_first>
  <action>
    Implement the explicit error policy split:
    - If background upload fails at finalize/non-retryable (per Phase 26 semantics), Cancel-all: cancel parse/upload (if running), clean up the created set (`deleteStudySet`), and reset to the initial upload step (D-14).
    - If parse fails / produces no usable output while upload is OK, expose the existing "Retry parse / adjust settings" affordances and do NOT force cancel-all (D-15).

    Also ensure the "no resume across refresh/close" constraint remains true (D-11):
    - Do not add any persisted "in-progress upload" state that restores UI mid-flow on reload.
    - Verify `UploadBox` remains the entry step after a reload.
  </action>
  <acceptance_criteria>
    - Upload finalize/non-retryable error path triggers the same Cancel-all cleanup/reset behavior as user-initiated Cancel-all (including `deleteStudySet`).
    - Parse error path does not delete the set automatically; instead it presents retry/adjust controls consistent with existing parse UX.
    - No new code persists "resume" flags to localStorage/IDB that rehydrate the in-progress flow after reload.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    Upload failure cancels all + resets; parse failure supports retry; and refresh/reopen continues to start at the upload step with no resume.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| background upload → UI gating | Upload completion and failure signals drive navigation gating and cancellation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-27-07 | T | Upload completion gating | mitigate | Gate navigation on explicit upload-complete state in upload-enabled mode; avoid ambiguous "probably done" heuristics. |
| T-27-08 | D | Failure loops | mitigate | Ensure upload finalization failure forces cancel-all so user cannot proceed into broken state; keep retry limited to parse-only failures. |
| T-27-09 | I | Resume semantics | accept | No resume is a product decision; avoid storing more state than needed. |
</threat_model>

<verification>
- `npm run lint`
- `npm run build`
</verification>

<success_criteria>
- In upload-enabled mode, continue/start is blocked until upload completes.
- Upload finalize failure cancels all and resets the flow (no proceeding into study).
- Parse failure supports retry/adjust settings without forcing cancel-all.
- Reloading the page does not resume any in-progress flow; user is back at upload.
</success_criteria>

<output>
After completion, create `.planning/phases/27-preview-first-parsing-while-full-upload-continues/27-03-SUMMARY.md`
</output>

