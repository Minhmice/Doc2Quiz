---
phase: 27-preview-first-parsing-while-full-upload-continues
plan: 02
type: execute
wave: 2
depends_on:
  - "27-01"
files_modified:
  - src/components/layout/ParseProgressStrip.tsx
  - src/components/ai/ParseProgressContext.tsx
  - src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx
autonomous: true
requirements:
  - PREVIEW-05
  - PREVIEW-06
  - PREVIEW-08
  - PREVIEW-11
must_haves:
  truths:
    - "User sees a sticky top strip showing parse progress and (when enabled) upload byte progress together."
    - "User can Cancel-all to cancel parse + upload and cleanup the in-progress set."
    - "If background upload completes, the UI shows a light 'Upload complete' toast."
    - "If upload capability isn't available, upload UI is hidden and flow behaves local-only with no cloud messaging."
  artifacts:
    - path: "src/components/layout/ParseProgressStrip.tsx"
      provides: "Sticky progress strip UI combining parse + upload progress"
    - path: "src/components/ai/ParseProgressContext.tsx"
      provides: "Single source of truth for live progress state; extended to include upload bytes when enabled"
    - path: "src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx"
      provides: "Wires Cancel-all and toast triggers into create flow"
  key_links:
    - from: "src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx"
      to: "ParseProgressStrip"
      via: "passes combined progress + cancel actions"
      pattern: "ParseProgressStrip"
    - from: "src/components/ai/ParseProgressContext.tsx"
      to: "ParseProgressStrip"
      via: "context/hook provides live progress model"
      pattern: "ParseProgressContext|useParseProgress"
---

<objective>
Expose combined progress (parse + background upload) and primary cancellation controls in a sticky strip, keeping local-only behavior clean when upload capability is not configured.

Purpose: Make preview-first feel immediate and controllable; unify mental model of "work in progress".
Output: Updated progress strip + progress context model + create-flow wiring for Cancel-all and upload completion toast.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-preview-first-parsing-while-full-upload-continues/27-CONTEXT.md

@.planning/phases/26-direct-multipart-resumable-upload-to-object-storage/26-CONTEXT.md

@src/components/layout/ParseProgressStrip.tsx
@src/components/ai/ParseProgressContext.tsx
@src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend progress model + sticky strip UI to show combined parse + upload progress (per D-05, D-11)</name>
  <files>
    - src/components/layout/ParseProgressStrip.tsx
    - src/components/ai/ParseProgressContext.tsx
    - src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx
  </files>
  <read_first>
    - src/components/layout/ParseProgressStrip.tsx
    - src/components/ai/ParseProgressContext.tsx
    - src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx
    - .planning/phases/27-preview-first-parsing-while-full-upload-continues/27-CONTEXT.md
    - .planning/phases/26-direct-multipart-resumable-upload-to-object-storage/26-CONTEXT.md
  </read_first>
  <action>
    Update the existing progress strip implementation so it can render a combined status line that includes:
    - Parse progress (existing live progress model).
    - Upload byte progress (only when direct-upload capability is enabled/available).

    Requirements:
    - The strip remains sticky at the top (D-05).
    - When upload capability is not available (not configured or provider not ready), hide upload-specific UI entirely (D-13 / PREVIEW-11) and avoid "cloud" copy.
    - Avoid introducing new global state: prefer extending existing `ParseProgressContext` data model or augmenting its consumer props so the strip can render both sources consistently.
  </action>
  <acceptance_criteria>
    - `ParseProgressStrip.tsx` can render parse-only mode (local-only) with no upload UI.
    - When upload capability is enabled, the strip displays both parse progress and upload bytes progress in the same sticky strip.
    - Grep shows the upload-progress UI is conditionally rendered behind a capability/feature flag check (no always-on cloud messaging).
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
    Sticky progress strip shows combined progress when available and stays local-only/clean when not.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Cancel-all + upload-complete toast into the create flow (per D-06, D-08, D-16)</name>
  <files>
    - src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx
    - src/components/layout/ParseProgressStrip.tsx
  </files>
  <read_first>
    - src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx
    - src/components/layout/ParseProgressStrip.tsx
    - src/lib/db/studySetDb.ts
    - .planning/phases/27-preview-first-parsing-while-full-upload-continues/27-CONTEXT.md
  </read_first>
  <action>
    In `NewStudySetPdfImportFlow.tsx` and the progress strip wiring:
    - Implement a primary Cancel-all action that cancels both parse and background upload (when enabled) and resets the UI back to the start state (D-06).
    - Preserve existing cleanup semantics: Cancel-all must call the established deletion/cleanup path (`deleteStudySet` or equivalent) so no junk sets remain (D-16).
    - On successful completion of the background upload, show a light toast "Upload complete" (or short equivalent) (D-08). This toast must only occur when upload is enabled; do nothing in local-only mode.

    Do not implement "Cancel upload only" as a requirement; it is optional discretion and can be added only if already ergonomically supported by the strip without complicating the primary Cancel-all flow.
  </action>
  <acceptance_criteria>
    - Cancel-all is present as the primary control in the sticky strip and triggers:
      - parse cancellation,
      - upload cancellation when enabled,
      - deletion/cleanup of the current in-progress study set,
      - UI reset to the initial upload step.
    - A toast is shown once when upload completes successfully (upload-enabled only).
    - Local-only mode has no upload-complete toast and no upload-only UI.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    Cancel-all works end-to-end with cleanup and upload completion is surfaced via a light toast when applicable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI actions → cancellation | User actions trigger cancellation and cleanup; must be idempotent and safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-27-04 | D | Cancel-all handler | mitigate | Ensure cancellation is idempotent; avoid race that leaves background tasks running after UI reset. |
| T-27-05 | T | Cleanup/deleteStudySet | mitigate | Reuse existing deletion path; ensure Cancel-all always calls cleanup exactly once for the created set id. |
| T-27-06 | I | Upload progress + toast | accept | No secrets exposed; only bytes/progress status. Avoid logging URLs/tokens. |
</threat_model>

<verification>
- `npm run lint`
- `npm run build`
</verification>

<success_criteria>
- A sticky strip shows combined progress (parse + upload when enabled) and supports Cancel-all as the primary control.
- Upload UI is hidden when capability is unavailable (local-only).
- Upload completion triggers a light toast only when upload is enabled and completes.
- Cancel-all cancels all work, deletes the in-progress set, and resets UI to the upload step.
</success_criteria>

<output>
After completion, create `.planning/phases/27-preview-first-parsing-while-full-upload-continues/27-02-SUMMARY.md`
</output>

