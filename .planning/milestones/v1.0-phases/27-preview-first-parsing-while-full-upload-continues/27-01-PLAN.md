---
phase: 27-preview-first-parsing-while-full-upload-continues
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx
  - src/lib/pdf/extractPdfText.ts
  - src/lib/pdf/extractText.ts
autonomous: true
requirements:
  - PREVIEW-01
  - PREVIEW-02
  - PREVIEW-03
  - PREVIEW-04
  - PREVIEW-09
  - PREVIEW-10
must_haves:
  truths:
    - "After selecting a PDF, AI parsing begins immediately (no waiting for background upload)."
    - "Preview questions/flashcards appear incrementally as they are produced."
    - "Preview work prioritizes the first 3–5 pages to reduce time-to-first-results."
    - "Preview parsing uses the local File via pdf.js and does not depend on any remote upload URL."
    - "A studySetId + meta persist early so preview is not blocked by later background persistence."
  artifacts:
    - path: "src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx"
      provides: "Starts parse immediately on file select; schedules preview-first 3–5 pages; persists set meta early"
    - path: "src/lib/pdf/extractPdfText.ts"
      provides: "PDF text extraction utilities used by preview-first parse lanes"
    - path: "src/lib/pdf/extractText.ts"
      provides: "PDF extraction helpers (pageCount and text) used by import flow"
  key_links:
    - from: "src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx"
      to: "Ai parse kickoff"
      via: "file selection handler triggers parse immediately (per D-01)"
      pattern: "on(File|file).*(start|kick).*parse|start.*parse.*file"
    - from: "src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx"
      to: "Preview budget"
      via: "limits preview-first work to first 3–5 pages (per D-04)"
      pattern: "(3|5).*(page|pages)|preview.*(budget|max)"
    - from: "src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx"
      to: "Local File-only parsing"
      via: "pdf.js reads from local File; no dependency on upload URL (per D-12)"
      pattern: "pdfjs|getDocument\\(|File"
---

<objective>
Deliver preview-first kickoff and scheduling so parsing begins immediately after file selection and streams incremental quiz/flashcard output while other work (including optional background upload) may proceed in parallel.

Purpose: Reduce time-to-first-result without weakening local-first persistence.
Output: Updated import flow orchestration to (a) create/persist set meta first and (b) prioritize preview parsing for the first 3–5 pages using the local File.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-preview-first-parsing-while-full-upload-continues/27-CONTEXT.md

@.planning/phases/26-direct-multipart-resumable-upload-to-object-storage/26-CONTEXT.md
@.planning/phases/25-skip-rasterization-for-born-digital-pdfs-extract-text-layer-/25-CONTEXT.md

@src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx
@src/components/ai/AiParseSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist studySet meta first, then start parse immediately on file select (per D-01, D-09)</name>
  <files>
    - src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx
  </files>
  <read_first>
    - src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx
    - src/lib/db/studySetDb.ts
    - src/types/studySet.ts (or equivalent StudySetMeta type file used by the flow)
    - .planning/phases/27-preview-first-parsing-while-full-upload-continues/27-CONTEXT.md
  </read_first>
  <action>
    In `NewStudySetPdfImportFlow.tsx`, restructure the "file selected" path so it:
    - Creates a new `studySetId` and persists the minimal required StudySetMeta immediately (before any long-running work), per D-09.
    - Starts AI parsing immediately after file selection, without waiting for background upload completion, per D-01.
    - Ensures the parse kickoff is the same for both Quiz and Flashcards surfaces, per D-03.
    - Ensures the parse pipeline reads from the local `File` (pdf.js) and does not use or require any uploaded URL, per D-12.

    The executor should keep existing "deleteStudySet on cancel" semantics intact (D-16), but full cancel wiring is handled in Plan 27-02/27-03.
  </action>
  <acceptance_criteria>
    - Grep in `NewStudySetPdfImportFlow.tsx` shows a single "file selected" handler path that:
      - writes/creates StudySet meta before parse kickoff, and
      - triggers parse kickoff without awaiting any upload completion.
    - Both Quiz and Flashcards create flows reach the same parse kickoff call path.
    - No code path requires an upload URL to begin parsing; local `File` is the input.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
    File selection reliably creates/persists `studySetId` + meta first and kicks off parsing immediately using local File input, aligned with D-01/D-03/D-09/D-12.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement preview budget + scheduling for first 3–5 pages (per D-02, D-04)</name>
  <files>
    - src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx
    - src/lib/pdf/extractPdfText.ts
    - src/lib/pdf/extractText.ts
  </files>
  <read_first>
    - src/app/(app)/edit/new/NewStudySetPdfImportFlow.tsx
    - src/lib/pdf/extractPdfText.ts
    - src/components/ai/AiParseSection.tsx
    - .planning/phases/25-skip-rasterization-for-born-digital-pdfs-extract-text-layer-/25-CONTEXT.md
    - .planning/phases/27-preview-first-parsing-while-full-upload-continues/27-CONTEXT.md
  </read_first>
  <action>
    Add a preview-first scheduling mechanism in the import flow so that:
    - The first 3–5 pages are prioritized for producing usable preview items quickly, per D-04.
    - Preview results stream into the existing live preview UI as soon as they exist (no milestone gate), per D-02.
    - The mechanism applies to both Quiz and Flashcards parsing paths (D-03).

    Concretely, the executor should:
    - Introduce a constant or derived `previewPageBudget` in the 3–5 range, and ensure the first pass uses only those pages to start producing questions/cards.
    - After preview-first pass begins, the system can continue parsing remaining pages as the existing pipeline dictates (do not block preview on full-document completion).
    - Do not add per-page routing; Phase 29 owns page-type routing (deferred).
  </action>
  <acceptance_criteria>
    - `NewStudySetPdfImportFlow.tsx` contains a clear preview-first page limit in the 3–5 range and uses it to start parsing.
    - Preview UI receives and renders incremental results before full-document parsing completes (observable via existing live preview components).
    - No new resume-on-refresh behavior is added (D-11); reload still restarts at upload step (enforced in Plan 27-03).
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    Preview-first scheduling is in place: 3–5-page prioritized early output streams into previews for both Quiz and Flashcards, without depending on upload URLs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user file → parser | Untrusted PDF bytes and derived text/images enter parsing pipeline |
| local state → persistence | Draft/meta persisted to IndexedDB based on untrusted parse output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-27-01 | D | Import flow kickoff | mitigate | Ensure preview parsing uses bounded preview pages (3–5) and preserves existing limits/timeouts so a huge PDF can't lock the UI indefinitely. |
| T-27-02 | T | Draft/meta persistence | mitigate | Persist meta first but validate required fields; reuse existing schema/validators for draft structures when streaming preview results. |
| T-27-03 | R | Parse run provenance | accept | Offline-first local app; audit logging handled by existing pipeline log patterns; this plan focuses on timing, not adding new audit features. |
</threat_model>

<verification>
- `npm run lint`
- `npm run build`
</verification>

<success_criteria>
- Selecting a PDF starts parsing immediately and preview items stream in without waiting for any background upload.
- Preview-first prioritizes first 3–5 pages and uses only the local File input (no remote URL dependency).
- Study set meta (`studySetId`) is persisted early so preview is not blocked by later persistence steps.
</success_criteria>

<output>
After completion, create `.planning/phases/27-preview-first-parsing-while-full-upload-continues/27-01-SUMMARY.md`
</output>

