---
phase: 05-score-repeat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/play/PlaySession.tsx
  - src/lib/studySet/activityTracking.ts
autonomous: true
requirements:
  - SCORE-01
  - SCORE-02
  - SCORE-03
  - SCORE-04
user_setup: []

must_haves:
  truths:
    - "End-of-session UI shows X/Y, percentage, and per-question outcomes on /sets/[id]/play"
    - "Wrong question IDs persist via recordQuizCompletion into studyWrongHistory"
    - "Drill mistakes starts /sets/{id}/play?review=mistakes when wrongCount > 0"
  artifacts:
    - "PlaySession finished branch implements 05-UI-SPEC results card"
  key_links:
    - "05-CONTEXT D-01–D-12; 05-UI-SPEC approved"
---

<objective>
Close **Phase 05 — Score & Repeat** by **verifying** implementation against **SCORE-01–04**, **05-CONTEXT.md**, and **05-UI-SPEC.md**. Most behavior already ships in **`PlaySession.tsx`** + **`activityTracking.ts`**; this plan focuses on **traceability**, **gap fixes** (if any), and **verification commands**.

Purpose: Confirm score display, wrong-ID tracking, drill route, and IndexedDB persistence match roadmap acceptance criteria.
Output: Optional code tweaks; **05-01-SUMMARY.md** documenting requirement mapping.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-score-repeat/05-CONTEXT.md
@.planning/phases/05-score-repeat/05-UI-SPEC.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@src/components/play/PlaySession.tsx
@src/lib/studySet/activityTracking.ts
@src/app/(app)/sets/[id]/play/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: SCORE requirement trace (read-only)</name>
  <files>src/components/play/PlaySession.tsx,src/lib/studySet/activityTracking.ts</files>
  <read_first>src/components/play/PlaySession.tsx</read_first>
  <read_first>src/lib/studySet/activityTracking.ts</read_first>
  <action>
Confirm in code (document in SUMMARY):
- **SCORE-01:** Finished state renders **percentage** (`Math.round(100 * correct / total)`), **X / Y correct**, and **Session complete** title.
- **SCORE-02:** **`wrongIdsRef`** collects incorrect answers; **`recordQuizCompletion`** receives **`wrongQuestionIds`** when session completes.
- **SCORE-03:** **Drill mistakes** uses **`/sets/${studySetId}/play?review=mistakes`** when **`wrongCount > 0`**; **`reviewMistakesOnly`** path filters via **`getMistakeQuestionIds`** (existing).
- **SCORE-04:** **`activityTracking.recordQuizCompletion`** writes **`quizSessions`** and **`studyWrongHistory`** / deletes row when no wrong IDs (silent clear per D-04).

Per **D-08:** Primary drill control is a **`Link`** styled with **`buttonVariants({ variant: &quot;default&quot; })`** (project **`Button`** may omit **`asChild`** — do not require Radix slot if typings block it).
Per **D-11:** No **session-complete** **`toast.success`** duplicating the card score (removed).
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `PlaySession.tsx` contains `Session complete` and `Drill mistakes`
    - `PlaySession.tsx` calls `recordQuizCompletion` with `wrongQuestionIds`
    - `activityTracking.ts` contains `studyWrongHistory` put/delete logic for wrong IDs
  </acceptance_criteria>
  <done>Trace documented</done>
</task>

<task type="auto">
  <name>Task 2: UI-SPEC spot-check vs finished card</name>
  <files>src/components/play/PlaySession.tsx</files>
  <read_first>.planning/phases/05-score-repeat/05-UI-SPEC.md</read_first>
  <read_first>src/components/play/PlaySession.tsx</read_first>
  <action>
Verify **visual contract** alignment (fix only if a clear miss):
- **Hero** line: **%** · **fraction** + **tabular-nums**; **Card** + **`shadow-lg`**.
- **Subline:** **“{n} to review”** vs **“All correct — nothing to drill.”**
- **Breakdown:** **`ScrollArea`** when **> 8** questions; rows **≥ 44px** (**`min-h-11`**); **Correct / Incorrect** badges with emerald/red treatment.
- **Footer order:** Drill → **Quiz again** → **Open library**; drill **disabled** + **`title`** when zero mistakes.
- **A11y:** **`aria-labelledby`** / **`role=&quot;region&quot;`** / **`role=&quot;list&quot;`** as spec.

If everything matches, **no code changes** in this task.
  </action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - Breakdown uses `ScrollArea` when question count threshold matches spec (8)
    - `Open library` link targets `/dashboard`
  </acceptance_criteria>
  <done>UI-SPEC checked</done>
</task>

<task type="auto">
  <name>Task 3: Build verification</name>
  <files></files>
  <action>
Run **`npx tsc --noEmit`**, **`npm run lint`**, **`npm run build`**. Fix only regressions introduced in this phase (none expected if Tasks 1–2 made no edits).
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - All three commands exit 0
  </acceptance_criteria>
  <done>Build green</done>
</task>

<task type="auto">
  <name>Task 4: Phase summary</name>
  <files>.planning/phases/05-score-repeat/05-01-SUMMARY.md</files>
  <action>
Create **`.planning/phases/05-score-repeat/05-01-SUMMARY.md`** with:
- Date, plan id **05-01**
- Table mapping **SCORE-01–04** → evidence (file + brief note)
- Note **D-11** toast dedupe status
- Verification commands run + results
  </action>
  <verify>test -f .planning/phases/05-score-repeat/05-01-SUMMARY.md</verify>
  <acceptance_criteria>
    - SUMMARY exists and mentions all four SCORE IDs
  </acceptance_criteria>
  <done>Summary written</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Browser IndexedDB | Quiz sessions and wrong-question IDs stay local; no server exfiltration in this phase |

## STRIDE (abbreviated)

| ID | Cat | Component | Risk | Mitigation |
|----|-----|-----------|------|------------|
| T-05-1 | I | IDB | Tampering by other scripts same origin | Same as rest of app; no new vectors |
| T-05-2 | E | Wrong IDs | Stale drill list | `recordQuizCompletion` overwrites `studyWrongHistory` per session end |

</threat_model>

<verification>
- [ ] `npx tsc --noEmit`
- [ ] `npm run lint`
- [ ] `npm run build`
</verification>

<success_criteria>
- SCORE-01–04 each have explicit evidence in **05-01-SUMMARY.md**
- No unchecked contradiction between **05-CONTEXT** decisions and **PlaySession** finished UI
</success_criteria>

<output>
After completion, ensure **`.planning/phases/05-score-repeat/05-01-SUMMARY.md`** exists.
</output>
