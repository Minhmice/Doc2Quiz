---
phase: 12-unified-parse-engine
plan: 02
type: execute
wave: 2
depends_on:
  - "12-01-PLAN.md"
files_modified:
  - src/components/ai/AiParseSection.tsx
  - src/components/ai/AiParseParseStrategyPanel.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "`getDocument(studySetId)` from `@/lib/db/studySetDb` is used to read `extractedText.length` and `pageCount` before routing in `runUnifiedParseInternal`, so the text pipeline already persisted at ingest time informs user-visible policy (Phase 12 goal)."
    - "`parseRoutePolicy.decideParseRoute` is invoked once per user-triggered parse (after `onBeforeParse` succeeds, before strategy handlers) and the decision is passed to `pipelineLog` under a stable key (e.g. `parseRoutePolicy` object field) for dev/UAT."
    - "User-facing copy distinguishes **Accurate = vision-first** vs **Fast/Hybrid = text+OCR aware** without removing vision requirement for OpenAI/Custom where the code still renders pages."
  artifacts:
    - path: src/components/ai/AiParseSection.tsx
      provides: Document-backed policy call + logging; optional refinement of `hintMessage` / blocked state copy using `decision.rationale` substring **only when** it does not contradict existing provider gates
    - path: src/components/ai/AiParseParseStrategyPanel.tsx
      provides: Short helper line under strategy controls when document signal is strong native text (e.g. suggest Fast) — string must include substring `text layer` (ASCII, for grep verification)
  key_links:
    - from: AiParseSection.tsx
      to: src/lib/ai/parseRoutePolicy.ts
      via: "`import { decideParseRoute } from \"@/lib/ai/parseRoutePolicy\"`"
    - from: AiParseSection.tsx
      to: src/lib/db/studySetDb.ts
      via: "`getDocument`"
---

<objective>
**Wave 2 — wire policy to UI + logs:** Connect IndexedDB **extracted text** + page metadata into the parse entrypoint so the product no longer behaves as if only vision signals exist, and surface policy output in strategy UI + structured logs.

**Output:** Updated `AiParseSection.tsx` (+ minimal presenter tweak in `AiParseParseStrategyPanel.tsx` if needed; optionally `AiParseSectionHeader.tsx` for a single-line banner).
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Logging full `extractedText` | High | Log only `extractedTextCharCount`, `pageCount`, `reasonCodes`, `executionFamily` — never the text body. |
| Extra IDB read blocks UI thread | Low | `getDocument` is async already in parse path; swallow errors and fall back to `extractedTextCharCount: 0` + log warning code `"document_read_failed"`. |
| Parse button enabled when unsafe | Medium | Do not remove `visionForwardReady` / `hasKey` guards; only enrich messaging and policy **inside** allowed runs. |

**Blockers:** Wave **12-01** must merge first (`parseRoutePolicy.ts` present).
</threat_model>

## Task 1 — Load document + compute policy in `runUnifiedParseInternal`

<action>
In `src/components/ai/AiParseSection.tsx`, inside `runUnifiedParseInternal` **after** successful `await onBeforeParse?.()` and **after** `hasKey` / `activePdfFile` validations, **before** the `visionForwardReady` early-return block:

1. `let extractedTextCharCount = 0` and `let pageCountForPolicy: number | null = pageCount` (reuse existing `pageCount` prop if non-null).
2. If `studySetId.trim().length > 0`, `try { const doc = await getDocument(studySetId.trim()); extractedTextCharCount = doc?.extractedText?.length ?? 0; if (pageCountForPolicy == null && typeof doc?.pageCount === \"number\") { pageCountForPolicy = doc.pageCount; } } catch { /* ignore */ }`.
3. Import `getDocument` from `@/lib/db/studySetDb` if not already present (grep first).
4. Call `decideParseRoute({ pageCount: pageCountForPolicy, extractedTextCharCount, parseStrategy, enableOcr })` and store in `const routeDecision`.
5. Call `pipelineLog` with scope e.g. `\"PARSE_ROUTE\"` or `\"VISION\"` (reuse existing logger conventions in file), **info** level, payload including: `studySetId`, `executionFamily: routeDecision.executionFamily`, `reasonCodes: [...routeDecision.reasonCodes]`, `extractedTextCharCount`, `pageCount: pageCountForPolicy` — **no raw text**.

Keep the existing `if (!visionForwardReady) { setError(\"Switch to OpenAI or Custom...` block **after** this logging unless product owner decides otherwise in execute — policy must not bypass provider capability gates in this phase.
</action>

<read_first>
- `src/components/ai/AiParseSection.tsx` (`runUnifiedParseInternal` ~lines 1418–1520, imports top of file)
- `src/lib/db/studySetDb.ts` (`getDocument`)
- `src/lib/ai/parseRoutePolicy.ts` (from 12-01)
</read_first>

<acceptance_criteria>
- `rg "decideParseRoute" src/components/ai/AiParseSection.tsx` returns at least one match.
- `rg "getDocument" src/components/ai/AiParseSection.tsx` returns at least one match.
- `rg "pipelineLog" src/components/ai/AiParseSection.tsx` matches existing usage; **new** log line must include literal substring `reasonCodes` **or** literal `executionFamily` in the same `pipelineLog` call’s message/meta (grep file for `reasonCodes` OR `executionFamily` inside `AiParseSection.tsx`).
- `npm run lint` exits 0.
</acceptance_criteria>

## Task 2 — Strategy panel: document signal hint

<action>
Update `src/components/ai/AiParseParseStrategyPanel.tsx` to accept optional props, e.g. `documentHint?: { kind: \"strong_text_layer\" | \"none\" }` (shape is flexible).

In `AiParseSection.tsx`, maintain **preview** policy for the panel (separate from per-parse log in Task 1): e.g. `useState` + `useEffect` that depends on `[studySetId, pageCount, parseStrategy, enableOcr]` and calls `getDocument(studySetId)` then `decideParseRoute({...})`. When `decision.reasonCodes` includes **`text_layer_strong`** (exact string from 12-01 `parseRoutePolicy.ts`), set hint `strong_text_layer`; else `none`.

Pass `documentHint` into every `<AiParseParseStrategyPanel` usage from that state.

When `documentHint?.kind === \"strong_text_layer\"`, render a muted one-line note containing the exact substring **`text layer`** (ASCII) suggesting Fast layout-aware path for token efficiency (wording otherwise free).

If panel is used in two places in `AiParseSection`, pass the hint in **both** or refactor to a single fragment — no duplicate divergent strings.
</action>

<read_first>
- `src/components/ai/AiParseParseStrategyPanel.tsx`
- `src/components/ai/AiParseSection.tsx` (all `<AiParseParseStrategyPanel` usages)
- `src/lib/ai/parseRoutePolicy.ts` (confirm `text_layer_strong` spelling)
</read_first>

<acceptance_criteria>
- `rg "text layer" src/components/ai/AiParseParseStrategyPanel.tsx` returns at least one match.
- `rg "documentHint" src/components/ai/AiParseParseStrategyPanel.tsx` returns at least one match.
- `rg "decideParseRoute" src/components/ai/AiParseSection.tsx` returns **at least two** matches (parse-time + preview effect or two call sites).
- `npm run lint` exits 0.
</acceptance_criteria>

## Verification (phase)

- `npm run lint` and `npm run build` exit 0.
- Manual: create/open a study set with a text-heavy PDF, open parse UI, confirm parse overlay log or strategy area reflects policy (strong text hint when applicable).
