---
phase: 30-replace-page-level-chunking-with-layout-aware-chunking
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/components/ai/AiParseSection.tsx
  - src/lib/pdf/classifyPdfPages.ts
  - src/lib/pdf/extractPdfLayoutBlocks.ts
  - src/lib/ai/layoutChunking.ts
autonomous: true
requirements: ["LAYOUT-30-01", "LAYOUT-30-02", "LAYOUT-30-03", "LAYOUT-30-04"]

must_haves:
  truths:
    - "In quiz mode, Phase 29 `text` pages are parsed without rasterization using layout-aware chunks derived from pdf.js text item geometry."
    - "Preview-first still prioritizes the first 3–5 pages; users see early questions as soon as preview chunks finish."
    - "If layout-chunk parse fails or looks weak for specific pages/chunks, only those pages are added to the vision page list (bounded by existing caps)."
  artifacts:
    - path: "src/components/ai/AiParseSection.tsx"
      provides: "Quiz lane uses layout-aware chunking for routePlan.textPageIndices"
    - path: "src/lib/pdf/extractPdfLayoutBlocks.ts"
      provides: "Supports extracting blocks for a subset of pages with abort + truncation metadata"
    - path: "src/lib/ai/layoutChunking.ts"
      provides: "Provides chunk metadata with page provenance for per-page fallback"
  key_links:
    - from: "src/components/ai/AiParseSection.tsx"
      to: "classifyPdfPages(...)"
      via: "Phase 29 routing before rasterization"
      pattern: "classifyPdfPages\\("
    - from: "src/components/ai/AiParseSection.tsx"
      to: "extractPdfLayoutBlocksForPageIndices(...)"
      via: "routePlan.textPageIndices extraction"
      pattern: "extractPdfLayoutBlocksForPageIndices\\("
    - from: "src/components/ai/AiParseSection.tsx"
      to: "runRenderPagesAndOptionalOcr(...pageIndices: ...)"
      via: "vision rasterization for bitmap + fallback pages only"
      pattern: "pageIndices:"
---

<objective>
Wire layout-aware chunking into the quiz text lane for Phase 29 `text` pages, preserving preview-first 3–5 pages and enabling conservative per-page fallback to vision (per D-04..D-06).

Purpose: Improve quiz parsing accuracy for born-digital/mixed-layout documents without increasing rasterization cost for text pages.
Output: Updated `AiParseSection` quiz routing path that uses layout chunks (pdf.js geometry) for text pages and escalates only problematic pages to vision within caps.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-replace-page-level-chunking-with-layout-aware-chunking/30-CONTEXT.md
@src/components/ai/AiParseSection.tsx
@src/lib/pdf/classifyPdfPages.ts
@src/lib/pdf/extractPdfText.ts
@src/lib/ai/runSequentialParse.ts
@src/lib/ai/chunkText.ts
</context>

<interfaces>
From `src/components/ai/AiParseSection.tsx` (current behavior to replace for routed text pages):
- `routePlan.textPageIndices` is a list of 1-based page indices to parse without rasterization.
- Current flow for routed text pages:
  - `text = await extractTextForPageIndices(file, routePlan.textPageIndices, signal)`
  - `chunks = chunkText(text)`
  - `runSequentialParse({ chunks, ... })`
We will replace the `extractTextForPageIndices + chunkText` step with:
- `extractPdfLayoutBlocksForPageIndices(file, routePlan.textPageIndices, ...)`
- `layoutBlocksToQuizChunks(...)` returning `{ chunkText, sourcePageIndices }[]`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace routed quiz text-page linear chunking with layout-aware chunking</name>
  <read_first>
    - src/components/ai/AiParseSection.tsx (section around Phase 29 routePlan + routedTextQuestions)
    - src/lib/pdf/extractPdfLayoutBlocks.ts (Plan 30-01 new helper)
    - src/lib/ai/layoutChunking.ts (Plan 30-01 new helper)
  </read_first>
  <files>src/components/ai/AiParseSection.tsx</files>
  <action>
In `handleVisionParse` under:
```ts
// Quiz lane: if page routing says we have text pages, parse them without rasterization.
```
Replace:
- `extractTextForPageIndices(...)` + `chunkText(text)`
With:
- `const layout = await extractPdfLayoutBlocksForPageIndices(activePdfFile, routePlan.textPageIndices, { signal: controller.signal, /* conservative caps */ })`
- `const routedChunks = layoutBlocksToQuizChunks(layout.pages, { /* target 1–2 MCQs */ })`
- `const chunks = routedChunks.map((c) => c.chunkText)`

Keep all existing progress/overlay updates and `runSequentialParse` wiring, but:
- Add overlay log line that includes: number of text pages, block count, chunk count.
- Preserve **no rasterization** behavior for text pages (do not call render-pages pipeline for these).

Do NOT change flashcard behavior (still vision-batch-only).
  </action>
  <acceptance_criteria>
    - For routed text pages, quiz mode no longer calls `chunkText(text)`; it uses layout-aware chunks derived from pdf.js geometry.
    - Behavior is deterministic: given the same PDF and routing plan, the same chunk strings are produced.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
  </verify>
  <done>
Quiz routed text pages use layout-aware chunking while keeping the existing sequential parse lane contract intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Preview-first for layout chunks + conservative per-page vision fallback list</name>
  <read_first>
    - .planning/phases/30-replace-page-level-chunking-with-layout-aware-chunking/30-CONTEXT.md (D-04, D-06)
    - src/components/ai/AiParseSection.tsx (preview budget clamping + bitmapIndicesForVision wiring)
  </read_first>
  <files>src/components/ai/AiParseSection.tsx, src/lib/ai/layoutChunking.ts</files>
  <action>
Implement preview-first for routed text pages:
- Use the existing `previewBudgetClamped` / `previewFirstPageBudget` logic to split `routePlan.textPageIndices` into:
  - `previewTextPageIndices` (first N pages in document order, N clamped 3–5 on product)
  - `restTextPageIndices`
- Extract blocks and build chunks for preview pages first; run `runSequentialParse` for preview chunks first so early questions appear quickly.
- After preview completes, continue with rest chunks if any.

Implement conservative per-page fallback list (D-04) without new UI:
- Use chunk provenance (`sourcePageIndices`) to attribute failures/weakness to specific pages.
- Define a deterministic “weak output” signal for preview/rest segments (Claude discretion, but conservative), such as:
  - 0 questions from chunks that came from a given page, OR
  - valid MCQ ratio below a threshold for questions whose `sourcePages` include that page (if available), OR
  - extractor reported `truncated: true` for that page (treat as high-risk)
- Build `fallbackPageIndices: number[]` as a subset of routed text pages.

Then, when preparing vision rendering, extend the existing `bitmapIndicesForVision` list to include `fallbackPageIndices`, but:
- Preserve existing caps from Phase 29 (do not exceed `VISION_MAX_PAGES_DEFAULT`; if the union exceeds cap, drop fallback pages first, and log count dropped).
- Preserve preview-first for vision: if `previewBudgetClamped > 0`, ensure fallback pages in the first preview window are prioritized in the raster preview list.
  </action>
  <acceptance_criteria>
    - Preview-first behavior still holds (first 3–5 pages produce early questions fast) for the layout-chunk path.
    - Only pages deemed weak/truncated are added to the vision `pageIndices` list; no whole-document fallback.
    - Unioning fallback pages with bitmap pages never increases vision workload beyond existing caps.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
  </verify>
  <done>
Layout chunking is preview-first and can escalate only problematic pages to the existing vision lane within caps.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| routed text pages → layout chunks | Untrusted PDF text geometry can be adversarial (many items, weird transforms) |
| fallback pages → vision | Fallback can increase cost if unbounded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-30-04 | D | routed text extraction + chunking in `AiParseSection` | mitigate | Keep per-page caps + truncation signals; avoid processing more than routed pages |
| T-30-05 | D | per-page fallback | mitigate | Union fallback pages with bitmap pages but clamp to `VISION_MAX_PAGES_DEFAULT` (drop fallback first) |
| T-30-06 | R | fallback reasoning | mitigate | Ensure deterministic reason codes / counts are available for the single summary log (implemented in Plan 30-03) |
</threat_model>

<verification>
- `npm run lint` passes
- `npm run build` passes
</verification>

<success_criteria>
- Quiz routed text pages no longer use linear `chunkText` in the Phase 29 routing seam.
- Preview-first remains 3–5 pages for both layout-chunk parse and any fallback vision preview.
- Fallback is per-page and bounded; bitmap pages remain vision as-is.
</success_criteria>

<output>
After completion, create `.planning/phases/30-replace-page-level-chunking-with-layout-aware-chunking/30-02-SUMMARY.md`
</output>

