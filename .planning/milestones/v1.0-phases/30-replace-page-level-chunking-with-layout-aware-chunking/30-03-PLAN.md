---
phase: 30-replace-page-level-chunking-with-layout-aware-chunking
plan: 03
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - src/components/ai/AiParseSection.tsx
  - src/lib/logging/pipelineLogger.ts
  - src/lib/pdf/extractPdfLayoutBlocks.ts
autonomous: true
requirements: ["LAYOUT-30-03", "LAYOUT-30-04"]

must_haves:
  truths:
    - "A single numeric summary `pipelineLog` is emitted for each layout-aware chunking run (counts/caps/fallbacks/walltime), with no new UI controls."
    - "Layout block extraction and chunking are bounded and observable even when pages are truncated or aborted."
  artifacts:
    - path: "src/components/ai/AiParseSection.tsx"
      provides: "One summary pipelineLog for layout chunking runs (D-07)"
  key_links:
    - from: "src/components/ai/AiParseSection.tsx"
      to: "pipelineLog(...)"
      via: "single summary emission"
      pattern: "pipelineLog\\("
---

<objective>
Harden the layout-aware chunking integration and add exactly one summary `pipelineLog` event for each layout-aware chunking run, containing numeric counts/caps/fallback counts and wall-time totals (per D-07), without adding UI controls.

Purpose: Make Phase 30 behavior debuggable and safe (bounded work + measurable fallbacks) while keeping product UX unchanged.
Output: One structured summary log for layout-chunk runs + guardrails around abort/truncation/caps.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-replace-page-level-chunking-with-layout-aware-chunking/30-CONTEXT.md
@src/components/ai/AiParseSection.tsx
@src/lib/logging/pipelineLogger.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit exactly-one summary pipelineLog for layout-aware chunking runs (D-07)</name>
  <read_first>
    - .planning/phases/30-replace-page-level-chunking-with-layout-aware-chunking/30-CONTEXT.md (D-07)
    - src/components/ai/AiParseSection.tsx (Phase 29 “page-route” summary log pattern)
  </read_first>
  <files>src/components/ai/AiParseSection.tsx</files>
  <action>
Add one summary log for layout-aware chunking runs, modeled after the Phase 29 routing summary:
- Log domain/stage: use a stable pair similar to existing patterns (e.g. `pipelineLog("VISION","layout-chunk-route",...)` or `("VISION","layout-chunking",...)`), but ensure it is **exactly one** call per run.
- Emit it once the routed text-page parse + fallback page decision is finalized (after preview/rest attempts, before calling vision render).

The log payload must be numeric/count-based only (no per-block text), including at minimum:
- `studySetId`
- `counts`:
  - `textPageCountPlanned`
  - `textPageCountExtracted`
  - `textPageTruncatedCount`
  - `layoutBlockCountTotal`
  - `layoutChunkCountTotal`
  - `layoutChunkParseQuestionCount` (total questions produced from layout chunks)
  - `fallbackToVisionPageCount` (subset from text pages)
  - `fallbackDroppedByCapCount` (if fallback pages dropped due to vision max cap)
- `caps`:
  - `previewFirstPageBudgetApplied`
  - `visionMaxPages`
- `wallMs`:
  - `extractBlocksMs`
  - `buildChunksMs`
  - `sequentialParseMs`
  - `layoutChunkingTotalMs`
- `stableReasonCodes` or `reasons`: stable string codes for why pages fell back (e.g. `truncated`, `zero_questions`, `weak_valid_ratio`), but do NOT include raw extracted text.

Ensure:
- Only one emission per run (even if early return or abort): if aborted mid-way, emit the summary with `aborted: true` and partial counts if safe.
  </action>
  <acceptance_criteria>
    - Exactly one summary `pipelineLog` is emitted per layout chunking run, including when the run aborts.
    - Payload is numeric/count-based; no raw PDF text or chunk content is logged.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
  </verify>
  <done>
The layout-aware chunking path is observable via a single stable summary log per run, satisfying D-07 without adding UI.
  </done>
</task>

<task type="auto">
  <name>Task 2: Harden truncation + abort handling and ensure caps enforcement is explicit</name>
  <read_first>
    - src/lib/pdf/extractPdfLayoutBlocks.ts (truncation metadata + abort behavior)
    - src/components/ai/AiParseSection.tsx (cap enforcement for vision pages)
  </read_first>
  <files>src/lib/pdf/extractPdfLayoutBlocks.ts, src/components/ai/AiParseSection.tsx</files>
  <action>
Harden edge cases so fallback remains bounded and deterministic:
- Ensure `extractPdfLayoutBlocksForPageIndices` returns `truncated` and `itemCount` per page; on abort, returns promptly with empty/partial but does not throw.
- In `AiParseSection`, ensure the union of:
  - Phase 29 `bitmapPageIndicesForVision`
  - Phase 30 `fallbackPageIndices` (from text pages)
  is deduped, sorted, and clamped to `VISION_MAX_PAGES_DEFAULT` with a clear “dropped fallback pages first” policy.
- If any truncation occurs on preview pages, ensure those pages are prioritized for fallback (within caps) and reflected in the summary log counts.
- Keep log verbosity bounded: per-page details (if any) must remain behind existing verbose toggles; default is summary-only.
  </action>
  <acceptance_criteria>
    - Fallback union + clamping is deterministic and never increases vision workload beyond caps.
    - Abort produces clean UI state (no uncaught errors) and summary log still records partial counts.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
  </verify>
  <done>
Integration is hardened: truncation/abort/caps behavior is explicit, bounded, and reflected in the single summary log.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF-derived geometry → logs | Risk of leaking content or bloating logs |
| fallback pages list → vision render | Risk of exceeding caps/cost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-30-07 | I | summary log payload | mitigate | Log counts only; never include chunk text; keep reason codes generic |
| T-30-08 | D | vision fallback list growth | mitigate | Explicit clamp policy; drop fallback pages first; log dropped count |
| T-30-09 | R | “exactly one log” contract | mitigate | Centralize emission in one codepath with finally-guard; include `aborted` boolean |
</threat_model>

<verification>
- `npm run lint` passes
- `npm run build` passes
</verification>

<success_criteria>
- D-07 satisfied: exactly one summary `pipelineLog` for layout-aware chunking runs with counts/caps/fallback/walltime.
- No new UI controls introduced; any extra per-page detail remains behind existing verbose logging.
</success_criteria>

<output>
After completion, create `.planning/phases/30-replace-page-level-chunking-with-layout-aware-chunking/30-03-SUMMARY.md`
</output>

