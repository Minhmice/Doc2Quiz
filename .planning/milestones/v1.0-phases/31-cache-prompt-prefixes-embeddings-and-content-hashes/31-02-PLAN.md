---
phase: 31-cache-prompt-prefixes-embeddings-and-content-hashes
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - src/lib/ai/visionParseCache.ts
  - src/lib/ai/runVisionBatchSequential.ts
autonomous: true
requirements:
  - "CACHE-31-02"
  - "CACHE-31-03"
  - "CACHE-31-04"
  - "CACHE-31-05"
  - "CACHE-31-07"

must_haves:
  truths:
    - "Vision batch cache lookups include model, forward provider, and prompt identity derived from the actual system prompt + bundle version."
    - "Cache hits persist across reloads when IndexedDB is available."
    - "When IndexedDB is unavailable or errors, vision parse still works using in-memory fallback (current behavior quality)."
  artifacts:
    - path: "src/lib/ai/visionParseCache.ts"
      provides: "IDB-backed get/set with memory Map fallback; key builder for vision lane"
    - path: "src/lib/ai/runVisionBatchSequential.ts"
      provides: "Passes modelId, forwardProvider, systemText into cache key; uses durable store"
  key_links:
    - from: "src/lib/ai/runVisionBatchSequential.ts"
      to: "src/lib/ai/visionParseCache.ts"
      via: "get/set cached batch items"
      pattern: "getCachedVisionBatchResult|setCachedVisionBatchResult"
    - from: "src/lib/ai/visionParseCache.ts"
      to: "src/lib/db/parseCacheDb.ts"
      via: "persist vision entries"
      pattern: "parseCacheGet|parseCacheSet"
---

<objective>
Migrate **vision batch** parse cache from a **memory-only `Map`** to **IndexedDB** using the shared cache contract from 31-01, with **backward-compatible in-memory fallback** and **non-regression** for prompt/model/provider separation (D-04, D-06).

Purpose: Eliminate redundant multimodal API calls across sessions for identical batches and configurations.
Output: Updated `visionParseCache` + `runVisionBatchSequential` integration only.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-cache-prompt-prefixes-embeddings-and-content-hashes/31-CONTEXT.md
@src/lib/ai/visionParseCache.ts
@src/lib/ai/runVisionBatchSequential.ts
@src/lib/db/parseCacheDb.ts
@src/lib/ai/parseCacheTypes.ts
@src/lib/ai/prompts/mcqExtractionPrompts.ts
@src/lib/ai/visionPrompts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vision cache key — include model, provider, prompt identity; retire VISION_BATCH_PROMPT_V as authority</name>
  <files>src/lib/ai/visionParseCache.ts</files>
  <action>
Per **D-03, D-04, CACHE-31-04**:
- Replace use of `VISION_BATCH_PROMPT_V` inside `hashVisionBatch` (or split responsibilities): **`contentFingerprint`** = hash of **page image fingerprints only** (keep existing per-page `pageIndex:dataUrl.length:head` logic) + `mode` + optional `extraFingerprint` (flashcard config JSON).
- Add new exported **`buildVisionBatchCacheKey(params)`** (or extend `hashVisionBatch` signature) accepting:
  - `pages`, `mode`, `extraFingerprint?` (same as today)
  - **`model: string`**, **`forwardProvider: "openai" | "custom"`**
  - **`systemText: string`** (exact string sent as system message for that batch wave)
  - Uses **`PROMPTS_BUNDLE_VERSION`** + **`hashPromptIdentity(systemText)`** from `mcqExtractionPrompts.ts` for `promptIdentity` field in `ParseCacheKeyParts`.
- Final **store key** = canonical key from `ParseCacheKeyParts` with `lane: "vision_batch"` + `contentFingerprint` from SHA-256/fallback over the **content material** (not including model/prompt — those are separate fields in key parts, then hashed together in `parseCacheDb` key derivation).

**Deprecate:** `VISION_BATCH_PROMPT_V` — either remove after updating all imports or keep **deprecated re-export** that points to `formatPromptKeyComponent` for one release; grep repo and fix.

- Keep **`clearVisionParseCacheMemory()`** for tests; add **`clearVisionParseCacheAll()`** optional that also clears IDB store entries for `vision_batch` (best-effort) if useful for dev.

**In-memory Map:** Retain as **L1** optional fast path: check memory first, then IDB, on miss populate both; or **IDB only + memory** per simpler design — choose **memory L1 + IDB L2** so micro-benchmarks stay fast within session (document in comment).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
`hashVisionBatch` / new builder produces keys that change when `systemText`, `model`, or `forwardProvider` change; `VISION_BATCH_PROMPT_V` no longer sole version gate.
  </done>
</task>

<task type="auto">
  <name>Task 2: IDB get/set + wire runVisionBatchSequential</name>
  <files>src/lib/ai/visionParseCache.ts, src/lib/ai/runVisionBatchSequential.ts</files>
  <action>
**`visionParseCache.ts`**
- Implement `getCachedVisionBatchResult` / `setCachedVisionBatchResult` to:
  1. Try L1 memory (if enabled).
  2. **`parseCacheGet`** from `vision_batch` store using full key.
  3. On set: **`parseCacheSet`** + memory L1 + **LRU enforcement** (already in db layer from 31-01).

**Validation:** On get, if parsed JSON fails schema expectations, **delete key** and return null (T-31-03).

**`runVisionBatchSequential.ts`**
- In `processBatchWave`, **before** `hashVisionBatch` call, compute **`systemText`** for that wave (already built later — refactor so system prompt is available at cache lookup time; same string passed to `postVisionBatchCompletion`).
- Pass **`modelId`**, **`forwardProvider`**, **`systemText`** into vision cache key builder.
- On **legacy fallback** path where `systemText` changes, keys must differ automatically (new system prompt digest).

Ensure **`onItemsExtracted` / `batch_cache_hit` events** still fire correctly.

**SSR:** Cache functions must no-op safely when `window`/`indexedDB` absent — memory L1 can still work on server? Vision runs client-only; guard defensively.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
Vision batches hit durable cache after reload for identical inputs; changing API key does not need to invalidate cache (key does not include apiKey per D-07 — acceptable; optional future: hash key prefix only).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → vendor API | Cache reduces calls; must not serve stale data across incompatible prompts/models. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | T | Cached vision items | mitigate | Key includes prompt digest + model + provider + content fingerprint (CACHE-31-02). |
| T-31-05 | D | Corrupt IDB entry | mitigate | Validate on read; delete and miss on parse failure. |
</threat_model>

<verification>
`npm run lint`; manual smoke: run vision parse twice — second run logs cache hits; reload page — third run still hits IDB.
</verification>

<success_criteria>
CACHE-31-05 satisfied for vision lane; CACHE-31-04 alignment live for vision system prompt hashing.
</success_criteria>

<output>
After completion, create `.planning/phases/31-cache-prompt-prefixes-embeddings-and-content-hashes/31-02-SUMMARY.md`
</output>
