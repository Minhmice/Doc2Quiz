---
phase: 18-parseScore-contract
plan: 02
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - src/lib/ai/deriveParseScores.ts
  - src/lib/ai/mappingQuality.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Pure deterministic functions derive `OcrRunQuality`, `OcrPageQuality`, `QuestionParseQuality`, and a (possibly empty) `ParseRetryHistory` from existing in-memory/persisted shapes without network I/O."
    - "A small display-oriented helper (or DTO builder) exists for future review/preview consumers without changing `getMappingQualityTier` outcomes."
    - "`npm run lint` and `npm run build` succeed; review badge behavior remains unchanged for the same `Question` inputs."
  artifacts:
    - path: src/lib/ai/deriveParseScores.ts
      provides: "Derivation + display DTO helpers"
      min_lines: 60
    - path: src/lib/ai/mappingQuality.ts
      provides: "Optional thin wiring only — tier ladder unchanged"
      min_lines: 1
  key_links:
    - from: src/lib/ai/deriveParseScores.ts
      to: src/types/parseScore.ts
      via: "Imports contract types; implements mapping described in `docs/PARSE-SCORE-contract.md`"
      pattern: "from \\\"@/types/parseScore\\\"|from '\\.\\./types/parseScore'"
---

<objective>
**Wave 2 — deterministic derivation:** Implement `src/lib/ai/deriveParseScores.ts` to **deterministically** build `parseScore` contract views from existing `Question`, `OcrRunResult`, and `OcrPageResult` data, plus helpers suitable for a future **display DTO**. Optionally expose one **thin** bridge from `mappingQuality.ts` **without** altering badge tier results.

**Purpose:** Make the Phase 18 contract **executable** in code while keeping `getMappingQualityTier` / uncertain-mapping behavior stable for current review UI.

**Output:** `deriveParseScores.ts` (+ minimal optional edits to `mappingQuality.ts`).
</objective>

<context>
@docs/PARSE-SCORE-contract.md (from plan 18-01)
@src/types/parseScore.ts (from plan 18-01)
@src/types/question.ts
@src/types/ocr.ts
@src/lib/ai/mappingQuality.ts (compatibility surface — do not break)

<interfaces>
Existing mapping API (must preserve semantics):

```typescript
// src/lib/ai/mappingQuality.ts
export type MappingQualityTier = "mapped" | "uncertain" | "unresolved";
export function getMappingQualityTier(q: Question): MappingQualityTier;
export function isUncertainPageMapping(q: Question): boolean;
```

Key derivation inputs:

```typescript
// OcrRunResult / OcrPageResult — src/types/ocr.ts
// Question — src/types/question.ts
```
</interfaces>
</context>

<threat_model>
| Threat ID | Category (STRIDE) | Component | Disposition | Mitigation |
|-----------|-------------------|-----------|-------------|------------|
| T-18-02-01 | T | `deriveParseScores.ts` | mitigate | Pure functions only — no `fetch`, no `localStorage`, no secrets; unit tests or grep-verified determinism from given inputs. |
| T-18-02-02 | I | Display DTO | mitigate | DTO is non-authoritative for security decisions; document that UI must not hide `unresolved` mapping based solely on OCR quality. |
| T-18-02-03 | E | `mappingQuality.ts` edit | mitigate | **Forbidden:** reordering or changing predicates inside `getMappingQualityTier`, `isUncertainPageMapping`, `isUnresolvedMapping`, `isBlanketSinglePageVision`. Allowed: new exports appended, comments, importing unused helpers (avoid if lint no-unused). Prefer wiring: re-export selected symbols from `deriveParseScores` **after** existing exports **or** a single new function `buildMappingTooltipContext(q)` that **does not** call tier functions. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement `src/lib/ai/deriveParseScores.ts`</name>
  <files>src/lib/ai/deriveParseScores.ts</files>
  <read_first>
  - `docs/PARSE-SCORE-contract.md`
  - `src/types/parseScore.ts`
  - `src/types/question.ts`
  - `src/types/ocr.ts`
  </read_first>
  <action>
  Add **new** file `src/lib/ai/deriveParseScores.ts` implementing at minimum:

  1. **`ocrPageQualityFromOcrPageResult(page: OcrPageResult): OcrPageQuality`** — maps `pageIndex`, `status` (default sensibly when missing, document default), block-level confidence rollup (mean or simple min/max as doc specifies), and region verification summary fields derived only from `page.regionVerification` / block counts — **no** `Question` access.

  2. **`ocrRunQualityFromOcrRunResult(run: OcrRunResult): OcrRunQuality`** — maps all pages via (1), sets `schemaVersion` to `PARSE_SCORE_SCHEMA_VERSION`, passes through or reconciles `run.stats`.

  3. **`questionParseQualityFromQuestion(q: Question): QuestionParseQuality`** — `structure` from `parseStructureValid`, options length / `correctIndex` range checks; `provenance` from `mappingMethod`, `mappingConfidence`, `verifiedRegionAvailable`, etc.; set `modelParseConfidence` from `q.parseConfidence` only — **never** read OCR block confidence here.

  4. **`emptyParseRetryHistory(): ParseRetryHistory`** — returns `{ events: [] }` (or readonly equivalent). Optional: `parseRetryHistoryFromProgress(record: ParseProgressRecord): ParseRetryHistory` that returns empty or a **single** synthetic event **only if** you can do so without inventing unreliable timestamps — **default** is empty events; document limitation in JSDoc.

  5. **Display DTO helper** — e.g. `buildParseScoreReviewDto(q: Question, ctx?: { ocrPage?: OcrPageResult })` returning a plain object with **separate** `ocrPageQuality?`, `questionParseQuality`, `retryHistory` fields for future UI — must **not** expose a single merged score property (no `overallScore`).

  Use explicit `undefined` handling consistent with existing app conventions; avoid throwing on normal partial data.

  **Deprecation path (comment only):** JSDoc on module: “Badge tiers in `mappingQuality.ts` remain the compact UX; use this module for full `parseScore` dimensions.”
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
  - `src/lib/ai/deriveParseScores.ts` exists.
  - `rg "export function (ocrPageQualityFromOcrPageResult|ocrRunQualityFromOcrRunResult|questionParseQualityFromQuestion|emptyParseRetryHistory|buildParseScoreReviewDto)" src/lib/ai/deriveParseScores.ts` matches **at least** the first four function names; fifth (`buildParseScoreReviewDto` or exact name chosen in action) must exist as the display helper.
  - `rg "overallScore|mergedScore|ocrQuality.*questionQuality" src/lib/ai/deriveParseScores.ts` returns **no** matches (no collapsed scalar naming).
  - `rg "fetch\\(|localStorage|indexedDB" src/lib/ai/deriveParseScores.ts` returns **no** matches (case-insensitive).
  </acceptance_criteria>
  <done>Derivation module complete; lint passes.</done>
</task>

<task type="auto">
  <name>Task 2: Optional `mappingQuality` bridge + build verification</name>
  <files>src/lib/ai/mappingQuality.ts</files>
  <read_first>
  - `src/lib/ai/mappingQuality.ts` (full file — preserve tier ladder byte-for-byte in logic)
  - `src/lib/ai/deriveParseScores.ts` (Task 1)
  </read_first>
  <action>
  Choose **one** minimal wiring (document which in SUMMARY):

  - **Option A (preferred):** Append re-exports from `deriveParseScores` (e.g. `export { questionParseQualityFromQuestion, buildParseScoreReviewDto } from "./deriveParseScores";`) **below** all existing implementations — no changes to tier functions.

  - **Option B:** Add **only** JSDoc `@see ./deriveParseScores` on `getMappingQualityTier` / module block — **no** new runtime imports if unused-export lint would fail.

  **Hard rule:** Do not change the return value of `getMappingQualityTier` for any input compared to pre-change behavior (same branch order and thresholds using `UNCERTAIN_MAPPING_CONFIDENCE_THRESHOLD`).

  Run **`npm run build`** from repo root after edits.
  </action>
  <verify>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
  - `rg -n "function getMappingQualityTier" -A 25 src/lib/ai/mappingQuality.ts` shows the same **sequence** of tier checks: `unresolved` → `vision_single_page` → low confidence → mapped/uncertain fallthrough (grep for string literals `unresolved`, `vision_single_page`, `mapped`, `uncertain` in that function body).
  - `npm run lint` exit code 0; `npm run build` exit code 0.
  </acceptance_criteria>
  <done>Optional bridge applied; production build green; badges logically unchanged.</done>
</task>

</tasks>

<verification>
- `npm run lint` and `npm run build` pass after both tasks.
- No Phase 17 / estimate UI files touched (`rg "AiParseEstimate|estimateParseRun" src/lib/ai/deriveParseScores.ts` → no matches).
</verification>

<success_criteria>
- Deterministic `parseScore` derivation exists and is consumed only as a **parallel** richer API to badges.
- CI-level checks (`lint`, `build`) validate TypeScript correctness for the new module.
</success_criteria>

<output>
After completion, create `.planning/phases/18-nh-ngh-a-parsescore-th-nh-contract-ch-nh-th-c-kh-ng-ch-badge/18-02-SUMMARY.md` using the standard summary template.
</output>
