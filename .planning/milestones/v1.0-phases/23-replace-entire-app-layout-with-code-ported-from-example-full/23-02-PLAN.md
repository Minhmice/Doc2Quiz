---
phase: 23-replace-entire-app-layout-with-code-ported-from-example-full
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/app/(app)/sets/[id]/play/page.tsx
  - docs/FRONTEND-REDESIGN-INVENTORY.md
autonomous: true
requirements: []
user_setup: []
---

<objective>
**Wave 2 — First production shell alignment:** After `/develop` proves mocks, apply **one** high-value layout pass to **quiz play** using structure/classes from **`example/doc2quiz_immersive_quiz_play_mode/code.html`** around existing play logic (no change to answer keyboard handlers or session state). Update **`docs/FRONTEND-REDESIGN-INVENTORY.md`** with a row linking play route ↔ mock ↔ phase 23.
</objective>

<context>
@.planning/phases/23-replace-entire-app-layout-with-code-ported-from-example-full/23-UI-SPEC.md §4
@example/doc2quiz_immersive_quiz_play_mode/code.html
@docs/FRONTEND-REDESIGN-INVENTORY.md
</context>

<threat_model>
| Threat | Severity | Mitigation | Blockers |
|--------|----------|------------|----------|
| Breaking keyboard answer keys 1–4 | **High** | Visual-only wrappers; do not change key handlers or focus targets | None |
| Regression in session / routing | **Medium** | No new `useEffect` for navigation; preserve `href` and data fetching | None |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Inventory doc — play ↔ immersive mock</name>
  <files>docs/FRONTEND-REDESIGN-INVENTORY.md</files>
  <read_first>docs/FRONTEND-REDESIGN-INVENTORY.md,src/app/(app)/sets/[id]/play/page.tsx</read_first>
  <action>
Append a **table row** or short subsection **Phase 23** stating: production route **`/sets/[id]/play`**, reference mock folder **`example/doc2quiz_immersive_quiz_play_mode/`**, status **In progress** (or **Planned** if only doc this wave), and note **`/develop`** lab for side-by-side.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "doc2quiz_immersive_quiz_play_mode" docs/FRONTEND-REDESIGN-INVENTORY.md` exits 0.
    - `rg -n "Phase 23" docs/FRONTEND-REDESIGN-INVENTORY.md` exits 0.
  </acceptance_criteria>
  <done>Traceability from mock to route documented.</done>
</task>

<task type="auto">
  <name>Task 2: Play page outer chrome (visual only)</name>
  <files>src/app/(app)/sets/[id]/play/page.tsx</files>
  <read_first>src/app/(app)/sets/[id]/play/page.tsx,example/doc2quiz_immersive_quiz_play_mode/code.html,.planning/phases/23-replace-entire-app-layout-with-code-ported-from-example-full/23-UI-SPEC.md</read_first>
  <action>
Read the **outer layout** regions of **`code.html`** (container max-width, padding, header strip, background treatment). Apply equivalent **Tailwind** utility classes to the **outermost** wrapper(s) in **`play/page.tsx`** only — **do not** rename exports, change imports of game logic, or alter inner question components beyond spacing wrappers. If the play page delegates to a large child component, add a **single** wrapping `div` with the new classes around that child instead of editing many files.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - Play page file still exports **default** function: `rg -n "export default function" "src/app/(app)/sets/[id]/play/page.tsx"` exits 0.
  </acceptance_criteria>
  <done>Play route shows closer Mint/immersive shell without behavior change.</done>
</task>

</tasks>
