---
phase: 07-layout-aware-chunk-based-parsing-token-optimized
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/lib/ai/runLayoutChunkParse.ts
  - src/components/ai/AiParseSection.tsx
  - src/components/ai/OcrInspector.tsx
  - src/app/(app)/sets/[id]/source/page.tsx
autonomous: true
requirements:
  - AI-05
  - AI-03
  - AI-04
user_setup: []

must_haves:
  truths:
    - "Existing merge/dedupe + document-level vision fallback union (D-11, D-23) and optional `Question` parse fields (D-13, D-25) remain correct — Task 2/3 must not regress `dedupeQuestionsByStem`, `applyQuestionPageMapping`, or draft persist; verify with `rg` anchors listed in plan acceptance where those paths are touched."
    - "Each layout chunk records summed wall time for every injected `parse()` call (initial + expanded retry), plus `parseAttempts` and optional per-attempt ms array (D-27, 07-RESEARCH.md)."
    - "One run-level wall clock `lastParseRunWallMs` covers the full user-triggered parse from after successful `onBeforeParse` through terminal state in a `finally` (D-28); includes vision fallback and post-parse work in the same flow as RESEARCH (with UI footnote when fallback ran)."
    - "Parse summary shows `Parse time: {n.n}s` (one decimal) directly under the summary line when a finite duration is known; if vision fallback merged, append muted `Includes full-page vision fallback.` (07-UI-SPEC.md)."
    - "Chunk parse debug: collapsible **default closed**, table columns Chunk id / Outcome / Expanded retry / AI time / Attempts; optional per-attempt breakdown collapsed by default; raw output in nested `<details>` (D-26, D-29, D-15, D-25)."
    - "localStorage parse mode key remains exactly `doc2quiz:parse:strategy` with values `fast|accurate|hybrid` (07-UI-SPEC.md)."
    - "Cancel control visible label is **Cancel parsing (stop AI processing)** everywhere it appears (full + embedded paths) (07-UI-SPEC Copywriting Contract)."
    - "Verbose `pipelineLog` lines for chunk timing use existing `PipelineDomain` (use **`VISION`**) and a **distinct** `stage` string (e.g. `chunk-timing`); gate with `isPipelineVerbose()` per 06-CONTEXT D-05 — no ad-hoc `console.info`."
  artifacts:
    - path: src/lib/ai/runLayoutChunkParse.ts
      provides: ChunkParseResult + timing aggregation + optional verbose pipelineLog
    - path: src/components/ai/AiParseSection.tsx
      provides: runUnifiedParseInternal timer, ParseRunResult extension, summary + parse time + cancel label + embedded summary/duration when applicable
    - path: src/components/ai/OcrInspector.tsx
      provides: Chunk parse debug disclosure + table + typography only per UI-SPEC for **new/edited** Phase 7 surfaces
    - path: src/app/(app)/sets/[id]/source/page.tsx
      provides: Passes chunk debug payload into OcrInspector after parse
  key_links:
    - from: AiParseSection.tsx (`runUnifiedParseInternal`, `runLayoutChunkPipelineFromPrepared`)
      to: runLayoutChunkParse.ts
      via: "injected `parse` callback wrapping `parseChunkSingleMcqOnce`"
    - from: runLayoutChunkParse.ts
      to: pipelineLogger.ts (import only)
      via: "import { pipelineLog, isPipelineVerbose } from '@/lib/logging/pipelineLogger'"
    - from: source/page.tsx
      to: OcrInspector.tsx
      via: "props e.g. chunkParseDebug + lastParseRunWallMs / usedVisionFallback"
---

<objective>
Implement **orchestration-layer** timing (**D-27–D-29**), **run-total** wall clock (**D-28**), **pipelineLog** verbose chunk timing (**D-29** + 06 D-05), and **approved UI-SPEC** surfaces: parse time line, vision-fallback footnote, chunk debug table in **OcrInspector**, **Cancel parsing (stop AI processing)** on **all** parse cancel buttons, **`doc2quiz:parse:strategy`** unchanged, and **typography limits** (at most **four** font sizes + **two** weights on Phase 07 code you add or replace in touched components — **no** `font-bold` / 700, **no** `text-[10px]` / arbitrary sizes in **new** code; align any **edited** lines in `OcrInspector` chunk-debug section to `text-xs|sm|base` + `font-semibold` only where UI-SPEC allows).

**Purpose:** Users and developers can attribute latency to chunk AI attempts vs retries vs vision fallback, without double-counting timers (**07-RESEARCH.md**).

**Output:** Updated `runLayoutChunkParse.ts`, `AiParseSection.tsx`, `OcrInspector.tsx`, `source/page.tsx`. **Do not** edit `pipelineLogger.ts` unless you add a new `PipelineDomain` (not recommended — use `VISION` + `stage: "chunk-timing"` per RESEARCH).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-layout-aware-chunk-based-parsing-token-optimized/07-CONTEXT.md
@.planning/phases/07-layout-aware-chunk-based-parsing-token-optimized/07-RESEARCH.md
@.planning/phases/07-layout-aware-chunk-based-parsing-token-optimized/07-UI-SPEC.md
@.planning/phases/07-layout-aware-chunk-based-parsing-token-optimized/07-01-PLAN.md
@src/lib/ai/runLayoutChunkParse.ts
@src/lib/ai/parseChunk.ts
@src/components/ai/AiParseSection.tsx
@src/components/ai/OcrInspector.tsx
@src/app/(app)/sets/[id]/source/page.tsx
@src/lib/logging/pipelineLogger.ts
</context>

<interfaces>
```typescript
// Extend (names may match repo — align types in runLayoutChunkParse.ts)
export type ChunkParseResult = {
  layoutChunkId: string;
  pageIndex: number;
  outcome: ChunkParseOutcome;
  usedExpandedText: boolean;
  /** D-27: sum of wall ms around each injected parse() for this chunk row */
  chunkAiWallMs: number;
  /** Count of parse() invocations (0 when empty chunk skipped AI) */
  parseAttempts: number;
  /** Optional: wall ms per parse() call in order */
  attemptWallMs?: number[];
};

// ParseRunResult (AiParseSection.tsx) — extend export
export type ParseRunResult = {
  ok: boolean;
  aborted: boolean;
  fatalError: string | null;
  questions: Question[];
  /** D-28: monotonic wall ms for full run when terminal */
  lastParseRunWallMs?: number;
  /** True when chunk path invoked document-level vision fallback merge */
  usedVisionFallback?: boolean;
  /** Session-only debug for OcrInspector (not IDB) */
  chunkParseDebug?: {
    byChunk: ChunkParseResult[];
    rawByChunkId?: Record<string, string>;
  };
};
```
</interfaces>

## Decision coverage (this plan)

| ID | Plan | Task | Full / Partial | Notes |
|----|------|------|----------------|-------|
| D-27 | 02 | 1 | Full | `performance.now()` only around `await parse(...)` inside `runLayoutChunkParse` |
| D-28 | 02 | 2 | Full | `runUnifiedParseInternal` + `finally` |
| D-29, D-26 | 02 | 3–4 | Full | OcrInspector + summary line |
| D-15, D-25 | 02 | 3–4 | Full | Raw in session state / props only |
| pipelineLog + 06 D-05 | 02 | 1 | Full | `isPipelineVerbose()` + `VISION` + stage `chunk-timing` |
| UI-SPEC copy + typography + a11y | 02 | 2–4 | Full | Exact strings cited in tasks |
| D-14, D-16–D-24 | 02 | 2 | Partial→Full | Keep existing mode/OCR/hybrid/fallback behavior; extend only for timing + copy |

<tasks>

<task type="auto">
  <name>Task 1: ChunkParseResult + per-parse wall timing + verbose pipelineLog</name>
  <files>src/lib/ai/runLayoutChunkParse.ts</files>
  <read_first>src/lib/ai/runLayoutChunkParse.ts, src/lib/logging/pipelineLogger.ts, .planning/phases/07-layout-aware-chunk-based-parsing-token-optimized/07-RESEARCH.md (Instrumentation, pipelineLog)</read_first>
  <action>
In **`runLayoutChunkParse`** (only place for per-chunk AI wall measurement per D-27 / RESEARCH):
- Before each `await parse(userContent, signal)`, `const t0 = performance.now()`.
- After each call, push `(performance.now() - t0)` into a per-chunk `attemptWallMs` array.
- Set **`parseAttempts`** = `attemptWallMs.length` for that chunk row (including retries).
- Set **`chunkAiWallMs`** = **sum** of `attemptWallMs`.
- For **empty** chunks (no AI call): `parseAttempts: 0`, `chunkAiWallMs: 0`, omit or use empty `attemptWallMs` consistently.

On each finalized `ChunkParseResult` (and **not** on empty throws before result construction), when `isPipelineVerbose()` from `@/lib/logging/pipelineLogger` is true, call:

`pipelineLog("VISION", "chunk-timing", "info", "layout chunk AI wall", { layoutChunkId, chunkAiWallMs, parseAttempts, studySetId?: … })`

Use **domain `"VISION"`** and **stage `"chunk-timing"`** so `PipelineDomain` typing unchanged (grep all `pipelineLog(` call sites if you add a domain).

**Do not** add a second `performance.now()` wrapper inside `parseChunkSingleMcqOnce` (07-01 guarantees none).

**D-13 / D-25 on each successful `Question` from chunk parse:** set `layoutChunkId` to the chunk’s stable id; set `parseStructureValid` from validator outcome; set `parseConfidence` to a **0..1** heuristic (e.g. 1 when structure valid and all four options non-empty, else lower) — document the formula in a code comment. If fields already set elsewhere, ensure they are **not** overwritten with `undefined`.

Optional: extend injected `parse` type in comments if needed — no behavior change to `parseChunkSingleMcqOnce` signature required for timing.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "chunkAiWallMs|parseAttempts|attemptWallMs" src/lib/ai/runLayoutChunkParse.ts` shows new fields populated.
    - `rg -n "performance\\.now" src/lib/ai/runLayoutChunkParse.ts` wraps **only** around `parse(` calls (two sites max per chunk: initial + expand).
    - `rg -n "pipelineLog\\(\"VISION\", \"chunk-timing\"" src/lib/ai/runLayoutChunkParse.ts` matches at least once (exact stage string `chunk-timing`; call wrapped in `if (isPipelineVerbose())` or equivalent guard).
    - `rg -n "type PipelineDomain" src/lib/logging/pipelineLogger.ts` still lists only `PDF | OCR | VISION | IDB | STUDY_SET` (no `PARSE` added).
    - `rg -n "layoutChunkId|parseConfidence|parseStructureValid" src/lib/ai/runLayoutChunkParse.ts` shows each field assigned when a chunk yields a validated `Question` (D-25 / D-13).
  </acceptance_criteria>
  <done>Chunk rows carry D-27 metrics; verbose logs gated per 06 D-05.</done>
</task>

<task type="auto">
  <name>Task 2: Run total wall clock + ParseRunResult + AiParseSection summary / cancel / embedded</name>
  <files>src/components/ai/AiParseSection.tsx</files>
  <read_first>src/components/ai/AiParseSection.tsx (runUnifiedParseInternal, runLayoutChunkPipelineFromPrepared, handleVisionParse),.planning/phases/07-layout-aware-chunk-based-parsing-token-optimized/07-UI-SPEC.md (Progress + summary, Parse time, Copywriting)</read_first>
  <action>
**D-28 — `runUnifiedParseInternal`:**
- After successful `await onBeforeParse?.()`, record `const runStarted = performance.now()` (or bigint pattern if you standardize — prefer `performance.now()` per RESEARCH).
- Wrap the remainder in `try { ... return result } finally { const ms = performance.now() - runStarted; attach to last result object or component state for terminal runs }`.
- Ensure **every** return path from internal handlers still passes through `finally` (use a local `let last: ParseRunResult` pattern if needed).
- **`lastParseRunWallMs`**: finite number ≥ 0 only when run reaches terminal completion (success or non-fatal stop per product — **include** vision fallback + `attachPageImagesForQuestions` + `persistQuestions` when they execute inside the same invoked parse for Fast/Hybrid chunk path per RESEARCH A1; document in code comment).

**ParseRunResult** (exported type in this file): add optional fields per `<interfaces>`: `lastParseRunWallMs`, `usedVisionFallback`, `chunkParseDebug`.

**Wire debug payload:**
- When `runLayoutChunkParse` completes, populate `chunkParseDebug.byChunk` from `byChunk` results.
- **`usedVisionFallback`**: set `true` when `needsVisionFallback` branch ran vision merge (even if fatal and kept chunks only).
- **Raw model text (D-15):** If not yet captured, add minimal hook (e.g. optional param on `parseChunkSingleMcqOnce` from 07-01) or capture assistant string in the `parse` closure — store only in `chunkParseDebug.rawByChunkId` in memory; **never** write to IDB on `Question`.

**07-UI-SPEC — Parse time line (D-28):**
- Format exactly: prefix **`Parse time: `** then one decimal then **`s`** (e.g. `Parse time: 12.4s`) via `(ms/1000).toFixed(1)`.
- **Placement:** Immediately **below** the summary `<p>` in the parse section (**full** variant). Use `text-sm text-muted-foreground` for an optional second line containing exactly **`Includes full-page vision fallback.`** when `usedVisionFallback === true` and parse time is shown.
- **While running:** omit parse time entirely (no placeholder).

**Embedded (`variant="embedded"`):**
- Today summary may be hidden — **show** the same `summary` + parse time block in embedded when terminal (so source page users see D-28), inside an `aria-live="polite"` region consistent with UI-SPEC §Progress + summary.

**Cancel label (Copywriting Contract — full + embedded):**
- Find **every** user-visible cancel control in `AiParseSection.tsx` (full `variant="full"` footer **and** `variant="embedded"` / dashed-card area, including any duplicate **Cancel** while `progress.status === "running"`).
- Set **each** visible label to the exact string **`Cancel parsing (stop AI processing)`** (same as `07-UI-SPEC.md` Copywriting Contract). Do not leave bare **`Cancel`** on any parse-stop control in this file after this task.

**localStorage:** keep `LS_PARSE_STRATEGY = "doc2quiz:parse:strategy"` — grep must show this key unchanged.

**Hybrid optional status (UI-SPEC):** When hybrid chooses vision for OCR gate, optional muted one-liner **`OCR quality below threshold — using full-page vision.`** (`text-sm text-muted-foreground`) may appear once — only if trivial to wire next to existing hybrid log site.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "lastParseRunWallMs" src/components/ai/AiParseSection.tsx` exists.
    - `rg -n "Parse time:" src/components/ai/AiParseSection.tsx` shows `toFixed(1)` and `s` suffix pattern.
    - `rg -n "Includes full-page vision fallback" src/components/ai/AiParseSection.tsx` exists.
    - `rg -n "Cancel parsing \\(stop AI processing\\)" src/components/ai/AiParseSection.tsx` matches **every** parse-cancel `Button` (verify `rg -n "onClick={handleCancel}" src/components/ai/AiParseSection.tsx` — each adjacent JSX must contain the exact contract string as visible child text; repo currently has **one** such button ~1880, shared by full + embedded).
    - `rg -n "doc2quiz:parse:strategy" src/components/ai/AiParseSection.tsx` shows read/write.
    - `rg -n "finally" src/components/ai/AiParseSection.tsx` near `runUnifiedParseInternal` shows run timer completion.
    - **AI-05:** `rg -n "<Progress|progress\\.total|showInlineProgress|aria-live=\"polite\"" src/components/ai/AiParseSection.tsx` — at least **two** distinct matches proving per-chunk / run progress feedback is still present after edits (not removed in favor of parse-time-only UI).
    - **D-16:** `rg -n "runOcrSequential|getOcrResult|putOcrResult" src/components/ai/AiParseSection.tsx` matches in **Fast** / **Hybrid** path (OCR prefetch before chunk parse); add code comment `// D-16` near the branch if not already obvious from grep context.
  </acceptance_criteria>
  <done>D-28 visible in UI; ParseRunResult carries debug + timing; cancel copy matches UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: OcrInspector chunk parse debug + typography for touched UI</name>
  <files>src/components/ai/OcrInspector.tsx,src/app/(app)/sets/[id]/source/page.tsx</files>
  <read_first>.planning/phases/07-layout-aware-chunk-based-parsing-token-optimized/07-UI-SPEC.md (Debug / OCR inspector, Typography, Empty states),src/components/ai/OcrInspector.tsx</read_first>
  <action>
**OcrInspector new optional props** (names illustrative — use clear names):
- `chunkParseDebug?: { byChunk: import("@/lib/ai/runLayoutChunkParse").ChunkParseResult[]; rawByChunkId?: Record<string, string> } | null`
- `lastParseRunWallMs?: number | null`
- `usedVisionFallback?: boolean`

**Section title:** **`Chunk parse debug`** (UI-SPEC). Place at **bottom** of inspector card content (D-26).

**Disclosure:** Native `<details>` **closed by default** (no `open` attribute). `<summary>` keyboard focus ring per UI-SPEC.

**Table columns (headers exact):** **`Chunk id`**, **`Outcome`**, **`Expanded retry`**, **`AI time`**, **`Attempts`** — map from `layoutChunkId`, `outcome`, `usedExpandedText`, `chunkAiWallMs`, `parseAttempts`. Monospace + **`text-xs`** only for chunk id column and numeric cells per Typography **Monospace / debug**.

**Per-attempt breakdown:** If `attemptWallMs` present, second line in cell or nested `<details>` default closed, format like `Attempt 1: 420ms · Attempt 2: 310ms` using **`text-xs font-mono`**.

**Raw output:** Nested `<details>` summary e.g. **`Raw output`**, default closed; `<pre className="max-h-40 overflow-auto font-mono text-xs ...">`.

**Optional duplicate parse time** in inspector header row: only if compact: `text-xs font-mono tabular-nums` showing same `Parse time: {n.n}s` string as main column when `lastParseRunWallMs` known; append same **`Includes full-page vision fallback.`** muted line when applicable.

**Empty states (UI-SPEC):**
- No OCR run (`!run`): **hide** chunk debug section entirely (or single hidden branch — prefer hidden).
- OCR exists, **0 chunks** this run: expanded table shows **`No layout chunks built from this OCR run.`**
- Accurate / vision-only (no chunk AI rows): show **`No chunk AI calls in this run (Accurate / vision-only).`**

**source/page.tsx:** Lift last parse debug from `ParseRunResult` via `onEmbeddedParseFinished` / manual parse handler: pass into `<OcrInspector ... />`. Bump `ocrInspectorKey` when parse completes if inspector must reload OCR — only if needed.

**Typography:** For **lines you add or replace** in `OcrInspector` for this feature, use only `text-xs`, `text-sm`, `text-base`, `text-2xl` and weights **400** / **600** (`font-semibold`, never `font-bold`). Do not add new arbitrary `text-[10px]` in new code.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Chunk parse debug" src/components/ai/OcrInspector.tsx` exists.
    - `rg -n "chunkParseDebug|lastParseRunWallMs" src/app/\\(app\\)/sets/\\[id\\]/source/page.tsx` wires props.
    - `rg -n "No chunk AI calls in this run \\(Accurate / vision-only\\)" src/components/ai/OcrInspector.tsx` exists.
    - `rg -n "<details" src/components/ai/OcrInspector.tsx` includes chunk debug wrapper with **no** default `open` attribute on that `<details>`.
    - `rg -n "AI time" src/components/ai/OcrInspector.tsx` matches column header.
  </acceptance_criteria>
  <done>D-26/D-29 satisfied; source page feeds inspector; collapsible default closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Debug UI | Raw model text and chunk text are sensitive — session-only, not persisted |

## STRIDE

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-1 | I | `chunkParseDebug.rawByChunkId` | mitigate | Never write to IDB; clear on navigation if needed |
| T-07-02-2 | I | Verbose `pipelineLog` | mitigate | Gate with `isPipelineVerbose()`; avoid logging full prompts |
</threat_model>

<verification>
- `npm run lint`
- `npm run build`
- Manual: Fast parse with OCR on → chunk table shows rows with non-zero **AI time** when chunks ran; Accurate → empty chunk message + optional run total only
</verification>

<success_criteria>
- D-27–D-29, D-28, D-26, D-15, D-25 reflected in code paths described above.
- **AI-05** satisfied via existing progress + new parse time line + chunk debug visibility.
- **AI-03 / AI-04** unchanged behavior, still enforced via existing parse stack.
</success_criteria>

<output>
After completion, create `.planning/phases/07-layout-aware-chunk-based-parsing-token-optimized/07-02-SUMMARY.md` per execute-plan workflow.
</output>
