---
phase: 21-vision-first-mvp-pipeline-10-page-batch-vision-overlap-expli
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - src/lib/ai/runVisionBatchSequential.ts
  - src/lib/ai/sameOriginForward.ts
  - src/components/ai/AiParseSection.tsx
  - src/lib/db/studySetDb.ts
  - src/app/(app)/sets/[id]/source/page.tsx
  - src/app/(app)/sets/[id]/flashcards/review/page.tsx
  - docs/WORKFLOW-vision-parse-detailed.md
  - docs/MIGRATION-vision-mvp-draft.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "MVP product parse path invokes `runVisionBatchSequential` (or equivalent) instead of `runVisionSequential` pair loop for OpenAI-compatible vision."
    - "`parseOutputMode` is derived from `StudySetMeta.contentKind` (+ explicit prop fallback) and passed through parse → persist → review list; flashcard path does not persist MCQ-only schema as the sole representation."
    - "Each completed parse emits `VisionParseBenchmark` via `formatVisionBenchmarkReport` + `visionPipelineEvent(benchmark_ready)`."
    - "`onItemsExtracted` updates draft UI before final dedupe persist (per PRD progressive preview)."
  artifacts:
    - path: src/lib/ai/runVisionBatchSequential.ts
      provides: "Batch loop, cache, retry, dedupe, callbacks"
    - path: docs/MIGRATION-vision-mvp-draft.md
      provides: "User-facing migration for old Question-only drafts"
  key_links:
    - from: AiParseSection
      to: runVisionBatchSequential
      via: "parseOutputMode + forward settings"
      pattern: "explicit mode, no inference"
---

<objective>
**Wave 2 — Runner + product integration:** Implement **`runVisionBatchSequential`** (HTTP multimodal, ≤10 images, batch retries per PRD §13), **mode-aware dedupe**, **incremental `onItemsExtracted`**, wire **`AiParseSection`** + **`/sets/[id]/source`** so MVP **không** chạy OCR prefetch on product, **thay** `runVisionSequential` bằng batch vision; **sửa bug** flashcard→quiz bằng persistence + parser branch; cập nhật **flashcard review** đọc đúng shape; **docs** workflow + migration note.

**Out of scope for 21-02:** multi-model ensemble runtime (chỉ stub `VisionModelConfig[]` + flag read nếu đã có từ 21-01); server-side parse queue.
</objective>

<context>
@.planning/phases/21-vision-first-mvp-pipeline-10-page-batch-vision-overlap-expli/21-CONTEXT.md
@.planning/phases/21-vision-first-mvp-pipeline-10-page-batch-vision-overlap-expli/21-01-PLAN.md
@.planning/phases/21-vision-first-mvp-pipeline-10-page-batch-vision-overlap-expli/21-UI-SPEC.md
@.planning/phases/21-vision-first-mvp-pipeline-10-page-batch-vision-overlap-expli/21-VALIDATION.md
@docs/ARCHITECTURE-domain-boundaries.md
</context>

<threat_model>
| Threat | Severity | Mitigation | Blockers |
|--------|----------|------------|----------|
| Request body too large (10 JPEGs) | High | Keep render limits; optional downscale for batch only; surface user-visible error before send | None |
| Incremental UI double-counts overlap | Medium | Dedupe set before append OR merge pass after each batch using normalized keys from PRD §12 | None |
| IDB migration breaks old sets | Medium | `getDraftQuestions` fallback; migration doc; version gate | 21-01 cache store bump |
| attachPageImages + batch | Medium | Define: attach mode = still **per-page** media after batch extract OR restrict attach when batching — **document decision** in SUMMARY | None |
| Regression on text-only Fast path | Medium | Scope: MVP **product** path vision-first; developer can keep legacy until removed — grep `surface` / `contentKind` | None |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: `runVisionBatchSequential` core</name>
  <files>src/lib/ai/runVisionBatchSequential.ts</files>
  <read_first>src/lib/ai/parseVisionPage.ts,src/lib/ai/sameOriginForward.ts,src/lib/ai/visionBatching.ts,src/lib/ai/visionPrompts.ts,src/lib/ai/visionParseCache.ts,src/lib/ai/visionBenchmark.ts,.planning/phases/21-vision-first-mvp-pipeline-10-page-batch-vision-overlap-expli/21-01-PLAN.md</read_first>
  <action>
Implement runner API roughly:

```ts
export type VisionBatchSequentialOptions = {
  pages: PageImageResult[];
  mode: ParseOutputMode;
  signal: AbortSignal;
  forwardProvider: "openai" | "custom";
  apiKey: string;
  apiUrl?: string;
  model?: string;
  onItemsExtracted?: (items: VisionParseItem[], meta: { batchIndex: number; startPage: number; endPage: number; cacheHit: boolean }) => void;
  studySetId?: string; // attach images if product requires — see threat model
};
```

Loop **`buildVisionBatches`**: for each batch, `hashVisionBatch` → cache get; miss → build messages with **`buildVisionSystemPrompt(mode)`** + user prompt with page range; POST like `postVisionCompletion` but **N image_url parts** (max 10); parse with **mode-specific** parser; compute confidence; call **`onItemsExtracted`**; update benchmark accumulator; cache set.

**Retry:** max **2** retries per batch on transient errors (reuse `pipelineStageRetry` pattern or local loop); on JSON/400 follow parseVisionPage behavior (retry without `json_object`).

**Dedupe:** after all batches, **`dedupeVisionItems(items, mode)`** in same file or `visionDedupe.ts` — normalized stems / fronts.

Return `{ items: VisionParseItem[]; benchmark: VisionParseBenchmark; failedBatches: number }` (shape final TBD, document).

**Ensemble stub:** read optional `process.env.NEXT_PUBLIC_D2Q_VISION_ENSEMBLE` — if not set, single model only; if set, TODO comment for follow-up — không implement dual call trong 21-02 unless timeboxed subtask cleared in SUMMARY.
  </action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `runVisionBatchSequential` exported and imported only from `AiParseSection` (or one helper) — grep entrypoint.
    - No call to `parseVisionPagePair` from MVP product code path after change (allow dev/legacy file to remain).
  </acceptance_criteria>
  <done>Batch runner lands.</done>
</task>

<task type="auto">
  <name>Task 2: Mode-aware draft persistence</name>
  <files>src/lib/db/studySetDb.ts</files>
  <read_first>src/lib/db/studySetDb.ts,src/types/question.ts,src/types/visionParse.ts</read_first>
  <action>
Choose **one** strategy and implement (document in `MIGRATION-vision-mvp-draft.md`):

**A)** Dual stores: `putDraftFlashcardItems` / `getDraftFlashcardItems` vs quiz `putDraftQuestions` — flashcard route reads flashcard store.

**B)** Single store with **`kind` field** on serialized rows + migration reader that maps flashcard → UI model.

Ensure **`contentKind === "flashcards"`** never writes only MCQ `Question[]` without front/back fields populated from flashcard parser — adjust `Question` type **only if** minimal extension (e.g. optional `flashcardFront`/`flashcardBack`) is smaller than parallel store.

Touch **`getDraftQuestions`/`putDraftQuestions`** call sites: `source/page`, `flashcards/review`, etc.
  </action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Creating flashcard set + parse produces **persisted** data readable by flashcard review **without** passing through quiz-only `questionsFromAssistantContent` for the flashcard body.
  </acceptance_criteria>
  <done>Persistence matches mode.</done>
</task>

<task type="auto">
  <name>Task 3: Wire `AiParseSection` + source page</name>
  <files>src/components/ai/AiParseSection.tsx,src/app/(app)/sets/[id]/source/page.tsx</files>
  <read_first>src/components/ai/AiParseSection.tsx,.planning/phases/21-vision-first-mvp-pipeline-10-page-batch-vision-overlap-expli/21-UI-SPEC.md</read_first>
  <action>
- Pass **`parseOutputMode`** into parse section from **`meta.contentKind`** (`quiz` | `flashcards` — map to `ParseOutputMode`; treat legacy `undefined` as **`quiz`** for MVP vision path unless product rule says otherwise).

- Replace inner vision execution for **product + forward-compatible** path to **`runVisionBatchSequential`** with **`onItemsExtracted`** updating React state draft list incrementally.

- Ensure **OCR prefetch** stays **disabled** for `surface === "product"` (already in codebase — verify still true after refactor).

- Confidence badges per **21-UI-SPEC** on draft preview rows.

- On parse end: log **`formatVisionBenchmarkReport`** via `visionPipelineEvent` + optional debug panel per UI-SPEC.
  </action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "runVisionSequential" src/components/ai/AiParseSection.tsx` — either **0** for vision-primary code path or commented with replacement pointer (prefer removal from active branch).
  </acceptance_criteria>
  <done>UI uses batch vision MVP.</done>
</task>

<task type="auto">
  <name>Task 4: Flashcard review route + migration doc</name>
  <files>src/app/(app)/sets/[id]/flashcards/review/page.tsx,docs/MIGRATION-vision-mvp-draft.md</files>
  <read_first>src/app/(app)/sets/[id]/flashcards/review/page.tsx,src/types/visionParse.ts</read_first>
  <action>
Update review page to consume **flashcard-shaped** draft (front/back + confidence) from new persistence. Add **`docs/MIGRATION-vision-mvp-draft.md`**: old `Question[]` only users, re-parse suggestion, `contentKind` interaction.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Flashcard review shows confidence tier or numeric per **21-UI-SPEC** (minimal).
  </acceptance_criteria>
  <done>Review aligns with new model.</done>
</task>

<task type="auto">
  <name>Task 5: Update workflow doc</name>
  <files>docs/WORKFLOW-vision-parse-detailed.md</files>
  <read_first>docs/WORKFLOW-vision-parse-detailed.md,src/lib/ai/runVisionBatchSequential.ts</read_first>
  <action>
Replace pair-centric sections with **batch 10 + overlap 2**, document `runVisionBatchSequential`, cache, benchmark, `parseOutputMode`, incremental callback. Deprecate old diagram or add “Legacy” subsection.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Doc mentions `buildVisionBatches` and `runVisionBatchSequential` by file path.
  </acceptance_criteria>
  <done>Docs match implementation.</done>
</task>

</tasks>
