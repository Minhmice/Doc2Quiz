---
phase: 21-vision-first-mvp-pipeline-10-page-batch-vision-overlap-expli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/visionParse.ts
  - src/lib/ai/visionBatching.ts
  - src/lib/ai/visionPrompts.ts
  - src/lib/ai/parseVisionQuizResponse.ts
  - src/lib/ai/parseVisionFlashcardResponse.ts
  - src/lib/ai/validateVisionQuizItems.ts
  - src/lib/ai/validateVisionFlashcardItems.ts
  - src/lib/ai/visionConfidence.ts
  - src/lib/ai/visionParseCache.ts
  - src/lib/ai/visionBenchmark.ts
  - src/lib/logging/pipelineLogger.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "`buildVisionBatches` implements batchSize=10, overlap=2 with no skipped page indexes and deterministic windows."
    - "`ParseOutputMode` and separate quiz vs flashcard parse/validate paths exist; flashcard does not reuse `questionsFromAssistantContent` for its primary schema."
    - "`VisionParseBenchmark` type + `formatVisionBenchmarkReport` (or equivalent) can render the PRD sample layout from in-memory stats."
    - "Structured vision pipeline events can be logged without ad-hoc string-only `console.log` for core stages (wrapper or extended `pipelineLogger`)."
  artifacts:
    - path: src/lib/ai/visionBatching.ts
      provides: "PageBatch[] from PageImageResult[]"
    - path: src/lib/ai/visionPrompts.ts
      provides: "buildVisionSystemPrompt / buildVisionUserPrompt(mode-aware, compressed)"
  key_links:
    - from: src/lib/ai/parseChunk.ts
      to: parseVisionQuizResponse.ts
      via: "quiz JSON still compatible where needed; flashcard split out"
      pattern: "no cross-parser reuse for flashcard body"
---

<objective>
**Wave 1 — Pure lib + types + logging/cache/benchmark skeleton:** Deliver **batching utility**, **mode-aware compressed prompts**, **separate quiz vs flashcard JSON parsers + validators**, **confidence heuristics**, **batch fingerprint cache API** (store/lookup; IDB wiring can be minimal behind async interface), **`VisionParseBenchmark` accumulator + text formatter**, and **structured pipeline log helper** for vision-batch stages. **Không** wire `AiParseSection` trong wave này (wave 2).

**Out of scope for 21-01:** replacing `runVisionSequential`, UI incremental list, ensemble runtime, full IDB migration of drafts.
</objective>

<context>
@.planning/phases/21-vision-first-mvp-pipeline-10-page-batch-vision-overlap-expli/21-CONTEXT.md
@.planning/phases/21-vision-first-mvp-pipeline-10-page-batch-vision-overlap-expli/21-RESEARCH.md
@docs/WORKFLOW-vision-parse-detailed.md
</context>

<threat_model>
| Threat | Severity | Mitigation | Blockers |
|--------|----------|------------|----------|
| Overlap batching off-by-one / skipped pages | High | Golden examples in code comments + acceptance table (1,11,12,20,21 pages) | None |
| Flashcard still parsed as MCQ shape | High | Separate modules + Zod/manual validate; grep ban `questionsFromAssistantContent` inside flashcard parser | None |
| Cache key collision quiz vs flashcard | Medium | Include `mode` in hash input | None |
| pipelineLog signature break callers | Medium | Add overload or new `visionPipelineEvent` without removing existing `pipelineLog(domain,…)` | None |
| SHA/async perf on 10 images | Low | Sample hash: concat lengths + first/last bytes OR full binary hash MVP — document cost | None |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Types — `ParseOutputMode`, batches, items, benchmark</name>
  <files>src/types/visionParse.ts</files>
  <read_first>src/types/question.ts,.planning/phases/21-vision-first-mvp-pipeline-10-page-batch-vision-overlap-expli/21-CONTEXT.md</read_first>
  <action>
Add **`src/types/visionParse.ts`** exporting at minimum:

- `export type ParseOutputMode = "quiz" | "flashcard"`
- `export type PageBatch = { batchIndex: number; startPage: number; endPage: number; pageIndexes: readonly number[]; }` (pages payload can stay `PageImageResult[]` in batching module to avoid circular imports, or duplicate minimal `{ pageIndex }` — choose minimal coupling).

- Quiz / flashcard **discriminated** item types per PRD (`kind: "quiz"` | `"flashcard"`), each with `confidence: number`, optional `sourcePages?: number[]`.

- `export type VisionParseItem = QuizVisionItem | FlashcardVisionItem` (names align repo style).

- `VisionParseBenchmark` interface matching `21-CONTEXT.md` §benchmark (per-batch array, confidenceSummary, cache hits, naive baseline ints, `requestReductionRatio`).

Re-export or import `PageImageResult` type from `renderPagesToImages` only in `visionBatching.ts` if `PageBatch` holds `pages: PageImageResult[]`.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - File `src/types/visionParse.ts` exists and exports `ParseOutputMode`, `VisionParseBenchmark`.
    - No import from `visionParse.ts` into `app/` in this wave (optional).
  </acceptance_criteria>
  <done>Shared vision MVP types land.</done>
</task>

<task type="auto">
  <name>Task 2: `buildVisionBatches` utility</name>
  <files>src/lib/ai/visionBatching.ts</files>
  <read_first>src/lib/pdf/renderPagesToImages.ts,.planning/phases/21-vision-first-mvp-pipeline-10-page-batch-vision-overlap-expli/21-CONTEXT.md</read_first>
  <action>
Implement **`buildVisionBatches(pages: PageImageResult[], batchSize = 10, overlap = 2): PageBatch[]`** (return type includes `pages: PageImageResult[]` per batch).

Rules from PRD:

- First batch starts at page index **pages[0].pageIndex** (usually 1); windows are **inclusive** on `pageIndex` axis matching rendered array order.
- Step: next batch starts at `previousEndPage - overlap + 1` equivalent in **array index space** — implement so **20 pages → [1–10],[9–18],[17–20]** when `pageIndex` is 1-based consecutive.
- Last batch may be &lt;10 pages; never skip a page; preserve order.

Document invariants at top of file + 2–3 inline examples.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - For mocked `pages` length 20 with `pageIndex` 1..20, `buildVisionBatches` returns **3** batches with `startPage/endPage` matching PRD example (adjust if using 0-based internally — **document mapping**).
    - For `pages.length === 1`, returns exactly **one** batch.
  </acceptance_criteria>
  <done>Batching helper ready for runner.</done>
</task>

<task type="auto">
  <name>Task 3: Compressed mode-aware prompts</name>
  <files>src/lib/ai/visionPrompts.ts</files>
  <read_first>src/lib/ai/prompts/mcq-extraction.prompts.json,.planning/phases/21-vision-first-mvp-pipeline-10-page-batch-vision-overlap-expli/21-CONTEXT.md</read_first>
  <action>
Add **`buildVisionSystemPrompt(mode: ParseOutputMode): string`** and **`buildVisionUserPrompt(input: { mode; startPage; endPage; totalPages }): string`**.

- System: short bullets — JSON only, no markdown fences, no invented content, merge cross-page duplicates in batch output, return empty arrays when nothing valid, **mode-specific** output shape (quiz: `questions` with 4 options; flashcard: schema TBD in parser task — document in prompt).

- User: only batch delta + total pages + schema reminder (minimize token repeat).

Do **not** delete JSON prompts yet; wave 2 switches caller. Optionally re-export legacy string for diff tests.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `buildVisionSystemPrompt("quiz").length` + `buildVisionSystemPrompt("flashcard").length` are each **&lt; 1200 chars** (tunable) OR documented why longer.
    - `buildVisionUserPrompt` includes `startPage`, `endPage`, `totalPages`, `mode`.
  </acceptance_criteria>
  <done>Prompt builders isolated.</done>
</task>

<task type="auto">
  <name>Task 4: Quiz + flashcard parsers & validators (separate files)</name>
  <files>src/lib/ai/parseVisionQuizResponse.ts,src/lib/ai/parseVisionFlashcardResponse.ts,src/lib/ai/validateVisionQuizItems.ts,src/lib/ai/validateVisionFlashcardItems.ts</files>
  <read_first>src/lib/ai/parseChunk.ts,.planning/phases/21-vision-first-mvp-pipeline-10-page-batch-vision-overlap-expli/21-CONTEXT.md</read_first>
  <action>
- **`parseVisionQuizResponse(raw: string): QuizVisionItem[]`:** reuse JSON extraction patterns from `parseJsonFromModelText` where sensible **or** import a shared `stripFenceAndParse` helper extracted minimally from `parseChunk.ts` (avoid duplication explosion). Output items include `kind: "quiz"` + `confidence` placeholder **0** (filled in Task 5).

- **`parseVisionFlashcardResponse(raw: string): FlashcardVisionItem[]`:** distinct JSON schema, e.g. `{ "cards": [{ "front", "back" }] }` — **document exact keys** in file header. Never parse via `questionsFromAssistantContent`.

- Validators: **`validateVisionQuizItems`**, **`validateVisionFlashcardItems`** — filter/drop invalid rows, clamp fields, return typed arrays.

Wire **confidence fill** in Task 5 after validators stable.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `parseVisionFlashcardResponse.ts` does **not** import `questionsFromAssistantContent`.
    - Validators export functions used by parsers or immediately after parse in same module — either is fine if documented.
  </acceptance_criteria>
  <done>Mode-specific parse path exists.</done>
</task>

<task type="auto">
  <name>Task 5: Confidence heuristics</name>
  <files>src/lib/ai/visionConfidence.ts</files>
  <read_first>src/types/visionParse.ts,.planning/phases/21-vision-first-mvp-pipeline-10-page-batch-vision-overlap-expli/21-CONTEXT.md</read_first>
  <action>
Implement **`computeQuizConfidence(item)`** and **`computeFlashcardConfidence(item)`** returning **[0,1]** per PRD §7 heuristics (non-empty fields, option lengths, duplicate proximity optional MVP-skip).

Apply inside parsers **or** export for runner to map after validate — single place documented.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Exported functions; empty stem flashcard → near **0**; well-formed mock quiz → **&gt; 0.5**.
  </acceptance_criteria>
  <done>Confidence scoring available.</done>
</task>

<task type="auto">
  <name>Task 6: Batch cache (fingerprint + IDB or async map)</name>
  <files>src/lib/ai/visionParseCache.ts</files>
  <read_first>src/lib/db/studySetDb.ts,.planning/phases/21-vision-first-mvp-pipeline-10-page-batch-vision-overlap-expli/21-CONTEXT.md</read_first>
  <action>
Implement **`hashVisionBatch(pages: PageImageResult[], mode: ParseOutputMode): Promise<string>`** — MVP: **SHA-256** of concatenated normalized payload (e.g. per page `pageIndex` + byte length + hash of data URL stripped prefix) **or** full string hash if performant enough for ≤10 pages; **include `mode` in key**.

API: `getCachedVisionBatchResult(key)`, `setCachedVisionBatchResult(key, payload)` using **IndexedDB** new object store **or** reuse pattern from `ocrDb`/media stores — if new store, bump `DB_VERSION` only if required and document in plan 02 migration.

If IDB in wave 2: wave 1 may use **in-memory Map** behind same interface + TODO for persistence — **decide in implementation** and record in `21-01-SUMMARY.md`.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Same `pages` + different `mode` → **different** hash string.
    - Cache round-trip test manual or dev-only log acceptable for wave 1.
  </acceptance_criteria>
  <done>Cache contract exists.</done>
</task>

<task type="auto">
  <name>Task 7: Benchmark accumulator + report formatter</name>
  <files>src/lib/ai/visionBenchmark.ts</files>
  <read_first>src/types/visionParse.ts,.planning/phases/21-vision-first-mvp-pipeline-10-page-batch-vision-overlap-expli/21-CONTEXT.md</read_first>
  <action>
- Export **`createVisionBenchmarkAccumulator(mode, totalPages, batchSize, overlap)`** returning object with methods: `startParse`, `endParse`, `recordBatch({ batchIndex, startPage, endPage, itemCount, latencyMs, estimatedTokens, cacheHit })`, `finalize(): VisionParseBenchmark`.

- **`naiveBaselineRequests`:** `max(0, totalPages - 1)` for old pair strategy (document in comment).

- **`formatVisionBenchmarkReport(b: VisionParseBenchmark): string`** — human-readable block ~ PRD §11.2.

- Token fields **marked estimated** in type JSDoc unless wired to usage API later.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `formatVisionBenchmarkReport` output includes `Mode`, `PDF pages`, `Total batches`, and per-batch lines.
  </acceptance_criteria>
  <done>Benchmark module ready.</done>
</task>

<task type="auto">
  <name>Task 8: Structured pipeline logging for vision batch</name>
  <files>src/lib/logging/pipelineLogger.ts</files>
  <read_first>src/lib/logging/pipelineLogger.ts,.planning/phases/21-vision-first-mvp-pipeline-10-page-batch-vision-overlap-expli/21-CONTEXT.md</read_first>
  <action>
Add **`visionPipelineEvent(event: VisionPipelineEvent): void`** (or similar) where `VisionPipelineEvent` is a **discriminated union** or interface with required **`stage`** string from PRD list (`parse_start`, `render_pages_start`, … `benchmark_ready`) + optional numeric fields. Implementation should call existing `pipelineLog` with **flattened** context object **or** `console.info` in dev for large payloads — **never** `console.error` for expected batch failures.

Keep backward compatibility for all existing `pipelineLog(domain, stage, level, …)` call sites.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - New export used in a **no-op** sample call inside `visionBenchmark.ts` or commented example — **or** wire one call from `createVisionBenchmarkAccumulator` for `benchmark_ready` only.
    - `rg "visionPipelineEvent" src/lib` returns ≥1 match.
  </acceptance_criteria>
  <done>Structured logging hook exists.</done>
</task>

</tasks>
