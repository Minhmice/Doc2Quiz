---
phase: 28-move-image-preprocessing-into-web-workers
plan: 02
type: execute
wave: 2
depends_on:
  - "28-01"
files_modified:
  - src/lib/pdf/renderPagesToImages.ts
autonomous: true
requirements:
  - PERF-28-01
  - PERF-28-02
must_haves:
  truths:
    - "PDF pages are still rendered on the main thread (no OffscreenCanvas worker render in this phase)."
    - "JPEG resize+encode uses the worker path when available, otherwise falls back automatically to the existing main-thread encode path."
    - "Callers still receive identical `dataUrl` strings and existing AbortSignal cancellation behavior remains correct."
  artifacts:
    - path: "src/lib/pdf/renderPagesToImages.ts"
      provides: "PDF raster loop that now delegates resize+JPEG encode to worker helper with safe fallback"
  key_links:
    - from: "src/lib/pdf/renderPagesToImages.ts"
      to: "src/lib/pdf/imagePreprocess/encodeJpegInWorker.ts"
      via: "conditional call around encode step"
      pattern: "encodeJpeg.*Worker|canUseImagePreprocessWorker"
    - from: "src/lib/pdf/renderPagesToImages.ts"
      to: "canvas.toDataURL\\(\"image/jpeg\""
      via: "fallback path"
      pattern: "toDataURL\\(\"image/jpeg\""
---

<objective>
Integrate the worker-backed JPEG resize+encode into `renderPdfPagesToImages()` while keeping **PDF rendering** on the **main thread**, preserving **`dataUrl`** output and **AbortSignal** semantics, and auto-falling back to the existing main-thread encode path when needed.

Purpose: Reduce main-thread CPU contention during rasterization bursts without changing user-visible controls or downstream APIs.
Output: Updated `renderPagesToImages.ts` that delegates preprocessing to a single sequential worker with safe fallback.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-move-image-preprocessing-into-web-workers/28-CONTEXT.md
@src/lib/pdf/renderPagesToImages.ts
@src/lib/pdf/pdfWorker.ts

## Next.js worker bundling note (keep bundling sane)
- Import the worker helper only from code that is already client-bundled (this module is used from the client parse flow).
- Use `new URL("./imagePreprocess.worker.ts", import.meta.url)` inside the helper (not in `renderPagesToImages.ts`) to avoid accidental server bundling.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire worker encode path with automatic fallback</name>
  <files>src/lib/pdf/renderPagesToImages.ts</files>
  <action>
Modify `renderPdfPagesToImages()` to keep **PDF render on main thread** (per D-02) but swap the **JPEG encode + resize** step (per D-01) to prefer the worker helper created in Plan 28-01:

- Keep existing page loop, canvas render, preview callbacks, and progress surfaces unchanged (per D-07).
- Immediately before encoding to `dataUrl`, attempt worker preprocessing:
  - If the worker helper reports it can run, call it and obtain a `dataUrl` string.
  - Ensure AbortSignal cancellation behaves the same:
    - If `signal.aborted` is true at any point, throw an `AbortError` (or whatever the current function uses) and stop work promptly (per D-05).
    - If the worker helper rejects with `AbortError`, propagate it unchanged.
- Auto-fallback rules (per D-03):
  - If worker APIs aren’t available, skip worker path.
  - If worker construction fails, or a request errors for a page, fall back to the existing main-thread path for that page (or for the remainder of the run, whichever is safer/less noisy), using the current `canvas.toDataURL("image/jpeg", jpegQuality)` behavior.
  - Fallback must be silent in UI; use existing pipeline logging only (if available), no new controls.
- Preserve the downstream contract (per D-04): the returned `dataUrls[]` (or equivalent) must still be `data:image/jpeg;base64,...` strings.

Important: do NOT attempt OffscreenCanvas or render-in-worker in this phase; keep pdf.js render as-is (per D-02, deferred ideas).
  </action>
  <verify>
<automated>npm run lint</automated>
  </verify>
  <done>
`renderPdfPagesToImages()` renders pages on main thread, encodes via worker when possible, auto-falls back to `canvas.toDataURL` when not, and preserves AbortSignal cancellation and dataUrl output contract.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regression verification for build and critical flow</name>
  <files>src/lib/pdf/renderPagesToImages.ts</files>
  <action>
Run a production build and ensure no worker bundling errors occur (e.g. server-side import issues). If build reveals that `renderPagesToImages.ts` can be imported from a server context, adjust by:
- Keeping the worker helper import inside a function scope that only runs client-side, or
- Lazy-loading the helper via dynamic import guarded by `typeof window !== "undefined"`.

Do not change the exported API of `renderPdfPagesToImages()`; changes must remain internal to avoid downstream refactors.
  </action>
  <verify>
<automated>npm run build</automated>
  </verify>
  <done>
Build succeeds; no runtime-only worker bundling errors are introduced; the integration remains an internal implementation detail with automatic fallback.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF bytes → canvas pixels | Untrusted user file drives memory/CPU workload |
| main thread → worker | Pixel payload crosses via `postMessage` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-28-04 | D | `renderPagesToImages.ts` | mitigate | Keep sequential processing (D-06); ensure abort checks stay in loop; fallback path prevents total failure when worker unavailable |
| T-28-05 | R | fallback behavior | accept | No new audit logs required; rely on existing pipeline logging conventions |
</threat_model>

<verification>
- `npm run lint`
- `npm run build`
</verification>

<success_criteria>
- Image preprocessing is offloaded to a single sequential Web Worker when available.
- PDF rendering remains on the main thread.
- Worker unavailability/errors automatically fall back to the current main-thread encode path.
- AbortSignal and `dataUrl` contract are preserved end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/28-move-image-preprocessing-into-web-workers/28-02-SUMMARY.md`
</output>

