---
phase: 17-byok-parse-estimate
plan: 02
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - src/components/ai/AiParseEstimatePanel.tsx
  - src/components/ai/AiParseSection.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Estimate UI uses `estimateParseRun` only — **no** API calls for numbers."
    - "Changing strategy or toggles updates visible text via **`aria-live=\"polite\"`** region (grep-verifiable)."
    - "Parse button behavior unchanged — estimate is advisory copy next to or above actions."
  artifacts:
    - path: src/components/ai/AiParseEstimatePanel.tsx
      provides: Presentational panel for counts + duration range + disclaimer
    - path: src/components/ai/AiParseSection.tsx
      provides: State for `extractedTextCharCount` / policy page count for memo + renders `AiParseEstimatePanel` in full + embedded variants where parse controls exist
  key_links:
    - from: AiParseSection.tsx
      to: src/lib/ai/estimateParseRun.ts
      via: "`import { estimateParseRun } from \"@/lib/ai/estimateParseRun\"`"
---

<objective>
**Wave 2 — Parse UI:** Show the Phase 17 estimate next to parse controls with accessible live updates when strategy or OCR/attach toggles change.

**Output:** `AiParseEstimatePanel.tsx` + `AiParseSection.tsx` wiring.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| PII in UI from extracted text | Low | Only pass **lengths** and `pageCount` into `estimateParseRun`, never raw `extractedText`. |
| SSR/hydration mismatch | Medium | Panel is client-only under `"use client"` parent; estimate `useMemo` runs only when `studySetId` + prefs stable — gate panel with `typeof window !== "undefined"` not required if entire section is client. |

**Blockers:** Wave **17-01** complete.
</threat_model>

## Task 1 — `AiParseEstimatePanel.tsx`

<action>
Create `src/components/ai/AiParseEstimatePanel.tsx` (`"use client"`).

**Props**

- `estimate: import("@/lib/ai/estimateParseRun").ParseRunEstimate | null` — when `null`, render nothing (or one line “Add a PDF to see estimates” only if parent passes `showEmptyHint?: boolean`; default no empty hint to avoid noise).

**Layout**

- Outer wrapper: `<div className="…" aria-live="polite" aria-atomic="true">` — must contain literal **`aria-live="polite"`** in source for grep.
- When `estimate` non-null, show plain text (not color-only):
  - Line 1: `~{estimate.visionApiCalls} vision API step(s)` (or `0` suppressed if 0 — if both vision and chunk zero show “No API steps estimated.”)
  - Line 2 (if `chunkParseApiCallsUpperBound > 0`): `Up to ~{estimate.chunkParseApiCallsUpperBound} chunk parse call(s) (upper bound)`
  - Line 3 (if `visionFallbackApiCallsUpperBound` present and &gt; 0): `If chunk path fails, up to ~{n} vision fallback step(s)` using the field from 17-01
  - Line 4: duration — `About {min}–{max} s (rough)` using min/max seconds from estimate
  - Line 5: tokens — `Vision tokens (upper ~): ~{in} in / ~{out} out` using `estimatedVisionInputTokensUpper` + `estimatedVisionOutputTokensUpper` from estimate (show `0` / hide line only if both 0 and chunk tokens also 0)
  - Line 6 (if chunk token upper &gt; 0): `Chunk parse tokens (upper ~): ~{in} in / ~{out} out` from `estimatedChunkInputTokensUpper` / `estimatedChunkOutputTokensUpper`
  - Final line: `{estimate.disclaimer}` in `text-muted-foreground` text-sm

Use `cn` from `@/lib/utils` only if already used in sibling components; otherwise plain `className` strings.

No `fetch`.
</action>

<read_first>
- `src/components/ai/AiParsePreferenceToggles.tsx` (Tailwind density)
- `src/lib/ai/estimateParseRun.ts` (after 17-01)
</read_first>

<acceptance_criteria>
- `src/components/ai/AiParseEstimatePanel.tsx` exists.
- `rg "aria-live=\"polite\"" src/components/ai/AiParseEstimatePanel.tsx` returns ≥1 match.
- `rg "ParseRunEstimate" src/components/ai/AiParseEstimatePanel.tsx` returns ≥1 match.
- `rg "estimatedVisionInputTokensUpper" src/components/ai/AiParseEstimatePanel.tsx` returns ≥1 match.
- `npm run lint` exits 0.
</acceptance_criteria>

## Task 2 — Wire `AiParseSection.tsx`

<action>
In `src/components/ai/AiParseSection.tsx`:

1. Add state `estimateDocChars: number` and `estimatePageCount: number | null` — update inside the **existing** `useEffect` that already calls `getDocument` / `getStudySetMeta` for `documentHint` (same async block): set `estimateDocChars` from `doc?.extractedText?.length ?? 0` and `estimatePageCount` from `pageCountForPolicy` after resolution.

2. `const parseEstimate = useMemo(() => estimateParseRun({ pageCount: estimatePageCount ?? pageCount, extractedTextCharCount: estimateDocChars, parseStrategy, enableOcr, attachPageImage }), [estimatePageCount, pageCount, estimateDocChars, parseStrategy, enableOcr, attachPageImage]);` — if `pageCount` and `estimatePageCount` both null, still call with `pageCount: null` (estimator handles per 17-01).

3. Import and render `<AiParseEstimatePanel estimate={parseEstimate} />` **above** `<AiParseActions>` in the **full** variant block, and in the **embedded** block (same relative order: estimate → actions). Do not render when `!hasKey` (no point) OR when `unifiedParseDisabled` due to missing file — parent may still show; requirement: show when `hasKey && activePdfFile` for full variant; embedded when `hasKey && activePdfFile` same as sibling parse controls.

4. When `studySetId` empty, `estimateDocChars` stays 0 and `estimatePageCount` null — panel may show placeholder; optional `showEmptyHint` — skip for minimalism.

Do not change `AiParseActions.tsx` API unless required; prefer sibling panel.
</action>

<read_first>
- `src/components/ai/AiParseSection.tsx` (effects for `documentHint`, JSX near `AiParseActions`)
- `src/lib/ai/estimateParseRun.ts`
</read_first>

<acceptance_criteria>
- `rg "estimateParseRun" src/components/ai/AiParseSection.tsx` returns ≥1 match.
- `rg "AiParseEstimatePanel" src/components/ai/AiParseSection.tsx` returns ≥1 match.
- `rg "estimateDocChars" src/components/ai/AiParseSection.tsx` returns ≥1 match.
- `npm run lint` exits 0.
</acceptance_criteria>

## Verification (phase)

- `npm run lint` and `npm run build` exit 0.
- Manual: open parse UI with PDF + key — numbers change when switching Fast/Accurate and toggling OCR.
