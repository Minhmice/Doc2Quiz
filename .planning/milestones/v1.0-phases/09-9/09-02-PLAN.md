---
phase: 09-math-notation-preview
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - src/components/review/QuestionCard.tsx
  - src/components/review/McqOptionsPreview.tsx
  - src/components/review/QuestionEditor.tsx
  - src/components/ai/QuestionPreviewList.tsx
  - src/components/play/PlaySession.tsx
  - src/components/flashcards/FlashcardSession.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Every user-visible stem/option string path identified in `09-CONTEXT.md` `<code_context>` uses **`MathText`** (or thin wrapper) instead of raw `{string}` for MCQ text (review, AI draft preview, practice, flashcards)."
    - "`QuestionEditor` shows **debounced** live math preview (**300–500ms**, D-04) **below** each textarea without stealing focus (D-05, 09-UI-SPEC)."
    - "Practice keyboard behavior from **04-CONTEXT** unchanged: **no** new global `keydown` listeners; option buttons remain buttons."
  artifacts:
    - path: src/components/review/QuestionCard.tsx
      provides: read-mode stem uses MathText
    - path: src/components/play/PlaySession.tsx
      provides: active question stem + option labels use MathText
    - path: src/components/review/QuestionEditor.tsx
      provides: debounced MathText previews
  key_links:
    - from: MathText.tsx
      to: QuestionCard.tsx
      via: "import MathText"
      pattern: "MathText"
---

<objective>
Wire **`MathText`** everywhere MCQ **text** is shown: **review** (`QuestionCard`, `McqOptionsPreview`, `QuestionEditor` previews), **AI draft** (`QuestionPreviewList`), **practice** (`PlaySession` stem, options, `ResultRow` stems), and **flashcards** (`FlashcardSession` front/back text). Depends on **`09-01`** (MathJax + splitter + component).

**Purpose:** Users see rendered math consistently across the study loop; editors get safe debounced preview.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-9/09-CONTEXT.md
@.planning/phases/09-9/09-UI-SPEC.md
@src/components/math/MathText.tsx
@src/components/review/QuestionCard.tsx
@src/components/review/McqOptionsPreview.tsx
@src/components/review/QuestionEditor.tsx
@src/components/ai/QuestionPreviewList.tsx
@src/components/play/PlaySession.tsx
@src/components/flashcards/FlashcardSession.tsx
@.planning/phases/04-practice-engine/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review read surfaces — QuestionCard + McqOptionsPreview</name>
  <files>src/components/review/QuestionCard.tsx,src/components/review/McqOptionsPreview.tsx</files>
  <read_first>src/components/review/QuestionCard.tsx,src/components/review/McqOptionsPreview.tsx,src/components/math/MathText.tsx,.planning/phases/09-9/09-UI-SPEC.md</read_first>
  <action>
- In **`QuestionCard`** read mode, replace the stem `<p className="whitespace-pre-wrap">{question.question}</p>` with `<MathText source={question.question} className="block text-sm leading-snug" />` wrapped in layout that preserves spacing rules from UI-SPEC.
- In **`McqOptionsPreview`**, replace bare `{opt}` with `<MathText source={opt} className="inline min-w-0" />` inside the existing **`flex-1`** span so A–D badges stay aligned.

Use **`next/dynamic`** import for `MathText` with **`{ ssr: false }`** **only if** required to avoid MathJax in RSC tree — prefer direct client import since parents are already `"use client"`.
  </action>
  <verify>
    <automated>npm run lint</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "MathText" src/components/review/QuestionCard.tsx` matches.
    - `rg -n "MathText" src/components/review/McqOptionsPreview.tsx` matches.
    - `QuestionCard.tsx` no longer contains `whitespace-pre-wrap">{question.question}` as a single plain text child pattern (grep for `question.question` — should be prop to MathText).
  </acceptance_criteria>
  <done>Review read paths render math.</done>
</task>

<task type="auto">
  <name>Task 2: QuestionEditor debounced previews</name>
  <files>src/components/review/QuestionEditor.tsx</files>
  <read_first>src/components/review/QuestionEditor.tsx,.planning/phases/09-9/09-CONTEXT.md (D-04)</read_first>
  <action>
For **stem** and **each option** textarea:
- Keep existing **react-hook-form** `Controller` / register wiring unchanged.
- Add **below** each field a **Preview** block (per 09-UI-SPEC) rendering **`<MathText debounceMs={400} source={watchValue} />`** (pick **400** as default within 300–500 band).

Use **`useWatch`** or `watch()` from RHF — ensure debounce does **not** reset focus (do not remount textarea keys).

If preview errors, show **one line** muted error under that preview only.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "MathText" src/components/review/QuestionEditor.tsx` matches.
    - `rg -n "debounceMs" src/components/review/QuestionEditor.tsx` matches with value between **300** and **500** inclusive.
    - `rg -n "Preview" src/components/review/QuestionEditor.tsx` matches (label for preview region).
  </acceptance_criteria>
  <done>Editor previews math safely.</done>
</task>

<task type="auto">
  <name>Task 3: AI draft QuestionPreviewList</name>
  <files>src/components/ai/QuestionPreviewList.tsx</files>
  <read_first>src/components/ai/QuestionPreviewList.tsx,src/components/math/MathText.tsx</read_first>
  <action>
Replace draft stem `<p>{q.question}</p>` with **`MathText`** equivalent (keep **`Badge`** row).

**Accessibility:** `aria-label` on card currently slices `q.question` — keep **plain text** slice for **`aria-label`** (do not inject TeX noise into SR label beyond first 80 chars of raw string).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "MathText" src/components/ai/QuestionPreviewList.tsx` matches.
  </acceptance_criteria>
  <done>AI preview lists render math.</done>
</task>

<task type="auto">
  <name>Task 4: PlaySession + ResultRow</name>
  <files>src/components/play/PlaySession.tsx</files>
  <read_first>src/components/play/PlaySession.tsx,.planning/phases/04-practice-engine/04-CONTEXT.md (D-04–D-07)</read_first>
  <action>
- Active stem: replace `{current.question}` paragraph content with **`MathText`**.
- Options: wrap each option’s visible text (inside the `<button>`) with **`MathText`** — maintain **full-width** button hit target.
- **`ResultRow`**: replace truncated `<p>{stem}</p>` with **`MathText`** inside **`min-w-0`**; if math height breaks **`truncate`**, switch to **`line-clamp-2`** on an outer wrapper (document choice in SUMMARY).

**Do not** attach keyboard listeners to `MathText` roots.
  </action>
  <verify>
    <automated>npm run lint</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "MathText" src/components/play/PlaySession.tsx` matches at least **2** times (stem + options paths).
    - `PlaySession.tsx` still contains `onKeyDown` only on session container / existing pattern — `rg -n "addEventListener\\(\"keydown\"" src/components/play/PlaySession.tsx` returns **no** new global listeners (ideally zero matches; if matches exist, must be pre-existing only — executor verifies no NEW `window` listener).
  </acceptance_criteria>
  <done>Practice renders math.</done>
</task>

<task type="auto">
  <name>Task 5: FlashcardSession faces</name>
  <files>src/components/flashcards/FlashcardSession.tsx</files>
  <read_first>src/components/flashcards/FlashcardSession.tsx,src/components/math/MathText.tsx</read_first>
  <action>
Replace **`{current.question}`** front face and **`{backText}`** back face plain paragraphs with **`MathText`** (keep `MediaImage` blocks unchanged).

Preserve **`tabIndex={0}`** session root and existing **`onKeyDown`** — no structural change to keyboard handler.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "MathText" src/components/flashcards/FlashcardSession.tsx` matches at least **2** times.
  </acceptance_criteria>
  <done>Flashcards render math.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Same as 09-01 | TeX via `MathText` |

## STRIDE

| ID | Cat | Component | Severity | Mitigation |
|----|-----|-----------|----------|------------|
| T-09-4 | X | XSS via practice option HTML | high | Still only `MathText` path — no raw HTML from strings |
| T-09-5 | E | Focus theft from preview re-render | medium | Debounced MathText; keys only on session root (04-CONTEXT) |
</threat_model>

<verification>
- [ ] `npm run lint`
- [ ] `npx tsc --noEmit`
- [ ] Manual: start dev, open a set review with `$x^2$` in stem — see rendered math in card + editor preview + play mode
</verification>

<output>
After completion, create `.planning/phases/09-9/09-02-SUMMARY.md`
</output>
