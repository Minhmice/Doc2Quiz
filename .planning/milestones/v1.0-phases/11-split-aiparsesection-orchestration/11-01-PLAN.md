---
phase: 11-split-aiparsesection-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai/parseLocalStorage.ts
  - src/lib/ai/attachPageImagesForQuestions.ts
  - src/components/ai/AiParseSection.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Parse behavior is unchanged: same `localStorage` keys `doc2quiz:parse:attachPageImage`, `doc2quiz:parse:enableOcr`, `doc2quiz:parse:strategy` and same string values (`1`/`0`, `fast|accurate|hybrid`)."
    - "`attachPageImagesForQuestions` still updates `Question` objects in-place the same way (media via `putMediaBlob`, `questionImageId` / `sourceImageMediaId` / `imagePageIndex`)."
    - "`AiParseSection` public exports `ParseRunResult`, `ParseStrategy`, `AiParseSectionHandle`, `AiParseSectionProps`, `countIncompleteMcqs` remain importable from `@/components/ai/AiParseSection` (re-export from new modules if types move)."
  artifacts:
    - path: src/lib/ai/parseLocalStorage.ts
      provides: Shared parse preference keys + readers/writers used by AiParseSection
    - path: src/lib/ai/attachPageImagesForQuestions.ts
      provides: Page-image attachment helper extracted from AiParseSection
    - path: src/components/ai/AiParseSection.tsx
      provides: Imports helpers; reduced line count without logic drift
  key_links:
    - from: AiParseSection.tsx
      to: parseLocalStorage.ts
      via: "import preference readers and `LS_*` constants (or only readers if constants stay private to lib)"
    - from: AiParseSection.tsx
      to: attachPageImagesForQuestions.ts
      via: "import `attachPageImagesForQuestions`"
---

<objective>
**Wave 1 — pure extraction:** Move **side-effect-free and loosely coupled** helpers out of `AiParseSection.tsx` into `src/lib/ai/` so the component file shrinks and Phase 11-02 presenters have stable import boundaries. No UX or parse routing changes.

**Output:** New `parseLocalStorage.ts` and `attachPageImagesForQuestions.ts`; `AiParseSection.tsx` updated imports only (logic copy-paste verbatim, then delete originals).
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Accidental change to where API keys or PDF bytes are logged | Medium | Do not touch `pipelineLog` payloads beyond import path changes; no new logging in extracted files. |
| New dependency pulling browser-only code into server bundle | Low | Keep `"use client"` only on components; new files under `src/lib/ai` must not import React. |

**Blockers:** None expected for file moves; if `attachPageImagesForQuestions` needs `fetch`, it stays client-called only (same as today).
</threat_model>

## Task 1 — Extract `parseLocalStorage` module

<action>
Create `src/lib/ai/parseLocalStorage.ts` with:

- Constants: `LS_ATTACH_PAGE_IMAGE`, `LS_ENABLE_OCR`, `LS_PARSE_STRATEGY` with values **`doc2quiz:parse:attachPageImage`**, **`doc2quiz:parse:enableOcr`**, **`doc2quiz:parse:strategy`** (verbatim).
- Readers (copy logic verbatim): `readAttachPageImagePreference`, `readEnableOcrPreference`, `readParseStrategyPreference`.
- **Pure persistence helpers** (extract the `try { localStorage.setItem(...) } catch {}` bodies from the three `useCallback` setters ~lines 448–473): e.g. `persistAttachPageImageToStorage(next: boolean)` using `next ? "1" : "0"`, `persistEnableOcrToStorage(next: boolean)`, `persistParseStrategyToStorage(next: ParseStrategy)` where `ParseStrategy` is exported as **`"fast" | "accurate" | "hybrid"`** from this file.

In `AiParseSection.tsx`, keep the `useCallback` wrappers but each calls `setState` then the corresponding `persist*ToStorage` from `@/lib/ai/parseLocalStorage`. Remove in-file duplicates of `LS_*`, readers, and the raw `localStorage.setItem` try blocks.

`AiParseSection.tsx` must still `export type { ParseStrategy }` (re-export from `@/lib/ai/parseLocalStorage`) if anything imports `ParseStrategy` from the component path today — grep before changing public API.
</action>

<read_first>
- `src/components/ai/AiParseSection.tsx` (full file — locate `LS_*`, `read*Preference`, setter functions)
- `src/lib/ai/parseChunk.ts` (import style only — do not modify)
</read_first>

<acceptance_criteria>
- `src/lib/ai/parseLocalStorage.ts` exists and contains the literal substring `doc2quiz:parse:strategy`.
- `rg "const LS_ATTACH_PAGE_IMAGE"` on `src/components/ai/AiParseSection.tsx` returns **no matches** (constants moved out).
- `npm run lint` exits 0.
</acceptance_criteria>

## Task 2 — Extract `attachPageImagesForQuestions`

<action>
Create `src/lib/ai/attachPageImagesForQuestions.ts` exporting `async function attachPageImagesForQuestions(studySetId: string, pages: PageImageResult[], questions: Question[]): Promise<number>` — **copy the function body verbatim** from `AiParseSection.tsx` including imports it needs (`PageImageResult` from `@/lib/pdf/renderPagesToImages`, `putMediaBlob` from `@/lib/db/studySetDb`, `Question` from `@/types/question`).

Delete the in-file duplicate from `AiParseSection.tsx` and import from `@/lib/ai/attachPageImagesForQuestions`.
</action>

<read_first>
- `src/components/ai/AiParseSection.tsx` (function `attachPageImagesForQuestions` at ~lines 80–122)
- `src/lib/pdf/renderPagesToImages.ts` (type `PageImageResult`)
</read_first>

<acceptance_criteria>
- `src/lib/ai/attachPageImagesForQuestions.ts` exists and contains `putMediaBlob(`.
- `rg "async function attachPageImagesForQuestions"` on `src/components/ai/AiParseSection.tsx` returns **no matches**.
- `npm run lint` exits 0.
</acceptance_criteria>

## Verification (phase)

- Run `npm run lint` and `npm run build` from repo root; both exit 0.
- Manual smoke (executor note): load a set source page with AI parse; toggling Fast/Accurate/Hybrid still persists after refresh (same keys).
