---
phase: 33-adopt-a-vector-store-matched-to-your-scale
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - src/lib/ai/buildEmbeddingIndex.ts
  - src/lib/ai/sameOriginForward.ts
  - src/components/ai/AiParseSection.tsx
autonomous: true
requirements:
  - "RAG-33-03"
  - "RAG-33-04"

must_haves:
  truths:
    - "Study set text is chunked (reuse `chunkText` or layout chunks — pick one and document) then embedded via /api/ai/embed client helper."
    - "Re-index or clear when embeddingModel/dimensions/indexVersion changes."
  artifacts:
    - path: "src/lib/ai/buildEmbeddingIndex.ts"
      provides: "embedChunksForStudySet, searchSimilarChunks"
---

<objective>
Implement **indexing** and **search** library code: client helper to call **`/api/ai/embed`**, batch chunks with **AbortSignal**, persist **`EmbeddingChunkRecord`** rows, and **`searchSimilarChunks(studySetId, query, k)`** using cosine ranker.

**Chunk unit (locked in code comment):** default to **`chunkText(fullExtractedText)`** slices for Phase 33 MVP unless layout chunks are already available synchronously — document tradeoff in file header.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/lib/ai/chunkText.ts
@src/lib/db/embeddingIndexDb.ts
@src/lib/ai/cosineSimilarity.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client embed forward helper</name>
  <files>src/lib/ai/sameOriginForward.ts or new src/lib/ai/embeddingForward.ts</files>
  <action>
Add **`postEmbeddingRequest`** (or extend `forwardAiPost` pattern) that `fetch("/api/ai/embed", { method: "POST", body: JSON.stringify({...}) })` with same error parsing style as `forwardAiPost`.

Parse response `data[0].embedding` (OpenAI shape) — document in comment.
  </action>
  <verify><automated>npm run lint</automated></verify>
</task>

<task type="auto">
  <name>Task 2: buildEmbeddingIndex.ts</name>
  <files>src/lib/ai/buildEmbeddingIndex.ts</files>
  <action>
- **`buildIndexFromPlainText(studySetId, fullText, opts)`** — split with `chunkText`, embed each chunk (sequential or small concurrency with cap), `contentHash` per chunk (`sha256` from `parseCacheDb` helpers), upsert records.

- **`searchSimilarChunks`** — embed query string once, load all records for `studySetId` (or stream cursor), rank top-`k`, return `{ text, score, id, sourceLabel }[]`.

- **Invalidation:** if stored `embeddingModel` !== current settings, **`clearStudySetIndex(studySetId)`** before rebuild (export function).

- **`pipelineLog`** on index build start/done, chunk count, errors.
  </action>
  <verify><automated>npm run lint</automated></verify>
</task>

<task type="auto">
  <name>Task 3: Wire trigger from AiParseSection (optional hook)</name>
  <files>src/components/ai/AiParseSection.tsx</files>
  <action>
After **successful text extraction** (when `fullText` or equivalent is available for quiz path), call **`buildIndexFromPlainText`** in **background** (`void` promise) with **AbortSignal** tied to parse controller — cancel on abort.

If too invasive, expose **`onIndexedDocument`** callback from a thin hook — minimal diff acceptable; **must** have one code path that populates index for dev testing.

**RAG-33-04:** respect caps inside `embeddingIndexDb` writes.
  </action>
  <verify><automated>npm run lint</automated></verify>
</task>

</tasks>
