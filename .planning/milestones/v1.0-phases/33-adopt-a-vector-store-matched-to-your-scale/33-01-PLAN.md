---
phase: 33-adopt-a-vector-store-matched-to-your-scale
plan: 01
type: execute
wave: 1
depends_on: ["32-03"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - src/app/api/ai/embed/route.ts
  - src/lib/ai/embeddingIndexTypes.ts
  - src/lib/db/embeddingIndexDb.ts
  - src/lib/ai/cosineSimilarity.ts
autonomous: true
requirements:
  - "RAG-33-01"
  - "RAG-33-02"
  - "RAG-33-03"
  - "RAG-33-04"

must_haves:
  truths:
    - "IndexedDB (or namespaced store) holds embedding vectors + metadata with studySetId."
    - "Embedding HTTP calls use a Route Handler that never persists API keys."
    - "Index records include model id, dimensions, indexVersion for invalidation."
  artifacts:
    - path: "src/lib/db/embeddingIndexDb.ts"
      provides: "open, put, query by study set, delete by study set, eviction"
    - path: "src/app/api/ai/embed/route.ts"
      provides: "forward OpenAI-compatible embeddings POST"
---

<objective>
Lock **RAG-33-01..04**: planning traceability, **vector store schema** (browser-only), **same-origin embeddings API**, **identity fields** for invalidation, **caps + eviction** constants, and a **pure cosine similarity** helper for query-vs-stored vectors.

Purpose: `33-02` indexes chunks; `33-03` adds UI — neither re-decides storage or forward contract.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/33-adopt-a-vector-store-matched-to-your-scale/33-CONTEXT.md
@src/app/api/ai/forward/route.ts
@src/lib/db/parseCacheDb.ts
</context>

<threat_model>
- **Key exfiltration:** embed route accepts `apiKey` in body like forward; never log or store.
- **Poisoned index:** chunk text from PDF is untrusted — store as string; no eval.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: REQUIREMENTS + ROADMAP reconcile</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Ensure **RAG-33-01..08** and Phase 33 **Goal** / three **plan bullets** match this handoff (no duplicate sections).
  </action>
  <verify><automated>npm run lint</automated></verify>
</task>

<task type="auto">
  <name>Task 2: POST /api/ai/embed</name>
  <files>src/app/api/ai/embed/route.ts</files>
  <action>
Add **Next.js Route Handler** `POST` that mirrors **`/api/ai/forward`** posture:

- Request body: `{ apiKey, targetUrl?, model?, input: string | string[] }` (align with OpenAI `v1/embeddings` or document custom).
- Server forwards to **`https://api.openai.com/v1/embeddings`** when `targetUrl` empty, else normalize custom base URL like forward route.
- Return JSON **as-is** from upstream on success; map **401/429** to same user-facing messages as chat forward where applicable.
- **Never** persist keys.

**RAG-33-02** evidence: browser calls this route with user key from client storage (same pattern as forward).
  </action>
  <verify><automated>npm run lint</automated></verify>
</task>

<task type="auto">
  <name>Task 3: embeddingIndexDb + types</name>
  <files>src/lib/ai/embeddingIndexTypes.ts, src/lib/db/embeddingIndexDb.ts</files>
  <action>
**Types** (`embeddingIndexTypes.ts`):

- `EmbeddingChunkRecord`: `{ id: string; studySetId: string; text: string; vector: number[]; createdAt: string; contentHash: string; embeddingModel: string; dimensions: number; indexVersion: number; sourceLabel?: string }` (adjust minimally).

**IDB** (`embeddingIndexDb.ts`):

- Separate DB name e.g. `doc2quiz-embedding-index`, versioned upgrade.
- Store **`chunks`** with key `id` (UUID); **index** `byStudySet` on `studySetId`.
- **RAG-33-04:** constants `MAX_VECTORS_PER_STUDY_SET` (e.g. 2000) and optional **byte budget** estimate; on insert, **evict LRU** by `lastAccessedAt` or oldest `createdAt` — document in module comment.
- **Guards:** no throw into UI on IDB failure — log + no-op (like parse cache).

**RAG-33-03:** `indexVersion` bump constant when on-disk schema or embedding contract changes.
  </action>
  <verify><automated>npm run lint</automated></verify>
</task>

<task type="auto">
  <name>Task 4: cosineSimilarity helper</name>
  <files>src/lib/ai/cosineSimilarity.ts</files>
  <action>
Export `cosineSimilarity(a: number[], b: number[]): number` and `rankByQuery(queryVec: number[], records: { vector: number[]; ... }[]): ranked` — pure functions, no network.

Handle dimension mismatch: return **-1** or filter incompatible rows and `pipelineLog` warn.
  </action>
  <verify><automated>npm run lint</automated></verify>
</task>

</tasks>
