---
phase: 32-use-draft-pass-generation-plus-validator-pass-rewrite
plan: 03
type: execute
wave: 2
depends_on: ["32-02"]
files_modified:
  - src/components/ai/AiParseSection.tsx
  - src/lib/ai/runVisionBatchSequential.ts
  - src/lib/ai/mcqDraftValidate.ts
autonomous: true
requirements:
  - "DRAFT-32-04"
  - "DRAFT-32-05"

must_haves:
  truths:
    - "User sees a short toast when the LLM validator pass runs (throttled across chunks)."
    - "Quiz and flashcard text paths share the same validator orchestration where both use chunk extraction; vision-only flashcard batch documents any limitation in code comments if parity is not 1:1."
  artifacts:
    - path: "src/components/ai/AiParseSection.tsx"
      provides: "toast + callback wiring into parse runners"
  key_links:
    - from: "src/components/ai/AiParseSection.tsx"
      to: "src/lib/ai/runSequentialParse.ts"
      via: "onValidatorStage"
      pattern: "runSequentialParse\\("
---

<objective>
Add **user-visible feedback** for the validator pass (**light toast**, Mint tone, **throttled** — D-05) and ensure **Quiz vs Flashcards** parity **where** the same chunk/batch extraction applies (DRAFT-32-04). Run **`npm run lint`** and **`npm run build`** as the phase verification gate.

Vision **batch** flashcard flow may differ structurally from per-chunk text — if a single batch response is not chunked, apply **draft→validate once per batch response** or document why validator is N/A with a code comment (align with REQUIREMENTS exception).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-use-draft-pass-generation-plus-validator-pass-rewrite/32-CONTEXT.md
@src/components/ai/AiParseSection.tsx
@src/lib/ai/runSequentialParse.ts
@src/lib/ai/runVisionBatchSequential.ts
</context>

<threat_model>
- **Toast spam:** Throttle to at most once per N seconds or once per parse session for LLM validator — document choice.
- **SSR:** Toasts only from client components; guard `window` / `useEffect` if needed.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: AiParseSection — toast + callbacks</name>
  <files>src/components/ai/AiParseSection.tsx</files>
  <read_first>
  - Existing toast library (e.g. `sonner`) in package.json
  - `runSequentialParse` and vision batch entry points
  </read_first>
  <action>
1. Thread **`onValidatorStage`** (from `32-02`) into **`runSequentialParse`** and any **`parseChunkOnce`** call sites inside this file.

2. On **`usedLlm: true`**, show a **short toast** (e.g. “Refining questions…” or “Validating extraction…”). Use **throttling** (e.g. `useRef` last toast time, min interval 8–15s) per CONTEXT discretion.

3. Add **`pipelineLog`** line when toast fires (optional, for debug parity).

4. Ensure **keyboard / parse abort** clears any pending toast state if needed (no stuck spinners).
  </action>
  <verify>
    <manual>Upload a PDF, run text parse, confirm toast appears at most per throttle when LLM validator runs.</manual>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Flashcard / vision batch parity</name>
  <files>src/components/ai/AiParseSection.tsx, src/lib/ai/runVisionBatchSequential.ts</files>
  <action>
- If vision batch returns **`FlashcardVisionItem[]`** or **`Question[]`** through a single API response: add **draft→validate** for that JSON payload **or** invoke shared validator helper on the parsed array **before** persisting.

- If architectural mismatch makes per-chunk validator impossible, add a **file-level comment** referencing **DRAFT-32-04** and REQUIREMENTS exception; prefer minimal shared helper from `mcqDraftValidate.ts` for any structured array.

Goal: no silent **quiz-only** quality path when flashcards share the same user expectation.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Verification — lint + build</name>
  <files>package.json</files>
  <action>
Run **`npm run lint`** and **`npm run build`**; fix any issues introduced by Phase 32.

Optional: add **`VALIDATION.md`** snippet under phase folder documenting manual toast check (if repo uses phase VALIDATION pattern).
  </action>
  <verify>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
  </verify>
</task>

</tasks>

<summary_template>
Note: toast copy, throttle constants, flashcard path decision (full parity vs documented exception).
</summary_template>
