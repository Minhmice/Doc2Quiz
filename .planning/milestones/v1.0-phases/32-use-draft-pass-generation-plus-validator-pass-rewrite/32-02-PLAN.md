---
phase: 32-use-draft-pass-generation-plus-validator-pass-rewrite
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - src/lib/ai/parseChunk.ts
  - src/lib/ai/runSequentialParse.ts
  - src/lib/ai/mcqDraftValidate.ts
  - src/lib/db/parseCacheDb.ts
autonomous: true
requirements:
  - "DRAFT-32-01"
  - "DRAFT-32-02"
  - "DRAFT-32-03"
  - "DRAFT-32-06"

must_haves:
  truths:
    - "After each successful draft parse for a text/layout chunk, deterministic validator runs; LLM validator runs when needed; merged list only sees post-validator questions."
    - "runSequentialParse progress callbacks and AbortSignal behavior unchanged from a caller perspective except added stages in pipelineLog."
    - "Validator LLM uses parse cache with validator lane + fingerprint from 32-01."
  artifacts:
    - path: "src/lib/ai/parseChunk.ts"
      provides: "draft→validate sequence inside parseChunkOnce / single MCQ helpers"
    - path: "src/lib/ai/runSequentialParse.ts"
      provides: "per-chunk orchestration unchanged except injected validate step"
  key_links:
    - from: "src/lib/ai/parseChunk.ts"
      to: "src/lib/ai/mcqDraftValidate.ts"
      via: "post-draft validation"
      pattern: "deterministicRepairDraftQuestions|runValidatorLlmPass"
---

<objective>
Wire **draft → validator** into the **text + layout** parse pipeline: `parseChunkOnce`, `parseChunkSingleMcqOnce`, and **`runSequentialParse`** chunk loop. Respect **always validate on success** (DRAFT-32-03), **hybrid** flow (DRAFT-32-02), and **Phase 31 cache** for both passes (DRAFT-32-06).

**Out of scope for this plan:** user-facing toast (`32-03`); flashcard vision batch UX (`32-03`) unless a thin callback hook is needed — prefer **`onValidatorLlmInvoked?: () => void`** optional param threaded from `AiParseSection` in `32-03`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-use-draft-pass-generation-plus-validator-pass-rewrite/32-CONTEXT.md
@src/lib/ai/parseChunk.ts
@src/lib/ai/runSequentialParse.ts
@src/lib/ai/mcqDraftValidate.ts
@src/lib/db/parseCacheDb.ts
@src/lib/ai/sameOriginForward.ts
</context>

<threat_model>
- **Double billing:** Draft cache hit must still run validator (DRAFT-32-03) — do not short-circuit merge on draft hit alone without validator stage.
- **Infinite loops:** Validator LLM retries follow existing `withRetries` / fatal error patterns; cap documented in code comment.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: parseChunkOnce — draft then validate</name>
  <files>src/lib/ai/parseChunk.ts</files>
  <read_first>
  - `parseChunkOnce` full flow including cache get/set for draft
  - `parseChunkSingleMcqOnce` if separate
  </read_first>
  <action>
For **`parseChunkOnce`** (and **`parseChunkSingleMcqOnce`** if it duplicates logic):

1. Keep **draft** API call + **draft cache** behavior as today (lane `text_multi_mcq` / `text_single_mcq`).

2. On **successful draft** (questions array, even if empty): run **`deterministicRepairDraftQuestions`**.

3. If **`needsValidatorLlm`**: call **`runValidatorLlmPass`** (fully implemented here or in `mcqDraftValidate.ts`) with **validator cache** get/set using **32-01** lane + fingerprint.

4. **Return** final `Question[]` + **aggregate `cacheHit`** — define semantics in code comment (e.g. `cacheHit` true only if both draft and validator were hits, or split fields — **pick one** and document; prefer extending `ParseChunkOnceResult` minimally).

5. **`pipelineLog` / debug:** emit structured reasons via **`ValidatorReasonCode`** (console or existing logger if present).

6. Add optional callback **`onValidatorStage?: (info: { usedLlm: boolean; reasons: ValidatorReasonCode[] }) => void`** for `32-03` toast — no UI in this file.

**Edge cases:** draft returns empty questions — still run deterministic pass (cheap); LLM validator likely skipped unless policy says otherwise (document).

**DRAFT-32-03:** Validator deterministic stage always runs on success path; LLM runs when `needsValidatorLlm` after deterministic repair.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
  - [ ] No regression in `FatalParseError` / abort handling
  - [ ] Validator uses distinct cache keys vs draft
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: runSequentialParse — per-chunk integration</name>
  <files>src/lib/ai/runSequentialParse.ts</files>
  <action>
Ensure each chunk iteration calls the updated **`parseChunkOnce`** and merges **post-validator** questions only.

Propagate **`onValidatorStage`** from params if `runSequentialParse` exposes callbacks — thread from `AiParseSection` in `32-03`.

Confirm **progress** events (chunk index, counts) reflect **final** question counts after validation.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Implement runValidatorLlmPass (if still stub)</name>
  <files>src/lib/ai/mcqDraftValidate.ts, src/lib/ai/parseChunk.ts</files>
  <action>
Complete **`runValidatorLlmPass`**: build user message with JSON of draft questions + chunk excerpt (truncate safely), system = `MCQ_VALIDATOR_SYSTEM_PROMPT`, use **`forwardAiPost`** like draft path, parse assistant JSON to `Question[]`, run **`validateQuestionsFromJson`** on output.

Integrate **parse cache** read/write for validator lane per **32-01** design.

Respect **`signal`**, reuse **`onRawAssistantText`** for debug parity.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
</task>

</tasks>

<summary_template>
Document: `ParseChunkOnceResult` shape; when LLM validator runs; example pipelineLog lines.
</summary_template>
