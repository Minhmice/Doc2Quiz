---
phase: 32-use-draft-pass-generation-plus-validator-pass-rewrite
plan: 01
type: execute
wave: 1
depends_on: ["31-03"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - src/lib/ai/parseCacheTypes.ts
  - src/lib/db/parseCacheDb.ts
  - src/lib/ai/prompts/mcq-extraction.prompts.json
  - src/lib/ai/prompts/mcqExtractionPrompts.ts
  - src/lib/ai/mcqDraftValidate.ts
autonomous: true
requirements:
  - "DRAFT-32-01"
  - "DRAFT-32-02"
  - "DRAFT-32-06"

must_haves:
  truths:
    - "Validator system prompt text lives in mcq-extraction.prompts.json with bundle version bump; exported digest/identity helpers mirror draft prompts (CACHE-31 compatible)."
    - "Parse cache lanes or key material distinguish draft vs validator API calls so CACHE-31 keys never collide across passes."
    - "Stable validator reason codes exist as a string union (or const object) used by pipelineLog in 32-02."
  artifacts:
    - path: "src/lib/ai/mcqDraftValidate.ts"
      provides: "deterministic repair entry point; optional LLM validator hook signature; reason codes"
    - path: "src/lib/ai/parseCacheTypes.ts"
      provides: "extended ParseCacheLane or documented validator fingerprint strategy"
  key_links:
    - from: "src/lib/ai/mcqDraftValidate.ts"
      to: "src/lib/ai/prompts/mcqExtractionPrompts.ts"
      via: "validator prompt identity hashing"
      pattern: "hashPromptIdentity|formatPromptKeyComponent"
---

<objective>
Establish **shared contracts** for Phase 32: requirement/traceability alignment, **validator prompt versioning**, **cache-safe key material** for a second API pass, and a **`mcqDraftValidate` module** that implements deterministic repair first and defines the LLM-validator hook (implementation wiring in `32-02-PLAN.md`).

Purpose: `32-02` only orchestrates; it does not re-decide types or cache math.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-use-draft-pass-generation-plus-validator-pass-rewrite/32-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

@src/lib/ai/parseCacheTypes.ts
@src/lib/db/parseCacheDb.ts
@src/lib/ai/prompts/mcq-extraction.prompts.json
@src/lib/ai/prompts/mcqExtractionPrompts.ts
@src/lib/ai/validateQuestions.ts
</context>

<threat_model>
- **Cache poisoning:** Validator must not read/write draft cache keys; extend lanes or include pass kind in `promptIdentity` / fingerprint per `canonicalizeParseCacheKeyParts` rules.
- **Secrets:** Validator calls still go through `POST /api/ai/forward` only; no new env keys.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: ROADMAP + REQUIREMENTS reconcile (Phase 32)</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
Ensure **ROADMAP** Phase 32 lists **Goal**, **DRAFT-32-01..06**, **Depends on Phase 31**, and **three plan bullets** (`32-01`..`32-03`) matching this phase split.

Ensure **REQUIREMENTS.md** has checkboxes + traceability rows for `DRAFT-32-01`..`06` (no duplicates).

If anything drifted during handoff, **reconcile only** — do not remove IDs.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
ROADMAP Phase 32 goal/requirements/plan list match REQUIREMENTS; markdown consistent.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validator prompts + exports (mcqExtractionPrompts)</name>
  <files>src/lib/ai/prompts/mcq-extraction.prompts.json, src/lib/ai/prompts/mcqExtractionPrompts.ts</files>
  <action>
Add a **validator** system prompt section to `mcq-extraction.prompts.json` (e.g. `mcqValidator` or `validatorPass`) with instructions to: preserve source fidelity, fix JSON/schema issues, keep four options and `correctIndex` in range, avoid inventing facts.

Bump **`version`** in JSON.

Export from `mcqExtractionPrompts.ts`:
- `MCQ_VALIDATOR_SYSTEM_PROMPT` (string)
- `PROMPTS_BUNDLE_VERSION` already covers bundle — ensure validator digest participates in **`formatPromptKeyComponent` / `hashPromptIdentity`** the same way as draft prompts (DRAFT-32-06).

Keep prompts **English** (product copy for toast stays in `32-03`).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
JSON validates; TypeScript exports compile; bundle version bumped once.
  </done>
</task>

<task type="auto">
  <name>Task 3: Parse cache — validator pass identity</name>
  <files>src/lib/ai/parseCacheTypes.ts, src/lib/db/parseCacheDb.ts</files>
  <action>
**Option A (preferred):** extend `ParseCacheLane` with e.g. `text_multi_mcq_validator` and `text_single_mcq_validator` mirroring draft lanes.

**Option B:** keep lanes, add explicit **`pass: "draft" | "validator"`** to key parts — only if `parseCacheDb` canonicalization is updated with a migration note.

Pick one approach; document in `parseCacheTypes.ts` header comment. Validator **content fingerprint** should bind to **draft output** (e.g. hash of normalized `Question[]` JSON + chunk fingerprint) so identical drafts hit validator cache — design the exact string in a **single helper** exported next to key builders.

Ensure **no cross-pass collision** with existing Phase 31 entries (DRAFT-32-06).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
Types + canonical key story documented; `parseCacheDb` still opens (version bump store if required).
  </done>
</task>

<task type="auto">
  <name>Task 4: mcqDraftValidate — deterministic layer + hook</name>
  <files>src/lib/ai/mcqDraftValidate.ts</files>
  <action>
Create **`mcqDraftValidate.ts`**:

1. **`ValidatorReasonCode`** — stable string union for logs, e.g. `schema_repaired`, `options_padded`, `needs_llm`, `llm_ok`, `llm_failed_fallback`.

2. **`deterministicRepairDraftQuestions(questions: Question[]): { questions: Question[]; reasons: ValidatorReasonCode[] }`** — reuse or wrap `validateQuestionsFromJson` patterns from `validateQuestions.ts` / existing normalization; trim empty options; clamp `correctIndex`; drop empty questions if policy says so — **document policy** in file comment.

3. **`needsValidatorLlm(reasons: ValidatorReasonCode[]): boolean`** — true when deterministic repair was insufficient (executor sets threshold: e.g. any question still invalid after repair).

4. Export **`runValidatorLlmPass` placeholder type** `(params)` matching future `parseChunk` needs: `provider`, `apiKey`, `apiUrl`, `model`, `signal`, draft `Question[]`, chunk context string optional, **`onRawAssistantText`**, **`studySetId`**. Implementation can `throw new Error("wired in 32-02")` until `32-02` or implement minimal forward call mirroring `parseChunk` patterns.

**Do not** wire into `parseChunk.ts` in this plan — that is `32-02`.

**AbortSignal:** propagate to any fetch in stub if implemented.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
Module exports compile; reason codes stable; deterministic path returns repaired questions.
  </done>
</task>

</tasks>

<summary_template>
See `@$HOME/.cursor/get-shit-done/templates/summary.md`. Include: prompt version, cache lane decision, exported symbols from `mcqDraftValidate.ts`.
</summary_template>
