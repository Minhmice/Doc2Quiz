---
phase: 01-pdf-ingestion
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/components/upload/UploadBox.tsx
  - src/components/viewer/RawTextViewer.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - PDF-01
  - PDF-03
  - PDF-04
user_setup: []

must_haves:
  truths:
    - "User can pick or drop one PDF; non-PDF and >10MB show inline errors per 01-CONTEXT D-14/D-08"
    - "Successful extract shows scrollable monospace text and page count (D-09–D-11)"
    - "Scanned/empty extraction shows the exact sentence: This PDF may be scanned. Text extraction failed."
  artifacts:
    - "src/components/upload/UploadBox.tsx exists"
    - "src/components/viewer/RawTextViewer.tsx exists"
    - "src/app/page.tsx composes UploadBox + RawTextViewer per 01-UI-SPEC"
  key_links:
    - "page.tsx calls validatePdfFile before extractText"
    - "page.tsx maps validation errors to D-14 strings"
---

<objective>
Build **UploadBox**, **RawTextViewer**, and the **main page** that wires validation, `extractText`, loading guard (D-15), and all copy/states per `01-CONTEXT.md` and `01-UI-SPEC.md`.

Purpose: Delivers PDF-01, PDF-03, PDF-04 and completes the Phase 1 user-facing loop.
Output: Fully composed home screen ready for Phase 2 to consume `text` + `pageCount` state.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-pdf-ingestion/01-CONTEXT.md
@.planning/phases/01-pdf-ingestion/01-UI-SPEC.md
@.planning/REQUIREMENTS.md
@src/lib/pdf/validatePdfFile.ts
@src/lib/pdf/extractText.ts
@src/types/pdf.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RawTextViewer component</name>
  <files>src/components/viewer/RawTextViewer.tsx</files>
  <read_first>.planning/phases/01-pdf-ingestion/01-CONTEXT.md, .planning/phases/01-pdf-ingestion/01-UI-SPEC.md</read_first>
  <action>
Create **`src/components/viewer/RawTextViewer.tsx`** exporting a component with props at minimum:

- `text: string | null` (null = idle placeholder)
- `pageCount: number | null` (show only when `pageCount !== null` and `pageCount > 0`)

Behavior:
- **Idle** (`text === null`): show copy from UI-SPEC idle viewer — must include the phrase **"Extracted text will appear here after you upload a PDF"** (D-12).
- **Has text**: render inside a **scrollable** container (`overflow-y-auto`), **`font-mono`**, **wrap** (`whitespace-pre-wrap` or equivalent), **bounded height** using `min-h-0` + flex parent pattern or `max-h-[calc(100vh-…)]` per D-10.
- **Metadata row**: when showing extracted content, display **"N pages"** using `pageCount` (D-11).

Use Tailwind only. No `dangerouslySetInnerHTML` for PDF text (plain text nodes — XSS safe).
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `src/components/viewer/RawTextViewer.tsx` exports a React component (default or named — document in SUMMARY)
    - File contains the substring `font-mono`
    - File contains the substring `Extracted text will appear here after you upload a PDF`
    - File contains `overflow-y-auto` or equivalent scroll class
  </acceptance_criteria>
  <done>Viewer meets D-09, D-10, D-11, D-12</done>
</task>

<task type="auto">
  <name>Task 2: UploadBox component</name>
  <files>src/components/upload/UploadBox.tsx</files>
  <read_first>src/lib/pdf/validatePdfFile.ts, .planning/phases/01-pdf-ingestion/01-UI-SPEC.md</read_first>
  <action>
Create **`src/components/upload/UploadBox.tsx`** with props shaped for parent-controlled state, e.g.:

- `disabled?: boolean` (true while extracting — D-15)
- `onFileSelected: (file: File) => void` **after** local validation passes
- Optional: `error: string | null` for **inline** display near the zone (D-08)

Implementation:
- Hidden `<input type="file" accept="application/pdf,.pdf" />` plus visible **Choose PDF** button (UI-SPEC CTA).
- **Drag-and-drop** on a bordered drop zone (D-02); `preventDefault` on dragover/drop.
- On file received: run **`validatePdfFile`** from `src/lib/pdf/validatePdfFile.ts`. If invalid, **do not** call `onFileSelected`; parent should set error messages:
  - type → **"Please choose a PDF file."**
  - size → message **must include "10 MB"** (D-14)
- After success, call `onFileSelected(file)`; parent shows **filename + human-readable size** (D-03) — either UploadBox displays them or exposes via callback; **must be visible on screen** after selection per D-03.

Styling: follow **01-UI-SPEC** colors (accent on active drop state).
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `src/components/upload/UploadBox.tsx` contains `accept="application/pdf` or `accept=".pdf"`
    - File references `validatePdfFile` via import from `@/lib/pdf/validatePdfFile` or relative path
    - File implements `onDragOver` and `onDrop` (or equivalent handlers)
  </acceptance_criteria>
  <done>UploadBox satisfies D-02, D-08, D-13–D-15 integration hooks</done>
</task>

<task type="auto">
  <name>Task 3: Compose page and extraction flow</name>
  <files>src/app/page.tsx</files>
  <read_first>src/components/upload/UploadBox.tsx, src/components/viewer/RawTextViewer.tsx, src/lib/pdf/extractText.ts, .planning/phases/01-pdf-ingestion/01-CONTEXT.md</read_first>
  <action>
Replace **`src/app/page.tsx`** with the Phase 1 single-column layout (D-01): title **Doc2Quiz**, short subtitle, **UploadBox**, then **RawTextViewer**.

State machine:
- **idle** — no file / no extract yet
- **loading** — `extractText` in flight; `disabled` on UploadBox; show loading text **"Extracting text…"** or equivalent (UI-SPEC)
- **success** — show `text` + `pageCount` in viewer; UploadBox still visible; **Choose another PDF** replaces initial CTA behavior (new file replaces — D-04)
- **error** — inline + optional summary: validation errors (D-14), extraction errors, **empty text** after extract

**PDF-04 / D-06:** When `extractText` returns **`text` that is empty or whitespace-only** with `pageCount >= 1`, OR extraction throws/fails, show a user-visible error whose body includes **exactly** this sentence (allow surrounding punctuation/line breaks):  
`This PDF may be scanned. Text extraction failed.`

**D-07:** Provide a clear control to pick another file after error (same file input flow).

Use `"use client"` on `page.tsx` OR split `PdfWorkspace.tsx` client — **one** pattern only, consistent imports (`@/components/...` if tsconfig paths set).

Configure **`@/`** alias in `tsconfig.json` if not already (`paths`: `{ "@/*": ["./src/*"] }`).
  </action>
  <verify>npm run build && npm run lint</verify>
  <acceptance_criteria>
    - `src/app/page.tsx` imports `UploadBox` and `RawTextViewer`
    - `src/app/page.tsx` calls `extractText(` (import from `@/lib/pdf/extractText` or relative)
    - `grep -n "This PDF may be scanned" src/app/page.tsx` OR the string appears in a child component file imported only from this flow (document path in SUMMARY if split)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>End-to-end Phase 1 UX matches CONTEXT + UI-SPEC + PDF-01/03/04</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Extracted text → DOM | Rendered as React children — must not use raw HTML injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I | XSS via PDF text | mitigate | No `dangerouslySetInnerHTML`; text via JSX `{text}` |
| T-03-02 | D | File upload UI | accept | Local-only; no server upload in Phase 1 |
</threat_model>

<verification>
- [ ] `npm run build` and `npm run lint` succeed
- [ ] Manual smoke: text PDF shows text; random bytes renamed `.pdf` or scanned-empty yields PDF-04 sentence
</verification>

<success_criteria>
- All requirements PDF-01, PDF-03, PDF-04 satisfied in UI
- Phase 2 can read `text` and `pageCount` from the same page state shape (document in SUMMARY)
</success_criteria>

<output>
After completion, create `.planning/phases/01-pdf-ingestion/01-03-SUMMARY.md`
</output>
