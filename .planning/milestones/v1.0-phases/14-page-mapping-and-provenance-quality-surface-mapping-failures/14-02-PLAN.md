---
phase: 14-page-mapping-and-provenance-quality-surface-mapping-failures
plan: 02
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified:
  - src/components/review/QuestionCard.tsx
  - src/components/review/ReviewSection.tsx
  - src/components/ai/QuestionPreviewList.tsx
  - .planning/codebase/WORKFLOW-OCR-AI-QUIZ.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Review `QuestionCard` shows a mapping quality Badge per 14-UI-SPEC when not editing."
    - "`ReviewSection` shows an Alert when `countUncertainMappings(questions) > 0`."
    - "WORKFLOW doc states mapping errors/uncertainty are user-visible; no silent swallow contract."
  artifacts:
    - path: src/components/review/QuestionCard.tsx
      provides: "Per-question mapping chip"
    - path: .planning/codebase/WORKFLOW-OCR-AI-QUIZ.md
      provides: "Doc alignment D-10"
  key_links:
    - from: src/components/review/QuestionCard.tsx
      to: src/lib/ai/mappingQuality.ts
      via: "import predicates"
      pattern: "isUncertainPageMapping / isUnresolvedMapping"
---

<objective>
Wire **14-CONTEXT D-06, D-07, D-10** and **14-UI-SPEC**: review surfaces show mapping quality; workflow doc matches implementation contract. Depends on **14-01** (`mappingQuality.ts`).

**Output:** Review UI + doc only — no changes to `mapQuestionsToPages` overlap math in this plan.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-page-mapping-and-provenance-quality-surface-mapping-failures/14-CONTEXT.md
@.planning/phases/14-page-mapping-and-provenance-quality-surface-mapping-failures/14-UI-SPEC.md
@src/components/review/QuestionCard.tsx
@src/components/review/ReviewSection.tsx
@.planning/codebase/WORKFLOW-OCR-AI-QUIZ.md
@src/lib/ai/mappingQuality.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: QuestionCard mapping Badge + tooltip</name>
  <files>src/components/review/QuestionCard.tsx</files>
  <read_first>src/components/review/QuestionCard.tsx,src/lib/ai/mappingQuality.ts,.planning/phases/14-page-mapping-and-provenance-quality-surface-mapping-failures/14-UI-SPEC.md</read_first>
  <action>
When `!isEditing`, next to `Q{n}` title row, render a **`Badge`** (from `@/components/ui/badge`) using **14-UI-SPEC** variants:

- If `isUnresolvedMapping(question)` → **Unresolved** badge (destructive style per spec).
- Else if `isUncertainPageMapping(question)` → **Uncertain** (amber outline per spec).
- Else if `question.mappingMethod` is defined → **Mapped** (`secondary`).
- Else (no mapping metadata yet) → omit badge **or** show small “—” muted text — pick one and document in code comment (prefer omit to reduce noise for text-only parses).

**Tooltip:** set `title={...}` on badge wrapper with truncated `mappingReason` + confidence + `sourcePageIndex` when any exist.

Do **not** change `QuestionEditor` in this task unless required for types.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "mappingQuality|Badge" src/components/review/QuestionCard.tsx` shows imports usage.
    - `rg -n "Unresolved|Uncertain|Mapped" src/components/review/QuestionCard.tsx` shows label strings for the three states.
  </acceptance_criteria>
  <done>Review cards show mapping tier.</done>
</task>

<task type="auto">
  <name>Task 2: ReviewSection aggregate Alert</name>
  <files>src/components/review/ReviewSection.tsx</files>
  <read_first>src/components/review/ReviewSection.tsx,src/lib/ai/mappingQuality.ts,.planning/phases/14-page-mapping-and-provenance-quality-surface-mapping-failures/14-UI-SPEC.md</read_first>
  <action>
After questions load (`reload` / state), compute `n = countUncertainMappings(questions)`.

If `n > 0`, render one **`Alert`** above `ReviewList` per 14-UI-SPEC (include count + short CTA).

If `n === 0`, render nothing extra.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "countUncertainMappings" src/components/review/ReviewSection.tsx` matches.
    - `rg -n "Alert" src/components/review/ReviewSection.tsx` shows conditional block.
  </acceptance_criteria>
  <done>Review page surfaces aggregate uncertainty.</done>
</task>

<task type="auto">
  <name>Task 3: QuestionPreviewList — mapping quality Badge (parse preview D-06)</name>
  <files>src/components/ai/QuestionPreviewList.tsx</files>
  <read_first>src/components/ai/QuestionPreviewList.tsx,src/lib/ai/mappingQuality.ts,.planning/phases/14-page-mapping-and-provenance-quality-surface-mapping-failures/14-UI-SPEC.md</read_first>
  <action>
In the row that currently shows **`Draft {i+1}`** (`Badge`), add a second **mapping quality** `Badge` using the **same tier rules** as `QuestionCard` Task 1 (import `isUnresolvedMapping`, `isUncertainPageMapping` from `mappingQuality.ts`; reuse variant classes per 14-UI-SPEC).

Keep layout: `flex flex-wrap items-center gap-2` — mapping badge immediately after Draft badge.

**Tooltip:** `title` on wrapper or badge with truncated `mappingReason` + numeric fields when present.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "isUncertainPageMapping|isUnresolvedMapping" src/components/ai/QuestionPreviewList.tsx` matches.
    - `rg -n "Draft" src/components/ai/QuestionPreviewList.tsx` still shows draft index badge.
  </acceptance_criteria>
  <done>Parse preview lists mapping tier per D-06.</done>
</task>

<task type="auto">
  <name>Task 4: WORKFLOW-OCR-AI-QUIZ mapping section (D-10)</name>
  <files>.planning/codebase/WORKFLOW-OCR-AI-QUIZ.md</files>
  <read_first>.planning/codebase/WORKFLOW-OCR-AI-QUIZ.md,.planning/phases/14-page-mapping-and-provenance-quality-surface-mapping-failures/14-CONTEXT.md</read_first>
  <action>
In the section that describes **question ↔ page mapping** (OCR overlap / `applyQuestionPageMapping`):

- State explicitly that mapping remains **heuristic**, but **exceptions are logged** (`MAPPING` domain) and **uncertain** rows are **shown in UI** after parse and on review.
- Remove or rewrite any phrasing that implies errors are **always** swallowed without user visibility (align with post–Phase-14 behavior).

Keep Vietnamese tone of doc if file is bilingual; add a short English clause if needed for implementers.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "MAPPING|uncertain|applyQuestionPageMapping" .planning/codebase/WORKFLOW-OCR-AI-QUIZ.md` shows at least **three** non-overlapping substantive lines in the mapping-related section (executor may widen search if needed).
  </acceptance_criteria>
  <done>Documentation matches contract.</done>
</task>

<task type="auto">
  <name>Task 5: Wave 2 build verification</name>
  <files>src/components/review/QuestionCard.tsx,src/components/review/ReviewSection.tsx,src/components/ai/QuestionPreviewList.tsx</files>
  <read_first>.planning/phases/14-page-mapping-and-provenance-quality-surface-mapping-failures/14-02-PLAN.md</read_first>
  <action>
Run **`npm run build`** from repo root after Tasks 1–4 doc edit. Fix only TypeScript/import issues introduced by this wave.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Production build green for wave 2 UI.</done>
</task>

</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| XSS via `mappingReason` in `title` | Use plain text truncation only; `mappingReason` is app-generated, not HTML — still truncate length. |
| Alert fatigue | Single banner per load; count-based copy only. |
</threat_model>

<verification>
- `npm run lint`
- `npm run build`
</verification>

## PLANNING COMPLETE
