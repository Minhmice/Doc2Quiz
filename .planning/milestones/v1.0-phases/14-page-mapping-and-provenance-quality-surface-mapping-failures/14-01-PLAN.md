---
phase: 14-page-mapping-and-provenance-quality-surface-mapping-failures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/logging/pipelineLogger.ts
  - src/lib/ai/mappingQuality.ts
  - src/components/ai/AiParseSection.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "`PipelineDomain` includes `MAPPING` and `pipelineLog('MAPPING', ...)` compiles for mapping exceptions."
    - "`finalizeVisionParseResult` no longer wraps `applyQuestionPageMapping` in an empty catch; throws become `toast.error` + `pipelineLog` error, draft still persists per 14-CONTEXT D-01."
    - "After successful persist in vision and layout-chunk finalize paths, `countUncertainMappings` drives `toast.warning` with count and summary string append when N > 0 per D-05."
  artifacts:
    - path: src/lib/ai/mappingQuality.ts
      provides: "Threshold + predicates for uncertain / unresolved / blanket single-page"
    - path: src/lib/logging/pipelineLogger.ts
      provides: "MAPPING domain for structured logs"
  key_links:
    - from: src/components/ai/AiParseSection.tsx
      to: src/lib/ai/mapQuestionsToPages.ts
      via: "applyQuestionPageMapping"
      pattern: "finalizeVisionParseResult / finalizeLayoutChunkParseResult"
---

<objective>
Implement **14-CONTEXT D-01–D-05, D-08** in code: remove silent mapping swallow in vision finalize, add **`mappingQuality`** helpers + **`MAPPING`** pipeline domain, surface **aggregate uncertain counts** via toast + parse summary after persist. Layout path keeps throws uncaught (D-02).

**Output:** `mappingQuality.ts`, extended `pipelineLogger`, updated `AiParseSection` finalize paths only (minimal diff outside those blocks).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-page-mapping-and-provenance-quality-surface-mapping-failures/14-CONTEXT.md
@.planning/phases/14-page-mapping-and-provenance-quality-surface-mapping-failures/14-UI-SPEC.md
@src/lib/ai/mapQuestionsToPages.ts
@src/components/ai/AiParseSection.tsx
@src/lib/logging/pipelineLogger.ts
</context>

## Decision coverage (14-CONTEXT)

| ID | Task | Notes |
|----|------|--------|
| D-01 | 2 | Remove empty catch; log + toast on throw |
| D-02 | 1 | Do not add catch on layout path |
| D-04–D-05 | 1, 3 | `mappingQuality.ts` + post-persist warnings |
| D-08 | 1 | No new persisted schema |

<tasks>

<task type="auto">
  <name>Task 1: mappingQuality helper + MAPPING pipeline domain</name>
  <files>src/lib/ai/mappingQuality.ts,src/lib/logging/pipelineLogger.ts</files>
  <read_first>src/types/question.ts,src/lib/ai/mapQuestionsToPages.ts,.planning/phases/14-page-mapping-and-provenance-quality-surface-mapping-failures/14-CONTEXT.md</read_first>
  <action>
**New file `src/lib/ai/mappingQuality.ts`:**
- Export `UNCERTAIN_MAPPING_CONFIDENCE_THRESHOLD = 0.45` (named constant).
- Export `isUnresolvedMapping(q: Question): boolean` — true when `mappingMethod === "unresolved"`.
- Export `isBlanketSinglePageVision(q: Question): boolean` — true when `mappingMethod === "vision_single_page"` (per 14-CONTEXT / 14-UI-SPEC: blanket page-1 provenance for multi-page exams is user-visible **Uncertain** tier).
- Export `isUncertainPageMapping(q: Question): boolean` — true when any of: unresolved; OR `mappingConfidence !== undefined && mappingConfidence < UNCERTAIN_MAPPING_CONFIDENCE_THRESHOLD`; OR `isBlanketSinglePageVision(q)`.
- Export `countUncertainMappings(questions: Question[]): number` — count where `isUncertainPageMapping` is true.

**`pipelineLogger.ts`:** extend `PipelineDomain` union with `"MAPPING"` (append to literal union). No runtime behavior change besides type allowing new call sites.

**Grep:** ensure no other file exhaustively switches on `PipelineDomain` without new case — if `switch (domain)` exists elsewhere, add `MAPPING` branch or default.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "MAPPING" src/lib/logging/pipelineLogger.ts` shows union includes MAPPING.
    - `rg -n "countUncertainMappings|UNCERTAIN_MAPPING" src/lib/ai/mappingQuality.ts` matches.
    - `rg -n "isUncertainPageMapping" src/lib/ai/mappingQuality.ts` matches.
  </acceptance_criteria>
  <done>Shared predicates and log domain ready.</done>
</task>

<task type="auto">
  <name>Task 2: Vision finalize — no empty catch; log + toast on mapping throw</name>
  <files>src/components/ai/AiParseSection.tsx</files>
  <read_first>src/components/ai/AiParseSection.tsx,src/lib/ai/mappingQuality.ts,src/lib/logging/pipelineLogger.ts</read_first>
  <action>
In `finalizeVisionParseResult`, **remove** the `try/catch` that wraps `applyQuestionPageMapping` (lines ~641–653 region — adjust to current file).

Replace with:
- Call `applyQuestionPageMapping(...)` **outside** try/catch, **or** use `try/catch` where `catch` calls `pipelineLog("MAPPING", "apply", "error", ...)` with `normalizeUnknownError(e)` fields, `toast.error("Page mapping failed — questions saved; review page links.")`, then **rethrow** OR continue without rethrow per product: **14-CONTEXT says still persist** — so **do not rethrow** if that would skip persist; **catch**, log, toast, then proceed to `setQuestions` / summary / `persistQuestions` as today.

Implement **catch** body that: logs + `toast.error` with short message; **does not** rethrow (so `setQuestions` and `persistQuestions` still run).

Remove the old comment about “best-effort / swallow”.

Import `toast` from `sonner` if not already in scope for this callback (reuse existing imports in file).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "applyQuestionPageMapping" src/components/ai/AiParseSection.tsx` shows call site **without** adjacent `catch \\{\\s*\\/\\* mapping is best-effort` block (grep for `best-effort` in file returns 0 or only unrelated).
    - `rg -n "MAPPING" src/components/ai/AiParseSection.tsx` shows at least one `pipelineLog` call with domain `MAPPING` in mapping failure path.
  </acceptance_criteria>
  <done>Vision path surfaces mapping exceptions.</done>
</task>

<task type="auto">
  <name>Task 3: Post-persist aggregate uncertain — toast.warning + summary append</name>
  <files>src/components/ai/AiParseSection.tsx</files>
  <read_first>src/components/ai/AiParseSection.tsx,src/lib/ai/mappingQuality.ts,.planning/phases/14-page-mapping-and-provenance-quality-surface-mapping-failures/14-UI-SPEC.md</read_first>
  <action>
After **`persistQuestions`** succeeds in:
- `finalizeVisionParseResult` (non-aborted, non-fatal branch)
- `finalizeLayoutChunkParseResult` (non-aborted branch)

Compute `n = countUncertainMappings(questionsArray)` on the **same array** just persisted (`result.questions` or `merged`).

If `n > 0`: `toast.warning` with message including **count** and hint to open Review (English string per existing app copy style).

Also **append** to the summary string already passed to `setSummary` **either** by building the full summary string with an extra clause `· N question(s) with uncertain page mapping` **or** by a small helper `appendMappingQualitySummary(prevSummary, n)` — ensure duplicate runs do not duplicate clause if function is called twice (prefer single final `setSummary` per path).

**Layout path:** wrap `applyQuestionPageMapping` only if needed for aggregate — **do not** add empty catch; only add post-persist uncertain toast (mapping already runs uncaught).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "countUncertainMappings" src/components/ai/AiParseSection.tsx` matches at least twice (vision + layout) OR once if shared helper used in both paths.
    - `rg -n "uncertain page mapping" src/components/ai/AiParseSection.tsx` matches (substring of toast or summary).
  </acceptance_criteria>
  <done>Aggregate uncertainty visible after parse.</done>
</task>

<task type="auto">
  <name>Task 4: OCR snapshot load failure — user-visible once (D-03)</name>
  <files>src/components/ai/AiParseSection.tsx</files>
  <read_first>src/components/ai/AiParseSection.tsx,.planning/phases/14-page-mapping-and-provenance-quality-surface-mapping-failures/14-CONTEXT.md</read_first>
  <action>
In **`finalizeVisionParseResult`**, the block that loads `ocrSnapshot` via `getOcrResult(studySetId)` inside `try/catch` with empty or silent catch: replace with **`catch` body** that:
- Calls `pipelineLog("VISION", "ocr-snapshot", "warn", ...)` (or `IDB` if more accurate) with `{ studySetId, message: normalized }` — **no API keys**.
- Shows **`toast.warning` once** with short copy e.g. “Could not load saved OCR for page mapping; mapping may use vision-only data.”

Use a **ref or module flag** if needed to avoid duplicate toasts when both vision and layout paths could run in one session — prefer **single toast per finalizeVision invocation** only.

Do **not** change layout path’s OCR fetch unless the same silent pattern exists there; if layout path has identical silent catch, apply the same pattern for symmetry.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "getOcrResult" src/components/ai/AiParseSection.tsx` shows a catch block that references `toast.warning` or `pipelineLog` (not empty `catch \\{\\s*\\/\\*` only).
  </acceptance_criteria>
  <done>OCR load failure is visible when mapping lacks OCR rows.</done>
</task>

<task type="auto">
  <name>Task 5: Wave 1 build verification</name>
  <files>src/components/ai/AiParseSection.tsx,src/lib/ai/mappingQuality.ts,src/lib/logging/pipelineLogger.ts</files>
  <read_first>.planning/phases/14-page-mapping-and-provenance-quality-surface-mapping-failures/14-01-PLAN.md</read_first>
  <action>
Run **`npm run build`** after Tasks 1–4. Fix only issues introduced by this wave.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Wave 1 compiles.</done>
</task>

</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Logging PII | Do not log full `mappingReason` bodies in `error` context if huge — truncate in payload. |
| Toast spam | Fire aggregate toast **once per finalize** path, not per question. |
| User alarm on benign throws | Message states questions **saved**; map fix is follow-up. |
</threat_model>

<verification>
- Tasks 1–4: `npm run lint` per task
- Task 5: `npm run build`
</verification>

## PLANNING COMPLETE
