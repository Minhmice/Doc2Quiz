---
phase: 25-skip-rasterization-for-born-digital-pdfs-extract-text-layer-
plan: 02
type: execute
wave: 2
depends_on:
  - "25-01"
files_modified:
  - src/components/ai/AiParseSection.tsx
  - src/components/ai/AiParseParseStrategyPanel.tsx
  - src/lib/ai/chunkText.ts
  - src/lib/ai/runSequentialParse.ts
autonomous: true
requirements:
  - PDFOPT-02
  - PDFOPT-03
  - PDFOPT-04
must_haves:
  truths:
    - "For quiz parses, when the text-layer signal is strong and strategy is not Accurate, the app runs text-first without rasterizing pages."
    - "If text-first results are too weak, the app automatically falls back to vision and tells the user (toast + overlay one-liner)."
    - "If the text-layer signal is uncertain, the default route is vision (safe fallback)."
  artifacts:
    - path: "src/components/ai/AiParseSection.tsx"
      provides: "Quiz routing: sampled text signal → text-first runSequentialParse or vision fallback"
    - path: "src/components/ai/AiParseParseStrategyPanel.tsx"
      provides: "UI hint/label for strong-text auto routing"
  key_links:
    - from: "src/components/ai/AiParseSection.tsx"
      to: "src/lib/pdf/sampleTextLayerSignal.ts"
      via: "compute sampled signal before routing"
      pattern: "sampleTextLayerSignal"
    - from: "src/components/ai/AiParseSection.tsx"
      to: "src/lib/ai/runSequentialParse.ts"
      via: "text-first chunk execution"
      pattern: "runSequentialParse"
    - from: "src/components/ai/AiParseSection.tsx"
      to: "src/lib/pdf/renderPagesToImages.ts"
      via: "vision fallback only (not pre-step when strong text)"
      pattern: "renderPdfPagesToImages"
---

<objective>
Re-enable a quiz text-first execution lane driven by Phase 25’s text-layer signal, skipping rasterization for born-digital PDFs, and add a deterministic quality gate that can auto-fallback to vision with short UX messaging and stable reason codes.

Purpose: Save time/money on born-digital PDFs while keeping safety/recall via automatic fallback.
Output: Updated `AiParseSection` routing + UI hint copy consistent with D-06, plus quality gate + logging.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-skip-rasterization-for-born-digital-pdfs-extract-text-layer-/25-CONTEXT.md
@src/components/ai/AiParseSection.tsx
@src/lib/ai/chunkText.ts
@src/lib/ai/runSequentialParse.ts
@src/lib/ai/parseRoutePolicy.ts
@src/lib/pdf/sampleTextLayerSignal.ts
@src/lib/pdf/renderPagesToImages.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a quiz text-first lane that skips rasterization when text is strong</name>
  <files>src/components/ai/AiParseSection.tsx</files>
  <action>
    Implement Phase 25 routing in `AiParseSection` for `parseOutputMode === "quiz"`:

    1) Compute text-layer signal by sampling the first 3–5 pages from `activePdfFile` using `sampleTextLayerSignal(...)` (per D-04), even if `doc.extractedText` exists in IDB.

    2) Decide route using the updated `decideParseRoute` input so it can use the sampled signal (D-01..D-03).
       - If parse strategy is Accurate → always vision-first (D-07).
       - If sampled signal is strong → auto-route to text-first for quiz and SKIP rasterization pre-step (D-05).
       - If sampled signal is uncertain → default to vision (D-03).
       - This is document-level only; no per-page routing (deferred).

    3) Text-first execution:
       - Extract full text ONLY when actually taking the text-first path:
         - Prefer IDB `doc.extractedText` if present; otherwise call `extractPdfText(activePdfFile, signal)` to produce it (still text-layer only).
       - Chunk via `chunkText(fullText)` and run `runSequentialParse(...)`.
       - Update overlay/progress log with a one-liner like: “Using text layer — skipping page images.” plus reason codes (D-13..D-16).
       - Persist questions via existing `persistQuestions` (same as other lanes).

    4) Ensure rasterization is only invoked in the vision lanes or in fallback, not before route decision when strong text.

    NOTE: This requires removing/relaxing the current “quiz is vision-batch-only” guard in `runUnifiedParseInternal` (currently forces quiz to vision). Replace with Phase 25 logic while keeping flashcards vision-batch-only unchanged.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
    In quiz mode with strong-text signal and non-Accurate strategy, `AiParseSection` can complete a parse without calling `renderPdfPagesToImages` unless a fallback triggers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic quality gate + automatic vision fallback with toast</name>
  <files>src/components/ai/AiParseSection.tsx</files>
  <action>
    Implement the Phase 25 quality gate (D-08..D-12) for the quiz text-first lane:

    Gate inputs (deterministic):
    - `questionCount` from text-first output
    - A validity/confidence summary derived from the output (D-11). Use only deterministic local checks; do not call any external service.

    Default gate rules:
    - If `questionCount < 5` → fallback to vision (D-12).
    - Also fallback when validity summary is poor. Recommended deterministic definition:
      - `validMcqCount = count of questions where isMcqComplete(q) === true`
      - `validRatio = validMcqCount / max(1, questionCount)`
      - fallback if `questionCount > 0` and `validRatio < 0.6`
    (Exact ratio threshold is at Claude’s discretion per context; MUST be logged with values.)

    Fallback behavior:
    - When gate fails, automatically call the existing vision path (batch vision for quiz is fine) and show:
      - a short toast (D-14), e.g. “Text parse looked weak — retrying with vision.”
      - an overlay one-liner with reason codes (D-15..D-16), including the gate metrics (`questionCount`, `validRatio`) as numbers only.

    Ensure no extra confirmation prompt is introduced (D-08).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    When text-first output is weak (<5 questions or low validity), quiz automatically reruns via vision and the user sees a toast + overlay log line; routing logs include reason codes and numeric gate metrics.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Parse Strategy UI hint copy for strong text routing</name>
  <files>src/components/ai/AiParseParseStrategyPanel.tsx</files>
  <action>
    Update the hint shown when `documentHint === "strong_text_layer"` to align with Phase 25 UX (D-06, D-13):
    - Make the hint a short one-liner and explicitly convey “text-first auto” / “skipping page images” for quiz when not Accurate.
    - Keep Accurate description as vision-first (D-07).

    IMPORTANT: Do not add new controls; the override must remain inside the existing parse strategy panel (D-06).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
    Strong-text hint is visible and clearly communicates the auto-route behavior without adding UI complexity.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User PDF → parsing + AI calls | Untrusted PDF content influences routing; AI calls cost money and may exfiltrate content to the chosen provider. |
| Local storage → routing | Stored `doc.extractedText` and settings influence whether expensive vision runs happen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-25-04 | D (DoS) | Text-first parsing loop | mitigate | Use existing sequential chunk execution; enforce chunk size via `chunkText` constants; respect AbortSignal in parse loops. |
| T-25-05 | I (Info Disclosure) | Overlay logs/toasts | mitigate | Ensure routing lines contain only reason codes + numeric metrics (no extracted text). |
| T-25-06 | S (Spoofing) | “Strong text” hint | accept | Hint is informational only; user can still choose Accurate (vision) explicitly; uncertain defaults to vision. |
</threat_model>

<verification>
- `npm run lint`
- `npm run build`
</verification>

<success_criteria>
- Quiz parses route to text-first (no rasterization) when sampled text layer is strong and strategy is not Accurate.
- Uncertain text-layer signal defaults to vision.
- Weak text-first output automatically triggers vision fallback with a toast and overlay reason codes.
- No per-page routing is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/25-skip-rasterization-for-born-digital-pdfs-extract-text-layer-/25-02-SUMMARY.md`
</output>

