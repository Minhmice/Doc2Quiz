---
phase: 34-add-async-workers-task-queue-for-full-indexing
plan: 02
type: execute
wave: 2
depends_on:
  - "34-01"
files_modified:
  - src/lib/ai/embeddingIndexScheduler.ts
  - src/lib/db/studySetDb.ts
  - src/components/ai/AiParseSection.tsx
autonomous: true
requirements:
  - "INDEX-34-05"
  - "INDEX-34-06"
  - "INDEX-34-07"

must_haves:
  truths:
    - "After extracted text is successfully persisted to `study_set_documents`, a debounced scheduler attempts full indexing without blocking `putDocument` callers."
    - "A second persist with the same text fingerprint + model identity does not spawn redundant network-heavy rebuilds when metadata says the index is fresh."
    - "Only one index job runs per `studySetId` at a time; new triggers coalesce or replace the pending debounced run."
  artifacts:
    - path: "src/lib/ai/embeddingIndexScheduler.ts"
      provides: "scheduleEmbeddingIndexForStudySet, cancelEmbeddingIndexSchedule, registerEmbeddingIndexRunner (or equivalent single-flight map)"
    - path: "src/lib/db/studySetDb.ts"
      provides: "calls into scheduler after putDocument success paths"
  key_links:
    - from: "src/lib/db/studySetDb.ts"
      to: "src/lib/ai/embeddingIndexScheduler.ts"
      via: "import side-effect safe: scheduler does not import heavy UI"
      pattern: "scheduleEmbeddingIndex|putDocument success"
    - from: "src/lib/ai/embeddingIndexScheduler.ts"
      to: "src/lib/ai/embeddingIndexJob.ts"
      via: "runEmbeddingIndexJob when runner registered"
      pattern: "runEmbeddingIndexJob"
---

<objective>
Wire **automatic, debounced full indexing** after extracted text is persisted (**D-02**, INDEX-34-05): hook **`putDocument`** and the **`enrichStudySetDocumentFromLocalPdf`** success path (already calls `putDocument`). Ensure **idempotency** via build metadata + content fingerprint (**INDEX-34-06**) and **single-flight** per `studySetId` (**INDEX-34-07**). The scheduler holds **no API keys** — it receives forward settings from **`AiParseSection`** via a small registration API.

Purpose: Indexing starts after extract without manual "Build embedding index" for the common path; Wave 3 only displays state.
Output: `embeddingIndexScheduler.ts`, minimal hooks in `studySetDb.ts` + `AiParseSection.tsx`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-add-async-workers-task-queue-for-full-indexing/34-CONTEXT.md

@src/lib/db/studySetDb.ts
@src/lib/ai/embeddingIndexJob.ts
@src/components/ai/AiParseSection.tsx
</context>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Scheduler → job | Must not run job without registered runner (no raw key in `studySetDb`). |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04 | E | debounce storm on rapid saves | mitigate | Debounce 400–800ms; coalesce by `studySetId`. |
| T-34-05 | I | logging | mitigate | Log ids + schedule events only — no `apiKey`. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: `embeddingIndexScheduler.ts` — debounce + single-flight + optional skip</name>
  <files>src/lib/ai/embeddingIndexScheduler.ts</files>
  <read_first>
@src/lib/ai/embeddingIndexJob.ts
@src/lib/db/embeddingIndexDb.ts
  </read_first>
  <action>
Create **`src/lib/ai/embeddingIndexScheduler.ts`**:

1. **Module-level state:** a `Map` from `studySetId` string to `AbortController` for in-flight jobs; a `Map` for debounce timers.

2. **`registerEmbeddingIndexRunner(runner)`** where `runner` is an async function on a context `{ studySetId, signal, getFullText }` (fullText promise factory) — the **implementation** is provided by **`AiParseSection`** (reads `getDocument`, `keyInput`, `urlInput`). Alternative: pass `apiKey`/`forwardBaseUrl` at schedule time from UI — **do not** read localStorage for keys inside `studySetDb`.

3. **`scheduleEmbeddingIndexAfterExtract(studySetId: string)`** — called from `putDocument` success. It **debounces** (pick **500ms** default, constant at top of file). On fire: if job running for id, **abort previous** or **skip** if same generation — **policy:** cancel in-flight only when new extract supersedes (use `AbortSignal` on `runEmbeddingIndexJob`). Expose **`cancelEmbeddingIndexForStudySet(studySetId)`** for Wave 3.

4. **Idempotency (INDEX-34-06):** Before calling runner, optionally call into **`embeddingIndexGetBuildMeta`** — if meta matches `sha256(trimmed text))`, model, version, and chunk count equals `chunkText(trim).length`, **skip** (no-op). This requires runner to resolve text — **fast path** can live inside runner registered from AiParseSection.

5. **Exports:** keep tree-shake friendly; no React imports.

6. **`pipelineLog`:** `embedding_index_schedule`, `embedding_index_debounced` with `studySetId` only.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
`rg "scheduleEmbeddingIndexAfterExtract|registerEmbeddingIndexRunner|cancelEmbeddingIndexForStudySet" src/lib/ai/embeddingIndexScheduler.ts` matches all three symbols.
  </acceptance_criteria>
  <done>
Scheduler module exists; single-flight + debounce; no API keys in module.
  </done>
</task>

<task type="auto">
  <name>Task 2: Hook `putDocument` + `createStudySet` text update paths</name>
  <files>src/lib/db/studySetDb.ts</files>
  <read_first>
@src/lib/db/studySetDb.ts (functions `putDocument`, `createStudySet`, `enrichStudySetDocumentFromLocalPdf`)
@src/lib/ai/embeddingIndexScheduler.ts
  </read_first>
  <action>
At the end of **`putDocument`** success block (after `putDocument success` log), if `doc.extractedText.trim().length > 0`, call **`scheduleEmbeddingIndexAfterExtract(doc.studySetId)`** (dynamic `import()` allowed to avoid circular deps if needed — prefer direct import if graph stays clean).

Ensure **`createStudySet`** paths that **update** `study_set_documents.extracted_text` without going through `putDocument` also schedule once (grep shows insert + update branches) — if text is written only via Supabase in `createStudySet`, duplicate the **schedule** call after successful text persist there.

**Do not** await the job inside `putDocument` — scheduling only.

`enrichStudySetDocumentFromLocalPdf` already ends with `putDocument` — **no duplicate** schedule call inside enrich if `putDocument` always fires.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
`rg "scheduleEmbeddingIndexAfterExtract" src/lib/db/studySetDb.ts` returns at least one line inside or immediately after `putDocument` success; `rg "putDocument success" src/lib/db/studySetDb.ts` still present.
  </acceptance_criteria>
  <done>
Extract persist triggers debounced schedule; non-blocking.
  </done>
</task>

<task type="auto">
  <name>Task 3: `AiParseSection` — register runner (keys + forward URL)</name>
  <files>src/components/ai/AiParseSection.tsx</files>
  <read_first>
@src/components/ai/AiParseSection.tsx (imports, `keyInput`, `urlInput`, `studySetId`, effects)
@src/lib/ai/embeddingIndexScheduler.ts
  </read_first>
  <action>
In **`AiParseSection`**, on mount (or when `studySetId` / key / URL stable), call **`registerEmbeddingIndexRunner`** with an implementation that:

1. Loads `getDocument(studySetId)` and uses `extractedText` as full text.
2. If `!apiKey.trim()`, **return** early (log pipeline skip, no throw).
3. Invokes **`runEmbeddingIndexJob`** with `apiKey`, `forwardBaseUrl` from component state (`urlInput` / forward settings pattern already used for parse).
4. Wires **`signal`** from scheduler context for cancel.

Use **`useEffect`** cleanup to **`cancelEmbeddingIndexForStudySet(studySetId)`** on unmount or `studySetId` change.

**Important:** Registration must **update** when `apiKey` or `forwardBaseUrl` changes so auto-index uses current Settings.

Keep **flashcard** lane behavior unchanged — if `isFlashcardParse` hides RAG, auto-index may still run for stored text (product decision: **still schedule** — text exists for potential quiz mode switch); do **not** add branching unless `34-CONTEXT` blocked it (it did not).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
`rg "registerEmbeddingIndexRunner|runEmbeddingIndexJob|embeddingIndexScheduler" src/components/ai/AiParseSection.tsx` matches; `rg "useEffect" src/components/ai/AiParseSection.tsx` includes new effect block with cleanup (count may increase by 1).
  </acceptance_criteria>
  <done>
Runner registered from UI layer; BYOK keys stay client-side; unmount cancels scheduled/in-flight work per study set.
  </done>
</task>

</tasks>

<verification>
Manual smoke (optional): save extract → observe `pipelineLog` / future UI in 34-03. Automated: `npm run lint`.
</verification>

<success_criteria>
`putDocument` with non-empty text schedules indexing; only one concurrent job per `studySetId`; fingerprint skip prevents redundant rebuild when meta matches.
</success_criteria>

<output>
After completion, create `.planning/phases/34-add-async-workers-task-queue-for-full-indexing/34-02-SUMMARY.md`
</output>
