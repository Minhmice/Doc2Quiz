---
phase: 34-add-async-workers-task-queue-for-full-indexing
plan: 03
type: execute
wave: 3
depends_on:
  - "34-02"
files_modified:
  - src/components/ai/RagChunkSearchPanel.tsx
  - src/components/ai/AiParseSection.tsx
  - src/lib/ai/embeddingIndexScheduler.ts
autonomous: true
requirements:
  - "INDEX-34-09"

must_haves:
  truths:
    - "RAG panel shows indexing state: idle, running (chunk progress), completed, error (last message), and user can cancel in-flight work."
    - "Manual “Build embedding index” still works and uses the same job entrypoint as auto-index."
    - "Production build and ESLint pass."
  artifacts:
    - path: "src/components/ai/RagChunkSearchPanel.tsx"
      provides: "indexingStatus UI + cancel button + progress text"
    - path: "src/components/ai/AiParseSection.tsx"
      provides: "props/callbacks wiring job state into RAG panel"
  key_links:
    - from: "src/components/ai/RagChunkSearchPanel.tsx"
      to: "src/lib/ai/embeddingIndexJob.ts"
      via: "manual build calls runEmbeddingIndexJob"
      pattern: "runEmbeddingIndexJob|handleBuildIndex"
    - from: "src/components/ai/RagChunkSearchPanel.tsx"
      to: "src/lib/ai/embeddingIndexScheduler.ts"
      via: "cancel uses cancelEmbeddingIndexForStudySet"
      pattern: "cancelEmbeddingIndex"
---

<objective>
Expose **indexing job state** in the **RAG / semantic panel** (**D-03**, INDEX-34-09): **idle / running / done / error**, **chunk progress** (e.g. `i/n`), **Cancel** (uses scheduler + `AbortSignal`), **last error** string; keep **manual “Build embedding index”** using **`runEmbeddingIndexJob`** with the same progress reporting. Finish with **`npm run lint`** and **`npm run build`**.

Purpose: User-visible observability for async indexing; primary UX is RAG panel, not a global jobs dashboard.
Output: updated `RagChunkSearchPanel.tsx`, coordinated props from `AiParseSection.tsx`, optional small additions to scheduler for observable state.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-add-async-workers-task-queue-for-full-indexing/34-CONTEXT.md

@src/components/ai/RagChunkSearchPanel.tsx
@src/components/ai/AiParseSection.tsx
@src/lib/ai/embeddingIndexJob.ts
@src/lib/ai/embeddingIndexScheduler.ts
</context>

<threat_model>
## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-06 | S | error messages | mitigate | Show user-safe strings from `Error.message` — no stack traces, no key substrings. |
| T-34-07 | X | cancel race | accept | UI disables cancel when idle; job handles abort. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Observable job state bridge (scheduler or React state)</name>
  <files>src/lib/ai/embeddingIndexScheduler.ts, src/components/ai/AiParseSection.tsx</files>
  <read_first>
@src/lib/ai/embeddingIndexScheduler.ts
@src/components/ai/AiParseSection.tsx (RagChunkSearchPanel usage ~3354)
  </read_first>
  <action>
Add a **subscription** or **callback** API so UI can observe indexing state per `studySetId`, e.g.:

- `subscribeEmbeddingIndexStatus(studySetId, (s) => void): () => void` where `s` is `{ status: "idle"|"running"|"done"|"error"; current?: number; total?: number; lastError?: string | null }`.

Update the **registered runner** in `AiParseSection` to **emit** progress from `onProgress` of `runEmbeddingIndexJob` and **set done/error** on completion. **Cancel** path: `cancelEmbeddingIndexForStudySet` aborts the job; surface `idle` or `error` with message "Cancelled" / abort reason.

Pass **`indexingStatus`** (or individual props) into **`RagChunkSearchPanel`**.

**Accessibility:** running state should use `aria-live="polite"` for progress text updates.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
`rg "subscribeEmbeddingIndexStatus|indexingStatus" src/lib/ai/embeddingIndexScheduler.ts src/components/ai/AiParseSection.tsx` matches new API or equivalent props (names may differ if consistent).
  </acceptance_criteria>
  <done>
RAG panel can render live status without polling `IDB` directly for progress.
  </done>
</task>

<task type="auto">
  <name>Task 2: `RagChunkSearchPanel` — progress, cancel, last error, manual build</name>
  <files>src/components/ai/RagChunkSearchPanel.tsx</files>
  <read_first>
@src/components/ai/RagChunkSearchPanel.tsx
@src/lib/ai/buildEmbeddingIndex.ts
  </read_first>
  <action>
Extend **`RagChunkSearchPanelProps`** with props for **indexing status** and **`onCancelIndexing`** (or inline cancel from scheduler).

**UI:**
- Row below or beside **Build embedding index**: show **Indexing: i/n** when `status==="running"` and totals known; show **Last error:** muted/red when `status==="error"`.
- **Cancel** button enabled when running; calls `onCancelIndexing`.
- **Manual build** button calls **`runEmbeddingIndexJob`** (not legacy loop) with `onProgress` updating local busy state **or** unified status — avoid duplicate conflicting state with auto-job: prefer **single** status source from scheduler subscription so manual + auto show the same progress.

**Search** and **context textarea** remain as today (Phase 33).

**D-05 safe display:** chunk text in hits remains plain text — no `dangerouslySetInnerHTML`.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
`rg "onCancelIndexing|runEmbeddingIndexJob|Indexing" src/components/ai/RagChunkSearchPanel.tsx` matches; `buildEmbeddingIndexFromPlainText` either removed from panel or only re-exported — prefer **`runEmbeddingIndexJob`** import.
  </acceptance_criteria>
  <done>
Panel is the primary surface for indexing UX per D-03.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify `npm run lint` + `npm run build`</name>
  <files>package.json</files>
  <read_first>
@package.json
  </read_first>
  <action>
Run **`npm run lint`** and **`npm run build`** from repo root; fix any errors introduced by Phase 34 changes.
  </action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
Both commands exit 0 with no unhandled TypeScript errors.
  </acceptance_criteria>
  <done>
CI-local verification green for Wave 3.
  </done>
</task>

</tasks>

<verification>
`npm run lint` + `npm run build` pass; manual: open parse page, observe RAG panel state when indexing runs.
</verification>

<success_criteria>
RAG panel shows progress/cancel/error; manual rebuild works; lint + build succeed.
</success_criteria>

<output>
After completion, create `.planning/phases/34-add-async-workers-task-queue-for-full-indexing/34-03-SUMMARY.md`
</output>
