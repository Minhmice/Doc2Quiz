---
phase: 02-ai-question-parsing
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/lib/ai/errors.ts
  - src/lib/ai/validateQuestions.ts
  - src/lib/ai/parseChunk.ts
  - src/lib/ai/runSequentialParse.ts
autonomous: true
requirements:
  - AI-03
  - AI-04
user_setup: []

must_haves:
  truths:
    - "OpenAI + Anthropic HTTP calls use fetch from browser with user key"
    - "Each chunk retried once on failure; then skipped with failedChunks++"
    - "AbortSignal aborts loop and preserves accumulated questions"
  artifacts:
    - "runSequentialParse or equivalent exported for UI"
  key_links:
    - "401/429 map to CONTEXT D-18 user strings"
---

<objective>
Implement **provider-specific AI calls**, **JSON extraction + validation** into `Question[]`, **one retry per chunk**, and **sequential runner** with **AbortSignal** (D-10, D-12–D-13, D-18–D-19).

Purpose: Core AI-03 / AI-04 logic without UI.
Output: `errors.ts`, `validateQuestions.ts`, `parseChunk.ts`, `runSequentialParse.ts`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ai-question-parsing/02-CONTEXT.md
@src/types/question.ts
@src/lib/ai/chunkText.ts
</context>

<interfaces>
**Models (concrete defaults — document in code comments):**
- OpenAI: `gpt-4o-mini` via `POST https://api.openai.com/v1/chat/completions`
- Anthropic: `claude-3-5-haiku-20241022` via `POST https://api.anthropic.com/v1/messages` with header `anthropic-version: 2023-06-01`

**Prompt output shape:** Instruct model to return JSON with top-level **`questions`** array of `{ "question": string, "options": string[4], "correctIndex": number }`. For OpenAI use `response_format: { "type": "json_object" }`. For Anthropic, parse first JSON object from assistant text (strip ``` fences if present).

**validateQuestions.ts:** Keep items where: non-empty `question`, `options.length === 4`, all options non-empty strings, `correctIndex` integer 0–3. Assign **`crypto.randomUUID()`** per accepted question.

**parseChunk.ts:** `parseChunkOnce(params: { provider, apiKey, chunkText, signal })` returns `Question[]`. On HTTP **401** throw error with message exactly `Invalid API key. Please check and try again.` On **429** throw `Too many requests. Please wait and try again.` These abort the **entire job** (runner catches and rethrows).

**runSequentialParse.ts:** Signature pattern:
```typescript
export type ParseProgress = {
  current: number;
  total: number;
  status: "idle" | "running" | "done";
};

export async function runSequentialParse(options: {
  provider: AiProvider;
  apiKey: string;
  chunks: string[];
  signal: AbortSignal;
  onProgress?: (p: { current: number; total: number }) => void;
}): Promise<{ questions: Question[]; failedChunks: number }>;
```
Loop: for `i` in `0..chunks.length-1`, if `signal.aborted` break immediately; call `parseChunkOnce` with **inner retry**: max **2** attempts (initial + 1 retry) for **transient** failures (network, parse); on final failure increment `failedChunks` and continue. **Do not** retry 401/429 (fail fast).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Errors + validation helpers</name>
  <files>src/lib/ai/errors.ts, src/lib/ai/validateQuestions.ts</files>
  <read_first>src/types/question.ts, .planning/phases/02-ai-question-parsing/02-CONTEXT.md</read_first>
  <action>
Add `src/lib/ai/errors.ts` exporting a small `FatalParseError` (or named similarly) carrying **user-facing** `message` for global abort. Add `validateQuestionsFromJson(raw: unknown): Question[]` in `validateQuestions.ts` implementing rules from `interfaces`.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `validateQuestions.ts` contains `options.length === 4` check (as `=== 4` or equivalent)
    - `correctIndex` bounded 0–3
  </acceptance_criteria>
  <done>Validation helpers compile</done>
</task>

<task type="auto">
  <name>Task 2: parseChunk (OpenAI + Anthropic)</name>
  <files>src/lib/ai/parseChunk.ts</files>
  <read_first>src/lib/ai/validateQuestions.ts, src/types/question.ts</read_first>
  <action>
Implement `parseChunkOnce` per CONTEXT + `interfaces`. Build a **system** message: you extract MCQs as JSON only; **user** message contains the chunk text. Parse JSON, normalize to `questions` array, run through `validateQuestionsFromJson`. Use `fetch` + `signal` on OpenAI requests (Anthropic supports abort via signal in fetch).
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - File contains substring `api.openai.com` and substring `api.anthropic.com`
    - File uses `fetch(` for HTTP
  </acceptance_criteria>
  <done>Both providers callable</done>
</task>

<task type="auto">
  <name>Task 3: runSequentialParse orchestration</name>
  <files>src/lib/ai/runSequentialParse.ts</files>
  <read_first>src/lib/ai/parseChunk.ts, src/lib/ai/chunkText.ts</read_first>
  <action>
Implement `runSequentialParse` per `interfaces`. Call `onProgress` **after** each chunk completes (success or skip) with `current: i+1`, `total: chunks.length`. On abort, return partial `{ questions, failedChunks }` immediately without clearing accumulated questions.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Exported function name `runSequentialParse` exists in `src/lib/ai/runSequentialParse.ts`
    - File references `AbortSignal` or `signal.aborted`
  </acceptance_criteria>
  <done>Runner ready for UI wiring</done>
</task>

</tasks>

<threat_model>
## Trust boundaries
| Boundary | Description |
|----------|-------------|
| Browser → OpenAI/Anthropic | User API key in `Authorization` / `x-api-key` |

## STRIDE
| ID | Cat | Mitigation |
|----|-----|------------|
| T-02c-1 | I | Keys only in memory/network to vendor; never log key |
| T-02c-2 | T | Validate JSON shape before use |
</threat_model>

<verification>
- [ ] `npm run build`
</verification>

<success_criteria>
- Sequential + retry + cancel semantics per CONTEXT
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-question-parsing/02-03-SUMMARY.md`
</output>
