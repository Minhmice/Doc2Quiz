---
phase: 02-ai-question-parsing
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - src/components/ai/AiParseSection.tsx
  - src/components/ai/QuestionPreviewList.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - AI-01
  - AI-05
user_setup: []

must_haves:
  truths:
    - "User can toggle provider, enter key with show/hide/clear, see trust copy"
    - "Progress shows Parsing questions… n / total chunks"
    - "Cancel aborts run and keeps prior questions in UI"
    - "Draft questions persisted to localStorage key LS_DRAFT_QUESTIONS on successful completion"
  artifacts:
    - "AiParseSection.tsx and QuestionPreviewList.tsx exist"
  key_links:
    - "page.tsx passes extractedText string into AiParseSection"
---

<objective>
Compose the **AI Parsing Section** under the Phase 1 viewer: provider toggle, API key UX, Parse / Cancel, progress, friendly errors, question preview, and **draft** persistence (D-01, D-05–D-07, D-14–D-19, D-21).

Purpose: Completes AI-01 (UI) and AI-05 (progress feedback) and wires all lib modules.
Output: `AiParseSection.tsx`, `QuestionPreviewList.tsx`, updated `page.tsx`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ai-question-parsing/02-CONTEXT.md
@.planning/phases/02-ai-question-parsing/02-UI-SPEC.md
@src/lib/ai/storage.ts
@src/lib/ai/chunkText.ts
@src/lib/ai/runSequentialParse.ts
@src/types/question.ts
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: QuestionPreviewList</name>
  <files>src/components/ai/QuestionPreviewList.tsx</files>
  <read_first>.planning/phases/02-ai-question-parsing/02-UI-SPEC.md, src/types/question.ts</read_first>
  <action>
Create a presentational component listing `questions: Question[]` — each item shows stem, four labeled options (A–D), visually indicate **correct** index (e.g. border or badge). **No** `dangerouslySetInnerHTML`. Compact card layout with Tailwind.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - File exports a component used for preview (named export)
    - Renders four options per question
  </acceptance_criteria>
  <done>Preview list ready</done>
</task>

<task type="auto">
  <name>Task 2: AiParseSection logic + UI</name>
  <files>src/components/ai/AiParseSection.tsx</files>
  <read_first>src/lib/ai/storage.ts, src/lib/ai/chunkText.ts, src/lib/ai/runSequentialParse.ts, .planning/phases/02-ai-question-parsing/02-CONTEXT.md</read_first>
  <action>
Props: `extractedText: string` (may be empty). Implement:

1. **State:** provider (sync from `getProvider()` on mount), keys loaded from storage, local input value for active key, `showKey` boolean, `questions`, `failedChunks`, `progress` `{ current, total, status }`, `error` string | null, `summary` string | null.

2. **Trust copy** exactly per UI-SPEC (D-06).

3. **Parse disabled** when: no non-empty `extractedText.trim()`, no key for active provider, or `status === "running"`.

4. On **Parse:** `const chunks = chunkText(extractedText)`; if `chunks.length === 0` show inline message (no API calls). Else `AbortController`, `runSequentialParse({ provider, apiKey, chunks, signal, onProgress })`. Progress label: **`Parsing questions…`** + **`{current} / {total} chunks`**.

5. **Cancel** button visible only while running; calls `abort()`.

6. **Errors:** Map fatal errors to D-18 strings; generic chunk failures already counted — show summary **Parsed X questions** and **Y chunks failed** (allow minor wording if counts correct).

7. **Persistence:** On successful completion (`status` done, no fatal throw), `localStorage.setItem(LS_DRAFT_QUESTIONS, JSON.stringify({ savedAt: new Date().toISOString(), questions }))`.

8. On mount, optionally **hydrate preview** from `LS_DRAFT_QUESTIONS` if parseable (Claude discretion).

Use `"use client"`.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `AiParseSection.tsx` contains substring `Your API key is stored locally in your browser`
    - Contains `Parsing questions` and substring `chunks`
    - Contains `Cancel` as visible button text or child text while running
  </acceptance_criteria>
  <done>AI section self-contained</done>
</task>

<task type="auto">
  <name>Task 3: Integrate into page.tsx</name>
  <files>src/app/page.tsx</files>
  <read_first>src/components/ai/AiParseSection.tsx, src/app/page.tsx</read_first>
  <action>
Import `AiParseSection`. Render it **below** `<RawTextViewer />` inside the same column layout. Pass `extractedText={extractedText ?? ""}` or equivalent so AI always receives a string (empty when null). **Do not remove** UploadBox or RawTextViewer.
  </action>
  <verify>npm run build && npm run lint</verify>
  <acceptance_criteria>
    - `src/app/page.tsx` imports `AiParseSection`
    - `AiParseSection` appears after `RawTextViewer` in JSX source order
  </acceptance_criteria>
  <done>Phase 2 visible on home page</done>
</task>

</tasks>

<threat_model>
## Trust boundaries
| Boundary | Description |
|----------|-------------|
| Key input → localStorage | User-controlled secret |

## STRIDE
| ID | Cat | Mitigation |
|----|-----|------------|
| T-02d-1 | I | Password input; mask by default |
</threat_model>

<verification>
- [ ] `npm run build` && `npm run lint`
</verification>

<success_criteria>
- End-to-end UX matches CONTEXT + UI-SPEC for Phase 2 shell
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-question-parsing/02-04-SUMMARY.md`
</output>
