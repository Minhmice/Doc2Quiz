---
phase: 02-ai-question-parsing
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/lib/ai/chunkText.ts
autonomous: true
requirements:
  - AI-02
user_setup: []

must_haves:
  truths:
    - "Long extracted text becomes multiple chunks each ≤ 1200 chars (after trim) except last"
    - "Paragraph boundaries preferred over mid-word splits"
  artifacts:
    - "src/lib/ai/chunkText.ts exports chunkText(text: string): string[]"
  key_links:
    - "No mutation of input string (return new chunks only)"
---

<objective>
Implement **deterministic text chunking** for AI requests: target **~800–1200** characters, prefer `\n\n` breaks, then hard split (D-08, D-09, D-11).

Purpose: Satisfies AI-02 and enforces “always chunk before AI” (CONTEXT hard rules).
Output: `src/lib/ai/chunkText.ts`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ai-question-parsing/02-CONTEXT.md
</context>

<interfaces>
```typescript
// Constants in chunkText.ts — concrete values
export const CHUNK_SOFT_TARGET = 1000;
export const CHUNK_HARD_MAX = 1200;
export const CHUNK_SOFT_MIN = 800;

export function chunkText(fullText: string): string[];
```
Split algorithm (must follow):
1. Normalize: trim `fullText`; if empty return `[]`.
2. Prefer merging paragraphs (split by `\n\n`) into chunks without exceeding **CHUNK_HARD_MAX**; if a single paragraph exceeds max, hard-slice it into segments ≤ **CHUNK_HARD_MAX**.
3. Aim for chunks between **CHUNK_SOFT_MIN** and **CHUNK_SOFT_TARGET** when possible by combining consecutive paragraphs without crossing **CHUNK_HARD_MAX**.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement chunkText</name>
  <files>src/lib/ai/chunkText.ts</files>
  <read_first>.planning/phases/02-ai-question-parsing/02-CONTEXT.md</read_first>
  <action>
Implement `chunkText` per `interfaces` block. **Do not** mutate the input string. Export constants. Add a brief file comment documenting sequential-only consumption (D-10).
  </action>
  <verify>npx tsc --noEmit && npm run build</verify>
  <acceptance_criteria>
    - `src/lib/ai/chunkText.ts` exports `chunkText` and `CHUNK_HARD_MAX` with value `1200`
    - `chunkText("a\\n\\n" + "b".repeat(2000))` returns more than one chunk (spot-test in devtools or unit comment — executor documents in SUMMARY)
  </acceptance_criteria>
  <done>Chunker ready for parse runner</done>
</task>

</tasks>

<threat_model>
## Trust boundaries
| Boundary | Description |
|----------|-------------|
| Text → chunks | Derived data only; no external I/O |

## STRIDE
| ID | Cat | Disp | Mitigation |
|----|-----|------|------------|
| T-02b-1 | E | accept | Pure function; no secrets |
</threat_model>

<verification>
- [ ] `npm run build`
</verification>

<success_criteria>
- Chunking honors CONTEXT D-08–D-09
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-question-parsing/02-02-SUMMARY.md`
</output>
