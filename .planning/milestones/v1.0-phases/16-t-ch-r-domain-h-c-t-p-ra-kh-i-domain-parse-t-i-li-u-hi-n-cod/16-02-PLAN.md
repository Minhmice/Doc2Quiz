---
phase: 16-learning-parse-domain
plan: 02
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - src/lib/learning/index.ts
  - src/lib/learning/mappingQuality.ts
  - src/components/review/ReviewSection.tsx
  - src/components/review/MappingQualityBadge.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Review UI components import mapping-quality helpers from `@/lib/learning` instead of deep `@/lib/ai/mappingQuality`."
    - "`src/lib/learning/index.ts` exports a stable barrel for learning-facing consumers (at minimum mapping-quality re-exports)."
    - "`AiParseSection.tsx` and other parse orchestrators remain unchanged in this plan (no rewrite; parse code may keep importing `@/lib/ai/mappingQuality` directly)."
    - "`npm run lint` and `npm run build` succeed after refactors."
  artifacts:
    - path: src/lib/learning/index.ts
      provides: "Public barrel for learning domain imports"
    - path: src/lib/learning/mappingQuality.ts
      provides: "Thin re-export facade over `@/lib/ai/mappingQuality`"
  key_links:
    - from: src/components/review/ReviewSection.tsx
      to: src/lib/learning/mappingQuality.ts
      via: "import path updated to `@/lib/learning`"
      pattern: "@/lib/learning"
---

<objective>
**Wave 2 — incremental code alignment:** Introduce `src/lib/learning/` as the **stable import surface** for learning/review UI, starting with **mapping quality** helpers that are logically "question provenance for review" but today live under `src/lib/ai/`. Use **thin re-exports** (no behavior change) so `16-01` doc rules can point to real paths.

**Purpose:** Reduce tight coupling from `src/components/review/*` → `@/lib/ai/*` without moving core implementation files yet (avoids risky parse-graph refactors).

**Constraints:** Do **not** rewrite `AiParseSection.tsx`. At most new small files + import path updates listed in `files_modified`.

**Depends on:** Plan `16-01` (architecture doc must exist first so cross-links in code comments optional but doc is the contract).
</objective>

<context>
@docs/ARCHITECTURE-domain-boundaries.md (from plan `16-01` — dependency direction + forbidden imports)
@src/lib/ai/mappingQuality.ts (implementation to re-export — read exports)
@src/components/review/ReviewSection.tsx
@src/components/review/MappingQualityBadge.tsx

<interfaces>
From `src/lib/ai/mappingQuality.ts` (executor: mirror **all** named exports through the facade or use `export * from "@/lib/ai/mappingQuality"` in `src/lib/learning/mappingQuality.ts`):

```typescript
export const UNCERTAIN_MAPPING_CONFIDENCE_THRESHOLD = 0.45;
export function isUnresolvedMapping(q: Question): boolean;
export function isBlanketSinglePageVision(q: Question): boolean;
export function isUncertainPageMapping(q: Question): boolean;
export function countUncertainMappings(questions: Question[]): number;
export type MappingQualityTier = "mapped" | "uncertain" | "unresolved";
export function getMappingQualityTier(q: Question): MappingQualityTier;
export function buildMappingQualityTooltip(q: Question): string;
```
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation | Blockers |
|--------|----------|------------|----------|
| Barrel re-export accidentally pulls parse runners into learning bundle | Medium | `mappingQuality.ts` must **only** re-export `@/lib/ai/mappingQuality` (no side-effect imports of `parseChunk`, vision, OCR). Review `mappingQuality.ts` diff before merge. | None |
| Circular dependency (`lib/ai` imports `lib/learning`) | High | **Do not** add any imports from `@/lib/learning` under `src/lib/ai/**`. Facade is one-way: `learning` → `ai`. | None if rule followed |
| Behavior drift between duplicate implementations | Low | Use re-export (`export * from "…"`) or explicit re-exports — **no** copied logic in `16-02`. | None |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add `src/lib/learning` facade + switch review imports</name>
  <files>src/lib/learning/index.ts, src/lib/learning/mappingQuality.ts, src/components/review/ReviewSection.tsx, src/components/review/MappingQualityBadge.tsx</files>
  <read_first>
  - `docs/ARCHITECTURE-domain-boundaries.md` (plan `16-01` output)
  - `src/lib/ai/mappingQuality.ts` (full file — confirm no dynamic imports)
  - `src/components/review/ReviewSection.tsx` and `MappingQualityBadge.tsx` (current import lines)
  </read_first>
  <action>
  1. Create `src/lib/learning/mappingQuality.ts` containing **only** re-exports from `@/lib/ai/mappingQuality` (preferred: single line `export * from "@/lib/ai/mappingQuality";` — if ESLint forbids barrel star, list each export explicitly matching `mappingQuality.ts` public API).

  2. Create `src/lib/learning/index.ts` exporting at minimum:
     - `export * from "./mappingQuality";`
     (Add a one-line file comment: "Learning-facing stable imports — see docs/ARCHITECTURE-domain-boundaries.md".)

  3. Update `src/components/review/ReviewSection.tsx` and `src/components/review/MappingQualityBadge.tsx` to import from `@/lib/learning` (or `@/lib/learning/mappingQuality` if barrel avoided — **prefer** `@/lib/learning` for consistency with doc).

  4. **Do not** edit `src/components/ai/AiParseSection.tsx`.

  5. Optional: If `src/lib/ai/mappingQuality.ts` has no other consumers under `src/components/review`, leave `AiParseSection` imports as `@/lib/ai/mappingQuality` (parse domain stays explicit there).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
  - `rg "@/lib/ai/mappingQuality" src/components/review` returns **no matches**.
  - `rg "@/lib/learning" src/components/review` returns ≥ 2 matches (both files updated).
  - `src/lib/learning/index.ts` and `src/lib/learning/mappingQuality.ts` exist.
  - `rg "parseChunk|runVisionSequential|runOcrSequential" src/lib/learning` returns **no matches**.
  </acceptance_criteria>
  <done>Review UI uses learning barrel; lint passes; no `AiParseSection` diff.</done>
</task>

<task type="auto">
  <name>Task 2: Production build verification</name>
  <files>(none new — verification only; fix any issues only in files from Task 1 if build fails)</files>
  <read_first>
  - Task 1 modified files if `npm run build` reports errors
  </read_first>
  <action>
  Run `npm run build` from repository root. If TypeScript reports path or export errors from the new `learning` module, fix **only** within `src/lib/learning/**` or the two review components — do not expand scope to `AiParseSection` or unrelated modules.

  Re-run `npm run lint` after any fix.
  </action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
  - `npm run lint` exit code 0.
  - `npm run build` exit code 0.
  </acceptance_criteria>
  <done>CI-local parity: lint + production build green.</done>
</task>

</tasks>

<verification>
- `npm run lint` and `npm run build` both exit 0.
- Learning-oriented review components no longer reference `@/lib/ai/mappingQuality` directly.
</verification>

<success_criteria>
- Stable `@/lib/learning` entrypoint exists and is used by review mapping UI.
- Parse orchestration code path unchanged for `AiParseSection`.
- Builds remain clean — no new ESLint suppressions unless unavoidable (avoid if possible).
</success_criteria>

<output>
After completion, create `.planning/phases/16-t-ch-r-domain-h-c-t-p-ra-kh-i-domain-parse-t-i-li-u-hi-n-cod/16-02-SUMMARY.md` using the standard summary template.
</output>
