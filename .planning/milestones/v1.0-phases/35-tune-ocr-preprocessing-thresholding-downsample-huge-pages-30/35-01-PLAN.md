---
phase: 35-tune-ocr-preprocessing-thresholding-downsample-huge-pages-30
plan: 01
type: execute
wave: 1
depends_on:
  - "34-03"
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - src/lib/pdf/ocrRasterPreprocess.ts
autonomous: true
requirements:
  - "OCR-35-01"
  - "OCR-35-04"
  - "OCR-35-08"

must_haves:
  truths:
    - "~300 DPI target and megapixel cap are documented constants in one module."
    - "Worker-first posture matches Phase 28; fallback path documented."
  artifacts:
    - path: "src/lib/pdf/ocrRasterPreprocess.ts"
      provides: "constants + pure helpers for scale/downsample decision (no integration yet)"
---

<objective>
Wave 1 — **IDs + core module**: add `OCR-35-*` traceability in ROADMAP, confirm REQUIREMENTS rows, introduce **`ocrRasterPreprocess.ts`** (or equivalent) with **DPI/megapixel constants**, pure **scale factor** helpers, and stubs for threshold/downsample that Wave 2 wires into the raster pipeline.

Purpose: executable contracts before touching hot paths.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-tune-ocr-preprocessing-thresholding-downsample-huge-pages-30/35-CONTEXT.md
@src/lib/pdf/renderPagesToImages.ts
</context>

<threat_model>
CPU DoS: cap work via megapixel ceiling; no unbounded canvas resize.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: ROADMAP + REQUIREMENTS reconcile</name>
  <files>.planning/ROADMAP.md</files>
  <action>Phase 35 plan rows: three unchecked `35-0x-PLAN.md` bullets; goal aligns with `35-CONTEXT.md`.</action>
  <verify><automated>npm run lint</automated></verify>
</task>

<task type="auto">
  <name>Task 2: `ocrRasterPreprocess.ts` skeleton</name>
  <files>src/lib/pdf/ocrRasterPreprocess.ts</files>
  <action>Export named constants (DPI target, max megapixels), `computeScaleForPage(...)`, `shouldDownsample(width, height)`, placeholder `applyOptionalThreshold` (no-op or minimal) behind a flag for Wave 2.</action>
  <verify><automated>npm run lint</automated></verify>
</task>

</tasks>

<verification>
npm run lint
</verification>

<success_criteria>
OCR-35-01 and OCR-35-04 satisfied at module level; OCR-35-08 reconcile done.
</success_criteria>

<output>
After completion, note readiness for `35-02-PLAN.md` integration wave.
</output>
