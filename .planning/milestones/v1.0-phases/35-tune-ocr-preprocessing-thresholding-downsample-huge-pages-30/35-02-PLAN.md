---
phase: 35-tune-ocr-preprocessing-thresholding-downsample-huge-pages-30
plan: 02
type: execute
wave: 2
depends_on:
  - "35-01"
files_modified:
  - src/lib/pdf/renderPagesToImages.ts
  - src/lib/ai/runOcrSequential.ts
  - src/lib/pdf/ocrRasterPreprocess.ts
autonomous: true
requirements:
  - "OCR-35-02"
  - "OCR-35-03"
  - "OCR-35-06"
  - "OCR-35-07"

must_haves:
  truths:
    - "Downsample runs before JPEG encode when over cap."
    - "Thresholding only when bitmap heuristic says so (Phase 29 classification when available)."
---

<objective>
Wave 2 — **Integrate** preprocessing into **vision/OCR raster** paths: call **`ocrRasterPreprocess`** from the Worker JPEG pipeline (Phase 28) so **OCR-35-02/03/06/07** hold; single code path for resize/threshold decisions.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/35-tune-ocr-preprocessing-thresholding-downsample-huge-pages-30/35-CONTEXT.md
@src/lib/pdf/ocrRasterPreprocess.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Worker + main fallback</name>
  <files>src/lib/pdf/renderPagesToImages.ts</files>
  <action>Invoke preprocess helpers where ImageData/canvas is produced; pass page classification flag when known.</action>
  <verify><automated>npm run lint</automated></verify>
</task>

<task type="auto">
  <name>Task 2: OCR sequential entrypoint</name>
  <files>src/lib/ai/runOcrSequential.ts</files>
  <action>Ensure same helper used for OCR-bound images (no duplicate constants).</action>
  <verify><automated>npm run lint</automated></verify>
</task>

</tasks>

<verification>npm run lint</verification>

<output>Prepare `35-03` for logging polish + build.</output>
