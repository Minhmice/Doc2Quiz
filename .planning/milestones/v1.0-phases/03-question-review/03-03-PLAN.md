---
phase: 03-question-review
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02-PLAN.md
files_modified:
  - src/lib/review/draftQuestions.ts
  - src/components/review/ReviewSection.tsx
  - src/components/ai/AiParseSection.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - REVIEW-01
  - REVIEW-02
  - REVIEW-03
  - REVIEW-04
user_setup: []

must_haves:
  truths:
    - "Review section sits below AiParseSection on home page"
    - "Approve replaces full approved bank; empty list cannot save"
    - "Exact error and tooltip strings from CONTEXT are visible in UI"
  artifacts:
    - "src/components/review/ReviewSection.tsx exists"
  key_links:
    - "onDraftPersisted refreshes review list after parse writes draft LS"
---

<objective>
Add **`ReviewSection`** (section chrome, summary line, Approve, success state, disabled **Start practice**), wire **`page.tsx`**, and **`onDraftPersisted`** on **`AiParseSection`** so the review list reloads after a successful parse writes **`LS_DRAFT_QUESTIONS`**. Persist working list back to draft LS on Save/Delete so refresh preserves edits. Per `03-CONTEXT.md` D-01, D-09, D-11–D-15, D-18 and `03-UI-SPEC.md`.

Purpose: Complete Phase 3 user flow on one page.
Output: `src/components/review/ReviewSection.tsx`, edits to `AiParseSection.tsx`, `page.tsx`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-question-review/03-CONTEXT.md
@.planning/phases/03-question-review/03-UI-SPEC.md
@src/components/ai/AiParseSection.tsx
@src/lib/review/approvedBank.ts
@src/lib/review/validateMcq.ts
@src/types/question.ts
</context>

<interfaces>
```typescript
// AiParseSection — add optional prop (draft loader lives in src/lib/review/draftQuestions.ts):
onDraftPersisted?: () => void;

// ReviewSection props:
type ReviewSectionProps = {
  draftReloadKey?: number;
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Shared draft loader + AiParseSection</name>
  <files>src/lib/review/draftQuestions.ts</files>
  <files>src/components/ai/AiParseSection.tsx</files>
  <read_first>src/components/ai/AiParseSection.tsx</read_first>
  <read_first>src/lib/ai/validateQuestions.ts</read_first>
  <action>
Add **`src/lib/review/draftQuestions.ts`** exporting **`loadDraftQuestions(): Question[]`** (SSR-safe) with **identical** behavior to the current `loadDraftQuestions` function inside `AiParseSection` (read **`LS_DRAFT_QUESTIONS`**, parse, **`validateQuestionsFromJson({ questions: data.questions }, { preserveIds: true })`**). Export **`persistDraftQuestions(questions: Question[]): void`** that writes **`JSON.stringify({ savedAt: new Date().toISOString(), questions })`** to **`LS_DRAFT_QUESTIONS`** inside try/catch (ignore failures, same as parse persist). In **`AiParseSection`**: remove the local `loadDraftQuestions` function; import **`loadDraftQuestions`** from **`@/lib/review/draftQuestions`**. Extend **`AiParseSectionProps`** with optional **`onDraftPersisted?: () => void`**. After successful **`localStorage.setItem(LS_DRAFT_QUESTIONS, ...)`** in the parse handler, call **`onDraftPersisted?.()`** once.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/lib/review/draftQuestions.ts` exists and exports `loadDraftQuestions` and `persistDraftQuestions`
    - `AiParseSectionProps` includes `onDraftPersisted`
    - `grep -n "onDraftPersisted" src/components/ai/AiParseSection.tsx` shows invocation after draft `setItem`
  </acceptance_criteria>
  <done>Draft module + callback</done>
</task>

<task type="auto">
  <name>Task 2: ReviewSection state and persistence</name>
  <files>src/components/review/ReviewSection.tsx</files>
  <read_first>src/components/review/ReviewList.tsx</read_first>
  <read_first>src/lib/review/draftQuestions.ts</read_first>
  <read_first>src/lib/review/approvedBank.ts</read_first>
  <read_first>src/lib/review/validateMcq.ts</read_first>
  <action>
Create client **`ReviewSection`**. Props: **`draftReloadKey?: number`**. **State:** `questions`, `editingId`, `approved`, optional `bank` from **`loadApprovedBank()`** on mount, approve error message string or null, **`initialTotalRef`** (`useRef`) updated whenever **`draftReloadKey`** changes and **`loadDraftQuestions()`** returns non-empty — set ref to **`loaded.length`**. On mount and when **`draftReloadKey`** changes, **`setQuestions(loadDraftQuestions())`**. After **Save** / **Delete**, call **`persistDraftQuestions`** with the new array. **Single editor:** `onEditRequest(id)`: if `editingId !== null && editingId !== id`, set **`editingId`** to **`null`** (cancel other without save). Then set **`editingId`** to **`id`**. **Summary:** **`{questions.length} questions ready — {Math.max(0, initialTotalRef.current - questions.length)} removed`** (if ref is 0, show **0** removed). **Approve:** disabled if `questions.length === 0`. If **`!allMcqsComplete(questions)`**, show exact **`Some questions are incomplete. Please fix before saving.`** in an **`aria-live="polite"`** region. Else if **`saveApprovedBank(questions)`** returns **`false`**, show the **same** message. On **`true`**: `setApproved(true)`, `setBank(loadApprovedBank())`, clear error; success copy must include exact **`Practice mode will be available in the next step.`** **Start practice:** disabled button with **`title="Practice will be unlocked in Phase 4"`** (exact). Section **`aria-labelledby`**, h2 **Review & save**. Render **`ReviewList`**. Empty state when no questions.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/components/review/ReviewSection.tsx` exports `ReviewSection`
    - File contains exact substring `Some questions are incomplete. Please fix before saving.`
    - File contains exact substring `Practice mode will be available in the next step.`
    - File contains `title="Practice will be unlocked in Phase 4"`
    - File imports `saveApprovedBank` from `@/lib/review/approvedBank`
  </acceptance_criteria>
  <done>Review section complete</done>
</task>

<task type="auto">
  <name>Task 3: Home page compose</name>
  <files>src/app/page.tsx</files>
  <read_first>src/app/page.tsx</read_first>
  <read_first>src/components/review/ReviewSection.tsx</read_first>
  <action>
In **`page.tsx`**: add **`const [draftReloadKey, setDraftReloadKey] = useState(0)`**. Pass **`onDraftPersisted={() => setDraftReloadKey((k) => k + 1)}`** into **`AiParseSection`**. Import and render **`<ReviewSection draftReloadKey={draftReloadKey} />`** **immediately after** **`AiParseSection`**. Keep **`"use client"`** and existing upload/viewer flow unchanged.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/app/page.tsx` imports `ReviewSection` from `@/components/review/ReviewSection`
    - `ReviewSection` appears after `AiParseSection` in JSX
    - `AiParseSection` receives prop `onDraftPersisted`
  </acceptance_criteria>
  <done>Page integrated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage | Draft + approved data; same as Phase 2 |

## STRIDE

| ID | Cat | Component | Disp | Mitigation |
|----|-----|-----------|------|------------|
| T-03c-1 | I | XSS | mitigate | React escaping; no unsafe HTML from questions |
</threat_model>

<verification>
- [ ] `npx tsc --noEmit`
- [ ] `npm run lint`
- [ ] `npm run build`
</verification>

<success_criteria>
- End-to-end flow matches CONTEXT user flow §VI and UI-SPEC
- REVIEW-01–04 behaviors testable manually on home page
</success_criteria>

<output>
After completion, create `.planning/phases/03-question-review/03-03-SUMMARY.md`
</output>
