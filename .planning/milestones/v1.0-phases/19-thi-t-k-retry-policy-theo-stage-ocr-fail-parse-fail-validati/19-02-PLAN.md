---
phase: 19-thi-t-k-retry-policy-theo-stage-ocr-fail-parse-fail-validati
plan: 02
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - src/lib/ai/pipelineStageRetry.ts
  - src/lib/ai/runOcrSequential.ts
  - src/lib/ai/parseChunk.ts
  - src/lib/db/studySetDb.ts
  - src/components/settings/AiProviderForm.tsx
  - src/lib/validations/aiSettings.ts
  - src/components/ai/AiParseSection.tsx
  - src/lib/ai/estimateParseRun.ts
  - src/components/ai/AiParseEstimatePanel.tsx
  - .planning/codebase/WORKFLOW-OCR-AI-QUIZ.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Settings UI shows exactly three inputs (no OpenAI/Anthropic/Custom tabs); parse UI uses `readForwardSettings` + `parseCapabilities` for early disable + copy."
    - "Stage-specific retry helpers exist and at least OCR + LLM parse + IDB paths consult them (validation/mapping policies documented inline if no code change)."
    - "`parseChunk` / `AiParseSection` no longer branch on `anthropic` for user-driven parse (server route may retain compat until follow-up if required)."
  artifacts:
    - path: src/lib/ai/pipelineStageRetry.ts
      provides: "Per-stage retry policy constants + predicates"
  key_links:
    - from: src/components/ai/AiParseSection.tsx
      to: src/lib/ai/parseCapabilities.ts
      via: "visionDisabled / canRun*"
      pattern: "early gate"
---

<objective>
Deliver **D-03, D-05, D-06** and finish **D-04** UX: wire `forwardSettings` + `parseCapabilities` through **settings + parse + estimate**; implement **`pipelineStageRetry`** and integrate **OCR**, **parse**, and **IDB persistence** retry behavior per table; trim Anthropic/GPT/Custom UX; update workflow doc.

Depends on **`19-01`** artifacts.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-thi-t-k-retry-policy-theo-stage-ocr-fail-parse-fail-validati/19-CONTEXT.md
@.planning/phases/19-thi-t-k-retry-policy-theo-stage-ocr-fail-parse-fail-validati/19-01-PLAN.md
@src/components/settings/AiProviderForm.tsx
@src/components/ai/AiParseSection.tsx
@src/lib/ai/runOcrSequential.ts
@src/lib/ai/parseChunk.ts
@src/lib/db/studySetDb.ts
</context>

## UI (embedded spec — no separate UI-SPEC file)

- **Settings:** Single card: labels **API base URL** (placeholder e.g. `https://api.openai.com/v1/chat/completions` or “OpenAI-compatible chat URL”), **API key**, **Model id**. Buttons: Save / Clear / Test text / Test vision (reuse `testConnection.ts` — update to accept `ForwardClientSettings`). Remove provider segmented control and Anthropic-specific hints from visible copy.

- **Parse tab:** If `vision_multimodal` or `vision_attach` **blocked**, disable vision controls and show inline text from `reasonKey` (map to English sentence in component or small `parseCapabilityMessages.ts`).

- **Accessibility:** Disabled controls keep `title` or visible explanation; not color-only.

<tasks>

<task type="auto">
  <name>Task 1: `pipelineStageRetry` policy table</name>
  <files>src/lib/ai/pipelineStageRetry.ts</files>
  <read_first>.planning/phases/19-thi-t-k-retry-policy-theo-stage-ocr-fail-parse-fail-validati/19-CONTEXT.md</read_first>
  <action>
New module exporting:

- `PipelineRetryStage` enum or union: `ocr_page` | `llm_chunk` | `llm_vision` | `json_validate` | `idb_put` (adjust names to match codebase).

- Per stage: `{ maxAttempts, delaysMs: number[], shouldRetry(err: unknown): boolean }` — concrete rules in comments referencing D-05:

  - **OCR:** retry on HTTP-like / network / 429 from adapter layer if error type detectable; max 2; backoff 300–800ms; log `OCR` domain.

  - **LLM chunk/vision:** retry once on empty assistant text or JSON parse failure **if** request idempotent (same chunk); do not retry `FatalParseError` user-facing blocks.

  - **json_validate:** `maxAttempts: 1` (document only) or hook for future re-prompt.

  - **idb_put:** retry 2–3 on `QuotaExceededError` or transient IDB errors with small backoff; then surface toast (existing patterns).

Export small helper `withRetries<T>(stage, fn)` or explicit loops — avoid pulling heavy deps.

**Mapping stage:** Document in module README comment: “no automatic retry; align Phase 14 — log + toast” (`MAPPING`).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - File exports stage config consumed by at least one caller in Task 2 or 3.
  </acceptance_criteria>
  <done>Retry policy module exists.</done>
</task>

<task type="auto">
  <name>Task 2: Wire OCR + parse retries</name>
  <files>src/lib/ai/runOcrSequential.ts,src/lib/ai/parseChunk.ts</files>
  <read_first>src/lib/ai/ocrAdapter.ts,src/lib/ai/pipelineStageRetry.ts</read_first>
  <action>
- **`runOcrSequential`:** Wrap `runOcrPage` (or the inner await) with `ocr_page` retry policy when failure matches `shouldRetry`. Preserve per-page `failed` row if exhausted. Do not retry on abort.

- **`parseChunk.ts`:** For OpenAI-compatible forward path only, apply `llm_chunk` retry around the network + parse of assistant JSON **where safe** (single-chunk parse). Remove or dead-code **Anthropic** client branch from **browser-invoked** parse paths if D-07 already collapsed types — if removal breaks server route, gate Anthropic behind server-only flag **or** leave server adapter but ensure UI cannot select it (prefer delete unused code).

- Ensure `pipelineLog` / `reportPipelineError` receive stage strings consistent with Phase 13 patterns.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "withRetries|pipelineStageRetry|ocr_page" src/lib/ai/runOcrSequential.ts` or equivalent loop visible.
  </acceptance_criteria>
  <done>OCR/parse retry integrated.</done>
</task>

<task type="auto">
  <name>Task 3: IDB draft persistence retries</name>
  <files>src/lib/db/studySetDb.ts</files>
  <read_first>src/lib/ai/pipelineStageRetry.ts</read_first>
  <action>
Identify hot paths: `putDraftQuestions`, `putMediaBlob`, `putOcrResult` (pick **at least** `putDraftQuestions`).

Wrap write in `idb_put` retry helper — only for transient errors; on exhaustion rethrow or return boolean so caller can toast (match existing `AiParseSection` / review error UX).

Keep transactions / versioning semantics unchanged.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "idb_put|putDraftQuestions" src/lib/db/studySetDb.ts` shows retry wrapper or helper use.
  </acceptance_criteria>
  <done>IDB retry path exists.</done>
</task>

<task type="auto">
  <name>Task 4: Settings + validations — three fields only</name>
  <files>src/components/settings/AiProviderForm.tsx,src/lib/validations/aiSettings.ts,src/lib/ai/testConnection.ts</files>
  <read_first>src/lib/ai/forwardSettings.ts,src/lib/ai/parseCapabilities.ts</read_first>
  <action>
- Replace zod schema with `{ baseUrl, apiKey, modelId }` (names aligned to `ForwardClientSettings`).

- Form: remove provider tabs / `selectProvider`; load/save via `readForwardSettings` / `writeForwardSettings`.

- Tests connection helpers: pass forward settings; default URL behavior documented.

- Remove visible “Anthropic”, “Custom”, “GPT” branding strings from this component (generic copy).

- Call `migrateForwardSettingsFromLegacy` on mount if not already done.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "anthropic|Anthropic|selectProvider" src/components/settings/AiProviderForm.tsx` returns 0 matches (except maybe comment `removed` — prefer zero).
  </acceptance_criteria>
  <done>Settings UI forward-only.</done>
</task>

<task type="auto">
  <name>Task 5: `AiParseSection` + estimate panel + capability gating</name>
  <files>src/components/ai/AiParseSection.tsx,src/lib/ai/estimateParseRun.ts,src/components/ai/AiParseEstimatePanel.tsx</files>
  <read_first>src/lib/ai/parseCapabilities.ts,src/lib/ai/forwardSettings.ts</read_first>
  <action>
- Replace `getProvider()` / `getKeyForProvider(provider)` pattern with forward settings reads.

- Compute `visionDisabled` / chunk availability from **`getSurfaceAvailability`** instead of `provider === "anthropic"`.

- Remove `forwardProvider === "openai" | "custom"` branching where collapsible to **always** openai-compatible forward payload (custom URL still user-provided in base URL field).

- Update `estimateParseRun` inputs if signature still references `AiProvider` — use forward settings or `void` provider.

- **`AiParseEstimatePanel`:** adjust copy if it referenced multiple vendors.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "getProvider\\(|AiProvider" src/components/ai/AiParseSection.tsx` — should trend to zero provider branching; grep may still hit imports until cleaned; goal: **no anthropic-specific vision disable**.
  </acceptance_criteria>
  <done>Parse UI uses matrix + forward settings.</done>
</task>

<task type="auto">
  <name>Task 6: Workflow doc + wave 2 build</name>
  <files>.planning/codebase/WORKFLOW-OCR-AI-QUIZ.md</files>
  <read_first>docs/BYOK-forward-only.md,.planning/codebase/WORKFLOW-OCR-AI-QUIZ.md</read_first>
  <action>
Update **WORKFLOW** doc: BYOK is **3-field forward**; remove or rewrite paragraphs that say users pick OpenAI vs Anthropic vs Custom; add bullet pointing to `parseCapabilities` + `pipelineStageRetry`.

Run **`npm run build`**.

Write **`19-02-SUMMARY.md`**.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Wave 2 complete.</done>
</task>

</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Infinite retry loops | `maxAttempts` hard caps; honor `AbortSignal` |
| Retry doubles token spend | LLM retry max 1; log when retry fires |
| UI regression on settings | Manual smoke: clear, save, test buttons |
</threat_model>

<verification>
- Per-task `npm run lint` where feasible; final `npm run build`
</verification>

<nyquist_validation>
| Requirement | Evidence in plan |
|-------------|------------------|
| R1 Retries bounded | Task 1 `maxAttempts`; threat_model |
| R2 UI early gate | Embedded UI spec + Task 5 `parseCapabilities` |
| R3 IDB semantics preserved | Task 3 “transactions unchanged” |
</nyquist_validation>

## PLANNING COMPLETE
