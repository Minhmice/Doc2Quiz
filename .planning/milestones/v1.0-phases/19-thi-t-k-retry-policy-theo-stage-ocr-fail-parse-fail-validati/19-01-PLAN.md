---
phase: 19-thi-t-k-retry-policy-theo-stage-ocr-fail-parse-fail-validati
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/question.ts
  - src/lib/ai/storage.ts
  - src/lib/ai/forwardSettings.ts
  - src/lib/ai/parseCapabilities.ts
  - docs/BYOK-forward-only.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Exactly three persisted BYOK fields (base URL, API key, model id) for the forward client; legacy multi-provider keys migrated once with idempotent flag."
    - "`parseCapabilities` (or equivalent) exposes a declarative matrix / query API used by later wave for gating."
    - "`AiProvider` / `getProvider` either removed or reduced to a single forward-facing path without Anthropic/Custom/OpenAI tabs in types."
  artifacts:
    - path: src/lib/ai/forwardSettings.ts
      provides: "Read/write/migrate minimal BYOK"
    - path: src/lib/ai/parseCapabilities.ts
      provides: "Surface × availability for UI + parse"
    - path: docs/BYOK-forward-only.md
      provides: "User + dev doc for 3-field BYOK + matrix intent"
  key_links:
    - from: src/lib/ai/storage.ts
      to: src/lib/ai/forwardSettings.ts
      via: "migration reads legacy LS_*"
      pattern: "one-time migrateForwardSettingsFromLegacy"
---

<objective>
Deliver **D-01, D-02, D-04 (declarative v1), D-07, D-08** from `19-CONTEXT.md`: forward-only settings module, legacy migration, capability matrix **pure functions + doc** — **no** full `AiParseSection` / settings UI swap yet (that is `19-02`).

**Output:** New modules + type updates + short doc; keep build green after each task cluster.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-thi-t-k-retry-policy-theo-stage-ocr-fail-parse-fail-validati/19-CONTEXT.md
@src/lib/ai/storage.ts
@src/types/question.ts
@src/lib/validations/aiSettings.ts
</context>

## Decision coverage (19-CONTEXT)

| ID | Task(s) |
|----|-----------|
| D-01, D-02 | 1–2 |
| D-04 v1 | 3 |
| D-07 | 1–2 |
| D-08 | 5 |

<tasks>

<task type="auto">
  <name>Task 1: `ForwardClientSettings` + localStorage keys</name>
  <files>src/lib/ai/forwardSettings.ts,src/types/question.ts</files>
  <read_first>src/types/question.ts,src/lib/ai/storage.ts,.planning/phases/19-thi-t-k-retry-policy-theo-stage-ocr-fail-parse-fail-validati/19-CONTEXT.md</read_first>
  <action>
**New file `src/lib/ai/forwardSettings.ts`:**

- Define stable LS key constants (e.g. `doc2quiz:ai:forwardBaseUrl`, `doc2quiz:ai:forwardApiKey`, `doc2quiz:ai:forwardModelId`) and a migration sentinel e.g. `doc2quiz:ai:forwardMigratedV1`.

- Export `type ForwardClientSettings = { baseUrl: string; apiKey: string; modelId: string }`.

- Export `readForwardSettings(): ForwardClientSettings`, `writeForwardSettings(partial)`, `migrateForwardSettingsFromLegacy(): void` (sync, browser-only).

**Migration policy (document in file comment):** If `forwardMigratedV1` not set: prefer **custom** URL+key+model when `LS_CUSTOM_URL` non-empty; else **openai** key/url/model; else **anthropic** key/url/model as last resort (still migrated into forward triple — Anthropic URL default from existing constants in `parseChunk.ts` if needed). Then set migration flag. Never write API keys to console.

**`types/question.ts`:** Add forward LS exports; deprecate or narrow `AiProvider` — acceptable approaches: (a) replace union with single literal `"forward"` and delete anthropic/custom from union, updating all references; or (b) keep union temporarily but mark `@deprecated` and add `/** @deprecated use forward settings */` on anthropic/custom. Planner preference: **(a)** if grep shows manageable call sites; else **(b)** with all **new** code using `forwardSettings` only.

**Do not** delete `storage.ts` per-provider functions until `19-02` rewires callers — in `19-01` you may add `forwardSettings.ts` and migration while leaving old exports for transitional compile.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "forwardMigratedV1|readForwardSettings" src/lib/ai/forwardSettings.ts` matches.
    - `migrateForwardSettingsFromLegacy` is called from a single safe entry (e.g. `readForwardSettings` first line, or explicit `ensureForwardSettingsMigrated` exported and documented).
  </acceptance_criteria>
  <done>Forward settings module exists.</done>
</task>

<task type="auto">
  <name>Task 2: Bridge `storage.ts` to forward reads (transitional)</name>
  <files>src/lib/ai/storage.ts</files>
  <read_first>src/lib/ai/forwardSettings.ts,src/lib/ai/storage.ts</read_first>
  <action>
Add **`getForwardClientForUi(): ForwardClientSettings`** (or rename) that runs migration then returns `readForwardSettings()`.

Optionally add thin wrappers `getKeyForProvider` / `getUrlForProvider` / `getModelForProvider` implementations that **delegate to forward triple** when migration flag set — **only if** needed to keep `19-01` build green before `19-02` lands; otherwise skip and let `19-02` replace call sites.

**Constraint:** No duplicate secret storage; single source of truth after migration is forward keys.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `storage.ts` imports nothing that creates circular dependency with `forwardSettings` (adjust import direction if needed).
  </acceptance_criteria>
  <done>Transitional read path defined.</done>
</task>

<task type="auto">
  <name>Task 3: Declarative `parseCapabilities` matrix</name>
  <files>src/lib/ai/parseCapabilities.ts</files>
  <read_first>src/lib/ai/parseRoutePolicy.ts,src/components/ai/AiParseSection.tsx,.planning/phases/19-thi-t-k-retry-policy-theo-stage-ocr-fail-parse-fail-validati/19-CONTEXT.md</read_first>
  <action>
**New file `src/lib/ai/parseCapabilities.ts`:**

- Define string union `ParsePipelineSurface` covering at minimum: `text_mcq`, `ocr_forward`, `layout_chunk_llm`, `vision_multimodal`, `vision_attach`, `idb_draft_persist`.

- Export `type SurfaceAvailability = { surface: ParsePipelineSurface; status: "allowed" | "blocked"; reasonKey?: string }`.

- Export `getSurfaceAvailability(args: { settings: ForwardClientSettings; attachPageImages: boolean }): SurfaceAvailability[]` **or** `isSurfaceAllowed(...)` helpers — v1 rules: all **allowed** when `apiKey` non-empty and (`baseUrl` non-empty or treat empty as default OpenAI chat URL documented in D-01); **blocked** `vision_multimodal` / `vision_attach` when key missing; optional **blocked** when `baseUrl` invalid shape (reuse or share URL trim/validation helper from `openAiEndpoint` / validations).

- Export stable `reasonKey` strings e.g. `forward.missing_api_key`, `forward.missing_base_url` for UI in `19-02`.

- Unit tests **optional** in this wave; if added, place under `src/lib/ai/__tests__/parseCapabilities.test.ts` or vitest pattern repo uses.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "ParsePipelineSurface|getSurfaceAvailability" src/lib/ai/parseCapabilities.ts` matches.
  </acceptance_criteria>
  <done>Matrix API ready for UI wiring.</done>
</task>

<task type="auto">
  <name>Task 4: Doc `docs/BYOK-forward-only.md`</name>
  <files>docs/BYOK-forward-only.md</files>
  <read_first>docs/BYOK-parse-estimate.md,.planning/codebase/WORKFLOW-OCR-AI-QUIZ.md</read_first>
  <action>
Short doc (English, concise):

- What the three fields mean and how they map to `POST /api/ai/forward`.

- Migration note: old OpenAI/Custom/Anthropic fields → forward triple once.

- Link to `parseCapabilities` surfaces and that matrix is declarative v1.

- Cross-link `BYOK-parse-estimate.md` (estimate still valid conceptually; executor updates formulas in `19-02` if keys change).
  </action>
  <verify>
    <manual>Spell-check / links resolve</manual>
  </verify>
  <acceptance_criteria>
    - File exists under `docs/` and mentions all three field names.
  </acceptance_criteria>
  <done>Doc published.</done>
</task>

<task type="auto">
  <name>Task 5: Wave 1 build</name>
  <files>src/lib/ai/forwardSettings.ts,src/lib/ai/parseCapabilities.ts,src/types/question.ts,src/lib/ai/storage.ts</files>
  <read_first>.planning/phases/19-thi-t-k-retry-policy-theo-stage-ocr-fail-parse-fail-validati/19-01-PLAN.md</read_first>
  <action>
Run **`npm run build`**. Fix only issues introduced by wave 1.

Write **`19-01-SUMMARY.md`** in phase dir with bullets + verification commands run.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Wave 1 complete.</done>
</task>

</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Lost API keys on migrate | Read-only merge; never clear legacy keys in `19-01` unless explicitly approved in `19-02` cleanup task |
| Circular imports | Keep `forwardSettings` free of React / parseChunk |
</threat_model>

<verification>
- Tasks 1–4: `npm run lint` per task or once at end of wave before build
- Task 5: `npm run build`
</verification>

<nyquist_validation>
| Requirement | Evidence in plan |
|-------------|------------------|
| R1 Forward keys single source | Task 1–2, D-01/D-02 in CONTEXT |
| R2 Matrix callable before UI | Task 3 exports for `19-02` Task 5 |
| R3 No secret logging | threat_model + CONTEXT D-06 |
</nyquist_validation>

## PLANNING COMPLETE
