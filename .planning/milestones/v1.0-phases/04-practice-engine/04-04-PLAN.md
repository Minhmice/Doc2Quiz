---
phase: 04-practice-engine
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03-PLAN.md
files_modified:
  - src/components/review/ReviewSection.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - PRAC-01
user_setup: []

must_haves:
  truths:
    - "Start practice runs only with non-empty approved bank"
    - "PracticeSection mounts below ReviewSection when session active"
  artifacts:
    - "ReviewSection accepts onBeginPractice and enables Start when bank has questions"
  key_links:
    - "04-CONTEXT D-02 D-18; replaces Phase 3 disabled stub"
---

<objective>
Wire **Start practice** in **`ReviewSection`** (bank-aware enable + **`onBeginPractice`**) and **home page** state so **`PracticeSection`** mounts **below** review with a **`sessionKey`** and **`Question[]`** snapshot from **`loadApprovedBank()`** per **`04-CONTEXT.md` D-01 D-02 D-18**.

Purpose: PRAC-01 entry and full page composition.
Output: edits to `src/components/review/ReviewSection.tsx`, `src/app/page.tsx`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-practice-engine/04-CONTEXT.md
@src/lib/review/approvedBank.ts
@src/components/review/ReviewSection.tsx
@src/app/page.tsx
@src/components/practice/PracticeSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReviewSection Start practice</name>
  <files>src/components/review/ReviewSection.tsx</files>
  <read_first>src/components/review/ReviewSection.tsx</read_first>
  <read_first>src/lib/review/approvedBank.ts</read_first>
  <action>
Extend **`ReviewSectionProps`** with optional **`onBeginPractice?: () => void`**. Derive **`canPractice = Boolean(bank?.questions?.length)`** from existing **`bank`** state (loaded on mount + after approve). Replace the **Start practice** button: **`disabled={!canPractice}`**; remove Phase 3 **`title="Practice will be unlocked in Phase 4"`**. When disabled, set **`title="Approve and save questions first."`** (exact string). When enabled, **`onClick={() => onBeginPractice?.()}`**. Style enabled button like other primary-outline (e.g. **teal** border/bg similar to **Approve** secondary — **Claude’s discretion**) so it is not grayed.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `ReviewSectionProps` includes `onBeginPractice`
    - `src/components/review/ReviewSection.tsx` contains exact substring `Approve and save questions first.`
    - File does not contain `Practice will be unlocked in Phase 4`
  </acceptance_criteria>
  <done>Review wired</done>
</task>

<task type="auto">
  <name>Task 2: Page composition</name>
  <files>src/app/page.tsx</files>
  <read_first>src/app/page.tsx</read_first>
  <read_first>src/lib/review/approvedBank.ts</read_first>
  <read_first>src/components/practice/PracticeSection.tsx</read_first>
  <action>
Import **`PracticeSection`** and **`loadApprovedBank`**. State: **`practiceActive: boolean`**, **`practiceSessionKey: number`** (start at 0), **`practiceQuestions: Question[]`** (empty array). Import type **`Question`** from **`@/types/question`**. **`handleBeginPractice`**: **`const b = loadApprovedBank()`**; if **`!b?.questions?.length`** return; **`setPracticeQuestions([...b.questions])`**, **`setPracticeSessionKey((k) => k + 1)`**, **`setPracticeActive(true)`**. Pass **`onBeginPractice={handleBeginPractice}`** to **`ReviewSection`**. After **`ReviewSection`**, render **`practiceActive ? <PracticeSection questions={practiceQuestions} sessionKey={practiceSessionKey} onClose={() => setPracticeActive(false)} /> : null`**. Keep existing **`draftReloadKey`** / **`AiParseSection`** unchanged.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/app/page.tsx` imports `PracticeSection` from `@/components/practice/PracticeSection`
    - `src/app/page.tsx` imports `loadApprovedBank` from `@/lib/review/approvedBank`
    - `ReviewSection` receives `onBeginPractice`
    - JSX renders `PracticeSection` after `ReviewSection`
  </acceptance_criteria>
  <done>Page integrated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage | Same approved bank read path as review |

## STRIDE

| ID | Cat | Component | Disp | Mitigation |
|----|-----|-----------|------|------------|
| T-04d-1 | I | stale bank | mitigate | Read `loadApprovedBank()` at Start click |
</threat_model>

<verification>
- [ ] `npx tsc --noEmit`
- [ ] `npm run lint`
- [ ] `npm run build`
</verification>

<success_criteria>
- User can start practice from approved bank without extra click for focus (handled in 04-03)
</success_criteria>

<output>
After completion, create `.planning/phases/04-practice-engine/04-04-SUMMARY.md`
</output>
