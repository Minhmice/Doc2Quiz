---
phase: 04-practice-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/practice.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Practice session types describe status, index, and per-question answers"
  artifacts:
    - "src/types/practice.ts exists"
  key_links:
    - "Aligns with 04-CONTEXT D-16 D-17"
---

<objective>
Add **`src/types/practice.ts`** with TypeScript types for in-memory practice session state per **`04-CONTEXT.md` D-16–D-17** (status, question snapshot, current index, answers by question id).

Purpose: Hook and UI components share one contract.
Output: `src/types/practice.ts` only.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-practice-engine/04-CONTEXT.md
@src/types/question.ts
</context>

<interfaces>
```typescript
export type PracticeSessionStatus = "idle" | "active" | "complete";

export type PracticeAnswerRecord = {
  choiceIndex: 0 | 1 | 2 | 3;
};

// Optional: export type for hook return shape — executor defines final names
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Practice session types</name>
  <files>src/types/practice.ts</files>
  <read_first>src/types/question.ts</read_first>
  <read_first>.planning/phases/04-practice-engine/04-CONTEXT.md</read_first>
  <action>
Create **`src/types/practice.ts`** exporting: **`PracticeSessionStatus`** = literal union **`"idle" | "active" | "complete"`**; **`PracticeAnswerRecord`** with **`choiceIndex: 0 | 1 | 2 | 3`**; a **`PracticeSessionSnapshot`** (or equivalent name) type holding **`questions: Question[]`**, **`currentIndex: number`**, **`answersByQuestionId: Record<string, PracticeAnswerRecord>`**, **`status: PracticeSessionStatus`** — import **`Question`** from **`@/types/question`**. Add brief JSDoc that **`questions`** is an **ordered snapshot** copied from the approved bank at session start.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/types/practice.ts` exports `PracticeSessionStatus` and `PracticeAnswerRecord`
    - File imports `Question` from `@/types/question`
    - File contains substring `answersByQuestionId` or `Record<string, PracticeAnswerRecord>`
  </acceptance_criteria>
  <done>Types added</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Types only | No runtime trust change |

## STRIDE

| ID | Cat | Component | Disp | Mitigation |
|----|-----|-----------|------|------------|
| T-04a-1 | — | N/A | — | Types document intent for safe UI state |
</threat_model>

<verification>
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>
- Types match CONTEXT D-17
</success_criteria>

<output>
After completion, create `.planning/phases/04-practice-engine/04-01-SUMMARY.md`
</output>
