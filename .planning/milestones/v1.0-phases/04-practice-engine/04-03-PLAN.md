---
phase: 04-practice-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02-PLAN.md
files_modified:
  - src/components/practice/QuestionMap.tsx
  - src/components/practice/PracticeQuestionView.tsx
  - src/components/practice/PracticeSection.tsx
autonomous: true
requirements:
  - PRAC-02
  - PRAC-03
  - PRAC-05
  - PRAC-06
user_setup: []

must_haves:
  truths:
    - "Keydown 1-4 and arrows work while practice section is active"
    - "Option buttons reflect correct/incorrect after answer"
  artifacts:
    - "Three files under src/components/practice/"
  key_links:
    - "Matches 04-UI-SPEC.md and 04-CONTEXT D-04 D-08 D-12 D-14"
---

<objective>
Build **`QuestionMap`**, **`PracticeQuestionView`**, and **`PracticeSection`** — render MCQ + map, **keyboard** handling, **focus** on start, **Session complete** stub per **`04-CONTEXT.md`** and **`04-UI-SPEC.md`**.

Purpose: Deliver visible practice UI and PRAC-02/03/05/06 behaviors.
Output: `src/components/practice/QuestionMap.tsx`, `PracticeQuestionView.tsx`, `PracticeSection.tsx`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-practice-engine/04-CONTEXT.md
@.planning/phases/04-practice-engine/04-UI-SPEC.md
@src/hooks/usePracticeSession.ts
@src/components/review/QuestionCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: QuestionMap</name>
  <files>src/components/practice/QuestionMap.tsx</files>
  <read_first>.planning/phases/04-practice-engine/04-UI-SPEC.md</read_first>
  <read_first>src/types/question.ts</read_first>
  <action>
Create client **`QuestionMap`** with props: **`total: number`**, **`currentIndex: number`**, **`answeredIndices: Set<number> | boolean[]`** (executor picks), **`onSelectIndex: (i: number) => void`**. Render **`total`** cells (1-based label optional). Styles per UI-SPEC: **current** = teal ring; **answered** = distinct fill; **unvisited** = muted. **`button type="button"`** per cell; **`aria-current`** on current. Call **`onSelectIndex`** on click.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/components/practice/QuestionMap.tsx` exports `QuestionMap`
    - File begins with `"use client"`
    - File contains `onSelectIndex`
  </acceptance_criteria>
  <done>Map component</done>
</task>

<task type="auto">
  <name>Task 2: PracticeQuestionView</name>
  <files>src/components/practice/PracticeQuestionView.tsx</files>
  <read_first>src/types/question.ts</read_first>
  <read_first>.planning/phases/04-practice-engine/04-UI-SPEC.md</read_first>
  <action>
Create client **`PracticeQuestionView`**. Props: **`question: Question`**, **`selectedChoice: 0|1|2|3 | null`** (user’s last pick for this question), **`showFeedback: boolean`**, **`onChoose: (i: 0|1|2|3) => void`**, optional **`correctIndex`** from question (always pass **`question.correctIndex`**). Render stem + four **clickable** options (keys 1–4 hint in label text or sublabel). When **`showFeedback`**: correct option **teal** success styling; wrong selected **red**; if wrong, correct still highlighted. When **`!showFeedback`**, neutral buttons. Call **`onChoose`** on click.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/components/practice/PracticeQuestionView.tsx` exports `PracticeQuestionView`
    - File contains `correctIndex` and `showFeedback`
  </acceptance_criteria>
  <done>Question view</done>
</task>

<task type="auto">
  <name>Task 3: PracticeSection shell</name>
  <files>src/components/practice/PracticeSection.tsx</files>
  <read_first>src/hooks/usePracticeSession.ts</read_first>
  <read_first>src/components/practice/QuestionMap.tsx</read_first>
  <read_first>src/components/practice/PracticeQuestionView.tsx</read_first>
  <action>
Create client **`PracticeSection`** with props: **`questions: Question[]`**, **`sessionKey: number`** (increment from parent each new session to force clean state), **`onClose: () => void`**. If **`questions.length === 0`**, return **`null`**. Inside: call **`usePracticeSession()`**; **`useEffect`** on **`[sessionKey, questions]`**: call **`reset()`** then **`startSession(questions)`** when **`questions.length > 0`** (parent remounts or bumps key per D-03). **Ref** **`containerRef`** on outer **`<section tabIndex={0}`** per CONTEXT D-06; **`useEffect`** when **`status === "active"`** after session start: **`queueMicrotask` or `requestAnimationFrame`** then **`containerRef.current?.focus()`** so keys work without click. **Keyboard (D-07):** **`useEffect`** add **`keydown`** listener on **`window`** while **`status === "active"`** (ignore when **`complete`**). At the **start** of the handler, if **`document.activeElement`** is an **`HTMLInputElement`**, **`HTMLTextAreaElement`**, or **`HTMLElement`** with **`isContentEditable === true`**, **return early** (do not steal keys from AI/review fields above). Otherwise: **`event.key`** **`"1"`-`"4"`** → **`answerChoice`** 0–3; **`ArrowLeft`** / **`ArrowRight`** → prev/next; **`preventDefault`** for those handled keys only. **Render:** section chrome **`h2` Practice** + short key hint; **`QuestionMap`**; **`PracticeQuestionView`** for **`currentQuestion`** with **`selectedChoice`** from answers record, **`showFeedback`** = **`isCurrentAnswered()`**; **Previous** / **Next** buttons (disabled at ends). When **`status === "complete"`**, show **Session complete** panel per UI-SPEC with at least one sentence mentioning **scoring** or **mistake** drills as a **future / next** step (Phase 5 handoff per CONTEXT D-15) + **Done** button calling **`onClose`**. **`aria-labelledby`** on section.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/components/practice/PracticeSection.tsx` exports `PracticeSection`
    - File imports `usePracticeSession` from `@/hooks/usePracticeSession`
    - File contains exact substring `Session complete`
    - File contains substring `scoring` (Phase 5 stub copy per D-15)
    - File contains `addEventListener("keydown"` or `'keydown'`
    - File contains `isContentEditable` or `contentEditable` (active-element guard per D-07)
    - Props type or destructuring includes `sessionKey`
  </acceptance_criteria>
  <done>Practice section</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM | Question text from bank — React text nodes only |

## STRIDE

| ID | Cat | Component | Disp | Mitigation |
|----|-----|-----------|------|------------|
| T-04c-1 | X | question HTML | mitigate | No `dangerouslySetInnerHTML` |
</threat_model>

<verification>
- [ ] `npx tsc --noEmit`
- [ ] `npm run lint`
</verification>

<success_criteria>
- Keyboard + map + feedback match CONTEXT and UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/04-practice-engine/04-03-SUMMARY.md`
</output>
