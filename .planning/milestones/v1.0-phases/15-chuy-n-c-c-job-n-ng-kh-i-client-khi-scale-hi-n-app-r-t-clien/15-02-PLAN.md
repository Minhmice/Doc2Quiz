---
phase: 15-server-parse-scale
plan: 02
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - src/app/api/parse-jobs/route.ts
  - src/app/api/parse-jobs/[id]/route.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "When `isServerParseQueueEnabled()` is false, **both** handlers respond with **404** and a small JSON body containing the literal substring `\"enabled\":false` (so clients can detect capability without treating as a hard 5xx)."
    - "When enabled, handlers return **501** with JSON body containing literal substring `\"not_implemented\"` (contract stub — no queue worker yet)."
    - "No PDF upload body is parsed in 15-02 — POST may read empty JSON `{}` or reject non-JSON with 400; do not add multipart PDF handling in this plan."
  artifacts:
    - path: src/app/api/parse-jobs/route.ts
      provides: POST `createParseJobStub` surface
    - path: src/app/api/parse-jobs/[id]/route.ts
      provides: GET job stub by id
  key_links:
    - from: route.ts files
      to: src/lib/serverParse/env.ts
      via: "`import { isServerParseQueueEnabled } from \"@/lib/serverParse/env\"`"
    - from: route.ts files
      to: src/types/parseJob.ts
      via: "typed JSON responses"
---

<objective>
**Wave 2 — HTTP contract stubs:** Add Next.js App Router endpoints under `src/app/api/parse-jobs/` so deploy/env can be wired **before** a real worker exists; responses are explicitly **501** when the feature flag is on and **404** when off.

**Output:** `route.ts` + `[id]/route.ts` only (no SSE file in this plan).
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Unauthenticated job spam when flag on | High | Document in code comment: production must add auth middleware / rate limit before enabling flag; 15-02 does not expose secrets. |
| Parsing huge POST bodies | Medium | Reject body &gt; 1MB with 413 or ignore body — specify in task. |

**Blockers:** Wave **15-01** must be merged first.
</threat_model>

## Task 1 — `POST /api/parse-jobs`

<action>
Create `src/app/api/parse-jobs/route.ts` with a **file-top JSDoc block** (first statement in file after imports) containing this exact sentence on its own line for grep verification:

`Production: add auth + rate limiting before enabling D2Q_SERVER_PARSE_ENABLED.`

Then export:

- `export async function POST(request: Request): Promise<Response>`
- If `!isServerParseQueueEnabled()`: return `NextResponse.json({ enabled: false }, { status: 404 })`.
- If enabled: read `request.json()` inside try/catch — invalid JSON → `400` with `{ "error": "invalid_json" }`. Valid JSON (including `{}`) → return `501` with body **`{ "error": "not_implemented", "enabled": true, "message": "Parse job queue stub — worker not wired yet." }`** (exact `not_implemented` substring).
- If `Content-Length` header present and numeric value &gt; **1048576** (1 MiB), return **413** without reading the full body (check header only).

Also export `export async function GET(): Promise<Response>` returning **404** + `{ "enabled": false }` when disabled; when enabled return **200** JSON **`{ "enabled": true, "version": "stub-15-02" }`** (literal `stub-15-02` for grep).

Use `NextResponse` from `next/server`. Import `isServerParseQueueEnabled` from `@/lib/serverParse/env`.
</action>

<read_first>
- `src/app/api/ai/vision-staging/route.ts` (NextResponse / route handler style)
- `src/lib/serverParse/env.ts`
- `src/types/parseJob.ts`
</read_first>

<acceptance_criteria>
- `src/app/api/parse-jobs/route.ts` exists.
- `rg "isServerParseQueueEnabled" src/app/api/parse-jobs/route.ts` returns at least one match.
- `rg "not_implemented" src/app/api/parse-jobs/route.ts` returns at least one match.
- `rg "enabled: false" src/app/api/parse-jobs/route.ts` returns at least one match (404 branch object literal).
- `rg "Production: add auth" src/app/api/parse-jobs/route.ts` returns at least one match.
- `npm run lint` exits 0.
</acceptance_criteria>

## Task 2 — `GET /api/parse-jobs/[id]`

<action>
Create `src/app/api/parse-jobs/[id]/route.ts` with the **same file-top JSDoc sentence** as Task 1:

`Production: add auth + rate limiting before enabling D2Q_SERVER_PARSE_ENABLED.`

Then implement:

- `export async function GET(_request: Request, context: { params: Promise<{ id: string }> })` (use Next 15 async `params` pattern — match other dynamic routes in repo if they use `Promise<{ id: string }>`).

Behavior:

- If `!isServerParseQueueEnabled()`: `404` + JSON `{ "enabled": false }`.
- If enabled: resolve `id` from `await context.params`; if `id` is empty string after trim, `400` with `{ "error": "missing_id" }`.
- Else return **501** JSON: `{ "error": "not_implemented", "enabled": true, "id": "<id>" }` where `<id>` is the requested id (echo only).

No other methods in this file.
</action>

<read_first>
- `src/app/api/ai/vision-staging/[id]/route.ts` (dynamic segment + NextResponse)
- `src/lib/serverParse/env.ts`
</read_first>

<acceptance_criteria>
- `src/app/api/parse-jobs/[id]/route.ts` exists.
- `rg "isServerParseQueueEnabled" src/app/api/parse-jobs/[id]/route.ts` returns at least one match.
- `rg "not_implemented" src/app/api/parse-jobs/[id]/route.ts` returns at least one match.
- `rg "Production: add auth" src/app/api/parse-jobs/[id]/route.ts` returns at least one match.
- `npm run lint` exits 0.
</acceptance_criteria>

## Verification (phase)

- `npm run lint` and `npm run build` exit 0.
- Optional curl (executor note): `POST /api/parse-jobs` with flag off → 404 body contains `enabled` and `false`.
