---
phase: 01-purge-parsing-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/**
  - src/types/**
  - src/hooks/**
  - src/proxy.ts
  - src/components/**
  - src/app/**
  - package.json
  - next.config.ts
autonomous: true
requirements:
  - PURGE-01
  - PURGE-02
  - PURGE-03
  - PURGE-04
  - PURGE-05
  - PURGE-06
  - PURGE-07
  - OOS-PDF-01
  - OOS-DOCX-01
  - OOS-OCR-01
  - OOS-VISION-01
  - OOS-GRAPHIFY-01
  - OOS-MEDIA-01
  - OOS-RESUME-01
user_setup: []
must_haves:
  truths:
    - "No PDF or DOCX upload UI remains in the app"
    - "No OCR or vision inspector UI remains"
    - "npm run build succeeds without imports from deleted parsing modules"
  artifacts:
    - "src/types/flashcard.ts exists with FlashcardVisionItem"
    - "src/components/edit/new/NewStudySetTextImportFlow.tsx wired on quiz/flashcards import"
    - ".planning/codebase/ removed"
  key_links:
    - "studySetDb uses simplified StudySetMeta without OCR fields"
---

<objective>
Purge v1 document parsing pipeline (PDF, DOCX, OCR, vision, graphify, resumable uploads) from `src/lib`, `src/types`, `src/hooks`, and related UI. Retain frontend shell and raw-text import stub. Remove dev backdoor. Update user-facing copy to text-only.
</objective>

# Tasks

1. Delete parsing directories: `src/lib/pdf`, `uploads`, `server/generateFromFile`, `serverParse`, `graphify-out`, upload APIs, parse-jobs, generate-from-file
2. Slim `src/types` — remove ocr/parse/upload/vision/canonical types; add `flashcard.ts`; simplify `studySet.ts` and `question.ts`
3. Keep only core `src/lib/ai` modules (forward, embeddings, title generation)
4. Simplify `studySetDb` — text-only documents, no media/parse progress
5. Remove ParseProgress/OCR/PDF components; wire `NewStudySetTextImportFlow`
6. Remove dev backdoor; fix auth to Supabase-only
7. Update dashboard/auth/layout copy from PDF to text
8. Verify `npm run typecheck` and `npm run build`
