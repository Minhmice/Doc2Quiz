"use client";

/**
 * Webpack dev can abort competing RSC requests while compiling route chunks.
 * ponytail: Restore route prefetching when dev navigation stays reliable.
 */
export function RoutePrefetch() {
  return null;
}
